# 交接说明：777 习题集建库（2026-09-24）

> [!abstract] 一句话状态
> **第 1 章已完整入库并推送（`ea5fa74`）**；第 2—8 章待录；**官方勘误表读取受阻**是当前唯一的硬阻塞。

## 一、已完成

| 项 | 内容 | commit |
| :-- | :-- | :-- |
| 第5章五处自编例题全部落到出处 | 例5.1→教材§5.1 图5-1/5-7；例5.2→习题5-3（厦门大学2012）；例5.3→教材§5.1 式(5-3)—(5-5)；例5.10→教材例5-2 数值化；例5.11→教材例5-9 | `a7146b7` |
| 777 目录页 | `控制理论/777习题集目录.md`（71 行，含进度表、页码对照、题源文件表） | `ea5fa74` |
| 777 第1章题目页 | `控制理论/777习题集/基础300题 第1章 自动控制的一般概念（题目）.md`（154 行，30 题） | `ea5fa74` |
| 777 第1章答案页 | `…（答案与解析）.md`（263 行，答案速查表 + 逐题解析） | `ea5fa74` |
| 4 张题图 | `附件/777-1-27…png`、`1-28…png`、`1-29…png`、`1-30…png` | `ea5fa74` |
| 入口挂载 | `控制理论/自动控制原理/自动控制原理.md` 第117行「附录与工具」加一行 | `ea5fa74` |

## 二、未完成（按优先级）

### P0 · 读官方勘误表（**硬阻塞**）

- 链接：`https://docs.qq.com/doc/DSVFReWxzU29ia1R5`
- 已试：**①** `tdoc_init` 返回 `READY`（鉴权 OK），但 `tdoc_schema` / `tdoc_list` 连续 3 次全部 `[SSL: UNEXPECTED_EOF_WHILE_READING]`；**②** `curl` 直连与走代理（`http://127.0.0.1:7897`）访问 `https://docs.qq.com/` **都返回 200**，网络是通的，是脚本侧 TLS 握手被中断；**③** `WebFetch` 能拿到标题「2027年《控制考研777习题集》勘误汇总」，但正文是 JS 渲染的空壳，抓不到条目。
- 三条出路（新窗口挑一条）：
  1. **最省事**：请用户在腾讯文档里「导出为 Markdown / PDF」或整篇复制粘贴发过来；
  2. 重试腾讯文档 MCP（换 `doc-mcp` endpoint，doc 类不走 `sheet-mcp`；也可先 `manage.query_file_info` 拿 file_id 再读）；
  3. 不改阻塞也能推进：**靠题目册与答案册互校**（目前就是这么做的），勘误表到了再回头批量修订。

### P1 · 777 基础300题 第 2—8 章录入

已录第1章，剩 7 章。页码（两册都是 **PDF 页 = 书内页 + 6**）：

| 章 | 题目册 PDF | 答案册 PDF |
| :-- | :-- | :-- |
| 2 控制系统的数学模型 | 15—35 | 14—32 |
| 3 线性系统的时域分析法 | 36—58 | 33—59 |
| 4 线性系统的根轨迹法 | 59—80 | 60—102 |
| 5 线性系统的频域分析法 | 81—102 | 103—136 |
| 6 线性系统的校正方法 | 103—125 | 137—175 |
| 7 线性离散系统的分析与校正 | 126—146 | 176—201 |
| 8 非线性控制系统分析 | 147—173 | 202—249 |

**第 5 章优先级最高**（本库第5章笔记最厚，且已登记 5-31 燕山大学2018 → 例5.30）。

**单章 SOP（照抄即可）：**

1. 渲染：`D:\miniconda3\python.exe .workbuddy\_pdf_render.py <pdf文件名关键词> <前缀> <起页> <止页> [dpi]`
   - 产物落在 `.workbuddy/_pdf/<前缀>_pNNN.jpg`，已存在会 skip。
   - 例：`_pdf_render.py 300题 c2 15 35`、`_pdf_render.py 答案册 a2 14 32`
2. 读题面：逐页 `Read` jpg。**⚠️ 本机 Read 只能读 jpg，读 PNG 一定报 "current model does not support images"**。
   - 缓存坑：出现「[Image unchanged]」「Same image content as previously loaded」时，**复制改名绕不过**，必须用更高 dpi 重渲染成新文件名（如 `c1_p013_hi.jpg` dpi=132）。
3. 建题目页 `基础300题 第N章 XXX（题目）.md`，`type: exam-paper`。
4. 建答案页 `…（答案与解析）.md`，`type: exam-solutions`：先「答案速查表」（两两并列），再逐题解析（含考点、难度★）。
5. **题图必补**（用户明确要求「题目里面有图的要一起补上图，截图自己画都行」）：
   - 有原图 → 用 `.workbuddy\_crop_fig.py` 裁；
   - 纯方框图 → 直接用 mermaid `flowchart LR` 手绘，比裁图干净且可维护（第1章 1-26~1-30 就是这么做的）。
6. 裁图流程（`_crop_fig.py`）：分数包围盒 → 按暗像素收紧 → 阈值 170 二值化去粉色水印 → 4 邻域去 <12px 孤立点 → 断言（框在包围盒内、不贴边、墨点比例 0.5%~60%）。
   - 定包围盒靠**行剖面**先列内容带（文字行 vs 图块），别目测；命令 `--list` / `--preview` / 指定 key。
   - 新增图要往 `FIGURES` 字典里加一条 `("源jpg", "输出png", (x0,y0,x1,y1), 阈值, 最小连通块)`。
   - 已知踩过的坑：右界不够会把元件符号切掉（1-30 的 $E_0$）、上沿不够会切掉小标题（1-28）、框大了会带上题干正文（1-27）。
7. 更新 `控制理论/777习题集目录.md` 进度表那一行。
8. 校验 + 提交（见第四节）。

### P2 · 另两册

- **自控强化 300 题**：题目册 289 页 / 答案册 282 页，未开工（偏大计算量、综合题、非常规题）。
- **现控 200 题**：`E:\BaiduNetdiskDownload\777现控200题➕答案\`，未开工。

### P3 · 777 题回灌进讲次笔记

被正式收进笔记的 777 题，要在对应章总览 **§⑤ 例题索引**登记落点（例：`05-4-e` 的例5.30 ＝ 本册 5-31 燕山大学2018）。
第 5 章总览已列**备用题清单**：5-2/4/5、5-10/5-12、5-11/5-14/5-15、5-21、5-24/5-34、5-27/5-35、5-40。

### P4 · 遗留：99 张图标注遮挡清理

- 约定「**标注不许贴曲线放**」：白底 bbox 压在曲线上＝把曲线咬掉一块。
- 99 张图里 **72 张**有同类遮挡待清（14 张单条 ≥100 采样点；最重 `nyquist-05-2-c` 889 点、`freq-same-mag-diff-phase` 618 点）。
- 工具：`_check_fig_layout.py`（查 `[压字]`/`[越界]`/`[压线]`，目标 0 处）、`_probe_layout.py`（先量再改落点）。

### P5 · 其他

- 工作区有 **7 处未提交改动**（数学区错题本 6 篇 + 高数 `15-2-1` 3 行），**非 777 主线**，另有 5 个文件是行尾幽灵（numstat 0/0）。新窗口别顺手 `git add -A`。
- 第 5 章笔记 `05 第5章 线性系统的频域分析法.md` 现 232 行，贴 250 上限，别再加内容。

## 三、资料清单

| 用途 | 路径 |
| :-- | :-- |
| 基础300题 题目册 | `E:\BaiduNetdiskDownload\777真诚27考研习题集\真橙控制自动化 777习题集300题.pdf`（178 页） |
| 基础300题 答案册 | 同目录 `…自控基础300题【答案册】.pdf`（249 页） |
| 强化300题 | 同目录 `…自控强化300题【题目册】.pdf`（289 页）/ `【答案册】.pdf`（282 页） |
| 现控200题 | `E:\BaiduNetdiskDownload\777现控200题➕答案\` |
| 教材 OCR 分章文本 | `D:\obsidian\教材OCR\分章\自控-第0N章.txt`（现控/线代/高数各讲也在），grep 例题号最快 |
| 已渲染页图 | `D:\obsidian\.workbuddy\_pdf\`（55 个：`base_p001-014` 第1章、`base_p081-102` 第5章、`ans_p001-016`、`c1_p013_hi/014_hi`） |
| 成图 | `D:\obsidian\附件\`（777 题图命名 `777-<章>-<题号>-<名>.png`） |

## 四、体例与提交约定（777 建库 spec）

- 文件名：`基础300题 第N章 XXX（题目）.md` / `…（答案与解析）.md`，放 `控制理论\777习题集\`。
- frontmatter：`create` / `modify` / `tags: [777习题集, 考研真题, 自动控制原理]` / `book: 自控基础300题` / `chapter: N` / `type: exam-paper`（题目）或 `exam-solutions`（答案）。
- 题号沿用原书 `章-序号`；**与教材习题是两套编号**（教材第5章 5-1—5-29，本册第5章 5-1—5-40），引用一律带院校年份。
- 题面里**原题插图必须一起入库**；方框图可用 mermaid。
- 答案页：答案速查表 → 逐题解析；题册与答案册不一致处用 **⚠️** 标出并「以题目册题面为准」（已发现：1-3 答案错位、`苏州大学 2025` 归属 题册挂 1-27 / 答案册挂 1-26）。
- **写完必须二进制转 CRLF**（Write 出来是裸 LF，本库 `core.autocrlf=true`）。
- 双校验：`python .scripts\check-math-format.py --path 控制理论`（渲染级，基线 0）、PowerShell `& .scripts\check-notes.ps1 *>` 落盘再读（PowerShell 不回显 stdout；bash 里禁止 `powershell.exe -Command`）。
- 提交信息写 `.workbuddy/_msgNN.txt` 再 `git commit -F`；**只 `git add` 自己的路径，禁 `add -A`**；agent 环境**一律走 SSH**（PortableGit 没有凭据助手，HTTPS 必失败）。
- 收尾更新 `.workbuddy\memory\YYYY-MM-DD.md`。
