# 2026-09-18
- 检查并修订 `控制理论\自动控制原理\05-4-c 例题·虚轴零点与奈氏判据.md`（例5.14，虚轴零点 $G=k(s^2+1)/[s(s+5)]$，结论 $k>0$）。改掉 4 处表述错误：①「整支曲线都在实轴下方（Im G<0 对一切 ω>0）」不成立——ω>1 时 Im G>0，曲线进第一象限，与该文自己的表格、配图矛盾；②终点 $(k,\mathrm j0)$ 是**自上方**逼近（Im G→0⁺），原文写「自下方」；③「窄带 $Re G>-k/25$ ⟹ 判据点不可能被包围」只在 $k<25$ 时成立（$k>25$ 时曲线左端越过 $-1$），改写为用穿越计数：实轴交点只有原点 ⟹ $(-\infty,-1)$ 段无穿越 ⟹ $N=0$，与 k 无关（数值算环绕数 k=0.5/2/10/25/30/100/1000 均为 0，Routh 复核 $k>0$ 稳定，两者一致）；④ warning 标题「曲线过原点不等于幅值为零」与正文自相矛盾，前言「缺口由 $1/\omega$ 因子补上」也不是理由（真因是分子在 ω=1 归零）。校验脚本 .scripts\check-notes.ps1 通过。
- 遗留待用户确认：该篇把题目标注为〔习题5-16〕，但教材（胡寿松第八版）习题5-16 是 $G(s)=K/[s(Ts+1)(s+1)]$（求 K、T 范围，PDF p258 / 书内 250 页），并非本题；库内 52 个 PDF（含 825 习题册）也搜不到本题与提示「arctan0.2=11.3°」，出处待定。该标注还作为 wikilink 别名散在 05 第5章索引、05-4-a（2 处）、05-4-b、05-4-1（2 处）共 6 处，改标要一起改。
- 另一处跨文件小不一致（本次未动）：05-4-c 与 05-4-b 把 05-4-a 的内容概括为含「开环不稳定」，但 05-4-a 实际收的是**结构不稳定**（$K/[s^2(Ts+1)]$，P=0），两个概念不是一回事。
- 经验：教材 PDF（第八版）文字层与 OCR 同样把公式打散（\uf02b 之类），搜公式要换思路——优先搜**提示语里的数字**（如 arctan 0.2 → 搜 "11.3"）或直接渲染页面看图；渲染命令：`D:\miniconda3\python.exe` + PyMuPDF，PDF 页 = 书内页 + 8。
- **用户随后澄清（问题关闭）**：05-4-c 的例5.14 是**用户自己习题册上的题**，不在教材/笔记体系里，是他单独补进笔记的；标注〔习题5-16〕指**习题册编号**，题目本身用户已核对无误。以后再看到这个标注不必拿教材第八版习题5-16（`G(s)=K/[s(Ts+1)(s+1)]`）去对号。
- 顺手修了本机 Bash 工具环境（详见 `~/.workbuddy/MEMORY.md`「本机 WorkBuddy Bash 工具环境」）：宿主给的 PATH 缺 Git 的 `usr\bin`，导致 `ls/cat/head/dirname` 全不可用、shim 每次报错；已往用户级 PATH 末尾补两条 coreutils 路径（重启 WorkBuddy 生效，**15:55 重启后已验证**：`ls/cat/head/sed/grep/awk/xargs` 全可用、stderr 无 shim 报错、Windows 的 find/sort/tar 与 `rm` 安全包装均未受影响），旧 PATH 备份在 `C:\Users\23720\.workbuddy\tmp\shim-backup-20260918\`。
- 开发工具链升级（用户只授权工具链、应用软件不动）：**uv 0.12.16、gh 2.101.0、CMake 4.4.3（用户 PATH 条目已原地换新）、Go 1.27.0（归档 15636 文件全齐，`go run`/`go build` 含 crypto/json/http 验证通过）、TeX Live 2026 更新 13 个包**；TeX 更新后 xelatex 编译 circuitikz+中文 冒烟测试通过。系统 Git 2.53→2.55 待用户跑官方安装包（`D:\Program Files\Git` 需提权，安装包已放 `C:\Users\23720\Downloads\`）。细节与教训见 `~/.workbuddy/MEMORY.md`。
- 整理用户发来的课程截图（§5.3.3「由对数幅频特性曲线确定传递函数」例4，Ⅱ 型带一阶微分＋振荡环节）：在 `05-2-b 由伯德图反求传函` 新增 **§5.11.4 与例5.26**，核心是低频渐近线**延长线**与 0 dB 线的交点 $\omega_0=K^{1/\nu}$、Ⅱ 型时 $K=\omega_0^2=\omega_1\omega_c$（$\omega_0$ 是 $\omega_1$ 与 $\omega_c$ 的对数中点）；幻灯片三解法里 II/III 是同一结论的两个入口，已按此归并。新配图 `频域-反求-Ⅱ型Bode.png`（源码 `.scripts\figures\freq-inverse-type2-omega0.py`）。章索引 §①§②§⑤、05-2 导航、05-2-1 反向应用、`附录 频域分析速查` 同步；校验 0 致命 0 待修；commit `3f872f9` 已推。
- 教材核对：反求的课本依据是 §5-2.4 **式(5-55)—(5-57) 方法一/二/三**（书内215—216 页，PDF223—224），方法三即 $\omega_0=K^{1/\nu}$——用户说的"方法二三是这俩"指的就是这两式。
- 顺手更正 `12 资料索引（OneDrive）`：原写"资料根目录 `OneDrive\自动控制原理\`"，实际是 `OneDrive\按章节-原视频和PPT\` 与 `OneDrive\Word-补充自O-God\` 两处（用户给出路径）。
- 工具有坑：**PowerPoint COM 在本机打不开任何 .ppt**（`Presentations.Open` 恒定 `Unexpected HRESULT`，换整数参、转 MsoTriState、复制到 `%TEMP%` 都不行）；改用原始字节扫 UTF-16LE 命中率很好——据此确认绘制方法在 `控制88-20`、反求同型题在 `控制88-19 例1` 与 `控制88-23 例3`（后者是"反求→由 γ 判稳→L(ω) 右移十倍频"的综合变体）。Word COM 转 txt 仍可用（公式是图片，只剩文字骨架）。
- 遗留判断（已答复用户）：反求专题主线齐了，仍缺 ① 用相频补辨识纯延迟 τ／右半平面零点的实操小节（现在只有"必须最小相位"的结论）；② 一张"题给什么→走哪条入口"的总表。另：`05-2-b` 已 249 行，再加内容须先拆。
- **第5章"找不到"问题**（用户主诉）：章页只有"按任务"入口，没有"按讲次/术语"入口。已新建 **`05-0 频域检索表（课件讲次·术语·缺口）`**（75 行）：控制88-17~28 讲次对照 + 术语→篇 + 术语换算（迟后=滞后、相角交界频率=ω_x、等N圆=等α圆、交接频率=转折频率）+ 缺口清单；章页摘要行指向它，`自动控制原理.md` 第5章条目同步。
- **缺口核对结论**（课件 vs 教材 vs 笔记，逐条对齐）：
  - **教材 §5-1—§5-6 逐小节都已覆盖**（章页 §④ 表），课本侧无缺口。
  - **课件侧 4 处"方法在、整题没连起来"**：① 由相频 φ(ω) 反求 G(s)（§5.3.3 例6，K 不影响 φ）→ 要点补进 `05-2-1` §5.3.1；② 反求后回画 φ(ω) 与 G(jω)（例5）；③ L(ω) 左右平移对 ω_c/γ/σ%/t_s 的影响（§5.6 例3）→ 结论补进 `05-5-3` §5.9；④ 由开环幅相曲线形状反求（§5.2.1 例3/例5）。
  - 另有 §5.4.2「闭环系统超稳定？」、§5.8 例3 笔录仪带宽两处课堂讨论未单独收录（后者与 `05-5-1` §5.6.3 带宽选择同源）。
- 技术收获：**旧 .ppt 文本用"记录头法"抽最干净**——`data.find(b'\x00\x00\xa0\x0f')`（TextCharsAtom）后 4 字节小端长度切片解 UTF-16LE（TextBytesAtom 是 `\xa8\x0f`，按 GBK 解）；12 份 PPT 得 2238 行文本，而整文件 UTF-16 盲扫会得 59 万行垃圾。已把这条与"PowerPoint COM 不可用"一起写进 `AGENTS.md`。
