# 2026-09-18
- 检查并修订 `控制理论\自动控制原理\05-4-c 例题·虚轴零点与奈氏判据.md`（例5.14，虚轴零点 $G=k(s^2+1)/[s(s+5)]$，结论 $k>0$）。改掉 4 处表述错误：①「整支曲线都在实轴下方（Im G<0 对一切 ω>0）」不成立——ω>1 时 Im G>0，曲线进第一象限，与该文自己的表格、配图矛盾；②终点 $(k,\mathrm j0)$ 是**自上方**逼近（Im G→0⁺），原文写「自下方」；③「窄带 $Re G>-k/25$ ⟹ 判据点不可能被包围」只在 $k<25$ 时成立（$k>25$ 时曲线左端越过 $-1$），改写为用穿越计数：实轴交点只有原点 ⟹ $(-\infty,-1)$ 段无穿越 ⟹ $N=0$，与 k 无关（数值算环绕数 k=0.5/2/10/25/30/100/1000 均为 0，Routh 复核 $k>0$ 稳定，两者一致）；④ warning 标题「曲线过原点不等于幅值为零」与正文自相矛盾，前言「缺口由 $1/\omega$ 因子补上」也不是理由（真因是分子在 ω=1 归零）。校验脚本 .scripts\check-notes.ps1 通过。
- 遗留待用户确认：该篇把题目标注为〔习题5-16〕，但教材（胡寿松第八版）习题5-16 是 $G(s)=K/[s(Ts+1)(s+1)]$（求 K、T 范围，PDF p258 / 书内 250 页），并非本题；库内 52 个 PDF（含 825 习题册）也搜不到本题与提示「arctan0.2=11.3°」，出处待定。该标注还作为 wikilink 别名散在 05 第5章索引、05-4-a（2 处）、05-4-b、05-4-1（2 处）共 6 处，改标要一起改。
- 另一处跨文件小不一致（本次未动）：05-4-c 与 05-4-b 把 05-4-a 的内容概括为含「开环不稳定」，但 05-4-a 实际收的是**结构不稳定**（$K/[s^2(Ts+1)]$，P=0），两个概念不是一回事。
- 经验：教材 PDF（第八版）文字层与 OCR 同样把公式打散（\uf02b 之类），搜公式要换思路——优先搜**提示语里的数字**（如 arctan 0.2 → 搜 "11.3"）或直接渲染页面看图；渲染命令：`D:\miniconda3\python.exe` + PyMuPDF，PDF 页 = 书内页 + 8。
- **用户随后澄清（问题关闭）**：05-4-c 的例5.14 是**用户自己习题册上的题**，不在教材/笔记体系里，是他单独补进笔记的；标注〔习题5-16〕指**习题册编号**，题目本身用户已核对无误。以后再看到这个标注不必拿教材第八版习题5-16（`G(s)=K/[s(Ts+1)(s+1)]`）去对号。
- 顺手修了本机 Bash 工具环境（详见 `~/.workbuddy/MEMORY.md`「本机 WorkBuddy Bash 工具环境」）：宿主给的 PATH 缺 Git 的 `usr\bin`，导致 `ls/cat/head/dirname` 全不可用、shim 每次报错；已往用户级 PATH 末尾补两条 coreutils 路径（重启 WorkBuddy 生效，**15:55 重启后已验证**：`ls/cat/head/sed/grep/awk/xargs` 全可用、stderr 无 shim 报错、Windows 的 find/sort/tar 与 `rm` 安全包装均未受影响），旧 PATH 备份在 `C:\Users\23720\.workbuddy\tmp\shim-backup-20260918\`。
- 开发工具链升级（用户只授权工具链、应用软件不动）：**uv 0.12.16、gh 2.101.0、CMake 4.4.3（用户 PATH 条目已原地换新）、Go 1.27.0（归档 15636 文件全齐，`go run`/`go build` 含 crypto/json/http 验证通过）、TeX Live 2026 更新 13 个包**；TeX 更新后 xelatex 编译 circuitikz+中文 冒烟测试通过。系统 Git 2.53→2.55 待用户跑官方安装包（`D:\Program Files\Git` 需提权，安装包已放 `C:\Users\23720\Downloads\`）。细节与教训见 `~/.workbuddy/MEMORY.md`。
- 整理用户发来的课程截图（§5.3.3「由对数幅频特性曲线确定传递函数」例4，Ⅱ 型带一阶微分＋振荡环节）：在 `05-2-b 由伯德图反求传函` 新增 **§5.11.4 与例5.26**，核心是低频渐近线**延长线**与 0 dB 线的交点 $\omega_0=K^{1/\nu}$、Ⅱ 型时 $K=\omega_0^2=\omega_1\omega_c$（$\omega_0$ 是 $\omega_1$ 与 $\omega_c$ 的对数中点）；幻灯片三解法里 II/III 是同一结论的两个入口，已按此归并。新配图 `频域-反求-Ⅱ型Bode.png`（源码 `.scripts\figures\freq-inverse-type2-omega0.py`）。章索引 §①§②§⑤、05-2 导航、05-2-1 反向应用、`附录 频域分析速查` 同步；校验 0 致命 0 待修；commit `3f872f9` 已推。
- 教材核对：反求的课本依据是 §5-2.4 **式(5-55)—(5-57) 方法一/二/三**（书内215—216 页，PDF223—224），方法三即 $\omega_0=K^{1/\nu}$——用户说的"方法二三是这俩"指的就是这两式。
- 顺手更正 `12 资料索引（OneDrive）`：原写"资料根目录 `OneDrive\自动控制原理\`"，实际是 `OneDrive\按章节-原视频和PPT\` 与 `OneDrive\Word-补充自O-God\` 两处（用户给出路径）。
- 工具有坑：**PowerPoint COM 在本机打不开任何 .ppt**（`Presentations.Open` 恒定 `Unexpected HRESULT`，换整数参、转 MsoTriState、复制到 `%TEMP%` 都不行）；改用原始字节扫 UTF-16LE 命中率很好——据此确认绘制方法在 `控制88-20`、反求同型题在 `控制88-19 例1` 与 `控制88-23 例3`（后者是"反求→由 γ 判稳→L(ω) 右移十倍频"的综合变体）。Word COM 转 txt 仍可用（公式是图片，只剩文字骨架）。
- 遗留判断（已答复用户）：反求专题主线齐了，仍缺 ① 用相频补辨识纯延迟 τ／右半平面零点的实操小节（现在只有"必须最小相位"的结论）；② 一张"题给什么→走哪条入口"的总表。另：`05-2-b` 已 249 行，再加内容须先拆。
- **第5章"找不到"问题**（用户主诉）：章页只有"按任务"入口，没有"按讲次/术语"入口。已新建 **`05-0 频域检索表（课件讲次·术语·缺口）`**（75 行）：控制88-17~28 讲次对照 + 术语→篇 + 术语换算（迟后=滞后、相角交界频率=ω_x、等N圆=等α圆、交接频率=转折频率）+ 缺口清单；章页摘要行指向它，`自动控制原理.md` 第5章条目同步。
- **缺口核对结论**（课件 vs 教材 vs 笔记，逐条对齐）：
  - **教材 §5-1—§5-6 逐小节都已覆盖**（章页 §④ 表），课本侧无缺口。
  - **课件侧 4 处"方法在、整题没连起来"**：① 由相频 φ(ω) 反求 G(s)（§5.3.3 例6，K 不影响 φ）→ 要点补进 `05-2-1` §5.3.1；② 反求后回画 φ(ω) 与 G(jω)（例5）；③ L(ω) 左右平移对 ω_c/γ/σ%/t_s 的影响（§5.6 例3）→ 结论补进 `05-5-3` §5.9；④ 由开环幅相曲线形状反求（§5.2.1 例3/例5）。
  - 另有 §5.4.2「闭环系统超稳定？」、§5.8 例3 笔录仪带宽两处课堂讨论未单独收录（后者与 `05-5-1` §5.6.3 带宽选择同源）。
- 技术收获：**旧 .ppt 文本用"记录头法"抽最干净**——`data.find(b'\x00\x00\xa0\x0f')`（TextCharsAtom）后 4 字节小端长度切片解 UTF-16LE（TextBytesAtom 是 `\xa8\x0f`，按 GBK 解）；12 份 PPT 得 2238 行文本，而整文件 UTF-16 盲扫会得 59 万行垃圾。已把这条与"PowerPoint COM 不可用"一起写进 `AGENTS.md`。
- ⚠️ **事故：`控制理论/自动控制原理/` 整个目录（122 个 md）在 16:4x 前后从磁盘消失**，`OneDrive\Apps\remotely-save\obsidian\控制理论\` 云端镜像里当时也只剩「现代控制理论／青岛大学825真题」。判断是**外部客户端广播删除**（同 2026-09-17 附件目录被清那次），不是本机操作——我那一轮只执行了 `git rm` 单个文件。已 `git checkout -- "控制理论/自动控制原理"` 完整恢复（122 文件，逐字节来自 HEAD），随后正常提交推送。**教训：动库结构前先停非作者端同步；发现目录消失先别慌，`git status` 报 " D" = 工作区删除、索引还在，`git checkout -- <目录>` 即可整体还原。**
- **第5章总览重排（用户要求"不要另开 5.0 页，把总览弄好"）**：`05 第5章` 变为唯一入口——§① 「找东西」（课件讲次表 控制88-17~28 + 术语/任务表 + 课件缺口清单）、§② 学习路线压成一张 8 行总表、§③ 对象、§④ 教材对照、§⑤ 例题表、§⑥ 套路；218 行（原 250 行上限告急）。删除独立的 `05-0 频域检索表` 页。
- **伯德族按「方法篇 / 例题篇」重组**（commit `b88e9f5`）：`05-2-1 开环伯德图（画法·相频·修正）`（原 05-2-1 + 原 05-2-a 的修正判据/三条自检）＝唯一画法方法篇；`05-2-b 由伯德图反求（方法与口径）`＝反求方法篇（三要素/三种入口/三类定式/前提）；`05-2-d 伯德图绘图例题`＝例5.9 + 例5.25；`05-2-e 伯德图反求例题`＝例5.23 + 例5.24 + 例5.26；`05-2-c` 自测保留。全库 40 处 wikilink 用脚本同步（两趟：先改目标名，再统一改写 05-2-1 新名），校验 0 致命 0 待修。
- 奈氏族本来就"方法在前、例题在后"（05-3-1/2/3/8/9 知识点 + 05-3-4~7 例题），只把 `05-3 导航页` 标签改成「知识点·画法／知识点·辨析与速查／例题（4 道）／知识点·与伯德图对读」（commit `cba5793`）。判据族（05-4-1/2/3 + 05-4-a~d）与综合族（05-5-1/2/3 + 05-5-b/b-1/b-2）同样是方法在前、例题在后，未动。
- **本轮有个"行尾符假改动"坑又出现了**：`git status` 报 05-2-c／05-3-3／05-3-4／05-3-5／05-3-a 为 M，但 `git diff` 空——纯 CRLF/LF 差异，`git restore` 归一即可，不要当成用户编辑提交。
- **用户批评："不要只改文件名，文件内容呢"** —— 奈氏族第一版我只改了导航标签与文件名，被当场指出。真合并版（commit `2de9a08`）：五篇知识点删到 2 篇（05-3-1 幅相曲线的画法与最小相位 247 行 + 附录 幅相曲线速查 489 行），例5.4 从 05-3-1 移入 05-3-4 并提到例5.5 之前，全族 5.3.2.x 用占位符两趟法重排。教训：**重构类任务每一轮都要落到内容移动，别停在标题层面**。
- **Python 写文件的行尾坑（本轮加重了）**：`io.open(..., newline='')` + `'\n'.join()` 会把原本 CRLF 的文件写成纯 LF；仓库 `core.autocrlf=true`，虽不影响 diff（归一后相同），但 git 会刷屏 "LF will be replaced by CRLF"。**收尾要跑一次「纯 LF → CRLF」归一**（判据：`b.count(b'\r\n')==0` 才转），本轮一次归一了 控制理论 下 102 个 md。
- **目录删除事故当晚第二次发生**：脚本执行 `git rm` 五个已合并旧篇后，同步插件立刻按云端状态把整个目录清掉。应对顺序固定为：**① 新篇先单独提交（纯新增，不含删除）；② 本地删旧篇后立刻 `ls | wc -l` 复查；③ 同步 cloud mirror 目录到目标状态（cp 新篇 + rm 旧篇），让双向同步不再拉偏**。
- **用户："可以适当补充图" + "ppt 的图就很清晰啊"** → 从课件抽原图入库。做法：裸扫 `.ppt` 的 PNG/JPEG 签名（不解 OLE）→ 导出 → 算墨量排序 + 拼联系表筛选（`%TEMP%` 下 `ppt_scan.py` / `montage2.py`）→ 挑中的存 `附件\自控-频域-*.png`。第5章 12 份 PPT 里真插图很少，多是公式位图与空白坐标纸；真正好用的是 `控制88-20` 的 Bode 折线示例。
- **本轮补了 4 张图**（commit `67ed055`）：自绘 `频域-反求-低频延长线与ω0.png`（05-2-b §5.11.2）、自绘 `频域-幅相-四种型别对照.png`（05-3-4 例5.4）、课件 `自控-频域-反求延长线与K.png`（05-2-b）、课件 `自控-频域-Bode折线与相频.png`（05-2-1）。两个自绘源码 `freq-inverse-omega0-convention.py`、`type-family-compare.py` 入库，README 补了"从课件 PPT 取原图"一节。
- **matplotlib 图内中文又踩一次**：含 `$…$` 的字符串不能混中文（mathtext 接管整串 → 方框）；含中文的字符串不要用 Unicode 下标 `₀`（YaHei/SimHei 缺 U+2080），直接写 `ω0`。希腊字母（ν、ω、Ⅰ 型）用 Unicode 字符走纯文本反而正常。
- **奈氏族的 `5.3.2.x` 是跨文件连号**：05-3-1(.1) → 附录典型曲线(.2) → 例5.4(.3) → 例5.5(.4) → … → 附录双惯性(.9)。插新题用占位符两趟法整体顺排，别只改一篇。
- **用户纠正："三方法可以算知识点吧，不要放到例题里面"** → 05-2-b §5.11.2 重写（commit `32e17b4`）：低频渐近线 $L_a=20\lg K-20\nu\lg\omega$ 一句话引出，三种取法列成表（取哪个点/公式/什么时候最好用），补 $\nu=0$ 没有方法三入口、三种都要先延长低频直线。**教训：知识点不能靠"前两种见 XX 篇""推演见例题篇"推给别人**——方法篇必须自足，例题篇只做对照演练。术语统一"三种入口 → 三种取法"。
- **05-2-1 画法篇格式整理**（commit `0251f31`）：六步 ①—⑥ 串写 → 真编号列表（第 6 步三条自检降为子列表，不再混用 ①②③）；几何法把公式里的中文注释移出公式、列表内嵌 `$$` 缩进 3 空格对齐；"顺手能读的三个量 + 两张图"收进 5.3.1 末尾的**粗体小标题**下（不新开小节——伯德族 5.3.x 与奈氏族 5.3.2.x 编号空间重叠，新开号会撞）；修了「见 §5.3.3」的断链（本篇无此节 → 指向 05-3-1）。
- **⚠️ 本次出现的 git 异常**：`.git/refs/remotes/origin/` 整个目录消失，`git status -sb` 报 `main...origin/main [gone]`，`git branch --set-upstream-to` 也失败；但 `git ls-remote` / `git push` 正常（远程 main = 本地 HEAD）。已手工写 `branch.main.remote/merge` 配置。**判断与之前"目录被同步插件清空"同源**，暂不影响用，但要留意。
- **课件 §5.3.4「最小相角系统和非最小相角」已核对并补全**（commit `c526b49`，已 push）：用户贴课件截图问「这部分内容有吗」。① 文字部分本库早有——`05-3-1` §5.3.3.1 收了最小/非最小相位定义、五种非最小相位环节表、互为倒数、镜像图、同幅不同相、判定分类三条与两条 warning，课件那几条 ★（非最小相角未必不稳定 / 由 L(ω) 不能唯一确定 G(s) / 相角变化绝对值更大）都能对上。② **缺的那块是课件四行表**：Ⅰ 型一个零点一个极点，零点与极点各分左右半平面，四种组合的 K_v 符号翻转、起点在负虚轴与正虚轴之间翻半圈。已补进 `附录 幅相曲线速查` §5.3.2.8 的「起点方向还会翻半圈：看 $K_v$ 的符号」，并说明**尾一形 (1+τs)/(1+Ts) 加 K>0 时 K_v 恒正、「起点只由 ν 决定」才成立；首一形 (s/ω±1) 的负号才是翻转来源**——这是原来「起点只由 ν 决定，与零点无关」那句话的口径前提，之前没写出来。③ 新图 `附件\频域-幅频相同相频不同.png`（脚本 `freq-same-mag-diff-phase.py`）：同一张 L(ω)、四条不同 φ(ω)，第 4 行按课件口径把 +90°→+270° 写成 −270°→−90°（np.unwrap 后减 360）。
- 踩坑回顾（又被咬一次）：图内**中文与 $…$ 混在一条字符串**→mathtext 接管整串、汉字掉字形；汉字必须包进 \mathrm{}，或整串改纯文本。图例放 `loc="lower left"` 会压住曲线，需把 ylim 下探到 −400 留白给图例。
- **课件例7（振荡环节反求）已补为 例5.27**（commit `afe85e6`，已 push）：用户又贴了一张课件截图（§5.3.2/§5.3.4 系列，控制88-20 末页）问「还有这个」。核对结果：**知识点在、整题不在**——`05-1-2-b` 94—98 行早有「$\omega=\omega_n$ 处 $\varphi=-90°$、$A=K/(2\zeta)$、曲线穿负虚轴」，`05-3-a` 23 行与 `附录 频域分析速查` 171 行也各有一处，但「由时域稳态输出 + 相位反求」的整题全库没有（grep $150$、$K=1.5$、$5/\pi$ 均无）。已新建 `05-1-2-c 例题·振荡环节参数反求.md`（115 行，`single-exercise: true`）：$f=5/\pi$ Hz → $\omega=10$；$-90°$ 钉 $\omega_n=10$；$1.5/(2\zeta)=3$ → $\zeta=0.25$；$G=150/(s^2+5s+100)$。三条自检（$2\zeta\omega_n=5$、$|G(0)|=K=1.5$、$\zeta<1/\sqrt2$ 时 $A_{\max}=K M_r\approx3.10>$ 3 且峰位 $\omega_r\approx9.35<\omega_n$）+ 三个易错点（$f$ 当 $\omega$、拿 $-90°$ 解 $\zeta$、漏掉 $K$ 在分子上会得 $\zeta=1/6$）+ 与 05-2-e 例5.24 的「峰定 $\zeta$、$-90°$ 定 $\omega_n$」分工对照。\n- 索引侧同步：05 第5章 §⑤ 加 例5.27 行（A 块）、术语表与课件讲次表 20 加入口、§① 缺口清单补一行「讲次 20 末页例7 已补」（原表列 4 处缺口时漏了它）；05-1 分章导航、05-1-2、05-1-2-b、05-1-1-b-1 各加交叉链接。章页 218 → 222 行，仍 <250。\n- 新图 `附件\频域-例527-稳态输出.png`（`ch5-ex527-osc-steady.py`）：$r(t)$ 幅值 1、$c_s(t)$ 幅值 3，输出滞后 $T/4$，峰值间画双向箭头标注。**又被中英混排坑一次**（$\Delta t=T/4$，即滞后 $90°$ 这种串）——汉字必须包进 \mathrm{}。短图（height 3.6）要 `fig.tight_layout()` 否则 xlabel 被裁。\n- 校验：`check-notes.ps1 -Dir 控制理论\自动控制原理` 121 文件、0 致命、0 待修。\n
- **错题本新篇：2000 数二第4题 斜渐近线截距漏项**（commit `a63c9b6`，已 push）。用户贴 AI 分析（$y=(2x-1)e^{1/x}$ 的斜渐近线，答案 $y=2x+1$）：错在算 $b$ 时把 $(1-1/(2x))$ 先当成 1，$\infty\cdot0$ 型里对局部取极限，漏 $-\mathrm e^{1/x}\to-1$。按 `templates\错题模板` 格式建 `数学\错题本\2000-4 斜渐近线截距漏项.md`（135 行）：原题 → 我的错解 → 正确解法（整体乘开 / 泰勒展开两条路）→ 同类与变式 → 复习记录。字段：`year 2000 / no 4 / kind 填空 / topic 一元函数微分学 / cause 概念不清 / mastery 1 / review 09-18·09-19·09-21·09-25·10-18`（+1/+3/+7/+30 相对建篇日）。挂链接：05-2（渐近线求法）、05-3 例题5、01-3 §1.8（四则运算前提）、01-4-1（替换陷阱·例题B）。解析页第4题答案下补「> 错题：」一行，总览手写索引加一行。\n- 判断 `cause` 时取了**概念不清**而非「计算出错」：根子是极限四则运算要求各部分极限存在且有限（$\infty\cdot0$ 不能拆），不是算错数。若用户想改标「计算出错」一句话即可。\n- 顺带确认：`数学\错题本` 目前只有 2000-2、2000-4 两篇；`2000-2` 有用户自己的未提交改动，本次未动。\n
- **错题本新篇：2000 数二第5题 求逆凑单位阵**（commit `ca16d40`，已 push）。用户一句「没看出来可以直接 e+b 用 e 凑出来一个」→ 建 `数学\错题本\2000-5 求逆凑单位阵.md`（136 行）。核心：$B=(E+A)^{-1}(E-A)$ 求 $(E+B)^{-1}$ 时，**把 $E$ 拆成 $(E+A)^{-1}(E+A)$ 提左公因子**，$E+B=2(E+A)^{-1}$ ⟹ $(E+B)^{-1}=\frac12(E+A)$。五步：拆 E → 提左因子 → 穿脱原则取逆 → 代回 A → 可逆性检查（$|E+A|=2\cdot4\cdot6\cdot8=384$）。口诀写进防坑：**提左因子写 $M^{-1}M$、提右因子写 $MM^{-1}$**（数值相等但能不能提出来只认写法）。`cause: 方法不会`（不是计算出错——没掌握这一招），`topic: 行列式与矩阵运算`（对齐目录专题九）。挂链 02-1（定义凑/穿脱原则/和差化积）、02-2、02-4、目录 9.3 里的 2002.19（同招解答题版）。\n- 用户报错题的口径在变：从「贴 AI 分析全文」变成**一句话说卡点**（如本条）。建篇时仍需自己补齐原题与标准解（库里有整卷解析），并判断 cause/挂链。\n
- **错题本新篇：2000 数二第6题 连续性排除分母零点**（commit `c0cc7a3`，已 push）。用户「做对了取的 a 等于零，没想到 a<0 有零点的问题」→ 建 `数学\错题本\2000-6 连续性排除分母零点.md`（119 行）。核心：**选对了 D 但理由不完备**——只验证 $a=0$ 成立就收工，没排除 $a<0$。补上真正的排除理由：$b\ne0$ 时 $\mathrm e^{bx}$ 值域是 $(0,+\infty)$，$a<0$ 则 $-a$ 落在值域内，$x_0=\ln(-a)/b$ 处分母为零 ⟹ 谈不上「在 ℝ 内连续」。三步解法（定 b → 定 a → 验证充分性），防坑强调「分式在 ℝ 上连续 ⟺ 分母恒不为零」与「选择题要逐项排除，不能靠试出一个能用的」。`cause: 概念不清`，`topic: 函数、极限与连续`（对齐目录专题一）。挂链 01-5 §1.16（连续÷连续要求分母≠0）、01-2、01-1、目录 1.1 的 2001.6。\n- **用户在 Obsidian 里自己完成了三处联动**（commit `72edec9`）：2000-2 次日、2000-4/5 当天重做——勾框由 Tasks 自动补 ✅ 日期、$review$ 里当日日期已删。已代为提交推送。注意 Obsidian 属性面板把 2000-2 的 YAML 内联数组改成了块列表（无害 churn）。\n- 建错题篇的固定套路（已跑通 2000-4/5/6 三篇）：从 `YYYY年考研数学二答案与解析.md` 取原题与标准解 → 套 `templates\错题模板.md` → `cause` 只标主因 → 解析页该题答案下补 `> 错题：` 一行 → 总览手写索引加一行 → 跑 `check-notes.ps1 -Dir 数学\错题本` → commit+push。**review 一律排 当天/+1/+3/+7/+30（相对建篇日）**。\n