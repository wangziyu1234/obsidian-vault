# 2026-09-18
- 检查并修订 `控制理论\自动控制原理\05-4-c 例题·虚轴零点与奈氏判据.md`（例5.14，虚轴零点 $G=k(s^2+1)/[s(s+5)]$，结论 $k>0$）。改掉 4 处表述错误：①「整支曲线都在实轴下方（Im G<0 对一切 ω>0）」不成立——ω>1 时 Im G>0，曲线进第一象限，与该文自己的表格、配图矛盾；②终点 $(k,\mathrm j0)$ 是**自上方**逼近（Im G→0⁺），原文写「自下方」；③「窄带 $Re G>-k/25$ ⟹ 判据点不可能被包围」只在 $k<25$ 时成立（$k>25$ 时曲线左端越过 $-1$），改写为用穿越计数：实轴交点只有原点 ⟹ $(-\infty,-1)$ 段无穿越 ⟹ $N=0$，与 k 无关（数值算环绕数 k=0.5/2/10/25/30/100/1000 均为 0，Routh 复核 $k>0$ 稳定，两者一致）；④ warning 标题「曲线过原点不等于幅值为零」与正文自相矛盾，前言「缺口由 $1/\omega$ 因子补上」也不是理由（真因是分子在 ω=1 归零）。校验脚本 .scripts\check-notes.ps1 通过。
- 遗留待用户确认：该篇把题目标注为〔习题5-16〕，但教材（胡寿松第八版）习题5-16 是 $G(s)=K/[s(Ts+1)(s+1)]$（求 K、T 范围，PDF p258 / 书内 250 页），并非本题；库内 52 个 PDF（含 825 习题册）也搜不到本题与提示「arctan0.2=11.3°」，出处待定。该标注还作为 wikilink 别名散在 05 第5章索引、05-4-a（2 处）、05-4-b、05-4-1（2 处）共 6 处，改标要一起改。
- 另一处跨文件小不一致（本次未动）：05-4-c 与 05-4-b 把 05-4-a 的内容概括为含「开环不稳定」，但 05-4-a 实际收的是**结构不稳定**（$K/[s^2(Ts+1)]$，P=0），两个概念不是一回事。
- 经验：教材 PDF（第八版）文字层与 OCR 同样把公式打散（\uf02b 之类），搜公式要换思路——优先搜**提示语里的数字**（如 arctan 0.2 → 搜 "11.3"）或直接渲染页面看图；渲染命令：`D:\miniconda3\python.exe` + PyMuPDF，PDF 页 = 书内页 + 8。
- **用户随后澄清（问题关闭）**：05-4-c 的例5.14 是**用户自己习题册上的题**，不在教材/笔记体系里，是他单独补进笔记的；标注〔习题5-16〕指**习题册编号**，题目本身用户已核对无误。以后再看到这个标注不必拿教材第八版习题5-16（`G(s)=K/[s(Ts+1)(s+1)]`）去对号。
- 顺手修了本机 Bash 工具环境（详见 `~/.workbuddy/MEMORY.md`「本机 WorkBuddy Bash 工具环境」）：宿主给的 PATH 缺 Git 的 `usr\bin`，导致 `ls/cat/head/dirname` 全不可用、shim 每次报错；已往用户级 PATH 末尾补两条 coreutils 路径（重启 WorkBuddy 生效，**15:55 重启后已验证**：`ls/cat/head/sed/grep/awk/xargs` 全可用、stderr 无 shim 报错、Windows 的 find/sort/tar 与 `rm` 安全包装均未受影响），旧 PATH 备份在 `C:\Users\23720\.workbuddy\tmp\shim-backup-20260918\`。
