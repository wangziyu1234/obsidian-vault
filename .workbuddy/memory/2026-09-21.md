# 2026-09-21 工作日志

## 高数笔记排版体检（数学\高等数学，113 篇）

用户要求：检查高数笔记，重点看格式优化空间与冗长句子。

### 结论：发现并修复（commit `9f2ade5`，44 个文件，+144/-104）

**渲染级错误（原本显示是坏的）**
- 公式缺反斜杠 5 处：`附录 常用曲线` 的 `$2pi a$`×2、`$0le e<1$`；`附录 幂指对函数图像与计算` 的 `$-infty$`×2、`$+infty$`；`02-1` 的 `$nge2$`。
- 公式内误用全角标点 3 处：`06-1` L142 中点拉格朗日、`03-2` L130、`04-2` L34。
- **`01-1 函数概念与性质` 的「四大性质」表被引用块从中间截断**，`单调性/奇偶性/周期性` 三行不渲染成表格——引用块整体移到表后修复（这是 check-notes.ps1 与肉眼都不容易发现的）。
- `03-3` 断链：`[[2000-17 可微增量式把 sinx 当增量]]` 与实际文件名 `2000-17 可微增量式把sinx当增量.md` 不一致（错题页名无空格）。

**重复块**：`04-3` 夹在莱布尼茨推导中的框式公式与上文重复（删）；`14-4-2` 「方法一」浓缩块与下文重复且两步完全相同（自相矛盾，删）；`09-7` 例题2 的「解」与「分析」推理重复（删）。

**排版**：嵌套列表 1/3 空格缩进→2 空格（32 处）；相邻两个 `$$` 块紧贴→补空行（33 处）；行尾空格（12 处）；正文/引用块 >200 字符长行断行（10 处）；`13-3-2` 把 `[!abstract]` 当小节提示用→`[!note]`；裸 `>` 承载的例题统一为 `[!example] ✏️`（01-1 四例、06-1、08-3、10-1、10-2、11-3 四例）。
> 注意：`01-1` 全篇原本没有任何 `[!example]`，例题一律用裸 `>`——本次一并统一。

**冗长**：删/改 20 处同义重复与废话分句，典型如
- `02` L66「这是"单调有界必有极限"的直接应用」（与前句重复）
- `02-1` L14 整段与 abstract 逐字重复（删段）
- `03-1` L81「含义：…」（与 L80 同义，删行）
- `15-5` 例题1「本题考查…能力，关键是要知道导数的几何意义…」整段空话
- `14-4-2` L81「因为积分收敛，所以可以用对称性处理」（偶性才是主因）

### 新工具：`.scripts/check-math-format.py`
补 `check-notes.ps1` 查不到的**渲染级**问题：公式缺反斜杠/命令与字母粘连（`$nge2$`）、表格被打断、公式内全角标点、附录 H3、相邻 `$$` 紧贴、1/3 空格缩进、行尾空格、>200 长行。默认扫全库，可传目录。
- 已知误报坑（写脚本时踩过，务必保留豁免）：`\operatorname{sinh}` 等**被 `\` 引导的长命令**要整词跳过，否则会匹配到 `ngle`（来自 `\angle`）、`yle`（来自 `\displaystyle`）；`$x^ne^{-x}$` 里的 `ne` 是变量不是命令；公式块内以 `|` 开头的行（绝对值）不是表格行，判表格要求**至少两个竖线**。
- 全库跑完：渲染级 0 处；剩余 209 处全是 `考研数学二`/`线性代数`/`英语` 里的 >200 字符长行（未处理）。

### 遗留（未改，供下次决定）
1. **238 处 `$$` 前一行是正文、176 处 `$$` 后一行是正文**（全在正文/callout 内）。不是渲染错误（Obsidian 能正常渲染为块级公式），只是与「块间留空行」风格不完全一致；**不建议批量加空行**——`02 第2讲`(250 行)、`08-4`(246)、`09-1`(244)、`07`(244) 等已在 250 行上限附近，加了会触发篇幅告警。
2. **跨文件重复例题**（未动，删题要用户点头）：
   - `06-3` 例题9 与 `06-2` 例题2 都是「$e^x=kx$ 实根个数」；
   - `10-3` 例题9 与 `10-1` 例题2 都是「摆线一拱面积」；
   - `13-5` 例题1 的 $I_2$ 与 `13-1` 例题2 都是夹逼证 $\frac{x^2y}{x^2+y^2}\to0$。
3. 4 处表格行超 200 字符（`05-1` L67、`09-1` L31、`10-1` L23/L31），是速查表形态，未动。
4. `02 第2讲 数列极限.md` 正好 250 行，已贴上限。

### 环境注意
- 本库 `core.autocrlf=true`，工作区为 CRLF、仓库内为 LF。`git checkout -- <路径>` 会把该路径下**所有**文件重写成 CRLF（本次 113 篇一起变了），diff 不受影响，但会触发 `LF will be replaced by CRLF` 警告，属正常。
- 写批量替换脚本时必须 `nl = '\r\n' if b'\r\n' in b else '\n'` 并**按原行尾回写**，否则整文件行尾被改。

---

## 第二轮（用户「继续」后，commit `ae100e9`，46 文件 +358/−49）

### 做了三件事
1. **跨文件重复例题 → 交叉引用**（保留题号与结论，只去掉重复推导）：
   - `06-3` 例题9（$e^x=kx$ 根数）→ `见 [[06-2 …]] 例题2`
   - `10-3` 例题9（摆线一拱面积）→ `见 [[10-1 几何应用公式]] 例题2`
   - `13-5` 例题1 的 $I_2$（夹逼）→ `见 [[13-1 …]] 例题2`；顺带删掉该题「要点」里复述解法的第 2、3 条，并把「这个极限依赖于 k，所以极限不存在。因此 I1 不存在」并成一句。
2. **公式块与正文之间补空行 346 处 / 43 篇**（callout 内补一行 `>`），落实「公式不密集」。**列表项上下文跳过**（否则会拆散列表结构，把公式变成独立段落），实际统计里只有 7 处属列表上下文，其余 407 处是普通正文行。
3. 公式定界符写法统一：`01-1` 那处 1 空格缩进的 `$$` 顶格；`09-5` 的 `> $$\begin{aligned}` / `\end{aligned}$$` 改为定界符独占一行。

### 关键决策：3 篇留待拆分后再补
`02 第2讲 数列极限`（250 行）、`07 第7讲 …物理应用`（249）、`09-1 积分方法…`（245）补空行后会越过 **250 行拆分线**（→261/270/272）。为不制造新的「待修」告警，这 3 篇**本次未补**。要补就得先按主题拆分它们。

### 方法坑（下次照抄）
- **别用 bash 双引号包 Python 单行脚本写 `\$`/`\\`**：Git Bash 会把反斜杠吃掉，正则和替换串全变形（本次踩了两次，一次把 `^(\s*>\s*)*\$\$\s*$` 变成废正则、一次 `\\dfrac` 失配）。**一律写成 `.workbuddy/_x.py` 再用绝对路径的 python 跑**。
- 补空前先算「插入后会超 250 行的文件」，按 `len(lines) - (末行为空?1:0)` 与实际行数比（`split('\n')` 会多出末尾空串，直接比会虚高 1 行）。
- 状态机只认「纯定界行」`^(\s*>\s*)*\$\$\s*$`；若文件里有 `> $$…$$` 这种行内写法（`09-5` 曾有），要单独看一眼配对是否仍然齐。补完统一跑一次：**公式块内部出现空行 = 0**、**$$ 不成对 = 0**。
- `$$` 行**有前导空格**（如 `01-1` 的 ` $$`）不匹配纯定界行正则，会漏进状态机 → 顺手顶格修掉。

### 第二轮后的状态
- `check-notes.ps1 -Dir 数学`：0 致命；唯一「待修」仍是 `数学\考研数学二目录.md`（用户明确不拆）。
- `check-math-format.py 数学/高等数学`：**0 处**（含新增的「定界符未独占一行」检查）；全库渲染级 0 处，其余 240 处是 `考研数学二`/`线性代数`/`英语` 的 >200 字符长行（未处理）。
- 脚本新增检查项：块级公式定界符未独占一行；docstring 改 raw，消除 `SyntaxWarning: invalid escape sequence '\d'`。

---

## 第三轮（用户选「要一致」→ 先拆再补，commit `a021559`）

### 为什么拆
`02 第2讲`(250) / `07 第7讲`(249) / `09-1 积分方法`(245) 补空行后会变成 261/270/272 行、越过 250 行拆分线。用户要求「一致」，于是**先拆到位，再补空行**。

### 拆法（原文件名一律不动 = 入口模式，22 处反向链接零改动）
照抄仓库既有入口页体例（`14-2` / `14-3` / `15-2`）：原文件改成
frontmatter → `> 返回目录：` → H1 → `> [!abstract]`（注明「已按内容拆分，本文件为总览入口，旧文件名与原有链接不变」）→ `## 🗂️ 分章导航` 表 → 章末解题套路。

| 原文件 | 新专题件 |
|:--|:--|
| `02 第2讲 数列极限`（保留 2.6 解题套路） | `02-2 数列极限的性质·海涅定理与 e 的定义`（2.1—2.3）、`02-3 数列极限三板斧与收敛判定`（2.4 + `### 补充工具：Stolz 定理与根值转比值`） |
| `07 第7讲 …物理应用`（保留 7.6 解题套路） | `07-1 导数的物理意义与相关变化率`（7.1—7.2）、`07-2 物理应用典型例题`（7.3）、`07-3 边际弹性与复利现值（数三选读）`（7.4—7.5，**数二可跳过**） |
| `09-1 积分方法…`（保留「参数与定义域」note） | `09-1-a 基本积分表与凑微分`（9.1—9.2）、`09-1-b 第二类换元`（9.3）、`09-1-c 分部积分与循环方程`（9.4） |

另外把 `02` 里的例题1—4 搬进 `02-1 数列极限典型例题`，**编号顺延为 例题7—10**（02-1 原有例题1—6），题型速查 note 提到 `## 🧮 2.5` 小节开头当导航；原「完整题型见 [[02-1 …]]」那句删掉（已无意义）。

### 结果
- 高数目录**再无一篇 >250 行**（唯一超限的是 `附录 变形技巧·等式变形` 286 行，附录豁免）。
- `check-notes.ps1 -Dir 数学`：0 致命；唯一「待修」仍是 `数学\考研数学二目录.md`（用户明确不拆）。
- `check-math-format.py 数学/高等数学`：**0 处**。
- 补空行：新子页 + 全库 60 处。

### 踩坑（下次直接用脚本生成入口页，别手拼字符串）
1. **`TAIL` 拼接把返回行拼成了 `> 返回：[[X]] | > 返回目录：…`**（9 个文件全中）——入口页的返回行要按整串写 `> 返回：[[父篇]] | 返回目录：[[高等数学公式速查]]`，不要用常量拼 `| `。批量修 `| > 返回目录：` → `| 返回目录：` 即可。
2. **切片包含标题行导致标题重复**：`trim(L[s_26:])` 已含 `## 2.6 解题套路`，我前面又手写了一遍 → 校验脚本报「小节号 2.6 重复」。切片要么从 `s+1` 开始，要么别再加标题。
3. `head1 = L1[:qk]` 里已含 `## 🧮 2.5 典型例题`，又补了一次 → 同款重复告警（要盯：**小节号重复**这条致命项最容易由拆分脚本引入）。
4. 新写的 md 统一用 `\r\n` 写盘，和当前工作区一致（`core.autocrlf=true` 下 git 内部仍是 LF）。
5. 拆分前先确认**没有带锚点的引用**（本次 `[[02 第2讲 数列极限#…]]` 为 0），否则留标题/搬内容就要额外处理锚点。
