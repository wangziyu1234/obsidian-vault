# 2026-09-24

## 05-4-3 稳定裕度：补「另一种图」＝奈氏图读法（commit `2f6d88f`、`c609bdf`）
- 用户问 05-4-3 的「另一种图的表示呢」——笔记 §5.4.5 只有一张伯德图，而 abstract 写了「两图通用，读法不同」，奈氏图那半是空的。
- 新增 `附件\频域-稳定裕度-奈氏图.png`（源码 `ch5-margin-nyquist.py`），与伯德图**同取 G=2/[s(s+1)(0.1s+1)]**，四个量一一对应：ωc=1.2437、γ=31.71°、ωx=√10=3.1623、h=5.5（14.807 dB）。
  - γ 画成**单位圆上从 (-1,j0) 到 G(jωc) 的圆心角**（两点同在单位圆上，角顶点在原点）——比画半径弧干净，且不与曲线相交。
  - h 画成负实轴上的**尺寸线**：O→交点实线（=1/h）+ 交点到临界点虚线（补足到 1），交点处加刻度线；数值块（ωx／|G(jωx)|／h）放右下空区，引线连回交点。
- **重绘 `频域-稳定裕度.png`（同名覆盖，旧图可从 git 取回）**：原图是旧批次出的，穿越频率标 **ω_g**、参考线写 **L = 0**，与笔记正文口径（ω_x、0 dB）矛盾。新源码 `ch5-margin-bode.py`，`figures_style` 统一风格，双栏 + 双向箭头标 γ、h(dB)。
- 正文新增：两图对照表（γ、h 各在哪读）+ `[!tip] 一句话记两图`（伯德读两条参考线、奈氏读两个参考图形；γ 挂 |G|=1 侧、h 挂 φ=−180° 侧）+ `[!note] 尼科尔斯图上的两个裕度`（= 到 0 dB 线／−180° 线的竖直、水平距离，接 [[05-1-1]] §5.1.5）。文件 137 → 157 行。
- 校验：check-notes 单文件 0 致命 0 待修；check-math-format 控制理论 0 处。行尾：05-4-3 全 LF（与本库工作副本一致），裸 LF 无混排；两图 push 成功。
- ⚠️ **踩坑（已写进 MEMORY.md）**：第一版图我肉眼判"单位圆被拉成椭圆"，补了 `ax.set_box_aspect(1)`——后来量 `ax.transData` 发现 1 单位 x = 1 单位 y = 165.94 px，**等比从头到尾都是对的，是看图错觉**；已删掉那段并重出图（与上一版**逐字节相同**，git 无改动即验证）。结论：判断等比要量 transData，不要靠眼睛。
- 备注：`git status` 里另有 6 篇 .md（05-4-1 / 05-4-a / 05-4-d / 05-5 / 错题本总览 / 高数 02-2）显示 M 但 `--numstat` 0 行变更＝既有行尾幽灵，未动。

## 习题5-31 → 例5.30，05-4-3 补 §5.4.7「无穿越点 ⟹ h=∞」（commit `fb36767`）
- 用户发三张照片（`D:\ProgramData\tx\qq\Screenshot_2026-09-24-*`，习题5-31 燕山大学2018）问两件事：
  「奈奎斯特图没有和实轴有交点的情况在裕度这里有讲吗」＋「整理照片为例题」。照片已能读出（本机 `Read` 可读 jpg）。
- **照片题的完整答案**（与讲义解答一致）：(1) γ=arctan(0.6/0.8)=36.87°、h=1/0.2=5；(2) K1=0.5、K2=1、K3=25，稳定区 K∈(0,0.5)∪(1,25)。ν=1、三交点 −10/−5/−0.2（K=5），沿曲线负、正、负穿越。
  - 关键理解：**h 只能取落在 −1 右侧的那个交点（−0.2）**，取 −5/−10 会得 h=1/5、1/10 与"已知稳定"矛盾；由 (2) 的上界 K=25 反查 25/5=5，与 (1) 自洽。
- **第 1 问的答案**：笔记原来只在〔两个容易推广过头的结论〕里一句，现已扩成 **§5.4.7**：判据 = 相角落到 −180° 之外 ⟺ **ν≤1 且 n−m≤2**（相角单调的「积分＋一阶惯性」形式），一阶、二阶系统都 h=∞。三条反例：延迟（相角无下界）、**带零点**（相角非单调会凹陷，`(s+1)²/[s(s+0.01)²]` 凹到 −247°，有 2 个交点）、ν≥2（结构不稳定，h=∞ 却根本不稳 ⟹ **h=∞ 不等于稳定**）。
  - 数值都自己跑过：一阶/I型二阶/0型二阶在 0<ω<∞ 无 −180° 交点；反例与延迟有交点。
  - ⚠️ 形式坑：`(s+0.01)²` 的极点在 ω=0.01，写成 `(0.01s+1)²` 极点在 ω=100——写反例时第一版就踩了（相角只到 −89.9°）。
- 落点：例5.30 写在 **05-4-e**（单题完整，含两问 + 两张图），05-4-a 例5.13 下加〔同型变式〕指引（按「同型不重复补例」不重推），总览 §⑤ 加一行 + 表头范围 + 判据例题行同步。行数：05-4-3 153→191、05-4-e 75→136、05-4-a 150→154、总览 230→232。
- 新图 3 张：`自控-频域-例530-读裕度.png`、`自控-频域-例530-K范围.png`（双对数交点轨迹 + 左侧交点计数阶梯）、`自控-频域-h无穷的判据.png`（上下两栏：够不到 vs 穿过 −180°）。
- ⚠️ **本机读不了 PNG**：`Read` 对 PNG 一律返回 "current model does not support images" ⟹ 成图无法目视。改写了 **`.scripts\figures\_check_fig_layout.py`**：monkeypatch `fs.save` 拿 fig，用 renderer 量全部文字 artist 的 bbox，报「压字」与「越出画布」。**它一次抓出三个真 bug**：krange 图只设了 x 轴 log（"斜率 1 的直线"根本没画成直线）、共享横轴上面板的刻度标签没隐藏、`tight_layout` 被 GridSpec 静默拒绝导致图级文字压住轴标签。
  - 4 类已知误报已在脚本里排除：Annotation 的 window extent 含箭头 patch（改用 `Text.get_window_extent`）、`fig.texts[i]` 与 `fig._suptitle` 是同一对象、legend frame 天然包住自己的文字、落在坐标范围外被裁掉的刻度标签。
  - 用法：`D:\miniconda3\python.exe _check_fig_layout.py <脚本.py> [...]`（脚本内 `if __name__=="__main__"` 要被执行，故 spec 名取 `__main__`）。
- ⚠️ **matplotlib 两个写法坑**（第一版图全报错）：`$\mathrm j\omega$`（`\mathrm` 后带空格）在 3.11 上 `ParseFatalException: Unknown symbol: \mathrm`，必须写 `$\mathrm{j}\omega$`；mathtext 也不认 `\lvert`。图内文字一律「纯文本 + Unicode 数学」（γ、−180°、(−1, j0)），再犯过一次掉汉字字形的老坑。
- 校验：check-notes ✔ 全部通过；check-math-format 控制理论 0 处（新写的一行 236 字符长行已拆）。

## 补入原题奈氏图（commit `52ee067`）
- 用户追问「习题5-31 题图怎么没有」——原来只放了自己画的两张**分析图**（读裕度、K 范围），**原题那个奈氏图没进笔记**。教训：整理照片/扫描件为例题时，**题图本体**必须一起落，分析图只是辅助，不能顶替题图。
- 题图来源：第三张照片 `Screenshot_2026-09-24-16-28-01-368_com.onyx.gala..jpg`。按暗像素包围盒定位（x 1721~2583、y 1049~1747），外扩 14 取 `CROP_BOX=(1707,1035,2597,1761)`；灰度阈值 150 二值化**顺带把页面粉色水印抹掉**；再抹孤立黑点。出图 `附件\自控-频域-例530-题图.png`（890x726、7 KB、黑像素 4.03%）。
- 留了可复现脚本 `.scripts\figures\ch5-ex530-problem-figure.py`（参数写死 + `build()` 断言黑像素占比 >2%、包围盒不贴边），重跑与已出图 `cmp` **逐字节相同**。
- 05-4-e：题图插在「题设」之后（`|430`），图注说明向上穿 −10／向下穿 −5／向上穿 −0.2、虚线圆为单位圆、黑点即 (−0.8,−0.6)、箭头为 ω 增大方向，并注明**原图小环笔触与「只有 3 个交点」略有出入，以题干 −10/−5/−0.2 为准**；读裕度图图注补「按题图重绘并**放大**到 −1 附近（−5、−10 已在画面外）」，交代自绘图为何看不到完整曲线。
- 校验：check-notes ✔ 全部通过；check-math-format 控制理论 0 处；三条图片链接与附件均已核对存在。

