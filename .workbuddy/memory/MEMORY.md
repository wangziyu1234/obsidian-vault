# 项目约定（D:\obsidian 知识库）

## 错题本机制（数学/错题本/）
- **按年份放子文件夹**（用户 2026-09-18 要求，commit `5bd66bb`）：`数学\错题本\YYYY\YYYY-NN 关键词.md`，如 `2000\2000-4 斜渐近线截距漏项.md`；根目录只留两篇：`00 错题本总览.md`（索引＋看板）与 `01 错题本使用规范.md`（流程/字段/复习标注/AI 口径），2026-09-18 从总览拆出（commit `e89737c`）。新年份**有第一篇错题时再建文件夹**，不预建空目录。
- 移目录不用改链接：Obsidian wikilink 认文件名不认路径；Dataview 的 `FROM "数学/错题本"` 与 Tasks 的 `path includes 数学/错题本` **都递归子文件夹**，看板照旧。用 `git mv` 移（保留历史）。
- **看板查询一律用标签 `FROM #错题` + `WHERE file.folder != "templates"`**（2026-09-18 改）：不再依赖文件夹路径，以后挪文件夹/加年份子目录都不用改查询。`#错题` 由 `templates\错题模板.md` 自带，模板本身靠那条 folder 排除掉（模板的 frontmatter 也是 `type: mistake`，不排除会混进统计）。
- **「待复习」表空着是正常现象，不是坏了**：查询条件是"有没有 `review` 日期 ≤ 今天"，当天那一档做完就把该日期删掉，于是下一个节点变成明天——**当天做完后当天必然空**。为此看板里另放了一张常驻表「尚未掌握（全部，含还没到期的）」，带 `下次复习` 与 `剩余节点` 两列，用来一眼判断看板是否真的失效。
- 一题一篇 `年份-题号 关键词.md`，`type: mistake`，字段 `year/no/kind/topic/cause/mastery/review`，模板在 `templates\错题模板.md`。
- **`mastery` 五档（2026-09-20 起，用户要求加 0 档）**：`0` **跳过／主动放弃**（整题没动笔、本轮不打算拿分，**典型是数二的技巧型证明题**）→ `1` 刚错 → `2` 会做但不熟 → `3` 稳（归档）。
  - **`0` 档不排复习节点**（`review: []`），题不进「待复习」，但进总览页的「**跳过挂起**」表（`mastery = 0`），也仍留在「尚未掌握（全部）」里且 `剩余节点 = 0`。想收回就把 `mastery` 改成 1、排满 5 个节点。
  - **配套 `cause: 主动放弃`**（cause 取值新增这一项），用来统计"故意丢了多少分"。
  - **弃题判据（用户 2026-09-20 明确：目标分不上 130 时证明题可弃）**：可弃 = 需自己构造辅助函数/反证的**技巧型**证明；慎弃 = 外壳是零点定理、变上限积分求导、保序性、单调性的题（这些方法选择题/填空题照考）；底线 = **弃题也要写到"免费的那一步"**（如零点定理给出一个零点），过程分按步给。判据一句话：**这题的方法换一道选择题还会不会用？会 → 不该进 0 档。**
  - 因此 `review` **只有 `mastery: 0` 允许为空**；`check-mistake-cards.py` 已同步（允许 0、0 档跳过「勾框 ↔ review」一致性检查且不报"没有可勾选日期项"）。`0` 档卡片的「⏱️ 复习记录」小节改写成一句挂起说明（不放假勾选框）。
  - ⚠️ 别在新文案里写「待补」二字——`check-notes.ps1` 的 TODO/FIXME/待补 占位符检查会报「待修」（本次把看板名从「跳过待补」改成「**跳过挂起**」就是为此）。
- **复习状态三处联动**：正文「复习记录」勾框（Tasks 自动补 `✅ 日期`）＋ frontmatter `review` 删掉已完成日期 ＋ `mastery` 1→2→3（3 归档）。
- **`review` 是写死的日期，不自动顺延**：漏做只让看板的「到期日」列一直偏早，不会推动后续节点。补做二选一：删掉漏掉的日期（后续不动）；或以补做日为起点重排剩余日期。
- 「待复习」查询条件：`type="mistake" AND mastery<3 AND any(review, d<=today)`。
- 建篇后要在当年解析页对应题号旁补 `> 错题：[[同名错题页]]`，并在总览手写索引表加一行。

## 知识点与例题的分工（用户 2026-09-18 明确）
- **方法/知识点必须写在方法篇里自足，不能靠"前两种见 XX 篇""推演见例题篇"把知识点推走**；例题篇只做同一套方法的对照演练（如 05-2-e 例5.26 把三种取法各演一遍），不重复讲方法。
- 判断标准：**这句话拿掉后读者还能不能独立做题**——不能，就属于知识点，留在方法篇。
- 术语要随改动一起统一（本次：低频渐近线的"三种入口" → "三种取法"，全库 grep 一遍再改）。

## 拆分超长篇的可复用套路（2026-09-18 线代 06-6/06-9 验证，commit `6ec59c3`）
- **留标题、搬内容**：被移走内容所在的小节标题（`## 🔶 6.6 …`、`## 🔶 6.13 …`）**留在方法篇**，这样绝大多数外部 `#锚点` 链接无需改动，只需改指向具体例题的那几条。改完必须 `grep '旧文件#旧锚点'` 确认为 0。
- **拆完要做三处索引同步**：① 讲次页分章导航给子篇编新号（本次加了 6.6.1/6.6.2/6.13.1）；② `数学\线性代数公式速查.md` 对应主题行加入口；③ `附录 线代教材对照与复习索引.md` 对应行加入口。子篇的 `> 返回：` 指向父篇。
- **顺手把散在例题里的方法上提到方法篇**（用户定过规矩），例：06-6 新加「$D^TD$ 三条结论与三处边界」「判定标准步骤与选型」；06-9 的 6.13 把题面→谱信息翻译表、两条写法、重数+正交论证全部上提。

## 线代第6讲 06-6 / 06-9 拆分后的结构（2026-09-18）
- `06-6 正定判定`（119 行）＝§6.6 理论（五个充要条件 / 两个必要条件 / 顺序主子式定义 / 常用结论 / 高频反例 / $D^TD$ 三结论三边界 / 判定步骤与选型）＋例题导航表；例题在 `06-6-a 例题·正定判定五种走法`（教材例6.10 五法 + 平方和补充练）与 `06-6-b 例题·范德蒙德型转置乘积正定`（张宇 P153 第25题，$s\le n$）。
- `06-9 正交变换与反求矩阵`（216 行）＝6.12 传递写法 + 教材例6.6、**6.13 反求方法全篇**、6.14 伴随/$A^2$/$D^TD$ 的反向命题；两道整题在 `06-9-a 例题·由正交标准形反求矩阵`（教材习题6.12、张宇 P152 第24题）。
- **`数学\考研数学二目录.md`（1714 行）用户明确不拆**（2026-09-18「前俩可以」只批准 06-6、06-9）。它仍是 `-Dir 数学` 唯一的「待修」告警，**不要再提议拆分，也不要为它改校验脚本的豁免规则**，除非用户开口。

## 校验与提交
- 校验：`.\.scripts\check-notes.ps1 -File/-Dir`；PowerShell 里跑要直接 `&` 调用并 `*>` 落盘再读（嵌套 `powershell -File` 会因编码报解析错误）。**本机 PowerShell 工具不回显 stdout**（一律只回 "Command completed with exit code 0"），任何输出都必须写文件再读。
- 附录/速查页里**不要字面写 `$`、`$$` 定界符**（会让脚本的公式配对计数变奇数）。
- 本库有 Obsidian 自动备份插件，历史里会混入 `auto backup YYYY-MM-DD hh:mm` 提交，非助手所为。

## 第5章 反求专题的现行结构（改前先确认）
- `05-2-b 由伯德图反求传函`：§5.11.1 三要素 → §5.11.2 例5.23（ν=0 一阶）→ §5.11.3 例5.24（二阶振荡，峰高定 ζ）→ §5.11.4 例5.26（Ⅱ 型，$\omega_0=K^{1/\nu}$ 与 $K=\omega_1\omega_c$）。**该篇常年贴着 250 行上限**（2026-09-18 为 249 行），再加内容就要按主题拆。
- `05-2-c 反求传函综合测试（自编）`：自测题，不占例题号。
- 例题编号已用到 **例5.27**（章索引 §⑤ 的 A/B/C/D 四张表要一起加行；新例题优先插末尾；`05-1-2-c 例题·振荡环节参数反求` = 例5.27，2026-09-18 补自课件例7）。
- 教材依据：§5-2.4 式(5-55)—(5-57) 的**方法一/二/三**（书内215—216页）＝低频渐近线用一个点钉住的三种取法；方法三给出 $\omega_0=K^{1/\nu}$。课件对应讲次：绘制在 `控制88-20`，反求同型题在 `控制88-19 例1`、`控制88-23 例3`。

## OneDrive 资料根目录（2026-09-18 核对）
- 课件与原视频：`C:\Users\23720\OneDrive\按章节-原视频和PPT\`（第5章 = 控制88-17 ~ 28）
- Word 补充：`C:\Users\23720\OneDrive\Word-补充自O-God\`
- 旧 `.doc` 用 Word COM `SaveAs(...,2)` 转纯文本可读（公式是图片，只拿到文字骨架）；**PowerPoint COM 在本机打不开 .ppt**（`Presentations.Open` 一律 `Unexpected HRESULT`），改用**文本框记录头法**：`data.find(b'\x00\x00\xa0\x0f')`（TextCharsAtom，`\xa8\x0f` 为 TextBytesAtom/GBK）命中后按紧随的 4 字节小端长度切片解 UTF-16LE，12 份第5章 PPT 抽出 2238 行干净文本。
- **课件里的插图能直接抽出来用**（用户 2026-09-18 认可："ppt 的图就很清晰"）：按图片签名裸扫整个 `.ppt`（PNG `\x89PNG…IEND\xaeB\x60\x82`、JPEG `\xff\xd8\xff…\xff\xd9`），不必解 OLE 目录。一节课件 70–230 张 PNG 里绝大多数是**公式位图**，真插图（300–800 px）只有 5–10 张；**筛图靠"墨量排序 + 拼联系表"**（灰度<190 像素占比 + 缩略图带文件名），一次看几十张。抽回来的图存 `附件\自控-频域-*.png`，流程写在 `.scripts\figures\README.md`。已入库：`自控-频域-反求延长线与K.png`（05-2-b）、`自控-频域-Bode折线与相频.png`（05-2-1）。
- 第5章 PPT 里值得再取的图（已定位）：`控制88-20` 有 002/072/082 三张 Bode 折线示例（082 带低频延长线）、040/087 是 `[s]` 平面零极点矢量图、073 是 $r(t)/c_s(t)$ 相位滞后图；`控制88-19`-200 是二阶反求图（峰 28 dB、$\omega_n=28.77$）。矢量图那批只有 200×220 px 左右，**分辨率不够嵌 430 宽，别用**。

## 第5章 的现行结构与查词入口（2026-09-18 重排后）
- **唯一入口 = `05 第5章 线性系统的频域分析法`**：§① 找东西（课件讲次表 控制88-17~28 / 术语表 / 课件缺口清单）、§② 学习路线总表、§③ 对象、§④ 教材逐小节对照、§⑤ 例题总表（例5.1—例5.27）、§⑥ 套路。**2026-09-18 已 222 行，别再往章页加内容**（>250 校验脚本报"待修"）。
- **全章统一按「知识点篇在前、例题篇在后」排**：
  - 地基族：`05-1-1 频率特性的基本概念`＋`05-1-2 典型环节频率特性`（含 `-b` 振荡/二阶微分/延迟）＝方法；例题 `05-1-1-b-1`（例5.2）· `05-1-1-b`（例5.3）· `05-1-2-c 例题·振荡环节参数反求`（例5.27）。
  - 伯德族：`05-2-1 开环伯德图（画法·相频·修正）`＋`05-2-b 由伯德图反求（方法与口径）`＝方法；`05-2-d 伯德图绘图例题`（例5.9、例5.25）＋`05-2-e 伯德图反求例题`（例5.23/5.24/5.26）＝例题；`05-2-c` 自测。
  - 奈氏族：**知识点只剩 2 篇**——`05-3-1 幅相曲线的画法与最小相位`（247 行：通式、起点/终点/象限、画图五步、最小相位与非最小相位）＋`附录 幅相曲线速查`（489 行：14 张典型曲线 / 零点组合九种 / 双惯性特征点，附录豁免 250 行）；例题 `05-3-4`（例5.4 型别对照 + 例5.5）· `05-3-5`（例5.6）· `05-3-6`（例5.7）· `05-3-7`（例5.8 参数判稳，278 行 `single-exercise` 豁免）；`05-3-a` 两图对读。原 `05-3-2/3/8/9` 已并入这两篇（2026-09-18 commit `2de9a08`）。
  - 判据族：`05-4-1/2/3` 方法 → `05-4-a~d` 例题。综合族：`05-5-1/2/3` 方法 → `05-5-b*` 例题。
- 奈氏族的序号是**跨文件连续**的：`5.3.2.1` 05-3-1 / `.2` 附录典型曲线 / `.3` 例5.4 / `.4` 例5.5 / `.5` 例5.6 / `.6` 例5.7 / `.7` 例5.8 / `.8` 附录零点组合 / `.9` 附录双惯性。加新题要插中间时，必须一次性把后面全部顺排（占位符两趟法）。
- 改名/重组后**必须跑全库链接替换脚本**（两趟法：先按目标名替换，再统一改写被改名文件的链接），最后跑 `check-notes.ps1 -Dir` 确认 0 致命。
- 课件是**西工大版**课程（§5.1–§5.9），**其 §5.9 频率法串联校正＝本库第 6 章**；术语换算：迟后＝滞后、相角交界频率＝$\omega_x$、等 $N$ 圆＝等 $\alpha$ 圆、交接频率＝转折频率。
- 本章反求题的三类定式与 $\omega_0=K^{1/\nu}$ 口径见 `05-2-b`；例题在 `05-2-e`。

## 排版体检：两个脚本各管一半（2026-09-21 新增）
- `.scripts/check-notes.ps1` 管**结构与链接**（断链 / `$$` 成对 / 编号重复 / H1 / abstract / 篇幅）。
- `.scripts/check-math-format.py`（2026-09-21 新增，见 commit `9f2ade5`）管**渲染级**问题，是前者查不到的：
  公式缺反斜杠与命令粘连（`$2pi a$`、`$0le e<1$`、`$-infty$`、`$nge2$`）、**表格被正文/引用块从中间打断**（后半截不渲染成表格）、公式内全角标点 `，、；`、附录 H3、相邻两个 `$$` 块紧贴、列表 1/3 空格缩进、行尾空格、正文/引用块 >200 字符长行。
  用法：`python .scripts/check-math-format.py [目录…]`，默认全库；退出码 1 = 有渲染级问题。改完笔记两个都跑一遍。
- 该脚本的**已知豁免/误报坑**（改脚本时保留）：`\operatorname{sinh}`、`\angle`、`\displaystyle` 这类被 `\` 引导的长命令要整词跳过，否则会误报 `ngle`/`yle`；`$x^ne^{-x}$` 的 `ne` 是变量；公式块内以 `|` 开头的是绝对值不是表格行，判表格要求**至少两个竖线**。
- 体检口径：**「渲染级」必须修、「排版」按需修、「风格」不批量动**。典型「风格不动」项——**238 处 `$$` 前一行是正文 / 176 处其后是正文**：Obsidian 能正常渲染成块级公式，不是错误；批量补空行会把 `02 第2讲 数列极限`(250 行)、`08-4`(246)、`09-1`(244)、`07`(244) 等顶上 250 行告警线，**不要动**。
- 跨文件重复例题（2026-09-21 已处理，commit `ae100e9`）：`06-3` 例题9 → 指向 `06-2` 例题2（$e^x=kx$ 根数）、`10-3` 例题9 → 指向 `10-1` 例题2（摆线一拱面积）、`13-5` 例题1 $I_2$ → 指向 `13-1` 例题2（夹逼证 $\frac{x^2y}{x^2+y^2}\to0$）。**处理方式照抄**：保留题号、题干与结论，把重复推导换成「同 [[已展开处]] 例题N」一句。
- **公式块与正文之间一律留空行**（2026-09-21，commit `ae100e9` + `a021559`，共 400+ 处）：callout 内补一行 `>`，正文补空行。**列表项上下文跳过**（否则公式会脱离列表、拆散结构）。`02 第2讲`/`07 第7讲`/`09-1 积分方法` 三篇先拆成总览入口（见下节）再补，现已全部合规。高数目录**唯一 >250 行的是 `附录 变形技巧·等式变形`（286 行，附录豁免）**。
- **拆分做法（2026-09-21 对 02/07/09-1 验证，commit `a021559`）**：原文件名不动 → 改成总览入口（frontmatter → `> 返回目录：` → H1 → `> [!abstract]` 注明「已按内容拆分，本文件为总览入口，旧文件名与原有链接不变」→ `## 🗂️ 分章导航` 表 → 章末解题套路）；正文按小节搬进 `NN-x 主题.md`，子页头部 `> 返回：[[总览名]] | 返回目录：[[高等数学公式速查]]`。**反向链接零改动**（入口模式的最大好处）。拆前先 grep 确认没有 `[[文件#锚点]]` 形式的引用。
  - 脚本坑（三处都踩过）：① 别用常量拼返回行，会得到 `> 返回：[[X]] | > 返回目录：…`；② 切片 `L[i:]` 若已含小节标题行，前面别再手写一遍标题，否则校验脚本报**「小节号重复」致命项**；③ 新写 md 用 `\r\n` 存盘，与工作区一致。

## 高数第2/7讲与09-1 的现行结构（2026-09-21 拆分后）
- `02 第2讲 数列极限`（总览，24 行，保留 2.6 解题套路＋分章导航）：`02-1 数列极限典型例题`（2.5，例题1—10：原 02 的例题1—4 已并入并顺延为 7—10）· `02-2 数列极限的性质·海涅定理与 e 的定义`（2.1—2.3）· `02-3 数列极限三板斧与收敛判定`（2.4＋`### 补充工具：Stolz 定理与根值转比值`）。
- `07 第7讲 一元函数微分学的应用（三）物理应用`（总览，24 行，保留 7.6）：`07-1 导数的物理意义与相关变化率`（7.1—7.2）· `07-2 物理应用典型例题`（7.3）· `07-3 边际弹性与复利现值（数三选读）`（7.4—7.5，**数三内容，数二可跳过**）。
- `09-1 积分方法（凑微分·换元·分部·部分分式）`（总览，25 行，保留「参数与定义域」note）：`09-1-a 基本积分表与凑微分`（9.1—9.2）· `09-1-b 第二类换元`（9.3）· `09-1-c 分部积分与循环方程`（9.4）。注意 `09-1 的基本积分表` 与 `附录 常用积分表·有理与三角` 的基础公式有重复，**未处理**。
- 块级公式定界符**必须独占一行**（`> $$` + 内容，不要写 `> $$\\begin{aligned}`）；`$$` 行也不许有前导空格（`01-1`、`09-5` 各修过一处）。
- 新加例题要**认准体例**：例题一律 `> [!example] ✏️ 标题`，别再用裸 `>` 引用块；小节提示用 `[!tip]`/`[!note]`，**`[!abstract]` 只留给每篇开头的「本讲定位」**（2026-09-21 修掉 `13-3-2` 一处误用）。
- 本库 `core.autocrlf=true`：工作区 CRLF、仓库内 LF。`git checkout -- <路径>` 会把该路径下**所有**文件重写成 CRLF（diff 无变化，但触发 `LF will be replaced by CRLF` 警告，属正常）。写批量替换脚本必须按原行尾回写（`nl = '\r\n' if b'\r\n' in b else '\n'`）。
- ⚠️ **别用 bash 双引号包 Python 单行脚本写 `\$`/`\\`**：Git Bash 会吃掉反斜杠，正则与替换串全变形（2026-09-21 踩过两次）。批量脚本一律**写成 `.workbuddy/_x.py` 再跑绝对路径的 python**。
- ⚠️ **批量脚本的缓存别把「文本」和「行尾符」塞进同一个 list 靠下标取**（2026-09-22 踩过：`cache[n][1]=新文本` 覆盖了行尾符槽位，写盘时 `t.replace('\n', nl)` 把**每个换行符换成整篇文本**，17 篇被撑到几万行）。用两个字典（`TEXT`/`EOL`）分开存，并**写完立刻核对行数**。恢复正常：`git checkout --` 回滚受损文件，再按「断行 → 拆分 → 补空行 → 小修」的正确顺序重跑（未入库的新文件先删再重建）。
- 「缩进 3 空格」**不一定是错**：父项是 `1. ` 这类 3 字符标记时，子项缩进 3 空格才是正确对齐（脚本已按父项标记宽度判定）。
- ⚠️ **批量脚本禁止用 `文本.replace('\n', 变量)` 这种写法**：2026-09-22 两次把文件写坏（17 篇被撑到几万行、附录被撑到 2030 行），根因都是行尾符变量被覆盖/取错。**一律按行列表组装、`'\r\n'.join(lines)` 写盘，并在写盘前断言行数落在合理区间、写盘后重读核对行数。**
- **整卷真题与解析不做排版统一**：`check-math-format.py` 已跳过 `NNNN年…试题 / NNNN年…答案与解析`。
- **自我链接（`[[本文件名]]`）等于没有链接**，属真 bug：全库用 `.workbuddy` 里的自指扫描查（2026-09-22 查出 8 处并修完）。
- **合并两页的稳妥做法**（2026-09-22 合并两篇相似变换附录，commit `3084795`）：以「名字覆盖更全」的那篇为存续页 → 两篇的重复小节只留一份 → 重排 `## (n)` 编号（附录禁用 H3，分部分靠定位行说明）→ 删掉被并页 → **把被并页的全部反向链接改写**（改名前先 grep 列举，改完再 grep 确认 0 残留）→ 校验。

- **`type:` 字段（2026-09-22 补全，462 篇）**：`cheatsheet`（附录/速查）· `index`（`## 🗂️ 分章导航` 入口页、讲次页、目录页）· `example`（名字或 H1 含例题/习题/练习**且正文真有例题**）· `method`（其余）；真题用 `exam-paper`/`exam-solutions`、错题卡用 `mistake`。**别再靠"小节标题里出现例题"推断**——会误判（方法页常带例题），只看名字/H1 + 是否真含例题。
- **`aliases:` 只写"页面里讲了、文件名里没有"的概念词**（2026-09-22 补 102 篇，另有 4 篇入口页本来就有）。名字里已有的词写进 aliases 是白费（Ctrl+O 本来就模糊匹配文件名）。自动抽取必须过滤样板标题（分章导航/解题套路/典型例题/题型/结论/方法/为什么/先把…）、句长>10、含数字量词与 `〔〕` 残留；覆盖率宁可低也不要噪声。**碰到哪篇顺手补哪篇。**
- **`.workbuddy\` 不是笔记**：`check-notes.ps1` 已加进排除项（2026-09-22）。那里的 `memory/*.md` 会原样摘抄 `[[…]]`、`$…$` 片段，扫进来必然误报。
- **`考研数学二目录.md` 本来就有锚点导航 + 每专题的「本章题型」锚点行**（2026-09-22 核对），不要再给它加"页内目录"，只需在顶部快速导航维护那几个锚点（现有：年份目录 / 专题目录与题量 / 逐年题量核对 / 使用方法 / 错题本）。

- **页尾统一出口（2026-09-22 加，268 篇）**：method / example 两类页在**页尾**加一行
  `> 相关：上一节 [[…]] · 下一节 [[…]] · 本讲 [[NN 讲次页]] · 速查 [[…公式速查]]`。
  原因：顶部 `> 返回：` 只在开头有用，读到底要找下一篇得往上滚。目标全部由**目录内文件名**推出：
  上一节/下一节 = 同一 `NN-` 家族内按文件名排序的前后篇；本讲 = 该家族讲次页（type: index）；速查 = 顶部 `返回目录：` 那页。
  入口页（已有分章导航表）与附录不加；真题、错题卡、模板不动。
- **方法页「考点速记」块（2026-09-22 起逐讲补）**：`type: method` 页在 `> [!abstract] 本讲定位` 之后、
  第一个 H2 之前插 `> [!tip] 🎯 考点速记`，**5 条、每条一行**（核心式 / 最易错点 / 口诀 / 高频变形 / 例题入口）。
  179 篇 method 页，进度见当日日志。**两条硬约束**：① 块内不得有 >200 字符的行（写完回测、超了就压句）；
  ② 改块必须**按行内容匹配**定位，**不许按行号**——本次按行号改时偏一位，覆盖掉邻条并使其丢了 `> ` 前缀；
  改完要 dump 全块验证「5 条齐全 + 每条以 `> ` 开头」。
- **插入速记会让 240+ 行的页过 250 线**：先看该页有无「同结论写第二遍」的重复块可删（如 `08-4` 的例题要点、
  `02-2` 的应试提示 warning），删掉重复再插，**不要动例题推导本身**；`02-2`/`02-6` 现均为 250 行（卡线安全）。
- **`.workbuddy/` 入库范围（2026-09-22 定）**：**只跟踪 `memory/`**；临时脚本与输出（`_*.py`/`_*.txt`、
  `check-*.txt`、`tmp/`）已在 `.git/info/exclude` 排除。原因：**Obsidian 的 `auto backup` 插件用 `git add -A`**，
  会把整个工作区（含这些临时文件）一起提交（本次 commit `8dd8c93` 即如此，还把当时未提交的笔记改动代为提交）。
  因此**每批改完要尽快 commit+push**，别让改动长时间摊在工作区。
- **`check-notes.ps1` 链接判定**（2026-09-22 修）：只有 `png|jpg|jpeg|gif|bmp|svg|webp|pdf|mp3|mp4|wav|zip|drawio|excalidraw` 才按附件查文件——否则页名里的 `.10`（`…与例5.10`）会被当成后缀误报"图片未找到"；笔记链接允许写 `[[页名.md]]`，比对时去掉 `.md`。
- **全库三个笔记区均已体检（2026-09-22 收尾）**：高数、线代、`控制理论` 跑 `check-math-format.py` 都是 **0 渲染级**。`控制理论` 那轮（commit `67ed84b`，27 篇）修的是 16 处长行 + 6 处定界符 + 66 处补空行。
  **批量写文件后必查行尾**：`\n` 总数 − `\r\n` 数 > 0 说明混进裸 LF（本次 9 个文件 13 处），规范化办法
  `b.replace(b'\r\n',b'\x00').replace(b'\n',b'\r\n').replace(b'\x00',b'\r\n')`，**只对本次改动的文件做**。
  **根因（2026-09-22 又踩一次，务必记住）**：`open(p, encoding='utf-8').read()` 默认 universal newlines，
  **会把 `\r\n` 悄悄转成 `\n`**，所以「读文本再判断 `'\r\n' in raw`」永远是 False → 整篇被写成 LF。
  正确写法：判断行尾必须**二进制读** `nl = '\r\n' if b'\r\n' in open(p,'rb').read() else '\n'`，
  或 `open(p, newline='')` 保留原行尾。写完再复查一次裸 LF 计数为 0。
  断行后要**复测一次行长**——`05 第5章`、`06-1-b` 都是断完仍偏长、补了二次断行。

## 线性代数附录里的相似专题（2026-09-22 合并后）
- **只有一篇**：`附录 相似变换的传递与不变量`（345 行）。结构：`## (1)—(5) 问题 A 传递`（运算保持相似的总表、换基视角、再接一次相似变换、乘积与混合、加法反例＋移位例外）、`## (6)—(9) 问题 B 不变量`（保持/不保持清单、特征向量搬运、相似与合同分工、对称性判据 $PP^{T}A=APP^{T}$ 与反例）、`## (10) 与二次型题的联系`。
- 原 `附录 相似变换的不变量.md` 已删除（2026-09-22），引用点已改写到存续页。

## 线性代数的现行结构（2026-09-22 体检后，commit `c3a85f0`）
- 目录 93 篇；**无一篇超过 250 行**，6 篇 `single-exercise: true` 的单题页（`05-2-c` 299 / `05-2-e` 308 / `06-2-a` 279 / `06-3-a` 444 / `06-3-b` 379 / `06-5-c` 400）按约定豁免。
- 2026-09-22 新拆出的族：`02-2-a 例题·伴随矩阵五题`、`02-3-a 例题·矩阵的秩与矩阵方程`、`03-1-1-a 判别定理 1—4`＋`03-1-1-b 判别定理 5—7 与易错辨析`（`03-1-1` 改为总览入口）、`04-2-2-c 习题4.7·用行列式翻译参数条件`、`06-4-e 例题·无平方项与初等变换法`。
- 体例：线代**例题有两种合法写法**——`#### ✏️ 例题N：标题` 小节式（35 篇）与 `> [!example] ✏️ 例题N` callout 式（54 篇），AGENTS.md 明确两者都允许，**不要互相转换**。
- abstract 标题统一为「本讲定位」（原 6 篇「本页定位」已改）。
