# 2026-09-20

## 2000 数二 第19题（原卷十一）第 (Ⅱ) 问解析扩写

- `数学\考研数学二\2000年考研数学二答案与解析.md`：把 4 行的「由初值及牛顿—莱布尼茨公式」扩成六步——① 凑齐初值 $f(0)=1$ 与导函数 $f'$；② 牛顿—莱布尼茨 $\int_0^x f'(t)\mathrm dt=f(x)-f(0)$ 建式；③ 在 $t\ge0$ 上逐点比较被积函数 $0<\frac{\mathrm e^{-t}}{t+1}\le\mathrm e^{-t}$；④ 定积分保序；⑤ 算出 $\int_0^x\mathrm e^{-t}\mathrm dt=1-\mathrm e^{-x}$；⑥ 用 1 减三端、不等号反向。另加 `[!tip]`：上界由单调性白送，下界才是这条路的产物。
- 用户追问「为什么不能直接写成对 $f'$ 的 $0$ 到 $x$ 的积分」「加 $C$ 代值可以吗」→ 补一条 `[!note]`：不定积分加 $C$ 与变上限积分加 $f(0)$ 是同一件事，$C$ 只能由 $f(0)=1$ 定出；取 $F(x)=-\int_0^x\frac{\mathrm e^{-t}}{t+1}\mathrm dt$ 时 $f(0)=F(0)+C=C=1$。
- 提交：`673cbbf`（扩写）、`277b96f`（补 note）。**两个 commit 只落在本地**：`git push` 连续三次被 GitHub 443 `Connection reset` 挡回，待网络恢复补推。

## 打通「笔记 → PDF」的同步链

- 用户指定工程 `C:\Users\23720\Documents\Codex\2026-09-06\2000-2026-md-pdf-github-obsidian-2`，要求「pdf 也要」（细节已记入用户级 MEMORY.md）。
- 排查结论：27 年解析里**只有 2000 年**与工程不一致（其余与 obsidian 逐字节相同），试题 27 份全一致。
- 新写 `work\from_obsidian.py`（obsidian → 备份 → outputs → xelatex → work/review_repo）；给 `work\build_answers.py` 的转换器补三处 Obsidian 语法支持：callout `> [!note] 标题` → 粗体小标题＋正文、wikilink 只留显示名、散文里的裸希腊字母自动进数学模式（此前 π 触发 `Missing character` 编译失败）。
- 重编成功：2000 年册 21 题 / 28 页 / 无 Overfull / 无缺字；用 PyMuPDF 渲染第 24–26 页看图确认真插图与版式正常，callout 与「错题：」行渲染为普通文字，无 wiki 残留。

## 定位更正（用户当日澄清）

- 用户明确：**`Codex\...-2` 只是给助手出 PDF 用的过程文件，`outputs\` 不用管；重点只在 `D:\obsidian`**。故 GitHub / OneDrive `Mathematics (II)` / `publish_year.py` / `sync_obsidian.py` 一律不动，先前"要不要发布"的问题作废。
- `work\from_obsidian.py` 随之改成**默认不碰 `work\review_repo`**（复制进发布镜像需显式加 `--to-repo`），输出只报 PDF 路径；改后复跑验证正常（21 题 / 28 页）。

## 新建 2000-19 错题卡

- `数学\错题本\2000\2000-19 变上限积分定上下界.md`（78 行，错题卡样式）：错因按用户自述记「第 (Ⅱ) 问一心想把 $f$ 积出来 → 积不动卡住；没认出牛顿—莱布尼茨；看答案还想直接求导；没考虑常数（初值）」。`cause: [方法不会, 概念不清]`，`mastery: 1`，`review` 排 09-20/21/23/27/10-20。
- 同型方法挂到 [[11-2 积分不等式]]「方法五：牛顿-莱布尼茨公式」（把 $f(b)-f(a)=\int f'$ 再放缩，与本问完全同路），方法挂 [[09-2 定积分的计算技巧]] §9.5 与 [[08-4 变限积分与反常积分]]。
- 三处联动已做：解析页 2000.19 题头下加错题链接、总览手写索引加一行。
- 校验：`check-mistake-cards.py` 0 错误（另提示既有 2000-15 卡 108 行超限，非本次引入）；`check-notes.ps1` 三个文件全通过。
- commit `15027c3`，**连同先前被网络挡回的 `673cbbf`、`277b96f` 一并推送成功**（`47b5329..15027c3`），obsidian 侧已无待推提交。

## 新建 2000-16 错题卡（整题跳过）

- 用户自述：第 16 题（原卷八，由两个积分条件证两个零点）**直接跳过、没动笔**，理由是"感觉证明题我拿不住，直接放弃了"。`cause: [方法不会]`（只记主因，不掺时间因素），`mastery: 1`，`review` 排 09-20/21/23/27/10-20。
- `数学\错题本\2000\2000-16 两个零点存在性证明.md`（74 行）。卡的重点不是某一步算错，而是**"没开始"**：把证明题当靠灵感的题、漏掉"零点定理给出一个零点"的过程分、没把两个"＝0"的积分条件当构造线索。纠正的关键一步写成固定动作：**至少两个零点 ⟹ 反设只有一个 $c$，造 $g=f(x)(\cos x-\cos c)$——按条件是 $0$、按保序又不为 $0$**，并给出"不会也能先写前两步"的落笔顺序。
- 链接：[[05-2 函数作图与方程的根零点]] §5.5 · [[06-1 中值定理与辅助函数构造]] §6.3 · 同型真题 2017.19 / 2015.19 · 专题 3.4 零点、驻点与交点的个数。
- 三处联动已做（解析页 2000.16 下加链接、总览索引加一行）；`check-mistake-cards.py` 0 错误、`check-notes.ps1` 三文件通过。commit `d8bef67` 已推送。
- 观察：用户对**证明题**有明显畏难（"拿不住就放弃"），2000 卷的证明题是 16（零点存在性）与 19(Ⅱ)（不等式）。后续复盘可留意是否需要补一套「证明题清单」类专题。

## 错题本增设 `mastery: 0` 档（用户提议）

- 用户提议："mastery 加一个 0 档，相当于我跳过的，不需要复习？"——采纳「区分跳过 / 做错」的动机，但把「不需要复习」改为「**暂不排复习节点、欠账进独立看板**」（跳过的题恰是最该补的，不能凭空消失）。
- 三处同步改动（commit `9387a75`）：
  1. `数学\错题本\01 错题本使用规范.md`：`mastery` 表加 `0` 行、字段表取值改为 `0（跳过未做）→1→2→3`、使用流程第 4 步补 0 档说明，并新增一整个 `[!note]` 讲 0 档用法（不排节点 / 进「跳过挂起」/ 补完方法改 1 再排 5 节点 / 0 档 = 挂起不是删除）。
  2. `数学\错题本\00 错题本总览.md`：新增「跳过挂起（`mastery: 0`）」dataview 表（`WHERE mastery = 0`），并在「尚未掌握（全部）」下加注「剩余节点 = 0 即 0 档」。
  3. `.scripts\check-mistake-cards.py`：允许 `mastery: 0`；`review` 仅 0 档可空；0 档跳过「勾框 ↔ review」一致性检查与"无可勾选日期项"提示。
- 验证：造了一份 `2000-99` 临时样例（0 档 + 空 review + 「复习记录」改挂起说明）跑脚本 → 0 错误 0 提示，验完已删除。
- ⚠️ 踩坑：文案里写「跳过**待补**」被 `check-notes.ps1` 的占位符检查判为「含 TODO/FIXME/待补」→ 改名「跳过**挂起**」后通过（已写入项目 MEMORY.md）。
- 现有 14 张卡未改档：2000-16 虽属"跳过"，但已配完整卡与 5 个节点，暂留 `mastery: 1`（是否改 0 待用户定）。

## 0 档定位修正：从「跳过未做」改为「跳过／主动放弃」（commit `cdce576`）

- 用户澄清："**我只想跳过证明题的**，这个不考 130 以上貌似不会也没啥"——即 0 档是**战略性取舍**，不是"暂时没能力、以后补"。据此把 0 档语义整条改写。
- 三处文案同步：规范里 0 档说明改为「本轮不打算拿这几分，不排复习节点」；`cause` 取值**新增 `主动放弃`**（统计"故意丢了多少分"）；总览「跳过挂起」表描述改为"主动放弃、目前主要是证明题"。
- **新增「弃题判据」表**（规范 `[!tip]`）：可弃＝需自造辅助函数/反证的技巧型证明；慎弃＝外壳是零点定理、变上限积分求导、保序性、单调性的题（选择题/填空题照考）；底线＝弃题也要写到"白送的那一步"。判据一句话：**这题的方法换一道选择题还会不会用？会 → 不该进 0 档。**
- `2000-16` 改为 `mastery: 0` + `cause: [主动放弃]` + `review: []`，「⏱️ 复习记录」换成挂起说明（「想收回就把 mastery 改 1 再排 5 个节点」），卡内文案从"畏难"改写为"取舍的边界"；解析页错题行与总览索引行同步。卡仍 69 行。
- 校验：`check-mistake-cards.py` 0 错误（「跳过挂起」表首次有内容）；`check-notes.ps1` 四文件全通过。已推送。
- 策略已写入用户级 `~/.workbuddy/MEMORY.md`（**不追求 130+，技巧型证明题可主动放弃**，但慎弃与"白送的一步"两条底线必须守）。
