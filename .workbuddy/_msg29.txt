docs: 方法页补「考点速记」块（自控第2章 余 2 篇，整章补完）

- `02-2 元部件、电路与机械建模`：机械-电路相似系统、齿轮系折算口诀（惯量/摩擦按 $i^2$、
  转矩按 $i$）、电路建模两法（复阻抗法与分压式）、运放（虚短虚断的前提与两个易错）、
  负载效应（电位器 $R_l\ge10R_p$、**两级 RC 直接串联分母多出 $R_1C_2s$**、三条消除办法）
- `02-6 典型系统模型·常用元部件与过程控制`：常用元部件传函表、电枢控制直流电动机三方程
  与忽略 $L_a$ 的一阶模型（扰动通道为负）、单容水槽线性化与液阻、电加热炉（**与单容水槽同构**）、
  无耦合双容水槽（**稳态增益只剩末槽**、有耦合须重列方程）

`02-2` 原为 246 行，加 7 行会过 250 线；删掉与正文「② 两级 RC 直接串联」**完全重复**
的应试提示 warning 块（3 行）后为 250 行。两篇现均 250 行（未触发篇幅提示）。

至此自控第 2 章（6 篇）整章补完。

累计进度：179 篇 method 页完成 94 篇（53%）。

校验：`控制理论` check-math-format 0 处；check-notes.ps1 全库 0 致命。
