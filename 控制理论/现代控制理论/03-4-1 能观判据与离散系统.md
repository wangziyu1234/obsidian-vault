---
create: 2026-09-07
modify: 2026-09-07
tags: [知识点, 现代控制理论, 公式速查]
---

> 返回：[[03-4 能观性与离散时变系统判别]] | 返回目录：[[现代控制理论]]

# 第 3 章 能观判据与离散系统

> [!abstract] 本讲定位
> 能观性的秩、PBH和格拉姆判据，及离散系统判别例题。

## 3.3 能观性的定义与判别

$$
\boxed{Q_o = \begin{bmatrix}C\\ CA\\ \vdots\\ CA^{n-1}\end{bmatrix}},\qquad
\text{完全能观} \ \Leftrightarrow\ \operatorname{rank}Q_o = n
$$

- 对角型直接法：$C$ 中对应列全零 ⇔ 该模态**不能观**

### 3.3.1 PBH 判据

- 能观：

$$
\operatorname{rank}\begin{bmatrix}\lambda I-A\\ C\end{bmatrix}=n
$$

（对所有特征值 $\lambda$）

与能控侧（§3.2.1）**对偶**：仅需对特征值 $\lambda_i$ 判秩；

$$
\begin{bmatrix}\lambda_i I-A\\ C\end{bmatrix}
$$

缺秩 ⇔ $\lambda_i$ 为**不能观**模态；可由 $C$ 的零列直接读出；$\operatorname{rank}Q_o=n$（$[C; CA; \cdots]$）与 PBH **充要等价**。

### 3.3.2 格拉姆矩阵判据

**能观格拉姆**（对偶对称）：

$$
W_o=\int_0^\infty e^{A^Tt}C^TCe^{At}\,dt
$$

满秩 ⇔ 完全能观；满足李雅普诺夫方程 $A^TW_o+W_oA=-C^TC$。

**与秩判据等价**：$W_o$ 非奇异 ⟺ $\operatorname{rank}Q_o=n$；格拉姆**数值上更稳**（对偶性质与能控侧对称，见 §3.2.2）。

### 3.3.3 能观判别例题

> [!example] ✏️ 示例
>
> **题目**：
>
> $$
> A=\begin{bmatrix}0&1\\-2&-3\end{bmatrix}
> $$
>
> $C=[1\ 0]$，判能观性
>
> **秩判据**：
>
> $$
> \begin{aligned}
> Q_o &=\begin{bmatrix}C\\ CA\end{bmatrix}\\[4pt]
> &=\begin{bmatrix}1&0\\ [1\ 0]\begin{bmatrix}0&1\\-2&-3\end{bmatrix}\end{bmatrix}\\[4pt]
> &=\begin{bmatrix}1&0\\0&1\end{bmatrix}
> \end{aligned}
> $$
>
> $\det(Q_o) = 1 \neq 0$，$\operatorname{rank}Q_o=2=n$ → **完全能观** ✓
>
> **对角型验证**（互异特征值 $\lambda_1=-1,\lambda_2=-2$）：对角化后
>
> $$
> A_d=\begin{bmatrix}-1&0\\0&-2\end{bmatrix}
> $$
>
> $$
> B_d=P^{-1}B=\begin{bmatrix}2&1\\-1&-1\end{bmatrix}\begin{bmatrix}0\\1\end{bmatrix}=\begin{bmatrix}1\\-1\end{bmatrix}
> $$
>
> $$
> C_d=CP=[1\ 0]\begin{bmatrix}1&1\\-1&-2\end{bmatrix}=[1\ 1]
> $$
>
> $B_d$ 无零行 → 两模态均能控；$C_d$ 无零列 → 两模态均能观 ✓

> [!example] ✏️ **例：含不能控模态的系统**
>
> $$
> A=\begin{bmatrix}-1&0\\0&-2\end{bmatrix}
> $$
>
> $$
> B=\begin{bmatrix}1\\0\end{bmatrix}
> $$
>
> $C=[0\ 1]$
>
> $$
> Q_c=[B\ AB]=\begin{bmatrix}1&-1\\0&0\end{bmatrix}
> $$
>
> $\operatorname{rank}Q_c=1<2$ → **不完全能控**
>
> $$
> Q_o=\begin{bmatrix}C\\CA\end{bmatrix}=\begin{bmatrix}0&1\\0&-2\end{bmatrix}
> $$
>
> $\operatorname{rank}Q_o=1<2$ → **不完全能观**
>
> 对角型直接看：$B$ 第 2 行 = 0 → $\lambda_2=-2$ **不能控**；$C$ 第 1 列 = 0 → $\lambda_1=-1$ **不能观**。

---

## 3.4 离散时间系统的能控性与能观性

离散系统 $x(k+1)=Gx(k)+Hu(k),\ y(k)=Cx(k)$，能控性/能观性判别与连续系统完全平行（用 $(G,H,C)$ 代 $(A,B,C)$）：

- **能控性**：判别矩阵 $\operatorname{rank}M_c=n$，其中

$$
M_c=[H\ \ GH\ \ G^2H\ \cdots\ G^{n-1}H]
$$

- **能观性**：判别矩阵 $\operatorname{rank}M_o=n$，其中

$$
M_o=\begin{bmatrix}C\\CG\\\vdots\\CG^{n-1}\end{bmatrix}
$$

> [!example] ✏️ 示例
>
> **题目**：例：
>
> $$
> G=\begin{bmatrix}0&1\\-0.06&0.5\end{bmatrix}
> $$
>
> $$
> H=\begin{bmatrix}0\\1\end{bmatrix}
> $$
>
> $$
> M_c=[H\ \ GH]=\begin{bmatrix}0&1\\1&0.5\end{bmatrix}
> $$
>
> $\operatorname{rank}=2$ ⟹ 能控（与 2.4 节的 $G(z)$ 对偶）；$C=[1\ \ 0]$ 时
>
> $$
> M_o=\begin{bmatrix}1&0\\0&1\end{bmatrix}
> $$
>
> 满秩 ⟹ 能观。

---
