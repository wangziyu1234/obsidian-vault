---
create: 2026-09-07
modify: 2026-09-09
tags: [知识点, 现代控制理论, 公式速查]
---

> 返回：[[03-2 能控能观结构特性]] | 返回目录：[[现代控制理论]]

# 第 3 章 对偶原理与能控标准型

> [!abstract] 本讲定位
> 对偶原理、标准型对照及能控标准Ⅰ型的变换例题。

## 3.6 能控性与能观性的对偶关系

**对偶系统**：$\Sigma=(A,B,C)$ 与

$$
\tilde\Sigma=(\tilde A,\tilde B,\tilde C)=(A^T,\,C^T,\,B^T)
$$

互为对偶（输入输出角色互换、矩阵转置）。若有 $D$，对偶取 $D^T$；相应传函为 $\widetilde G(s)=G(s)^T$。**对偶不是一般的状态坐标变换**，MIMO 时输入输出维数也可能交换。

$$
\boxed{(A,B,C)\ \text{能控} \ \Leftrightarrow\ (A^T,C^T,B^T)\ \text{能观}}
$$

> [!derivation] 由判别矩阵验证对偶关系
>
> **互证**（秩一眼看穿）：
>
> $$
> \begin{aligned}
> \tilde Q_c &=\bigl[\tilde B\ \ \tilde A\tilde B\ \cdots\ \tilde A^{n-1}\tilde B\bigr]\\[4pt]
> &=\bigl[C^T\ \ A^{T}C^{T}\ \cdots\ (A^{n-1})^{T}C^{T}\bigr]\\[4pt]
> &=Q_o^{T}
> \end{aligned}
> $$
>
> 转置不改变秩 ⟹
>
> $$
> \operatorname{rank}\tilde Q_c=\operatorname{rank}Q_o
> $$
>
> 对偶成立。同理 $\tilde Q_o=Q_c^T$。

**推论与应用**：

- $(A,B,C)$ 能观 ⟺ 其对偶系统能控；求反馈增益 $K$ 与求观测器增益 $L$ 可对偶处理（阿克曼公式及其对偶式，见 [[05-1 状态反馈与观测器]] §5.2.1）；
- 对角/约当型的判别技巧也在对偶下互换（模态"看得见 vs 够得着"）。

---

## 3.7 状态空间表达式的能控标准型与能观标准型

| 标准型 | $A$ 结构 | $B$ / $C$ 位置 |
|:--|:--|:--|
| **能控标准型** | 友矩阵 $A_c$（末行 $[-a_0\ -a_1\cdots]$） | $B_c=[0\cdots0\ 1]^T$ |
| **能观标准型** | $A_o = A_c^T$ | $C_o=[0\cdots0\ 1]$ |

由传函数直读实现：

$$
G(s)=\frac{\beta_{n-1}s^{n-1}+\cdots+\beta_0}{s^n+a_{n-1}s^{n-1}+\cdots+a_0}
$$

$$
\Rightarrow
$$

$$
C_c = [\beta_0\ \beta_1\ \cdots\ \beta_{n-1}],\ D=0
$$

> [!note]
> 分子、分母同阶时先长除分离出 $D$；非真有理传函不能用普通有限维状态方程直接实现。
>
> 本表能控形式为教材Ⅰ型，其转置对应能观Ⅱ型。四种命名、坐标方向及分子直读规则见 [[03-2-3 四种标准型变换速查]]。
>
> 本节为速查对照表：变换矩阵如何具体构造见 [[控制理论/现代控制理论/01-3 线性变换与传递函数阵|01-3]] §1.5.2；四种标准型的模拟结构图与直读例题见 [[控制理论/现代控制理论/01-1 状态变量与模拟结构图|01-1]] §1.2.1–1.2.5（按库约定内容只留一处）。

> [!example] ✏️ 笔记例3.12 能控标准Ⅰ型变换（自设例，非教材同号原题）
>
> **题目**：将状态空间表达式
>
> $$
> \dot x = \begin{bmatrix}2&1&0\\0&2&1\\0&0&1\end{bmatrix}x + \begin{bmatrix}0\\0\\1\end{bmatrix}u
> $$
>
> $y=[1\ \ 0\ \ 0]x$ 变换成能控标准Ⅰ型。
>
> **解**：
>
> **(1) 判别能控性**
>
> $$
> M = [b\ \ Ab\ \ A^2b] = \begin{bmatrix}0&0&1\\0&1&3\\1&1&1\end{bmatrix}
> $$
>
> $\operatorname{rank}M=3=n$ → 系统能控，可变换为能控标准型。
>
> **(2) 计算特征多项式**
>
> $$
> \begin{aligned}
> \det(sE-A)&=\begin{vmatrix}s-2&-1&0\\0&s-2&-1\\0&0&s-1\end{vmatrix}\\
> &=(s-2)^2(s-1)\\
> &=s^3-5s^2+8s-4
> \end{aligned}
> $$
>
> 故 $a_2=-5$，$a_1=8$，$a_0=-4$。
>
> **(3) 构造变换矩阵**
>
> 取新状态 $\bar x=T_cx$。Ⅰ型不能只把 $[b,Ab,A^2b]$ 倒序排列，还须补偿特征多项式系数：
>
> $$
> \begin{aligned}
> T_c^{-1}&=[A^2b+a_2Ab+a_1b\ \ Ab+a_2b\ \ b]\\
> &=\begin{bmatrix}1&0&0\\-2&1&0\\4&-4&1\end{bmatrix}
> \end{aligned}
> $$
>
> $$
> T_c=(T_c^{-1})^{-1}=\begin{bmatrix}1&0&0\\2&1&0\\4&4&1\end{bmatrix}
> $$
>
> **(4) 计算变换后的矩阵**
>
> $$
> \bar A = T_c A T_c^{-1} = \begin{bmatrix}0&1&0\\0&0&1\\4&-8&5\end{bmatrix}
> $$
>
> $$
> \bar b = T_c b = \begin{bmatrix}0\\0\\1\end{bmatrix}
> $$
>
> $$
> \bar c = c T_c^{-1} = [1\ \ 0\ \ 0]\begin{bmatrix}1&0&0\\-2&1&0\\4&-4&1\end{bmatrix} = [1\ \ 0\ \ 0]
> $$
>
> **能控标准Ⅰ型**：
>
> $$
> \dot{\bar x} = \begin{bmatrix}0&1&0\\0&0&1\\4&-8&5\end{bmatrix}\bar x + \begin{bmatrix}0\\0\\1\end{bmatrix}u
> $$
>
> $$
> y = [1\ \ 0\ \ 0]\bar x
> $$
>
> **传递函数**（直读）：
>
> $$
> W(s) = \frac{1}{s^3-5s^2+8s-4} = \frac{\beta_0}{s^3+a_2s^2+a_1s+a_0}
> $$
>

---
