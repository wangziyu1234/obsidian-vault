# AGENTS.md — 给 AI 助手看的长期记忆与操作规范

> 本文件是仓库的「长期记忆 + 操作手册」。任何会话编辑本库笔记前，请先通读「笔记规范总纲」与「检查与修改流程」；文末「历史记录」记载历次审阅、拆分、检查的全部结论与已修复问题，避免重复踩坑。本文件自身不参与笔记检查。

## 仓库概况与目录地图

- 路径：`D:\obsidian\`（**git 仓库根目录**，Obsidian 库即本仓库内容）
- Git：`main` 分支，远程 `git@github.com:wangziyu1234/obsidian-vault.git`；不依赖自动备份脚本，但 **AI 助手每次修改完必须主动 `git add -A` → commit → push**，与 `origin/main` 保持同步
- 提交信息：中文、按用途加前缀——`fix:` 修错误 / `refactor:` 结构调整 / `docs:` 笔记内容 / `chore:` 杂项清理
- 笔记主体：**自动控制原理**（24 个 .md，含 `现代控制理论\` 5 个分章 + 2 个第 6 章专题）+ **数学**（50 个 .md：高等数学 15 讲 + `01-1~01-5`/`06-1~06-2`/`08-1~08-4`/`09-1~09-3`/`15-1~15-3` 共 17 个专题 + 7 个附录 + 线性代数 8 个 + 2 张速查表 + 入口 `数学.md`）
- 活跃 .md 共 **94 个**（含本文件）；`附件\` 现有 **74 张 png + 1 张 jpg**（仅 `频域-典型幅相曲线.jpg` 被引用）

### 目录地图（决定扫描边界）

| 目录 | 内容 | 检查时怎么处理 |
| --- | --- | --- |
| `数学\` | 高数 15 讲、08 讲专题 4 件、附录 7 个、线代 00–07、速查表、入口 | 全部检查 |
| `自动控制原理\` | 第 1–12 章、`03-1~03-3` 专题、`00 拉普拉斯`、`现代控制理论\` 01–05、入口 | 全部检查 |
| `附件\` | 图片素材（png 为当前格式） | 校验嵌入命中；jpg 应只剩 `频域-典型幅相曲线.jpg` 一张 |
| `templates\` | 3 个模板 | 只查跨文链接，模板占位符不算问题 |
| `Excalidraw\` | 1 个绘图文件（JSON 正文） | 基本不查 |
| `copilot\copilot-custom-prompts\` | 15 个插件 prompt | 不是笔记：无 H1、无链接都正常 |
| `_moved_out\` | 归档区（gitignore）：全库备份 + `冗余jpg_未引用_2026-08-14\` | 不检查、不进 git；待删素材移这里，不物理删除；**必须在 Obsidian 排除**（`app.json` 的 `userIgnoreFilters`），否则备份与活跃文件同名会让 basename 链接解析歧义（曾致第 9 章入口失效） |
| `.venv\` `.obsidian\` `.git\` `.trash\` | 环境 / 会话状态 / 回收站 | 不检查 |

## 笔记规范总纲

### 文件命名
- 章节/讲次：`NN 第N章（第N讲） 描述.md`，前缀两位数字连续
- 附录：不带数字前缀，直接 `附录 XXX.md`
- 专题拆分件：`NN-x 描述.md`，与总览同目录平铺（前缀排序相邻）

### 文件头部（强制顺序）
1. frontmatter：`create` / `modify` / `tags`
2. `> 返回目录：[[目录]]`（专题件为 `> 返回：[[总览文件名]] | 返回目录：[[目录]]`）
3. `# H1`
4. `> [!abstract] 本讲定位`

- 笔记文件**有且仅有一个 H1**；讲义内部细分可用 `###`，**附录不用 H3**（历史遗留 1 处：`附录 图像变换` 的 `### 一般对称性的代数判别`，可不动）
- 既有稳定风格勿改：高数 05–12 讲文件名无破折号、H1 含 `——`；15 讲 H1 为「微分方程（常微分方程）」

### 小节标题
- 章节小节：`## 图标 X.Y 标题`（标题前缀图标沿用各章现有样式，X 章号、Y 小节号，**连续不重号**）；每章末 `## X.N 解题套路`
- 附录方法序列可用 `## (n) 名称`；两大块式附录参考 `附录 变形技巧.md`：`## 总览`（表格 + 锚点导航 `[[#标题\|别名]]`）→ `## 一、…` → `## 二、…` → `## 收尾`

### wikilink 与图片
- 跨文引用一律 `[[ ]]`；跨目录优先 `[[现代控制理论/02 状态空间表达式|别名]]` 短路径，**不用 `../` 相对路径**；引用附录写 `[[附录 XXX]]`
- wikilink **不要放进 `$$…$$`**（MathJax 不解析），放公式外或正文
- 图片：`![[文件名.png|尺寸]]`，正文图 `|430` 左右、速查大图 `|520` 左右
- **表格单元格内**的链接管道与图片尺寸必须转义：`[[目标\|别名]]`、`![[图.png\|220]]`。`\|` 是 Obsidian 表格解析的转义（渲染正常），**不是 bug，勿改**
- 附件图片统一 .png；清理冗余素材移到 `_moved_out\冗余xxx_YYYY-MM-DD\`，不物理删除

### LaTeX
- 行内 `$…$`、块级 `$$…$$`；**`$$` 必须成对**（曾出现 `$$` 开、`$` 闭的渲染 bug）
- `\begin/\end`、`\left/\right` 必须配对；`\leftrightarrow` 箭头含 `\left` 子串，会让 `\left/\right` 计数检查**误报**
- 微分方程系数下标：胡寿松教材约定，$a_n$ 为 $c^{(n)}$ 系数（最高阶），$a_0$ 为 $c$ 系数
- 观测器极点倍数统一「2~5 倍」；MATLAB 用函数句柄 `@fun`，不用字符串传函数
- 跨章主线符号一致：$r(t)$ 参考输入、$e=r-b$ 偏差、$\zeta$ 阻尼比、$\omega_n$ 自然频率、$A^T$ 转置、$E$ 单位阵、$r(A)$ 秩

### 拆分约定（笔记 >400 行时）
- **入口模式**：原文件保留为总览入口（文件名不变、现有链接不断），正文移入同目录 `NN-x` 专题文件
- 已拆分：第 9 章（总览化，正文指向 `现代控制理论\01–05`）、第 3 章（总览 + `03-1/2/3`）、第 8 讲（总览 + `08-1/2/3/4`）、高数 09 讲（总览 + `09-1/2/3`）、高数 01 讲（总览 + `01-1/2`，其中 `01-2` 再拆 `01-3/4/5`）、自控第 6 章（总览 + `06-1/2`）、高数 06 讲（总览 + `06-1/2`）、高数 15 讲（总览 + `15-1/2/3`）
- 总览样式参照 `09 第9讲`：frontmatter + 返回行 + H1 + abstract（注明「已拆分…总览入口」）+ `## 🗂️ 分章导航` 表格 +（可选）解题流程；专题件头部 `> 返回：[[总览文件名]] | 返回目录：[[目录]]`

## 检查与修改流程

### 检查清单（收到「检查笔记」任务时逐项执行）
1. 先 `git status` + `git log` 看增量；扫描边界按「目录地图」
2. **断链**：所有 `[[ ]]` 与 `![[ ]]` 目标必须命中（链接按 basename / 短路径解析；表格内 `\|` 先按转义拆出目标再解析）
3. **LaTeX**：`$$` 成对、`\begin/\end` 配对、`\left/\right` 配对（剔除 `\leftrightarrow`）
4. **编号**：`## X.Y` 小节号无重复；无 TODO/FIXME/待补/`??`；无空链接 `[[]]`
5. **结构**：唯一 H1、`> [!abstract]` 在 H1 之后、附录无 H3、无 >400 行文件（超了按拆分约定处理）
6. 检查脚本注意：`read` 工具 `limit` 上限 **2000**；输出中文注意 UTF-8 编码
7. **数学内容深度核对**：按科目分 7 组并行派子代理逐条推导复核（高数 01–08 讲 / 09–15 讲 / 附录与速查 / 线代 / 自控 00–06 章 / 07–12 章 / 现代控制理论），主代理对每一条报告**重新推导确认**后再修改，宁缺毋滥

### 已知误报与豁免（不要重复报告）
- `05 第5章` 表格内 `![[…png\|220]]`：转义正确，勿改
- `\leftrightarrow` → `\left/\right` 计数误报
- `copilot\copilot-custom-prompts\*.md` 无 H1 正常；`AGENTS.md` 自身、`_moved_out\`、`.venv\` 不参与检查
- `_moved_out\` 内的备份与活跃文件**同名**：必须在 Obsidian「设置 → 文件与链接 → 排除文件」里排除 `_moved_out/`（已写入 `.obsidian/app.json` 的 `userIgnoreFilters`），否则 `[[现代控制理论/01 绪论]]` 这类短路径链接会因重名解析失败

### 修改流程
- 修改/重命名前先 `grep` 检查跨文件引用，避免断链；重命名用 PowerShell `Move-Item -LiteralPath`（路径含中文与括号）
- 每次修改完主动 commit + push（中文提交信息，前缀见「仓库概况」）
- **自绘图**：写 GDI+（System.Drawing）PowerShell 脚本生成 PNG 存 `附件\`（命名沿用 `自控-xxx.png`/`高数-xxx.png`），嵌入 `![[...png|430]]`；本模型不能读图，用**像素抽样**自检（关键元素位置取色验证）后再入库
- **git push 与沙箱**：受限沙箱下 `git push` 会因 ssh.exe 无法创建 signal pipe 而失败（Win32 error 5）。优先直接执行；只有沙箱拦截且会话允许带权限重试（审批策略不是 never）时，才用 `sandbox_permissions` 重试同一条命令；**审批策略为 never 时不要设置 `sandbox_permissions`**（会被自动拒绝），改为在回复中说明失败原因

## 历史记录

以下为历次会话的完整记录，供回溯参考；其中结论已沉淀到上方的规范总纲与误报清单。

## 笔记审阅记录（2026-08-05）

唯一一次系统扫描三类笔记（共 38 个 .md）。**所有问题已修复**，未做任何破坏性改动（未删除文件，仅编辑与重命名）。

### 已修复的问题

**高严重度（3 项）**

1. `自动控制原理\07 第7章 线性离散系统的分析与校正.md`
   - 原 `## 7.2 采样与保持`（line 24）与 `## 7.2 z 变换`（line 52）小节编号重复，导致 7.2→7.8 整章错位
   - 已重排为 7.1→7.9（7.2 采样与保持 / 7.3 z 变换 / 7.4 差分方程 / 7.5 稳定性 / 7.6 稳态误差 / 7.7 数字 PID / 7.8 最少拍 / 7.9 解题套路）
   - 同步更新文件内 3 处交叉引用（7.4→7.5、7.5→7.6、7.7→7.8）

2. `数学\线性代数\04 线代第4讲 线性方程组.md` line 66–67 例 3
   - 原通解错误：特解 `(0,1,0)^T`、基础解系 `(1,-1,1)^T` 代入均不成立
   - 已改正：方程组第二式减去 2 倍第一式得 $x_2=x_3$、$x_1=1-2x_3$；特解 $\eta^*=(1,0,0)^T$，基础解系 $\xi=(-2,1,1)^T$

3. 高数附录命名冲突——3 个文件用 `10/11/12` 数字前缀混入讲次序列，已重命名：
   - `10 附录 三角与代数补充.md` → `附录 三角与代数补充.md`
   - `11 附录 常用曲线.md` → `附录 常用曲线.md`
   - `12 傅里叶级数.md` → `附录 傅里叶级数.md`
   - 同步更新引用：`数学\高等数学公式速查.md`、`04 第4讲…`、`09 第9讲…`、`10 第10讲…`、`自动控制原理\10 附录 常用数学补充.md`

**中严重度（6 项）**

4. `自动控制原理\02 第2章 控制系统的数学模型.md`
   - 微分方程系数下标原为 $a_0$ 最高（与第 1 章相反），改为教材约定 $a_n$ 最高：
     - L15：`a_n c^{(n)} + … + a_0 c = b_m r^{(m)} + … + b_0 r`
     - L23–24：传函分子分母下标颠倒对应
     - L43：$K^*=\dfrac{b_m}{a_n}$（原 $b_0/a_0$）
     - L49：$K=\dfrac{b_0}{a_0}$（原 $b_m/a_n$，时间常数形式）

5. `自动控制原理\04 第4章 线性系统的根轨迹法.md` 补 H2 标题
   - 4.1 根轨迹方程（line 9 后加）
   - 4.2 绘制法则（line 44 后加）
   - 4.3 零极点对系统性能的影响（line 61 后加）
   - `解题套路` 编号为 4.5

6. 高数 13/14/15 讲 callout 顺序调整：原 callout 在 H1 之前，统一改为 H1 在前、callout 在后
   - `13 第13讲 多元函数微分学.md`、`14 第14讲 二重积分.md`、`15 第15讲 微分方程.md`

7. `数学\高等数学\09 第9讲 一元函数积分学的计算.md` line 250
   - 原 wikilink `[[14 第14讲 二重积分]]` 写在 `$$…$$` 内 `\text{}` 中，MathJax 不解析
   - 已移出公式：公式内只留「概率积分」字样，公式下方加引用块给出链接

8. `数学\高等数学\11 第11讲…积分等式与积分不等式.md` line 126
   - 「张宇第14讲技巧」改为「张宇第11讲技巧」（本讲即第11讲）

9. `数学\线性代数\07 线代第3讲 向量组·内积与正交.md` H1
   - 原 `# 线代第3讲 向量组 · 内积与正交` 与 `03` 同名，改为 `# 线代第3讲（续）向量组 · 内积与正交`

**低严重度（9 项）**

10. `自动控制原理\09 第9章 线性系统的状态空间分析与综合.md`
    - L391 / L522 观测器极点倍数「3~5 倍」统一为「2~5 倍」（与 L405 callout 一致）

11. `自动控制原理\11 MATLAB 基础速查.md` L38
    - `ode45('fun', t, x0)` 改为 `ode45(@fun, t, x0)`（推荐函数句柄）

12. `自动控制原理\12 资料索引（OneDrive）.md` L104
    - `现代控制理论/02` 代码段改为 wiki 链接 `[[现代控制理论/02 状态空间表达式|2.1 建模]]`

13. `数学\高等数学\13 第13讲 多元函数微分学.md` L71
    - `\boxed{AC-B²}\begin{cases}…` 改为整体入框 `\boxed{\,AC-B²\ \begin{cases}…\end{cases}\,}`

14. 高数 3 处附录图加尺寸参数 `|520`：
    - `附录 三角与代数补充.md` L15
    - `附录 常用曲线.md` L18、L32

15. `数学\高等数学\附录 常用曲线.md` L14 重复句删除
    - 原 callout 与正文首段重复「对齐张宇《基础30讲》附录2…」一句，保留 callout 内

16. `数学\线性代数\02 线代第2讲 矩阵（矩阵与秩）.md` L21
    - 表头 `| 性质 | 性质 |` 改为 `| 公式 | 公式 |`

17. `数学\线性代数\05 线代第5讲 特征值与特征向量.md` L37
    - 三阶特征多项式 `\operatorname{tr}\,\lambda^{2}` 缺参，改为 `\operatorname{tr}(A)\,\lambda^{2}`

### 未改动（已确认无问题的项）

- 所有 `![[xxx.png]]` 图片引用全部命中（图片在 `附件\` 或同目录）
- 所有 `[[ ]]` wiki 链接无空指
- LaTeX 配对完整（`$/$$`、`\begin/\end`、`\left/\right`）
- 无 TODO / FIXME / 待补 / `??` 占位符
- 跨章主线符号一致：$r(t)$ 参考、$e=r-b$ 偏差、$\zeta$ 阻尼比、$\omega_n$ 自然频率、$A^T$ 转置、$E$ 单位阵、$r(A)$ 秩

### 故意未改（已是稳定一致的风格）

- 高数 05/06/07/10/11/12 讲：文件名无破折号、H1 含 `——` 破折号，6 文件统一
- 高数 15 讲：文件名 `微分方程`、H1 `微分方程（常微分方程）`，同上风格

## 笔记拆分记录（2026-08-09）

按用户要求「太长的笔记小拆」，拆分 3 个超长文件（>400 行）。采用**入口模式**：原文件保留为总览入口（文件名不变，现有 `[[链接]]` 全部不断链），详细内容移入同目录 `NN-x` 专题文件。

### 第9章 状态空间（550 行 → 约 60 行，总览化）
- 原文件是「合并版全文」，内容与 `现代控制理论/01–05` 分章文件一致 → 改为精简总览 + 9.9 题型速查 + 综合大题流程，正文全部指向 5 个分章文件
- 未新建文件；9.9 表格内引用已改为指向分章文件

### 第3章 时域分析（448 行 → 总览 + 3 专题）
- `03 第3章 线性系统的时域分析法.md`（总览）
- `03-1 一阶与二阶系统的时域响应.md`（3.1–3.3）
- `03-2 高阶系统、稳定性与稳态误差.md`（3.4–3.7）
- `03-3 解题套路与典型题型.md`（3.8–3.10 + 教材习题）
- 同步更新 `12 资料索引（OneDrive）.md` 中 4 处小节链接（3.3/3.5/3.6/3.7 → 对应专题）

### 第8讲 积分概念与性质（410 行 → 总览 + 4 专题）
- `08 第8讲 一元函数积分学的概念与性质.md`（总览）
- `08-1 不定积分的概念.md`
- `08-2 定积分的精确定义与性质.md`
- `08-3 数列极限化定积分（等分形式专题）.md`
- `08-4 变限积分与反常积分.md`

### 拆分约定
- 专题命名：`NN-x 描述.md`，与总览同目录平铺（前缀排序相邻）
- 专题头部：`> 返回：[[总览文件名]] | 返回目录：[[目录]]`
- 拆分后全文链接校验通过（60 个 .md 无断链）；`05 第5章` 中 `频域-奈氏-*.png\|220` 的 `\|` 转义为既有格式，图片均在 `附件\`

## 笔记检查记录（2026-08-14）

第二次全库扫描：78 个活跃 .md、243 条 wikilink、72 处图片嵌入、112 个图片资源。四项修复（用户确认后执行）：

1. `数学\高等数学\12 第12讲 一元函数积分学的应用（三）物理应用.md` L55：公式块以 `$$` 开、单个 `$` 闭（MathJax 渲染错误）→ 补为 `$$`
2. `数学\高等数学\15 第15讲 微分方程.md` L68：全库唯一 `../` 相对路径链接 → 规范为 `[[现代控制理论/03 状态方程的解]]`（与 `12 资料索引` 的 `现代控制理论/XX` 写法一致）
3. 14 个文件 callout/H1 顺序统一为「H1 在前、`> [!abstract]` 在后」：线代 00–07 共 8 个 + 高数附录 6 个（三角与代数补充/常用曲线/变形技巧/常用空间图形/双曲函数/图像变换）
4. 附件 40 个未被引用的 .jpg（与 .png 同图同尺寸的重复导出，约 1.9MB）→ 移至 `_moved_out\冗余jpg_未引用_2026-08-14\`（可恢复）；仅保留被第 5 章引用的 `频域-典型幅相曲线.jpg`

### 确认无问题（避免重复踩坑）

- `05 第5章` 表格内 `![[…png\|220]]` 的 `\|` 是 Obsidian 表格转义：表格解析消费反斜杠后单元格内容为 `|220`，图片正常缩放，**不是 bug，勿改**
- `\leftrightarrow` 箭头含 `\left` 子串，会让 `\left/\right` 计数检查误报
- `copilot\copilot-custom-prompts\*.md`（15 个）是插件 prompt，无 H1 属正常，不计入检查

### 遗留观察项

- ~~变形技巧 H1 当小节标题~~ → **已于 2026-08-14 规范化**（用户确认「怎么好看怎么来」）：`# 一、`/`# 二、` 降为 `## 📝 一、`/`## ⚖️ 二、`，`### 口诀` 并入 `## 📝 (13) 常用等式变形口诀`（口诀转表格），总览表加锚点导航；并修复 6 处数学错误（特殊值法 f(−1) 符号、取倒数递推、复合根式恒等式、除项乘项口诀、单调减放缩方向、高阶导相位）+ 3 处补全（积分估值 dx、$e^x-\ln x>2$ 备注、平方差精化式）；`高等数学公式速查` 同步改「13 法」

### 数学内容深度核对（同日第二轮）

7 组并行子代理核对全部 59 个内容 .md，主代理逐条复验后共修复 **45 处**数学错误（现代控制理论 0 错；附录 6 / 自控 00-06 章 4 / 自控 07-12 章 2 / 线代 11 / 高数 09-15 讲 11 / 高数 01-08 讲 11）。错误类型分布：

- **缺因子/缺项**：傅里叶锯齿波系数 $2A/\pi\to A/\pi$、公式 10-1 少 $1/\sqrt{A^2+B^2}$、椭圆面积少 $\sqrt2$、e 裂项缺 $\frac{1}{e-1}$、拉氏变换丢 $\sqrt2/2$、$D^2+pD+q$ 漏平方、$A^n=(E+B)^n$ 丢幂、解耦 $K=E^{-1}[c_iA^{d_i+1}]$ 少一次幂、高阶导缺相位 $n\pi/2$
- **方向/符号反**：横向伸缩拉伸压缩互换、$f$/$f'$ 有界方向、$\sqrt{a+b}$ 不等式链、$\int x^{-n}$ 符号、$a_n\le\sqrt{a_na_{n+1}}$ 方向、$f(1)=0$ 试根判据
- **变量/下标错**：arcsin 分段（$-\pi-x$）、$e^x$ 递推比值、二项求和哑标 $k/m$、RREF 矩阵与 $\alpha_5$ 表出、$(A^k)^*=(A^*)^k$、行/列满秩互换
- **条件缺失**：$0<a<1$ 第三式需 $a\ge\frac{\sqrt5-1}{2}$、算子法 $F(-\beta^2)$ 仅 $p=0$ 成立、特征多项式才首一
- **例证矛盾**：$|x|$ 无反函数、$f(2)\ne0$ 罗尔区间、间断类型（跳跃/可去）、$1/x$ 左邻域、Pappus 力臂 $y$ 非 $x$、$\int\ln x$ 有初等原函数、$\frac{1}{1+x^2+y^2}$ 实为对称、瑕点少 $x=1$、13 讲拉氏反例区域仍连通

### 例题重解与考纲核对（同日第三轮）

- **例题重解**：7 组子代理逐题重算全部例题（200+ 题），修复 7 处——09 讲凑微分符号（$+d(1/g)$）、01 讲夹逼下界（$1-\frac1x$）、14 讲对称性示例（$\sqrt{x-y}\to x-y$）、01 讲 arcsin 分段口径（$[-3\pi/2,3\pi/2]$ 三段）、04 现控最小实现维数（$\operatorname{rank}(Q_oQ_c)$，min 只是上界）、06 章扰动补偿器符号（$G_n\to G_c$）、变形技巧柯西标注（$g=f^2$）；其余全部重算通过
- **数二考纲逐点核对**：**无缺失考点**；补 2 处薄弱小节（13 讲「二元函数的极限与连续」、15 讲「基本概念」）；修正 15 讲「一阶偏微分方程」超纲标注（数二不考，作为自控数学基础保留，同傅里叶级数处理）
- **入口索引补全**：`高等数学公式速查` 补 08 专题 4 件；`自动控制原理.md` 目录补 03 专题 3 件 + 现代控制理论 5 章（共 12 链接）；此前检查发现无孤立文件、无小节跳号

### 数二题型清单逐项核对（同日第四轮，用户提供题型大纲）

按用户题型清单（函数/微分学/积分学/常微分方程/多元/二重积分 6 大类）逐项映射到笔记：**0 项 ✗、10 项 △**，△ 全部补写（提交 58e0700 + 后续）：02 讲 n 项连乘（取对数化求和+2 例）、01 讲反求极限参数（泰勒比较系数）、04 讲反函数求导例 6（含二阶 $-y''/(y')^3$）、06 讲双中值（两次拉格朗日、拉格朗日+柯西）与二阶导不等式（泰勒端点相加减）、10 讲侧面积（参数/极坐标式 + 例 6 $y=\sqrt x$ 侧面积 $\frac{\pi}{6}(5\sqrt5-1)$，例 5 标题改「弧长」）、15 讲高阶方程降阶独立小节（3 类 3 例）、15 讲题型五函数方程（2 例）、13 讲二重极限求法（夹逼/极坐标化）、14 讲综合与证明（对称性/中值定理 2 例）。其余约 40 项全部 ✓

### 主题式补强（同日第五轮，用户逐个点名考点）

**线代**（逐主题查重补齐）：可逆等价条件全家桶（02 讲）；07 讲内积/施密特/正交矩阵补 `##` 小节结构与三角不等式、正交向量组无关、标准正交基；特征值两条高频结论（$AB/BA$ 非零特征值相同、$A/A^T$ 同特征值）；同解/公共解补非齐次判据、解的包含关系与例题；行满秩/列满秩结论包（乘满秩不改秩、左右逆、消去律）；分块矩阵补分块转置/上三角逆/分块秩/准对角性质；逆序数与展开定义块；初等矩阵行列式与三对象（行列式/秩/解）影响对比表；秩 1 矩阵判定、$\lvert E+A\rvert=1+\operatorname{tr}A$、对角化条件；相似对角化三步与不可对角化判定；充分/必要/充要辨析表（12 行）；PAQ 求 $P,Q$ 步骤、配方法写 $C$、实对称矩阵性质（$k$ 重恰 $k$ 个、秩=非零特征值数、$A=Q\Lambda Q^T$）；合同变换法求标准形与规范形矩阵形式；正定/负定/半定/不定判定表全；特征向量相似变换（$P^{-1}\xi$）与不唯一性；三向量共面⟺相关；解的系数和结论与特解/基础解系不唯一性；化行阶梯形/行最简形的定义与标准流程、非齐次方程组完整求解五步；秩表补 $\lvert r(A)-r(B)\rvert\le r(A+B)$ 下界；$r(A^{*})$ 三情形推导与口诀「满秩满、降一秩一、降二变零」；正交矩阵等价定义链（七条）与判定技巧；长度三性质、展开式与勾股定理；极大无关组定义与三条性质；向量组等价三性质与「秩相同 ⇏ 等价」辨析；实对称正交对角化五步详版；标准形→规范形的缩放操作；不定二次型判定行。

**高数**（第五轮专项排查 19 文件，结论/关系类缺口 5 处全补）：原函数存在性⟷黎曼可积性四象限关系表（含 $\operatorname{sgn}x$、$2x\sin\frac1{x^2}-\frac2x\cos\frac1{x^2}$ 反例）；一元概念关系链（导函数连续⟹可导⟺可微⟹连续，均不可逆）；佩亚诺/拉格朗日余项的可导阶数前提；连续函数的四则与复合保持连续；隐函数求导前提 $F_y\ne0$。

### 自控例题整理（同日第六轮，用户提供手写推导）

- `08 第8章` 新增例题：死区（半宽 2、斜率 1）+ $G(s)=1/s^{2}$ 单位负反馈判自持振荡。**三轮修正**：初版误按 $G=1/[s(s+1)]$、死区 ±1（结论无自振）→ 用户更正为 $1/s^{2}$、±2 → 结论改为**一族等幅振荡**：$-1/N(A)\in(-\infty,-1]$ 与 $G(j\omega)=-1/\omega^{2}$（整条负实轴）**重合**，交点无穷多且非横截，判据退化，振幅/频率由初值决定（$\omega=\sqrt{N(A)}$）。
- **相平面验证**：$\ddot e=-u$，II/III 区奇点 $(\pm2,0)$ 为**中心**（$\lambda^{2}+1=0$，无阻尼），I 区水平直线 ⟹ 拼接成**操场形闭轨族**（两端半圆 + 两段直线）。
- 自绘两张图入库（GDI+ 脚本 → `附件\`）：`自控-死区双积分结构图.png`（原题结构图）、`自控-死区双积分操场形闭轨.png`（闭轨族 + 平衡段）。附件 png 计数 72→74。
- 经验：① 描述函数判据的**退化情形**——$G(j\omega)$ 与 $-1/N(A)$ 轨迹重合 → 非横截无穷交点 → 振幅不定（保守系统无唯一极限环）；② 转录手写题时先核对「区域方程 ↔ 非线性特性 ↔ 奇点类型」三者一致性（本题初版三处互相矛盾即由此发现）。

## 格式修复与内容补充（2026-08-20）

### 格式修复（全库扫描）

逐文件扫描自动控制原理（22 个）、高等数学（26 个）、线性代数（8 个）共 56 个 .md，修复以下格式问题：

**callout 语法修复（10 处）**
- `自动控制原理\03-1` L58/L131、`11 MATLAB` L11、`自动控制原理.md` L9、`05 系统综合` L16/L41、`05 频域分析法` L147、`08 非线性` L159、`07 离散系统` L155：`>[!type]` → `> [!type]`

**YAML frontmatter 补全（7 个文件）**
- `自动控制原理\03-1`、`03-2`、`03-3`：补全 frontmatter（create/modify/tags）
- `数学\高等数学\08-1`、`08-2`、`08-3`、`08-4`：补全 frontmatter

**`$$` 公式块间距修复（~100 处）**
- 自动控制原理：02、03-1、03-3、04、05、06、07、08 共 8 个文件
- 现代控制理论：02、03、04、05 共 4 个文件
- 高等数学：14 个文件
- 线性代数：8 个文件
- 修复方式：`$$` 闭合后补空行；`$$` 内多余空行删除

**其他格式修复**
- `自动控制原理.md` L6-7：frontmatter 与正文间补空行
- `08 非线性` L47：表分隔符 `|---|` → `|:--|`
- `11 MATLAB` L31：表分隔符 `| -- |` → `| :-- |`
- `00 拉普拉斯变换` L141：callout 内容逃逸，补 `> ` 前缀

### 内容补充

**电路建模基础（第 2 章新增 §电路建模基础）**
- KVL/KCL 定义
- R/L/C 元件时域与 s 域对照表（含非零初始条件说明）
- 两种电路建模方法：列微分方程→拉氏变换（通用）/ 复阻抗法（快捷）
- RLC 串联电路完整例题（5 步：选状态→KVL→电容方程→写矩阵→验证传函）

**校正目的与方式（第 6 章新增 §6.1）**
- 校正目的概念说明
- 串联/反馈/前馈三种校正方式对比表

**非线性李雅普诺夫应用（现代控制理论 §5.2.5 新增）**
- 三种构造方法表（能量法/变量梯度法/克拉索夫斯基法）
- 例 1：二次型 $V=x^Tx$ 判非线性系统局部渐近稳定（矩阵形式）
- 例 2：克拉索夫斯基方法 $V=f^Tf$，由雅可比矩阵 $J^T+J$ 负定判稳（矩阵形式）
- 考试要点 callout（4 条）

**现代控制理论例题步骤补全**
- `02 状态空间表达式`：RLC 电路建模例题改为 5 步完整流程
- `03 状态方程的解`：新增 2 道完整例题——$e^{At}$ 三种求法（拉氏反变换/对角化/凯莱-哈密顿，同一矩阵互相验证）+ 求状态响应 $x(t)$（零输入+零状态完整计算）
- `04 能控性和能观测性`：新增 3 道完整例题——能控能观判别（满秩+对角型验证）、含不能控模态、按能控性结构分解（选无关列→构造 $P$→求 $\bar A,\bar B,\bar C$→读子系统）
- `05 系统综合`：综合大题例题改为 4 步完整展开（验能控能观→比较系数求 $K$→对偶求 $L$→验分离原理，含行列式展开过程）

### 大纲核对

对照考研自控大纲（7 章）逐条核对：**全部考点已覆盖，无遗漏**。重点确认：
- 第 2 章：电路建模基础已补充（KVL/KCL/s 域等效）
- 第 6 章：校正目的已补充
- 第 7 章：非线性李雅普诺夫应用已补充（原仅一句话提及）
- 第 7 章：离散系统状态空间已齐全（§5.5）

### 经验

- `$$` 闭合后缺空行是最普遍的格式问题（~100 处/56 个文件），批量修复用 PowerShell 正则 `\$\$\n([^\n\$])` → `` $$\n\n$1 ``
- 检查脚本需区分**开头 `$$`**（前一行是文字，后一行是公式）和**闭合 `$$`**（前一行是公式，后一行是文字）：仅后者需补空行；可通过前一行是否含 `\{}=^_` 等 LaTeX 符号判断
- 现代控制理论的例题应统一使用矩阵形式（$\begin{bmatrix}\end{bmatrix}$），标量写法改为 $\dot V = 2x^Tf(x)$ 等向量/矩阵表达

### 文件拆分（同日）

按 >400 行阈值拆分 3 个超长文件（实际均 >300 行，提前拆分）。采用**入口模式**：原文件保留为精简总览，详细内容移入同目录 `NN-x` 专题文件。

**高等数学 09 讲**（353 行 → 总览 + 3 专题）
- `09 第9讲 一元函数积分学的计算.md`（总览，26 行）
- `09-1 积分方法（凑微分·换元·分部·部分分式）.md`（139 行）
- `09-2 定积分的计算技巧.md`（102 行）
- `09-3 反常积分.md`（132 行）

**高等数学 01 讲**（339 行 → 总览 + 2 专题）
- `01 第1讲 函数极限与连续.md`（总览，19 行）
- `01-1 函数概念与性质.md`（88 行）
- `01-2 极限与连续.md`（261 行）

**自动控制原理第 6 章**（312 行 → 总览 + 2 专题）
- `06 第6章 线性系统的校正方法.md`（总览，19 行）
- `06-1 串联校正（超前·滞后·滞后-超前）.md`（221 行）
- `06-2 PID与反馈前馈校正.md`（101 行）

拆分后全库无 >300 行文件。

### 例题格式统一（同日）

全库扫描并修复 288 处「例题多步计算挤在一行」的问题。修复方式：`> [!example]` callout 格式统一改为 `#### ✏️` 标题格式，每步计算单独一行用 `$$` 块级公式，说明文字与公式之间有空行分隔。

**高数**（13 个文件，~60 道例题）：
- 01-2（6 道）、02（5 道）、03（5 道）、04（6 道）、05（5 道）、06（6 道）、07（4 道）
- 10（6 道）、11（2 道）、12（4 道）、13（6 道）、14（2 道）、15（7 道）

**线代**（8 个文件）：无需修复，例题格式已规范

**自动控制原理**（4 个文件）：无需修复，格式已规范

**附录**（6 个文件）：无需修复，无例题

### 全库格式最终状态

- 无 callout 缺空格（`>[!type]` → `> [!type]`）
- 无单独 `$` 做块级公式（均已改为 `$$`）
- 无 BOM 字符
- 无 `$$` 闭合后缺空行
- 无 frontmatter 缺失
- 例题格式统一为 `#### ✏️` + 每步一行
- 活跃 .md 共 **86 个**（含 AGENTS.md）

## 排版去拥挤复查（2026-08-20 第二轮，同日）

用户反馈「例题概念挤在一起、公式最好一行一个」。全库 66 个 .md 扫描（剔除表格行/`$$` 块公式后），主代理定基准样式（线代04、高数01-2）后派 6 个并行子代理分科目修复，共改 **46 个文件、+861/−255 行**（净增约 606 行，均为拆行）。

### 三类修复与数量
- **A 例题多步计算挤一行**（主战场）：callout 内整行塞多步推导（「由…得…，代入…得…，故…」）→ 每步拆一行，核心推导抽独占一行的 `$$…$$` 块（callout 内用 `> $$` + 内容 + `> $$` 多行块或 `> $$…$$` 单行块，段落间用单独 `>` 行）
- **B 超长概念/结论行**（>200 字符且 ≥4 个行内公式）→ 拆子列表/换行/公式链抽块；步骤式内容（①②③④）逐条拆行
- **C 超长单行块公式**（>170 字符）→ 改 `\begin{aligned}` + `\\` 断行 + `&=` 对齐（现控 03/04、线代 00/02 的矩阵长式）
- 最长战果：14 讲 L21（430 字符）、自控 08 L170（404）、变形技巧 L60（336）、06 讲 L94（367）、09-1 L57（349）、06-1 L138（361）

### 复查结果
- LaTeX 配对全库 0 失败（`$$` 偶 / 单 `$` 偶 / `\begin`=`\end`）；表格行 0 改动；wikilink/图片/标题/callout 标记 0 改动
- 残余豁免：150~200 字符单句陈述（定理/结论/方法，包装后 2 行可读）与 callout 内表格行（15 讲 L172-174、03-2 L70-72 等）按「宁缺毋滥」保留；`$$` 块内长公式行保留
- 顺带修复：08-1 两处块内空行、11 讲/12 讲各一处单 `$` 块级分隔符渲染 bug、线代05 L173「顺序对应）。」内容重复笔误
- 提交 `3b46b21`（含此前 12 个未提交的 `$`→`$$`/删空行格式修复），已 push

### 经验
- 子代理分工（线代 7 / 高数 7+10+9 / 自控 11 / 现控 4）+ 统一规则模板 + 每文件自检配对，46 文件一次通过复查，无返工
- 长公式换行统一用 `\begin{aligned}`（`&=` 对齐 + `\\` 断行），单纯源码换行 MathJax 只当空格，无法解决横向溢出
- 豁免红线要写死：表格行、`> $$…$$` ≤170 字符、行内公式 ≤2 个的短句一律不动，避免子代理过度修改

## 超长文件再拆分（2026-08-20 第三轮，同日）

排版拆行后 3 个文件超出 300 行（库内提前拆分线），按入口模式再拆，共新建 **8 个专题**，全库 74 个活跃 .md 无 >300 行文件。

### 15 讲 微分方程（419 行 → 总览 27 行 + 3 专题）
- `15 第15讲 微分方程.md`（总览：分章导航表 + 解题流程）
- `15-1 一阶方程与降阶（含偏微分方程）.md`（113 行）：基本概念、一阶方程四类型、高阶降阶、一阶线性偏微分方程
- `15-2 线性微分方程解的结构与二阶常系数解法.md`（123 行）：解的结构定理、二阶常系数完整解法、反推表、欧拉方程
- `15-3 常考题型与典型例题.md`（208 行）：题型一~五 + 典型例题

### 06 讲 中值定理与泰勒公式（331 行 → 总览 20 行 + 2 专题）
- `06 第6讲 一元函数微分学的应用（二）中值定理与泰勒公式.md`（总览）
- `06-1 中值定理与辅助函数构造.md`（141 行）：费马引理、四大中值定理、构造辅助函数
- `06-2 泰勒公式·不等式证明·题型例题.md`（203 行）：泰勒公式、五种不等式证明、题型速查、典型例题

### 01-2 极限与连续（304 行 → 总览 21 行 + 3 专题）
- `01-2 极限与连续.md`（总览，仍为 01 讲专题件，返回 `[[01 第1讲]]`）
- `01-3 极限的概念·性质与两大准则.md`（121 行）
- `01-4 无穷小与极限计算技巧.md`（135 行）：等价无穷小表、洛必达、反求参数
- `01-5 连续与间断·典型例题.md`（75 行）

### 方法与验证
- 用 PowerShell 按「## 小节」边界行号切分（15-1:[13,112]、15-2:[114,223]、15-3:[225,419]；06-1:[13,140]、06-2:[142,331]；01-3:[11,118]、01-4:[120,241]、01-5:[243,304]），正文逐字节搬移，专题件补 frontmatter（create 沿用原文件、modify 2026-08-20）+ `> 返回：[[总览]] | 返回目录：[[高等数学公式速查]]` + H1 + abstract；总览重写为「分章导航」表格（样式参照 `09 第9讲`）
- **注意坑**：PowerShell 单引号字符串里 `'` 需写 `''`，写含 `$y''+py'+qy$` 之类公式的描述文本时极易转义错，改用纯文本描述规避
- 验证：全库 74 文件 LaTeX 配对 0 失败；外部引用（`高等数学公式速查`、`01 第1讲`、04 讲、08-2、01-4 等 14 处）均为 basename 链接指向原文件名，入口模式不断链；无锚点（`#`/`|`）引用
- 提交 `refactor:` 前缀（结构调整）

## 优化复查（2026-08-20 第四轮，同日）

用户问「有没有可优化的地方」，按确认清单执行四项（提交 `fix:` 前缀）：

1. **导航补全**：`高等数学公式速查` 表下新增「已拆分讲次的专题直达」区（第 1/6/8/9/15 讲全部 17 个专题）；`01 第1讲` 总览导航补 `01-3/4/5` 三行，abstract 改「已拆分为 5 个专题」（原停在 01-2）
2. **`$$` 块内空行批量清理**（125 处 / 26 文件）：用「配对状态机」逐行扫描（行 trim == `$$` 翻转块内状态，块内空行删除），只删块内空行、保留块间与关闭符后空行 → 渲染零影响（MathJax 忽略块内空白），视觉不挤；重验配对 0 失败
3. **callout 真空行 → `>` 空行**（15 处 / 10 文件）：callout 内部用真空行会把一个 callout 渲染成多段（如线代03 例子被拆 3 段）→ 规则「真空行前后行均以 `>` 开头、且后一行非 `> [!`」改为 `>` 行，保持 callout 连续；独立 callout 之间真空行保留（验证无误合并）
4. **150~200 字符陈述行再拆**（48 条 / 18 文件，子代理执行）：断句换行或拆子列表，断点取 `；`/`。` 自然分句；剩 1 条豁免（自控 04 根轨迹 L108「由时域指标反解期望主导极点」浑然一体单步项）；全库 150~200c 行从 49 → 1
5. **frontmatter modify 统一为 2026-08-20**（41 文件）：行级处理并保留 CRLF/LF 行尾

### 经验（踩坑）
- **CRLF 陷阱**：PowerShell 正则 `^---\n` 匹配不了 CRLF 文件（`---\r\n`），frontmatter 处理必须行级做（`Get-Content` 数组 + `EndsWith("\`r")` 判断保留行尾），否则脚本静默零生效
- 配对状态机是删「块内空行」的可靠方法：`$$` 行翻转开关，开状态下遇空行即删，天然区分开块/关块，不会误删「关闭符后」的正常空行
- callout 空行规则要排除 `> [!`（新 callout 标记）——连续两个独立 callout 之间的真空行必须保留，否则会把两个 callout 合并成一个

## 步骤详细度与例题覆盖审查 + 教材校准（2026-08-20 第五轮，同日）

用户问「有没有步骤不详细的、缺少例题的」。派 7 组只读审查子代理（高数 3 + 附录 1 + 线代 1 + 自控 2）全库排查，发现 **A 类步骤不详细 10 项 + B 类缺例题 34 项 + 1 处数学错误**；用户确认按「错误 + A 类 + 核心缺例题」范围补全，提交 `6cc6f66`（18 文件 +429/−19）。

### 数学错误修复（最高优先）
- **现控04 §4.5.4 按能控性分解例题整体重写**：原例题 $Q_c$ 第三列 $(1,-3,7)$ 首分量非零、det$=-1\neq0$，系统实际完全能控（rank=3），原 rank=2 判断与全部分解错误。主代理重算验证后重写为 $A=\begin{bmatrix}0&1&0\\0&0&0\\0&0&2\end{bmatrix}$、$B=[0\ 1\ 0]^T$：$Q_c$ rank=2、$P$ 为置换阵、$\bar A$ 块三角（$\bar A_{11}$ 能控 2 维、$\bar A_{22}=[2]$ 不能控），传函 $\bar C_1(sI-\bar A_{11})^{-1}\bar B_1=1/s^2$ 与直接算 $C(sI-A)^{-1}B$ 双路径一致

### A 类步骤补全（10 处）
- 高数：02 讲 e 单调递增补 Bernoulli 放缩；06-2 例6 补 $f'(\xi_1)-f'(\xi_2)=4f(c)/(b-a)$ 与 $\lvert\xi_1-\xi_2\rvert\le b-a$ 两步；11 讲题型三展开为可操作五步+小例；变形技巧 (9) 补适用场景
- 线代：01 例3 解出 $C_1,C_2$ 闭式并验证 $n=1,2,3$；06 例1 反解出 $C=\begin{bmatrix}1&-1&-2\\0&1&2\\0&0&1\end{bmatrix}$ 并双向验证
- 自控：06-1 滞后-超前六步补⑤⑥反解/校验式；06-2 反馈校正补测速反馈 $K_t=2\zeta^*\sqrt K-2$ 推导+例题；07 最少拍补圆上/外零极点修正规则四条；04 套路①补完整绘制算例

### 核心例题 15 道（均主代理逐题重算复核）
- 自控 7：根轨迹 $K^*/[s(s+1)(s+2)]$（$d=-0.423$、$K^*_{cr}=6$、$\omega=\sqrt2$）、劳斯 $0<K<6$、稳态误差 $K_v=5/3$、梅森 $C/R=(13s+21)/(s+2)(s+3)$、奈氏（$\omega_x=\sqrt2$、$Z=2$ 不稳定）、最少拍（$G(z)=(1.135z+0.594)/[(z-1)(z-0.135)]$、1 拍无差）、由曲线反求 $G(s)$
- 线代 2：PAQ 求 $P,Q$（$A=\begin{bmatrix}1&2&3\\2&4&6\end{bmatrix}$，验 $PAQ=(E_1;O)$）、合同变换法（$C^TAC=E$ 验证）
- 高数 6：09-1 换元 $\int dx/[x^2\sqrt{1+x^2}]$、09-1 部分分式、02 阶乘/幂 $\lim 2^n/n!$、03 多绝对值可导判别（**注意**：$x=3$ 处实为可导 $f'(3)=2$，任务初版结论有误已由子代理更正）、05 证唯一根、06-2 单调性法证 $\ln x\le x-1$

### 教材校准（胡寿松《自动控制原理》第八版 PDF）
- 提取目录 + 关键章节（2-3 信号流图/梅森、3-5 劳斯、3-6 稳态误差、4-2 根轨迹法则与例 4-1~4-5、5-3 奈氏、6-4 串联校正式 6-35~6-40 与例 6-4/6-5、7-7 最少拍）逐条比对
- 校对结果：笔记 06-1 超前/滞后公式与教材式 6-35~6-40 一致（例 6-5 数值吻合：$\gamma_0=-27.6°$、$\omega_c''=2.7$、$b=0.09$、$T=41$）
- **主代理自身错误被纠正 1 处**：给子代理的 06-1 ⑤ 滞后相角公式 $\arctan(\omega_c''/\omega_a)-\arctan(\omega_c''/\omega_b)$ 符号反了（滞后对 $(1+s/\omega_b)/(1+s/\omega_a)$ 相角应为负），子代理照录并标注，主代理对照教材确认后改为 $\arctan(\omega_c''/\omega_b)-\arctan(\omega_c''/\omega_a)<0$
- 经验：给子代理的预验证推导也可能错，子代理「自己验算+标注疑点」机制有效；教材 PDF 文字版可提取（692 页），书内页码 → PDF 页偏移 +8（第八版）

### 未补（用户范围外，留待需要时）
- 34 项 B 类中未选的次要缺例题（自控 05 曲线反求已补、现控02 输入含导数建模/对角约当实现、现控05 李雅普诺夫方程法/降维观测器、05 讲含参零点直接讨论法/含参极值拐点/证 f≥g、14 讲一般换元、15-1 齐次型/偏微分特征线、15-3 题型三、03-2/03-3 部分、11 题型四 等约 20 项）——已在审查报告中定位，需要时按报告补
