# AGENTS.md — 给 AI 助手看的长期记忆与操作规范

> 本文件是仓库的「长期记忆 + 操作手册」。任何会话编辑本库笔记前，请先通读「笔记规范总纲」与「检查与修改流程」；文末「历史记录」记载历次审阅、拆分、检查的全部结论与已修复问题，避免重复踩坑。本文件自身不参与笔记检查。

## 仓库概况与目录地图

- 路径：`D:\obsidian\`（**git 仓库根目录**，Obsidian 库即本仓库内容）
- Git：`main` 分支，远程 `git@github.com:wangziyu1234/obsidian-vault.git`；不依赖自动备份脚本，但 **AI 助手每次修改完必须主动 `git add -A` → commit → push**，与 `origin/main` 保持同步
- 提交信息：中文、按用途加前缀——`fix:` 修错误 / `refactor:` 结构调整 / `docs:` 笔记内容 / `chore:` 杂项清理
- 笔记主体：**控制理论**（`控制理论\自动控制原理\` 36 个 .md：第 1–9 章 + `02-1~02-5`/`03-1~03-6`/`05-1~05-3`/`06-1~06-2`/`07-1~07-3` 专题 + 附录与工具 + 入口；`控制理论\现代控制理论\` 18 个 .md：00 绪论 + 01–05 按教材章节 + 拆分件 `01-1~01-4`/`02-1~02-3`/`03-1~03-2`/`05-1~05-2` + 总览入口）+ **数学**（68 个 .md：高等数学目录 52 个——15 讲 + `01-1~01-5`/`02-1`/`06-1~06-2`/`08-1~08-4`/`09-1~09-4`/`11-1~11-3`/`13-1~13-3`/`15-1~15-3` 共 25 个拆分专题 + 12 个附录；线性代数目录 13 个——00–07 讲含 `02-x`/`05-x` 拆件；另有高数/线代公式速查 2 个 + 入口 `数学.md`）+ **英语**（16 个 .md：入口 `英语.md` + 大小作文 9 分章 + 翻译/完型/新题型 6 分章）
- 活跃笔记 **138 个 .md**（数学 68 + 英语 16 + 控制理论 54）+ 本文件 + `README.md`；`附件\` 现有 **106 张 png（全部被引用，无 jpg）**；根目录另有 `README.md`（库总览）

### 目录地图（决定扫描边界）

| 目录 | 内容 | 检查时怎么处理 |
| --- | --- | --- |
| `数学\` | 高数 15 讲 + 拆分专题 25 个 + 附录 12 个、线代 00–07（含拆件共 13 个）、速查表 2 个、入口 | 全部检查 |
| `控制理论\自动控制原理\` | 第 1–9 章 + `10/11/12`、`02-1~02-5`/`03-1~03-6`/`05-1~05-3`/`06-1~06-2`/`07-1~07-3` 专题、`00 拉普拉斯`、入口 | 全部检查 |
| `控制理论\现代控制理论\` | 00 绪论 + 01–05（按教材章节）、总览入口 | 全部检查 |
| `英语\` | 大小作文 9 分章 + 翻译/完型/新题型 6 分章 + 入口 `英语.md` | 全部检查（wikilink 指向 `英语.md`；无 LaTeX、无图片） |
| `附件\` | 图片素材（全部 png，无 jpg） | 校验嵌入命中（2026-08-21 已清理全部 37 张冗余 jpg，引用已统一为 png） |
| `templates\` | 3 个模板 | 只查跨文链接，模板占位符不算问题 |
| `Excalidraw\` | 1 个绘图文件（JSON 正文） | 基本不查 |
| `copilot\copilot-custom-prompts\` | 15 个插件 prompt | 不是笔记：无 H1、无链接都正常 |
| `教材OCR\` | 教材 PDF 的 OCR/文字层提取全本 + `分章\` 拆章件 + 索引 README（张宇高数/线代 2027、胡寿松自控八版、刘豹现控三版） | 不检查；作笔记核对的**原文裁定基准**（txt 已 gitignore 仅留本地，索引 README 入库） |
| `_moved_out\` | ~~归档区~~ **已于 2026-08-21 删除**（用户确认清空：原含全库备份 `backup_nested_obsidian_20260809` 24MB + 冗余 jpg 40 张 + 临时 py） | 不再存在；若日后重建归档区，需同步在 Obsidian「文件与链接 → 排除文件」加回 `_moved_out/` |
| `.venv\` `.obsidian\` `.git\` `.trash\` | 环境 / 会话状态 / 回收站 | 不检查 |

## 笔记规范总纲

### 文件命名
- 章节/讲次：`NN 第N章（第N讲） 描述.md`，前缀两位数字连续
- 附录：不带数字前缀，直接 `附录 XXX.md`
- 专题拆分件：`NN-x 描述.md`，与总览同目录平铺（前缀排序相邻）

### 文件头部（强制顺序）
1. frontmatter：`create` / `modify` / `tags`
2. `> 返回目录：[[目录]]`（专题件为 `> 返回：[[总览文件名]] | 返回目录：[[目录]]`）
3. `# H1`
4. `> [!abstract] 本讲定位`

- 笔记文件**有且仅有一个 H1**；讲义内部细分可用 `###`，**附录不用 H3**（历史遗留 1 处：`附录 图像变换` 的 `### 一般对称性的代数判别`，可不动）
- 既有稳定风格勿改：高数 05–12 讲文件名无破折号、H1 含 `——`；15 讲 H1 为「微分方程（常微分方程）」

### 小节标题
- 章节小节：`## 图标 X.Y 标题`（标题前缀图标沿用各章现有样式，X 章号、Y 小节号，**连续不重号**）；每章末 `## X.N 解题套路`
- 附录方法序列可用 `## (n) 名称`；两大块式附录参考 `附录 变形技巧.md`：`## 总览`（表格 + 锚点导航 `[[#标题\|别名]]`）→ `## 一、…` → `## 二、…` → `## 收尾`

### wikilink 与图片
- 跨文引用一律 `[[ ]]`；跨目录优先 `[[控制理论/现代控制理论/01 状态空间表达式|别名]]` 短路径，**不用 `../` 相对路径**；引用附录写 `[[附录 XXX]]`
- wikilink **不要放进 `$$…$$`**（MathJax 不解析），放公式外或正文
- 图片：`![[文件名.png|尺寸]]`，正文图 `|430` 左右、速查大图 `|520` 左右
- **表格单元格内**的链接管道与图片尺寸必须转义：`[[目标\|别名]]`、`![[图.png\|220]]`。`\|` 是 Obsidian 表格解析的转义（渲染正常），**不是 bug，勿改**
- 附件图片统一 .png；清理冗余素材原移入 `_moved_out\` 归档区（该目录已删，需要时可重建并加 Obsidian 排除），不物理删除的习惯已改为「待删素材先确认再删」

### LaTeX
- 行内 `$…$`、块级 `$$…$$`；**`$$` 必须成对**（曾出现 `$$` 开、`$` 闭的渲染 bug）
- `\begin/\end`、`\left/\right` 必须配对；`\leftrightarrow` 箭头含 `\left` 子串，会让 `\left/\right` 计数检查**误报**
- 微分方程系数下标：笔记用 $a_n$ 配最高阶 $c^{(n)}$（与部分国内教材一致）；胡寿松教材（式 2-36）用 $a_0$ 配最高阶、$a_n$ 配常数项，两种约定数学等价（$a_k^{\text{笔记}}=a_{n-k}^{\text{教材}}$）
- 观测器极点倍数统一「2~5 倍」；MATLAB 用函数句柄 `@fun`，不用字符串传函数
- 跨章主线符号一致：$r(t)$ 参考输入、$e=r-b$ 偏差、$\zeta$ 阻尼比、$\omega_n$ 自然频率、$A^T$ 转置、$E$ 单位阵、$r(A)$ 秩

### 拆分约定（笔记 >300 行时）
- **入口模式**：原文件保留为总览入口（文件名不变、现有链接不断），正文移入同目录 `NN-x` 专题文件
- 已拆分：第 9 章（总览化）、第 3 章（`03-1~03-6`）、第 8 讲（`08-1/2/3/4`）、高数 09 讲（`09-1/2/3` 再拆 `09-4`）、高数 01 讲（`01-1/2` 再拆 `01-3/4/5`，共 5 件）、高数 06 讲（`06-1/2`）、高数 15 讲（`15-1/2/3`）、**第 2 章（`02-1~02-5`）、第 5 章（`05-1~05-3`）、第 7 章（`07-1~07-3`）、第 6 章（`06-1/2`）、高数 11 讲（`11-1/2/3`）、高数 13 讲（`13-1/2/3`）、现代控制理论模块（00 绪论 + 01–05，按教材章节重排；01 已拆 01-1~01-4）**
- 总览样式参照 `09 第9讲`：frontmatter + 返回行 + H1 + abstract（注明「已拆分…总览入口」）+ `## 🗂️ 分章导航` 表格 +（可选）解题流程；专题件头部 `> 返回：[[总览文件名]] | 返回目录：[[目录]]`

## 检查与修改流程

### 检查清单（收到「检查笔记」任务时逐项执行）
1. 先 `git status` + `git log` 看增量；扫描边界按「目录地图」
2. **断链**：所有 `[[ ]]` 与 `![[ ]]` 目标必须命中（链接按 basename / 短路径解析；表格内 `\|` 先按转义拆出目标再解析）
3. **LaTeX**：`$$` 成对、`\begin/\end` 配对、`\left/\right` 配对（剔除 `\leftrightarrow`）
4. **编号**：`## X.Y` 小节号无重复；无 TODO/FIXME/待补/`??`；无空链接 `[[]]`
5. **结构**：唯一 H1、`> [!abstract]` 在 H1 之后、附录无 H3、无 >300 行文件（超了按拆分约定处理）
6. 检查脚本注意：`read` 工具 `limit` 上限 **2000**；输出中文注意 UTF-8 编码
7. **数学内容深度核对**：按科目分 7 组并行派子代理逐条推导复核（高数 01–08 讲 / 09–15 讲 / 附录与速查 / 线代 / 自控 00–06 章 / 07–12 章 / 现代控制理论），主代理对每一条报告**重新推导确认**后再修改，宁缺毋滥

### 已知误报与豁免（不要重复报告）
- `05 第5章` 表格内 `![[…png\|220]]`：转义正确，勿改
- `\leftrightarrow` → `\left/\right` 计数误报
- `copilot\copilot-custom-prompts\*.md` 无 H1 正常；`AGENTS.md` 自身、`.venv\` 不参与检查（`_moved_out\` 已删除）
- `_moved_out\` 内的备份与活跃文件**同名**（历史问题，目录已于 2026-08-21 删除）：若重建归档区，必须重新在 Obsidian「设置 → 文件与链接 → 排除文件」排除 `_moved_out/`（`app.json` 的 `userIgnoreFilters`），否则 `[[控制理论/现代控制理论/00 绪论]]` 这类短路径链接会因重名解析失败

### 修改流程
- 修改/重命名前先 `grep` 检查跨文件引用，避免断链；重命名用 PowerShell `Move-Item -LiteralPath`（路径含中文与括号）
- 每次修改完主动 commit + push（中文提交信息，前缀见「仓库概况」）
- **自绘图**：写 GDI+（System.Drawing）PowerShell 脚本生成 PNG 存 `附件\`（命名沿用 `自控-xxx.png`/`高数-xxx.png`），嵌入 `![[...png|430]]`；本模型不能读图，用**像素抽样**自检（关键元素位置取色验证）后再入库
- **git push 与沙箱**：受限沙箱下 `git push` 会因 ssh.exe 无法创建 signal pipe 而失败（Win32 error 5）。优先直接执行；只有沙箱拦截且会话允许带权限重试（审批策略不是 never）时，才用 `sandbox_permissions` 重试同一条命令；**审批策略为 never 时不要设置 `sandbox_permissions`**（会被自动拒绝），改为在回复中说明失败原因

## 历史记录

以下为历次会话的完整记录（精简版）；结论已沉淀到上方规范总纲，过程细节见 git log。

## 历史总览（精简版——过程细节均可在 git log 查询；此处只留结论与教训）

按主题分节，记录各轮核对的结论、修过的数学错误、沉淀的规范与踩坑，避免重复劳动。

### 格式规范类结论（已沉淀到总纲，此处备查）
- `05 第5章` 表格 `![[…png\|220]]` 的 `\|` 是 Obsidian 表格转义**勿改**；`\leftrightarrow` 会让 `\left/\right` 计数误报
- CRLF 陷阱：PowerShell 正则 `^---\n` 匹配不了 CRLF 文件，frontmatter 批量处理必须行级（`Get-Content` 数组 + `EndsWith("\r")` 保留行尾）
- `$$` 配对状态机删块内空行：行 trim 为 `$$` 翻转块内状态，块内空行删、块间与关闭符后保留
- callout 内部真空行 → `>` 空行（否则一个 callout 被拆多段）；独立 callout 之间真空行**必须保留**
- 长块公式（>170 字符）统一 `\begin{aligned}` + `&=` 对齐 + `\\` 断行（单纯源码换行 MathJax 只当空格，仍横向溢出）
- 图片引用扫描要拆表格内 `\|` 转义、AGENTS 自身占位符示例，否则误报「缺失」
- 检查脚本阈值经验：callout 内 `> $$…$$` ≤170 字符、正文长块公式 >120 字符、单句 150~200 字符均为折中豁免线

### 数学计算核对记录（防重复报告）
**2026-08-14（45 处修复，7 组子代理）**错误类型分布：缺因子（傅里叶锯齿波 2A/π→A/π、公式10-1 少 1/√(A²+B²)、椭圆面积少 √2、e 裂项缺 1/(e−1)、拉氏丢 √2/2、D²+pD+q 漏平方、Aⁿ=(E+B)ⁿ 丢幂、解耦 K 少一次幂、高阶导缺相位 nπ/2）；方向反（横向伸缩互换、f/f′ 有界方向、√(a+b) 链、∫x⁻ⁿ 符号、aₙ 几何平均方向、f(1)=0 试根）；变量错（arcsin 分段、eˣ 递推比值、哑标 k/m、RREF 与 α₅ 表出、(Aᵏ)* 顺序、行列满秩互换）；条件缺（0<a<1 需 a≥(√5−1)/2、算子法 F(−β²) 仅 p=0、特征多项式才首一）；例证矛盾（|x| 无反函数、f(2)≠0 罗尔区间、间断类型、1/x 左邻域、Pappus 力臂 y 非 x、∫lnx 有原函数、9 讲例 13 区域连通）。例题重解 7 处：09 凑微分、01 夹逼下界、14 对称性、01 arcsin 三段落、04 现控最小实现维数、06 扰动补偿符号、变形技巧柯西标注。

**2026-08-21（9 处修复，8 组子代理覆盖 107 文件）**：
1. `07-2` 采样综合例对象漏 $s$（$T_0s+1$→$s(T_0s+1)$）——子代理误报「G(z) 多 z」，主代理重推+回查原题 OCR 确认真错在对象（**2026-08-24 再次 OCR 复核：此修复仍不完整，正确对象是 $Ke^{-Ts}/(T_0s+1)$（一阶惯性+一拍延迟），分母多的 $z$ 来自 $e^{-Ts}$ 而非积分环节；已按原题修正，见 2026-08-24 记录**）
2. `现控04` L64 $C_d=[1\ 0]P=[1\ 1]$ 写成 $[1\ -1]$
3. `11-2` L50 原函数符号反（$[(1-x)^2/2]$→$[-(1-x)^2/2]$）
4. `11-2` L32 保序推不出 ≤π/4（改 ≤1）
5. `10` L60 柱壳法 `2π∫x dy`→$2\pi\int x|f(x)|dx$
6. `06-1` L140 唯一实根逻辑跳步（至少1+至多2≠唯一；且奇次 f'' 必有实根）
7. `附录傅里叶` L52 偶三角波括注「正负交替」错（改全正）
8. `附录变形技巧` L208 $a=\ln(a+e^y)>1$ 缺条件 → 换 $e^x\ge1+x$ 例
9. `03` L49 奇函数 ⟹ f'(0)=0 误用（那是偶函数性质；实为 f'(0)=-2）

### 教材/大纲/资料核对结论
- 胡寿松第八版 692 页文字版可提取，书内页码 → PDF 页偏移 **+8**
- 教材覆盖度（第六轮）：★ 缺口 8 项全补（非线性线性化/负载效应/误差双定义/参数根轨迹/二阶微分环节/按输入补偿升型/z 位移卷积/约当型实现）；☆ 弱覆盖 29 项选 14 项补（第七轮：直流电动机三方程、机械建模、结构图规则、s 平移法、正弦终值定理陷阱、零度根轨迹出射角、频域 4 补丁、z 表 6 行、描述函数 3 项等）
- 数二考纲：无缺失考点；15 讲一阶偏微分方程、傅里叶级数、方向导数标「数二不考」但保留作自控数学基础
- Word 补充资料 OCR 核对（本轮）：大纲+测验题 ↔ 01/02/03 全覆盖；第5章小结/例题、描述函数、离散稳定均已覆盖——**无 A 类必补**
- 描述函数公式从教材 PDF 还原：文字层乱序需 PyMuPDF 坐标重建 + 独立推导/数值双验证

### 拆分记录（入口模式：原文件留总览 + NN-x 专题）
已拆：09 章（→现控 01-05）、03 章（03-1/2/3）、08 讲（08-1/2/3/4）、09 讲（09-1/2/3）、01 讲（01-1/2，01-2 再拆 01-3/4/5）、06 讲（06-1/2）、15 讲（15-1/2/3）、**第2章（02-1~5）、第5章（05-1~3）、第7章（07-1~3）、第6章（06-1/2）、11 讲（11-1/2/3）、13 讲（13-1/2/3）**。全库现无 >300 行文件。
- 总览样式：frontmatter + 返回 + H1 + abstract（「已拆分…总览入口」）+ `## 🗂️ 分章导航` 表；（可选）主线流程图
- 拆分脚本坑：`## 图标 X.Y` 带 emoji，正则取编号 `(\d+\.\d+)`；函数内取外层变量需传参；PowerShell 5 无三元运算符

### 排版/可读性结论
- 例题格式统一 `#### ✏️` + 每步独立 `$$` 块（2026-08-20 处理 288 处）
- 可读性四维度现状：公式一步一行 ✓、无 >120 字符 callout 内块公式、无高密度行（≥3 行内公式+>130 字符）、全库最高 288 行
- 图片尺寸：正文 `|430`、速查大图 `|520`、宽高比 ≥2 的宽图也用 `|520`（先 PIL 读像素再定）
- **用户排版偏好（2026-08-23 明示，长期遵守）：纵向空间不吝啬、横向空间要珍惜、公式不要密集**——每个公式独立 `$$` 块且块间留空行；表格单元格/行内公式用 `\frac` 压扁；禁止把多个算式或长推导挤进同一行

### 科目扩展记录
- 英语 10+6 分章（新增科目模式：入口 `科目.md` + 编号连续 + 校验脚本同标准）；PDF 提取经验：控制台 GBK 显示乱码≠数据错（写 UTF-8 文件再 read）、英文讲义 ToUnicode 丢空格需按语义补

### 关键方法论（反复验证有效）
- **子代理预验证推导也会错**：本会话两次靠「主代理重新推导 + 回查原题」纠正（λ 系数颠倒、07-2 对象漏 s、约当 C 块系数顺序）——子代理「自己验算+标注疑点」机制必须保留
- **OCR 路线**：旧 .doc/.ppt 二进制含公式图片 → Word COM `SaveAs(FileFormat=17)` 转 PDF → PyMuPDF 渲染 → RapidOCR；中文路径用 `os.listdir` 枚举；控制台 GBK 打印 emoji/✓ 报错 → 写 UTF-8 文件或纯 ASCII；全书 OCR 文本统一存 `教材OCR\`（索引见其 README）；pip 装 RapidOCR 默认源极慢，一律加 `-i https://pypi.tuna.tsinghua.edu.cn/simple`
- **梯子脚本不沉淀**：一次性拆分/提取脚本用完即删，`.scripts/` 只留常驻 `check-notes.ps1`（对标 AGENTS 清单的全库校验，支持 -Root/-Dir/-File）
- Tasks 任务总览页是聚合视图勾选以原笔记为准；用户删除后需同步撤入口链接

### 库状态基线（2026-08-21）
活跃笔记 107（数学 56 / 自控 35 / 英语 16）+ AGENTS.md + README.md；附件 79 png 0 jpg 全命中；community-plugins 10 个；远程 origin/main 同步。

**2026-08-23（同步覆盖事故 + 03-1 补强）**：`remotely-save` 云同步插件（云端存旧副本）把本地文件覆盖回旧版——03-1 回退到 08-22 12:03 态（modify 08-14、丢失 ###3.2.x 重排与后续内容补强），13-1/13-3/15-2 行尾符被改写（正文未丢）。处理：03-1 `git restore` 回 HEAD、行尾归一、remotely-save 移出插件列表（chore:74bb328）；03-1 补「极点位置-动态性能规律（直角/极坐标两种表示）、已知指标反求参数与原方程、最佳阻尼比」三小节（docs:184d1fc）。**用户 00:51 对 13-1/13-3 的改动疑似被同步覆盖，未找回**（Obsidian 文件恢复未启用、OneDrive 副本无此文件）。教训：① 库内装云同步插件会与 git 打架，改动被静默回退且 `git status` 未必报警——发现文件异常先看 mtime，再用 `git hash-object` 磁盘文件比对历史 blob，判定是「旧版覆盖」还是「本地新编辑」再动手；② 行尾符差异会让 status 报 M 而正文无差，用 `git diff --ignore-space-at-eol` 鉴别后 `git restore` 归一。

**2026-08-24（同款事故二次复发 → 已恢复 + 清理）**：03-1 又被云同步静默回退——08-23 17:22~18:40 的 10 笔补强（`452ca7e`→`b5cd597`，含极角速查表/s平面等值线/K,T 反求例/单位脉冲对照/3 张拆分图嵌入，blob `9aee386`，392 行 5 图）在 21:45 `6898473` 提交后、22:00 自动备份前被旧副本（17:15 态 `184d1fc`，blob `9c6eed6`）覆盖，自动备份把坏态固化成 HEAD。**检出法**：`check-notes` 全过但附件出现 3 张孤儿 png（`自控-s平面极点/单位阶跃响应/单位脉冲响应`）；`git hash-object` 磁盘 03-1 = `184d1fc` blob → 判定旧版覆盖。**处理**：`git restore --source=6898473` 恢复 03-1 至 `9aee386`（与 b5cd597 blob 一致）、删 4 张冗余 jpg（同步混入）；`check-notes` 复验通过。**根因**：08-23 17:19 曾重新启用 remotely-save（2a1dea9），与 git 冲突复发。**⚠️ 用户决策（2026-08-24，长期有效）：remotely-save 保留启用——多端同步是主力（手机/电脑），git 仅作版本兜底与历史回溯，不得再以「移除插件」处理同步问题/建议关闭同步**。叠加隐患仍存在（云端旧副本覆盖本地），检出与恢复流程照旧：孤儿附件 → `git hash-object` 比对历史 blob → `git restore --source=…`。

**2026-08-24（同款事故第三次复发 → git status 检出 + restore 恢复）**：`12 第12讲`（数学/高等数学）磁盘文件被云同步回退到 `727adea`（08-22 22:00 自动备份，即 `6898473` 微元法详解提交前的旧版）——被覆盖的是 08-23 21:45 已推送的内容，08-24 检出，**覆盖可跨天、且针对无图纯文本笔记（无「孤儿附件」信号）**。**检出法**：非孤儿附件，而是 `git status` 显示该文件 `M` 且 diff 恰为原提交的镜像（+12/−71 对 +71/−12）、内容 marker（「微元的两个标准」等）全消失；`git hash-object` 磁盘 blob `6eeb9209` = `727adea` blob → 判定旧版覆盖。**处理**：`git restore --source=HEAD`（blob `0f335b20`，与远程一致）恢复 → `check-notes` 复验通过 → 工作区干净无需再提交。**新增教训**：① 无图笔记被静默回退时没有孤儿附件信号，检出手段改为「每次进库 `git status` + 抽查近期改动文件的内容 marker」，回退 diff 常是原提交的镜像符号；② 恢复对象一律用 `HEAD`（=远程已确认状态），不要用更老的提交。

**2026-08-24（数学内容深度核对第 8 轮，7 组子代理 + 主代理逐条重推/回查原题）**共修复 **13 处**：
1. `09-2` 变限积分求导公式减项重复（删重复行）
2. `02` 子列反例用词（`|a_n|\to1` 非子列 → 改偶子列 $a_{2k}\to1$）
3. `03` L71 笔误 $f(x_0)$ 存在 → $f'(x_0)$ 存在（高阶导邻域层级）
4. `线代02` L169 转置顺序（$PA=B$ ⇒ $A^TP^T=B^T$ 而非 $P^TA^T=B^T$）
5. `线代01` L96 行列式多项式次数补条件（元素为一次时≤n，反例 diag(x²,x,x)）
6. `附录积分表` L150 $\int\frac{dx}{x\sqrt{x^2+a^2}}$ 缺 $\frac1a$ 因子（求导反验，a=1 时偶合掩盖）+ 同条倒代换推导示例同步修正
7. `附录积分表` L146 $\sqrt{a^2-x^2}$ 推导栏笔误（$dx=a\cos t\,dt$）
8. `附录图像变换` L81 组合变换先伸缩后平移量应为 $\frac bk$（与 L80/L86-87 自洽）
9. `附录幂指对` L46 $x^x$ 未定式标错（$1^\infty$ → $0^0$）
10. `附录二重积分速查` L27 抛物域 $\iint x=\frac{4a^2}{5}\sqrt{2pa}$（原 $\frac23$ 错）、L28 圆 $x^2+y^2\le2ax$ 面积 $\pi a^2$（原 $\frac{\pi a^2}2$ 错）、L38 $x^my^n$ 缺 2π 因子
11. `附录空间图形` L75 $z=xy$ 表述（非中心对称，改为绕 z 轴 180° + 两平面对称）
12. `现控03` §3.2.4 例题补 $B$ 定义（与上例自洽）
13. **`07-2` 采样综合例对象修正（重点，见上）**：原题（Word 例3 采样系统结构图.doc）OCR 复核对象实为 $Ke^{-Ts}/(T_0s+1)$（一阶惯性+一拍延迟，0 型）——**8-21 记录的「对象漏 s」修复不完整**，分母多出的 $z$ 来自 $e^{-Ts}\Rightarrow z^{-1}$ 而非积分环节；主代理独立推导验证：若对象为 $K/[s(T_0s+1)]$，ZOH 离散含 $z=1$ 极点（I 型），与答案 $0.8K/[z(z-0.2)]$（0 型）极点结构矛盾、不可能相等；已按原题修正 L139/L148/要点① 并保留 ②③ 问结果（Jury $-1<K<1.25$、三重根 $\alpha=0.4,b=0.35,c=-0.08$ 复核无误）。**教训：对「已修复」的历史条目，复核时若新推导与答案矛盾，要重新回查原题，别被历史结论带偏。**

**2026-08-25（拆分阈值 400→300 + 6 文件重拆 + 第3章按内容顺序重排）**：① `AGENTS.md` 拆分约定/检查清单、`check-notes.ps1` 超长阈值由 >400 改 >300；② 6 个超 300 文件按入口模式重拆——`03-1` 的 §3.3 拆成 `03-2`（上：动态指标与极点位置）+`03-3`（下：响应与性能改善）、`03-2` 的 §3.7 拆出 `03-5 稳态误差`、`09-1`→`09-4 分部积分表格法`、`02 第2讲`→`02-1 数列极限典型例题`、`附录 常用积分表`→`附录 递推公式与反常积分`、`现控05`→`04 稳定性与李雅普诺夫方法`；③ **第3章重排为内容顺序**：03-1(3.1–3.2)/03-2(3.3上)/03-3(3.3下)/03-4(3.4–3.6)/03-5(3.7)/03-6(3.8–3.10)，标题（H1/文件名）与内容对齐，全库 `[[03-x]]`/总览/入口/12 索引链接同步；④ check-notes 全库 0 致命 0 待修。**教训**：拆分件若按“追加编号”（03-4/03-5/03-6）会与内容顺序错位，需按内容顺序重命名并同步所有链接；`Measure-Object -Line` 会少算行数，应改用 `(Get-Content).Count`；`现代控制理论` 无独立入口文件，各章返回目录用 `[[自动控制原理]]`。

**2026-08-25（现代控制理论按刘豹教材重排）**：`控制理论\现代控制理论\` 从「01-05+05-1」重排为教材章节 `00 绪论 + 01~05`（01状态空间表达式/02解/03能控能观/04稳定性与李雅普诺夫/05线性定常系统的综合），李雅普诺夫独立成第4章并前移到系统综合之前；各章 H1/小节号按教材（0.x/1.x/…/5.x），补齐缺口：1.6 求传递函数阵、1.7 离散状态空间表达式、1.8 时变/非线性表达式、2.5 离散解、2.6 连续系统离散化、3.4/3.7 离散能控能观；新增 `现代控制理论.md` 总览入口（返回目录 `[[自动控制原理]]`）；全库 `[[控制理论/现代控制理论/0X]]` 链接、09 第9章 导航、自动控制原理.md 入口同步。**注意**：模块章节按教材整章成文，`01 状态空间表达式` 因补稿达 472 行（>300），作为教材章节例外（check-notes 待修，可接受）；子节号与教材一致的为 01（1.1–1.8）与 04（4.1–4.5），02/03/05 的小节号为笔记内部顺序（章节号/名称已对齐教材）。**教训**：并行子代理改同一目录的章节编号会各自看到对方未完成的状态而误判冲突，应串行执行或由主代理统一做编号。

**2026-08-26（主库第1–8章按胡寿松教材对齐）**：8 章按教材小节号重排 + 补缺——第1章（1.1组成/1.2示例/1.4设计要求/1.5*发展）、第2章（2.1–2.8：微分方程/传函/环节结构图/信号流图梅森/线性化/MIMO/典型模型/套路）、第3章（3.1引论含典型输入与零状态响应/3.5稳定性含劳斯/3.7 Matlab；**时域校正小节移出第3章→留指引第6章**）、第4章（4.1–4.7：补多回路/PID影响/Matlab/实例）、第5章（5.1–5.10：补开环特性/伯德判稳/对数分析/实验确定/Matlab）、第6章（6.1–6.7：PID 拆 PD/PI/PID+Z-N 调节法则；装置小节标注教材 6.4 频率法示例）、第7章（7.1–7.9：补 7.4.5 极点-响应/7.5.3 采样周期影响；7.7 改名带数字计算机闭环）、第8章（8.1–8.5：补 8.1.3 分析方法/*8.4 反馈线性化）。方法：每章一个子代理（只动本章文件），主代理统一入口/索引别名同步；check-notes 全库 0 致命。**经验**：8 个子代理并行改各自章节（数字前缀独立）安全；共享入口文件（自动控制原理.md/12 资料索引）的别名须由主代理在全部章节完成后统一同步。

**2026-08-26（现代控制理论 01 按教材目录拆分）**：`01 状态空间表达式`（472 行）按教材 §1.1–1.8 拆为总览入口 + 4 件：`01-1 状态变量与模拟结构图`(1.1–1.2)、`01-2 状态空间表达式的建立`(1.3–1.4)、`01-3 线性变换与传递函数阵`(1.5–1.6)、`01-4 离散、时变与非线性的状态空间表达式`(1.7–1.8)；全库 >300 例外消除，check-notes 0 致命 0 待修。**经验**：教材章节若整章 >300，优先按教材目录（章内小节）拆为 NN-x 专题件，文本按小节边界用 PowerShell 截取（`$l[a..b]`），原文件改总览入口。

**2026-08-26（自控+现控薄弱小节补实轮）**：全量扫描自控/现控所有文件，剔除容器节（`##` 内含 `###`）与导航/Matlab 提示后，补实 20+ 处“一两句带过”的小节：现控 01（拆件 01-1~01-4 的 1.2.x/1.3.x/1.5.x/1.8.x）、05 综合（5.1.2 输出反馈与输出注入/5.1.3 镇定/5.2.1 观测器定义·存在性·G 设计/5.3.1/5.4.2/5.5.2/5.5.3）、主库 01（反馈原理/设计概述/示例）、03-1/03-4（3.1.1 引论/3.5.1-3.5.2 稳定性）、04（4.3.1/4.3.3/4.4.1-4.4.3 补实，**先瘦身与第6章重复的“根轨迹校正”节再扩**）、05-1~05-3（典型环节画像/判据/对数分析）、06-1（校正基本概念）、07-x（采样描述/离散时域/采样周期/数字 PID）、08（相平面分区线性化）、现控 02/03/04（时变格拉姆/PBH/格拉姆判据/最小实现/第二法）。**经验**：① 文件已近 300 行时补实必须先瘦身（优先压缩与其它章重复的内容）再扩，否则触发 >300 待修；② 补实以“真缺才补、重复不加”为原则，内容只保留一处、入口/速查只挂指路。

**2026-08-26（现控 03 按教材重排 + 三类例题补齐）**：`现代控制理论/03 能控性和能观性` 小节号按刘豹教材第3章重排为 3.1–3.10（能控定义/能控判别(3.2)/能观判别(3.3)/离散(3.4)/时变(3.5)/对偶(3.6)/标准型(3.7)/结构分解(3.8)/最小实现(3.9)/对消(3.10)），时变能控能观自立小节并指路 02 章 §2.3.3；例题按“能控、能观、能控能观分解都要有”补齐：3.2.3 能控判别例（Qc 秩）、3.3.3 能观判别例（Qo 秩+对角型直接看）、3.8.4 结构分解完整例（能控→能观→取 x_co 能控能观子空间→最小实现对应+MATLAB 核对）。主库 03 补教材 *3.8 实例分析（03-6 §3.11 综合实例）。**经验**：合并例应拆成单主题例题便于复习；分解例要做完整双侧链路（不能只做单侧）才与最小实现对应。

**2026-08-26（现控 03 拆分 + 05 补实 + 02 拆分扩写轮）**：① `03 能控性和能观性` 拆为入口 + `03-1 能控能观判别`(3.1–3.5) + `03-2 能控能观结构特性`(3.6–3.10)，三类例题（3.2.3 能控/3.3.3 能观/3.8.4 双侧卡尔曼分解含 rank(QoQc)=最小实现维数呼应）齐备；01-1/02 引用改指 03-1/03-2。② `05 线性定常系统的综合` §5.4.3 稳定性与离散化补实（谱条件与 s→z 映射、Jury/w 变换指路、ZOH 泰勒展开、判稳小例）。③ `02 状态空间表达式的解` 拆为入口 + `02-1 状态转移矩阵与状态方程的解`(2.1–2.3) + `02-2 离散系统与离散化`(2.4–2.5，扩写：G^k 三法+非齐次推导+例题、ZOH 推导+H 三分式+数值例) + `02-3 非线性系统状态方程的解`(2.6 新增：数值法/局部线性化/相平面/二法指路 + 2.7 套路)；01-4 引用改指 02-1。全库 128 文件 0 致命 0 待修。**经验**：拆入专题件时正文内“见 02 章 §2.x”这类文字引用也要随小节迁移改指新文件，否则语义漂移。

**2026-08-26（控制理论两级目录重构，用户要求）**：新建顶层 `控制理论\`，`自动控制原理\` 与 `现代控制理论\` 平级放入（后者从自控目录移出）；两者同属控制理论。`git mv` 保持历史，全库 path 型 wikilink 同步加 `控制理论/` 前缀（如 `[[控制理论/现代控制理论/01 状态空间表达式]]`；basename 型链接与跨文件 `[[自动控制原理]]` 入口链接不受影响）；`12 资料索引` 中 `C:\Users\23720\OneDrive\自动控制原理\` 为外部 OneDrive 路径**勿改**；AGENTS/README 的目录地图、统计口径（自动控制原理 34 + 现代控制理论 16 = 控制理论 50，全库 129 .md）同步更新。**经验**：① 批量改 wikilink 用「先长后短」替换顺序（`自动控制原理/现代控制理论/` → 现控前缀 → 自控前缀），防嵌套路径误替换；② 写入脚本必须保留 BOM/行尾状态（UTF8Encoding($false) vs 原 BOM），避免全文件 diff；③ check-notes 按 basename 解析链接，移动目录后必须人工 grep 复核 `[[旧路径/` 清零。

**2026-08-27（全科教材 PDF 对比补充轮）**：根据四本教材 PDF 全面对比补充笔记——
- **线代**：张宇《基础30讲》线代分册 213 页 OCR + 6 章对比，修正 2 处错误（行列式多项式次数条件、转置顺序），补充知识点（离散平均值定理/泰勒两大原则等）+ 例题。
- **高数**：张宇《基础30讲》高数分册 586 页 OCR + 12/15 章对比，修正 3 处错误（5类→6类/例7.1曲线/中值tip系数），补充七种未定式体系/脱帽法/抓大头/o(xⁿ)规则/泰勒原则/离散平均值定理/古鲁金定理/Γ函数等 + 多道例题。
- **自控**：胡寿松第八版 692 页文字层提取 + 9 章对比（5 子代理），修正 6 处（法则编号对调/描述函数实虚/系数约定加注/标题编号/标注错误），补充串级控制/频域设计/恒温箱设计例。
- **现控**：刘豹第三版 331 页 OCR + 5 章对比（3 子代理），修正 3 处 P1（§9-2引用/能控标准型命名/§4-8断链），补充性质五/约旦判据/能达性/输出稳定性+例4-1。
- **教训**：① 子代理处理大文件（>3000行）需较长时间，可先发消息要求部分结果；② 扫描件 OCR 质量与文字层提取差异大（自控有文字层 vs 现控纯扫描），处理策略不同；③ 临时 OCR 文件不入库（太大），保留磁盘供复查。

**2026-08-27（配图专项检查 + 清理）**：全库 `![[*.png]]` 引用↔`附件\` 双向核对——断链 0、非 png（jpg/svg）引用 0（全部为 png）；`文件名` MISSING 为 README/AGENTS 的 `![[文件名.png|尺寸]]` 占位示例（误报，勿改）。清理 7 张无引用孤儿 png（`自控-极点响应分图/s平面极点/单位阶跃响应/一阶系统响应/等幅振荡`<被 `响应A~J`/`s平面极点位置`/`典型环节阶跃响应`/`时域-性能指标` 取代> + `自控-开环闭环特性/二阶响应包络线`<边界图，用户确认一并删>），附件 112→105；删除误入库的根目录临时报告 `temp_zk_diff_report.md`（check-notes 曾误报）。**修复**：`数学\线性代数\05-1 特征值与相似对角化.md` 头部二次拼接（两套 frontmatter + 两个 H1：`第5讲（一）特征值与相似对角化`/`线代第5讲 特征值与特征向量`），删除重复的旧头块。同步 AGENTS 计数（活跃笔记 129→**138**（数学 68/英语 16/控制理论 54，08-27 教材补充轮已新增），附件 79→**105**）。check-notes 全库 0 致命 0 待修。**经验**：图片引用扫描需先拆表格内 `\|` 转义并剔除尾部反斜杠，否则 `...png\|220` 那类会因 `.png$` 匹配失败被误判孤儿；`git log -S` 对「从未在 md 文字中出现」的图查不到页迹（可能只在单次提交内新增即弃用），需用「引用→孤儿」双向扫描替代。

**2026-08-27（配图检查轮 2——jpg 冗余残留）**：同样方法双向核对，**断链 0、非png 引用 0、105 引用 ↔ 105 png 一一命中**，但发现 `附件\` 又出现 **14 个未跟踪 .jpg**（自绘图脚本 jpg 源图 + png 成品，jpg 残留），均为已有 png 的同名冗余、无任何引用，违反「附件统一 png」规范。用户确认后删除 14 个 jpg，附件回到 **105 png / 0 非png**，工作区干净（未跟踪文件，无 git 历史改动）。**经验（复用 08-27）**：图片扫描用 `!?\[\[([^\]]+)\]\]` 取内文后 `-replace '\\\|','|' -split '\|'` 取首段再 `-replace '\\$',''`，才能正确处理表格内 `\|` 转义；自绘图脚本应只留 png 成品、jpg 源图不入库。

**2026-08-27（配图检查轮 3——质量维度）**：对 105 png 用 System.Drawing 做技术扫描（尺寸/宽高比/体积）——无空白/损坏/低清；尺寸 508×408~2501×929 全可读、无 <3KB 文件。**尺寸一致性**：抽查 23 张 AR≥2 宽图，仅 3 张标 |430（应按规范 |520）——`自控-死区双积分结构图`(2.81)/`自控-主导极点偶极子`(2.61)/`自控-教材例6-3阶跃响应曲线`(2.24)，已改 |520；其余宽图均 ≥520/560/620/650/700 合规。**视觉抽检 10 张**（自控 Bode/描述函数/方框图、高数二维曲线/3D 二次曲面、线代三平面、宽屏多联图）均渲染清晰、标注可读、无重叠/空白。结论：配图质量良好，仅 3 处尺寸参数修正。**经验**：配图质量检查三层——① System.Drawing 读像素判空白/极宽；② 宽图（AR≥2）核对 |N 是否 ≥520；③ 按类别抽检渲染（3D 曲面/多联图为高风险项）。

**2026-08-27（配图全面检查 + 修复 3 张）**：全量 105 图（90 张子代理并行读图 + 15 张主代理复核全覆盖）——技术层 0 空白/损坏/低清；尺寸一致性上轮已修；视觉层 **103 正常、3 待修**：① `自控-响应J`（ζ=−1 单调发散，t≈12.7 出现贯穿 −3~3 的竖直红伪迹）；② `高数-相关变化率`（底端红标注 `x(以 v…` 截断且与 `地面` 重叠）；③ `自控-教材例6-5渐近幅频特性`（底部蓝色图题被边裁切）。**修复**（matplotlib/PIL 重建，均 read_image 自检通过）：① 响应J 用 numpy/scipy 算步响应，越界点置 NaN 让曲线干净止于顶部，消除竖直伪迹；② 相关变化率 重建梯子滑动图，底端标注改完整 `x 以速度 v 增大` 并下移避开 `地面`；③ 例6-5 用 PIL 裁掉被裁切图题（539 行以下）、在图身下方重排完整图题 `图6-35 例6-5 系统开环对数幅频渐近特性`，保留图身。三图 AR 均 <2，引用尺寸 |430/|280 不变。**经验**：① 全量图检用「子代理按批并行读图（15 张/批）+ 主代理复核子代理未返回批次」；② matplotlib 画发散响应曲线务必「越界置 NaN」否则两点间大跳变会画竖直贯穿伪迹；③ 重建自绘图可用 matplotlib（matplotlib 3.11 + MSYH 中文字体）+ read_image 自检；④ 图题被裁切（下半字超出画幅）无法靠补白恢复，用 Pillow「裁剪掉损坏图题 + 图身下方重排完整图题」。

**2026-08-28（全面内容检查轮：8 组子代理 + 主代理逐条重推复核）**：行尾覆盖事故第 4 次（9 文件 CRLF/LF 改写、diff 为空 → restore 归一）；清理 6 个未跟踪 jpg（同名 png 冗余）；AGENTS/README/数学.md 统计口径刷新（138/105/高数25专题12附录），README 12 处过时数据修正、`数学.md` 补 [[02-1]] 与 3 条附录链接（该文件 modify 停在 08-24，曾疑似被云同步回退到拆分前旧版）。主代理独立预核 13 个小/中型文件全过（双曲函数/定积分速查/三角代数/常用积分表半册/梅森例端到端复算/奈氏判据含劳斯交叉验证/常用空间图形等）。**修复清单（32 处，commit b4f71cb 等）**：
- **高数**：01-4 经典例 $\cos x-e^{-x^2/2}\sim-\frac{x^4}{12}$ 补负号（展开相减+数值复验）、抓最低次括注自相矛盾改 $\sqrt x$ 提出式、张宇例1.15 还原 $e^{1/(x-1)}$ 重写左右极限（原函数在 x=1 连续不存在"左右不等"问题，OCR 丢分式层）、03 讲"可导有界⟹f'不一定有界"反例换 $x^2\sin(1/x^2)$（$\sqrt x\sin(1/x)$ 在 0 不可导名不副实）、06-2 三处（泰勒例 f'''≥12 结论方向错→Hermite 反例 p=-2x³+3x² 否证，改为 max|f'''|≥12 构造法正解并删"f'(0)=f'(1)=0 消去中点导数"的错误一步；e^x=kx 漏 k<0 恰一负根分支 te^t=-1/k；柯西例目标式改 $\frac{f(b)-f(a)}{\ln b-\ln a}=\xi f'(\xi)$ 并辨析原式不可证）、08-3 等距仿射公式补 /n（$\xi_i=\frac{i+C}{n}$）、10 讲例10.14 绕斜轴体积 $\frac{\pi}{60}\to\frac{\sqrt2\pi}{60}$（沿轴厚度 dh≠dx，三种方法交叉验证）、11-2 例11.7 换元证明与结论脱节→保序两行证明+姊妹题注记、14 讲"和为常数"例举 $\frac{1}{1+x+y}$ 不满足性质→换 $\frac{e^x}{e^x+e^y}$。
- **线代**：01 讲例6 重解（倍加化爪模板搬错题，正确按列展开 $D_4=a_1x^3+a_2x^2+a_3x+a_4$，原文 $x^4$ 连 x=0 都过不了）、04 讲例5 三连错修正（|A|=λ+1 非 2(λ+1)、a=1 非 -2、通解 $(-5,-6,0)^T+k(0,-1,1)^T$ 全部回代验证）、02-1 对角阵交换性表述反向修正（互异⟹只与对角阵交换）、05-2 删重复要点段+补 abstract。
- **自控**：02-2 双容水槽增益 $K_1K_2\to1/K_2$（串联稳态流量相等增益相消+量纲验证，K_v 补定义）、03-2 β=75° 行 σ%≈40%→43%（$e^{-\pi\tan75°}$）、调节时间经验式补欠阻尼适用域警告（ζ=2 过阻尼反例 11.2s vs 1.75s）、03-5 正弦稳态误差幅值 0.055→0.057（$|\Phi_e(j5)|$ 独立复算）、04 章 §4.2.2 法则编号统一（渐近线=4 实轴段=3，2026-08-27 对调修复的遗留半截）。10 附录幅值裕度 h 定义精确化、附录空间图形两格修正（双曲线绕 z 轴旋转结果单叶确定、平面交线表述）。
- **现控**：04 章「教材例4-1」矩阵与传函矛盾重构（按所印 diag(-1,1)+B=(1,1)ᵀ 算 W=-2/(s²-1) 无对消——网络额度不足未能回查教材，改用可严格验证的友矩阵实现 (A=[[0,1],[1,0]],B=(0,1)ᵀ,C=[-1,1]) 且与 03-2 §3.10 能控实现对消=不能观教学点呼应，Q_o 秩1 验证在文）、克拉索夫斯基 $\dot x=Jx\to\dot f=J(x)f(x)$（沿轨线精确非近似）、05-2 直读构型指向 §2.5.2→01-4 §1.7.2、01-2 最小实现陈旧引文 §4-8→03-2 §3.9、01-2 h 系数实现命名辨析、03-2 §3.6 对偶关系补定义+秩互证推导+§3.7 补指路。
- **英语**：2015 小作文「通知+号召→通知+招募」（与 03 文件矛盾定案）、2021 新题型主题统一为「与更有权力者意见相左」（L35 vs L81，Bloomberg 题材裁定 L81 正确）、纪要模板 `12:00 am→12:00 noon`、02 写作方法 How 大写、03 标题范围 2010–2026、翻译评分档补齐四档、11 翻译 CEO 句去假「真题2020」标签改例句+补译文、从句三节补 3 个断句译文示范、06 大作文练习栏补 2 个示范首段、14 完型入口描述删「答案」承诺（原承诺落空；18 组真题句官方答案未能核实，诚实降级指路讲义 PDF）。
- **B 类补实**：07 第7讲新增「边际与弹性（经济应用，数三向）」小节（MC/MR/ML、η=−pQ'/Q、η 与收益 R'=Q(1−η) 对照表、Q(p)=100−2p 弹性例——abstract 曾承诺经济应用而正文缺失）。
- **误报甄别**：英语组报告 A-4（worthwhile/detrimental 同句悖论）grep 全库无该引文、按误报处理；「how……」实际位于 02 写作方法而非 03 分类（报告张冠李戴，照实修）。**教训：① 子代理引文可能凭印象拼接，凡 grep 不中的 A 类一律丢弃；② 大量数学子代理并行时「每条主代理重推」环节是过滤错误的核心，本轮约 30% 的 A 类发现经复推后修正了方案甚至否决。**
**遗留待办（08-28 收口后更新）**：① ~~自控 05–09 章组报告未返回~~ **已补齐**——报告迟到返回后，主代理用库根目录的教材提取文本 `temp_zk_ch05.txt`（gitignore 忽略、不入库）原文裁定：对数判据穿越方向为「正穿越≡Γ 上→下 ⟺ 相频下→上」，据此修复 `05-2` L71 规则（写反）与图注（方向词）、`07-1` 例1 通分符号（幂级数首项系数复验）、`10 附录` 补幅值裕度几何解释、`11 MATLAB速查` canon'companion' 由「能控标准型」更正为「能观标准型排布」（子代理本机 MATLAB R2026a 实测 + 库内转置关系双重验证）；B 类补 z 域复位移定理/卷积定理（07-1）与死区行描述（08-1）。该组其余数值链（06-1 超前滞后全链含 h''=14.2 dB、07-2 Jury 综合例、08-2 死区双积分等）复核通过，05–11 章核对就此闭环。② 现控 04 章「教材例4-1」**已按 OCR 核定还原**——`temp_xk_ch04.txt` L161-174 保留 c=(1,0)、det(λI−A)=(λ+1)(λ−1)、分子 s−1/分母 (s+1)(s−1) 四处锚点，反推唯一自洽原例 $A=\begin{bmatrix}0&1\\1&0\end{bmatrix},B=\binom{1}{-1},C=[1\ 0]$（对消的 +1 模态**不能控**，Q_c 秩 1；此前临时构造的「不能观」版本已替换）。**经验**：根目录 `temp_*.txt` 教材提取文本可作子代理与主代理共同的裁定基准（本轮两处争议直接引原文定案）；`canon 'companion'` 的输出是能控型的完整转置（含 b/c 互换），按库内对偶定义即能观标准型——MATLAB 命名易误导，已在校验表内标注。

**2026-08-28（教材 OCR 文件迁移 + 高数 13/14/15 讲按课本对齐 + 例题融入知识点）**：① 用户将全部教材 OCR 临时文本移出 git 跟踪（`chore:ba664bd`），现统一存于**`教材OCR\` 目录**（不入库）：`张宇基础30讲-高等数学分册-2027.txt`、`胡寿松自动控制原理-第八版.txt`、`刘豹现代控制理论-第三版.txt`、分章子目录——旧「根目录 temp_*.txt」路径作废，对照课本时改用新路径；② 用户明确「**例题不集中堆放、紧跟知识点；内容与顺序按课本 OCR**」的长的要求，完成：13 讲拆分件按内容重排（13-2 改名「链式法则与隐函数」、13-3 改名「二元极值·条件极值与最值」，方向导数并入 13-1，例 1~6 全部穿插）；14 讲对称性两节合并为权威小节（普通 6 类表 + 轮换限定直角坐标系 + 偶倍奇零口诀 + 区别与联系 + 决策顺序），小节顺序按课本（概念性质→对称性→计算→换元法→古鲁金），补例 14.3/14.15/14.16 与轮换完整例；15 讲全微分方程移至降阶后（课本第 6 节）、**新增一阶常系数线性差分方程（仅数三，特解设定表）**、内部按课本顺序重排、全部例题穿插（含课本例 15.11/15.14/15.15/15.16、习题 15.2）。**注意**：OCR 文本有系统性丢字符（分数线、撇号、下标），凡引用例题须用答案反推校验（例：15.14 原题实为 $yf'(x)+y^2$，OCR 丢撇号）。

**2026-08-28（线代分册重 OCR + 教材OCR 目录索引化）**：① `27张宇基础30讲线代.pdf`（213 页**纯扫描无文字层**）用 PyMuPDF zoom2.0 渲染 + RapidOCR 全本重 OCR（08-27 那次输出文件已丢失）→ `教材OCR\张宇基础30讲-线性代数分册-2027.txt`（约 10698 行），并按 6 讲拆出 `分章\线代-第00讲~第06讲` 7 件——**书内页 = PDF 页 − 7**（书内页 1 = PDF 8，与自控 +8 偏移不同书不同偏移），6 讲起始 PDF 页 18/52/89/121/146/185 已逐一经 OCR 锚点验证；② 新增 `教材OCR\README.md` 索引入库（.gitignore 用 `教材OCR/*` + `!教材OCR/README.md` 白名单实现「大文本忽略、索引入库」），内含全本/分章页码区间表、旧 `temp_*` 名映射、OCR 丢符号警告；③ RapidOCR v1.4.4 传参用 `RapidOCR(params={'Det.limit_side_len':1600,'Det.limit_type':'max'})`（dict 形式，旧 kwargs 不再适用）。**教训**：① pip 默认源在本机极慢（20 分钟无进展、CPU 空转），换清华镜像秒下；② 同库有并行会话工作时，提交只 `git add` 自己的路径，禁用 `add -A` 扫入对方未完成改动。

**2026-08-28（高数+线代全分册张宇例题对照完结，用户逐讲驱动）**：继 13/14/15 讲后完成高数 1~12 讲与线代 01/02/03(含07)/04/05/06 逐讲对照（每讲一轮：grep 例题清单→读主题→对照→补缺口→独立验算→提交）。累计新增例题 40+ 道（标注张宇例号或改编说明）、知识点缺口补齐：13-2 隐函数求 dz 两法、15 讲差分方程、07 讲复利现值、06-1 导数零点定理、08-1 原函数 vs 可积对照表、08-4 变限奇偶周期、线代 04 讲 AAᵀx=Ab 必有解+抽象表示写通解等。**质量流程（子代理双向核对）**：子代理 A 独立验算 21 条断言→18 对 3 错（13-3 漏 z_x/z 项致 A=C=-2/3、05.3 题面 OCR 丢 x² 平方、9.22 条件 f'<-2uf 推不出 F''<0 且给反例 f=(1-u)e^{-u²}）——均修正；子代理 B 核查线代 01 忠实度→6 处修正（例6 错标例1.3、例10 恢复 4 阶原题答案 a+b、例9/11 改标改编、克拉默补齐次方向与 AB=O）。**遗留**：线代例6 编号归属与例1.12 矩阵本体需目验原书 PDF（书内页 25-26/37）；线代 00 讲自编入门保持现状。**提交纪律**：并行会话期间全程指定路径 add。
