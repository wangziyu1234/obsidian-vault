---
create: 2026-08-28
modify: 2026-09-09
tags: [知识点, 高等数学, 公式速查]
---

> 返回：[[15-2 线性微分方程解的结构与二阶常系数解法|15-2]] | 返回目录：[[高等数学公式速查]]

# 第15讲（二-2）高阶齐次、反推特征根重数表与欧拉方程

> [!abstract] 本讲定位
> n（n>2）阶常系数齐次方程（例题7）、**反推最小特征根重数表**、解的稳定性/有界性判断、欧拉方程（例题8、9）。对应教材第 15 讲第二部分的“n 阶推广与欧拉方程”。

## 🧮 15.10 n（n>2）阶常系数齐次线性方程（二阶规则的自然推广）

方程 $y^{(n)}+a_1y^{(n-1)}+\cdots+a_{n-1}y'+a_ny=0$ 的特征方程

$$
r^n+a_1r^{n-1}+\cdots+a_{n-1}r+a_n=0
$$

恰有 $n$ 个根（计重数，复根成对共轭出现），每类根拼装一块、相加即通解（恰含 $n$ 个独立任意常数）：

| 特征根（重数 $k$） | 通解中贡献的基解组 |
| :-- | :-- |
| $k$ 重实根 $r$ | $e^{rx},\ xe^{rx},\ \dots,\ x^{k-1}e^{rx}$ |
| $k$ 重复根 $\alpha\pm\beta i$ | $x^j e^{\alpha x}\cos\beta x$ 与 $x^j e^{\alpha x}\sin\beta x$（$j=0,1,\dots,k-1$） |

> [!example] ✏️ 例题7. 三重实根
> **题目**：解 $y'''+3y''+3y'+y=0$。
> **解**：特征方程 $(r+1)^3=0$ 有三重根 $r=-1$ ⟹ 通解 $y=(C_1+C_2x+C_3x^2)e^{-x}$。

## 🧮 15.11 反推时最小特征根重数表（必背速查）

以下只对**已确认的齐次解分量**，或带独立任意常数的通解方向反推特征根；给出的重数是下界，不总是恰好等于：

| 解中含 | 最小重数 / 复根重数 |
|:--|:--|
| $e^{rx}$ 和 $e^{rx'}$（$r\ne r'$） | $r$ 单根、$r'$ 单根 |
| $(C_1+C_2x)e^{rx}$ | $r$ **二重根** |
| $x^{k-1}e^{rx}$ 出现 | $r$ **$k$ 重根** |
| $e^{\alpha x}\cos\beta x$ 或 $e^{\alpha x}\sin\beta x$ | $\alpha\pm\beta i$ **单复根** |
| $xe^{\alpha x}\cos\beta x$ 或 $xe^{\alpha x}\sin\beta x$ | $\alpha\pm\beta i$ **二重复根** |
| $x$ 或 $C$（$e^{0x}$）出现于通解 | $r=0$ 是相应重数的根 |

> [!warning] 仅知道一个非齐次解，不能反推齐次特征根
> 若只知 $y=e^{2x}+e^x+xe^x$ 是 $y''+py'+qy=f(x)$ 的一个解，而 $f$ 未知，那么任取 $p,q$ 都可定义相应 $f$，无法确定参数。
>
> 例如 $p=q=0$ 时，右端取 $f=4e^{2x}+(x+3)e^x$ 就已成立，不能强行断言 $p=-3,q=2$。

> [!example] ✏️ 改成“已知通解”才可确定
> 若已知通解为
>
> $$
> y=C_1e^{2x}+C_2e^x+xe^x
> $$
>
> 独立常数对应齐次解 $e^{2x},e^x$，故特征方程为 $(r-2)(r-1)=0$，得到 $p=-3,q=2$。再代 $xe^x$ 求出右端 $f(x)=-e^x$。

> [!note] 模态有界性与周期性
> 对非零模态 $x^je^{\alpha x}\cos\beta x$ 或 $x^je^{\alpha x}\sin\beta x$：当 $x\to+\infty$，$\alpha<0$ 时趋零；$\alpha>0$ 时无界；$\alpha=0$ 时还要看多项式因子，$j>0$ 通常无界，$j=0$ 才有界。
>
> 单个非零正弦模态周期为 $2\pi/|\beta|$；多种频率的和需有公共周期，不是“所有实部为零”就一定周期。常数解和零解单独处理。判断某个具体解时还须检查对应模态的系数是否为零。

## 🧮 15.12 欧拉方程（仅数学一，可化为常系数）

形如

$$
x^ny^{(n)}+a_{n-1}x^{n-1}y^{(n-1)}+\cdots+a_1xy'+a_0y=f(x)
$$

的方程称欧拉方程。做**自变量替换** $x=e^t$（$x<0$ 时取 $x=-e^t$，或直接令 $t=\ln\lvert x\rvert$），记 $D=\frac{d}{dt}$，则

$$
xy' = Dy
$$

$$
x^2y'' = D(D-1)y
$$

$$
x^3y''' = D(D-1)(D-2)y
$$

代入后化为以 $t$ 为自变量的**常系数线性微分方程**，后续完全按上表求解，最后代回 $t=\ln\lvert x\rvert$。

> 二阶速查：$x^2y''+pxy'+qy=f(x)\ \xrightarrow{\ x=e^t\ }\ \frac{d^2y}{dt^2}+(p-1)\frac{dy}{dt}+qy=f(e^t)$。

> [!example] ✏️ 例题8. 欧拉方程（二重根）
> **题目**：解 $x^2y''-xy'+y=0$。
> **解**：令 $x=e^t$，$xy'=Dy$，$x^2y''=D(D-1)y$，得 $[D(D-1)-D+1]y=0$ 即 $(D-1)^2y=0$。
>
> 特征方程 $(r-1)^2=0$，先在 $x>0$ 上得到 $y=(C_1+C_2\ln x)x$。统一两半轴写作 $y=x(C_1+C_2\ln|x|)$，解定义在不跨过 $x=0$ 的区间；方程在0处最高阶系数为零，不能按常规通解直接跨过。

> [!example] ✏️ 例题9. 欧拉方程（相异实根）
> **题目**：解 $x^2y''+xy'-y=0$。
> **解**：令 $x=e^t$，$x^2y''=D(D-1)y$，$xy'=Dy$：
>
> $$
> [D(D-1)+D-1]y=(D^2-1)y=0
> $$
>
> 特征根 $r=\pm1$：
>
> $$
> y(t)=C_1e^t+C_2e^{-t} \implies y=C_1x+\frac{C_2}{x}
> $$