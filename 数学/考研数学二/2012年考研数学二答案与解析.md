# 2012年考研数学二答案与解析

> 我的解答，未与官方核对。

## 答案速查

> 选择题：1.C　2.A　3.B　4.D　5.D　6.D　7.C　8.B。

> 填空题：9.$1$；10.$\pi/4$；11.$0$；12.$\sqrt x$；13.$(-1,0)$；14.$-27$。

15. $a=1,k=1$。

16. 极大值为 $\mathrm e^{-1/2}$，极小值为 $-\mathrm e^{-1/2}$。

17. 面积为2，体积为 $\dfrac{2\pi}{3}(\mathrm e^2-1)$。

18. 积分为 $16/15$。

19. $f(x)=\mathrm e^x$，所求曲线的唯一拐点为 $(0,0)$。

20. 不等式证明见正文。

21. 数列极限为 $1/2$。

22. $a=-1$；通解 $\boldsymbol x=(0,-1,0,0)^{\mathrm T}+t(1,1,1,1)^{\mathrm T}$。

23. $a=-1$；正交变换后的标准形可取 $2y_1^2+6y_2^2$。

## 一、选择题（1～8）

**1.（2012.1）答案：C，共2条渐近线。**

**第一步：先写清定义域，再约分。**

分母分解为 $x^2-1=(x-1)(x+1)$，所以原函数的定义域是 $x\ne1,-1$。在这个定义域内，

$$
y=\frac{x(x+1)}{(x-1)(x+1)}
=\frac{x}{x-1}
=1+\frac1{x-1}.
$$

约去因子 $x+1$ 只是为了便于研究极限，不能把原先排除的点 $x=-1$ 加回定义域。

**第二步：检查可能的竖直渐近线。**

当 $x$ 从两侧趋于1时，

$$
\lim_{x\to1^-}y=-\infty,
\qquad
\lim_{x\to1^+}y=+\infty.
$$

因此 $x=1$ 是竖直渐近线。

在另一个无定义点，

$$
\lim_{x\to-1}y
=1+\frac1{-2}
=\frac12.
$$

极限有限，因此 $x=-1$ 只是可去间断点，不是竖直渐近线。其余有限点附近函数连续，不可能再产生竖直渐近线。

**第三步：检查无穷远处。**

由 $1/(x-1)\to0$，得到

$$
\lim_{x\to+\infty}y
=\lim_{x\to-\infty}y=1.
$$

所以两端共有同一条水平渐近线 $y=1$，应当只计一条。又因为函数在无穷远处趋于有限值，不存在斜率非零的斜渐近线。

综上，渐近线为 $x=1$ 和 $y=1$，共2条，选 C。

**2.（2012.2）答案：A，$(-1)^{n-1}(n-1)!$。**

**第一步：利用乘积中在零点消失的因子。**

记

$$
g(x)=\prod_{k=2}^{n}(\mathrm e^{kx}-k),
\qquad f(x)=(\mathrm e^x-1)g(x).
$$

当 $n\ge2$ 时，$g$ 是有限个可微函数的乘积，因而可微。对 $f$ 求导，

$$
f'(x)=\mathrm e^x g(x)+(\mathrm e^x-1)g'(x).
$$

在 $x=0$ 处，第二项含有因子 $\mathrm e^0-1=0$，所以不必逐项展开 $g'(x)$，直接得到

$$
f'(0)=g(0).
$$

**第二步：分别整理符号和绝对值。**

$$
\begin{aligned}
g(0)
&=(1-2)(1-3)\cdots(1-n)\\
&=(-1)(-2)\cdots[-(n-1)]\\
&=(-1)^{n-1}(n-1)!.
\end{aligned}
$$

共有 $n-1$ 个负因子，因此符号是 $(-1)^{n-1}$；各因子的绝对值从1到 $n-1$，乘积是 $(n-1)!$。

也可以从导数定义直接看出这一点：因为 $f(0)=0$，

$$
f'(0)=\lim_{x\to0}\frac{\mathrm e^x-1}{x}
\prod_{k=2}^{n}(\mathrm e^{kx}-k)
=1\cdot\prod_{k=2}^{n}(1-k).
$$

当 $n=1$ 时，$f(x)=\mathrm e^x-1$，故 $f'(0)=1$；通式也给出 $(-1)^0 0!=1$。因此选 A。

**3.（2012.3）答案：B，充分非必要条件。**

把两个命题分别记为：$P$ 表示部分和数列 $\{S_n\}$ 有界，$Q$ 表示原数列 $\{a_n\}$ 收敛。需要检验 $P\Rightarrow Q$ 与 $Q\Rightarrow P$。

**第一步：证明充分性。**

因为所有 $a_n>0$，所以

$$
S_{n+1}-S_n=a_{n+1}>0.
$$

因此 $\{S_n\}$ 严格单调增加。如果它有界，单调有界定理保证存在有限的 $S$，使得

$$
S_n\longrightarrow S.
$$

把收敛数列的指标平移一位，仍有 $S_{n-1}\to S$。由 $n\ge2$ 时 $a_n=S_n-S_{n-1}$，得到

$$
\lim_{n\to\infty}a_n
=\lim_{n\to\infty}S_n-\lim_{n\to\infty}S_{n-1}
=S-S=0.
$$

所以 $\{a_n\}$ 收敛，充分性成立。正项条件在这里的作用，是先保证部分和单调，从而能由有界推出收敛。

**第二步：用反例否定必要性。**

取 $a_n=1$。它满足所有项为正，并且

$$
a_n\longrightarrow1,
\qquad S_n=1+\cdots+1=n\longrightarrow+\infty.
$$

这说明原数列收敛时，部分和仍可能无界，所以必要性不成立。

因此 $\{S_n\}$ 有界是 $\{a_n\}$ 收敛的充分非必要条件，选 B。

**4.（2012.4）答案：D，$I_2<I_1<I_3$。**

本题不需要求 $\mathrm e^{x^2}\sin x$ 的原函数。把积分按正弦函数的半周期拆开，比较各段的大小即可。

**第一步：将各段写成正数。**

令

$$
J_k=\int_{(k-1)\pi}^{k\pi}\mathrm e^{x^2}|\sin x|\,\mathrm dx,
\qquad k=1,2,3.
$$

每个积分区间内部的被积函数均为正，故 $J_k>0$。作平移 $x=t+(k-1)\pi$，可得

$$
J_k=\int_0^\pi\mathrm e^{[t+(k-1)\pi]^2}\sin t\,\mathrm dt.
$$

当 $0<t<\pi$ 时，三个指数中的底数都非负，并且依次严格增大。因此

$$
\mathrm e^{t^2}\sin t
<\mathrm e^{(t+\pi)^2}\sin t
<\mathrm e^{(t+2\pi)^2}\sin t.
$$

在同一区间积分，得到

$$
0<J_1<J_2<J_3.
$$

**第二步：恢复各半周期的正负号。**

$\sin x$ 在 $(0,\pi)$ 为正，在 $(\pi,2\pi)$ 为负，在 $(2\pi,3\pi)$ 为正，所以

$$
I_1=J_1,
\qquad I_2=J_1-J_2,
\qquad I_3=J_1-J_2+J_3.
$$

由 $J_2>J_1>0$，有 $I_2<0<I_1$。再比较 $I_3$ 和 $I_1$：

$$
I_3-I_1=J_3-J_2>0.
$$

故 $I_2<I_1<I_3$，选 D。

**5.（2012.5）答案：D。**

**第一步：把偏导数的符号转化为单变量的单调性。**

固定 $y$，函数 $x\mapsto f(x,y)$ 的导数是 $f_x(x,y)>0$，所以它关于 $x$ 严格增加。

固定 $x$，函数 $y\mapsto f(x,y)$ 的导数是 $f_y(x,y)<0$，所以它关于 $y$ 严格减少。

因此，要使函数值从 $f(x_1,y_1)$ 增加到 $f(x_2,y_2)$，可以让 $x$ 增大，同时让 $y$ 减小。

**第二步：插入一个中间点进行严格比较。**

取选项 D 的条件 $x_1<x_2$、$y_1>y_2$。先在水平线上比较：

$$
f(x_1,y_1)<f(x_2,y_1).
$$

再在竖直线上比较。因为 $y_2<y_1$，而函数关于 $y$ 递减，故

$$
f(x_2,y_1)<f(x_2,y_2).
$$

把两步连接起来，得到

$$
f(x_1,y_1)<f(x_2,y_1)<f(x_2,y_2).
$$

这就证明了 D 的充分性。

也可用一元中值定理写出函数值之差：存在 $\xi\in(x_1,x_2)$、$\eta\in(y_2,y_1)$，使得

$$
\begin{aligned}
f(x_2,y_2)-f(x_1,y_1)
&=[f(x_2,y_2)-f(x_2,y_1)]\\
&\quad+[f(x_2,y_1)-f(x_1,y_1)]\\
&=f_y(x_2,\eta)(y_2-y_1)
+f_x(\xi,y_1)(x_2-x_1)>0.
\end{aligned}
$$

两项均为正，因而无需比较两个偏导数的绝对值。

**6.（2012.6）答案：D，$-\pi$。**

**第一步：根据边界写出积分区域。**

在 $-\pi/2\le x\le\pi/2$ 上，$\sin x\le1$，所以竖直截线从 $y=\sin x$ 延伸到 $y=1$：

$$
D=\left\{(x,y):-\frac\pi2\le x\le\frac\pi2,
\quad\sin x\le y\le1\right\}.
$$

区域的下边界随 $x$ 改变，而且整个区域不关于 $y$ 轴对称。例如 $(-\pi/4,-1/2)$ 在区域内，它关于 $y$ 轴的对称点却不在区域内。因此不能直接利用被积函数中含有 $x$ 就消去该项。

**第二步：先对 $y$ 积分。**

$$
\begin{aligned}
I
&=\int_{-\pi/2}^{\pi/2}\mathrm dx
  \int_{\sin x}^{1}(xy^5-1)\,\mathrm dy\\
&=\int_{-\pi/2}^{\pi/2}
  \left[\frac{xy^6}{6}-y\right]_{\sin x}^{1}\mathrm dx\\
&=\int_{-\pi/2}^{\pi/2}
  \left[\frac x6(1-\sin^6x)+\sin x-1\right]\mathrm dx.
\end{aligned}
$$

代入下限时，$xy^6/6$ 产生 $x\sin^6x/6$，而 $-y$ 产生 $-\sin x$；外层减去下限后，后者变为 $+\sin x$。

**第三步：对所得的一元积分使用奇偶性。**

$\sin^6x$ 是偶函数，所以 $1-\sin^6x$ 也是偶函数，与 $x$ 相乘后为奇函数。另一项 $\sin x$ 本身也是奇函数。因此

$$
\int_{-\pi/2}^{\pi/2}x(1-\sin^6x)\,\mathrm dx=0,
\qquad
\int_{-\pi/2}^{\pi/2}\sin x\,\mathrm dx=0.
$$

只剩下常数项：

$$
I=-\int_{-\pi/2}^{\pi/2}1\,\mathrm dx=-\pi.
$$

故选 D。

**7.（2012.7）答案：C。**

题目要求的是对任意 $c_1,c_2,c_3,c_4$ 都线性相关的向量组。因此需要找出不依赖这些参数的关系。

**第一步：检查选项 C。**

把 $\alpha_1,\alpha_3,\alpha_4$ 按列排成矩阵，得到

$$
M_C=\begin{pmatrix}
0&1&-1\\
0&-1&1\\
c_1&c_3&c_4
\end{pmatrix}.
$$

第二行恰为第一行的相反数，无论第三行如何取值，都有

$$
\det M_C=0.
$$

三个三维向量线性相关当且仅当以它们为列的三阶行列式为零，因此 C 恒成立。

从几何角度看，这三个向量的前两个分量之和都是0，所以它们都属于子空间

$$
W=\{(u,v,w)^{\mathrm T}:u+v=0\}
=\operatorname{span}\{(1,-1,0)^{\mathrm T},(0,0,1)^{\mathrm T}\}.
$$

$W$ 的维数只有2，任取其中三个向量都必线性相关。

**第二步：说明其他选项为什么不能保证相关。**

分别计算列行列式，得到

$$
\det(\alpha_1,\alpha_2,\alpha_3)=-c_1,
\qquad
\det(\alpha_1,\alpha_2,\alpha_4)=c_1,
$$

$$
\det(\alpha_2,\alpha_3,\alpha_4)=-(c_3+c_4).
$$

取 $c_1=1$ 时，前两个行列式均不为零；取 $c_3=1,c_4=0$ 时，最后一个也不为零。因此 A、B、D 都会因参数的取值而改变相关性，只有 C 满足题意。

**8.（2012.8）答案：B。**

**第一步：从相似对角化读出原来的特征向量。**

记 $\Lambda=\operatorname{diag}(1,1,2)$。由 $P^{-1}AP=\Lambda$，左乘 $P$ 得

$$
AP=P\Lambda.
$$

按列比较，有

$$
A\alpha_1=\alpha_1,
\qquad A\alpha_2=\alpha_2,
\qquad A\alpha_3=2\alpha_3.
$$

因此 $\alpha_1,\alpha_2$ 属于特征值1的特征子空间，$\alpha_3$ 属于特征值2的特征子空间。

**第二步：考察新矩阵的第一列。**

矩阵乘法具有线性性，故

$$
A(\alpha_1+\alpha_2)
=A\alpha_1+A\alpha_2
=\alpha_1+\alpha_2.
$$

这说明新第一列仍对应特征值1。它不可能是零向量，否则 $\alpha_1=-\alpha_2$，与 $P$ 可逆矛盾。

**第三步：验证 $Q$ 可逆，并按新列顺序写出对角阵。**

有

$$
Q=PS,
\qquad
S=\begin{pmatrix}1&0&0\\1&1&0\\0&0&1\end{pmatrix}.
$$

由于 $\det S=1$，$Q$ 是两个可逆矩阵的乘积，也可逆。逐列作用可得

$$
AQ=(\alpha_1+\alpha_2,\alpha_2,2\alpha_3)=Q\Lambda.
$$

左乘 $Q^{-1}$，得到

$$
Q^{-1}AQ=\begin{pmatrix}1&0&0\\0&1&0\\0&0&2\end{pmatrix}.
$$

对角元的顺序由对应特征向量的列顺序决定。这里前两列仍对应1，第三列仍对应2，所以选 B。

## 二、填空题（9～14）

**9.（2012.9）答案：$1$。**

**第一步：确定计算点的纵坐标。**

把 $x=0$ 代入原方程，得到

$$
1-y=\mathrm e^y,
\quad\text{即}\quad y+\mathrm e^y=1.
$$

$y=0$ 满足该等式。又因为函数 $h(y)=y+\mathrm e^y$ 的导数 $h'(y)=1+\mathrm e^y>0$，方程至多有一个实根，所以 $y(0)=0$。

令 $F(x,y)=x^2-y+1-\mathrm e^y$，则

$$
F_y=-1-\mathrm e^y\ne0.
$$

因此原方程在计算点附近确能确定可微的隐函数，下面的隐函数求导有依据。

**第二步：对等式求一次导数。**

注意 $y$ 本身是 $x$ 的函数，由链式法则，

$$
2x-y'=\mathrm e^y y',
\qquad
2x=(1+\mathrm e^y)y'.
$$

代入 $x=0,y=0$，得到 $y'(0)=0$。

**第三步：再求一次导数。**

右边是两个函数的乘积，应同时对两个因子求导：

$$
\begin{aligned}
2
&=\frac{\mathrm d}{\mathrm dx}\big[(1+\mathrm e^y)y'\big]\\
&=\mathrm e^y(y')^2+(1+\mathrm e^y)y''.
\end{aligned}
$$

这里 $\mathrm e^y(y')^2$ 中的两个 $y'$，分别来自对 $\mathrm e^y$ 求导和原有的乘因子。

在 $x=0$ 处代入已知值，

$$
2=1\cdot0^2+2y''(0),
\qquad y''(0)=1.
$$

**10.（2012.10）答案：$\pi/4$。**

**第一步：把分母中的 $n^2$ 提出来。**

将括号内各项统一记为求和，原式为

$$
n\sum_{k=1}^{n}\frac1{k^2+n^2}.
$$

对每一项，

$$
\frac{n}{k^2+n^2}
=\frac1n\frac1{1+(k/n)^2}.
$$

因此待求极限可以写成

$$
\lim_{n\to\infty}\frac1n
\sum_{k=1}^{n}\frac1{1+(k/n)^2}.
$$

**第二步：识别定积分的黎曼和。**

把区间 $[0,1]$ 等分成 $n$ 段，每段长度为

$$
\Delta t=\frac1n.
$$

在第 $k$ 段取右端点 $t_k=k/n$，并令 $g(t)=1/(1+t^2)$，则上式恰为

$$
\lim_{n\to\infty}\sum_{k=1}^{n}g(t_k)\Delta t.
$$

$g$ 在闭区间 $[0,1]$ 上连续，所以可积，且这些黎曼和收敛到该定积分。

**第三步：计算定积分。**

$$
\begin{aligned}
\lim_{n\to\infty}n\sum_{k=1}^{n}\frac1{k^2+n^2}
&=\int_0^1\frac{\mathrm dt}{1+t^2}\\
&=[\arctan t]_0^1\\
&=\frac\pi4.
\end{aligned}
$$

其中积分区间的右端点是 $n/n=1$，外面的因子 $1/n$ 对应分割宽度，这两部分共同确定了黎曼和。

**11.（2012.11）答案：$0$。**

**第一步：引入中间变量。**

设

$$
u=\ln x+\frac1y,
\qquad z=f(u).
$$

原式的定义域要求 $x>0,y\ne0$，在该定义域内两个内层函数均可微。

**第二步：分别计算两个偏导数。**

对 $x$ 求偏导时把 $y$ 当作常数，所以 $1/y$ 的偏导数为0：

$$
u_x=\frac1x,
\qquad
z_x=f'(u)u_x=\frac{f'(u)}x.
$$

对 $y$ 求偏导时把 $x$ 当作常数，所以 $\ln x$ 的偏导数为0：

$$
u_y=-\frac1{y^2},
\qquad
z_y=f'(u)u_y=-\frac{f'(u)}{y^2}.
$$

两个外层导数 $f'(u)$ 的自变量完全相同，都是 $u=\ln x+1/y$。

**第三步：按题设的系数组合。**

$$
\begin{aligned}
x\frac{\partial z}{\partial x}
+y^2\frac{\partial z}{\partial y}
&=x\frac{f'(u)}x
+y^2\left[-\frac{f'(u)}{y^2}\right]\\
&=f'(u)-f'(u)\\
&=0.
\end{aligned}
$$

也可以先把公因子提出来：$xz_x+y^2z_y=f'(u)(xu_x+y^2u_y)=f'(u)(1-1)$。因此结论与 $f$ 的具体形式无关。

**12.（2012.12）答案：$y=\sqrt x$，$x>0$。**

**第一步：将原微分式合成全微分。**

由乘积微分公式，

$$
\mathrm d(xy)=y\,\mathrm dx+x\,\mathrm dy,
\qquad
\mathrm d(y^3)=3y^2\,\mathrm dy.
$$

因此

$$
y\,\mathrm dx+(x-3y^2)\,\mathrm dy
=\mathrm d(xy-y^3).
$$

原微分方程可写成 $\mathrm d(xy-y^3)=0$，积分得到隐式通解

$$
xy-y^3=C.
$$

**第二步：代入初值，并选定对应的分支。**

由 $y(1)=1$，得 $C=1\cdot1-1^3=0$，于是

$$
y(x-y^2)=0.
$$

这个等式包含 $y=0$ 和 $y^2=x$。初值点的纵坐标为1，故 $y=0$ 不符合初值；而在 $y^2=x$ 的两个分支中，只有正分支经过 $(1,1)$。

所以满足初值的解是

$$
y=\sqrt x.
$$

**第三步：说明区间，并代回检验。**

该分支在 $x>0$ 上可微，且

$$
y'=\frac1{2\sqrt x}.
$$

将微分方程写成 $y+(x-3y^2)y'=0$，代入可得

$$
\sqrt x+(x-3x)\frac1{2\sqrt x}
=\sqrt x-\sqrt x=0.
$$

初值也满足。由于 $y'$ 在 $x\to0^+$ 时无界，作为可微函数解，包含初值点的最大区间为 $(0,+\infty)$。

另一种求法是把 $x$ 看作 $y$ 的函数：$y\,\mathrm dx/\mathrm dy+x=3y^2$，左侧恰为 $\mathrm d(xy)/\mathrm dy$，积分同样得到 $xy-y^3=C$。

**13.（2012.13）答案：$(-1,0)$。**

**第一步：写出曲率公式。**

对于平面曲线 $y=y(x)$，当二阶导数存在时，其曲率为

$$
\kappa=\frac{|y''|}{[1+(y')^2]^{3/2}}.
$$

本题 $y=x^2+x$，故

$$
y'=2x+1,
\qquad y''=2,
\qquad
\kappa(x)=\frac2{[1+(2x+1)^2]^{3/2}}.
$$

**第二步：根据给定曲率解出横坐标。**

由 $\kappa=\sqrt2/2$，

$$
[1+(2x+1)^2]^{3/2}
=\frac2{\sqrt2/2}
=2\sqrt2=2^{3/2}.
$$

两边均为正数，取 $2/3$ 次幂，得到

$$
1+(2x+1)^2=2,
\qquad (2x+1)^2=1.
$$

因而应当保留正、负两个平方根：

$$
2x+1=1\quad\text{或}\quad2x+1=-1,
$$

即 $x=0$ 或 $x=-1$。

**第三步：使用题设的范围限制。**

题目给定 $x<0$，排除 $x=0$，只剩 $x=-1$。代回曲线方程，

$$
y=(-1)^2+(-1)=0.
$$

因此所求点为 $(-1,0)$。在此点 $y'=-1$，曲率为 $2/(1+1)^{3/2}=\sqrt2/2$，与题设一致。

**14.（2012.14）答案：$-27$。**

本题中的 $A^*$ 是伴随矩阵。分别求出 $|B|$ 和 $|A^*|$，再利用乘积行列式公式即可。

**第一步：处理交换两行的影响。**

$B$ 由 $A$ 交换第1行和第2行得到。行列式交换任意两行后变号，所以

$$
|B|=-|A|=-3.
$$

**第二步：求伴随矩阵的行列式。**

因为 $A$ 是三阶矩阵，且 $|A|=3\ne0$，由伴随矩阵恒等式，

$$
AA^*=|A|I_3.
$$

两边取行列式，有

$$
|A|\,|A^*|
=\big||A|I_3\big|
=|A|^3.
$$

这里 $|A|I_3$ 的三个对角元都是 $|A|$，所以其行列式是 $|A|^3$。除以非零的 $|A|$，得到

$$
|A^*|=|A|^2=9.
$$

一般地，对于 $n$ 阶可逆矩阵，有 $|A^*|=|A|^{n-1}$。

**第三步：计算目标行列式。**

$$
|BA^*|=|B|\,|A^*|=(-3)\cdot9=-27.
$$

行交换只影响 $B$ 的行列式，$A^*$ 仍是原矩阵 $A$ 的伴随矩阵。

## 三、解答题（15～23）

**15.（2012.15）答案：$a=1$，$k=1$。**

**（Ⅰ）先把极限拆成容易识别的部分。**

题中两个含分母的项分别无界，不能单独求极限后相减。将它们整理为

$$
\begin{aligned}
f(x)
&=\left(\frac1{\sin x}-\frac1x\right)+\frac{x}{\sin x}\\
&=\frac{x-\sin x}{x\sin x}+\frac{x}{\sin x}.
\end{aligned}
$$

由基本极限 $\sin x/x\to1$，有 $x/\sin x\to1$。再利用零点附近的展开式

$$
\sin x=x-\frac{x^3}{6}+o(x^3),
$$

得到 $x-\sin x=x^3/6+o(x^3)$，且 $x\sin x\sim x^2$。因此

$$
\frac{x-\sin x}{x\sin x}\sim\frac{x}{6}\longrightarrow0.
$$

所以

$$
a=\lim_{x\to0}f(x)=0+1=1.
$$

**（Ⅱ）对 $f(x)-a$ 作整体通分，找出首个非零项。**

把 $a=1$ 代入，

$$
\begin{aligned}
f(x)-1
&=\frac{1+x}{\sin x}-\frac1x-1\\
&=\frac{1+x}{\sin x}-\frac{1+x}{x}\\
&=\frac{(1+x)(x-\sin x)}{x\sin x}.
\end{aligned}
$$

分子中 $1+x\to1$，而 $x-\sin x$ 是三阶无穷小；分母 $x\sin x$ 是二阶无穷小，故商是一阶无穷小。严格地，

$$
\begin{aligned}
\lim_{x\to0}\frac{f(x)-1}{x}
&=\lim_{x\to0}
\left[(1+x)\frac{x-\sin x}{x^3}\frac{x}{\sin x}\right]\\
&=1\cdot\frac16\cdot1=\frac16.
\end{aligned}
$$

这个极限有限且不为零，正好满足同阶无穷小的定义。因此

$$
f(x)-1\sim\frac{x}{6},
\qquad k=1.
$$

**补充：关键三阶极限也可用洛必达法则求出。**

$$
\begin{aligned}
\lim_{x\to0}\frac{x-\sin x}{x^3}
&=\lim_{x\to0}\frac{1-\cos x}{3x^2}\\
&=\lim_{x\to0}\frac{\sin x}{6x}
=\frac16.
\end{aligned}
$$

两次求导前都属于 $0/0$ 型。这里必须把 $\sin x$ 的三阶信息保留下来；只用 $\sin x\sim x$，不足以判断相减后 $x-\sin x$ 的阶数。

**16.（2012.16）答案：在 $(1,0)$ 处取极大值 $\mathrm e^{-1/2}$，在 $(-1,0)$ 处取极小值 $-\mathrm e^{-1/2}$。**

函数在整个平面上光滑，极值点必须是驻点。先求出全部驻点，再用二阶偏导数判别。

**第一步：计算一阶偏导数。**

记 $E=\mathrm e^{-(x^2+y^2)/2}$，则 $E>0$，且

$$
E_x=-xE,
\qquad E_y=-yE.
$$

由 $f=xE$，利用乘积法则，

$$
f_x=E-x^2E=(1-x^2)E,
\qquad
f_y=-xyE.
$$

驻点方程为

$$
\begin{cases}
(1-x^2)E=0,\\
-xyE=0.
\end{cases}
$$

因为指数函数从不为零，第一式给出 $x=\pm1$。这时 $x\ne0$，第二式只能给出 $y=0$。因此全部驻点只有 $(1,0)$ 和 $(-1,0)$。

**第二步：计算二阶偏导数。**

$$
\begin{aligned}
f_{xx}
&=-2xE+(1-x^2)(-xE)
=x(x^2-3)E,\\
f_{xy}
&=(1-x^2)(-yE)
=y(x^2-1)E,\\
f_{yy}
&=-xE+(-xy)(-yE)
=x(y^2-1)E.
\end{aligned}
$$

设 $\Delta=f_{xx}f_{yy}-f_{xy}^2$。当 $\Delta>0$ 时，若 $f_{xx}<0$ 则为严格极大值，若 $f_{xx}>0$ 则为严格极小值。

**第三步：逐点代入判别。**

在 $(1,0)$ 处，

$$
f_{xx}=-2\mathrm e^{-1/2},
\quad f_{xy}=0,
\quad f_{yy}=-\mathrm e^{-1/2},
\quad\Delta=2\mathrm e^{-1}>0.
$$

又 $f_{xx}<0$，故该点是极大值点，极大值为

$$
f(1,0)=\mathrm e^{-1/2}.
$$

在 $(-1,0)$ 处，

$$
f_{xx}=2\mathrm e^{-1/2},
\quad f_{xy}=0,
\quad f_{yy}=\mathrm e^{-1/2},
\quad\Delta=2\mathrm e^{-1}>0.
$$

又 $f_{xx}>0$，故该点是极小值点，极小值为

$$
f(-1,0)=-\mathrm e^{-1/2}.
$$

**第四步：还可验证它们是全局最大值和最小值。**

对任意 $(x,y)$，

$$
|f(x,y)|
=|x|\mathrm e^{-x^2/2}\mathrm e^{-y^2/2}
\le |x|\mathrm e^{-x^2/2}.
$$

令 $t=|x|\ge0$，函数 $h(t)=t\mathrm e^{-t^2/2}$ 的导数为

$$
h'(t)=(1-t^2)\mathrm e^{-t^2/2}.
$$

它在 $0<t<1$ 时为正，在 $t>1$ 时为负，所以 $h(t)\le h(1)=\mathrm e^{-1/2}$。于是

$$
-\mathrm e^{-1/2}\le f(x,y)\le\mathrm e^{-1/2},
$$

两个等号分别在 $(-1,0)$、$(1,0)$ 取得，与驻点判别一致。

**17.（2012.17）答案：面积 $S=2$，体积 $V=\dfrac{2\pi}{3}(\mathrm e^2-1)$。**

**第一步：利用切线经过的点确定切点 $A$。**

设切点为 $A=(a,\ln a)$，其中 $a>0$。曲线 $y=\ln x$ 在该点的导数为 $1/a$，故切线方程是

$$
y-\ln a=\frac1a(x-a),
\quad\text{即}\quad y=\frac xa+\ln a-1.
$$

切线经过 $(0,1)$，代入得到

$$
1=\ln a-1,
\qquad\ln a=2,
\qquad a=\mathrm e^2.
$$

所以 $A=(\mathrm e^2,2)$。曲线与 $x$ 轴交点满足 $\ln x=0$，故 $B=(1,0)$。

**第二步：写出围成区域的直线 $AB$，确定上下边界。**

直线 $AB$ 连接曲线上的两个点，因此它是弦线，其方程为

$$
y=\ell(x)=\frac{2}{\mathrm e^2-1}(x-1).
$$

前面所作切线的作用是确定 $A$；题目要求的区域由曲线与这条弦线围成。由于

$$
(\ln x)''=-\frac1{x^2}<0,
$$

对数曲线在两个端点之间位于弦线之上。因此

$$
D=\left\{(x,y):1\le x\le\mathrm e^2,
\quad\ell(x)\le y\le\ln x\right\}.
$$

两条边界在该区间都非负，这一点也决定了旋转体可以直接用圆环截面法计算。

**第三步：计算面积。**

面积是上边界减下边界的积分：

$$
S=\int_1^{\mathrm e^2}\ln x\,\mathrm dx
-\frac2{\mathrm e^2-1}\int_1^{\mathrm e^2}(x-1)\,\mathrm dx.
$$

其中

$$
\begin{aligned}
\int_1^{\mathrm e^2}\ln x\,\mathrm dx
&=[x\ln x-x]_1^{\mathrm e^2}\\
&=(2\mathrm e^2-\mathrm e^2)-(0-1)
=\mathrm e^2+1,
\end{aligned}
$$

$$
\frac2{\mathrm e^2-1}\int_1^{\mathrm e^2}(x-1)\,\mathrm dx
=\frac2{\mathrm e^2-1}\frac{(\mathrm e^2-1)^2}{2}
=\mathrm e^2-1.
$$

相减得到

$$
S=(\mathrm e^2+1)-(\mathrm e^2-1)=2.
$$

**第四步：写出旋转体体积积分。**

固定一个 $x$，区间 $\ell(x)\le y\le\ln x$ 绕 $x$ 轴旋转后形成圆环。外半径是 $\ln x$，内半径是 $\ell(x)$，因此

$$
V=\pi\int_1^{\mathrm e^2}
\left[(\ln x)^2-\frac{4(x-1)^2}{(\mathrm e^2-1)^2}\right]\mathrm dx.
$$

圆环面积是半径的平方之差，而不是两条曲线高度之差的平方。

**第五步：分别计算两个平方项的积分。**

对第一项分部积分：

$$
\begin{aligned}
\int(\ln x)^2\,\mathrm dx
&=x(\ln x)^2-2\int\ln x\,\mathrm dx\\
&=x\big[(\ln x)^2-2\ln x+2\big]+C.
\end{aligned}
$$

故

$$
\int_1^{\mathrm e^2}(\ln x)^2\,\mathrm dx
=\mathrm e^2(4-4+2)-2
=2(\mathrm e^2-1).
$$

第二项为

$$
\begin{aligned}
\frac4{(\mathrm e^2-1)^2}
\int_1^{\mathrm e^2}(x-1)^2\,\mathrm dx
&=\frac4{(\mathrm e^2-1)^2}\frac{(\mathrm e^2-1)^3}{3}\\
&=\frac43(\mathrm e^2-1).
\end{aligned}
$$

因此

$$
V=\pi\left[2-\frac43\right](\mathrm e^2-1)
=\frac{2\pi}{3}(\mathrm e^2-1).
$$

**18.（2012.18）答案：$\dfrac{16}{15}$。**

**第一步：根据极坐标边界写出积分限。**

边界曲线为 $r=1+\cos\theta$，角度范围是 $0\le\theta\le\pi$。在这个角度范围内 $1+\cos\theta\ge0$，区域从原点沿各射线延伸到曲线，因此

$$
D:\quad0\le\theta\le\pi,
\qquad0\le r\le1+\cos\theta.
$$

使用极坐标变换

$$
x=r\cos\theta,
\qquad y=r\sin\theta,
\qquad\mathrm d\sigma=r\,\mathrm dr\,\mathrm d\theta.
$$

面积元必须带上雅可比因子 $r$。所以 $xy\,\mathrm d\sigma$ 变为

$$
r^2\sin\theta\cos\theta\cdot r\,\mathrm dr\,\mathrm d\theta
=r^3\sin\theta\cos\theta\,\mathrm dr\,\mathrm d\theta.
$$

**第二步：先对半径积分。**

$$
\begin{aligned}
I
&=\int_0^\pi\int_0^{1+\cos\theta}
 r^3\sin\theta\cos\theta\,\mathrm dr\,\mathrm d\theta\\
&=\int_0^\pi\sin\theta\cos\theta
 \left[\frac{r^4}{4}\right]_0^{1+\cos\theta}\mathrm d\theta\\
&=\frac14\int_0^\pi
 \sin\theta\cos\theta(1+\cos\theta)^4\,\mathrm d\theta.
\end{aligned}
$$

这里 $\cos\theta$ 在后半段为负，应保留其符号；原积分中的 $xy$ 本来就会随所在象限改变符号。

**第三步：换元化为多项式积分。**

令 $u=1+\cos\theta$，则

$$
\mathrm du=-\sin\theta\,\mathrm d\theta,
\qquad\cos\theta=u-1.
$$

当 $\theta=0$ 时，$u=2$；当 $\theta=\pi$ 时，$u=0$。于是

$$
\begin{aligned}
I
&=-\frac14\int_2^0(u-1)u^4\,\mathrm du\\
&=\frac14\int_0^2(u^5-u^4)\,\mathrm du\\
&=\frac14\left[\frac{u^6}{6}-\frac{u^5}{5}\right]_0^2\\
&=\frac14\left(\frac{64}{6}-\frac{32}{5}\right)\\
&=\frac14\cdot\frac{64}{15}
=\frac{16}{15}.
\end{aligned}
$$

换元时的负号与上下限的反向相互抵消，最终得到上述正值。

**19.（2012.19）答案：$f(x)=\mathrm e^x$；所求曲线的唯一拐点为 $(0,0)$。**

**（Ⅰ）通过求导与消元确定 $f$。**

两条已知关系是

$$
f'''+f'-2f=0,
\qquad f''+f=2\mathrm e^x.
$$

第二条比第一条低一阶。将第二条两边对 $x$ 求导，得到

$$
f'''+f'=2\mathrm e^x.
$$

而第一条可写为 $f'''+f'=2f$。两式的左边相同，所以

$$
2f=2\mathrm e^x,
\qquad f(x)=\mathrm e^x.
$$

这是同时满足两式的必要条件。再代回检验：

$$
f'''+f'-2f=(1+1-2)\mathrm e^x=0,
$$

$$
f''+f=2\mathrm e^x.
$$

因此该函数确实满足所有条件，并且由消元结果可知它已唯一确定，无须分别求出两条微分方程的通解。

**（Ⅱ）先把曲线写成便于求导的形式。**

代入 $f$ 的表达式，

$$
y(x)=\mathrm e^{x^2}\int_0^x\mathrm e^{-t^2}\,\mathrm dt.
$$

记 $H(x)=\int_0^x\mathrm e^{-t^2}\,\mathrm dt$。被积函数连续，由微积分基本定理，

$$
H'(x)=\mathrm e^{-x^2}.
$$

故利用乘积求导，

$$
\begin{aligned}
y'
&=2x\mathrm e^{x^2}H(x)+\mathrm e^{x^2}H'(x)\\
&=2xy+\mathrm e^{x^2}\mathrm e^{-x^2}\\
&=2xy+1.
\end{aligned}
$$

再求一次导数：

$$
\begin{aligned}
y''
&=2y+2xy'\\
&=2y+2x(2xy+1)\\
&=2(1+2x^2)y+2x.
\end{aligned}
$$

这样把不便直接求原函数的积分保留在 $y$ 中，判断符号即可。

**分别讨论 $x>0$、$x<0$ 和 $x=0$。**

当 $x>0$ 时，$H(x)$ 是正函数在正向区间上的积分，因此 $H(x)>0$。又 $\mathrm e^{x^2}>0$，所以 $y(x)>0$。于是

$$
y''(x)=2(1+2x^2)y(x)+2x>0.
$$

当 $x<0$ 时，

$$
H(x)=-\int_x^0\mathrm e^{-t^2}\,\mathrm dt<0,
$$

故 $y(x)<0$。此时 $2(1+2x^2)y(x)$ 与 $2x$ 均为负，得到 $y''(x)<0$。

当 $x=0$ 时，$H(0)=0$，所以 $y(0)=0$，并且 $y''(0)=0$。

**根据二阶导数在两侧变号确定拐点。**

$y''$ 在 $(-\infty,0)$ 上恒负，在 $(0,+\infty)$ 上恒正，曲线在零点两侧的凹凸性发生改变，所以 $(0,0)$ 是拐点。

其他位置的二阶导数既不为零，也没有符号变化，因此不存在其他拐点。所求唯一拐点为

$$
(0,0).
$$

**20.（2012.20）不等式成立，等号当且仅当 $x=0$。**

**第一步：利用偶性，把讨论范围缩到 $0\le x<1$。**

设

$$
G(x)=x\ln\frac{1+x}{1-x}+\cos x.
$$

对 $|x|<1$，对数中的分子、分母都为正。将 $x$ 换成 $-x$，

$$
(-x)\ln\frac{1-x}{1+x}
=x\ln\frac{1+x}{1-x}.
$$

因此对数乘积是偶函数，$\cos x$ 与右端 $1+x^2/2$ 也都是偶函数，只需证明 $0\le x<1$ 的情况。

**第二步：为对数项建立下界。**

由于

$$
\frac{\mathrm d}{\mathrm dt}\ln\frac{1+t}{1-t}
=\frac1{1+t}+\frac1{1-t}
=\frac2{1-t^2},
$$

且该对数在 $t=0$ 处等于0，所以

$$
\ln\frac{1+x}{1-x}
=2\int_0^x\frac{\mathrm dt}{1-t^2}.
$$

当 $0\le t\le x<1$ 时，$0<1-t^2\le1$，故 $1/(1-t^2)\ge1$。于是

$$
\ln\frac{1+x}{1-x}\ge2x.
$$

因为当前范围内 $x\ge0$，两边乘以 $x$ 不改变不等号方向，得到

$$
x\ln\frac{1+x}{1-x}\ge2x^2.
$$

**第三步：为余弦项建立下界。**

由恒等式及基本估计 $|\sin u|\le|u|$，

$$
1-\cos x=2\sin^2\frac x2
\le2\left(\frac x2\right)^2
=\frac{x^2}{2}.
$$

因此

$$
\cos x\ge1-\frac{x^2}{2}.
$$

**第四步：将两个下界相加。**

$$
\begin{aligned}
x\ln\frac{1+x}{1-x}+\cos x
&\ge2x^2+1-\frac{x^2}{2}\\
&=1+\frac{3x^2}{2}\\
&\ge1+\frac{x^2}{2}.
\end{aligned}
$$

所以所求不等式在 $0\le x<1$ 上成立。结合第一步的偶性，它在整个 $-1<x<1$ 上成立。

当 $x=0$ 时，左右两边都为1，等号成立；当 $x\ne0$ 时，$1+3x^2/2>1+x^2/2$，所以原不等式严格成立。

**另一种证明：研究差函数的单调性。**

令

$$
F(x)=x\ln\frac{1+x}{1-x}+\cos x-1-\frac{x^2}{2}.
$$

仍只需讨论 $0\le x<1$。对 $0<x<1$，

$$
F'(x)=\ln\frac{1+x}{1-x}+\frac{2x}{1-x^2}-\sin x-x.
$$

由对数项为正、$2x/(1-x^2)\ge2x$ 以及 $\sin x\le x$，得到

$$
F'(x)>2x-x-x=0.
$$

因此 $F$ 在 $(0,1)$ 上严格递增，而 $F(0)=0$，所以 $F(x)>0$。再用偶性可得全部结论。

**21.（2012.21）区间 $(1/2,1)$ 内有唯一实根 $x_n$，且 $\displaystyle\lim_{n\to\infty}x_n=\frac12$。**

**（Ⅰ）先证明根存在，再证明根唯一。**

令

$$
F_n(x)=x+x^2+\cdots+x^n,
\qquad n\ge2.
$$

$F_n$ 是多项式，在 $[1/2,1]$ 上连续。利用等比数列求和公式，

$$
F_n(1/2)=\frac12+\frac1{2^2}+\cdots+\frac1{2^n}
=1-\frac1{2^n}<1.
$$

另一端有 $F_n(1)=n>1$。因此连续函数 $F_n(x)-1$ 在区间两端异号，由零点定理，至少存在一个

$$
x_n\in(1/2,1)
$$

使 $F_n(x_n)=1$。

再求导：

$$
F_n'(x)=1+2x+3x^2+\cdots+nx^{n-1}>0
\qquad(x>0).
$$

所以 $F_n$ 在正半轴上严格增加，特别地，在 $(1/2,1)$ 内不可能两次取得相同的值1。因此该区间内的根有且仅有一个。

**（Ⅱ）比较相邻两个方程，证明根数列递减。**

把 $x_n$ 代入多一项的函数 $F_{n+1}$，得到

$$
F_{n+1}(x_n)
=F_n(x_n)+x_n^{n+1}
=1+x_n^{n+1}>1.
$$

而 $F_{n+1}(x_{n+1})=1$。由于 $F_{n+1}$ 严格增加，较小的函数值对应较小的自变量，因此

$$
x_{n+1}<x_n.
$$

结合每一项的位置，

$$
\frac12<x_{n+1}<x_n<1.
$$

所以 $\{x_n\}$ 单调递减且有下界。由单调有界定理，存在极限 $L$，并且 $L\ge1/2$。

**利用等比数列求和式求出 $L$。**

由 $0<x_n<1$，可以将根方程写为

$$
\frac{x_n(1-x_n^n)}{1-x_n}=1.
$$

乘以非零的 $1-x_n$，整理得

$$
x_n-x_n^{n+1}=1-x_n,
\qquad2x_n-1=x_n^{n+1}.
$$

关键是证明右边趋于0。因为数列递减，所有 $n\ge2$ 都满足

$$
0<x_n\le x_2.
$$

其中 $x_2$ 是 $x+x^2=1$ 的正根，即

$$
x_2=\frac{\sqrt5-1}{2}<1.
$$

这是一个与 $n$ 无关、严格小于1的统一上界。于是

$$
0<x_n^{n+1}\le x_2^{n+1}\longrightarrow0.
$$

对 $2x_n-1=x_n^{n+1}$ 取极限，得到

$$
2L-1=0,
\qquad L=\frac12.
$$

事实上还得到了误差估计

$$
0<x_n-\frac12=\frac12x_n^{n+1}
\le\frac12\left(\frac{\sqrt5-1}{2}\right)^{n+1}.
$$

该估计说明 $x_n$ 从上方趋于 $1/2$。证明幂趋于零时，不能仅凭每一个 $x_n<1$；例如 $(1-1/n)^n$ 就不趋于0，所以上述统一上界是必要的论证环节。

**22.（2012.22）答案：$|A|=1-a^4$；无穷多解当且仅当 $a=-1$，通解见下。**

**（Ⅰ）沿第一行展开行列式。**

第一行只有两个非零元，因此

$$
\begin{aligned}
|A|
&=\begin{vmatrix}1&a&0\\0&1&a\\0&0&1\end{vmatrix}
-a\begin{vmatrix}0&a&0\\0&1&a\\a&0&1\end{vmatrix}.
\end{aligned}
$$

第一个行列式为上三角行列式，值是1。第二个行列式沿第一列展开，只有第三行的 $a$ 非零，其代数余子式符号为 $(-1)^{3+1}=1$，所以

$$
\begin{vmatrix}0&a&0\\0&1&a\\a&0&1\end{vmatrix}
=a\begin{vmatrix}a&0\\1&a\end{vmatrix}=a^3.
$$

因此

$$
|A|=1-a\cdot a^3=1-a^4.
$$

**（Ⅱ）先确定无穷多解的候选参数。**

若 $|A|\ne0$，系数矩阵可逆，方程组只有唯一解。因此要有无穷多解，必须先有

$$
1-a^4=0.
$$

题设 $a$ 为实数，故只有 $a=1$ 或 $a=-1$。行列式为零只说明不能唯一解，还需要分别检查方程组是否相容。

**检查 $a=1$。**

此时四个方程为

$$
\begin{cases}
x_1+x_2=1,\\
x_2+x_3=-1,\\
x_3+x_4=0,\\
x_1+x_4=0.
\end{cases}
$$

将第一式减第二式、加第三式、再减第四式，左边所有未知量抵消，右边却等于

$$
1-(-1)+0-0=2.
$$

于是得到矛盾 $0=2$。所以 $a=1$ 时无解。

**检查 $a=-1$ 并求通解。**

方程组变为

$$
\begin{cases}
x_1-x_2=1,\\
x_2-x_3=-1,\\
x_3-x_4=0,\\
-x_1+x_4=0.
\end{cases}
$$

令 $x_4=t$，其中 $t\in\mathbb R$。由第三式得 $x_3=t$，由第二式得 $x_2=t-1$，由第一式得 $x_1=t$。将这些值代入第四式，左边为 $-t+t=0$，所以没有额外限制。

因此全部解为

$$
\begin{cases}
x_1=t,\\x_2=t-1,\\x_3=t,\\x_4=t,
\end{cases}
\qquad t\in\mathbb R.
$$

写成特解加齐次方程通解的形式：

$$
\boldsymbol x=
\begin{pmatrix}0\\-1\\0\\0\end{pmatrix}
+t\begin{pmatrix}1\\1\\1\\1\end{pmatrix},
\qquad t\in\mathbb R.
$$

**用秩条件核实自由参数个数。**

$a=-1$ 时，$A$ 的前三行、前三列组成的上三角子式行列式为1，因此 $r(A)\ge3$；又 $|A|=0$，所以 $r(A)=3$。方程组相容，故增广矩阵的秩也为3。未知量有4个，因而恰有 $4-3=1$ 个自由参数，与上式一致。

**23.（2012.23）答案：$a=-1$；正交变换后标准形可取 $2y_1^2+6y_2^2$。**

**（Ⅰ）先证明 $A^{\mathrm T}A$ 与 $A$ 的秩相同。**

二次型的矩阵为 $B=A^{\mathrm T}A$。对任意三维实向量 $\boldsymbol x$，

$$
\boldsymbol x^{\mathrm T}A^{\mathrm T}A\boldsymbol x
=(A\boldsymbol x)^{\mathrm T}(A\boldsymbol x)
=\|A\boldsymbol x\|^2.
$$

若 $A\boldsymbol x=0$，当然有 $A^{\mathrm T}A\boldsymbol x=0$；反过来，若 $A^{\mathrm T}A\boldsymbol x=0$，两边左乘 $\boldsymbol x^{\mathrm T}$，便得 $\|A\boldsymbol x\|^2=0$，从而 $A\boldsymbol x=0$。

所以两矩阵的零空间相同。它们的列数均为3，由秩与零空间维数之和等于列数，得到

$$
r(A^{\mathrm T}A)=r(A).
$$

题设二次型秩为2，因此 $r(A)=2$。

**利用行向量的线性关系求 $a$。**

$A$ 的前两行分别为

$$
r_1=(1,0,1),
\qquad r_2=(0,1,1),
$$

它们显然线性无关。要使全部行的秩为2，第三行 $r_3=(-1,0,a)$ 必须是这两行的线性组合。设

$$
r_3=s r_1+t r_2=(s,t,s+t).
$$

比较前两个分量，必须有 $s=-1,t=0$；再比较第三个分量，得到 $a=s+t=-1$。

当 $a=-1$ 时，第三行为 $-r_1$，第四行 $(0,-1,-1)$ 为 $-r_2$。所以秩确实为2。故必要条件也是充分条件，

$$
a=-1.
$$

**（Ⅱ）先计算对称矩阵 $B=A^{\mathrm T}A$。**

代入参数，$A$ 的三列为

$$
c_1=(1,0,-1,0)^{\mathrm T},\quad
c_2=(0,1,0,-1)^{\mathrm T},\quad
c_3=(1,1,-1,-1)^{\mathrm T}.
$$

$B$ 的第 $i,j$ 个元素是内积 $c_i^{\mathrm T}c_j$。依次计算可得

$$
B=\begin{pmatrix}2&0&2\\0&2&2\\2&2&4\end{pmatrix}.
$$

该矩阵为实对称矩阵，可以用正交变换对角化。

**求特征值。**

$$
\begin{aligned}
\det(B-\lambda I)
&=\begin{vmatrix}2-\lambda&0&2\\0&2-\lambda&2\\2&2&4-\lambda\end{vmatrix}\\
&=(2-\lambda)\big[(2-\lambda)(4-\lambda)-4\big]
-4(2-\lambda)\\
&=(2-\lambda)\big[(2-\lambda)(4-\lambda)-8\big]\\
&=(2-\lambda)(\lambda^2-6\lambda)\\
&=(2-\lambda)\lambda(\lambda-6).
\end{aligned}
$$

故三个特征值为 $2,6,0$。其中有两个非零特征值，与二次型的秩为2相符；它们均非负，也与 $\|A\boldsymbol x\|^2\ge0$ 相符。

**分别求特征向量。**

当 $\lambda=2$ 时，方程 $(B-2I)v=0$ 给出

$$
v_3=0,
\qquad v_1+v_2=0.
$$

因此可取 $p_1=(1,-1,0)^{\mathrm T}$。

当 $\lambda=6$ 时，前两行方程分别为

$$
-4v_1+2v_3=0,
\qquad-4v_2+2v_3=0.
$$

所以 $v_1=v_2$、$v_3=2v_1$；第三行也自动满足，可取 $p_2=(1,1,2)^{\mathrm T}$。

当 $\lambda=0$ 时，前两行给出

$$
v_1=-v_3,
\qquad v_2=-v_3.
$$

第三行是前两行的和，不增加约束。取 $v_3=-1$，得到 $p_3=(1,1,-1)^{\mathrm T}$。

**将特征向量单位化，组成正交矩阵。**

三个特征值互异，对应的特征向量应当正交；直接计算也有

$$
p_1^{\mathrm T}p_2=1-1=0,
\quad p_1^{\mathrm T}p_3=1-1=0,
\quad p_2^{\mathrm T}p_3=1+1-2=0.
$$

它们的长度分别为

$$
\|p_1\|=\sqrt2,
\qquad\|p_2\|=\sqrt6,
\qquad\|p_3\|=\sqrt3.
$$

将单位特征向量依次排为列，得到

$$
Q=\begin{pmatrix}
\dfrac1{\sqrt2}&\dfrac1{\sqrt6}&\dfrac1{\sqrt3}\\
-\dfrac1{\sqrt2}&\dfrac1{\sqrt6}&\dfrac1{\sqrt3}\\
0&\dfrac2{\sqrt6}&-\dfrac1{\sqrt3}
\end{pmatrix}.
$$

于是 $Q^{\mathrm T}Q=I$，并且

$$
Q^{\mathrm T}BQ=\operatorname{diag}(2,6,0).
$$

**写出正交变换与标准形。**

取 $\boldsymbol x=Q\boldsymbol y$，即

$$
\begin{cases}
x_1=\dfrac{y_1}{\sqrt2}+\dfrac{y_2}{\sqrt6}+\dfrac{y_3}{\sqrt3},\\
x_2=-\dfrac{y_1}{\sqrt2}+\dfrac{y_2}{\sqrt6}+\dfrac{y_3}{\sqrt3},\\
x_3=\dfrac{2y_2}{\sqrt6}-\dfrac{y_3}{\sqrt3}.
\end{cases}
$$

代入二次型，

$$
\begin{aligned}
f
&=(Q\boldsymbol y)^{\mathrm T}B(Q\boldsymbol y)\\
&=\boldsymbol y^{\mathrm T}(Q^{\mathrm T}BQ)\boldsymbol y\\
&=2y_1^2+6y_2^2.
\end{aligned}
$$

$y_3^2$ 的系数为零，因此标准形中没有该项。
