# 2012年考研数学二答案与解析

> 我的解答，未与官方核对。

## 答案速查

> 选择题：1.C　2.A　3.B　4.D　5.D　6.D　7.C　8.B。

> 填空题：9.$1$；10.$\pi/4$；11.$0$；12.$\sqrt x$；13.$(-1,0)$；14.$-27$。

15. $a=1,k=1$。

16. 极大值为 $\mathrm e^{-1/2}$，极小值为 $-\mathrm e^{-1/2}$。

17. 面积为2，体积为 $\dfrac{2\pi}{3}(\mathrm e^2-1)$。

18. 积分为 $16/15$。

19. $f(x)=\mathrm e^x$，所求曲线的唯一拐点为 $(0,0)$。

20. 不等式证明见正文。

21. 数列极限为 $1/2$。

22. $a=-1$；通解 $\boldsymbol x=(0,-1,0,0)^{\mathrm T}+t(1,1,1,1)^{\mathrm T}$。

23. $a=-1$；正交变换后的标准形可取 $2y_1^2+6y_2^2$。

## 一、选择题（1～8）

**1.（2012.1）答案：C，共2条渐近线。**

在原定义域内可以约分为

$$
y=\frac{x}{x-1}=1+\frac1{x-1}.
$$

因此当 $x\to1$ 时，函数无界，竖直渐近线为 $x=1$；当 $x\to\pm\infty$ 时，函数趋于1，水平渐近线为 $y=1$。

原式还在 $x=-1$ 处无定义，但该点极限有限，是可去间断点，不产生竖直渐近线。故总共2条。

**2.（2012.2）答案：A，$(-1)^{n-1}(n-1)!$。**

对乘积求导时，每一项都由一个因子的导数乘以其余因子构成。

在 $x=0$ 处，第一个因子 $\mathrm e^x-1$ 等于零，其余因子 $\mathrm e^{kx}-k$（$k\ge2$）均不为零。因此只有对第一个因子求导的那一项留下。

于是

$$
f'(0)=1\cdot\prod_{k=2}^n(1-k)
=(-1)^{n-1}(n-1)!.
$$

当 $n=1$ 时，空乘积为1，公式仍成立。

**3.（2012.3）答案：B，充分非必要条件。**

因为 $a_n>0$，部分和 $S_n$ 严格增加。如果 $\{S_n\}$ 有界，则由单调有界定理，$S_n$ 收敛。

于是

$$
a_n=S_n-S_{n-1}\longrightarrow0,
$$

所以 $\{a_n\}$ 收敛，充分性成立。

反过来，取 $a_n=1$，数列收敛，但 $S_n=n$ 无界，所以条件不是必要的。

**4.（2012.4）答案：D，$I_2<I_1<I_3$。**

记每段半周期的正面积为

$$
J_k=\int_{(k-1)\pi}^{k\pi}\mathrm e^{x^2}|\sin x|\,\mathrm dx,
\qquad k=1,2,3.
$$

令 $x=t+(k-1)\pi$ 后，三个积分的正弦因子都变成 $\sin t$，而指数因子随 $k$ 严格增大，因此

$$
0<J_1<J_2<J_3.
$$

按正弦函数的正负号，

$$
I_1=J_1,\quad I_2=J_1-J_2,\quad I_3=J_1-J_2+J_3.
$$

因此 $I_2<0<I_1$，且 $I_3-I_1=J_3-J_2>0$，得所求顺序。

**5.（2012.5）答案：D。**

当 $x_1<x_2$、$y_1>y_2$ 时，先保持 $y=y_1$，再保持 $x=x_2$，利用两个偏导数的符号，得到

$$
f(x_1,y_1)<f(x_2,y_1)<f(x_2,y_2).
$$

所以这组条件足以保证题设不等式，选 D。

**6.（2012.6）答案：D，$-\pi$。**

区域可写为 $-\pi/2\le x\le\pi/2$、$\sin x\le y\le1$。先对 $y$ 积分：

$$
\begin{aligned}
I&=\int_{-\pi/2}^{\pi/2}\left[\frac{x y^6}{6}-y\right]_{y=\sin x}^{y=1}\mathrm dx\\
&=\int_{-\pi/2}^{\pi/2}\left[\frac x6(1-\sin^6x)+\sin x-1\right]\mathrm dx.
\end{aligned}
$$

其中 $x(1-\sin^6x)$ 与 $\sin x$ 都是奇函数，在对称区间上积分为零。

只剩常数项 $-1$，所以积分为 $-\pi$。

**7.（2012.7）答案：C。**

向量 $\alpha_1,\alpha_3,\alpha_4$ 的前两个分量之和都为零，因此它们都在二维平面

$$
x_1+x_2=0
$$

内，第三个分量的取值不影响这个事实。

三个向量位于二维子空间，必线性相关，所以 C 对所有给定常数均成立。

**8.（2012.8）答案：B。**

由 $AP=P\operatorname{diag}(1,1,2)$，可知 $\alpha_1,\alpha_2$ 都属于特征值1，$\alpha_3$ 属于特征值2。

因此 $\alpha_1+\alpha_2$ 仍是特征值1的特征向量。新的三列

$$
\alpha_1+\alpha_2,\quad\alpha_2,\quad\alpha_3
$$

也线性无关，因为它们是从原基作一次可逆列变换得到的。

所以

$$
AQ=Q\operatorname{diag}(1,1,2),
\qquad Q^{-1}AQ=\operatorname{diag}(1,1,2).
$$

## 二、填空题（9～14）

**9.（2012.9）答案：$1$。**

在 $x=0$ 处，方程为 $1-y=\mathrm e^y$。左边与右边相等的唯一实数为 $y=0$，因为 $y+\mathrm e^y$ 严格增加。

一阶求导得

$$
2x=(1+\mathrm e^y)y',
$$

所以 $y'(0)=0$。

再求导：

$$
2=(1+\mathrm e^y)y''+\mathrm e^y(y')^2.
$$

代入 $x=0,y=0,y'=0$，得 $2=2y''(0)$，故 $y''(0)=1$。

**10.（2012.10）答案：$\pi/4$。**

整理为黎曼和：

$$
n\sum_{k=1}^n\frac1{k^2+n^2}
=\frac1n\sum_{k=1}^n\frac1{1+(k/n)^2}.
$$

连续性给出极限

$$
\int_0^1\frac{\mathrm dt}{1+t^2}=[\arctan t]_0^1=\frac\pi4.
$$

**11.（2012.11）答案：$0$。**

令 $u=\ln x+1/y$，则

$$
z_x=\frac{f'(u)}x,
\qquad z_y=-\frac{f'(u)}{y^2}.
$$

所以

$$
xz_x+y^2z_y=f'(u)-f'(u)=0.
$$

**12.（2012.12）答案：$y=\sqrt x$。**

观察到原方程为全微分形式：

$$
y\,\mathrm dx+(x-3y^2)\,\mathrm dy
=\mathrm d(xy-y^3)=0.
$$

积分得 $xy-y^3=C$。由 $y(1)=1$，得 $C=0$，即 $y(x-y^2)=0$。

经过 $(1,1)$ 的分支满足 $y^2=x$ 且 $y>0$，故为 $y=\sqrt x$，$x>0$。

**13.（2012.13）答案：$(-1,0)$。**

有 $y'=2x+1$、$y''=2$，所以曲率条件为

$$
\frac2{[1+(2x+1)^2]^{3/2}}=\frac{\sqrt2}{2}.
$$

整理后得到 $1+(2x+1)^2=2$，即 $x=0$ 或 $x=-1$。

题设限制 $x<0$，因此取 $x=-1$，此时 $y=x^2+x=0$，故为点 $(-1,0)$。

**14.（2012.14）答案：$-27$。**

交换两行使行列式变号，所以 $|B|=-|A|=-3$。

三阶伴随矩阵满足 $|A^*|=|A|^2=9$，因此

$$
|BA^*|=|B|\,|A^*|=(-3)\cdot9=-27.
$$

## 三、解答题（15～23）

**15.（2012.15）答案：$a=1,k=1$。**

将函数精确整理为

$$
f(x)-1
=\frac{1+x}{\sin x}-\frac{1+x}{x}
=\frac{(1+x)(x-\sin x)}{x\sin x}.
$$

由 $x-\sin x\sim x^3/6$、$\sin x\sim x$，得到

$$
f(x)-1\sim\frac x6.
$$

因此 $f(x)\to1$，即 $a=1$；而 $f(x)-a$ 与 $x$ 同阶，故 $k=1$。

**16.（2012.16）答案：极大值 $\mathrm e^{-1/2}$，极小值 $-\mathrm e^{-1/2}$。**

**第一步：求驻点。**

$$
f_x=(1-x^2)\mathrm e^{-(x^2+y^2)/2},
\qquad f_y=-xy\mathrm e^{-(x^2+y^2)/2}.
$$

指数因子恒正，由 $f_x=0$ 得 $x=\pm1$，再由 $f_y=0$ 得 $y=0$。

**第二步：计算二阶偏导。**

$$
f_{xx}=(x^3-3x)\mathrm e^{-(x^2+y^2)/2},
$$

$$
f_{xy}=-y(1-x^2)\mathrm e^{-(x^2+y^2)/2},
\qquad f_{yy}=x(y^2-1)\mathrm e^{-(x^2+y^2)/2}.
$$

在 $(1,0)$ 处，Hessian 对角元为 $-2\mathrm e^{-1/2}$、$-\mathrm e^{-1/2}$，交叉项为零，故是极大值点，值为 $\mathrm e^{-1/2}$。

在 $(-1,0)$ 处，两对角元均为正，故是极小值点，值为 $-\mathrm e^{-1/2}$。

此外，$|x|\mathrm e^{-x^2/2}\le\mathrm e^{-1/2}$，且 $\mathrm e^{-y^2/2}\le1$，所以这也是全局最大值和最小值。

**17.（2012.17）答案：面积为2，体积为 $\dfrac{2\pi}{3}(\mathrm e^2-1)$。**

**第一步：确定切点 $A$ 和交点 $B$。**

设切点横坐标为 $a>0$，切线为

$$
y-\ln a=\frac1a(x-a).
$$

它经过 $(0,1)$，所以 $1=\ln a-1$，得到 $a=\mathrm e^2$。因此 $A=(\mathrm e^2,2)$。

对数曲线与 $x$ 轴的交点为 $B=(1,0)$。连接 $A,B$ 的直线是

$$
y=\frac2{\mathrm e^2-1}(x-1).
$$

**第二步：计算面积。**

$\ln x$ 在该区间的图像位于弦 $AB$ 上方。记 $E=\mathrm e^2$，则

$$
\begin{aligned}
S&=\int_1^E\left[\ln x-\frac2{E-1}(x-1)\right]\mathrm dx\\
&=(E+1)-(E-1)=2.
\end{aligned}
$$

其中对数积分为 $[x\ln x-x]_1^E=E+1$，弦下三角形面积为 $E-1$。

**第三步：计算绕 $x$ 轴的圆环体积。**

在 $[1,E]$ 上两条边界均非负，所以

$$
V=\pi\int_1^E\left[(\ln x)^2-\frac4{(E-1)^2}(x-1)^2\right]\mathrm dx.
$$

分别计算：

$$
\int_1^E\ln^2x\,\mathrm dx
=\left[x(\ln^2x-2\ln x+2)\right]_1^E=2(E-1),
$$

$$
\frac4{(E-1)^2}\int_1^E(x-1)^2\,\mathrm dx=\frac43(E-1).
$$

因此 $V=(2\pi/3)(E-1)=(2\pi/3)(\mathrm e^2-1)$。

**18.（2012.18）答案：$16/15$。**

区域在上半平面，极坐标范围为

$$
0\le\theta\le\pi,\qquad0\le r\le1+\cos\theta.
$$

换元时，$xy=r^2\sin\theta\cos\theta$，面积元为 $r\,\mathrm dr\,\mathrm d\theta$。因此

$$
I=\frac14\int_0^\pi\sin\theta\cos\theta(1+\cos\theta)^4\,\mathrm d\theta.
$$

令 $u=1+\cos\theta$，则 $\mathrm du=-\sin\theta\,\mathrm d\theta$，上下限从2到0，得到

$$
\begin{aligned}
I&=\frac14\int_0^2(u-1)u^4\,\mathrm du\\
&=\frac14\left[\frac{u^6}{6}-\frac{u^5}{5}\right]_0^2
=\frac{16}{15}.
\end{aligned}
$$

**19.（2012.19）答案：$f(x)=\mathrm e^x$，唯一拐点为 $(0,0)$。**

**（Ⅰ）联立两条微分关系。**

将 $f''+f=2\mathrm e^x$ 求导，得到

$$
f'''+f'=2\mathrm e^x.
$$

另一条方程给出 $f'''+f'=2f$，比较可得 $f=\mathrm e^x$。代回两条原方程均满足，所以函数已唯一确定。

**（Ⅱ）先整理曲线及其导数。**

所求曲线为

$$
y(x)=\mathrm e^{x^2}\int_0^x\mathrm e^{-t^2}\,\mathrm dt.
$$

求导得

$$
y'=2xy+1,
\qquad y''=2y+2xy'=2(1+2x^2)y+2x.
$$

当 $x>0$ 时，积分为正，所以 $y>0$，从而 $y''>0$；当 $x<0$ 时，积分为负，$y<0$，从而 $y''<0$。

在零点 $y(0)=0$，两侧凹凸性由下弯变上弯，因此 $(0,0)$ 是拐点。其他位置二阶导数不为零且不变号，所以拐点唯一。

**20.（2012.20）不等式的证明。**

待证不等式两边均为偶函数，因此只需讨论 $0\le x<1$。

首先，

$$
\ln\frac{1+x}{1-x}
=2\int_0^x\frac{\mathrm dt}{1-t^2}\ge2x.
$$

其次，由 $|\sin u|\le|u|$，有

$$
1-\cos x=2\sin^2(x/2)\le\frac{x^2}{2},
\qquad \cos x\ge1-\frac{x^2}{2}.
$$

两式结合，得到

$$
\begin{aligned}
x\ln\frac{1+x}{1-x}+\cos x
&\ge2x^2+1-\frac{x^2}{2}\\
&=1+\frac{3x^2}{2}
\ge1+\frac{x^2}{2}.
\end{aligned}
$$

由偶性，结论对 $-1<x<1$ 全部成立。

**21.（2012.21）唯一实根及数列极限。**

**（Ⅰ）证明存在且唯一。**

记 $F_n(x)=x+x^2+\cdots+x^n$，其中 $n\ge2$。在正半轴上，$F_n$ 连续且严格增加。

又有

$$
F_n(1/2)=1-2^{-n}<1,
\qquad F_n(1)=n>1.
$$

零点定理保证 $F_n(x)=1$ 在 $(1/2,1)$ 内至少有一个解；严格单调性保证至多一个，故有唯一实根 $x_n$。

**（Ⅱ）先证明单调有界。**

由 $F_n(x_n)=1$，可知

$$
F_{n+1}(x_n)=1+x_n^{n+1}>1.
$$

因为 $F_{n+1}$ 严格增加，所以它等于1的根必在 $x_n$ 左侧，即 $x_{n+1}<x_n$。

因此 $1/2<x_{n+1}<x_n$，数列单调递减有下界，必有极限 $L\ge1/2$。

**再确定极限。**

利用有限等比数列求和，

$$
\frac{x_n(1-x_n^n)}{1-x_n}=1,
\qquad2x_n-1=x_n^{n+1}.
$$

由于 $0<x_n\le x_2<1$，有

$$
0<x_n^{n+1}\le x_2^{n+1}\longrightarrow0.
$$

所以 $2L-1=0$，即 $L=1/2$。这里使用了统一的上界 $x_2<1$。

**22.（2012.22）答案：$a=-1$，通解如下。**

**（Ⅰ）计算行列式。**

沿第一行展开，两个非零项分别给出1与 $-a\cdot a^3$，因此

$$
\det A=1-a^4.
$$

**（Ⅱ）逐一检查奇异参数。**

有无穷多解必须 $\det A=0$，对实数 $a$，只需检查 $a=1,-1$。

当 $a=1$ 时，系数矩阵的四行满足 $R_1-R_2+R_3-R_4=0$，但右端同样组合为 $1-(-1)+0-0=2$，所以不相容，无解。

当 $a=-1$ 时，方程为

$$
\begin{cases}
x_1-x_2=1,\\x_2-x_3=-1,\\x_3-x_4=0,\\-x_1+x_4=0.
\end{cases}
$$

令 $x_4=t$，依次解得 $x_3=t,x_2=t-1,x_1=t$，最后一式也满足。

因此唯一允许参数为 $a=-1$，通解是

$$
\boldsymbol x=\begin{pmatrix}0\\-1\\0\\0\end{pmatrix}
+t\begin{pmatrix}1\\1\\1\\1\end{pmatrix},\qquad t\in\mathbb R.
$$

**23.（2012.23）答案：$a=-1$，标准形为 $2y_1^2+6y_2^2$。**

**（Ⅰ）先把二次型的秩转化为 $A$ 的秩。**

因为

$$
\boldsymbol x^{\mathrm T}A^{\mathrm T}A\boldsymbol x
=\|A\boldsymbol x\|^2,
$$

所以 $A^{\mathrm T}A$ 与 $A$ 有相同零空间，从而秩相同。题设给出 $r(A)=2$。

$A$ 的前两行 $(1,0,1)$、$(0,1,1)$ 线性无关，因此第三行必须是它们的线性组合。

第三行 $(-1,0,a)$ 的前两项强制组合系数为 $-1,0$，其第三项便应为 $-1$，故 $a=-1$。此时第四行也恰为第二行的相反数，秩确为2。

**（Ⅱ）求 Gram 矩阵的特征向量。**

代入参数后，

$$
B=A^{\mathrm T}A=\begin{pmatrix}2&0&2\\0&2&2\\2&2&4\end{pmatrix}.
$$

其特征值为 $2,6,0$，对应的互相正交特征向量可分别取

$$
p_1=(1,-1,0)^{\mathrm T},\quad
p_2=(1,1,2)^{\mathrm T},\quad
p_3=(1,1,-1)^{\mathrm T}.
$$

直接相乘可验证 $Bp_1=2p_1$、$Bp_2=6p_2$、$Bp_3=0$。

将它们单位化，得到

$$
Q=\begin{pmatrix}
1/\sqrt2&1/\sqrt6&1/\sqrt3\\
-1/\sqrt2&1/\sqrt6&1/\sqrt3\\
0&2/\sqrt6&-1/\sqrt3
\end{pmatrix}.
$$

于是 $Q^{\mathrm T}Q=I$，且 $Q^{\mathrm T}BQ=\operatorname{diag}(2,6,0)$。

所以在 $\boldsymbol x=Q\boldsymbol y$ 下，二次型化为 $2y_1^2+6y_2^2$。
