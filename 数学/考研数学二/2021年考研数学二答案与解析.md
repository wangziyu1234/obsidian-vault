# 2021年考研数学二答案与解析

> 我的解答，未与官方核对。

## 答案速查

> 选择题：1.C　2.D　3.C　4.A　5.D　6.C　7.B　8.B　9.D　10.C。

> 填空题：11.$\dfrac1{\ln3}$；12.$\dfrac23$；13.$1$；14.$\dfrac\pi2\cos\dfrac2\pi$；15.$C_1\mathrm e^x+\mathrm e^{-x/2}(C_2\cos\frac{\sqrt3x}{2}+C_3\sin\frac{\sqrt3x}{2})$；16.$-5$。

17. 极限为 $\dfrac12$。

18. 凹区间为 $(-\infty,-1)$、$(0,+\infty)$，凸区间为 $(-1,0)$；渐近线为 $x=-1$、$y=-x+1$、$y=x-1$。

19. 弧长 $s=\dfrac{22}{3}$；旋转曲面面积 $A=\dfrac{425\pi}{9}$。

20. $y=1+\dfrac{x^6}{3}$；所求点为 $P=(1,\dfrac43)$。

21. $\iint_Dxy\,\mathrm dx\mathrm dy=\dfrac1{48}$。

22. $(a,b)=(1,1)$ 或 $(-1,3)$；对应的相似变换矩阵见正文。

## 一、选择题（1～10）

**1.（2021.1）答案：C，高阶无穷小。**

当 $t\to0$ 时，

$$
\mathrm e^{t^3}-1=t^3+O(t^6).
$$

在 $0\le t\le x^2$ 上积分，得到

$$
\begin{aligned}
\int_0^{x^2}(\mathrm e^{t^3}-1)\,\mathrm dt
&=\frac{(x^2)^4}{4}+O((x^2)^7)\\
&=\frac{x^8}{4}+O(x^{14}).
\end{aligned}
$$

所以它与 $x^7$ 的比值为

$$
\frac1{x^7}\int_0^{x^2}(\mathrm e^{t^3}-1)\,\mathrm dt
=\frac x4+O(x^7)\longrightarrow0.
$$

按高阶无穷小的定义，选 C。积分上限是 $x^2$，将主项 $t^3$ 积分后应得到 $x^8$。

这里 $x\to0$ 可以从两侧进行：积分上限 $x^2$ 始终非负，所得主项为 $x^8/4$。虽然 $x^7$ 在零点两侧异号，但比值都趋于0，因而两侧均符合高阶无穷小的定义。

**2.（2021.2）答案：D，$f'(0)=\dfrac12$。**

先看连续性。由 $\mathrm e^x-1\sim x$，

$$
\lim_{x\to0}\frac{\mathrm e^x-1}{x}=1=f(0),
$$

所以 $f$ 在 $0$ 处连续。

再直接使用导数定义：

$$
\begin{aligned}
f'(0)
&=\lim_{x\to0}\frac{f(x)-f(0)}x\\
&=\lim_{x\to0}\frac{\mathrm e^x-1-x}{x^2}
=\frac12.
\end{aligned}
$$

最后一步用了 $\mathrm e^x=1+x+x^2/2+o(x^2)$。导数非零，因此 D 正确，$0$ 也不是极值点。

也可从 $f(x)=1+x/2+o(x)$ 看出，充分小的正 $x$ 使 $f(x)>1$，充分小的负 $x$ 使 $f(x)<1$，所以原点附近同时有比 $f(0)$ 大和小的函数值。A、B 所说的最值因此不成立；C 的导数值也不对。

**3.（2021.3）答案：C。**

设半径、高分别为 $r(t),h(t)$。题设给出 $r'=2$、$h'=-3$。

**体积变化率。**

$$
V=\pi r^2h,
\qquad V'=2\pi rr'h+\pi r^2h'.
$$

在 $r=10$、$h=5$ 时，

$$
V'=2\pi\cdot10\cdot2\cdot5+\pi\cdot100\cdot(-3)
=-100\pi\ \mathrm{cm^3/s}.
$$

**表面积变化率。** 总表面积包含侧面与两个底面：

$$
S=2\pi rh+2\pi r^2,
$$

$$
S'=2\pi(r'h+rh')+4\pi rr'.
$$

代入得

$$
S'=2\pi(2\cdot5-3\cdot10)+4\pi\cdot10\cdot2
=40\pi\ \mathrm{cm^2/s}.
$$

因此体积在减少、表面积在增加，选 C。

体积变化率可以按两个因素的贡献分别核算：半径增加的贡献为 $2\pi\cdot10\cdot2\cdot5=200\pi$，高度降低的贡献为 $\pi\cdot10^2\cdot(-3)=-300\pi$，合计 $-100\pi$。表面积中两个底面的贡献为 $4\pi rr'=80\pi$，侧面的贡献为 $2\pi(r'h+rh')=-40\pi$，合计 $40\pi$。

**4.（2021.4）答案：A，$\dfrac ba>\mathrm e$。**

定义域为 $x>0$。若 $b\le0$，则

$$
f'(x)=a-\frac bx\ge a>0,
$$

函数严格增加，不可能有两个零点。因此必须 $b>0$。

令 $c=b/a>0$，除以正数 $a$ 后，研究

$$
F(x)=x-c\ln x.
$$

有

$$
F'(x)=1-\frac cx.
$$

故 $F$ 在 $(0,c)$ 上递减，在 $(c,+\infty)$ 上递增，在 $c$ 处取得唯一最小值

$$
F(c)=c(1-\ln c).
$$

此外，$F(x)$ 在 $x\to0^+$ 和 $x\to+\infty$ 时均趋于 $+\infty$。

所以恰有两个零点，当且仅当最小值严格小于零，即

$$
c(1-\ln c)<0\quad\Longleftrightarrow\quad c>\mathrm e.
$$

$c=\mathrm e$ 时只有一个零点，不能包含端点。

当 $c>\mathrm e$ 时，由连续性，函数从左端的正无穷下降到负的最小值，必在 $(0,c)$ 内经过零点；随后从负值上升到正无穷，必在 $(c,+\infty)$ 内再经过零点。两个区间内分别严格单调，因而各只有一个零点。若 $0<c<\mathrm e$，最小值为正，没有零点；$c=\mathrm e$ 时最小值为0，仅有 $x=c$ 这一个零点。

**5.（2021.5）答案：D，$a=0,b=\dfrac12$。**

二次泰勒多项式为

$$
f(0)+f'(0)x+\frac{f''(0)}2x^2.
$$

对 $f(x)=\sec x$，有

$$
f(0)=1,
\qquad f'(x)=\sec x\tan x,
\qquad f'(0)=0,
$$

$$
f''(x)=\sec x\tan^2x+\sec^3x,
\qquad f''(0)=1.
$$

因此多项式为 $1+x^2/2$，对应 $a=0,b=1/2$。

从余项角度看，$\sec x$ 在0附近光滑，因此 $\sec x=1+x^2/2+o(x^2)$。也可由 $\cos x=1-x^2/2+O(x^4)$ 取倒数，得到 $1/\cos x=1+x^2/2+O(x^4)$，其中一次项为零、二次项系数为正。

**6.（2021.6）答案：C，$\mathrm df(1,1)=\mathrm dy$。**

记 $p=f_x(1,1)$、$q=f_y(1,1)$。可微性保证复合函数的链式法则成立；要求全微分，需要求出这两个偏导数。

**第一步：沿第一条曲线得到一个方程。**

把复合关系中的单变量记为 $t$，对 $f(t+1,\mathrm e^t)=t(t+1)^2$ 求导：

$$
f_x(t+1,\mathrm e^t)+\mathrm e^t f_y(t+1,\mathrm e^t)
=(t+1)^2+2t(t+1).
$$

$t=0$ 时两个内变量为 $(1,1)$，所以

$$
p+q=1.
$$

**第二步：沿第二条曲线得到另一个方程。**

对 $f(t,t^2)=2t^2\ln t$ 求导，$t>0$，得到

$$
f_x(t,t^2)+2t f_y(t,t^2)=4t\ln t+2t.
$$

这次应取 $t=1$，使内变量也等于 $(1,1)$，故

$$
p+2q=2.
$$

**第三步：解出偏导数，写全微分。**

两式相减得 $q=1$，再代回得 $p=0$。因此

$$
\boxed{\mathrm df(1,1)=p\,\mathrm dx+q\,\mathrm dy=\mathrm dy}.
$$

两条参数曲线在 $(1,1)$ 处的切向量分别为 $(1,1)$、$(1,2)$，它们线性无关，所以两个方向上的导数足以唯一确定 $p,q$。两次代入的参数分别是0和1，不能混用。

**7.（2021.7）答案：B。**

将 $[0,1]$ 等分为 $n$ 段，每一段长度为 $\Delta x=1/n$，第 $k$ 段的中点为

$$
\xi_k=\frac12\left(\frac{k-1}{n}+\frac{k}{n}\right)=\frac{2k-1}{2n}.
$$

因 $f$ 连续，其黎曼和收敛到积分：

$$
\int_0^1f(x)\,\mathrm dx
=\lim_{n\to\infty}\sum_{k=1}^n
f\left(\frac{2k-1}{2n}\right)\frac1n.
$$

这正是 B。

A 的权重只有应有的一半；C 有 $2n$ 项，却用 $1/n$ 作权重；D 同样有 $2n$ 项，却用 $2/n$ 作权重。以常函数 $f\equiv1$ 代入，就分别得到 $1/2,2,4$，都不是积分值 $1$。

连续性保证 $f$ 在闭区间上黎曼可积，所以每小段可以任取样点，既可用中点，也可用左、右端点。但每项的权重必须与实际分段长度一致：$n$ 段用 $1/n$，$2n$ 段用 $1/(2n)$，不能只根据样点落在 $[0,1]$ 就判断正确。

**8.（2021.8）答案：B，正、负惯性指数为 $1,1$。**

令

$$
u=x_1+x_2,\qquad v=x_2+x_3.
$$

则 $x_3-x_1=v-u$，二次型可化简为

$$
f=u^2+v^2-(v-u)^2=2uv.
$$

进一步令

$$
y_1=\frac{u+v}{\sqrt2},\qquad
y_2=\frac{u-v}{\sqrt2},\qquad y_3=x_2.
$$

这是一组可逆线性变换，因为可以反解

$$
x_2=y_3,\quad
x_1=\frac{y_1+y_2}{\sqrt2}-y_3,\quad
x_3=\frac{y_1-y_2}{\sqrt2}-y_3.
$$

代入后

$$
f=y_1^2-y_2^2.
$$

所以正平方项、负平方项各有一个，还有一个零平方项。正、负惯性指数依次为 $1,1$。

关键在于 $u,v$ 已经满足 $x_3-x_1=v-u$，原来三个平方中的线性式并不独立，所以不能直接数原式的“两个正号、一个负号”。补上 $y_3=x_2$ 后变换才成为三维的可逆变换，此时数标准形中的正、负平方项才给出惯性指数。

**9.（2021.9）答案：D。**

题设的“$A$ 的列向量可由 $B$ 的列向量线性表出”意味着存在矩阵 $C$，使

$$
A=BC.
$$

两边转置得到

$$
A^{\mathrm T}=C^{\mathrm T}B^{\mathrm T}.
$$

若 $B^{\mathrm T}\boldsymbol x=0$，则

$$
A^{\mathrm T}\boldsymbol x
=C^{\mathrm T}(B^{\mathrm T}\boldsymbol x)=0.
$$

因此 $B^{\mathrm T}\boldsymbol x=0$ 的解都是 $A^{\mathrm T}\boldsymbol x=0$ 的解，选 D。

这里控制的是列空间的包含关系，转置以后才对应齐次方程组的解空间。不能直接把列空间包含改成 $Ax=0$、$Bx=0$ 的解空间包含。

**用反例排除其余关系。**

取 $A=0,B=I_3$，题设显然满足，但 $AX=0$ 和 $A^{\mathrm T}X=0$ 都有任意向量作为解，$BX=0$ 与 $B^{\mathrm T}X=0$ 只有零解，所以 A、B 不成立。

对 C，取

$$
A=\begin{pmatrix}0&1&0\\0&0&0\\0&0&0\end{pmatrix},
\qquad B=\begin{pmatrix}1&0&0\\0&0&0\\0&0&0\end{pmatrix}.
$$

$A$ 的每列仍可由 $B$ 的列线性表出。对 $X=(0,1,0)^{\mathrm T}$，有 $BX=0$，但 $AX=(1,0,0)^{\mathrm T}\ne0$，故 C 也不成立。

**10.（2021.10）答案：C。**

**第一步：用下三角矩阵完成行变换。**

希望先消去主对角线下方的元素。取

$$
P=\begin{pmatrix}1&0&0\\2&-1&0\\-3&2&1\end{pmatrix},
\qquad PA=\begin{pmatrix}1&0&-1\\0&1&-3\\0&0&0\end{pmatrix}.
$$

乘积中的第二行是原来的 $2R_1-R_2$，第三行是原来的 $-3R_1+2R_2+R_3$，分别计算得到 $(0,1,-3)$ 与 $(0,0,0)$。

**第二步：用上三角矩阵完成列变换。**

取

$$
Q=\begin{pmatrix}1&0&1\\0&1&3\\0&0&1\end{pmatrix},
\qquad PAQ=\begin{pmatrix}1&0&0\\0&1&0\\0&0&0\end{pmatrix}.
$$

若把 $PA$ 的三列记为 $c_1,c_2,c_3$，右乘 $Q$ 后三列分别为 $c_1,c_2,c_1+3c_2+c_3$，第三列确为零：

$$
c_1+3c_2+c_3
=\begin{pmatrix}1\\0\\0\end{pmatrix}
+\begin{pmatrix}0\\3\\0\end{pmatrix}
+\begin{pmatrix}-1\\-3\\0\end{pmatrix}=0.
$$

**第三步：检查题设的全部条件。**

$P$ 为下三角，$Q$ 为上三角，且 $\det P=-1$、$\det Q=1$，二者均可逆，所取矩阵正是 C。

对角矩阵允许对角元为零；原矩阵 $A$ 的秩为2，因此变换后的对角矩阵恰有两个非零对角元。

## 二、填空题（11～16）

**11.（2021.11）答案：$\dfrac1{\ln3}$。**

**第一步：利用偶性，化为正半轴上的反常积分。**

因为 $|x|3^{-x^2}$ 是非负偶函数，正、负半轴的积分相等。先对有限的 $R>0$ 计算

$$
I_R=2\int_0^R x\mathrm e^{-(\ln3)x^2}\,\mathrm dx.
$$

**第二步：换元并取上限极限。**

令 $u=(\ln3)x^2$，则 $\mathrm du=2(\ln3)x\,\mathrm dx$，从而

$$
\begin{aligned}
I_R&=\frac1{\ln3}\int_0^{(\ln3)R^2}\mathrm e^{-u}\,\mathrm du\\
&=\frac{1-\mathrm e^{-(\ln3)R^2}}{\ln3}.
\end{aligned}
$$

由于 $\ln3>0$，指数余项趋于零，每一侧积分都收敛，所以

$$
\boxed{\int_{-\infty}^{+\infty}|x|3^{-x^2}\,\mathrm dx
=\lim_{R\to+\infty}I_R=\frac1{\ln3}}.
$$

绝对值使负半轴的贡献也为正，不能把被积函数误当成奇函数。

**12.（2021.12）答案：$\dfrac23$。**

先求参数导数：

$$
x'(t)=2\mathrm e^t+1,
$$

$$
y'(t)=4t\mathrm e^t+2t=2t(2\mathrm e^t+1).
$$

因此

$$
\frac{\mathrm dy}{\mathrm dx}=
\frac{y'(t)}{x'(t)}=2t.
$$

再对 $x$ 求导时，要除以 $x'(t)$：

$$
\frac{\mathrm d^2y}{\mathrm dx^2}
=\frac{\mathrm d(2t)/\mathrm dt}{\mathrm dx/\mathrm dt}
=\frac2{2\mathrm e^t+1}.
$$

在 $t=0$ 处，结果为 $2/3$。

本题 $x'(t)=2\mathrm e^t+1>0$，因此参数附近可把 $t$ 看成 $x$ 的函数。求 $y'(t)$ 时，$4(t-1)\mathrm e^t$ 的导数为 $4\mathrm e^t+4(t-1)\mathrm e^t=4t\mathrm e^t$，是约去公共因子的关键。

用一般公式也可核验：$t=0$ 时 $x'=3,x''=2,y'=0,y''=6$，所以

$$
\frac{\mathrm d^2y}{\mathrm dx^2}
=\frac{y''x'-y'x''}{(x')^3}
=\frac{6\cdot3-0\cdot2}{27}=\frac23.
$$

**13.（2021.13）答案：$1$。**

首先确定对应的 $z$ 值。在 $(x,y)=(0,2)$ 处，隐式方程化为

$$
z+2\ln z=1.
$$

因为 $z>0$ 且左边对 $z$ 的导数为 $1+2/z>0$，解唯一；代入可知 $z=1$。

保持 $y$ 不变，对原方程求 $x$ 偏导：

$$
z+\left(x+1+\frac yz\right)z_x
-\frac{2y}{1+4x^2y^2}=0.
$$

代入 $x=0,y=2,z=1$，得到

$$
1+3z_x-4=0,
\qquad z_x=1.
$$

系数 $x+1+y/z$ 在该点为 $3\ne0$，故隐函数求导成立。

若记 $F(x,y,z)=(x+1)z+y\ln z-\arctan(2xy)-1$，则在 $z>0$ 的邻域内 $F$ 的一阶偏导连续，且 $F_z(0,2,1)=3\ne0$。因此可由隐函数定理确定局部可微函数 $z(x,y)$，并且

$$
z_x=-\frac{F_x}{F_z}
=\frac{\dfrac{2y}{1+4x^2y^2}-z}{x+1+y/z}.
$$

这里求 $F_x$ 时暂把 $z$ 当作独立变量，得到 $F_x=z-2y/(1+4x^2y^2)$；若直接对原方程求导，则必须同时保留 $z_x$ 项，两种写法得到相同结果。

**14.（2021.14）答案：$\dfrac\pi2\cos\dfrac2\pi$。**

**第一步：在所求点附近交换二重积分的次序。**

因为 $\pi/2>1$，只需在 $t>1$ 的邻域内求导。原积分区域为

$$
1\le x\le t^2,\qquad \sqrt x\le y\le t.
$$

这些条件等价于 $1\le y\le t$、$1\le x\le y^2$。换序后

$$
f(t)=\int_1^t\mathrm dy\int_1^{y^2}\sin\frac xy\,\mathrm dx.
$$

此时参数 $t$ 只出现在最外层积分的上限，被积函数在区域内连续，且 $y\ge1$，没有分母为零的问题。

**第二步：先求内层积分，再用变上限积分定理。**

对固定的 $y$，$\int\sin(x/y)\,\mathrm dx=-y\cos(x/y)$，所以

$$
f(t)=\int_1^t y\left(\cos\frac1y-\cos y\right)\mathrm dy,
\qquad
f'(t)=t\left(\cos\frac1t-\cos t\right).
$$

代入 $t=\pi/2$，利用 $\cos(\pi/2)=0$，得到

$$
\boxed{f'\left(\frac\pi2\right)=\frac\pi2\cos\frac2\pi}.
$$

**直接求导的核验。** 若仍按原次序，外层上限求导产生的边界项为

$$
2t\int_{\sqrt{t^2}}^t\sin\frac{t^2}{y}\,\mathrm dy=0,
$$

因为此处 $t>1$，$\sqrt{t^2}=t$。内层上限求导的贡献为 $\int_1^{t^2}\sin(x/t)\,\mathrm dx$，积分后同样得到上述 $f'(t)$。

**15.（2021.15）答案：下列通解。**

**第一步：求特征方程的全部根。**

把 $y=\mathrm e^{rx}$ 代入 $y'''-y=0$，得到 $(r^3-1)\mathrm e^{rx}=0$。由于指数不为零，特征方程为

$$
r^3-1=(r-1)(r^2+r+1)=0.
$$

一次因子给出 $r_1=1$；二次因子由求根公式给出

$$
r_{2,3}=\frac{-1\pm\sqrt{1-4}}2
=-\frac12\pm\frac{\sqrt3}{2}\mathrm i.
$$

**第二步：把共轭复根对应的解写成实函数。**

实根1对应 $\mathrm e^x$。由欧拉公式，复根 $\alpha\pm\mathrm i\beta$ 对应的两个实解为 $\mathrm e^{\alpha x}\cos\beta x$ 和 $\mathrm e^{\alpha x}\sin\beta x$，本题即

$$
\mathrm e^{-x/2}\cos\frac{\sqrt3x}{2},
\qquad \mathrm e^{-x/2}\sin\frac{\sqrt3x}{2}.
$$

三个特征根互不相同，这三个实解构成基础解系，所以三阶方程的通解含三个独立常数：

$$
\boxed{y=C_1\mathrm e^x+\mathrm e^{-x/2}
\left(C_2\cos\frac{\sqrt3x}{2}+C_3\sin\frac{\sqrt3x}{2}\right)},
\qquad C_1,C_2,C_3\in\mathbb R.
$$

**16.（2021.16）答案：$-5$。**

先作不改变行列式的行变换 $R_1\leftarrow R_1-2R_4$，得到

$$
f(x)=\begin{vmatrix}
x-4&x+2&-1&0\\
1&x&2&-1\\
2&1&x&1\\
2&-1&1&x
\end{vmatrix}.
$$

按第一行展开，对应三个三阶子式分别为

$$
\begin{vmatrix}x&2&-1\\1&x&1\\-1&1&x\end{vmatrix}=x^3-4x-3,
$$

$$
\begin{vmatrix}1&2&-1\\2&x&1\\2&1&x\end{vmatrix}=x^2-2x+1,
$$

$$
\begin{vmatrix}1&x&-1\\2&1&1\\2&-1&x\end{vmatrix}=-2x^2+3x+5.
$$

注意展开时第二项的代数余子式带负号，所以

$$
\begin{aligned}
f(x)&=(x-4)(x^3-4x-3)\\
&\quad-(x+2)(x^2-2x+1)-(-2x^2+3x+5)\\
&=x^4-5x^3-2x^2+13x+5.
\end{aligned}
$$

故 $x^3$ 项的系数为 $-5$。

若只求三次项，无须完成最后的全部展开：第一项 $(x-4)(x^3-4x-3)$ 的三次项系数为 $-4$；第二项 $-(x+2)(x^2-2x+1)$ 的三次项系数为 $-1$；第三项次数不超过2，不贡献三次项。因此所求系数直接为 $-4-1=-5$。

行变换的作用是把第一行第四列的 $2x$ 消成0，使余子式中的最高次幂容易识别；不能仅观察原行列式的主对角线来判断三次项。

## 三、解答题（17～22）

**17.（2021.17）答案：$\dfrac12$。**

**第一步：把两个发散项合并。**

记 $H(x)=\int_0^x\mathrm e^{t^2}\,\mathrm dt$，则原式等于

$$
\frac{[1+H(x)]\sin x-(\mathrm e^x-1)}{(\mathrm e^x-1)\sin x}.
$$

**第二步：确定足够阶数的展开。**

由 $\mathrm e^{t^2}=1+t^2+O(t^4)$，积分得到

$$
H(x)=x+\frac{x^3}{3}+O(x^5)=x+O(x^3).
$$

另外，

$$
\sin x=x+O(x^3),\qquad
\mathrm e^x-1=x+\frac{x^2}{2}+O(x^3).
$$

于是分子为

$$
\begin{aligned}
[1+H(x)]\sin x-(\mathrm e^x-1)
&=(1+x+O(x^3))(x+O(x^3))\\
&\quad-x-\frac{x^2}{2}+O(x^3)\\
&=\frac{x^2}{2}+O(x^3).
\end{aligned}
$$

而分母为 $x^2+O(x^3)$。因此

$$
\lim_{x\to0}\frac{x^2/2+O(x^3)}{x^2+O(x^3)}=\frac12.
$$

本题两个原分式都含 $1/x$ 级主项，合并后的一次项相消，保留二次项才能得到极限。

**用洛必达法则核验。**

记合并后的分子、分母为 $N(x),D(x)$。因为 $H'=\mathrm e^{x^2}$、$H''=2x\mathrm e^{x^2}$，有

$$
\begin{aligned}
N'(x)&=H'\sin x+(1+H)\cos x-\mathrm e^x,\\
N''(x)&=H''\sin x+2H'\cos x-(1+H)\sin x-\mathrm e^x,\\
D'(x)&=\mathrm e^x\sin x+(\mathrm e^x-1)\cos x,\\
D''(x)&=2\mathrm e^x\cos x+\sin x.
\end{aligned}
$$

代入0，得到 $N(0)=D(0)=N'(0)=D'(0)=0$，而 $N''(0)=1,D''(0)=2$。这些函数在0附近光滑，$D''$ 在该邻域非零，且 $D'$ 在去心邻域非零，因此可连续使用两次洛必达法则，极限同样为 $1/2$。

**18.（2021.18）凹凸区间及渐近线如下。**

**第一步：去掉绝对值并作除法。** 定义域为 $x\ne-1$，且

$$
f(x)=\begin{cases}
-x+1-\dfrac1{x+1},&x<0,\\[4pt]
x-1+\dfrac1{x+1},&x\ge0.
\end{cases}
$$

**第二步：求二阶导数，判断凹凸性。**

对 $x<0,x\ne-1$，

$$
f'(x)=-1+\frac1{(x+1)^2},\qquad
f''(x)=-\frac2{(x+1)^3}.
$$

对 $x>0$，

$$
f'(x)=1-\frac1{(x+1)^2},\qquad
f''(x)=\frac2{(x+1)^3}.
$$

因此符号分布为

$$
\begin{cases}
f''(x)>0,&x\in(-\infty,-1),\\
f''(x)<0,&x\in(-1,0),\\
f''(x)>0,&x\in(0,+\infty).
\end{cases}
$$

所以凹区间（向上弯曲）为 $(-\infty,-1)$、$(0,+\infty)$，凸区间（向下弯曲）为 $(-1,0)$。

在 $x=0$ 处，两侧函数值均趋于 $0=f(0)$，且

$$
f'(0)=\lim_{h\to0}\frac{h|h|}{h(1+h)}=0.
$$

曲线在此处连续，两侧凹凸性改变，因此 $(0,0)$ 是拐点。$x=-1$ 不在定义域中，不能称为拐点。

**第三步：求竖直渐近线。** 由负半轴上的表达式，

$$
\lim_{x\to-1^-}f(x)=+\infty,
\qquad \lim_{x\to-1^+}f(x)=-\infty.
$$

因此 $x=-1$ 为竖直渐近线。

**第四步：求两个方向的斜渐近线。**

当 $x\to+\infty$ 时，

$$
f(x)-(x-1)=\frac1{x+1}\longrightarrow0,
$$

故斜渐近线为 $y=x-1$。

当 $x\to-\infty$ 时，

$$
f(x)-(-x+1)=-\frac1{x+1}\longrightarrow0,
$$

故斜渐近线为 $y=-x+1$。定义域内没有其他奇点，两端也没有水平渐近线。

**检查分段点的二阶导数。**

虽然 $f'(0)=0$，但二阶导数不存在。对负、正的 $h$ 分别计算，

$$
\begin{aligned}
\lim_{h\to0^-}\frac{f'(h)-f'(0)}h
&=\lim_{h\to0^-}\frac{-1+(1+h)^{-2}}h=-2,\\
\lim_{h\to0^+}\frac{f'(h)-f'(0)}h
&=\lim_{h\to0^+}\frac{1-(1+h)^{-2}}h=2.
\end{aligned}
$$

因此本题应按 $-1,0$ 分段考察凹凸性。拐点要求曲线在该点连续且两侧凹凸性改变，并不要求二阶导数必须存在或等于零。

**19.（2021.19）答案：$s=\dfrac{22}{3}$，$A=\dfrac{425\pi}{9}$。**

**第一步：由不定积分关系确定 $f(x)$。**

不定积分等式给出一个原函数，对两边求导，常数 $C$ 消失，得到

$$
\frac{f(x)}{\sqrt x}=\frac x3-1,
\qquad f(x)=\frac13x^{3/2}-\sqrt x=\frac{\sqrt x(x-3)}3.
$$

本题积分区间为 $4\le x\le9$，其中 $x>3$，所以 $f(x)>0$。旋转半径可以直接取 $f(x)$。

**第二步：把弧长微元中的根式化为完全平方。**

先求导并通分，

$$
f'(x)=\frac12\sqrt x-\frac1{2\sqrt x}
=\frac{x-1}{2\sqrt x}.
$$

因此

$$
\begin{aligned}
1+[f'(x)]^2
&=1+\frac{(x-1)^2}{4x}
=\frac{x^2+2x+1}{4x}=\frac{(x+1)^2}{4x},\\
\sqrt{1+[f'(x)]^2}&=\frac{x+1}{2\sqrt x}.
\end{aligned}
$$

最后一步用了 $x>0$，故 $x+1>0$，开平方后绝对值可以去掉。

**第三步：积分求弧长。**

$$
\begin{aligned}
s&=\int_4^9\sqrt{1+[f'(x)]^2}\,\mathrm dx
=\frac12\int_4^9\left(\sqrt x+\frac1{\sqrt x}\right)\mathrm dx\\
&=\left[\frac13x^{3/2}+\sqrt x\right]_4^9
=(9+3)-\left(\frac83+2\right)
=\boxed{\frac{22}{3}}.
\end{aligned}
$$

**第四步：用曲面面积公式计算 $A$。**

曲线上长度为 $\mathrm ds$ 的小段绕 $x$ 轴旋转，面积微元为 $2\pi f(x)\,\mathrm ds$。把两个因子先相乘，根式恰好约去：

$$
2\pi f(x)\sqrt{1+[f'(x)]^2}
=2\pi\cdot\frac{\sqrt x(x-3)}3\cdot\frac{x+1}{2\sqrt x}
=\frac\pi3(x^2-2x-3).
$$

于是

$$
\begin{aligned}
A&=\frac\pi3\int_4^9(x^2-2x-3)\,\mathrm dx\\
&=\frac\pi3\left[\frac{x^3}{3}-x^2-3x\right]_4^9\\
&=\frac\pi3\left[\left(243-81-27\right)
-\left(\frac{64}{3}-16-12\right)\right]\\
&=\frac\pi3\left(135+\frac{20}{3}\right)=\boxed{\frac{425\pi}{9}}.
\end{aligned}
$$

这里计算的是曲线 $L$ 旋转形成的曲面面积，积分范围正是 $4\le x\le9$，没有另外添加两端圆盘的面积。

**20.（2021.20）答案：$y=1+\dfrac{x^6}{3}$；$P=(1,\dfrac43)$。**

**（Ⅰ）用积分因子法解一阶线性方程。**

因为 $x>0$，可除以 $x$，将方程化为

$$
y'-\frac6x y=-\frac6x.
$$

积分因子为 $\mu(x)=\mathrm e^{\int(-6/x)\,\mathrm dx}=x^{-6}$。两边同乘后，左端成为乘积的导数：

$$
x^{-6}y'-6x^{-7}y=-6x^{-7}
\quad\Longrightarrow\quad
(x^{-6}y)'=-6x^{-7}.
$$

积分得 $x^{-6}y=x^{-6}+C$，所以 $y=1+Cx^6$。由初值条件，

$$
1+C(\sqrt3)^6=10
\quad\Longrightarrow\quad 1+27C=10
\quad\Longrightarrow\quad C=\frac13.
$$

因此 $\boxed{y(x)=1+x^6/3}$。代回方程可见 $xy'-6y=2x^6-6-2x^6=-6$，且 $y(\sqrt3)=10$。

**（Ⅱ）第一步：将法线截距写成点的横坐标的函数。**

令 $P=(t,1+t^6/3)$，其中 $t>0$。切线斜率为 $y'(t)=2t^5>0$，所以法线斜率存在并等于 $-1/(2t^5)$。用 $(X,Y)$ 表示法线上的点，有

$$
Y-1-\frac{t^6}{3}=-\frac{X-t}{2t^5}.
$$

令 $X=0$，得到法线在 $y$ 轴上的截距

$$
I(t)=1+\frac{t^6}{3}+\frac1{2t^4},\qquad t>0.
$$

**第二步：求驻点并验证为全局最小值点。**

$$
I'(t)=2t^5-2t^{-5}=\frac{2(t^{10}-1)}{t^5}.
$$

分母为正，分子在 $0<t<1$ 时为负，在 $t>1$ 时为正。因此 $I$ 在 $(0,1)$ 上严格递减，在 $(1,+\infty)$ 上严格递增，唯一最小值点为 $t=1$。

两端 $t\to0^+$ 与 $t\to+\infty$ 时 $I(t)$ 均趋于正无穷，也与这一全局最小值结论一致。所求点及最小截距分别为

$$
\boxed{P=\left(1,\frac43\right)},
\qquad I_{\min}=I(1)=1+\frac13+\frac12=\frac{11}{6}.
$$

在此点，法线为 $Y-4/3=-(X-1)/2$，令 $X=0$ 再次得到截距 $11/6$。

**21.（2021.21）答案：$\dfrac1{48}$。**

**第一步：把边界化为极坐标。**

令 $x=r\cos\theta$、$y=r\sin\theta$。边界方程化为

$$
r^4=r^2\cos2\theta.
$$

除去原点后，边界为 $r^2=\cos2\theta$。题目位于第一象限，所以 $\cos2\theta\ge0$ 给出

$$
0\le\theta\le\frac\pi4,
\qquad0\le r\le\sqrt{\cos2\theta}.
$$

**第二步：连同面积元一起换元。**

$$
xy=r^2\sin\theta\cos\theta,
\qquad \mathrm dx\mathrm dy=r\,\mathrm dr\,\mathrm d\theta.
$$

故

$$
\begin{aligned}
I&=\int_0^{\pi/4}\int_0^{\sqrt{\cos2\theta}}
 r^3\sin\theta\cos\theta\,\mathrm dr\,\mathrm d\theta\\
&=\frac18\int_0^{\pi/4}\sin2\theta\cos^22\theta\,\mathrm d\theta.
\end{aligned}
$$

上式的系数可以分开核对：径向积分贡献 $\int_0^{\sqrt{\cos2\theta}}r^3\,\mathrm dr=\cos^22\theta/4$，而角度因子满足 $\sin\theta\cos\theta=\sin2\theta/2$，乘积系数因此为 $1/8$。

**第三步：计算一元积分。** 令 $u=\cos2\theta$，则 $\mathrm du=-2\sin2\theta\,\mathrm d\theta$，上下限由 $1$ 变为 $0$。

$$
I=\frac1{16}\int_0^1u^2\,\mathrm du=\frac1{48}.
$$

**另一种换元：用边界中的两个二次式作变量。**

令 $u=x^2+y^2$、$v=x^2-y^2$。题设曲线变为 $v=u^2$，$x$ 轴变为 $v=u$。第一象限内可以反解

$$
x=\sqrt{\frac{u+v}{2}},
\qquad y=\sqrt{\frac{u-v}{2}}.
$$

区域内部满足 $u^2<v<u$，所以新区域为 $0\le u\le1$、$u^2\le v\le u$。其雅可比行列式为

$$
\frac{\partial(u,v)}{\partial(x,y)}
=\begin{vmatrix}2x&2y\\2x&-2y\end{vmatrix}=-8xy.
$$

在内部 $x,y>0$，变换一一对应且雅可比不为零，故 $xy\,\mathrm dx\mathrm dy=\tfrac18\,\mathrm du\mathrm dv$。边界上可能出现的零雅可比不影响积分，得到

$$
I=\frac18\int_0^1\mathrm du\int_{u^2}^{u}\mathrm dv
=\frac18\int_0^1(u-u^2)\,\mathrm du
=\frac18\left(\frac12-\frac13\right)=\frac1{48}.
$$

**22.（2021.22）答案：$(a,b)=(1,1)$ 或 $(-1,3)$。**

**第一步：确定可能的特征值及可对角化条件。**

矩阵右上角为零块，按最后一列展开特征行列式，得到

$$
\det(\lambda I-A)
=(\lambda-b)[(\lambda-2)^2-1]
=(\lambda-b)(\lambda-1)(\lambda-3).
$$

特征值1和3始终存在。“仅有两个不同的特征值”要求 $b=1$ 或 $b=3$，使其中一个特征值成为二重根。

三阶矩阵可对角化，当且仅当能取到三个线性无关的特征向量。简单特征值的特征子空间维数为1，因此二重特征值 $\lambda_0$ 必须满足 $\dim\ker(A-\lambda_0I)=2$，由秩与零空间维数之和为3，等价于 $r(A-\lambda_0I)=1$。

**情形一：$b=1$，二重特征值为1。**

此时

$$
A-I=\begin{pmatrix}1&1&0\\1&1&0\\1&a&0\end{pmatrix}.
$$

作 $R_2\leftarrow R_2-R_1$、$R_3\leftarrow R_3-R_1$，得到行向量 $(1,1,0)$、$0$、$(0,a-1,0)$。秩恰为1的充要条件为 $a=1$。

取 $(a,b)=(1,1)$ 后，解特征向量方程：

$$
\begin{aligned}
\lambda=1:&\quad u+v=0,\qquad w\text{ 任意};\\
\lambda=3:&\quad -u+v=0,\quad u+v-2w=0
\quad\Longrightarrow\quad v=u,\ w=u.
\end{aligned}
$$

因此分别取两个属于1的独立特征向量 $p_1=(1,-1,0)^{\mathrm T}$、$p_2=(0,0,1)^{\mathrm T}$，以及属于3的特征向量 $p_3=(1,1,1)^{\mathrm T}$。组成

$$
\boxed{P_1=\begin{pmatrix}1&0&1\\-1&0&1\\0&1&1\end{pmatrix}},
\qquad \Lambda_1=\operatorname{diag}(1,1,3).
$$

按第二列展开得 $\det P_1=-2\ne0$。逐列相乘验证：
$$
AP_1=(p_1,p_2,3p_3)=P_1\Lambda_1,
\qquad \boxed{P_1^{-1}AP_1=\operatorname{diag}(1,1,3)}.
$$

**情形二：$b=3$，二重特征值为3。**

这时

$$
A-3I=\begin{pmatrix}-1&1&0\\1&-1&0\\1&a&0\end{pmatrix}.
$$

第一、二行互为相反数。作 $R_3\leftarrow R_3-R_2$，第三行变为 $(0,a+1,0)$，所以秩恰为1，当且仅当 $a=-1$。

取 $(a,b)=(-1,3)$ 后，对特征值1有

$$
(A-I)(u,v,w)^{\mathrm T}=0
\iff\begin{cases}u+v=0,\\u-v+2w=0,\end{cases}
\iff v=-u,\ w=-u.
$$

故可取 $q_1=(1,-1,-1)^{\mathrm T}$。对特征值3，方程只有 $v=u$ 这一独立限制，$w$ 任意，基础解系可取

$$
q_2=(1,1,0)^{\mathrm T},\qquad q_3=(0,0,1)^{\mathrm T}.
$$

按照特征值 $1,3,3$ 的顺序排列，得到

$$
\boxed{P_2=\begin{pmatrix}1&1&0\\-1&1&0\\-1&0&1\end{pmatrix}},
\qquad \Lambda_2=\operatorname{diag}(1,3,3).
$$

其行列式为 $\det P_2=2\ne0$，并且

$$
AP_2=(q_1,3q_2,3q_3)=P_2\Lambda_2,
\qquad \boxed{P_2^{-1}AP_2=\operatorname{diag}(1,3,3)}.
$$

两种参数情形已穷尽全部可能；每种情形均构造了可逆矩阵并验证相似等式，所以既满足必要条件，也证明了充分性。
