# 2014年考研数学二答案与解析

> 我的解答，未与官方核对。

## 答案速查

> 选择题：1.B　2.C　3.D　4.C　5.D　6.A　7.B　8.A。

> 填空题：9.$3\pi/8$；10.$1$；11.$-\dfrac12\mathrm dx-\dfrac12\mathrm dy$；12.$y=\dfrac\pi2-\dfrac2\pi x$；13.$11/20$；14.$[-2,2]$。

15. 极限为 $1/2$。

16. 极小值为0，极大值为1。

17. 积分为 $-3/4$。

18. $f(u)=\dfrac{\mathrm e^{2u}-\mathrm e^{-2u}}{16}-\dfrac u4$。

19. 积分不等式的证明见正文。

20. 极限为1。

21. 体积为 $\pi(2\ln2-5/4)$。

22. 齐次基础解系可取 $(-1,2,3,1)^{\mathrm T}$；全部右逆矩阵见正文。

23. 两矩阵均相似于 $\operatorname{diag}(n,0,\ldots,0)$。

## 一、选择题（1～8）

**1.（2014.1）答案：B，$1<\alpha<2$。**

当 $x\to0^+$ 时，

$$
\ln^\alpha(1+2x)\sim(2x)^\alpha.
$$

它比 $x$ 高阶，要求 $\alpha>1$。

另一个无穷小满足

$$
(1-\cos x)^{1/\alpha}\sim\left(\frac{x^2}{2}\right)^{1/\alpha},
$$

其阶数为 $2/\alpha$。在 $\alpha>1$ 的前提下，要使它也比 $x$ 高阶，还需 $2/\alpha>1$，即 $\alpha<2$。

合并为 $1<\alpha<2$。

**把“高阶”写成比值极限。**

两个要求分别是

$$
\frac{\ln^\alpha(1+2x)}x\to0,
\qquad
\frac{(1-\cos x)^{1/\alpha}}x\to0.
$$

由于 $x\to0^+$，底数均为正，可直接比较幂次：

$$
\frac{\ln^\alpha(1+2x)}x\sim2^\alpha x^{\alpha-1},
\qquad
\frac{(1-\cos x)^{1/\alpha}}x
\sim2^{-1/\alpha}x^{2/\alpha-1}.
$$

第一个极限为零，先给出 $\alpha>1$；此时乘以 $\alpha$ 不改变不等号方向，第二个条件才等价于 $2>\alpha$。若 $\alpha=1$，第一个比值趋于2；若 $\alpha=2$，第二个比值趋于 $1/\sqrt2$。两端都是同阶而非高阶，因而均应排除。

**2.（2014.2）答案：C。**

对 C，当 $x\to\pm\infty$ 时，

$$
y-x=\sin\frac1x\longrightarrow0.
$$

因此直线 $y=x$ 是它的斜渐近线。

A 中若存在斜渐近线，斜率只能为1，但截距极限 $\lim\sin x$ 不存在。

B、D 在无穷远按 $x^2$ 增长，不能趋近任何斜率有限的直线。它们在有限点处也没有趋于无穷的情形：B 处处连续，D 在零点附近有界振荡。

所以只有 C 有渐近线。

**逐类检查渐近线的定义。**

直线 $y=kx+b$ 是无穷远处的渐近线，要求

$$
\lim_{x\to\pm\infty}[f(x)-kx-b]=0.
$$

若极限成立，必有 $k=\lim f(x)/x$，再有 $b=\lim[f(x)-kx]$。对 A，第一步得到 $k=1$，第二步却需要不存在的 $\lim\sin x$。例如沿 $x=2m\pi$ 和 $x=2m\pi+\pi/2$，余项分别为0和1，所以不能固定一个截距。

对 B、D，$f(x)/x=x+O(1/x)$，没有有限斜率。C 则有 $k=1,b=0$，且余项 $\sin(1/x)\to0$。

竖直渐近线需要函数在某有限横坐标附近趋于无穷。C、D 虽在 $x=0$ 无定义，但 $\sin(1/x)$ 始终在 $[-1,1]$ 内，所以 $x=0$ 不是竖直渐近线。

**3.（2014.3）答案：D。**

令 $h(x)=f(x)-g(x)$。由 $g$ 是连接两端函数值的直线，得

$$
h(0)=h(1)=0,
\qquad h''(x)=f''(x)\ge0.
$$

所以 $h'$ 单调不减。

固定 $0<x<1$，在 $[0,x]$、$[x,1]$ 上分别使用中值定理，存在 $u<x<v$，使

$$
\frac{h(x)}x=h'(u),
\qquad -\frac{h(x)}{1-x}=h'(v).
$$

由 $h'(u)\le h'(v)$，得到

$$
h(x)\left(\frac1x+\frac1{1-x}\right)\le0.
$$

括号内为正，所以 $h(x)\le0$，即 $f(x)\le g(x)$。端点处取等号，因此 D 成立。

**为什么只知道单调性不能确定与弦的位置。**

这里

$$
g(x)=(1-x)f(0)+xf(1)
$$

是端点间的弦。$f''\ge0$ 保证图像在弦下方，这与一阶导数是否为正是不同的信息。

例如在 $[0,1]$ 上取 $f(x)=x^2$，则 $f'\ge0$，但 $f(x)\le x=g(x)$，且内部严格小于，否定 A；取 $f(x)=2x-x^2$，同样有 $f'\ge0$，却有 $f(x)\ge x=g(x)$，且内部严格大于，否定 B。第一个例子也否定 C。两端取等号不影响区间内部的判断。

**4.（2014.4）答案：C，曲率半径为 $10\sqrt{10}$。**

当 $t=1$ 时，参数导数为

$$
x'=2,\quad y'=6,\quad x''=2,\quad y''=2.
$$

参数曲线的曲率半径公式为

$$
R=\frac{[(x')^2+(y')^2]^{3/2}}{|x'y''-y'x''|}.
$$

代入后，

$$
R=\frac{(4+36)^{3/2}}{|4-12|}
=\frac{80\sqrt{10}}8=10\sqrt{10}.
$$

**用直角坐标曲率公式再展开一次。**

在 $t=1$ 附近有 $x'(t)\ne0$，故可把曲线局部看成 $y(x)$。此时

$$
\frac{\mathrm dy}{\mathrm dx}
=\frac{2t+4}{2t}=1+\frac2t,
$$

$$
\frac{\mathrm d^2y}{\mathrm dx^2}
=\frac{\mathrm d}{\mathrm dt}\left(1+\frac2t\right)\frac1{2t}
=-\frac1{t^3}.
$$

代入 $t=1$，得 $y'=3,y''=-1$，所以曲率为

$$
\kappa=\frac{|y''|}{[1+(y')^2]^{3/2}}
=\frac1{10\sqrt{10}}.
$$

题目求曲率半径，需再取倒数 $R=1/\kappa=10\sqrt{10}$。绝对值保证半径为正。

**5.（2014.5）答案：D，$1/3$。**

由 $f'(\xi)=1/(1+\xi^2)$，给定关系可以写成

$$
\arctan x=\frac{x}{1+\xi^2}.
$$

因此

$$
\frac{\xi^2}{x^2}
=\frac{x-\arctan x}{x^2\arctan x}.
$$

再利用 $x-\arctan x=x^3/3+O(x^5)$、$\arctan x\sim x$，得到极限为 $1/3$。

**不用泰勒展开的计算。**

先把前面的比值分成

$$
\frac{\xi^2}{x^2}
=\frac{x-\arctan x}{x^3}\cdot\frac{x}{\arctan x}.
$$

第二个因子趋于1。第一个因子是 $0/0$ 型，应用一次洛必达法则便得到

$$
\lim_{x\to0}\frac{x-\arctan x}{x^3}
=\lim_{x\to0}\frac{1-1/(1+x^2)}{3x^2}
=\lim_{x\to0}\frac1{3(1+x^2)}=\frac13.
$$

因此无需先解出 $\xi$ 的正负。题目只问 $\xi^2/x^2$，由给定等式确定 $\xi^2=x/\arctan x-1$ 已足够；当 $x\ne0$ 时 $0<\arctan x/x<1$，也保证了这个平方值非负。

**6.（2014.6）答案：A。**

连续函数在有界闭区域上必能取到最大值和最小值。只需说明它们不可能在内部取得。

如果内部有极值点，则一阶偏导应为零。该点的 Hessian 行列式满足

$$
\begin{aligned}
u_{xx}u_{yy}-u_{xy}^2
&=-u_{xx}^2-u_{xy}^2<0,
\end{aligned}
$$

因为 $u_{yy}=-u_{xx}$，且 $u_{xy}\ne0$。

行列式为负说明此驻点是鞍点，不是极值点，矛盾。因此最大值和最小值均在边界上取得。

**把驻点判别的逻辑补全。**

有界闭区域是紧集，连续性先保证最大值、最小值存在。如果其中一个在内点取得，可微性给出 $u_x=u_y=0$。令该点的二阶偏导为

$$
a=u_{xx},\qquad b=u_{xy},\qquad u_{yy}=-a.
$$

二阶项对应的矩阵为

$$
H=\begin{pmatrix}a&b\\b&-a\end{pmatrix}.
$$

其特征值为 $\pm\sqrt{a^2+b^2}$。因 $b\ne0$，一个严格正、另一个严格负。在两个相应方向上，二阶泰勒主部异号，于是驻点附近既有比该点大的函数值，也有比该点小的函数值，不可能是最大或最小点。排除内部后，两个最值只能在边界取得。

**7.（2014.7）答案：B，$-(ad-bc)^2$。**

先交换第1列与第2列，行列式改变一次符号，所得矩阵为

$$
\begin{pmatrix}
a&0&b&0\\0&a&0&b\\c&0&d&0\\0&c&0&d
\end{pmatrix}
=\begin{pmatrix}aI_2&bI_2\\cI_2&dI_2\end{pmatrix}.
$$

再对行、列同时作相同的重新排列，即可得到两个 $\begin{pmatrix}a&b\\c&d\end{pmatrix}$ 组成的分块对角矩阵；这一步行列式符号不变。

所以交换一列后的行列式为 $(ad-bc)^2$，原行列式为其相反数，即 $-(ad-bc)^2$。

**写出重新排列后的矩阵并追踪符号。**

完成第1、2列交换后，再同时交换第2、3行和第2、3列，得到

$$
\begin{pmatrix}
a&b&0&0\\
c&d&0&0\\
0&0&a&b\\
0&0&c&d
\end{pmatrix}.
$$

后两次交换共乘以 $(-1)^2=1$，因此这一步不改变行列式值。分块对角矩阵的行列式为

$$
\begin{vmatrix}a&b\\c&d\end{vmatrix}
\begin{vmatrix}a&b\\c&d\end{vmatrix}=(ad-bc)^2.
$$

只有最开始单独交换两列引入一个负号，故原式为 $-(ad-bc)^2$。整个过程没有除以 $a,d$ 等参数，参数为零时仍然适用。

**8.（2014.8）答案：A，必要非充分条件。**

**必要性。** 若 $\alpha_1,\alpha_2,\alpha_3$ 线性无关，对任意 $k,l$，设

$$
c_1(\alpha_1+k\alpha_3)+c_2(\alpha_2+l\alpha_3)=0.
$$

整理为

$$
c_1\alpha_1+c_2\alpha_2+(kc_1+lc_2)\alpha_3=0.
$$

由三向量独立，得 $c_1=c_2=0$，所以新的两个向量线性无关。

**不充分。** 取 $\alpha_1=(1,0,0)^{\mathrm T}$、$\alpha_2=(0,1,0)^{\mathrm T}$、$\alpha_3=0$。任意 $k,l$ 都不改变前两个向量，它们始终独立，但原三向量组含零向量，线性相关。

所以前述条件必要但不充分。

**条件方向与“任意”的含义。**

记原三向量无关为条件 $P$，题干中“对所有 $k,l$，新二向量无关”为条件 $Q$。证明必要性应证 $P\Rightarrow Q$；证明不充分则需要给出 $Q$ 成立而 $P$ 不成立的例子。

反例中选 $\alpha_3=0$ 很关键：这样无论 $k,l$ 怎样取，新向量组总是 $e_1,e_2$。它满足的是“对任意常数”而不只是“能找到某一对常数”。原向量组又包含零向量，所以必定相关。

## 二、填空题（9～14）

**9.（2014.9）答案：$3\pi/8$。**

分母配方：

$$
x^2+2x+5=(x+1)^2+4.
$$

于是

$$
\begin{aligned}
I&=\frac12\left[\arctan\frac{x+1}{2}\right]_{-\infty}^{1}\\
&=\frac12\left(\frac\pi4+\frac\pi2\right)=\frac{3\pi}{8}.
\end{aligned}
$$

**反常积分的下限按极限处理。**

令 $t=(x+1)/2$，则 $\mathrm dx=2\,\mathrm dt$，并且

$$
x^2+2x+5=4(1+t^2).
$$

当上限为 $x=1$ 时，新的上限是 $t=1$。因此

$$
I=\frac12\lim_{M\to+\infty}\int_{-M}^{1}\frac{\mathrm dt}{1+t^2}
=\frac12\left[\arctan1-\lim_{M\to+\infty}\arctan(-M)\right].
$$

利用 $\arctan1=\pi/4$ 和 $\arctan(-\infty)=-\pi/2$，才得到 $3\pi/8$。该被积函数在实轴没有奇点，唯一的反常端点是负无穷。

**10.（2014.10）答案：$1$。**

奇函数满足 $f(0)=0$。由给出的导函数，

$$
f(1)=f(0)+\int_0^1 2(x-1)\,\mathrm dx=-1.
$$

再利用周期为4和奇性，

$$
f(7)=f(7-8)=f(-1)=-f(1)=1.
$$

**先确定导数已知区间上的函数。**

在 $0\le x\le2$ 上积分，可写成

$$
f(x)=x^2-2x+C.
$$

奇性给出 $f(0)=0$，所以 $C=0$。于是 $f(1)=-1$，并且 $f(2)=0$，与奇性和4周期性所要求的 $f(2)=f(-2)=-f(2)$ 相符。

计算 $f(7)$ 时要先把自变量移入已知区间或其对称区间：$7-2\times4=-1$，再用奇性转为 $x=1$。不能把只在 $[0,2]$ 上成立的多项式公式直接用于 $x=7$。

**11.（2014.11）答案：$-\dfrac12\mathrm dx-\dfrac12\mathrm dy$。**

在 $(x,y)=(1/2,1/2)$ 处，隐式方程化为

$$
\mathrm e^z+z=1.
$$

左边严格增加，且 $z=0$ 是解，因此对应值为0。

对原式取全微分：

$$
\mathrm e^{2yz}(2y\,\mathrm dz+2z\,\mathrm dy)
+\mathrm dx+2y\,\mathrm dy+\mathrm dz=0.
$$

代入 $x=y=1/2,z=0$，得到 $\mathrm dx+\mathrm dy+2\mathrm dz=0$，即所求结果。

**用隐函数偏导公式核对。**

令

$$
F(x,y,z)=\mathrm e^{2yz}+x+y^2+z-\frac74.
$$

把 $x,y,z$ 先看成独立变量，有

$$
F_x=1,\qquad F_y=2z\mathrm e^{2yz}+2y,
\qquad F_z=2y\mathrm e^{2yz}+1.
$$

在 $(1/2,1/2,0)$ 处，$F_z=2\ne0$，符合隐函数定理的局部条件。因此

$$
z_x=-\frac{F_x}{F_z}=-\frac12,
\qquad z_y=-\frac{F_y}{F_z}=-\frac12.
$$

最后以 $\mathrm dz=z_x\,\mathrm dx+z_y\,\mathrm dy$ 合成全微分，与直接微分结果一致。

**12.（2014.12）答案：$y=\dfrac\pi2-\dfrac2\pi x$。**

把极坐标方程改写为参数方程：

$$
x=\theta\cos\theta,\qquad y=\theta\sin\theta.
$$

在 $\theta=\pi/2$ 处，点坐标为 $(0,\pi/2)$，且

$$
x'=\cos\theta-\theta\sin\theta=-\frac\pi2,
\qquad y'=\sin\theta+\theta\cos\theta=1.
$$

所以切线斜率为 $-2/\pi$，切线方程即为所列直线。

**区分极坐标点与直角坐标点。**

给定的 $(\pi/2,\pi/2)$ 是 $(r,\theta)$，不能直接当作切点的 $(x,y)$。转换应为

$$
x_0=r\cos\theta=0,\qquad y_0=r\sin\theta=\frac\pi2.
$$

参数导数构成切向量 $(-\pi/2,1)$，其横分量不为零，故存在有限斜率。切线点斜式为

$$
y-\frac\pi2=-\frac2\pi(x-0).
$$

也可写成 $2x+\pi y=\pi^2/2$。该式经过 $(0,\pi/2)$，且切向量与法向量 $(2,\pi)$ 的内积为零。

**13.（2014.13）答案：$11/20$。**

细棒总质量为

$$
M=\int_0^1(-x^2+2x+1)\,\mathrm dx=\frac53.
$$

关于原点的一阶质量矩为

$$
M_1=\int_0^1x(-x^2+2x+1)\,\mathrm dx
=-\frac14+\frac23+\frac12=\frac{11}{12}.
$$

所以质心横坐标为

$$
\bar x=\frac{M_1}{M}=\frac{11/12}{5/3}=\frac{11}{20}.
$$

**质心公式从质量微元得到。**

位于 $x$ 附近的小段质量为 $\mathrm dm=\rho(x)\,\mathrm dx$，其对原点的质量矩为 $x\,\mathrm dm$，所以

$$
\bar x\int_0^1\rho(x)\,\mathrm dx
=\int_0^1x\rho(x)\,\mathrm dx.
$$

这里

$$
\int_0^1\rho(x)\,\mathrm dx
=\left[-\frac{x^3}{3}+x^2+x\right]_0^1=\frac53,
$$

$$
\int_0^1x\rho(x)\,\mathrm dx
=\left[-\frac{x^4}{4}+\frac{2x^3}{3}+\frac{x^2}{2}\right]_0^1
=\frac{11}{12}.
$$

线密度 $\rho'(x)=2-2x\ge0$，右侧更重，结果 $11/20>1/2$ 也符合这一分布特征。

**14.（2014.14）答案：$-2\le a\le2$。**

直接配方：

$$
f=(x_1+ax_3)^2-(x_2-2x_3)^2+(4-a^2)x_3^2.
$$

对应的变量替换是可逆的，因此不改变惯性指数。

第一个平方项恒为正号，第二个恒为负号。要使负惯性指数恰好为1，第三项系数不能为负，故

$$
4-a^2\ge0.
$$

所以 $a\in[-2,2]$。端点时第三项为零，负惯性指数仍为1，故端点应包含。

**说明配方变换的可逆性，并列全参数情形。**

取新变量

$$
u=x_1+ax_3,\qquad v=x_2-2x_3,\qquad w=x_3.
$$

其变换矩阵为上三角矩阵，对角元均为1，行列式为1，任意 $a$ 下都可逆。反变换为 $x_1=u-aw,x_2=v+2w,x_3=w$。

新形式是 $u^2-v^2+(4-a^2)w^2$。若 $|a|<2$，正、负惯性指数分别为2、1；若 $|a|=2$，分别为1、1，并有一个零平方项；若 $|a|>2$，分别为1、2。因此题目只限制负惯性指数为1时，必须把 $a=\pm2$ 包含在内。

## 三、解答题（15～23）

**15.（2014.15）答案：$1/2$。**

先展开分子被积函数在无穷远的行为：

$$
\mathrm e^{1/t}=1+\frac1t+\frac1{2t^2}+O(t^{-3}),
$$

因此

$$
t^2(\mathrm e^{1/t}-1)-t=\frac12+O(t^{-1})\longrightarrow\frac12.
$$

所以分子积分趋于正无穷；分母 $x^2\ln(1+1/x)\sim x$ 也趋于正无穷，可以使用洛必达法则。

分母求导时，

$$
\frac{\mathrm d}{\mathrm dx}\left[x^2\ln(1+1/x)\right]
=2x\ln(1+1/x)-\frac{x}{x+1}\longrightarrow1.
$$

于是原极限等于

$$
\lim_{x\to+\infty}
\frac{x^2(\mathrm e^{1/x}-1)-x}{2x\ln(1+1/x)-x/(x+1)}
=\frac12.
$$

**分子为什么确实趋于无穷。**

记 $h(t)=t^2(\mathrm e^{1/t}-1)-t$。由 $h(t)\to1/2$，存在 $T>1$，使 $t\ge T$ 时 $h(t)>1/4$，于是

$$
\int_1^xh(t)\,\mathrm dt
\ge\int_1^T h(t)\,\mathrm dt+\frac{x-T}{4}\to+\infty.
$$

因此前面的洛必达法则不是只凭分母无穷就使用，而是已经确认 $\infty/\infty$ 型。分母的导数趋于1，故在充分大的区间内也不为零。

**另一种展开：先看积分的平均增长率。**

余项 $h(t)=1/2+O(1/t)$ 积分后给出

$$
\int_1^x h(t)\,\mathrm dt=\frac{x-1}{2}+O(\ln x).
$$

另一方面，令 $s=1/x\to0^+$，有

$$
x^2\ln(1+1/x)=x-\frac12+O(1/x).
$$

因此分子、分母同时除以 $x$：

$$
\frac{\frac12-\frac1{2x}+O(\ln x/x)}
{1-\frac1{2x}+O(1/x^2)}\to\frac12.
$$

这里必须保留指数展开中的二次项，因为一次项会与题目中的 $-t$ 抵消。

**16.（2014.16）答案：极小值为0，极大值为1。**

**第一步：积分并使用初始条件。**

整理方程，得

$$
(1+y^2)y'=1-x^2.
$$

因此

$$
y+\frac{y^3}{3}=x-\frac{x^3}{3}+C.
$$

由 $y(2)=0$，得 $C=2/3$，即

$$
y^3+3y=3x-x^3+2.
$$

左边关于 $y$ 严格增加，所以对每个 $x$ 都唯一确定 $y$。

**第二步：由导数符号判断极值。**

$$
y'=\frac{1-x^2}{1+y^2}.
$$

分母恒正。因此函数在 $(-\infty,-1)$ 上减少，在 $(-1,1)$ 上增加，在 $(1,+\infty)$ 上减少。

所以 $x=-1$ 为极小值点，$x=1$ 为极大值点。

当 $x=-1$ 时，$y^3+3y=0$，得 $y=0$；当 $x=1$ 时，$y^3+3y=4$，得 $y=1$。

故极小值、极大值分别为0、1。

**为什么可以只用隐式关系求极值。**

设 $H(y)=y^3+3y$，则 $H'(y)=3y^2+3>0$，并且 $H(y)\to\pm\infty$ 当 $y\to\pm\infty$。所以每个实数右端都对应唯一的实数 $y$，无需写出复杂的三次方程根式。

初值代入常数的过程是

$$
0=2-\frac83+C,
\qquad C=\frac23.
$$

驻点由 $1-x^2=0$ 给出，只有 $x=\pm1$。在 $x=-1$ 处，方程成为

$$
y^3+3y=y(y^2+3)=0,
$$

唯一实根为0。在 $x=1$ 处，

$$
y^3+3y-4=(y-1)(y^2+y+4)=0.
$$

后一个二次因子的判别式为 $1-16<0$，唯一实根为1。

还可对 $(1+y^2)y'=1-x^2$ 求导：

$$
(1+y^2)y''+2y(y')^2=-2x.
$$

在驻点处 $y'=0$，所以 $y''(-1)=2>0$、$y''(1)=-1<0$，分别验证极小与极大。这里求的是局部极值，不能由“极大值为1”推断函数在整条实轴上都不超过1。

**17.（2014.17）答案：$-3/4$。**

**第一步：用对称性消去分式中的方向因子。**

区域在交换 $x,y$ 后不变，所以

$$
I=\iint_D\frac{x\sin(\pi\sqrt{x^2+y^2})}{x+y}\,\mathrm dx\mathrm dy
=\iint_D\frac{y\sin(\pi\sqrt{x^2+y^2})}{x+y}\,\mathrm dx\mathrm dy.
$$

两式相加，得

$$
2I=\iint_D\sin(\pi\sqrt{x^2+y^2})\,\mathrm dx\mathrm dy.
$$

**第二步：改用极坐标。**

区域为第一象限的圆环部分：$1\le r\le2$、$0\le\theta\le\pi/2$。因此

$$
I=\frac\pi4\int_1^2 r\sin\pi r\,\mathrm dr.
$$

**第三步：计算径向积分。**

$$
\int_1^2r\sin\pi r\,\mathrm dr
=\left[-\frac{r\cos\pi r}{\pi}+\frac{\sin\pi r}{\pi^2}\right]_1^2
=-\frac3\pi.
$$

所以 $I=(\pi/4)(-3/\pi)=-3/4$。

**直接使用极坐标也能分离两个积分。**

令 $x=r\cos\theta,y=r\sin\theta$，面积微元为 $r\,\mathrm dr\,\mathrm d\theta$。原积分成为

$$
I=\left(\int_0^{\pi/2}\frac{\cos\theta}{\cos\theta+\sin\theta}\,\mathrm d\theta\right)
\left(\int_1^2r\sin(\pi r)\,\mathrm dr\right).
$$

记第一个积分为 $J$，用 $\theta=\pi/2-t$ 得

$$
J=\int_0^{\pi/2}\frac{\sin t}{\sin t+\cos t}\,\mathrm dt.
$$

两式相加即 $2J=\int_0^{\pi/2}1\,\mathrm dt=\pi/2$，所以 $J=\pi/4$。这正是前面交换 $x,y$ 对称性的角度形式。

径向积分分部时，取 $u=r$、$\mathrm dv=\sin(\pi r)\,\mathrm dr$，得

$$
\int_1^2r\sin(\pi r)\,\mathrm dr
=\left[-\frac{r\cos(\pi r)}\pi\right]_1^2
+\frac1\pi\int_1^2\cos(\pi r)\,\mathrm dr
=-\frac2\pi-\frac1\pi.
$$

在 $1<r<2$ 上 $\sin(\pi r)<0$，且原分式的另一个因子非负，因此积分为负，也与 $-3/4$ 一致。

**18.（2014.18）答案：$f(u)=\dfrac{\mathrm e^{2u}-\mathrm e^{-2u}}{16}-\dfrac u4$。**

**第一步：利用复合结构化为常微分方程。**

令 $u=\mathrm e^x\cos y$，则

$$
u_x=u,\quad u_{xx}=u,\quad
u_y=-\mathrm e^x\sin y,\quad u_{yy}=-u.
$$

所以

$$
z_{xx}=f''(u)u^2+f'(u)u,
$$

$$
z_{yy}=f''(u)\mathrm e^{2x}\sin^2y-f'(u)u.
$$

相加后，一阶导数项抵消，得到

$$
z_{xx}+z_{yy}=\mathrm e^{2x}f''(u).
$$

与题设右端比较，约去 $\mathrm e^{2x}$，得

$$
f''(u)-4f(u)=u.
$$

**第二步：求通解。**

对应齐次方程特征根为 $\pm2$，右端为一次多项式，取特解 $-u/4$，故

$$
f(u)=C_1\mathrm e^{2u}+C_2\mathrm e^{-2u}-\frac u4.
$$

**第三步：代入两个初始条件。**

$$
C_1+C_2=0,
\qquad 2C_1-2C_2-\frac14=0.
$$

解得 $C_1=1/16,C_2=-1/16$，即为所求函数。代入可检验 $f(0)=f'(0)=0$ 且 $f''-4f=u$。

**二阶链式法则逐项计算。**

先求一阶偏导：

$$
z_x=f'(u)\mathrm e^x\cos y,
\qquad z_y=-f'(u)\mathrm e^x\sin y.
$$

再分别求导，注意 $f'(u)$ 自身也要经 $u$ 求导：

$$
z_{xx}=f''(u)\mathrm e^{2x}\cos^2y+f'(u)\mathrm e^x\cos y,
$$

$$
z_{yy}=f''(u)\mathrm e^{2x}\sin^2y-f'(u)\mathrm e^x\cos y.
$$

相加用 $\sin^2y+\cos^2y=1$，才消去角度和一阶项。

**非齐次特解的系数如何确定。**

对 $f''-4f=u$，设 $f_p=Au+B$，则 $f_p''=0$。比较

$$
-4Au-4B=u
$$

的系数，得到 $A=-1/4,B=0$。齐次部分的特征方程为 $r^2-4=0$，根 $2,-2$ 不重复，所以两项均是纯指数。

对最终表达式求导：

$$
f'(u)=\frac{\mathrm e^{2u}+\mathrm e^{-2u}}8-\frac14,
\qquad
f''(u)=\frac{\mathrm e^{2u}-\mathrm e^{-2u}}4.
$$

于是 $f'(0)=0$，且

$$
f''(u)-4f(u)=u.
$$

这同时核对了常数、一次项的符号和原偏微分方程。

**19.（2014.19）积分不等式的证明。**

**（Ⅰ）直接积分已知界。**

因为 $0\le g(t)\le1$，当 $a\le x\le b$ 时，

$$
0\le\int_a^xg(t)\,\mathrm dt\le\int_a^x1\,\mathrm dt=x-a.
$$

第一问得证。

**（Ⅱ）将区间分成两段。**

记

$$
h=\int_a^b g(t)\,\mathrm dt,\qquad c=a+h.
$$

由第一问，$0\le h\le b-a$，所以 $c\in[a,b]$。

考察待证不等式右边减左边：

$$
\begin{aligned}
R&=\int_a^b f(x)g(x)\,\mathrm dx-\int_a^c f(x)\,\mathrm dx\\
&=-\int_a^c f(x)[1-g(x)]\,\mathrm dx+\int_c^b f(x)g(x)\,\mathrm dx.
\end{aligned}
$$

在 $[a,c]$ 上，$f(x)\le f(c)$、$1-g(x)\ge0$；在 $[c,b]$ 上，$f(x)\ge f(c)$、$g(x)\ge0$。因此

$$
\begin{aligned}
R&\ge-f(c)\int_a^c[1-g(x)]\,\mathrm dx+f(c)\int_c^b g(x)\,\mathrm dx\\
&=f(c)\left[\int_a^b g(x)\,\mathrm dx-(c-a)\right]=0.
\end{aligned}
$$

最后一步用了 $c-a=h=\int_a^b g$。所以右边不小于左边，结论成立。

**把比较式整理成两个非负积分。**

构造分点 $c$ 的目的，是使左端区间长度等于 $g$ 的总积分：

$$
c-a=\int_a^b g(x)\,\mathrm dx.
$$

这等价于两侧权重相等：

$$
\int_a^c[1-g(x)]\,\mathrm dx=\int_c^b g(x)\,\mathrm dx.
$$

在差值 $R$ 中加减同一个常数 $f(c)$ 后，可精确写为

$$
\begin{aligned}
R={}&\int_a^c[f(c)-f(x)][1-g(x)]\,\mathrm dx\\
&+\int_c^b[f(x)-f(c)]g(x)\,\mathrm dx.
\end{aligned}
$$

第一段中 $f(c)-f(x)\ge0$、$1-g(x)\ge0$；第二段中 $f(x)-f(c)\ge0$、$g(x)\ge0$。两项各自非负，故 $R\ge0$。

这种写法也说明 $f$ 本身可以取负值，无需附加 $f\ge0$。全程只用连续性、单调性和积分保序性，没有要求 $f'$ 存在。若 $c=a$ 或 $c=b$，相应的零长度积分为零，公式同样有效。

**20.（2014.20）答案：$1$。**

**第一步：求迭代函数的显式表达式。**

先有 $f_1(x)=x/(1+x)$。若 $f_n(x)=x/(1+nx)$，则

$$
f_{n+1}(x)=\frac{f_n(x)}{1+f_n(x)}
=\frac{x}{1+(n+1)x}.
$$

故数学归纳法给出

$$
f_n(x)=\frac{x}{1+nx}.
$$

**第二步：计算面积。**

函数在 $[0,1]$ 上非负，所以

$$
\begin{aligned}
S_n&=\int_0^1\frac{x}{1+nx}\,\mathrm dx\\
&=\int_0^1\left[\frac1n-\frac1{n(1+nx)}\right]\mathrm dx\\
&=\frac1n-\frac{\ln(n+1)}{n^2}.
\end{aligned}
$$

因此

$$
nS_n=1-\frac{\ln(n+1)}n\longrightarrow1.
$$

**迭代公式的发现过程。**

对 $x>0$，取倒数后迭代关系变成加法：

$$
\frac1{f_{n+1}(x)}
=\frac{1+f_n(x)}{f_n(x)}
=\frac1{f_n(x)}+1.
$$

又 $1/f_1(x)=1/x+1$，逐次累加便得到

$$
\frac1{f_n(x)}=\frac1x+n,
\qquad f_n(x)=\frac{x}{1+nx}.
$$

$x=0$ 时所有迭代值为0，显式公式也成立。且 $f$ 把 $[0,1]$ 映到 $[0,1/2]$，所以迭代中的每一步都在定义域内。

**最后的数列极限如何验证。**

面积计算中的分式分解来自

$$
x=\frac{(1+nx)-1}{n}.
$$

因 $n$ 对积分变量 $x$ 是常数，

$$
\int_0^1\frac{\mathrm dx}{1+nx}
=\frac1n\bigl[\ln(1+nx)\bigr]_0^1
=\frac{\ln(n+1)}n.
$$

又可令实变量 $t\to+\infty$，应用洛必达法则证明

$$
\lim_{t\to+\infty}\frac{\ln(t+1)}t
=\lim_{t\to+\infty}\frac1{t+1}=0.
$$

因此相应整数子列也趋于零，$nS_n\to1$。

**21.（2014.21）答案：$V=\pi(2\ln2-5/4)$。**

**第一步：恢复二元函数。**

由 $f_y=2(y+1)$，可得

$$
f(x,y)=(y+1)^2+h(x).
$$

代入对角线条件，得到

$$
h(x)=-(2-x)\ln x,\qquad x>0.
$$

所以曲线方程为

$$
(y+1)^2=(2-x)\ln x.
$$

**第二步：确定封闭区域范围。**

右边必须非负。对 $x>0$ 分析 $2-x$ 与 $\ln x$ 的符号，可知恰好需要 $1\le x\le2$。

两条边界为

$$
y=-1\pm\sqrt{(2-x)\ln x},
$$

它们在两端相接，关于旋转轴 $y=-1$ 对称。

**第三步：计算圆盘体积。**

固定 $x$ 后，截线绕旋转轴成为半径 $\sqrt{(2-x)\ln x}$ 的圆盘，因此

$$
V=\pi\int_1^2(2-x)\ln x\,\mathrm dx.
$$

分别积分两项：

$$
\int_1^2 2\ln x\,\mathrm dx=4\ln2-2,
$$

$$
\int_1^2 x\ln x\,\mathrm dx=2\ln2-\frac34.
$$

所以

$$
V=\pi\left[(4\ln2-2)-(2\ln2-3/4)\right]
=\pi\left(2\ln2-\frac54\right).
$$

**积分常数为什么是一个函数。**

由 $f_y=2(y+1)$ 对 $y$ 积分时，$x$ 被视为常量，所以“积分常数”可以随 $x$ 改变，写成 $h(x)$。把第二个自变量也取为 $x$，题设条件给出

$$
(x+1)^2+h(x)=(x+1)^2-(2-x)\ln x,
$$

于是准确确定了 $h(x)$。

**界限和圆盘半径的确定。**

在 $0<x<1$ 上，$2-x>0,\ln x<0$；在 $1<x<2$ 上，两者都正；在 $x>2$ 上，前者负、后者正。因此仅 $[1,2]$ 能使 $(y+1)^2\ge0$。固定该区间内的 $x$，图形截线包含旋转轴 $y=-1$，旋转截面是实心圆盘，没有需要扣除的内孔。

**将两个定积分的分部过程展开。**

由分部积分，

$$
\int\ln x\,\mathrm dx=x\ln x-x+C,
$$

$$
\int x\ln x\,\mathrm dx
=\frac{x^2}{2}\ln x-\frac12\int x\,\mathrm dx
=\frac{x^2}{2}\ln x-\frac{x^2}{4}+C.
$$

所以总原函数可取

$$
F(x)=\left(2x-\frac{x^2}{2}\right)\ln x-2x+\frac{x^2}{4}.
$$

端点值分别为 $F(2)=2\ln2-3$、$F(1)=-7/4$，相减得 $2\ln2-5/4$，最后乘以 $\pi$。

**22.（2014.22）基础解系及所有右逆矩阵。**

**（Ⅰ）对齐次方程作消元。**

先作 $R_3\leftarrow R_3-R_1$，再用第2行消元，得到等价方程

$$
x_3-3x_4=0,\qquad x_2-x_3+x_4=0,
\qquad x_1-2x_2+3x_3-4x_4=0.
$$

依次回代，得

$$
x_3=3x_4,\qquad x_2=2x_4,\qquad x_1=-x_4.
$$

因此基础解系可取单个向量

$$
n=\begin{pmatrix}-1\\2\\3\\1\end{pmatrix}.
$$

**（Ⅱ）先求一个右逆矩阵。**

要求 $AB=I_3$，等价于 $B$ 的三列分别满足 $A b_j=e_j$。

对一般右端 $b=(b_1,b_2,b_3)^{\mathrm T}$，同样的消元给出

$$
\boldsymbol x=
\begin{pmatrix}2b_1+6b_2-b_3\\-b_1-3b_2+b_3\\-b_1-4b_2+b_3\\0\end{pmatrix}
+t\begin{pmatrix}-1\\2\\3\\1\end{pmatrix}.
$$

分别取右端 $e_1,e_2,e_3$，且都取 $t=0$，得到一个特解矩阵

$$
B_0=\begin{pmatrix}2&6&-1\\-1&-3&1\\-1&-4&1\\0&0&0\end{pmatrix}.
$$

直接相乘可验 $AB_0=I_3$。

**最后给出全部解。**

任意另一个解 $B$ 都满足 $A(B-B_0)=0$，因此 $B-B_0$ 的每一列都必须是齐次基础向量 $n$ 的倍数。

故所有矩阵恰为

$$
B=B_0+
\begin{pmatrix}-1\\2\\3\\1\end{pmatrix}
\begin{pmatrix}c_1&c_2&c_3\end{pmatrix},
\qquad c_1,c_2,c_3\in\mathbb R.
$$

反过来，因为 $An=0$，上述任意参数所给矩阵都满足 $AB=I_3$，所以没有遗漏。

**把一般右端的行变换完整写出。**

将右端记为 $\beta=(\beta_1,\beta_2,\beta_3)^{\mathrm T}$，以免与所求矩阵的列混淆。增广矩阵为

$$
\left(\begin{array}{rrrr|r}
1&-2&3&-4&\beta_1\\
0&1&-1&1&\beta_2\\
1&2&0&-3&\beta_3
\end{array}\right).
$$

先作 $R_3\leftarrow R_3-R_1$，再作 $R_3\leftarrow R_3-4R_2$，得

$$
\left(\begin{array}{rrrr|r}
1&-2&3&-4&\beta_1\\
0&1&-1&1&\beta_2\\
0&0&1&-3&\beta_3-\beta_1-4\beta_2
\end{array}\right).
$$

第三行给出 $x_3=3x_4+\beta_3-\beta_1-4\beta_2$。回代第二行：

$$
x_2=\beta_2+x_3-x_4
=2x_4+\beta_3-\beta_1-3\beta_2.
$$

再回代第一行：

$$
x_1=\beta_1+2x_2-3x_3+4x_4
=-x_4+2\beta_1+6\beta_2-\beta_3.
$$

三个主元说明 $r(A)=3$，故任何三维右端都有解；自由变量只有 $x_4$，齐次解空间维数为 $4-3=1$。

**所有右逆按参数逐项写成矩阵。**

由三个列向量可以分别选取自由参数，得到

$$
B=\begin{pmatrix}
2-c_1&6-c_2&-1-c_3\\
-1+2c_1&-3+2c_2&1+2c_3\\
-1+3c_1&-4+3c_2&1+3c_3\\
c_1&c_2&c_3
\end{pmatrix}.
$$

三个参数彼此独立，不能强迫它们相等。$A$ 是 $3\times4$ 矩阵，所以 $B$ 必须是 $4\times3$ 矩阵；这里的“右逆”是 $AB=I_3$，并不要求 $BA=I_4$。

**23.（2014.23）两矩阵相似的证明。**

记全1矩阵为 $A$，另一个矩阵为 $B$，并令

$$
u=(1,1,\ldots,1)^{\mathrm T},\qquad
v=(1,2,\ldots,n)^{\mathrm T}.
$$

**第一步：为 $A$ 构造特征向量基。**

显然 $Au=nu$。对标准单位向量 $e_i$，有

$$
A(e_i-e_n)=0,\qquad i=1,\ldots,n-1.
$$

这些 $n-1$ 个向量线性无关，且与非零特征值对应的 $u$ 一起构成一组基。因此 $A$ 相似于 $\operatorname{diag}(n,0,\ldots,0)$。

**第二步：为 $B$ 构造同样的对角形式。**

$B$ 只有最后一列非零，可写为

$$
B=v e_n^{\mathrm T}.
$$

于是

$$
Bv=nv,
\qquad Be_i=0\quad(i=1,\ldots,n-1).
$$

$v$ 的最后一个分量为 $n\ne0$，而 $e_1,\ldots,e_{n-1}$ 的最后分量均为零，所以这 $n$ 个向量也线性无关。

因此 $B$ 同样相似于 $\operatorname{diag}(n,0,\ldots,0)$。由相似关系的传递性，$A$ 与 $B$ 相似。

**线性无关性的系数证明。**

对第一组特征向量，假设

$$
a_0u+\sum_{i=1}^{n-1}a_i(e_i-e_n)=0.
$$

把所有分量相加，第二项中每个向量的分量和均为零，得 $na_0=0$，故 $a_0=0$；再看前 $n-1$ 个分量，得各 $a_i=0$。因此它们确实构成基。

第二组若满足 $b_0v+\sum_{i=1}^{n-1}b_ie_i=0$，先看最后分量便得 $nb_0=0$，再逐分量得到其余系数为零。

**把相似矩阵写成可验证的形式。**

令

$$
P=(u,e_1-e_n,\ldots,e_{n-1}-e_n),
$$

$$
Q=(v,e_1,\ldots,e_{n-1}),
\qquad \Lambda=\operatorname{diag}(n,0,\ldots,0).
$$

上述基的证明保证 $P,Q$ 可逆，且

$$
AP=P\Lambda,\qquad BQ=Q\Lambda.
$$

取 $S=PQ^{-1}$，直接计算可得

$$
S^{-1}AS=QP^{-1}APQ^{-1}=Q\Lambda Q^{-1}=B.
$$

这给出了相似关系的具体构造。$n=1$ 时两矩阵同为 $(1)$，结论也成立。
