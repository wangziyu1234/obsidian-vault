# 2003年考研数学二答案与解析

> 我的解答，未与官方核对。

## 答案速查

> 填空题（原卷一）：1.$-4$；2.$y=x$；3.$(\ln2)^n/n!$；4.$(\mathrm e^{4\pi a}-1)/(4a)$；5.$3$；6.$1/2$。

> 选择题（原卷二）：1.D　2.B　3.A　4.C　5.B　6.D。

三（2003.13）：$a=-1$ 时连续；$a=-2$ 时为可去间断点。

四（2003.14）：$y''(9)=-\mathrm e/[16(1+2\ln2)^2]$。

五（2003.15）：原函数为 $\dfrac{(x-1)\mathrm e^{\arctan x}}{2\sqrt{1+x^2}}+C$。

六（2003.16）：$y''-y=\sin x$，所求解为 $y=\mathrm e^x-\mathrm e^{-x}-\tfrac12\sin x$。

七（2003.17）：$k<4$ 时无交点，$k=4$ 时1个，$k>4$ 时2个。

八（2003.18）：$x^2+2y^2=1$ 的第一象限部分；$s=l/(2\sqrt2)$。

九（2003.19）：$t=\varphi(y)^2-4$，$x=2\mathrm e^{\pi y/6}$。

十（2003.20）：三个证明见正文。

十一（2003.21）：$a=0$，相似变换矩阵见正文。

十二（2003.22）：充要条件为 $a+b+c=0$，三条直线共同经过 $(3,3/2)$。

## 一、填空题（原卷一，习题册1～6）

**1.（2003.1；原卷一·1）答案：$a=-4$。**

等价无穷小要求两式的比值趋于1。

由二项式展开，

$$
(1-ax^2)^{1/4}-1=-\frac a4x^2+O(x^4).
$$

而 $x\sin x=x^2+O(x^4)$，所以两式之比趋于 $-a/4$。

令 $-a/4=1$，即得 $a=-4$。将此值代入后，分子主项为 $x^2$，与分母一致。

**2.（2003.2；原卷一·2）答案：$y=x$。**

对原隐式方程求导时，$xy$ 用乘积法则，$y^4$ 用链式法则：

$$
y+xy'+\frac2x=4y^3y'.
$$

移项得到

$$
y'=\frac{y+2/x}{4y^3-x}.
$$

在 $(x,y)=(1,1)$ 处，分母为3，不为零，且 $y'=3/3=1$。

所以通过 $(1,1)$ 的切线为 $y-1=x-1$，即 $y=x$。

**3.（2003.3；原卷一·3）答案：$(\ln2)^n/n!$。**

写成 $2^x=\mathrm e^{x\ln2}$，将指数函数的麦克劳林级数代入：

$$
2^x=\sum_{n=0}^{\infty}\frac{(\ln2)^n}{n!}x^n.
$$

所以所求系数为 $(\ln2)^n/n!$。题目问的是系数，并非第 $n$ 阶导数，因此必须保留分母 $n!$。

**4.（2003.4；原卷一·4）答案：$\dfrac{\mathrm e^{4\pi a}-1}{4a}$。**

由极坐标扇形微元，面积元为 $\frac12\rho^2\mathrm d\theta$。在一周内，曲线与极轴形成封闭边界，因此

$$
S=\frac12\int_0^{2\pi}\mathrm e^{2a\theta}\,\mathrm d\theta.
$$

$a>0$，积分得

$$
S=\frac1{4a}[\mathrm e^{2a\theta}]_0^{2\pi}
=\frac{\mathrm e^{4\pi a}-1}{4a}.
$$

这里指数变为 $2a\theta$，来自半径的平方，不能直接对半径积分。

**5.（2003.5；原卷一·5）答案：$3$。**

设 $\alpha=(a_1,a_2,a_3)^{\mathrm T}$。外积矩阵 $\alpha\alpha^{\mathrm T}$ 的主对角线元素依次为 $a_1^2,a_2^2,a_3^2$，题中三者均为1。

所以

$$
\alpha^{\mathrm T}\alpha=a_1^2+a_2^2+a_3^2
=\operatorname{tr}(\alpha\alpha^{\mathrm T})=3.
$$

无需确定各分量的正负号即可求出这一标量。

**6.（2003.6；原卷一·6）答案：$1/2$。**

先整理矩阵等式：

$$
(A^2-I)B=A+I,
\qquad (A+I)(A-I)B=A+I.
$$

要约去 $A+I$，先检查可逆性。对给定矩阵，有

$$
|A+I|=\begin{vmatrix}2&0&1\\0&3&0\\-2&0&2\end{vmatrix}=18\ne0.
$$

所以左乘 $(A+I)^{-1}$，得到 $(A-I)B=I$，即 $B=(A-I)^{-1}$。

再计算

$$
|A-I|=\begin{vmatrix}0&0&1\\0&1&0\\-2&0&0\end{vmatrix}=2,
$$

故 $|B|=1/|A-I|=1/2$。

## 二、选择题（原卷二，习题册7～12）

**7.（2003.7；原卷二·1）答案：D。**

由 $b_n\to1$，存在 $N$，使 $n>N$ 时 $b_n>1/2$。由于 $c_n\to+\infty$，有

$$
b_nc_n\ge\frac12c_n\longrightarrow+\infty.
$$

因此乘积没有有限极限，D 成立。

A、B 把“充分大的 $n$”写成了“任意 $n$”，极限条件不能控制最初有限项，所以不一定成立。

C 也不成立：取 $a_n=1/n$、$c_n=n$，它们满足题设，但乘积恒为1，有限极限存在。

**8.（2003.8；原卷二·2）答案：B。**

**第一步：先准确计算含参积分。**

令 $u=1+x^n$，则 $\mathrm du=nx^{n-1}\mathrm dx$。因此

$$
\begin{aligned}
na_n
&=\frac32\int_1^{1+(n/(n+1))^n}\sqrt u\,\mathrm du\\
&=\left[u^{3/2}\right]_1^{1+(n/(n+1))^n}\\
&=\left[1+\left(\frac n{n+1}\right)^n\right]^{3/2}-1.
\end{aligned}
$$

**第二步：计算底数中的极限。**

$$
\left(\frac n{n+1}\right)^n
=\left(1+\frac1n\right)^{-n}\longrightarrow\mathrm e^{-1}.
$$

由连续性，最终极限为 $(1+\mathrm e^{-1})^{3/2}-1$，对应 B。

**9.（2003.9；原卷二·3）答案：A。**

对已知解 $y=x/\ln x$ 求导，得到

$$
y'=\frac{\ln x-1}{\ln^2x}
=\frac1{\ln x}-\frac1{\ln^2x}.
$$

其中 $y/x=1/\ln x$，所以微分方程要求

$$
\varphi(x/y)=y'-\frac yx=-\frac1{\ln^2x}=-\frac{y^2}{x^2}.
$$

注意外函数的自变量为 $x/y=\ln x$，因此 $\varphi(u)=-1/u^2$，代回同样得到 A。

**10.（2003.10；原卷二·4）答案：C，两个极小值点和两个极大值点。**

判断原函数的极值，关键是按横坐标从左到右，逐次观察导函数的符号变化。

图中左侧抛物线第一次穿过 $x$ 轴时，导数由正变负，对应一个极大值点；第二次穿过时由负变正，对应一个极小值点。

到竖直渐近线对应位置，导数左侧为正、右侧为负。原函数在该点连续，因此左增右减，再产生一个极大值点。

右支穿过 $x$ 轴时，导数由负变正，再产生一个极小值点。

合计两个极大值点和两个极小值点，故选 C。导函数在某点不存在，并不自动排除原函数在那里取极值。

**11.（2003.11；原卷二·5）答案：B，$1>I_1>I_2$。**

**先比较两个积分。** 对 $0<x<\pi/4$，$\tan x>x>0$，因此

$$
\frac{\tan x}{x}>1>\frac{x}{\tan x}.
$$

在同一区间上积分，得到 $I_1>I_2$。

**再给 $I_1$ 找出小于1的上界。**

在 $[0,\pi/4]$ 上，$\tan x$ 的图形严格位于两端连线 $y=4x/\pi$ 的下方，所以内部有

$$
\frac{\tan x}{x}<\frac4\pi.
$$

于是

$$
I_1<\int_0^{\pi/4}\frac4\pi\,\mathrm dx=1.
$$

两个不等式合并就是 $1>I_1>I_2$。零点处的比值均可按极限补为1，不影响积分。

**12.（2003.12；原卷二·6）答案：D。**

向量组Ⅰ可由向量组Ⅱ线性表示，因此它所张成的空间维数不超过Ⅱ的向量个数 $s$：

$$
r(\mathrm I)\le r(\mathrm {II})\le s.
$$

如果Ⅰ有 $r>s$ 个向量，它们却位于至多 $s$ 维的空间中，必线性相关，所以 D 成立。

其余选项均不必成立。例如让Ⅱ取标准基，Ⅰ取其中一个向量，便可使 $r<s$ 而两组都独立，排除 A、C；让Ⅰ含重复的标准基向量而Ⅱ仍为基，则 $r>s$ 也不迫使Ⅱ相关，排除 B。

## 三、解答题（原卷三～十二，习题册13～22）

**13.（2003.13；原卷三）答案：$a=-1$ 时连续，$a=-2$ 时为可去间断点。**

**第一步：分别求两个单侧极限。**

左侧分子、分母都从三次项开始：

$$
\ln(1+ax^3)=ax^3+O(x^6),
\qquad x-\arcsin x=-\frac{x^3}{6}+O(x^5).
$$

所以左极限为 $L_-=-6a$。

右侧指数展开为 $\mathrm e^{ax}=1+ax+a^2x^2/2+O(x^3)$，与其他项抵消后，分子为 $(a^2/2+1)x^2+O(x^3)$。

又 $x\sin(x/4)=x^2/4+O(x^4)$，因此右极限为

$$
L_+=4\left(\frac{a^2}{2}+1\right)=2a^2+4.
$$

**第二步：令两侧极限相等。**

$$
-6a=2a^2+4
\quad\Longleftrightarrow\quad
2(a+1)(a+2)=0.
$$

所以只有 $a=-1$、$a=-2$ 时，双侧有限极限存在。

**第三步：与零点函数值比较。**

$a=-1$ 时，双侧极限为6，等于 $f(0)=6$，因此连续。

$a=-2$ 时，双侧极限为12，与 $f(0)=6$ 不同，因此零点是可去间断点。

其他参数的左右极限不相等，既不连续，也不是可去间断点。

**14.（2003.14；原卷四）答案：$-\dfrac{\mathrm e}{16(1+2\ln2)^2}$。**

**第一步：先确定参数值。**

由 $x=1+2t^2=9$，得 $t^2=4$。结合 $t>1$，取 $t=2$。

**第二步：分别求 $y_t$ 与 $x_t$。**

变上限积分求导要乘以上限的导数：

$$
\frac{\mathrm dy}{\mathrm dt}
=\frac{\mathrm e^{1+2\ln t}}{1+2\ln t}\cdot\frac2t
=\frac{2\mathrm e t}{1+2\ln t}.
$$

另一方面，$\mathrm dx/\mathrm dt=4t$，因此

$$
\frac{\mathrm dy}{\mathrm dx}=\frac{\mathrm e}{2(1+2\ln t)}.
$$

**第三步：再次对参数求导，再除以 $x_t$。**

$$
\frac{\mathrm d}{\mathrm dt}\left[\frac{\mathrm e}{2(1+2\ln t)}\right]
=-\frac{\mathrm e}{t(1+2\ln t)^2}.
$$

所以

$$
\frac{\mathrm d^2y}{\mathrm dx^2}
=-\frac{\mathrm e}{4t^2(1+2\ln t)^2}.
$$

代入 $t=2$，即得答案。二阶导数每多求一次，都需要除以 $\mathrm dx/\mathrm dt$。

**15.（2003.15；原卷五）不定积分。**

**第一步：用反正切换元消去根式。**

令 $t=\arctan x$，则 $x=\tan t$，$-\pi/2<t<\pi/2$，$\cos t>0$。

有

$$
\mathrm dx=\sec^2t\,\mathrm dt,
\qquad(1+x^2)^{3/2}=\sec^3t.
$$

因此原积分成为

$$
I=\int\mathrm e^t\sin t\,\mathrm dt.
$$

**第二步：连续两次分部积分。**

令 $J=\int\mathrm e^t\cos t\,\mathrm dt$，则

$$
I=\mathrm e^t\sin t-J,
\qquad J=\mathrm e^t\cos t+I.
$$

将第二式代入第一式，得到 $2I=\mathrm e^t(\sin t-\cos t)$。

**第三步：还原原变量。**

$$
\sin t=\frac{x}{\sqrt{1+x^2}},\qquad
\cos t=\frac1{\sqrt{1+x^2}}.
$$

所以

$$
I=\frac{(x-1)\mathrm e^{\arctan x}}{2\sqrt{1+x^2}}+C.
$$

**16.（2003.16；原卷六）答案：$y''-y=\sin x$，$y=\mathrm e^x-\mathrm e^{-x}-\tfrac12\sin x$。**

**（Ⅰ）第一步：推导反函数导数关系。**

因为 $y'\ne0$，反函数的一阶导数为

$$
\frac{\mathrm dx}{\mathrm dy}=\frac1{y'}.
$$

再次对 $y$ 求导，要将 $\mathrm d/\mathrm dy$ 换成 $(1/y')\mathrm d/\mathrm dx$，所以

$$
\frac{\mathrm d^2x}{\mathrm dy^2}
=\frac1{y'}\frac{\mathrm d}{\mathrm dx}\frac1{y'}
=-\frac{y''}{(y')^3}.
$$

**第二步：代回原反函数方程。**

$$
-\frac{y''}{(y')^3}+(y+\sin x)\frac1{(y')^3}=0.
$$

同乘非零的 $(y')^3$，整理为 $y''-y=\sin x$。

**（Ⅱ）求特解并代入初值。**

齐次方程的特征根为 $1,-1$。对右端正弦函数，设特解为 $A\sin x+B\cos x$，代入得 $-2A\sin x-2B\cos x=\sin x$，所以 $A=-1/2,B=0$。

因此通解为

$$
y=C_1\mathrm e^x+C_2\mathrm e^{-x}-\frac12\sin x.
$$

两个初始条件给出

$$
C_1+C_2=0,
\qquad C_1-C_2-\frac12=\frac32.
$$

解得 $C_1=1,C_2=-1$。所求解为

$$
y=\mathrm e^x-\mathrm e^{-x}-\frac12\sin x.
$$

此外 $y'=\mathrm e^x+\mathrm e^{-x}-\frac12\cos x\ge3/2>0$，所以确实满足反函数存在所需的非零导数条件。

**17.（2003.17；原卷七）交点个数的完整讨论。**

两条曲线的交点满足

$$
k=4x-4\ln x+\ln^4x,\qquad x>0.
$$

**第一步：用对数换元化简导数符号。**

令 $t=\ln x$，则 $x=\mathrm e^t$，交点个数等于水平直线 $y=k$ 与函数

$$
H(t)=4\mathrm e^t-4t+t^4
$$

图形的交点个数。

**第二步：证明函数先减后增。**

$$
H'(t)=4(\mathrm e^t-1+t^3).
$$

当 $t<0$ 时，$\mathrm e^t-1<0$ 且 $t^3<0$，所以 $H'<0$。

当 $t>0$ 时，两项都为正，所以 $H'>0$。因此 $H$ 在0处取得唯一最小值 $H(0)=4$。

**第三步：检查两端行为，确定每段根数。**

当 $t\to+\infty$ 时，指数项趋于无穷，$H(t)\to+\infty$；当 $t\to-\infty$ 时，$t^4-4t\to+\infty$，同样 $H(t)\to+\infty$。

所以 $k<4$ 时无交点；$k=4$ 时只有 $t=0$，对应 $x=1$，只有1个交点；$k>4$ 时，递减段和递增段各有1个根，共2个交点。

**18.（2003.18；原卷八）答案：$x^2+2y^2=1$ 的第一象限部分，$s=l/(2\sqrt2)$。**

**（Ⅰ）第一步：把法线中点条件写成微分方程。**

法线在点 $P=(x,y)$ 处的方程为

$$
Y-y=-\frac1{y'}(X-x).
$$

令 $X=0$，得到 $Q$ 的纵坐标 $Y_Q=y+x/y'$。因为线段 $PQ$ 的中点在 $x$ 轴上，$Y_Q=-y$，所以

$$
y+\frac{x}{y'}=-y,
\qquad y'=-\frac{x}{2y}.
$$

第一象限中 $x,y>0$；若 $y'=0$，法线竖直且不能与 $y$ 轴相交，所以这里的除法有依据。

**第二步：积分并用已知点确定常数。**

$$
2y\,\mathrm dy=-x\,\mathrm dx,
\qquad y^2+\frac{x^2}{2}=C.
$$

代入 $(\sqrt2/2,1/2)$，得 $C=1/2$，于是 $x^2+2y^2=1$。

**（Ⅱ）将两条曲线的弧长化成同一个积分。**

椭圆弧可参数化为

$$
x=\cos t,\qquad y=\frac1{\sqrt2}\sin t,
\qquad0\le t\le\frac\pi2.
$$

所以

$$
s=\int_0^{\pi/2}\sqrt{\sin^2t+\frac12\cos^2t}\,\mathrm dt
=\frac1{\sqrt2}\int_0^{\pi/2}\sqrt{1+\sin^2t}\,\mathrm dt.
$$

而正弦曲线给定弧长为

$$
l=\int_0^\pi\sqrt{1+\cos^2x}\,\mathrm dx
=2\int_0^{\pi/2}\sqrt{1+\sin^2t}\,\mathrm dt.
$$

第二个等号先利用对称性，再作 $x=\pi/2-t$ 的换元。比较两式，得到 $s=l/(2\sqrt2)$。

**19.（2003.19；原卷九）答案：$t=\varphi(y)^2-4$，$x=2\mathrm e^{\pi y/6}$。**

**（Ⅰ）由液面面积的变化率建立关系。**

设液面高度为 $y=y(t)$，水平液面面积为 $S(t)$。初始底面半径为2，所以 $S(0)=4\pi$。

已知 $S'(t)=\pi$，积分后得到

$$
S(t)=4\pi+\pi t.
$$

另一方面液面半径为 $\varphi(y)$，所以 $S(t)=\pi\varphi(y)^2$。两者比较，得 $t=\varphi(y)^2-4$。

**（Ⅱ）用体积变化率求高度与时间。**

液体体积的变化率是3。高度增加 $\mathrm dy$ 时，体积增加 $S(t)\mathrm dy$，故

$$
3=\frac{\mathrm dV}{\mathrm dt}=S(t)\frac{\mathrm dy}{\mathrm dt},
\qquad \frac{\mathrm dy}{\mathrm dt}=\frac3{\pi(t+4)}.
$$

由 $y(0)=0$，积分得到

$$
y=\frac3\pi\ln\frac{t+4}{4}.
$$

再利用 $t+4=\varphi(y)^2$，得

$$
y=\frac6\pi\ln\frac{\varphi(y)}2.
$$

解出侧壁曲线为 $x=\varphi(y)=2\mathrm e^{\pi y/6}$，$y\ge0$。当 $y=0$ 时半径确为2，与底面相符。

**20.（2003.20；原卷十）三个结论的证明。**

**（Ⅰ）先证明 $f(a)=0$。**

题设极限有限，且分母 $x-a\to0$，因此分子 $f(2x-a)\to0$。由端点连续性，同时有 $f(2x-a)\to f(a)$，故 $f(a)=0$。

因为 $f'>0$，函数在 $[a,b]$ 严格增加，所以对 $x\in(a,b)$，$f(x)>f(a)=0$。

特别地，$I=\int_a^b f(x)\,\mathrm dx>0$，后续分式有意义。

**（Ⅱ）为目标分式选择两个辅助函数。**

令

$$
F(x)=\int_a^x f(t)\,\mathrm dt,
\qquad G(x)=x^2-a^2.
$$

二者在闭区间连续、开区间可导，且 $F'(x)=f(x)>0$。柯西中值定理保证存在 $\xi\in(a,b)$，使

$$
\frac{G(b)-G(a)}{F(b)-F(a)}=\frac{G'(\xi)}{F'(\xi)}.
$$

代入各项，便得

$$
\frac{b^2-a^2}{\int_a^b f(x)\,\mathrm dx}=\frac{2\xi}{f(\xi)}.
$$

**（Ⅲ）在更小的区间制造另一个中值点。**

在 $[a,\xi]$ 上对 $f$ 使用拉格朗日中值定理，存在 $\eta\in(a,\xi)$，使

$$
f'(\eta)=\frac{f(\xi)-f(a)}{\xi-a}=\frac{f(\xi)}{\xi-a}.
$$

由第二问，把关系写成不除以 $\xi$ 的形式：

$$
f(\xi)(b^2-a^2)=2\xi I.
$$

两式结合，得到

$$
f'(\eta)(b^2-a^2)=\frac{2\xi}{\xi-a}I.
$$

由于 $\eta<\xi$，这两个点自动互不相同；整个证明也适用于 $\xi=0$ 的情形。

**21.（2003.21；原卷十一）答案：$a=0$，可逆矩阵如下。**

**第一步：求特征值及其重数。**

矩阵为分块上三角矩阵，所以

$$
\det(\lambda I-A)
=(\lambda-6)[(\lambda-2)^2-16]
=(\lambda+2)(\lambda-6)^2.
$$

特征值6为二重根。要可对角化，它对应的特征空间必须为二维，即 $r(A-6I)=1$。

**第二步：用秩条件确定 $a$。**

$$
A-6I=\begin{pmatrix}-4&2&0\\8&-4&a\\0&0&0\end{pmatrix}.
$$

前两行线性相关当且仅当 $a=0$。因此 $a=0$ 是可对角化的必要条件。

**第三步：构造基，验证充分性。**

当 $a=0$ 时，属于 $-2$ 的特征向量可取 $(1,-2,0)^{\mathrm T}$；属于6的两个独立特征向量可取 $(1,2,0)^{\mathrm T}$、$(0,0,1)^{\mathrm T}$。

所以令

$$
P=\begin{pmatrix}1&1&0\\-2&2&0\\0&0&1\end{pmatrix},
\qquad \Lambda=\operatorname{diag}(-2,6,6).
$$

有 $\det P=4\ne0$，且 $AP=P\Lambda$，故 $P^{-1}AP=\Lambda$。因此 $a=0$ 也充分。

**22.（2003.22；原卷十二）三条直线共点的充要条件。**

**必要性：由共同解得到行列式为零。**

如果三条直线经过同一点 $(x,y)$，则非零向量 $(x,y,1)^{\mathrm T}$ 满足

$$
\begin{pmatrix}a&2b&3c\\b&2c&3a\\c&2a&3b\end{pmatrix}
\begin{pmatrix}x\\y\\1\end{pmatrix}=0.
$$

所以系数矩阵行列式为零。提出第2、3列的因子2与3，计算并分解，得到

$$
0=-6(a+b+c)(a^2+b^2+c^2-ab-bc-ca).
$$

第二个因子可以写成

$$
\frac12[(a-b)^2+(b-c)^2+(c-a)^2].
$$

若它为零，则 $a=b=c$；非零时三条直线完全相同，全部为零时又不构成有效直线，均与“三条不同直线”矛盾。

因此只能 $a+b+c=0$。

**充分性：直接找出共同交点。**

如果 $a+b+c=0$，取 $x=3,y=3/2$。三条直线左端依次为

$$
3a+3b+3c,
\qquad3b+3c+3a,
\qquad3c+3a+3b,
$$

全部为零，所以三条直线均经过 $(3,3/2)$。

因为三条直线互不相同，共同经过这一点就说明它们交于同一点。充分性与必要性均已证明。
