# 2013年考研数学二答案与解析

> 我的解答，未与官方核对。

## 答案速查

> 选择题：1.C　2.A　3.C　4.D　5.A　6.B　7.B　8.B。

> 填空题：9.$\sqrt{\mathrm e}$；10.$\sqrt{\dfrac{\mathrm e}{\mathrm e-1}}$；11.$\pi/12$；12.$x+y=\pi/4+\tfrac12\ln2$；13.$\mathrm e^{3x}-\mathrm e^x-x\mathrm e^{2x}$；14.$-1$。

15. $n=2,a=7$。

16. $a=7\sqrt7$。

17. 积分为 $416/3$。

18. 两个中值结论的证明见正文。

19. 最短距离为1，最长距离为 $\sqrt2$。

20. $f$ 的最小值为1；数列极限为1。

21. 弧长为 $(\mathrm e^2+1)/4$；形心横坐标为 $\dfrac{3(\mathrm e^4-2\mathrm e^2-3)}{4(\mathrm e^3-7)}$。

22. $a=-1,b=0$；$C=\begin{pmatrix}r+s+1&-r\\r&s\end{pmatrix}$，$r,s$ 任意。

23. 正交变换后的标准形为 $2y_1^2+y_2^2$。

## 一、选择题（1～8）

**1.（2013.1）答案：C，同阶但不等价。**

由题设，

$$
\sin\alpha(x)=\frac{\cos x-1}{x}\sim-\frac x2\longrightarrow0.
$$

又因为 $|\alpha(x)|<\pi/2$，正弦函数在该范围内可取连续的反函数，所以 $\alpha(x)\to0$。

因此可以使用 $\sin\alpha\sim\alpha$，得到

$$
\frac{\alpha(x)}x
=\frac{\alpha(x)}{\sin\alpha(x)}\cdot\frac{\sin\alpha(x)}x
\longrightarrow-\frac12.
$$

极限有限且非零，但不等于1，所以与 $x$ 同阶而不等价。

**2.（2013.2）答案：A，$2$。**

先令 $x=0$，由隐式方程得到 $1+\ln y=1$，所以 $f(0)=1$。

对原式求导：

$$
-\sin(xy)(y+xy')+\frac{y'}y-1=0.
$$

在 $(x,y)=(0,1)$ 处，得到 $f'(0)=1$。

于是令 $h=2/n$，原极限化为

$$
\lim_{n\to\infty}n[f(2/n)-1]
=2\lim_{h\to0^+}\frac{f(h)-f(0)}h
=2f'(0)=2.
$$

**3.（2013.3）答案：C，连续但不可导。**

把变上限积分分段写出：

$$
F(x)=\begin{cases}
1-\cos x,&0\le x\le\pi,\\
2+2(x-\pi),&\pi\le x\le2\pi.
\end{cases}
$$

两段在 $x=\pi$ 处均取值2，所以连续。

左导数为 $\sin\pi=0$，右导数为2。左右导数不相等，因此在 $\pi$ 处不可导。

**4.（2013.4）答案：D，$0<\alpha<2$。**

反常积分需要同时在两个瑕点收敛。

靠近 $x=1$ 时，被积函数为 $(x-1)^{-(\alpha-1)}$，其可积条件是

$$
\alpha-1<1,
\qquad\alpha<2.
$$

在无穷远，令 $u=\ln x$，得到

$$
\int_{\mathrm e}^{+\infty}\frac{\mathrm dx}{x\ln^{\alpha+1}x}
=\int_1^{+\infty}u^{-\alpha-1}\,\mathrm du.
$$

它收敛当且仅当 $\alpha>0$。合并为 $0<\alpha<2$。

**5.（2013.5）答案：A，$2yf'(xy)$。**

按乘积法则和链式法则，

$$
z_x=-\frac{y}{x^2}f(xy)+\frac{y^2}{x}f'(xy),
$$

$$
z_y=\frac1x f(xy)+y f'(xy).
$$

将第一式乘以 $x/y$ 后与第二式相加，含 $f(xy)$ 的两项抵消，得到

$$
\frac xy z_x+z_y=2yf'(xy).
$$

**6.（2013.6）答案：B。**

在第二象限的内部，$y>0,x<0$，所以 $y-x>0$，从而 $I_2>0$。

第四象限内部则有 $y-x<0$，故 $I_4<0$。

第一、第三象限中的四分之一圆盘均关于直线 $y=x$ 对称。交换 $x,y$ 后，被积函数 $y-x$ 变为其相反数，因此

$$
I_1=I_3=0.
$$

所以只有 B 的正号结论成立。

**7.（2013.7）答案：B。**

由 $C=AB$，$C$ 的每一列都可由 $A$ 的列向量线性表示。

又因为 $B$ 可逆，$A=CB^{-1}$，所以 $A$ 的每一列也可由 $C$ 的列向量线性表示。

两边互相线性表示，说明 $C$ 与 $A$ 的列向量组等价，故选 B。

**8.（2013.8）答案：B，$a=0$，$b$ 任意。**

**必要性。** 相似矩阵的平方仍相似，因此平方的迹也相同。

对第一个矩阵，平方的迹为

$$
4+4a^2+b^2;
$$

对目标对角矩阵，则为 $4+b^2$。两者相等，得到 $a=0$。

**充分性。** 当 $a=0$ 时，矩阵为

$$
\begin{pmatrix}1&0&1\\0&b&0\\1&0&1\end{pmatrix}.
$$

向量 $(1,0,1)^{\mathrm T}$、$(0,1,0)^{\mathrm T}$、$(1,0,-1)^{\mathrm T}$ 分别是属于特征值 $2,b,0$ 的特征向量，且始终线性无关。

所以任意 $b$ 都可以将矩阵相似对角化为 $\operatorname{diag}(2,b,0)$，充分性成立。

## 二、填空题（9～14）

**9.（2013.9）答案：$\sqrt{\mathrm e}$。**

由对数展开，

$$
\frac{\ln(1+x)}x=1-\frac x2+O(x^2),
$$

因此括号内为 $1+x/2+O(x^2)$。

取对数后，

$$
\frac1x\ln\left[2-\frac{\ln(1+x)}x\right]\longrightarrow\frac12.
$$

故原极限为 $\mathrm e^{1/2}$。

**10.（2013.10）答案：$\sqrt{\dfrac{\mathrm e}{\mathrm e-1}}$。**

首先找出 $y=0$ 对应的原自变量。由积分定义，$f(-1)=0$，所以这里应取 $x=-1$。

变上限积分的导数为

$$
f'(x)=\sqrt{1-\mathrm e^x}.
$$

在 $x=-1$ 处，导数为 $\sqrt{1-\mathrm e^{-1}}>0$，所以反函数求导公式适用：

$$
\left.\frac{\mathrm dx}{\mathrm dy}\right|_{y=0}
=\frac1{f'(-1)}
=\frac1{\sqrt{1-\mathrm e^{-1}}}
=\sqrt{\frac{\mathrm e}{\mathrm e-1}}.
$$

**11.（2013.11）答案：$\pi/12$。**

在给定角度范围内 $r\ge0$，描述一片封闭花瓣。面积为

$$
\begin{aligned}
S&=\frac12\int_{-\pi/6}^{\pi/6}\cos^23\theta\,\mathrm d\theta\\
&=\frac14\left[\theta+\frac16\sin6\theta\right]_{-\pi/6}^{\pi/6}
=\frac\pi{12}.
\end{aligned}
$$

**12.（2013.12）答案：$x+y=\dfrac\pi4+\dfrac12\ln2$。**

当 $t=1$ 时，曲线点为 $(\pi/4,\frac12\ln2)$。

又有

$$
x'=\frac1{1+t^2},\qquad y'=\frac t{1+t^2},
$$

所以切线斜率 $\mathrm dy/\mathrm dx=t$，在该点为1，法线斜率为 $-1$。

由点斜式，

$$
y-\frac12\ln2=-\left(x-\frac\pi4\right),
$$

整理即得结论。

**13.（2013.13）答案：$\mathrm e^{3x}-\mathrm e^x-x\mathrm e^{2x}$。**

任意两个非齐次解之差都是齐次解，所以

$$
y_1-y_3=\mathrm e^{3x},\qquad y_2-y_3=\mathrm e^x
$$

给出两个线性无关的齐次解。取 $y_3=-x\mathrm e^{2x}$ 为非齐次特解，通解为

$$
y=C_1\mathrm e^{3x}+C_2\mathrm e^x-x\mathrm e^{2x}.
$$

初始条件给出

$$
C_1+C_2=0,\qquad3C_1+C_2-1=1.
$$

解得 $C_1=1,C_2=-1$，故为所列解。

**14.（2013.14）答案：$-1$。**

设 $D=\det A$。题设中每个元素与对应代数余子式互为相反数，因此

$$
A^*=-A^{\mathrm T}.
$$

取行列式，利用三阶伴随矩阵的行列式为 $D^2$，得到

$$
D^2=-D,
$$

所以 $D=0$ 或 $D=-1$。

下面排除 $D=0$。若 $r(A)=1$，则全部二阶子式为零，$A^*=0$，与 $A^*=-A^{\mathrm T}$ 及 $A\ne0$ 矛盾。

若 $r(A)=2$，则 $r(A^*)=1$，但 $r(A^{\mathrm T})=2$，也与等式矛盾。秩为0又意味着零矩阵，已经被题设排除。

因此 $A$ 必可逆，只能 $D=-1$。

## 三、解答题（15～23）

**15.（2013.15）答案：$n=2,a=7$。**

对 $k=1,2,3$，均有

$$
\cos kx=1-\frac{k^2x^2}{2}+O(x^4).
$$

将三个展开式相乘，二次项由每次只取一个二次项得到，交叉乘积属于四次及更高阶。因此

$$
\cos x\cos2x\cos3x
=1-\frac{1^2+2^2+3^2}{2}x^2+O(x^4)
=1-7x^2+O(x^4).
$$

所以原无穷小与 $7x^2$ 等价，得到 $n=2,a=7$。

**16.（2013.16）答案：$a=7\sqrt7$。**

绕 $x$ 轴旋转，用圆盘法：

$$
V_x=\pi\int_0^a x^{2/3}\,\mathrm dx=\frac{3\pi}{5}a^{5/3}.
$$

绕 $y$ 轴旋转，用圆柱壳法：

$$
V_y=2\pi\int_0^a x\cdot x^{1/3}\,\mathrm dx=\frac{6\pi}{7}a^{7/3}.
$$

由 $V_y=10V_x$，约去正的公共因子后，得到

$$
a^{2/3}=7.
$$

因为 $a>0$，所以 $a=7^{3/2}=7\sqrt7$。

**17.（2013.17）答案：$416/3$。**

**第一步：确定三个顶点。**

两条过原点的直线与 $x+y=8$ 分别交于 $(6,2)$、$(2,6)$，区域是它们与原点组成的三角形。

**第二步：按竖直截线分段。**

当 $0\le x\le2$ 时，$x/3\le y\le3x$；当 $2\le x\le6$ 时，$x/3\le y\le8-x$。

因此

$$
\begin{aligned}
I&=\int_0^2x^2\left(3x-\frac x3\right)\mathrm dx
+\int_2^6x^2\left(8-x-\frac x3\right)\mathrm dx\\
&=\frac83\int_0^2x^3\,\mathrm dx
+\int_2^6\left(8x^2-\frac43x^3\right)\mathrm dx.
\end{aligned}
$$

计算得到

$$
I=\frac{32}{3}+\left[\frac83x^3-\frac13x^4\right]_2^6
=\frac{32}{3}+128=\frac{416}{3}.
$$

**18.（2013.18）两个中值结论的证明。**

**（Ⅰ）使用奇性和拉格朗日中值定理。**

奇函数满足 $f(0)=0$，而 $f(1)=1$。在 $[0,1]$ 上用中值定理，存在 $\xi\in(0,1)$，使

$$
f'(\xi)=\frac{f(1)-f(0)}{1-0}=1.
$$

**（Ⅱ）为目标等式构造导数。**

奇函数的导函数为偶函数，所以 $f'(-\xi)=f'(\xi)=1$。

令

$$
H(x)=\mathrm e^x[f'(x)-1].
$$

则 $H(-\xi)=H(\xi)=0$。函数 $H$ 在这两个端点之间连续、可导，因此由罗尔定理，存在 $\eta\in(-\xi,\xi)$，使

$$
H'(\eta)=\mathrm e^\eta[f''(\eta)+f'(\eta)-1]=0.
$$

指数因子不为零，故 $f''(\eta)+f'(\eta)=1$。且 $(-\xi,\xi)\subset(-1,1)$，区间条件满足。

**19.（2013.19）答案：最短距离为1，最长距离为 $\sqrt2$。**

**第一步：用和与积改写约束。**

令 $s=x+y$、$p=xy$。原方程等价于

$$
s^3-(3s+1)p=1,
\qquad p=\frac{s^3-1}{3s+1}.
$$

因为 $x,y\ge0$ 且不全为零，所以 $s>0,p\ge0$，从而 $s\ge1$。

又有 $p\le s^2/4$，所以

$$
4(s^3-1)\le s^2(3s+1).
$$

整理并因式分解，得到

$$
(s-2)(s^2+s+2)\le0.
$$

第二因子恒正，因此 $s\le2$。故全部曲线点都满足 $1\le s\le2$。

**第二步：把距离平方写成 $s$ 的函数。**

$$
q=x^2+y^2=s^2-2p
=\frac{s^3+s^2+2}{3s+1}.
$$

其导数为

$$
q'(s)=\frac{2(3s^3+3s^2+s-3)}{(3s+1)^2}>0,
\qquad1\le s\le2.
$$

分子括号内至少为 $3+3+1-3=4$，所以严格为正。

因此

$$
1=q(1)\le q\le q(2)=2.
$$

**第三步：检查等号能否达到。**

$s=1$ 时，$p=0$，对应点为 $(1,0)$、$(0,1)$，都在曲线上，距离为1。

$s=2$ 时，$p=1$，对应点为 $(1,1)$，也在曲线上，距离为 $\sqrt2$。

所以上述上下界均可达到，最短、最长距离分别为1和 $\sqrt2$。

**20.（2013.20）答案：最小值为1，数列极限为1。**

**（Ⅰ）求函数最小值。**

$$
f'(x)=\frac1x-\frac1{x^2}=\frac{x-1}{x^2},\qquad x>0.
$$

函数在 $(0,1)$ 上递减，在 $(1,+\infty)$ 上递增，所以唯一最小值点为1，最小值 $f(1)=1$。

**（Ⅱ）先证明数列单调。**

各项均为正。由第一问，

$$
\ln x_n+\frac1{x_n}\ge1.
$$

与题设严格不等式比较，得到

$$
\frac1{x_{n+1}}<\frac1{x_n},
\qquad x_{n+1}>x_n.
$$

所以数列严格增加。

**再证明有上界。** 因 $1/x_{n+1}>0$，题设还推出 $\ln x_n<1$，故 $x_n<\mathrm e$。

因此数列有极限 $L$，且 $L\ge x_1>0$。

**最后确定极限。** 在已知不等式中取极限，得到

$$
\ln L+\frac1L\le1.
$$

而第一问说明左边总是不小于1，且等号只在 $L=1$ 处成立。因此 $\lim x_n=1$。

**21.（2013.21）弧长及形心横坐标。**

**（Ⅰ）计算弧长。**

曲线导数为

$$
y'=\frac x2-\frac1{2x}.
$$

因为 $x>0$，

$$
\sqrt{1+(y')^2}=\frac x2+\frac1{2x}.
$$

所以

$$
\begin{aligned}
L&=\int_1^{\mathrm e}\left(\frac x2+\frac1{2x}\right)\mathrm dx\\
&=\left[\frac{x^2}{4}+\frac12\ln x\right]_1^{\mathrm e}
=\frac{\mathrm e^2+1}{4}.
\end{aligned}
$$

**（Ⅱ）先求面积和一阶面积矩。**

在 $[1,\mathrm e]$ 上，曲线位于 $x$ 轴上方。面积为

$$
\begin{aligned}
S&=\int_1^{\mathrm e}\left(\frac{x^2}{4}-\frac12\ln x\right)\mathrm dx\\
&=\left[\frac{x^3}{12}-\frac12(x\ln x-x)\right]_1^{\mathrm e}
=\frac{\mathrm e^3-7}{12}.
\end{aligned}
$$

一阶面积矩为

$$
\begin{aligned}
M_y&=\int_1^{\mathrm e}x\left(\frac{x^2}{4}-\frac12\ln x\right)\mathrm dx\\
&=\left[\frac{x^4}{16}-\frac{x^2\ln x}{4}+\frac{x^2}{8}\right]_1^{\mathrm e}\\
&=\frac{\mathrm e^4-2\mathrm e^2-3}{16}.
\end{aligned}
$$

因此形心横坐标为

$$
\bar x=\frac{M_y}{S}
=\frac{3(\mathrm e^4-2\mathrm e^2-3)}{4(\mathrm e^3-7)}.
$$

**22.（2013.22）答案：$a=-1,b=0$，全部矩阵 $C$ 如下。**

**第一步：先用迹确定 $b$。**

因为 $\operatorname{tr}(AC)=\operatorname{tr}(CA)$，所以交换子 $AC-CA$ 的迹为零。故 $\operatorname{tr}B=b=0$。

**第二步：按矩阵元素解方程。**

设

$$
C=\begin{pmatrix}p&q\\r&s\end{pmatrix}.
$$

计算得

$$
AC-CA=\begin{pmatrix}
ar-q&q+a(s-p)\\
p-r-s&q-ar
\end{pmatrix}.
$$

与 $B$ 比较，得到

$$
q=ar,\qquad q+a(s-p)=1,\qquad p-r-s=1.
$$

后两式结合第一式，给出

$$
a(r+s-p)=1.
$$

而 $r+s-p=-1$，因此 $a=-1$。

**第三步：写出全部自由参数。**

代入 $a=-1$，有 $q=-r$、$p=r+s+1$。所以全部解为

$$
C=\begin{pmatrix}r+s+1&-r\\r&s\end{pmatrix},
\qquad r,s\in\mathbb R.
$$

将此式代回可直接得到 $AC-CA=\begin{pmatrix}0&1\\1&0\end{pmatrix}$，所以参数条件充分，全部自由度也已列全。

**23.（2013.23）二次型矩阵与标准形的证明。**

**（Ⅰ）把线性式写成向量内积。**

有

$$
a_1x_1+a_2x_2+a_3x_3=\alpha^{\mathrm T}\boldsymbol x,
\qquad
b_1x_1+b_2x_2+b_3x_3=\beta^{\mathrm T}\boldsymbol x.
$$

因此

$$
\begin{aligned}
f(\boldsymbol x)
&=2(\alpha^{\mathrm T}\boldsymbol x)^2+(\beta^{\mathrm T}\boldsymbol x)^2\\
&=\boldsymbol x^{\mathrm T}(2\alpha\alpha^{\mathrm T}+\beta\beta^{\mathrm T})\boldsymbol x.
\end{aligned}
$$

括号内为对称矩阵，正是二次型矩阵。

**（Ⅱ）利用正交单位向量构造特征向量。**

记 $A=2\alpha\alpha^{\mathrm T}+\beta\beta^{\mathrm T}$。由 $\alpha,\beta$ 正交且均为单位向量，得到

$$
A\alpha=2\alpha,\qquad A\beta=\beta.
$$

再在它们的正交补中取一个单位向量 $\gamma$，则 $\alpha^{\mathrm T}\gamma=\beta^{\mathrm T}\gamma=0$，所以 $A\gamma=0$。

令 $Q=(\alpha,\beta,\gamma)$，则 $Q$ 正交，并有

$$
Q^{\mathrm T}AQ=\operatorname{diag}(2,1,0).
$$

因此在正交变换 $\boldsymbol x=Q\boldsymbol y$ 下，标准形为 $2y_1^2+y_2^2$。
