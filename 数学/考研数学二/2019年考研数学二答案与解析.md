# 2019年考研数学二答案与解析

> 我的解答，未与官方核对。

## 答案速查

> 选择题：1.C　2.B　3.D　4.D　5.A　6.A　7.A　8.C。

> 填空题：9.$4\mathrm e^2$；10.$2+\dfrac{3\pi}{2}$；11.$z$；12.$\dfrac12\ln3$；13.$\dfrac{\cos1-1}{4}$；14.$-4$。

15. 极大值为 $f(0)=1$；两个极小值为 $f(-1)=1-\mathrm e^{-1}$、$f(\mathrm e^{-1})=\mathrm e^{-2/\mathrm e}$。

16. 不定积分为 $\ln\dfrac{x^2+x+1}{(x-1)^2}-\dfrac3{x-1}+C$。

17. $y=\sqrt x\,\mathrm e^{x^2/2}$；体积 $\dfrac\pi2(\mathrm e^4-\mathrm e)$。

18. 积分值为 $\dfrac{43\sqrt2}{120}$。

19. $S_n=\dfrac{\mathrm e^\pi+1}{2(\mathrm e^\pi-1)}(1-\mathrm e^{-n\pi})$，极限为 $\dfrac{\mathrm e^\pi+1}{2(\mathrm e^\pi-1)}$。

20. $a=-\dfrac34$，$b=\dfrac34$。

21. 两个存在性结论的证明见正文。

22. $a\ne-1$；可取 $\beta_3=\alpha_1-\alpha_2+\alpha_3$。

23. $x=3,y=-2$；满足 $P^{-1}AP=B$ 的矩阵见正文。

## 一、选择题（1～8）

**1.（2019.1）答案：C，$k=3$。**

在 $x=0$ 附近，

$$
\tan x=x+\frac{x^3}{3}+O(x^5),
$$

因此

$$
x-\tan x=-\frac{x^3}{3}+O(x^5),
\qquad\frac{x-\tan x}{x^3}\longrightarrow-\frac13.
$$

比值趋于有限非零常数，说明二者同阶，故 $k=3$。这里要求“同阶”，不要求比值为 $1$。

**2.（2019.2）答案：B，拐点为 $(\pi,-2)$。**

先求导：

$$
y'=x\cos x-\sin x,
\qquad y''=-x\sin x.
$$

在给定区间 $(-\pi/2,2\pi)$ 中，二阶导数的零点只有 $0$ 与 $\pi$。

当 $x<0$ 且接近 $0$ 时，$x$ 与 $\sin x$ 同负，故 $y''<0$；当 $0<x<\pi$ 时，同样 $y''<0$。所以 $0$ 的两侧凹凸性不改变，不是拐点。

在 $\pi$ 左侧 $y''<0$，右侧 $y''>0$，凹凸性改变。又

$$
y(\pi)=\pi\sin\pi+2\cos\pi=-2,
$$

故拐点为 $(\pi,-2)$。

**3.（2019.3）答案：D。**

前三个积分均收敛，可以直接计算：

$$
\int_0^{+\infty}x\mathrm e^{-x}\,\mathrm dx=1,
\qquad
\int_0^{+\infty}x\mathrm e^{-x^2}\,\mathrm dx=\frac12.
$$

C 中令 $u=\arctan x$，有 $\mathrm du=\mathrm dx/(1+x^2)$，所以

$$
\int_0^{+\infty}\frac{\arctan x}{1+x^2}\,\mathrm dx
=\int_0^{\pi/2}u\,\mathrm du=\frac{\pi^2}{8}.
$$

D 中

$$
\int_0^R\frac{x}{1+x^2}\,\mathrm dx=\frac12\ln(1+R^2)
\longrightarrow+\infty.
$$

故只有 D 发散。

**4.（2019.4）答案：D，$a=2,b=1,c=4$。**

齐次通解含有 $\mathrm e^{-x}$ 与 $x\mathrm e^{-x}$，所以特征方程有二重根 $r=-1$。

因此

$$
r^2+ar+b=(r+1)^2=r^2+2r+1,
$$

得到 $a=2,b=1$。

给出的非齐次特解为 $y_p=\mathrm e^x$，代入左边：

$$
y_p''+2y_p'+y_p=(1+2+1)\mathrm e^x=4\mathrm e^x.
$$

所以 $c=4$。

**5.（2019.5）答案：A，$I_3<I_2<I_1$。**

记 $r=\sqrt{x^2+y^2}$。在区域 $D$ 内，

$$
0\le r\le|x|+|y|\le\frac\pi2.
$$

当 $0<r<\pi/2$ 时，一方面 $\sin r<r$；另一方面

$$
\sin r-(1-\cos r)=\sin r+\cos r-1>0.
$$

于是

$$
1-\cos r<\sin r<r.
$$

在 $D$ 上积分即可比较三个积分。严格不等式在区域内部除原点以外成立，且这部分面积为正，因此积分后的不等式也严格成立：$I_3<I_2<I_1$。

**6.（2019.6）答案：A，充分非必要条件。**

**充分性。** 令 $h=x-a$。由二阶泰勒展开，

$$
\begin{aligned}
f(a+h)-g(a+h)
&=[f(a)-g(a)]\\
&\quad+[f'(a)-g'(a)]h\\
&\quad+\frac12[f''(a)-g''(a)]h^2+o(h^2).
\end{aligned}
$$

若此差是 $o(h^2)$，比较常数项、一次项、二次项，得

$$
f(a)=g(a),\qquad f'(a)=g'(a),\qquad f''(a)=g''(a).
$$

前两式说明两条曲线在同一点有同一切线；第三式结合曲率公式

$$
K=\frac{|y''|}{[1+(y')^2]^{3/2}}
$$

说明曲率相等，故条件充分。

**不是必要条件。** 取

$$
f(x)=(x-a)^2,\qquad g(x)=-(x-a)^2.
$$

它们在 $(a,0)$ 处具有同一条水平切线，曲率均为 $2$，但

$$
\frac{f(x)-g(x)}{(x-a)^2}=2,
$$

极限不是零。曲率使用二阶导数的绝对值，因此曲率相等不能推出二阶导数本身相等。

**7.（2019.7）答案：A，$r(A^*)=0$。**

方程组 $Ax=0$ 有两个基础解系向量，零空间维数为2。由秩与零度关系，

$$
r(A)=4-2=2.
$$

伴随矩阵的各元素来自三阶子式，但秩为2的矩阵全部三阶子式均为零。因此

$$
A^*=0,
\qquad r(A^*)=0.
$$

**8.（2019.8）答案：C。**

若 $\lambda$ 为 $A$ 的特征值，则由 $A^2+A=2I$，有

$$
\lambda^2+\lambda-2=0,
\qquad (\lambda-1)(\lambda+2)=0.
$$

故每个特征值都属于 $\{1,-2\}$。

三个特征值的乘积为 $\det A=4$，所以只能是 $1,-2,-2$。

由于 $A$ 实对称，可以正交对角化，二次型有一个正特征值、两个负特征值。将非零平方项缩放为系数 $\pm1$，规范形为

$$
y_1^2-y_2^2-y_3^2.
$$

## 二、填空题（9～14）

**9.（2019.9）答案：$4\mathrm e^2$。**

由指数展开，

$$
x+2^x=1+(1+\ln2)x+O(x^2).
$$

先对原式取对数：

$$
\frac2x\ln(x+2^x)
=\frac2x\bigl[(1+\ln2)x+O(x^2)\bigr]
\longrightarrow2+2\ln2.
$$

再取指数，得到

$$
\mathrm e^{2+2\ln2}=4\mathrm e^2.
$$

**10.（2019.10）答案：$2+\dfrac{3\pi}{2}$。**

当 $t=3\pi/2$ 时，点的坐标为

$$
x_0=\frac{3\pi}{2}+1,\qquad y_0=1.
$$

参数方程的导数为

$$
\frac{\mathrm dy}{\mathrm dx}=
\frac{\sin t}{1-\cos t},
$$

在该点等于 $-1$。因此切线为

$$
y-1=-\left(x-\frac{3\pi}{2}-1\right).
$$

令 $x=0$，得到纵截距 $2+3\pi/2$。

**11.（2019.11）答案：$z$。**

令 $u=y^2/x$，则 $z=yf(u)$。链式法则给出

$$
z_x=-\frac{y^3}{x^2}f'(u),
\qquad
z_y=f(u)+\frac{2y^2}{x}f'(u).
$$

因此

$$
\begin{aligned}
2xz_x+yz_y
&=-\frac{2y^3}{x}f'(u)+yf(u)+\frac{2y^3}{x}f'(u)\\
&=yf(u)=z.
\end{aligned}
$$

含 $f'$ 的两项抵消，留下原函数 $z$。

**12.（2019.12）答案：$\dfrac12\ln3$。**

曲线的导数为 $y'=-\tan x$。在给定区间内 $\cos x>0$，故

$$
\sqrt{1+(y')^2}=\sqrt{1+\tan^2x}=\sec x.
$$

于是弧长为

$$
\begin{aligned}
L&=\int_0^{\pi/6}\sec x\,\mathrm dx\\
&=\left[\ln(\sec x+\tan x)\right]_0^{\pi/6}\\
&=\ln\left(\frac2{\sqrt3}+\frac1{\sqrt3}\right)
=\ln\sqrt3=\frac12\ln3.
\end{aligned}
$$

**13.（2019.13）答案：$\dfrac{\cos1-1}{4}$。**

在 $0\le x\le1$ 上，先调换内层积分的方向：

$$
I=\int_0^1f(x)\,\mathrm dx
=-\int_0^1x\,\mathrm dx\int_x^1\frac{\sin t^2}{t}\,\mathrm dt.
$$

积分区域为 $0\le x\le t\le1$。换序后，

$$
\begin{aligned}
I&=-\int_0^1\frac{\sin t^2}{t}\left(\int_0^t x\,\mathrm dx\right)\mathrm dt\\
&=-\frac12\int_0^1t\sin t^2\,\mathrm dt\\
&=\frac14\left[\cos t^2\right]_0^1
=\frac{\cos1-1}{4}.
\end{aligned}
$$

在 $t=0$ 处，$\sin t^2/t\to0$，可连续补定义，不影响换序。

**14.（2019.14）答案：$-4$。**

按原矩阵第一行展开行列式，有

$$
\det A=1\cdot A_{11}+(-1)\cdot A_{12}=A_{11}-A_{12}.
$$

所以只需求 $\det A$。作行变换 $R_2\leftarrow R_2+2R_1$、$R_3\leftarrow R_3-3R_1$ 后，第一列只有首项非零，得到

$$
\det A=\begin{vmatrix}-1&-1&1\\1&2&-1\\0&3&4\end{vmatrix}.
$$

沿第一行展开：

$$
\det A=-1(8+3)+4+3=-4.
$$

## 三、解答题（15～23）

**15.（2019.15）导数与极值如下。**

**第一步：分别对两段求导。**

当 $x<0$ 时，

$$
f(x)=x\mathrm e^x+1,
\qquad f'(x)=(x+1)\mathrm e^x.
$$

当 $x>0$ 时，先写为 $f(x)=\mathrm e^{2x\ln x}$，所以

$$
f'(x)=2x^{2x}(1+\ln x).
$$

**第二步：单独判断 $x=0$ 处的可导性。**

$\lim_{x\to0^+}x^{2x}=1=f(0)$，因此函数在拼接处连续。

左导数等于 $1$。右侧差商则满足

$$
\frac{x^{2x}-1}{x}
=\frac{\mathrm e^{2x\ln x}-1}{2x\ln x}\cdot2\ln x
\longrightarrow-\infty.
$$

所以 $f'(0)$ 不存在。

**第三步：判断负半轴上的极值。**

因为 $\mathrm e^x>0$，$f'$ 在 $(-\infty,-1)$ 上为负，在 $(-1,0)$ 上为正。因此 $x=-1$ 是极小值点，

$$
f(-1)=1-\mathrm e^{-1}.
$$

**第四步：判断正半轴上的极值。**

$1+\ln x$ 在 $x=\mathrm e^{-1}$ 处变号：左侧为负，右侧为正。因此

$$
x=\mathrm e^{-1}
$$

是另一个极小值点，极小值为

$$
f(\mathrm e^{-1})=(\mathrm e^{-1})^{2/\mathrm e}
=\mathrm e^{-2/\mathrm e}.
$$

**第五步：检查不可导点。**

当 $x<0$ 时，$x\mathrm e^x+1<1$；当 $0<x<1$ 时，$2x\ln x<0$，所以 $x^{2x}<1$。

故在原点两侧的一个邻域内，除原点外均有 $f(x)<f(0)=1$。因此 $x=0$ 是极大值点，极大值为 $1$。

除了上述两个驻点和拼接点外，导数不再变号，所以极值已全部列出。

**16.（2019.16）答案：$\ln\dfrac{x^2+x+1}{(x-1)^2}-\dfrac3{x-1}+C$。**

**第一步：部分分式分解。** 设

$$
\frac{3x+6}{(x-1)^2(x^2+x+1)}
=\frac A{x-1}+\frac B{(x-1)^2}+\frac{Cx+D}{x^2+x+1}.
$$

两边同乘分母并比较系数，可得

$$
A=-2,\qquad B=3,\qquad C=2,\qquad D=1.
$$

因此

$$
\frac{3x+6}{(x-1)^2(x^2+x+1)}
=-\frac2{x-1}+\frac3{(x-1)^2}+\frac{2x+1}{x^2+x+1}.
$$

**第二步：逐项积分。** 最后一项的分子正好是分母的导数，故

$$
\begin{aligned}
I&=-2\ln|x-1|-\frac3{x-1}+\ln(x^2+x+1)+C\\
&=\ln\frac{x^2+x+1}{(x-1)^2}-\frac3{x-1}+C.
\end{aligned}
$$

由于 $x^2+x+1>0$，该对数无需再加绝对值；结果分别适用于不跨越 $x=1$ 的区间。

**17.（2019.17）答案：$y=\sqrt x\,\mathrm e^{x^2/2}$，$V=\dfrac\pi2(\mathrm e^4-\mathrm e)$。**

**（Ⅰ）求解微分方程。**

方程的一次项系数为 $-x$，积分因子为

$$
\mu(x)=\mathrm e^{-x^2/2}.
$$

两边同乘积分因子，得

$$
\left(y\mathrm e^{-x^2/2}\right)'=\frac1{2\sqrt x}.
$$

积分后

$$
y\mathrm e^{-x^2/2}=\sqrt x+C,
\qquad y=(\sqrt x+C)\mathrm e^{x^2/2}.
$$

由 $y(1)=\sqrt{\mathrm e}$，有 $(1+C)\sqrt{\mathrm e}=\sqrt{\mathrm e}$，故 $C=0$。

**（Ⅱ）用圆盘法求体积。**

每个垂直于 $x$ 轴的截面，旋转后为半径 $y(x)$ 的圆盘，所以

$$
\begin{aligned}
V&=\pi\int_1^2y(x)^2\,\mathrm dx\\
&=\pi\int_1^2x\mathrm e^{x^2}\,\mathrm dx\\
&=\frac\pi2\left[\mathrm e^{x^2}\right]_1^2
=\frac\pi2(\mathrm e^4-\mathrm e).
\end{aligned}
$$

**18.（2019.18）答案：$\dfrac{43\sqrt2}{120}$。**

**第一步：确定极坐标范围。**

由 $|x|\le y$，区域位于两条直线 $y=x$、$y=-x$ 之间的上方，故

$$
\frac\pi4\le\theta\le\frac{3\pi}{4}.
$$

另一条件变为

$$
r^6\le r^4\sin^4\theta.
$$

当 $r>0$ 时，约去 $r^4$，得到 $r\le\sin^2\theta$；原点也包含在区域中。因此

$$
0\le r\le\sin^2\theta.
$$

**第二步：换元并利用对称性。**

$$
\frac{x+y}{\sqrt{x^2+y^2}}=\cos\theta+\sin\theta,
\qquad \mathrm dx\mathrm dy=r\,\mathrm dr\,\mathrm d\theta.
$$

于是

$$
I=\frac12\int_{\pi/4}^{3\pi/4}
(\cos\theta+\sin\theta)\sin^4\theta\,\mathrm d\theta.
$$

其中含 $\cos\theta$ 的部分关于 $\theta=\pi/2$ 反对称，积分为零；另一部分对称，所以

$$
I=\int_{\pi/4}^{\pi/2}\sin^5\theta\,\mathrm d\theta.
$$

**第三步：积分。** 令 $u=\cos\theta$，有

$$
\begin{aligned}
I&=\int_0^{\sqrt2/2}(1-u^2)^2\,\mathrm du\\
&=\left[u-\frac23u^3+\frac15u^5\right]_0^{\sqrt2/2}\\
&=\frac{\sqrt2}{2}-\frac{\sqrt2}{6}+\frac{\sqrt2}{40}
=\frac{43\sqrt2}{120}.
\end{aligned}
$$

**19.（2019.19）面积及极限如下。**

题目要求几何面积，必须积分绝对值：

$$
S_n=\int_0^{n\pi}\mathrm e^{-x}|\sin x|\,\mathrm dx.
$$

**第一步：计算每个半周期的面积。**

在第 $k$ 段 $[k\pi,(k+1)\pi]$ 上，令 $x=k\pi+t$，则

$$
|\sin(k\pi+t)|=\sin t,\qquad0\le t\le\pi.
$$

所以该段面积为

$$
\mathrm e^{-k\pi}\int_0^\pi\mathrm e^{-t}\sin t\,\mathrm dt.
$$

利用原函数 $-\frac12\mathrm e^{-t}(\sin t+\cos t)$，得到

$$
\int_0^\pi\mathrm e^{-t}\sin t\,\mathrm dt
=\frac12(1+\mathrm e^{-\pi}).
$$

**第二步：求等比数列和。**

记 $q=\mathrm e^{-\pi}\in(0,1)$，则

$$
\begin{aligned}
S_n&=\frac{1+q}{2}\sum_{k=0}^{n-1}q^k\\
&=\frac{1+q}{2(1-q)}(1-q^n)\\
&=\frac{\mathrm e^\pi+1}{2(\mathrm e^\pi-1)}(1-\mathrm e^{-n\pi}).
\end{aligned}
$$

因为 $q^n\to0$，所以

$$
\lim_{n\to\infty}S_n
=\frac{\mathrm e^\pi+1}{2(\mathrm e^\pi-1)}.
$$

**20.（2019.20）答案：$a=-\dfrac34$，$b=\dfrac34$。**

**第一步：把各阶导数写出。** 对 $u=v\mathrm e^{ax+by}$，有

$$
u_x=\mathrm e^{ax+by}(v_x+av),
\qquad
u_y=\mathrm e^{ax+by}(v_y+bv),
$$

$$
u_{xx}=\mathrm e^{ax+by}(v_{xx}+2av_x+a^2v),
$$

$$
u_{yy}=\mathrm e^{ax+by}(v_{yy}+2bv_y+b^2v).
$$

**第二步：代回并按导数阶数收集。** 约去不为零的指数因子，原方程变为

$$
\begin{aligned}
2v_{xx}-2v_{yy}
&+(4a+3)v_x+(-4b+3)v_y\\
&+[2a^2-2b^2+3a+3b]v=0.
\end{aligned}
$$

要消去全部一阶偏导数，必须且只需

$$
4a+3=0,\qquad -4b+3=0.
$$

因此 $a=-3/4$、$b=3/4$。

这时零阶项系数也恰好为零，方程进一步化为

$$
v_{xx}-v_{yy}=0.
$$

**21.（2019.21）两个存在性结论的证明。**

**（Ⅰ）先证明函数在内部有值大于 $1$。**

若 $f(x)\le1$ 对所有 $x\in[0,1]$ 都成立，由 $f(0)=0$ 和连续性，原点附近存在一小段区间，使 $f(x)$ 严格小于 $1$。

于是整个区间的积分必严格小于 $1$，与 $\int_0^1f(x)\,\mathrm dx=1$ 矛盾。因此存在 $c\in(0,1)$，使 $f(c)>1$。

$f$ 在闭区间连续，能取到最大值；而两端值分别为 $0,1$，都小于 $f(c)$，故最大值只能在某个内部点 $\xi$ 取得。

由费马定理，$f'(\xi)=0$，第一问得证。

**（Ⅱ）通过一个抛物线构造辅助函数。**

令

$$
g(x)=f(x)-(2x-x^2).
$$

则

$$
g(0)=g(1)=0,
$$

$$
\int_0^1g(x)\,\mathrm dx
=1-\int_0^1(2x-x^2)\,\mathrm dx
=1-\frac23=\frac13>0.
$$

因此存在 $c\in(0,1)$，使 $g(c)>0$。

分别在 $[0,c]$ 和 $[c,1]$ 上使用拉格朗日中值定理，存在 $u\in(0,c)$、$v\in(c,1)$，使

$$
g'(u)=\frac{g(c)}c>0,
\qquad
g'(v)=-\frac{g(c)}{1-c}<0.
$$

因为 $g$ 二阶可导，$g'$ 在 $[u,v]$ 连续、在 $(u,v)$ 可导。再次使用中值定理，存在 $\eta\in(u,v)$，使

$$
g''(\eta)=\frac{g'(v)-g'(u)}{v-u}<0.
$$

最后由 $g''=f''+2$，得到

$$
f''(\eta)<-2.
$$

各个中值点均位于 $(0,1)$ 内，结论的区间要求也满足。

**22.（2019.22）答案：$a\ne-1$；$\beta_3=\alpha_1-\alpha_2+\alpha_3$。**

**第一步：分别考察两组向量的秩。**

以两组向量为列组成矩阵 $U,V$。计算行列式，得

$$
\det U=1-a^2,\qquad \det V=2(a^2-1).
$$

例如 $U$ 中将第3行减去第1行的4倍，即可提出因子 $a^2-1$；$V$ 中作 $C_3\leftarrow C_3-C_1-C_2$，新第三列为 $(0,0,a^2-1)^{\mathrm T}$。

当 $a\ne\pm1$ 时，两组向量均为三维空间的一组基，所以等价。

**第二步：检查 $a=1$。**

此时 $U$ 的列都满足第三分量等于第一分量的4倍。由于 $\alpha_1,\alpha_2$ 线性无关，$U$ 的列空间就是二维平面

$$
z=4x.
$$

$V$ 的列也都在同一平面内，且其前两列线性无关。因此两组仍等价，$a=1$ 应保留。

**第三步：检查 $a=-1$。**

$U$ 的列空间仍为 $z=4x$，但

$$
\beta_1=(1,1,2)^{\mathrm T}
$$

不在该平面内，因此两组不等价。故参数范围为所有 $a\ne-1$。

**第四步：求线性表示。**

直接作向量运算：

$$
\alpha_1-\alpha_2=(0,1,0)^{\mathrm T}.
$$

加上 $\alpha_3=(1,2,a^2+3)^{\mathrm T}$，正好得到

$$
\beta_3=(1,3,a^2+3)^{\mathrm T}.
$$

所以统一可取 $\beta_3=\alpha_1-\alpha_2+\alpha_3$。

当 $a\ne\pm1$ 时，这是唯一表示；当 $a=1$ 时，表示不唯一，例如全部系数可以写为

$$
\beta_3=(3-2t)\alpha_1+(t-2)\alpha_2+t\alpha_3,
\qquad t\in\mathbb R.
$$

取 $t=1$ 即为上面所给的表示。

**23.（2019.23）答案：$x=3,y=-2$，一组 $P$ 如下。**

**（Ⅰ）利用特征值与迹确定参数。**

$A$ 为分块上三角矩阵，右下角的一阶块为 $-2$，所以 $-2$ 一定是 $A$ 的特征值。

与它相似的 $B$ 的特征值为 $2,-1,y$，故必须 $y=-2$。

再利用相似矩阵迹相等：

$$
\operatorname{tr}A=x-4,
\qquad \operatorname{tr}B=1+y=-1,
$$

得到 $x=3$。

此时 $A$ 的三个特征值确为 $2,-1,-2$，两矩阵的特征值一致且互异。

**（Ⅱ）按照目标矩阵的列关系构造 $P$。**

令 $P=(p_1,p_2,p_3)$。等式 $AP=PB$ 等价于

$$
Ap_1=2p_1,\qquad
Ap_2=p_1-p_2,\qquad
Ap_3=-2p_3.
$$

先解特征向量方程，可取

$$
p_1=(3,-6,0)^{\mathrm T},\qquad
p_3=(-1,2,4)^{\mathrm T}.
$$

对于第二列，需要解 $(A+I)p_2=p_1$，可取

$$
p_2=(-1,-1,0)^{\mathrm T}.
$$

于是

$$
P=\begin{pmatrix}3&-1&-1\\-6&-1&2\\0&0&4\end{pmatrix}.
$$

其行列式为 $-36\ne0$，故可逆。逐列检验：

$$
Ap_1=(6,-12,0)^{\mathrm T}=2p_1,
$$

$$
Ap_2=(4,-5,0)^{\mathrm T}=p_1-p_2,
$$

$$
Ap_3=(2,-4,-8)^{\mathrm T}=-2p_3.
$$

所以 $AP=PB$，左乘 $P^{-1}$ 得 $P^{-1}AP=B$，满足题目指定的目标矩阵。
