# 2009年考研数学二答案与解析

> 我的解答，未与官方核对。

## 答案速查

> 选择题：1.C　2.A　3.D　4.C　5.B　6.D　7.B　8.A。

> 填空题：9.$y=2x$；10.$-2$；11.$0$；12.$-3$；13.$\mathrm e^{-2/\mathrm e}$；14.$2$。

15. 极限为 $1/4$。

16. 不定积分表达式见正文。

17. 全微分及混合偏导表达式见正文。

18. $y=3x^2+2x$，体积为 $17\pi/6$。

19. 积分为 $-8/3$。

20. 负半轴曲线为 $\sqrt{\pi^2-x^2}$，非负半轴曲线为 $\pi\cos x+\sin x-x$。

21. 两个中值结论的证明见正文。

22. 全部向量及线性无关的证明见正文。

23. 特征值为 $a,a+1,a-2$；参数 $a=2$。

## 一、选择题（1～8）

**1.（2009.1）答案：C，共3个可去间断点。**

**第一步：找出全部可能的间断点。**

分母为零的条件是

$$
\sin\pi x=0\Longleftrightarrow x=k,\quad k\in\mathbb Z.
$$

非整数处，分子、分母连续且分母非零，所以函数连续。全部间断点都只能位于整数处。

**第二步：筛选可能具有有限极限的整数。**

分子分解为

$$
x-x^3=x(1-x)(1+x).
$$

因此分子仅在 $-1,0,1$ 处为零。若整数 $k$ 不属于这三个数，分子趋于非零常数而分母趋于零，函数在该点附近无界，不可能是可去间断点。

**第三步：分别求三个 $0/0$ 型极限。**

在 $k=-1,0,1$ 处，分母导数 $\pi\cos\pi k\ne0$，可用洛必达法则：

$$
\lim_{x\to k}\frac{x-x^3}{\sin\pi x}
=\frac{1-3k^2}{\pi\cos\pi k}.
$$

代入分别得到

$$
\lim_{x\to-1}f(x)=\frac2\pi,
\qquad\lim_{x\to0}f(x)=\frac1\pi,
\qquad\lim_{x\to1}f(x)=\frac2\pi.
$$

三个两侧极限都存在且有限，而原函数在这些点没有定义，因而三点均为可去间断点。故个数为3，选C。

分母在无穷多个点为零，并不意味着有无穷多个可去间断点；还必须检查分子是否同时消去分母的一阶零点。

**2.（2009.2）答案：A，$a=1,b=-1/6$。**

**第一步：根据无穷小的阶数确定 $a$。**

$x\to0$ 时，

$$
\sin ax=ax-\frac{a^3x^3}{6}+O(x^5),
$$

所以

$$
x-\sin ax=(1-a)x+\frac{a^3}{6}x^3+O(x^5).
$$

另一方面，对固定的 $b$，

$$
\ln(1-bx)=-bx+O(x^2),
$$

故

$$
x^2\ln(1-bx)=-bx^3+O(x^4).
$$

若 $b=0$，后一函数恒等于零，不能与题给的非零无穷小等价；因此需要 $b\ne0$。它的主项为三次项，前一函数的一次项就必须消失，即

$$
1-a=0\Longrightarrow a=1.
$$

**第二步：比较最低非零次项的系数。**

此时

$$
x-\sin x=\frac{x^3}{6}+O(x^5),
$$

所以两函数之比的极限为

$$
\lim_{x\to0}\frac{x-\sin x}{x^2\ln(1-bx)}
=\frac{1/6}{-b}.
$$

等价要求该极限为1，于是

$$
-\frac1{6b}=1\Longrightarrow\boxed{b=-\frac16}.
$$

取这组参数后，分母的对数为 $\ln(1+x/6)$，两侧在足够小的零点邻域内都有定义，且两函数共同的主项为 $x^3/6$，确实等价。

**3.（2009.3）答案：D，原点是极小值点。**

**第一步：从全微分读出两个偏导数。**

全微分的一般形式是

$$
\mathrm dz=f_x\,\mathrm dx+f_y\,\mathrm dy.
$$

与题式比较得

$$
f_x=x,\qquad f_y=y.
$$

对第一式关于 $x$ 积分时，积分常数可以依赖于 $y$，因此

$$
f(x,y)=\frac{x^2}{2}+\varphi(y).
$$

再对 $y$ 求偏导，并与 $f_y=y$ 比较，得到 $\varphi'(y)=y$，从而

$$
f(x,y)=\frac{x^2+y^2}{2}+C.
$$

**第二步：直接比较原点附近的函数值。**

原点处 $f(0,0)=C$。对任何非零点 $(x,y)$，

$$
f(x,y)-f(0,0)=\frac{x^2+y^2}{2}>0.
$$

所以原点是严格极小值点，在整个给定表达式的定义域内也取得最小值，选D。

也可以用二阶导数判别：$f_{xx}=f_{yy}=1$、$f_{xy}=0$，海塞矩阵为单位矩阵，正定。但只知道 $f_x(0,0)=f_y(0,0)=0$ 只能说明原点是驻点，不能单独推出极小值。

**4.（2009.4）答案：C。**

**第一步：把第一个累次积分写成区域条件。**

第一个积分对应

$$
D_1=\{(x,y):1\le x\le2,\ x\le y\le2\}.
$$

这等价于 $1\le x\le y\le2$。固定 $y$ 时，$y$ 的范围为 $[1,2]$，而 $x$ 从1变化到 $y$，所以换序后为

$$
\int_1^2\mathrm dx\int_x^2f(x,y)\,\mathrm dy
=\int_1^2\mathrm dy\int_1^y f(x,y)\,\mathrm dx.
$$

**第二步：把两段内积分接起来。**

第二个积分已经以 $y$ 为外变量，区域为

$$
D_2=\{(x,y):1\le y\le2,\ y\le x\le4-y\}.
$$

对同一个 $y\in[1,2]$，两段 $x$ 区间分别为 $[1,y]$ 和 $[y,4-y]$。由于 $y\le4-y$，它们首尾相接，内部不重叠。

因此两积分之和为

$$
\begin{aligned}
&\int_1^2\mathrm dy\left[
\int_1^y f(x,y)\,\mathrm dx
+\int_y^{4-y}f(x,y)\,\mathrm dx\right]\\
&\qquad=\boxed{\int_1^2\mathrm dy\int_1^{4-y}f(x,y)\,\mathrm dx}.
\end{aligned}
$$

这正是C。换序时改变的是同一区域的描述方式；不能只交换 $\mathrm dx,\mathrm dy$ 而保留原上下限。

令 $f\equiv1$ 可作面积检查：第一块面积为 $1/2$，第二块为1，合并区域面积为 $3/2$，与上式一致。

**5.（2009.5）答案：B，无极值点、有零点。**

**第一步：从曲率圆得到切线方向。**

曲率圆的圆心是原点，与曲线在 $(1,1)$ 处具有相同切线。连接圆心和切点的半径方向为 $(1,1)$，切线与半径垂直，所以

$$
f(1)=1,\qquad f'(1)=-1.
$$

**第二步：由曲率中心的位置确定二阶导数。**

曲线 $y=f(x)$ 的曲率中心纵坐标为

$$
y_c=f(x)+\frac{1+[f'(x)]^2}{f''(x)}.
$$

本题在 $(1,1)$ 处 $y_c=0$，故

$$
0=1+\frac{1+(-1)^2}{f''(1)}
=1+\frac2{f''(1)}.
$$

于是

$$
f''(1)=-2<0.
$$

曲率圆不仅给出曲率大小，也通过圆心所在方向确定了弯曲的符号。

**第三步：利用二阶导数不变号控制 $f'$。**

由 $f''(1)<0$ 且 $f''$ 不变号，知相关区间内 $f''(x)\le0$，因而 $f'$ 单调不增。

还需要一个严格的不等式。由二阶导数在1处的定义，

$$
\lim_{h\to0^+}\frac{f'(1+h)-f'(1)}h=-2<0,
$$

所以对充分小的 $h>0$，有 $f'(1+h)<f'(1)=-1$。再结合 $f'$ 单调不增，就得到整个 $(1,2)$ 上

$$
f'(x)<-1<0.
$$

这一推理只使用 $f''(1)$ 存在，不需要假定 $f''$ 连续。

**第四步：分别判断极值点和零点。**

$f'$ 在 $(1,2)$ 内始终为负，因此 $f$ 严格递减，没有极值点。

在 $[1,2]$ 上使用拉格朗日中值定理，存在 $c\in(1,2)$，使

$$
f(2)-f(1)=f'(c)(2-1)<-1.
$$

由 $f(1)=1$ 得 $f(2)<0$。函数连续，两端一正一负，由介值定理，$(1,2)$ 内至少有一个零点；严格递减又保证零点唯一。因此选B。

**6.（2009.6）答案：D。**

**第一步：确定原点函数值和负半轴上的线段。**

不论 $f(0)$ 取何值，定积分上下限相同时都有

$$
F(0)=\int_0^0 f(t)\,\mathrm dt=0.
$$

原图在 $[-1,0)$ 上给出 $f(t)=1$。因此对 $-1\le x<0$，

$$
F(x)=\int_0^x f(t)\,\mathrm dt
=-\int_x^0 1\,\mathrm dt=x.
$$

所以左侧必须是从 $(-1,-1)$ 到 $(0,0)$ 的斜率为1的线段。这排除了左侧斜率为负的A，也排除了不经过原点的C。

**第二步：由 $f$ 的符号判断 $F$ 的增减。**

在 $f$ 连续的各开区间，变上限积分求导给出 $F'(x)=f(x)$。

在 $(0,1)$ 上，原图位于 $x$ 轴下方，$f<0$，所以 $F$ 递减。

在 $(1,2)$ 上，原图位于 $x$ 轴上方，$f>0$，所以 $F$ 递增。因此 $x=1$ 是 $F$ 的极小值点。

在 $(2,3)$ 上，$f=0$，所以 $F$ 保持常数。其值应为此前累计的积分 $F(2)$，不因 $f$ 变为零而自动变成零。

**第三步：检查跳跃处的连续性。**

原图中的 $f$ 有界且分段连续。若 $|f|\le M$，则

$$
|F(x+h)-F(x)|
=\left|\int_x^{x+h}f(t)\,\mathrm dt\right|
\le M|h|\longrightarrow0.
$$

因此 $F$ 在整个区间上连续，包括 $f$ 发生跳跃的地方。B在 $x=2$ 处把累计值突然降到0，与此不符。

D同时满足经过原点、左侧为 $F=x$、先减后增、最后连续接上水平线段这四项要求，故选D。

原函数图中某一个端点取实心还是空心，不改变对应定积分的值；识图时要区分被积函数的跳跃与积分函数的连续性。

**7.（2009.7）答案：B。**

记

$$
M=\begin{pmatrix}O&A\\B&O\end{pmatrix}.
$$

**第一步：计算四阶分块矩阵的行列式。**

把后两行整体移到前两行前面，相当于每一行跨过两行，共产生 $2\times2=4$ 次行交换，符号不变。交换后成为分块对角矩阵，故

$$
|M|=(-1)^4\left|\begin{matrix}B&O\\O&A\end{matrix}\right|
=|B|\,|A|=3\times2=6.
$$

$|A|,|B|$ 均非零，所以各自可逆，$M$ 也可逆。

**第二步：按分块乘法求逆矩阵。**

验证

$$
\begin{pmatrix}O&A\\B&O\end{pmatrix}
\begin{pmatrix}O&B^{-1}\\A^{-1}&O\end{pmatrix}
=\begin{pmatrix}I&O\\O&I\end{pmatrix}.
$$

反向相乘也得到单位矩阵。因此

$$
M^{-1}=\begin{pmatrix}O&B^{-1}\\A^{-1}&O\end{pmatrix}.
$$

注意右上角对应 $B^{-1}$，左下角对应 $A^{-1}$，位置须由矩阵乘法确定。

**第三步：用逆矩阵换成伴随矩阵。**

对可逆矩阵有 $M^*=|M|M^{-1}$，又

$$
A^{-1}=\frac{A^*}{2},\qquad B^{-1}=\frac{B^*}{3}.
$$

所以

$
M^*=6\begin{pmatrix}O&B^{-1}\\A^{-1}&O\end{pmatrix}
=\boxed{\begin{pmatrix}O&2B^*\\3A^*&O\end{pmatrix}}.
$

这与B相同。将结果与 $M$ 相乘，两个对角块分别为 $3AA^*=6I$、$2BB^*=6I$，也符合 $MM^*=|M|I$。

**8.（2009.8）答案：A。**

**第一步：用列变换矩阵表示 $Q$。**

$Q$ 的三列在 $P$ 的列向量下的坐标依次为 $(1,1,0)^{\mathrm T}$、$(0,1,0)^{\mathrm T}$、$(0,0,1)^{\mathrm T}$。

因此

$$
Q=PS,\qquad
S=\begin{pmatrix}1&0&0\\1&1&0\\0&0&1\end{pmatrix}.
$$

右乘 $S$ 将第2列加到第1列，其余列保持原样。

**第二步：把已知的合同关系代入。**

记 $D=P^{\mathrm T}AP=\operatorname{diag}(1,1,2)$，则

$$
Q^{\mathrm T}AQ=(PS)^{\mathrm T}A(PS)
=S^{\mathrm T}DS.
$$

先算右边的乘积，

$$
DS=\begin{pmatrix}1&0&0\\1&1&0\\0&0&2\end{pmatrix},
$$

再左乘 $S^{\mathrm T}$，得到

$$
\boxed{Q^{\mathrm T}AQ
=\begin{pmatrix}1&1&0\\0&1&0\\0&0&1\end{pmatrix}
\begin{pmatrix}1&0&0\\1&1&0\\0&0&2\end{pmatrix}
=\begin{pmatrix}2&1&0\\1&1&0\\0&0&2\end{pmatrix}}.
$$

故选A。

还可用二次型展开检查左上角的系数。若新坐标为 $z$，则旧坐标为 $Sz=(z_1,z_1+z_2,z_3)^{\mathrm T}$，原二次型变为

$$
z_1^2+(z_1+z_2)^2+2z_3^2
=2z_1^2+2z_1z_2+z_2^2+2z_3^2.
$$

## 二、填空题（9～14）

**9.（2009.9）答案：$y=2x$。**

**第一步：确定原点对应的参数。**

由于 $\mathrm e^{-u^2}>0$，函数

$$
H(v)=\int_0^v\mathrm e^{-u^2}\,\mathrm du
$$

严格递增，而且只有在 $v=0$ 时等于零。因此 $x=0$ 等价于 $1-t=0$，即 $t=1$。代入另一参数方程，得到 $y=\ln1=0$，确实对应题给原点。

**第二步：分别对参数 $t$ 求导。**

变上限积分求导还要乘以上限的导数，所以

$$
\frac{\mathrm dx}{\mathrm dt}
=\mathrm e^{-(1-t)^2}\frac{\mathrm d(1-t)}{\mathrm dt}
=-\mathrm e^{-(1-t)^2}.
$$

对 $y$ 使用乘积与复合函数求导法则：

$$
\frac{\mathrm dy}{\mathrm dt}
=2t\ln(2-t^2)-\frac{2t^3}{2-t^2}.
$$

在 $t=1$ 处，$x'(1)=-1\ne0$，$y'(1)=-2$。于是参数曲线可在该点附近看成 $y$ 关于 $x$ 的函数，切线斜率为

$$
k=\left.\frac{y'(t)}{x'(t)}\right|_{t=1}
=\frac{-2}{-1}=2.
$$

切线过原点，故其方程为 $\boxed{y=2x}$。参数值应由两条方程共同核实，不能仅从 $y=0$ 就任取一个 $t$。

**10.（2009.10）答案：$k=-2$。**

**第一步：先判断反常积分何时收敛。**

被积函数关于 $x$ 为偶函数，且非负，故原积分收敛时有

$$
\int_{-\infty}^{+\infty}\mathrm e^{k|x|}\,\mathrm dx
=2\int_0^{+\infty}\mathrm e^{kx}\,\mathrm dx.
$$

若 $k=0$，被积函数恒为1，积分发散；若 $k>0$，被积函数至少为1，积分同样发散。因此必须有 $k<0$。

**第二步：在收敛范围内计算积分。**

当 $k<0$ 时，$\mathrm e^{kR}\to0$，所以

$$
\begin{aligned}
2\int_0^{+\infty}\mathrm e^{kx}\,\mathrm dx
&=2\lim_{R\to+\infty}\left[\frac{\mathrm e^{kx}}k\right]_0^R\\
&=\frac2k(0-1)=-\frac2k.
\end{aligned}
$$

根据积分值为1，解得

$$
-\frac2k=1\Longrightarrow\boxed{k=-2}.
$$

所得参数满足 $k<0$，且代回后两条半轴各贡献 $1/2$，总积分恰为1。

**11.（2009.11）答案：$0$。**

**第一步：用分部积分把振荡项转化为 $1/n$ 因子。**

记原积分为 $I_n$。取 $u=\mathrm e^{-x}$、$\mathrm dv=\sin(nx)\,\mathrm dx$，则

$$
\mathrm du=-\mathrm e^{-x}\,\mathrm dx,
\qquad v=-\frac{\cos(nx)}n.
$$

由 $\int u\,\mathrm dv=uv-\int v\,\mathrm du$，得到

$$
I_n=\left[-\frac{\mathrm e^{-x}\cos(nx)}n\right]_0^1
-\frac1n\int_0^1\mathrm e^{-x}\cos(nx)\,\mathrm dx.
$$

**第二步：分别估计边界项与积分项。**

利用 $|\cos(nx)|\le1$，有

$$
\begin{aligned}
|I_n|
&\le\frac{1+\mathrm e^{-1}}n
+\frac1n\int_0^1\mathrm e^{-x}\,\mathrm dx\\
&=\frac{1+\mathrm e^{-1}+1-\mathrm e^{-1}}n
=\frac2n.
\end{aligned}
$$

右端趋于零，由夹逼定理，$\boxed{\lim_{n\to\infty}I_n=0}$。

这里 $\sin(nx)$ 一般没有随 $n$ 趋于无穷的点态极限，不能把它在积分号内直接换成零。积分趋零来自振荡抵消，分部积分给出了这种抵消的定量估计。

**12.（2009.12）答案：$-3$。**

**第一步：确定零点处的函数值与可导条件。**

令 $x=0$，题给方程变为 $\mathrm e^y=1$，故 $y(0)=0$。设

$$
F(x,y)=xy+\mathrm e^y-x-1.
$$

则 $F_y=x+\mathrm e^y$，在 $(0,0)$ 处等于1，非零。因此方程在该点附近确定光滑隐函数，可以连续求两次导数。

**第二步：求一阶导数，并代入零点。**

对 $xy+\mathrm e^y=x+1$ 两边关于 $x$ 求导，注意 $y=y(x)$：

$$
y+xy'+\mathrm e^y y'=1,
\qquad y+(x+\mathrm e^y)y'=1.
$$

代入 $x=0,y=0$，得到 $y'(0)=1$。

**第三步：再求一次导数。**

乘积项的导数为

$$
\frac{\mathrm d}{\mathrm dx}\bigl[(x+\mathrm e^y)y'\bigr]
=(1+\mathrm e^y y')y'+(x+\mathrm e^y)y''.
$$

因此一阶导数等式继续求导后化为

$$
2y'+\mathrm e^y(y')^2+(x+\mathrm e^y)y''=0.
$$

在零点代入 $y(0)=0,y'(0)=1$：

$$
2\cdot1+1\cdot1^2+1\cdot y''(0)=0.
$$

故 $\boxed{y''(0)=-3}$。其中 $(y')^2$ 来自 $\mathrm e^y y'$ 的乘积求导，不能遗漏。

**13.（2009.13）答案：$\mathrm e^{-2/\mathrm e}$。**

**第一步：取对数，化简幂指函数的求极值问题。**

对 $x>0$，$y=x^{2x}>0$。设

$$
h(x)=\ln y=2x\ln x.
$$

由于指数函数严格递增，$h$ 与 $y=\mathrm e^h$ 在同一点取得最小值。

**第二步：求驻点并判断两侧单调性。**

$$
h'(x)=2(1+\ln x),
\qquad h'(x)=0\Longleftrightarrow x=\frac1{\mathrm e}.
$$

在 $(0,1/\mathrm e)$ 上，$\ln x<-1$，故 $h'<0$；在 $(1/\mathrm e,1]$ 上，$h'>0$。于是 $h$ 先严格递减、后严格递增，在 $x=1/\mathrm e$ 取得全区间唯一最小值。

**第三步：代回原函数求最小值。**

$$
h\left(\frac1{\mathrm e}\right)=-\frac2{\mathrm e},
\qquad
\boxed{y_{\min}=\mathrm e^{-2/\mathrm e}}.
$$

还可核对两端：$y(1)=1$，而 $x\ln x\to0$ 给出 $\lim_{x\to0^+}y=1$。两端值或极限均大于所得最小值；题目左端点虽未取到，不影响内部最小值的存在。

**14.（2009.14）答案：$2$。**

**第一步：利用相似不变量求外积矩阵的迹。**

相似矩阵具有相同的特征多项式与迹。由题给对角矩阵可知

$$
\operatorname{tr}(\alpha\beta^{\mathrm T})=2+0+0=2.
$$

**第二步：把矩阵的迹写成所求的内积。**

记 $\alpha=(a_1,a_2,a_3)^{\mathrm T}$、$\beta=(b_1,b_2,b_3)^{\mathrm T}$。外积矩阵的元素为

$$
\alpha\beta^{\mathrm T}
=\begin{pmatrix}
a_1b_1&a_1b_2&a_1b_3\\
a_2b_1&a_2b_2&a_2b_3\\
a_3b_1&a_3b_2&a_3b_3
\end{pmatrix}.
$$

所以

$$
\operatorname{tr}(\alpha\beta^{\mathrm T})
=a_1b_1+a_2b_2+a_3b_3
=\beta^{\mathrm T}\alpha.
$$

从而 $\boxed{\beta^{\mathrm T}\alpha=2}$。注意 $\alpha\beta^{\mathrm T}$ 是三阶矩阵，而 $\beta^{\mathrm T}\alpha$ 是一个标量，两者通过取迹建立联系。

## 三、解答题（15～23）

**15.（2009.15）答案：$1/4$。**

**第一步：把乘积中各部分所需的阶数分开。**

分母 $\sin^4x$ 是四阶无穷小，$1-\cos x$ 是二阶无穷小。因此需要把差式 $x-\ln(1+\tan x)$ 算到二阶。将原式写成

$$
\frac{1-\cos x}{x^2}
\cdot\frac{x-\ln(1+\tan x)}{x^2}
\cdot\left(\frac{x}{\sin x}\right)^4.
$$

第一、第三个因子的极限分别为 $1/2$ 和1，关键是中间因子。

**第二步：展开复合对数，保留相减后剩下的主项。**

当 $x\to0$ 时，

$$
\tan x=x+O(x^3),
\qquad \ln(1+t)=t-\frac{t^2}{2}+O(t^3).
$$

代入 $t=\tan x$，得到

$$
\begin{aligned}
\ln(1+\tan x)
&=\tan x-\frac{\tan^2x}{2}+O(x^3)\\
&=x-\frac{x^2}{2}+O(x^3).
\end{aligned}
$$

于是

$$
x-\ln(1+\tan x)=\frac{x^2}{2}+O(x^3),
$$

中间因子的极限为 $1/2$。故

$$
\boxed{\lim_{x\to0}\frac{(1-\cos x)[x-\ln(1+\tan x)]}{\sin^4x}
=\frac12\cdot\frac12\cdot1=\frac14}.
$$

**另一种求中间因子的方法：一次洛必达。**

分子、分母都趋于零，在零点附近 $1+\tan x>0$，函数有定义且可导。因此

$$
\begin{aligned}
\lim_{x\to0}\frac{x-\ln(1+\tan x)}{x^2}
&=\lim_{x\to0}\frac{1-\sec^2x/(1+\tan x)}{2x}\\
&=\lim_{x\to0}\frac{\tan x}{2x}
\frac{1-\tan x}{1+\tan x}
=\frac12.
\end{aligned}
$$

差式中两个一阶主项恰好抵消，若直接用 $\ln(1+\tan x)\sim x$ 后相减，就会丢掉决定答案的二阶项。

**16.（2009.16）不定积分。**

**第一步：拆开对数，准备分部积分。**

在 $x>0$ 的条件下，所有根式和对数均有定义。记

$$
S=\sqrt{x}+\sqrt{x+1},\qquad R=\sqrt{x(x+1)},
$$

以及

$$
L=\ln\left(1+\sqrt{\frac{x+1}{x}}\right)
=\ln S-\frac12\ln x.
$$

利用根式求导，

$$
\begin{aligned}
\frac{\mathrm d}{\mathrm dx}\ln S
&=\frac{\frac1{2\sqrt{x}}+\frac1{2\sqrt{x+1}}}
{\sqrt{x}+\sqrt{x+1}}\\
&=\frac1{2\sqrt{x(x+1)}}=\frac1{2R}.
\end{aligned}
$$

所以 $L'=1/(2R)-1/(2x)$。

**第二步：使用分部积分，把对数积分化为根式积分。**

取 $u=L$、$\mathrm dv=\mathrm dx$，则

$$
\begin{aligned}
I=\int L\,\mathrm dx
&=xL-\int xL'\,\mathrm dx\\
&=xL-\frac12\int\frac{x}{R}\,\mathrm dx+\frac x2.
\end{aligned}
$$

**第三步：计算剩余的根式积分。**

由 $R^2=x^2+x$，得

$$
R'=\frac{2x+1}{2R},
\qquad \frac{x}{R}=R'-\frac1{2R}.
$$

再利用第一步得到的 $(\ln S)'=1/(2R)$，可知

$$
\int\frac{x}{R}\,\mathrm dx=R-\ln S+C.
$$

代回并将常数统一，原积分为

$$
\begin{aligned}
I={}&x\ln\left(1+\sqrt{\frac{x+1}{x}}\right)\\
&+\frac12\left[x-\sqrt{x(x+1)}
+\ln\bigl(\sqrt{x}+\sqrt{x+1}\bigr)\right]+C.
\end{aligned}
$$

**第四步：对所得原函数求导核验。**

用 $L,S,R$ 的简写，所得原函数为

$$
F=xL+\frac x2-\frac R2+\frac12\ln S.
$$

由于 $R'-(\ln S)'=x/R$，有

$$
\begin{aligned}
F'&=L+x\left(\frac1{2R}-\frac1{2x}\right)
+\frac12-\frac{x}{2R}\\
&=L.
\end{aligned}
$$

因此求导后正好回到原被积函数，积分结果成立。

**17.（2009.17）全微分与混合偏导数。**

**第一步：明确中间变量和偏导符号。**

令

$$
u=x+y,\qquad v=x-y,\qquad w=xy,
\qquad z=f(u,v,w).
$$

以下 $f_u,f_{uv}$ 等都表示 $f$ 对自身三个自变量的偏导数，并在 $(u,v,w)=(x+y,x-y,xy)$ 处取值。

三个中间变量的微分为

$$
\mathrm du=\mathrm dx+\mathrm dy,
\quad\mathrm dv=\mathrm dx-\mathrm dy,
\quad\mathrm dw=y\,\mathrm dx+x\,\mathrm dy.
$$

**第二步：用全微分的链式法则。**

$$
\begin{aligned}
\mathrm dz
&=f_u\,\mathrm du+f_v\,\mathrm dv+f_w\,\mathrm dw\\
&=(f_u+f_v+yf_w)\,\mathrm dx
+(f_u-f_v+xf_w)\,\mathrm dy.
\end{aligned}
$$

因而

$$
\boxed{\mathrm dz=(f_u+f_v+yf_w)\,\mathrm dx
+(f_u-f_v+xf_w)\,\mathrm dy}.
$$

**第三步：将 $z_x$ 的每一项对 $y$ 求导。**

先由上式读出 $z_x=f_u+f_v+yf_w$。对 $y$ 求偏导时，$u_y=1,v_y=-1,w_y=x$，所以

$$
\frac{\partial f_u}{\partial y}=f_{uu}-f_{uv}+xf_{uw},
$$

$$
\frac{\partial f_v}{\partial y}=f_{vu}-f_{vv}+xf_{vw}.
$$

第三项还要使用乘积法则：

$$
\frac{\partial(yf_w)}{\partial y}
=f_w+y(f_{wu}-f_{wv}+xf_{ww}).
$$

这里单独出现的 $f_w$ 来自系数 $y$ 的导数，不能只对 $f_w$ 作链式求导。

**第四步：利用二阶连续偏导合并同类项。**

题设保证混合偏导可以交换次序，即

$$
f_{uv}=f_{vu},\qquad f_{uw}=f_{wu},\qquad f_{vw}=f_{wv}.
$$

相加后，$-f_{uv}+f_{vu}$ 抵消；$f_{uw}$ 的系数为 $x+y$，$f_{vw}$ 的系数为 $x-y$。最终

$$
\boxed{\begin{aligned}
\frac{\partial^2z}{\partial x\partial y}
={}&f_{uu}-f_{vv}+(x+y)f_{uw}\\
&+(x-y)f_{vw}+xyf_{ww}+f_w.
\end{aligned}}
$$

若先对 $y$ 求导，再对 $x$ 求导，会得到相同结果；二阶连续偏导条件正是交换这两次求导的依据。

**18.（2009.18）答案：$y=3x^2+2x$，$V=17\pi/6$。**

**第一步：降阶求解微分方程。**

方程不显含 $y$，令 $p=y'$，在 $x>0$ 上有

$$
xp'-p+2=0,
\qquad xp'-p=-2.
$$

除以 $x^2$，左边可以写成商的导数：

$$
\left(\frac px\right)'=-\frac2{x^2}.
$$

两边积分，得到

$$
\frac px=\frac2x+C_1,
\qquad p=2+C_1x.
$$

再次积分，并把 $C_1/2$ 记为 $a$，有

$$
y=ax^2+2x+b.
$$

由于求解中除以了 $x$，先在 $x>0$ 上推导，再用曲线经过原点、在零点连续的条件确定 $b=0$。

**第二步：用面积条件确定剩余常数。**

题设 $y$ 非负，区域 $D$ 位于 $0\le x\le1$、$0\le y\le y(x)$。因此面积直接等于曲线下方积分：

$$
2=\int_0^1(ax^2+2x)\,\mathrm dx
=\frac a3+1.
$$

解得 $a=3$，所以

$$
\boxed{y=3x^2+2x}.
$$

这条曲线在 $x\ge0$ 上非负，并且

$$
x\cdot6-(6x+2)+2=0,
$$

满足原微分方程。

**第三步：用圆柱壳法计算绕 $y$ 轴的体积。**

在位置 $x$ 取竖直窄条，绕 $y$ 轴后成为薄圆柱壳：半径为 $x$，高度为 $3x^2+2x$，厚度为 $\mathrm dx$。故

$$
\begin{aligned}
V&=2\pi\int_0^1x(3x^2+2x)\,\mathrm dx\\
&=2\pi\left[\frac{3x^4}{4}+\frac{2x^3}{3}\right]_0^1\\
&=2\pi\left(\frac34+\frac23\right)
=\boxed{\frac{17\pi}{6}}.
\end{aligned}
$$

**另解：用水平截面的圆环面积核对。**

曲线在 $[0,1]$ 上严格递增，纵坐标范围为 $0\le y\le5$，反函数为

$$
x(y)=\frac{\sqrt{1+3y}-1}{3}.
$$

固定 $y$ 时，区域的横坐标从 $x(y)$ 到1，旋转截面是外半径1、内半径 $x(y)$ 的圆环。因此

$$
V=\pi\int_0^5\bigl[1-x(y)^2\bigr]\,\mathrm dy.
$$

换回 $y=3x^2+2x$，$\mathrm dy=(6x+2)\,\mathrm dx$，得到

$$
\begin{aligned}
V&=\pi\int_0^1(1-x^2)(6x+2)\,\mathrm dx\\
&=\pi\left[3x^2+2x-\frac32x^4-\frac23x^3\right]_0^1
=\frac{17\pi}{6}.
\end{aligned}
$$

两种截取方式给出相同体积。

**19.（2009.19）答案：$-8/3$。**

**第一步：平移圆心，同时改写直线条件。**

令 $u=x-1,v=y-1$，则 $\mathrm dx\,\mathrm dy=\mathrm du\,\mathrm dv$，区域变为

$$
u^2+v^2\le2,\qquad v\ge u.
$$

这是以原点为圆心、半径为 $\sqrt2$ 的半圆。被积函数为

$$
x-y=(u+1)-(v+1)=u-v.
$$

原坐标的圆心并不在原点，先平移后再用极坐标可以使积分限更简单。

**第二步：确定极坐标范围。**

令 $u=r\cos\theta,v=r\sin\theta$。圆给出 $0\le r\le\sqrt2$；半平面条件给出

$$
\sin\theta-\cos\theta\ge0
\Longleftrightarrow
\sin\left(\theta-\frac\pi4\right)\ge0.
$$

因此可选连续的角度区间

$$
\frac\pi4\le\theta\le\frac{5\pi}{4}.
$$

被积函数 $u-v=r(\cos\theta-\sin\theta)$，面积元还要乘 $r$，所以径向因子是 $r^2$。

**第三步：分别计算角度积分与径向积分。**

$$
\begin{aligned}
I&=\int_{\pi/4}^{5\pi/4}\int_0^{\sqrt2}
 r^2(\cos\theta-\sin\theta)\,\mathrm dr\,\mathrm d\theta\\
&=\left[\frac{r^3}{3}\right]_0^{\sqrt2}
\left[\sin\theta+\cos\theta\right]_{\pi/4}^{5\pi/4}\\
&=\frac{2\sqrt2}{3}\,(-\sqrt2-\sqrt2)
=\boxed{-\frac83}.
\end{aligned}
$$

区域内始终有 $x-y\le0$，且内部大部分点严格小于零，积分应为负，所得符号符合这一检查。

**另一种坐标选择：让直线边界变为坐标轴。**

设

$$
s=\frac{y-x}{\sqrt2},\qquad t=\frac{x+y-2}{\sqrt2}.
$$

这是一种平移后正交变换，面积伸缩因子的绝对值为1。此时

$$
s^2+t^2\le2,\quad s\ge0,\qquad x-y=-\sqrt2s.
$$

先对 $t$ 积分，得到

$$
\begin{aligned}
I&=-\sqrt2\int_0^{\sqrt2}s
\int_{-\sqrt{2-s^2}}^{\sqrt{2-s^2}}\mathrm dt\,\mathrm ds\\
&=-2\sqrt2\int_0^{\sqrt2}s\sqrt{2-s^2}\,\mathrm ds\\
&=-2\sqrt2\left[-\frac{(2-s^2)^{3/2}}3\right]_0^{\sqrt2}
=-\frac83.
\end{aligned}
$$

**20.（2009.20）分段曲线的表达式。**

**第一步：将负半轴的几何条件写成微分方程。**

在点 $(x,y)$ 处，曲线的切向量可取 $(1,y')$。法线经过原点，说明从原点指向该点的向量 $(x,y)$ 与切向量垂直，因此

$$
(x,y)\cdot(1,y')=0,
\qquad x+yy'=0.
$$

左边是 $(x^2+y^2)/2$ 的导数，故在 $-\pi<x<0$ 上

$$
x^2+y^2=C.
$$

给定点的两个坐标平方均为 $\pi^2/2$，所以 $C=\pi^2$。

**第二步：根据经过的点选择圆的正确分支。**

在 $-\pi<x<0$ 内，$\pi^2-x^2>0$，故 $y$ 不可能为零。曲线连续，而且经过的点纵坐标为正，因此整个这一段都应取正支：

$$
y=\sqrt{\pi^2-x^2},\qquad-\pi<x<0.
$$

它的一阶导数为

$$
y'=-\frac{x}{\sqrt{\pi^2-x^2}}.
$$

于是左端逼近零点时，函数值与斜率分别趋于

$$
\lim_{x\to0^-}y(x)=\pi,
\qquad \lim_{x\to0^-}y'(x)=0.
$$

**第三步：用光滑连接确定非负半轴的初值。**

两段曲线在零点应连续且具有相同切线，故右侧方程的初值为

$$
y(0)=\pi,\qquad y'(0)=0.
$$

这些是函数值和一阶导数的拼接条件，不需要另加二阶导数连续的条件。

**第四步：解右侧常系数微分方程。**

齐次方程 $y''+y=0$ 的特征根为 $\pm\mathrm i$，齐次通解是

$$
y_h=C_1\cos x+C_2\sin x.
$$

非齐次项为 $-x$，取特解 $y_p=-x$ 即有 $y_p''+y_p=-x$。因此

$$
y=C_1\cos x+C_2\sin x-x.
$$

代入 $y(0)=\pi$，得 $C_1=\pi$。再由

$$
y'=-C_1\sin x+C_2\cos x-1
$$

和 $y'(0)=0$，得到 $C_2=1$。最终

$$
\boxed{y(x)=\begin{cases}
\sqrt{\pi^2-x^2},&-\pi<x<0,\\
\pi\cos x+\sin x-x,&0\le x<\pi.
\end{cases}}
$$

左段满足 $x+yy'=0$，故其法线过原点；右段代入 $y''+y+x$ 后恒为零。两段在零点的函数值均为 $\pi$、一阶导数均为0，满足所需连接条件。

**21.（2009.21）中值定理及右导数结论的证明。**

**（Ⅰ）由罗尔定理证明拉格朗日中值定理。**

**第一步：减去端点连线，构造两端等值的函数。**

设端点连线的斜率为

$$
k=\frac{f(b)-f(a)}{b-a}.
$$

取辅助函数

$$
F(x)=f(x)-f(a)-k(x-a).
$$

其中 $f(a)+k(x-a)$ 恰是经过 $(a,f(a))$、$(b,f(b))$ 的直线。减去这条直线后，两端函数值都变为零：

$$
F(a)=0,
\qquad F(b)=f(b)-f(a)-k(b-a)=0.
$$

**第二步：逐项核实罗尔定理的条件。**

$f$ 在 $[a,b]$ 连续，在 $(a,b)$ 可导，线性函数处处连续可导，所以 $F$ 同样在闭区间连续、开区间可导，又有 $F(a)=F(b)$。

因此由罗尔定理，存在 $\xi\in(a,b)$，使

$$
F'(\xi)=f'(\xi)-k=0.
$$

代回 $k$ 的定义并乘以 $b-a$，得到

$$
\boxed{f(b)-f(a)=f'(\xi)(b-a)}.
$$

**（Ⅱ）由导数的右极限证明右导数存在。**

**第一步：在从零点出发的小区间上使用第一问。**

任取 $h$ 满足 $0<h<\delta$。$f$ 在0处连续，在 $(0,\delta)$ 可导，因而在 $[0,h]$ 连续、在 $(0,h)$ 可导，符合第一问的条件。

于是存在 $\xi_h\in(0,h)$，使

$$
f(h)-f(0)=f'(\xi_h)h,
\qquad \frac{f(h)-f(0)}h=f'(\xi_h).
$$

**第二步：说明中值点也从右侧趋于零。**

当 $h\to0^+$ 时，$0<\xi_h<h$。由夹逼定理，$\xi_h\to0^+$。结合已知的导数极限，有

$$
f'(\xi_h)\longrightarrow A.
$$

所以差商的右极限存在，且

$$
\boxed{f'_+(0)=\lim_{h\to0^+}\frac{f(h)-f(0)}h=A}.
$$

对不同的 $h$，中值点 $\xi_h$ 可以不同；只要始终位于 $(0,h)$，就足以推出趋零。整个论证只在开区间使用可导性，没有预先假定 $f$ 在0处具有右导数。

**22.（2009.22）全部解向量及线性无关的证明。**

**（Ⅰ）第一步：求满足 $A\xi_2=\xi_1$ 的全部向量。**

设 $\xi_2=(x_1,x_2,x_3)^{\mathrm T}$。矩阵方程展开为

$$
\begin{cases}
x_1-x_2-x_3=-1,\\
-x_1+x_2+x_3=1,\\
-4x_2-2x_3=-2.
\end{cases}
$$

第二个方程是第一个的相反数，第三个约去 $-2$，只剩两个独立条件：
$$
x_1-x_2-x_3=-1,
\qquad2x_2+x_3=1.
$$

令自由变量 $x_2=t$，则 $x_3=1-2t$，再代回第一式得 $x_1=-t$。故

$$
\boxed{\xi_2=\begin{pmatrix}-t\\t\\1-2t\end{pmatrix}
=\begin{pmatrix}0\\0\\1\end{pmatrix}
+t\begin{pmatrix}-1\\1\\-2\end{pmatrix},\quad t\in\mathbb R.}
$$

**第二步：求满足 $A^2\xi_3=\xi_1$ 的全部向量。**

先进行矩阵乘法：

$$
\begin{aligned}
A^2&=\begin{pmatrix}1&-1&-1\\-1&1&1\\0&-4&-2\end{pmatrix}
\begin{pmatrix}1&-1&-1\\-1&1&1\\0&-4&-2\end{pmatrix}\\
&=\begin{pmatrix}2&2&0\\-2&-2&0\\4&4&0\end{pmatrix}.
\end{aligned}
$$

设 $\xi_3=(z_1,z_2,z_3)^{\mathrm T}$，则三个方程分别为

$$
2z_1+2z_2=-1,\quad-2z_1-2z_2=1,\quad4z_1+4z_2=-2.
$$

它们等价于同一个条件 $z_1+z_2=-1/2$，而 $z_3$ 不受限制。令 $z_2=p,z_3=q$，得到

$$
\boxed{\xi_3=\begin{pmatrix}-1/2-p\\p\\q\end{pmatrix},
\quad p,q\in\mathbb R.}
$$

这两个方程的参数相互独立，$t,p,q$ 可以任意选取。

**（Ⅱ）第一步：找出矩阵作用后的关键关系。**

直接计算

$$
A\xi_1
=\begin{pmatrix}-1-1+2\\1+1-2\\-4+4\end{pmatrix}
=\begin{pmatrix}0\\0\\0\end{pmatrix},
\qquad\xi_1\ne0.
$$

于是，无论第一问中的参数怎样取，均有

$$
A^2\xi_1=0,\qquad
A^2\xi_2=A\xi_1=0,\qquad
A^2\xi_3=\xi_1.
$$

**第二步：对线性关系作用 $A^2$，先消去第三个系数。**

设

$$
c_1\xi_1+c_2\xi_2+c_3\xi_3=0.
$$

两边左乘 $A^2$，得到 $c_3\xi_1=0$。因为 $\xi_1\ne0$，必有 $c_3=0$。

**第三步：再作用 $A$，依次消去剩余系数。**

余下 $c_1\xi_1+c_2\xi_2=0$。左乘 $A$，得 $c_2\xi_1=0$，所以 $c_2=0$；再代回，$c_1\xi_1=0$ 给出 $c_1=0$。

因此只有零系数才能组成零向量，$\boxed{\xi_1,\xi_2,\xi_3\text{ 线性无关}}$。

证明只使用题给的矩阵作用关系，对所有自由参数都成立。这里不能额外假定任取的 $\xi_2$ 都等于 $A\xi_3$；直接作用 $A^2$ 正好避免了这一不必要的限制。

**23.（2009.23）答案：特征值为 $a,a+1,a-2$；$a=2$。**

**（Ⅰ）第一步：写出二次型对应的实对称矩阵。**

二次型中混合项的系数为矩阵相应元素的两倍。例如 $2x_1x_3$ 对应 $a_{13}=a_{31}=1$。因此

$$
A=\begin{pmatrix}a&0&1\\0&a&-1\\1&-1&a-1\end{pmatrix},
\qquad f=x^{\mathrm T}Ax.
$$

**第二步：计算特征多项式。**

令 $t=\lambda-a$，则

$$
\lambda I-A=\begin{pmatrix}t&0&-1\\0&t&1\\-1&1&t+1\end{pmatrix}.
$$

沿第一行展开：

$$
\begin{aligned}
\det(\lambda I-A)
&=t\bigl[t(t+1)-1\bigr]-t\\
&=t\bigl[t(t+1)-2\bigr]\\
&=t(t-1)(t+2).
\end{aligned}
$$

故 $t=0,1,-2$，对应的三个特征值为

$$
\boxed{\lambda_1=a,\qquad\lambda_2=a+1,\qquad\lambda_3=a-2}.
$$

三者之和为 $3a-1$，等于矩阵的迹，可作运算检查。

**（Ⅱ）第一步：将规范形要求改写为惯性指数要求。**

实对称矩阵可以通过正交变换对角化。再用非零实数缩放各变量，就能把正的系数化成1、负的系数化成 $-1$。

因此规范形为 $y_1^2+y_2^2$，意味着正惯性指数为2、负惯性指数为0、零特征值的个数为1；也就是矩阵半正定且秩为2。

**第二步：根据最小特征值确定参数。**

三个特征值中最小的是 $a-2$，半正定要求

$$
a-2\ge0\Longleftrightarrow a\ge2.
$$

如果 $a>2$，三个特征值都为正，矩阵秩为3，不符合题意。只有 $a=2$ 时，特征值为 $2,3,0$，恰好有两个正特征值、一个零特征值。

所以必要且充分的参数为 $\boxed{a=2}$。

为直接核实充分性，$a=2$ 时可通过正交变换得到标准形 $2z_1^2+3z_2^2$，再令 $y_1=\sqrt2z_1,y_2=\sqrt3z_2,y_3=z_3$，即得到所需规范形。规范形中的两个系数1并不要求原矩阵的非零特征值本身等于1。
