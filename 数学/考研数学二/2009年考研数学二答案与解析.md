# 2009年考研数学二答案与解析

> 我的解答，未与官方核对。

## 答案速查

> 选择题：1.C　2.A　3.D　4.C　5.B　6.D　7.B　8.A。

> 填空题：9.$y=2x$；10.$-2$；11.$0$；12.$-3$；13.$\mathrm e^{-2/\mathrm e}$；14.$2$。

15. 极限为 $1/4$。

16. 不定积分表达式见正文。

17. 全微分及混合偏导表达式见正文。

18. $y=3x^2+2x$，体积为 $17\pi/6$。

19. 积分为 $-8/3$。

20. 负半轴曲线为 $\sqrt{\pi^2-x^2}$，非负半轴曲线为 $\pi\cos x+\sin x-x$。

21. 两个中值结论的证明见正文。

22. 全部向量及线性无关的证明见正文。

23. 特征值为 $a,a+1,a-2$；参数 $a=2$。

## 一、选择题（1～8）

**1.（2009.1）答案：C，共3个可去间断点。**

分母 $\sin\pi x$ 在所有整数处为零。分子 $x-x^3=x(1-x)(1+x)$ 的零点只有 $-1,0,1$。

在这三个点，分子和分母都是一阶零点，其极限为

$$
\lim_{x\to k}\frac{x-x^3}{\sin\pi x}
=\frac{1-3k^2}{\pi\cos\pi k},\qquad k=-1,0,1,
$$

均为有限数，因此都是可去间断点。

其他整数处分子不为零，函数无界，不是可去间断点。故共有3个。

**2.（2009.2）答案：A，$a=1,b=-1/6$。**

展开得到

$$
x-\sin ax=(1-a)x+\frac{a^3}{6}x^3+O(x^5),
$$

$$
x^2\ln(1-bx)=-bx^3+O(x^4).
$$

要等价，左边的一次项必须消失，所以 $a=1$。此时三次项系数应相同，即 $1/6=-b$，故 $b=-1/6$。

**3.（2009.3）答案：D，极小值点。**

给定全微分意味着 $f_x=x$、$f_y=y$，因此

$$
f(x,y)=\frac{x^2+y^2}{2}+C.
$$

平方和总非负，且仅在原点为零，所以原点是极小值点，并且也是全局最小值点。

**4.（2009.4）答案：C。**

先将第一部分换序。其区域为 $1\le x\le y\le2$，所以

$$
\int_1^2\mathrm dx\int_x^2f(x,y)\,\mathrm dy
=\int_1^2\mathrm dy\int_1^y f(x,y)\,\mathrm dx.
$$

它与第二部分的外积分范围相同，两段内积分首尾相接。因此总和为

$$
\int_1^2\mathrm dy\int_1^{4-y}f(x,y)\,\mathrm dx,
$$

即 C。

**5.（2009.5）答案：B，无极值点、有零点。**

曲率圆在 $(1,1)$ 处的切线斜率为 $-1$，所以 $f'(1)=-1$。

由曲率中心的纵坐标关系，

$$
0=1+\frac{1+[f'(1)]^2}{f''(1)},
$$

得到 $f''(1)=-2$。

因为 $f''$ 不变号，所以在右侧 $f''\le0$，导函数单调不增；又由 $f''(1)<0$，在1的右邻域有 $f'(x)<-1$，进而在整个 $(1,2)$ 上都有 $f'(x)<-1$。

所以函数严格减少，没有极值点。由中值定理还得 $f(2)-f(1)<-1$，即 $f(2)<0$。

结合 $f(1)=1>0$ 和连续性，区间 $(1,2)$ 内有零点，故选 B。

**6.（2009.6）答案：D。**

首先 $F(0)=0$。在 $[-1,0]$ 上，图中 $f=1$，所以

$$
F(x)=\int_0^x1\,\mathrm dt=x.
$$

因此左侧必须是经过原点、斜率为1的线段。

从原函数图形继续读出：在 $(0,1)$ 上 $f<0$，故 $F$ 减少；在 $(1,2)$ 上 $f>0$，故 $F$ 增加；在 $(2,3)$ 上 $f=0$，故 $F$ 为常数。

四个选项中，只有 D 同时满足原点函数值、左侧线段、右侧增减及最后的水平段。

**7.（2009.7）答案：B。**

记分块矩阵为 $M=\begin{pmatrix}0&A\\B&0\end{pmatrix}$。交换两个大小为2的行组需要4次行交换，因此不变号，有 $\det M=|A||B|=6$。

其逆矩阵为

$$
M^{-1}=\begin{pmatrix}0&B^{-1}\\A^{-1}&0\end{pmatrix}.
$$

所以

$$
M^*=(\det M)M^{-1}
=\begin{pmatrix}0&6B^{-1}\\6A^{-1}&0\end{pmatrix}
=\begin{pmatrix}0&2B^*\\3A^*&0\end{pmatrix},
$$

其中用了 $B^{-1}=B^*/3$、$A^{-1}=A^*/2$，对应 B。

**8.（2009.8）答案：A。**

令

$$
S=\begin{pmatrix}1&0&0\\1&1&0\\0&0&1\end{pmatrix},
$$

则新矩阵 $Q=PS$。因此

$$
Q^{\mathrm T}AQ
=S^{\mathrm T}(P^{\mathrm T}AP)S
=S^{\mathrm T}\operatorname{diag}(1,1,2)S
=\begin{pmatrix}2&1&0\\1&1&0\\0&0&2\end{pmatrix}.
$$

所以选 A。

## 二、填空题（9～14）

**9.（2009.9）答案：$y=2x$。**

$x=0$ 意味着积分上限 $1-t=0$，所以对应参数为 $t=1$，此时 $y=0$。

参数导数为

$$
x'(t)=-\mathrm e^{-(1-t)^2},
$$

$$
y'(t)=2t\ln(2-t^2)-\frac{2t^3}{2-t^2}.
$$

在 $t=1$ 处分别为 $-1,-2$，所以切线斜率为2。又经过原点，故切线是 $y=2x$。

**10.（2009.10）答案：$k=-2$。**

偶性给出

$$
\int_{-\infty}^{+\infty}\mathrm e^{k|x|}\,\mathrm dx
=2\int_0^{+\infty}\mathrm e^{kx}\,\mathrm dx.
$$

收敛先要求 $k<0$，此时积分为 $-2/k$。由 $-2/k=1$，得 $k=-2$，符合收敛条件。

**11.（2009.11）答案：$0$。**

记积分为 $I_n$。分部积分得到

$$
I_n=\left[-\frac{\mathrm e^{-x}\cos nx}{n}\right]_0^1
-\frac1n\int_0^1\mathrm e^{-x}\cos nx\,\mathrm dx.
$$

因此

$$
|I_n|\le\frac{1+\mathrm e^{-1}}n+\frac1n\int_0^1\mathrm e^{-x}\,\mathrm dx
=\frac2n\longrightarrow0.
$$

故原极限为零。

**12.（2009.12）答案：$-3$。**

令 $x=0$，原方程给出 $\mathrm e^y=1$，所以 $y(0)=0$。

对原式求导：

$$
y+(x+\mathrm e^y)y'=1,
$$

得到 $y'(0)=1$。再次求导，得

$$
2y'+\mathrm e^y(y')^2+(x+\mathrm e^y)y''=0.
$$

代入零点处的值，得到 $2+1+y''(0)=0$，故 $y''(0)=-3$。

**13.（2009.13）答案：$\mathrm e^{-2/\mathrm e}$。**

取对数函数 $h(x)=\ln y=2x\ln x$，指数函数严格增加，所以先求 $h$ 的最小值。

$$
h'(x)=2(1+\ln x).
$$

导数在 $x=\mathrm e^{-1}$ 处由负变正，此点位于 $(0,1]$ 内，故为唯一最小值点。

$$
y_{\min}=\exp\left(2\mathrm e^{-1}\ln\mathrm e^{-1}\right)
=\mathrm e^{-2/\mathrm e}.
$$

**14.（2009.14）答案：$2$。**

相似矩阵的迹相同，所以

$$
\operatorname{tr}(\alpha\beta^{\mathrm T})=2.
$$

另一方面，外积矩阵的主对角线元素为 $\alpha_i\beta_i$，所以其迹等于内积 $\beta^{\mathrm T}\alpha$。因此所求标量为2。

## 三、解答题（15～23）

**15.（2009.15）答案：$1/4$。**

由 $\tan x=x+O(x^3)$，以及 $\ln(1+u)=u-u^2/2+O(u^3)$，可得

$$
\ln(1+\tan x)=x-\frac{x^2}{2}+O(x^3).
$$

因此 $x-\ln(1+\tan x)\sim x^2/2$。再利用 $1-\cos x\sim x^2/2$、$\sin x\sim x$，有

$$
\frac{(1-\cos x)[x-\ln(1+\tan x)]}{\sin^4x}
\longrightarrow\frac12\cdot\frac12=\frac14.
$$

**16.（2009.16）不定积分。**

**第一步：改写对数。** 记

$$
S=\sqrt x+\sqrt{x+1},\qquad R=\sqrt{x(x+1)}.
$$

由于 $x>0$，有

$$
L(x)=\ln\left(1+\sqrt{\frac{x+1}{x}}\right)=\ln S-\frac12\ln x.
$$

直接求导可得

$$
(\ln S)'=\frac1{2R},
\qquad L'(x)=\frac1{2R}-\frac1{2x}.
$$

**第二步：分部积分。**

$$
\int L(x)\,\mathrm dx
=xL(x)-\int xL'(x)\,\mathrm dx
=xL(x)+\frac x2-\frac12\int\frac{x}{R}\,\mathrm dx.
$$

**第三步：计算剩余原函数。**

因为 $R'=(2x+1)/(2R)$，所以

$$
(R-\ln S)'=\frac{x}{R}.
$$

故最终结果为

$$
\boxed{
 x\ln\left(1+\sqrt{\frac{x+1}{x}}\right)
 +\frac{x-\sqrt{x(x+1)}+\ln(\sqrt x+\sqrt{x+1})}{2}+C
}.
$$

**17.（2009.17）全微分与混合偏导数。**

令 $u=x+y$、$v=x-y$、$w=xy$。以下外函数偏导数均在 $(u,v,w)=(x+y,x-y,xy)$ 处取值。

**第一步：求全微分。**

$$
\mathrm du=\mathrm dx+\mathrm dy,\quad
\mathrm dv=\mathrm dx-\mathrm dy,\quad
\mathrm dw=y\,\mathrm dx+x\,\mathrm dy.
$$

所以

$$
\mathrm dz=(f_u+f_v+yf_w)\,\mathrm dx
+(f_u-f_v+xf_w)\,\mathrm dy.
$$

**第二步：再对一阶偏导求导。**

从 $z_x=f_u+f_v+yf_w$ 出发，对 $y$ 求导，得到

$$
\begin{aligned}
z_{xy}
&=(f_{uu}-f_{uv}+xf_{uw})\\
&\quad+(f_{vu}-f_{vv}+xf_{vw})\\
&\quad+f_w+y(f_{wu}-f_{wv}+xf_{ww}).
\end{aligned}
$$

由二阶偏导连续，相应混合偏导相等，整理为

$$
\boxed{z_{xy}=f_{uu}-f_{vv}+(x+y)f_{uw}+(x-y)f_{vw}+xyf_{ww}+f_w}.
$$

最后一项 $f_w$ 来自对前面的系数 $y$ 求导。

**18.（2009.18）答案：$y=3x^2+2x$，$V=17\pi/6$。**

**第一步：降阶求通解。**

令 $p=y'$，原方程变为 $xp'-p=-2$。在 $x>0$ 上同除以 $x^2$，得到

$$
\left(\frac px\right)'=-\frac2{x^2}.
$$

积分得 $p=2+Cx$，再积分并重新记常数，得到

$$
y=ax^2+2x+b.
$$

经过原点，所以 $b=0$。

**第二步：使用面积条件。**

函数非负，题设区域面积为

$$
\int_0^1(ax^2+2x)\,\mathrm dx=\frac a3+1=2.
$$

因此 $a=3$，曲线为 $y=3x^2+2x$，确实在 $x\ge0$ 上非负。

**第三步：用圆柱壳法计算体积。**

绕 $y$ 轴旋转，壳半径为 $x$，壳高为 $y(x)$，所以

$$
\begin{aligned}
V&=2\pi\int_0^1x(3x^2+2x)\,\mathrm dx\\
&=2\pi\left(\frac34+\frac23\right)=\frac{17\pi}{6}.
\end{aligned}
$$

**19.（2009.19）答案：$-8/3$。**

**第一步：把圆心平移到原点。**

令 $u=x-1$、$v=y-1$，区域成为 $u^2+v^2\le2$、$v\ge u$，被积函数成为 $u-v$。

**第二步：使用以新圆心为中心的极坐标。**

令 $u=r\cos\theta$、$v=r\sin\theta$。范围为

$$
0\le r\le\sqrt2,
\qquad\frac\pi4\le\theta\le\frac{5\pi}{4}.
$$

因此

$$
\begin{aligned}
I&=\int_{\pi/4}^{5\pi/4}(\cos\theta-\sin\theta)\,\mathrm d\theta
\int_0^{\sqrt2}r^2\,\mathrm dr\\
&=[\sin\theta+\cos\theta]_{\pi/4}^{5\pi/4}
\cdot\frac{2\sqrt2}{3}\\
&=(-2\sqrt2)\frac{2\sqrt2}{3}=-\frac83.
\end{aligned}
$$

**20.（2009.20）分段曲线的表达式。**

**第一步：由负半轴上的法线条件求曲线。**

点 $(x,y)$ 的位置向量沿法线方向，与切向量 $(1,y')$ 垂直，所以

$$
x+yy'=0.
$$

积分得到 $x^2+y^2=C$。代入给定点，得 $C=\pi^2$。

取经过给定正纵坐标点的分支，得到

$$
y=\sqrt{\pi^2-x^2},\qquad-\pi<x<0.
$$

**第二步：由光滑拼接得到右侧初值。**

当 $x\to0^-$ 时，$y\to\pi$，且 $y'=-x/\sqrt{\pi^2-x^2}\to0$。所以右側解应满足 $y(0)=\pi,y'(0)=0$。

**第三步：解右侧常系数方程。**

方程 $y''+y=-x$ 的通解为

$$
y=C_1\cos x+C_2\sin x-x.
$$

代入两个初值得 $C_1=\pi,C_2=1$。因此

$$
y(x)=\begin{cases}
\sqrt{\pi^2-x^2},&-\pi<x<0,\\
\pi\cos x+\sin x-x,&0\le x<\pi.
\end{cases}
$$

两侧在零点函数值和一阶导数相同，满足光滑连接条件。

**21.（2009.21）中值定理及右导数结论的证明。**

**（Ⅰ）证明拉格朗日中值定理。**

令弦的斜率为 $k=[f(b)-f(a)]/(b-a)$，并构造

$$
F(x)=f(x)-f(a)-k(x-a).
$$

$F$ 在 $[a,b]$ 连续，在 $(a,b)$ 可导，且 $F(a)=F(b)=0$。

由罗尔定理，存在 $\xi\in(a,b)$，使 $F'(\xi)=0$，即 $f'(\xi)=k$。这就得到

$$
f(b)-f(a)=f'(\xi)(b-a).
$$

**（Ⅱ）由导数的右极限推出右导数。**

任取 $0<x<\delta$，在 $[0,x]$ 上使用第一问的中值定理，存在 $\xi_x\in(0,x)$，使

$$
\frac{f(x)-f(0)}x=f'(\xi_x).
$$

当 $x\to0^+$ 时，$0<\xi_x<x$ 保证 $\xi_x\to0^+$。利用已知 $f'(t)\to A$，可得

$$
f'_+(0)=\lim_{x\to0^+}\frac{f(x)-f(0)}x=A.
$$

因此右导数存在并等于 $A$，证明没有预先假设零点处可导。

**22.（2009.22）全部解向量及线性无关的证明。**

**（Ⅰ）先求 $\xi_2$。**

方程 $A\xi_2=\xi_1$ 化为

$$
x_1-x_2-x_3=-1,\qquad2x_2+x_3=1.
$$

令 $x_2=t$，可得

$$
\xi_2=\begin{pmatrix}-t\\t\\1-2t\end{pmatrix}
=\begin{pmatrix}0\\0\\1\end{pmatrix}
+t\begin{pmatrix}-1\\1\\-2\end{pmatrix},\qquad t\in\mathbb R.
$$

**再求 $\xi_3$。**

计算

$$
A^2=\begin{pmatrix}2&2&0\\-2&-2&0\\4&4&0\end{pmatrix}.
$$

方程 $A^2\xi_3=\xi_1$ 等价于 $x_1+x_2=-1/2$，第三分量自由，因此

$$
\xi_3=\begin{pmatrix}-1/2-p\\p\\q\end{pmatrix},
\qquad p,q\in\mathbb R.
$$

**（Ⅱ）用矩阵幂逐步消去系数。**

直接计算可知 $A\xi_1=0$，且 $\xi_1\ne0$。

假设 $c_1\xi_1+c_2\xi_2+c_3\xi_3=0$。两边左乘 $A^2$，利用

$$
A^2\xi_1=0,\qquad A^2\xi_2=A\xi_1=0,\qquad A^2\xi_3=\xi_1,
$$

得到 $c_3\xi_1=0$，所以 $c_3=0$。

再对剩余等式左乘 $A$，得到 $c_2\xi_1=0$，所以 $c_2=0$。最后 $c_1\xi_1=0$ 给出 $c_1=0$。

故任意上述 $\xi_2,\xi_3$ 都使三向量线性无关。

**23.（2009.23）答案：特征值为 $a,a+1,a-2$；$a=2$。**

**（Ⅰ）写出二次型矩阵并求特征值。**

$$
A=\begin{pmatrix}a&0&1\\0&a&-1\\1&-1&a-1\end{pmatrix}.
$$

令 $t=\lambda-a$，则特征行列式为

$$
\det(\lambda I-A)=t[t(t+1)-2]=t(t-1)(t+2).
$$

所以三个特征值为 $a,a+1,a-2$。

**（Ⅱ）使用规范形的惯性指数。**

规范形 $y_1^2+y_2^2$ 表示两个正特征值和一个零特征值。因此矩阵应半正定且秩为2。

三个特征值中最小的是 $a-2$，半正定要求 $a\ge2$。如果 $a>2$，三个特征值都为正，秩为3；只有 $a=2$ 时，特征值为 $2,3,0$，满足要求。

故 $a=2$。
