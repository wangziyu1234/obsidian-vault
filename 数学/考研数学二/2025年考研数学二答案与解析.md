# 2025年考研数学二答案与解析

> 我的解答，未与官方核对。题意依据所提供的扫描试卷，最终答案已与该文件第 4 页答案速查交叉检查。

## 答案速查

选择题：1.A；2.B；3.C；4.C；5.A。

选择题：6.B；7.D；8.D；9.B；10.D。

填空题：11.$2$；12.$y=x-1$；13.$-\dfrac14$；14.$\mathrm e$。

15.$\displaystyle y=\frac{2x+\sqrt{20-11x^2}}5$（取包含初值点的分支）。

16.$\displaystyle \boldsymbol x=(5,4,-4,0)^{\mathrm T}+t(1,1,-1,-1)^{\mathrm T}$，$t\in\mathbb R$。

17.$\displaystyle \frac{3\ln2+\pi}{10}$。

18.$f$ 在 $0$ 处可导，$f'(0)=5$。

19.$\displaystyle f(x,y)=(-x^2+y+2)\mathrm e^{-y}$；在 $(0,-1)$ 取极大值 $\mathrm e$，无极小值。

20.$\displaystyle 12\pi-\frac{112}{3}$。

21.充要条件成立，证明见第 21 题。

22.（1）$a=4$，$k>0$；（2）$k=3$，正交矩阵 $Q$ 见第 22 题。

## 一、选择题（1～10）

**1.（2025.1）A**

**第一步：写出隐函数关系。**

令

$$
F(x,y,z)=z+\ln z-\int_y^x\mathrm e^{-t^2}\,\mathrm dt.
$$

由 $\ln z$ 可知 $z>0$，从而

$$
F_z=1+\frac1z=\frac{z+1}{z}\ne0.
$$

因此可对 $F(x,y,z(x,y))=0$ 使用隐函数求导法。

**第二步：分别处理积分的上限和下限。**

对 $x$ 求偏导时，$y$ 固定，有

$$
\left(1+\frac1z\right)z_x-\mathrm e^{-x^2}=0.
$$

所以

$$
z_x=\frac{z}{z+1}\mathrm e^{-x^2}.
$$

对 $y$ 求偏导时，$x$ 固定。积分对下限求导带一个负号，题目前面又有负号，故

$$
\left(1+\frac1z\right)z_y+\mathrm e^{-y^2}=0,
$$

即

$$
z_y=-\frac{z}{z+1}\mathrm e^{-y^2}.
$$

**第三步：相加。**

$$
\boxed{z_x+z_y=\frac{z}{z+1}\left(\mathrm e^{-x^2}-\mathrm e^{-y^2}\right)}.
$$

因此选择 A。

**2.（2025.2）B**

**第一步：判断 $f$ 在零点是否取得极值。**

由变上限积分求导公式，

$$
f'(x)=\mathrm e^{x^2}\sin x.
$$

在 $0$ 的充分小左邻域内，$\sin x<0$，所以 $f'(x)<0$；在右邻域内，$f'(x)>0$。

$f$ 在零点附近先减后增，故 $x=0$ 为极小值点。

又

$$
f''(x)=\mathrm e^{x^2}(2x\sin x+\cos x),\qquad f''(0)=1.
$$

$f''$ 连续，因而在零点两侧的一个小邻域内均为正，$(0,0)$ 不是 $y=f(x)$ 的拐点。

**第二步：判断 $g$ 的凹凸性变化。**

记

$$
H(x)=\int_0^x\mathrm e^{t^2}\,\mathrm dt,\qquad g(x)=H(x)\sin^2x.
$$

由乘积求导法，

$$
g'(x)=\mathrm e^{x^2}\sin^2x+H(x)\sin2x,
$$

$$
g''(x)=2x\mathrm e^{x^2}\sin^2x
+2\mathrm e^{x^2}\sin2x+2H(x)\cos2x.
$$

因为

$$
\frac{H(x)}x\longrightarrow H'(0)=1,
\qquad \frac{\sin2x}x\longrightarrow2,
$$

所以

$$
\lim_{x\to0}\frac{g''(x)}x=0+4+2=6>0.
$$

因此在零点附近，$g''(x)$ 与 $x$ 同号：左侧为负，右侧为正。

曲线 $y=g(x)$ 在 $(0,0)$ 处改变凹凸性，所以该点为拐点。选择 B。

**3.（2025.3）C，$-2<a<0$**

**第一步：把积分收敛性转化为特征根条件。**

特征方程为

$$
r^2-2ar+(a+2)=0.
$$

若所有特征根实部均小于 $0$，解由衰减指数函数、指数乘多项式、或衰减指数乘正弦余弦组成，均可在 $[0,+\infty)$ 上积分。

若有实部大于或等于 $0$ 的特征根，则可以选取一个不衰减的相应解，其反常积分不收敛。因此“任一解的积分均收敛”要求所有特征根实部严格小于 $0$。

**第二步：讨论实根。**

判别式为

$$
\Delta=4(a^2-a-2)=4(a-2)(a+1).
$$

有实根时，两根均为负的条件是

$$
r_1+r_2=2a<0,\qquad r_1r_2=a+2>0.
$$

即 $-2<a<0$。再与 $\Delta\ge0$ 结合，得到

$$
-2<a\le-1.
$$

在 $a=-1$ 时出现重根 $r=-1$，通解为 $(C_1+C_2x)\mathrm e^{-x}$，仍然可积。

**第三步：讨论共轭复根。**

当 $\Delta<0$，即 $-1<a<2$ 时，根为

$$
r=a\pm i\sqrt{-(a^2-a-2)}.
$$

实部为 $a$，故还需 $a<0$，得到 $-1<a<0$。

合并两种情形，

$$
\boxed{-2<a<0}.
$$

端点 $a=-2$ 会产生零特征根；端点 $a=0$ 会产生不衰减的振荡解，均不满足题设。选择 C。

**4.（2025.4）C**

“$f$ 是 $g$ 的高阶无穷小”表示

$$
g(x)\to0,\qquad \frac{f(x)}{g(x)}\to0.
$$

由于 $\mathrm e^u-1\sim u$（$u\to0$），

$$
\frac{f(x)}{\mathrm e^{g(x)}-1}
=\frac{f(x)}{g(x)}\frac{g(x)}{\mathrm e^{g(x)}-1}
\longrightarrow0\cdot1=0.
$$

所以

$$
\boxed{f(x)=o(\mathrm e^{g(x)}-1)},
$$

选择 C。

其余选项可以直接检查：

对于 A，

$$
\frac{f+g}{g}=\frac fg+1\longrightarrow1,
$$

故不是 $o(g)$。

对于 B，

$$
\frac{fg}{f^2}=\frac gf,
$$

其绝对值趋于无穷大，不可能趋于 $0$。

对于 D，可取 $g(x)=x^2$、$f(x)=x^3$。这时 $f/g=x\to0$，但

$$
\frac{f}{g^2}=\frac1x
$$

不趋于零。因此 D 不一定成立。

**5.（2025.5）A**

**第一步：把原积分表示成区域条件。**

$$
D=\{(x,y):-2\le x\le2,\ 4-x^2\le y\le4\}.
$$

边界是向下开口的抛物线 $y=4-x^2$ 与水平线 $y=4$。

整个区域的纵坐标范围为 $0\le y\le4$。

**第二步：固定 $y$，求横坐标范围。**

由 $4-x^2\le y$，有

$$
x^2\ge4-y.
$$

结合 $-2\le x\le2$，得到左右两段：

$$
-2\le x\le-\sqrt{4-y},
\qquad \sqrt{4-y}\le x\le2.
$$

**第三步：按先 $x$ 后 $y$ 的顺序积分。**

$$
\boxed{
\int_0^4\left[
\int_{-2}^{-\sqrt{4-y}}f(x,y)\,\mathrm dx
+\int_{\sqrt{4-y}}^2f(x,y)\,\mathrm dx
\right]\mathrm dy}.
$$

这就是选项 A。

区域虽关于 $y$ 轴对称，但题目没有假定 $f(x,y)$ 关于 $x$ 为偶函数，因此不能直接把右半区域上的积分乘以 $2$。

**6.（2025.6）B**

**第一步：计算引力在运动方向的分量。**

当移动质点位于 $P=(x,0)$ 时，固定质点为 $Q=(0,1)$，距离为

$$
r=\sqrt{x^2+1}.
$$

两个质点均为单位质量，所以引力大小为 $G/r^2$。

从 $P$ 指向 $Q$ 的单位方向向量为

$$
\frac{(-x,1)}r.
$$

故作用于 $P$ 的引力向量是

$$
\boldsymbol F=\frac{G(-x,1)}{(x^2+1)^{3/2}}.
$$

沿 $x$ 轴正向运动时，只有横向分量做功；该分量为负，阻碍运动。

**第二步：积分克服引力所作的功。**

外力克服引力所作的微元功是引力功的相反数：

$$
\mathrm dW=-F_x\,\mathrm dx
=\frac{Gx}{(x^2+1)^{3/2}}\,\mathrm dx.
$$

所以

$$
\boxed{W=\int_0^l\frac{Gx}{(x^2+1)^{3/2}}\,\mathrm dx},
$$

选择 B。

若继续求值，利用

$$
\frac{\mathrm d}{\mathrm dx}(1+x^2)^{-1/2}
=-x(1+x^2)^{-3/2},
$$

可得

$$
W=G\left(1-\frac1{\sqrt{1+l^2}}\right).
$$

当 $l>0$ 时该值为正，符合克服引力做正功的物理意义。

**7.（2025.7）D，四个条件均充分**

这里“极限存在”指双侧有限极限存在。

**先证明一个共同使用的结论。**

若 $f(0)=0$ 且

$$
L=\lim_{x\to0}\frac{|f(x)|}{x}
$$

存在，则右侧商非负，左侧商非正，因此 $L\ge0$ 且 $L\le0$，只能有 $L=0$。

进一步，

$$
\left|\frac{f(x)}x\right|
=\left|\frac{|f(x)|}x\right|\longrightarrow0,
$$

从而

$$
f'(0)=\lim_{x\to0}\frac{f(x)-f(0)}x=0.
$$

**检查条件①。**

极限存在说明分子必须趋于 $0$。再利用 $f$ 连续，得到

$$
|f(0)|-f(0)=0,\qquad f(0)\ge0.
$$

若 $f(0)>0$，由连续性，零点附近 $f(x)>0$，于是 $|f(x)|=f(x)$，条件①就是 $f'(0)$ 存在。

若 $f(0)=0$，条件①变成前面已经证明的情形，仍有 $f'(0)=0$。所以①充分。

**检查条件②。**

同样由分子趋于零得到 $f(0)=|f(0)|$。

因此条件②中的差商就是

$$
\frac{f(x)-|f(0)|}{x}=\frac{f(x)-f(0)}x.
$$

其极限存在直接表示 $f$ 在 $0$ 处可导。②充分。

**检查条件③。**

极限存在要求 $|f(x)|\to0$，结合连续性得到 $f(0)=0$。

于是适用共同结论，$f'(0)=0$，③充分。

**检查条件④。**

若 $f(0)>0$，零点附近有 $|f|=f$，条件④就是 $f$ 的差商极限存在。

若 $f(0)<0$，零点附近有 $|f|=-f$，条件④中的差商是 $f$ 的差商的相反数，仍能推出 $f'(0)$ 存在。

若 $f(0)=0$，再次应用共同结论。

因此四个条件均能推出可导，答案为

$$
\boxed{\text{D，4 个}}.
$$

**8.（2025.8）D，$a<4$ 且 $b<0$**

题中矩阵为实对称矩阵，可以通过相应二次型的惯性指数判断特征值的正负个数。

对应二次型是

$$
f=x_1^2+4x_1x_2+ax_2^2+bx_3^2.
$$

配方得

$$
f=(x_1+2x_2)^2+(a-4)x_2^2+bx_3^2.
$$

变量替换

$$
y_1=x_1+2x_2,\qquad y_2=x_2,\qquad y_3=x_3
$$

可逆，故不改变正、负惯性指数。

标准形中第一项系数为 $1>0$。要恰好有一个正特征值和两个负特征值，其余两个系数必须都严格为负：

$$
a-4<0,\qquad b<0.
$$

反过来，这两个条件成立时，标准形恰好为一正两负，亦满足题设。

所以

$$
\boxed{a<4,\quad b<0},
$$

选择 D。

**9.（2025.9）B**

**第一步：对 B 直接作初等行变换。**

B 中矩阵为

$$
\begin{pmatrix}
1&1&0&1\\
1&1&2&5\\
1&1&1&3
\end{pmatrix}.
$$

先作 $R_2\leftarrow R_2-R_1$、$R_3\leftarrow R_3-R_1$，得到

$$
\begin{pmatrix}
1&1&0&1\\
0&0&2&4\\
0&0&1&2
\end{pmatrix}.
$$

再作 $R_2\leftarrow\frac12R_2$，最后作 $R_3\leftarrow R_3-R_2$，得到

$$
\begin{pmatrix}
1&1&0&1\\
0&0&1&2\\
0&0&0&0
\end{pmatrix},
$$

正好为题设矩阵，所以选 B。

**第二步：说明如何排除其他选项。**

初等行变换不改变行空间。目标矩阵的任一行空间向量都形如

$$
s(1,1,0,1)+t(0,0,1,2)=(s,s,t,s+2t).
$$

所以必须满足“第一分量等于第二分量”和“第四分量等于第一分量加第三分量的两倍”。

A 的第二行 $(1,2,1,3)$ 不满足第一条件；C 的第一行 $(1,0,0,1)$ 也不满足第一条件。

D 的第一行 $(1,1,2,3)$ 中 $3\ne1+2\times2$，不满足第二条件。

因此其他三项不能仅经初等行变换化为目标矩阵。

**10.（2025.10）D**

**第一步：缩小可能的秩。**

记 $r(BA)=k$，则由题设 $r(AB)=k+1$。

若 $r(AB)=3$，则 $AB$ 可逆，进而 $A$、$B$ 均可逆；这样 $BA$ 也可逆，秩应为 $3$，与秩相差 $1$ 矛盾。

所以 $r(AB)\le2$，即

$$
k=r(BA)\le1.
$$

**第二步：估计两个系数矩阵的秩。**

注意乘积次序：

$$
ABA=A(BA),\qquad BAB=(BA)B.
$$

根据乘积矩阵秩不超过任一因子的秩，

$$
r(ABA)\le r(BA)\le1,
\qquad r(BAB)\le r(BA)\le1.
$$

**第三步：把“公共解”写成一个联合方程组。**

两个方程组有公共非零解，等价于下面的三元齐次方程组有非零解：

$$
\begin{pmatrix}ABA\\BAB\end{pmatrix}\boldsymbol x=\boldsymbol0.
$$

其系数矩阵的秩满足

$$
r\begin{pmatrix}ABA\\BAB\end{pmatrix}
\le r(ABA)+r(BAB)\le2<3.
$$

由秩与解空间维数的关系，联合解空间维数至少为 $3-2=1$，故存在非零解。

这正是选项 D。

## 二、填空题（11～16）

**11.（2025.11）$a=2$**

**第一步：检查反常积分有意义的参数范围。**

积分区间为 $[1,+\infty)$。若 $a\le-2$，则 $2x+a$ 在该区间存在零点，且分子不为零，积分在该点发散。

因此收敛必须有 $a>-2$。此时 $2x+a>0$，下面可以直接使用实对数。

**第二步：分解被积函数。**

$$
\frac{a}{x(2x+a)}=\frac1x-\frac2{2x+a}.
$$

先把无穷上限改成有限的 $R$：

$$
\int_1^R\frac{a}{x(2x+a)}\,\mathrm dx
=\left[\ln x-\ln(2x+a)\right]_1^R.
$$

整理得

$$
\int_1^R\frac{a}{x(2x+a)}\,\mathrm dx
=\ln\frac{R}{2R+a}+\ln(2+a).
$$

**第三步：取极限并解参数。**

当 $R\to+\infty$，有 $R/(2R+a)\to1/2$，所以

$$
\int_1^{+\infty}\frac{a}{x(2x+a)}\,\mathrm dx
=\ln\frac{2+a}{2}.
$$

根据给定积分值，

$$
\ln\frac{2+a}{2}=\ln2
\quad\Longrightarrow\quad \frac{2+a}{2}=2.
$$

因此

$$
\boxed{a=2}.
$$

它满足 $a>-2$，代回的反常积分确实收敛。

**12.（2025.12）$y=x-1$**

求斜渐近线 $y=kx+b$，要分别求斜率和截距；仅说明函数与 $x-1$ 等价，并不足以确定渐近线。

**第一步：求斜率。**

记 $y=\sqrt[3]{x^3-3x^2+1}$。对 $x\ne0$，

$$
\frac yx=\sqrt[3]{1-\frac3x+\frac1{x^3}}.
$$

在 $x\to+\infty$ 和 $x\to-\infty$ 两种情况下，均有

$$
k=\lim\frac yx=1.
$$

**第二步：求截距。**

利用立方差公式，

$$
(y-x)(y^2+xy+x^2)=y^3-x^3=-3x^2+1.
$$

所以

$$
y-x=\frac{-3x^2+1}{y^2+xy+x^2}
=\frac{-3+1/x^2}{(y/x)^2+y/x+1}.
$$

由 $y/x\to1$，得到

$$
b=\lim(y-x)=\frac{-3}{1+1+1}=-1.
$$

因此曲线两端的斜渐近线均为

$$
\boxed{y=x-1}.
$$

实立方根对所有实数有定义，原函数在每个有限点连续，因此没有竖直渐近线；斜率为 $1\ne0$，也没有水平渐近线。

**13.（2025.13）$-\dfrac14$**

**第一步：整理成黎曼和。**

原式可写为

$$
\frac1{n^2}\sum_{k=1}^{n-1}k\ln\frac kn
=\frac1n\sum_{k=1}^{n-1}\frac kn\ln\frac kn.
$$

这对应区间 $[0,1]$、步长 $1/n$，以及函数 $h(t)=t\ln t$。

虽然 $\ln t$ 在 $0$ 处无定义，但是

$$
\lim_{t\to0^+}t\ln t=0.
$$

定义 $h(0)=0$ 后，$h$ 在 $[0,1]$ 连续。还可以补上 $k=n$ 的一项，因为 $h(1)=0$，不改变原和式。

因此极限等于

$$
\int_0^1t\ln t\,\mathrm dt.
$$

**第二步：分部积分。**

取 $u=\ln t$，$\mathrm dv=t\,\mathrm dt$，则

$$
\int t\ln t\,\mathrm dt
=\frac{t^2}{2}\ln t-\int\frac t2\,\mathrm dt
=\frac{t^2}{2}\ln t-\frac{t^2}{4}.
$$

下端点使用 $t^2\ln t\to0$，所以

$$
\int_0^1t\ln t\,\mathrm dt
=\left[\frac{t^2}{2}\ln t-\frac{t^2}{4}\right]_0^1
=-\frac14.
$$

故答案为

$$
\boxed{-\frac14}.
$$

**14.（2025.14）$\mathrm e$**

这是参数方程求导，关键是先确定 $t=0$ 对应的 $y$，然后分别求 $\mathrm dx/\mathrm dt$ 和 $\mathrm dy/\mathrm dt$。

**第一步：确定对应点。**

当 $t=0$，积分关系变为

$$
\int_1^y\mathrm e^{-u^2}\,\mathrm du=0.
$$

被积函数恒正，积分关于上限严格递增，因此只有 $y=1$ 能使积分为零。

**第二步：对参数求导。**

由 $x=\ln(1+2t)$，

$$
\frac{\mathrm dx}{\mathrm dt}=\frac2{1+2t},
\qquad \left.\frac{\mathrm dx}{\mathrm dt}\right|_{t=0}=2.
$$

对积分关系求导时，要对上限 $y+t^2$ 使用链式法则：

$$
2-\mathrm e^{-(y+t^2)^2}
\left(\frac{\mathrm dy}{\mathrm dt}+2t\right)=0.
$$

代入 $t=0$、$y=1$，得到

$$
2-\mathrm e^{-1}\left.\frac{\mathrm dy}{\mathrm dt}\right|_{t=0}=0,
$$

所以

$$
\left.\frac{\mathrm dy}{\mathrm dt}\right|_{t=0}=2\mathrm e.
$$

**第三步：使用参数求导公式。**

由于此时 $\mathrm dx/\mathrm dt=2\ne0$，

$$
\boxed{\left.\frac{\mathrm dy}{\mathrm dx}\right|_{t=0}
=\left.\frac{\mathrm dy/\mathrm dt}{\mathrm dx/\mathrm dt}\right|_{t=0}
=\mathrm e}.
$$

**15.（2025.15）$\displaystyle y=\frac{2x+\sqrt{20-11x^2}}5$**

**第一步：识别全微分方程。**

记

$$
M(x,y)=2y-3x,\qquad N(x,y)=2x-5y.
$$

有

$$
M_y=2=N_x.
$$

因此可寻找势函数 $F$，使得 $\mathrm dF=M\,\mathrm dx+N\,\mathrm dy$。

**第二步：积分求势函数。**

对 $F_x=2y-3x$ 关于 $x$ 积分，得

$$
F(x,y)=2xy-\frac32x^2+\phi(y).
$$

再对 $y$ 求偏导，并与 $N$ 比较：

$$
F_y=2x+\phi'(y)=2x-5y.
$$

所以 $\phi'(y)=-5y$，可取 $\phi(y)=-5y^2/2$。

原方程的隐式解为

$$
2xy-\frac32x^2-\frac52y^2=C.
$$

**第三步：用初始条件确定常数。**

代入 $x=1,y=1$，得到

$$
C=2-\frac32-\frac52=-2.
$$

因此

$$
4xy-3x^2-5y^2=-4.
$$

**第四步：化为显式解，并选择正确分支。**

关于 $y$ 整理为二次方程：

$$
5y^2-4xy+3x^2-4=0.
$$

由求根公式，

$$
y=\frac{4x\pm\sqrt{16x^2-20(3x^2-4)}}{10}
=\frac{2x\pm\sqrt{20-11x^2}}5.
$$

在 $x=1$ 时，正号给出 $(2+3)/5=1$，负号给出 $-1/5$。因此初值要求选正号：

$$
\boxed{y(x)=\frac{2x+\sqrt{20-11x^2}}5}.
$$

包含初值点的最大光滑区间为

$$
-\sqrt{\frac{20}{11}}<x<\sqrt{\frac{20}{11}}.
$$

**16.（2025.16）$\boldsymbol x=(5,4,-4,0)^{\mathrm T}+t(1,1,-1,-1)^{\mathrm T}$**

**第一步：用三个线性无关向量表示第四列和右端。**

由已知列向量关系，

$$
\alpha_4=\alpha_1+\alpha_2-\alpha_3.
$$

因此右端为

$$
\alpha_1+4\alpha_4
=5\alpha_1+4\alpha_2-4\alpha_3.
$$

**第二步：把矩阵方程化为标量方程。**

方程左端为

$$
A\boldsymbol x=x_1\alpha_1+x_2\alpha_2+x_3\alpha_3+x_4\alpha_4.
$$

代入第四列的表示式，得到

$$
A\boldsymbol x=(x_1+x_4)\alpha_1+(x_2+x_4)\alpha_2+(x_3-x_4)\alpha_3.
$$

由于 $\alpha_1,\alpha_2,\alpha_3$ 线性无关，同一向量的表示系数唯一，故

$$
x_1+x_4=5,\qquad x_2+x_4=4,\qquad x_3-x_4=-4.
$$

**第三步：引入自由参数。**

令 $x_4=-t$，则

$$
x_1=5+t,\qquad x_2=4+t,\qquad x_3=-4-t.
$$

故通解为

$$
\boxed{\boldsymbol x=
\begin{pmatrix}5\\4\\-4\\0\end{pmatrix}
+t\begin{pmatrix}1\\1\\-1\\-1\end{pmatrix},\quad t\in\mathbb R}.
$$

也可以使用特解 $(1,0,0,4)^{\mathrm T}$。因为

$$
\begin{pmatrix}5\\4\\-4\\0\end{pmatrix}
=\begin{pmatrix}1\\0\\0\\4\end{pmatrix}
+4\begin{pmatrix}1\\1\\-1\\-1\end{pmatrix},
$$

所以两种参数表示给出同一解集。

## 三、解答题（17～22）

**17.（2025.17）$\dfrac{3\ln2+\pi}{10}$**

**第一步：部分分式分解。**

设

$$
\frac1{(x+1)(x^2-2x+2)}
=\frac A{x+1}+\frac{Bx+C}{x^2-2x+2}.
$$

两边同乘分母，得到恒等式

$$
1=A(x^2-2x+2)+(Bx+C)(x+1).
$$

展开并比较同次幂系数：

$$
A+B=0,\qquad -2A+B+C=0,\qquad 2A+C=1.
$$

由此得

$$
A=\frac15,\qquad B=-\frac15,\qquad C=\frac35.
$$

所以原积分为

$$
I=\frac15\int_0^1\left[
\frac1{x+1}+\frac{-x+3}{x^2-2x+2}
\right]\mathrm dx.
$$

**第二步：将二次分母配方，并拆分分子。**

$$
x^2-2x+2=(x-1)^2+1,
$$

$$
-x+3=-\frac12(2x-2)+2.
$$

第一部分对应对数积分，第二部分对应反正切积分，因此一个原函数为

$$
\frac15\left[
\ln(x+1)-\frac12\ln((x-1)^2+1)+2\arctan(x-1)
\right].
$$

**第三步：分别计算上下限。**

在 $x=1$ 时，方括号内为

$$
\ln2-\frac12\ln1+2\arctan0=\ln2.
$$

在 $x=0$ 时，方括号内为

$$
\ln1-\frac12\ln2+2\arctan(-1)
=-\frac12\ln2-\frac\pi2.
$$

因此

$$
I=\frac15\left(\ln2+\frac12\ln2+\frac\pi2\right)
=\boxed{\frac{3\ln2+\pi}{10}}.
$$

**18.（2025.18）$f$ 在 $0$ 处可导，且 $f'(0)=5$**

证明可导性时，不能预先写出含未知导数 $f'(0)$ 的泰勒展开。应先用给定极限确定 $f$ 的局部表达，再回到导数定义。

**第一步：确定分子的二阶主部。**

当 $x\to0$ 时，

$$
\ln(1+x)+\ln(1-x)=\ln(1-x^2)=-x^2+o(x^2).
$$

由给定商的极限为 $-3$，得到

$$
xf(x)-\mathrm e^{2\sin x}+1
=3x^2+o(x^2).
$$

**第二步：只展开已知的初等函数。**

因为

$$
\sin x=x+o(x^2),
$$

以及 $\mathrm e^u=1+u+u^2/2+o(u^2)$，所以

$$
\mathrm e^{2\sin x}=1+2x+2x^2+o(x^2).
$$

代入第一步的等式，整理得

$$
xf(x)=2x+5x^2+o(x^2).
$$

对于 $x\ne0$，除以 $x$：

$$
f(x)=2+5x+o(x).
$$

**第三步：使用连续性求 $f(0)$。**

上式说明 $\lim_{x\to0}f(x)=2$。

又题设给出 $f$ 在 $0$ 处连续，因此

$$
f(0)=2.
$$

**第四步：直接计算差商极限。**

$$
\frac{f(x)-f(0)}x
=\frac{5x+o(x)}x=5+o(1).
$$

于是

$$
\boxed{f'(0)=5}.
$$

差商极限存在，同时就证明了 $f$ 在 $0$ 处可导。

**19.（2025.19）$f(x,y)=(-x^2+y+2)\mathrm e^{-y}$；极大值为 $\mathrm e$**

**第一步：从全微分读出两个偏导数。**

$$
f_x=-2x\mathrm e^{-y},\qquad
f_y=(x^2-y-1)\mathrm e^{-y}.
$$

固定 $y$，对 $f_x$ 关于 $x$ 积分，得

$$
f(x,y)=-x^2\mathrm e^{-y}+h(y).
$$

这里“积分常数”可能随 $y$ 变化，必须写作 $h(y)$。

**第二步：利用另一个偏导数确定 $h$。**

对上式关于 $y$ 求导：

$$
f_y=x^2\mathrm e^{-y}+h'(y).
$$

与题设比较，

$$
h'(y)=-(y+1)\mathrm e^{-y}.
$$

因为

$$
\frac{\mathrm d}{\mathrm dy}[(y+2)\mathrm e^{-y}]
=-(y+1)\mathrm e^{-y},
$$

故

$$
f(x,y)=(-x^2+y+2)\mathrm e^{-y}+C.
$$

再由 $f(0,0)=2$，有 $2+C=2$，所以 $C=0$。

因此

$$
\boxed{f(x,y)=(-x^2+y+2)\mathrm e^{-y}}.
$$

**第三步：求全部驻点。**

令 $f_x=f_y=0$。指数因子恒正，于是

$$
-2x=0,\qquad x^2-y-1=0.
$$

解得唯一驻点

$$
(x,y)=(0,-1).
$$

**第四步：用二阶偏导数判别。**

$$
f_{xx}=-2\mathrm e^{-y},\qquad
f_{xy}=2x\mathrm e^{-y},\qquad
f_{yy}=(y-x^2)\mathrm e^{-y}.
$$

代入驻点，

$$
f_{xx}(0,-1)=-2\mathrm e,
\quad f_{xy}(0,-1)=0,
\quad f_{yy}(0,-1)=-\mathrm e.
$$

判别式为

$$
\Delta=f_{xx}f_{yy}-f_{xy}^2=2\mathrm e^2>0.
$$

又 $f_{xx}<0$，故该点为极大值点；其函数值为

$$
\boxed{f(0,-1)=\mathrm e}.
$$

函数处处可微，其他点不是驻点，不能为极值点，因此无极小值。

还可核实这是全局最大值：$f(x,y)\le(y+2)\mathrm e^{-y}$，而后一个一元函数在 $y=-1$ 取得最大值 $\mathrm e$。

**20.（2025.20）$12\pi-\dfrac{112}{3}$**

**第一步：分析区域并改用极坐标。**

两个不等式分别对应圆盘

$$
(x-2)^2+y^2\le4,\qquad x^2+(y-2)^2\le4.
$$

其交集满足 $x\ge0,y\ge0$，位于第一象限。

令

$$
x=r\cos\theta,\qquad y=r\sin\theta,
\qquad \mathrm dx\mathrm dy=r\,\mathrm dr\mathrm d\theta.
$$

当 $r>0$ 时，区域条件化为

$$
r\le4\cos\theta,\qquad r\le4\sin\theta,
\qquad 0\le\theta\le\frac\pi2.
$$

两条径向边界在 $\theta=\pi/4$ 相交。

**第二步：利用对称性只算一半。**

区域和被积函数 $(x-y)^2$ 关于直线 $y=x$ 对称。

在 $0\le\theta\le\pi/4$ 内，$\sin\theta\le\cos\theta$，所以径向上界为 $4\sin\theta$。

此外

$$
(x-y)^2=r^2(\cos\theta-\sin\theta)^2
=r^2(1-\sin2\theta).
$$

连同雅可比因子 $r$，原积分为

$$
I=2\int_0^{\pi/4}\int_0^{4\sin\theta}
 r^3(1-\sin2\theta)\,\mathrm dr\mathrm d\theta.
$$

先对 $r$ 积分，得到

$$
I=128\int_0^{\pi/4}\sin^4\theta(1-\sin2\theta)\,\mathrm d\theta.
$$

**第三步：分别计算两个三角积分。**

由降幂公式，

$$
\sin^4\theta=\frac38-\frac12\cos2\theta+\frac18\cos4\theta.
$$

因此

$$
\begin{aligned}
J_1&=\int_0^{\pi/4}\sin^4\theta\,\mathrm d\theta\\
&=\left[\frac{3\theta}{8}-\frac{\sin2\theta}{4}
+\frac{\sin4\theta}{32}\right]_0^{\pi/4}\\
&=\frac{3\pi}{32}-\frac14.
\end{aligned}
$$

另一个积分利用 $\sin2\theta=2\sin\theta\cos\theta$：

$$
\begin{aligned}
J_2&=\int_0^{\pi/4}\sin^4\theta\sin2\theta\,\mathrm d\theta\\
&=2\int_0^{\pi/4}\sin^5\theta\cos\theta\,\mathrm d\theta\\
&=\left[\frac{\sin^6\theta}{3}\right]_0^{\pi/4}
=\frac1{24}.
\end{aligned}
$$

**第四步：合并结果。**

$$
\begin{aligned}
I&=128(J_1-J_2)\\
&=128\left(\frac{3\pi}{32}-\frac14-\frac1{24}\right)\\
&=\boxed{12\pi-\frac{112}{3}}.
\end{aligned}
$$

该结果为正，约为 $0.3658$，与被积函数非负相符。

**21.（2025.21）导函数严格递增的充要条件证明**

记两点割线斜率为

$$
s(u,v)=\frac{f(v)-f(u)}{v-u},\qquad u<v.
$$

需要证明的等价关系是：$f'$ 严格递增，当且仅当任意三个递增的点所确定的相邻两段割线斜率严格递增。

**第一部分：证明必要性。**

假设 $f'$ 在 $(a,b)$ 内严格递增。

任取 $a<x_1<x_2<x_3<b$。由 $f$ 可导知它连续，可分别在两个闭子区间上应用拉格朗日中值定理。

存在

$$
\xi_1\in(x_1,x_2),\qquad \xi_2\in(x_2,x_3),
$$

使得

$$
s(x_1,x_2)=f'(\xi_1),\qquad
s(x_2,x_3)=f'(\xi_2).
$$

由于 $\xi_1<\xi_2$，由严格递增性有

$$
f'(\xi_1)<f'(\xi_2).
$$

所以

$$
\frac{f(x_2)-f(x_1)}{x_2-x_1}
<\frac{f(x_3)-f(x_2)}{x_3-x_2}.
$$

必要性得证。

**第二部分：先由割线条件证明 $f'$ 单调不减。**

现假设给定的三点不等式对任意三点均成立。

任取 $a<u<v<b$，再取 $u<w<v$。由假设，

$$
s(u,w)<s(w,v).
$$

而整段割线斜率是两段斜率的加权平均：

$$
s(u,v)=\frac{w-u}{v-u}s(u,w)
+\frac{v-w}{v-u}s(w,v).
$$

两个权重均为正且和为 $1$，故

$$
s(u,w)<s(u,v)<s(w,v).
$$

在左边不等式中令 $w\to u^+$，由导数定义得

$$
f'(u)\le s(u,v).
$$

在右边不等式中令 $w\to v^-$，同理得

$$
s(u,v)\le f'(v).
$$

注意严格不等式取极限后只能先得到非严格不等式。合并可得

$$
f'(u)\le s(u,v)\le f'(v),
$$

因而 $f'$ 单调不减。

**第三部分：排除两个不同点的导数相等。**

反设存在 $u<v$，使 $f'(u)=f'(v)=c$。

由单调不减性，对每个 $x\in[u,v]$ 都有

$$
f'(u)\le f'(x)\le f'(v),
$$

所以 $f'(x)=c$ 在整个区间恒成立。

于是 $f(x)-cx$ 在该区间导数恒为零，由中值定理知它为常数，即 $f$ 在 $[u,v]$ 上是一次函数。

在这个区间内任取三个不同点，则两段割线斜率均为 $c$，与题设严格不等式矛盾。

因此当 $u<v$ 时必有 $f'(u)<f'(v)$，即 $f'$ 严格递增。

充分性得证，两者等价。

**22.（2025.22）（1）$a=4,k>0$；（2）$k=3$，正交矩阵见下**

**（1）利用合同保持惯性指数确定参数。**

矩阵 $A$ 对应的二次型为

$$
\boldsymbol x^{\mathrm T}A\boldsymbol x
=4x_1^2+x_2^2+ax_3^2+2x_1x_2-4x_1x_3+2x_2x_3.
$$

先对 $x_1$ 配方，再对余下的 $x_2,x_3$ 配方，得到

$$
\boldsymbol x^{\mathrm T}A\boldsymbol x
=4\left(x_1+\frac{x_2}{4}-\frac{x_3}{2}\right)^2
+\frac34(x_2+2x_3)^2+(a-4)x_3^2.
$$

所用变量替换是可逆的，故 $A$ 与

$$
\operatorname{diag}\left(4,\frac34,a-4\right)
$$

合同。前两个对角系数均正，说明 $A$ 至少有两个正特征值。

另一方面，$B=\operatorname{diag}(k,6,0)$ 的正特征值个数要达到 $2$，只能有 $k>0$。此时 $B$ 的秩为 $2$，恰有一个零特征值。

由于合同保持秩与惯性指数，必须使 $a-4=0$，因此

$$
\boxed{a=4,\qquad k>0}.
$$

反过来，在这些条件下，$A$、$B$ 的正、负、零特征值个数都是 $(2,0,1)$，根据惯性定理它们确实合同，所以这些条件也充分。

也可用行列式交叉检查：

$$
\det A=3a-12=0\quad\Longrightarrow\quad a=4.
$$

**（2）先确定 $k$。**

若 $Q$ 正交，则 $Q^{\mathrm T}=Q^{-1}$，因此 $Q^{\mathrm T}AQ=B$ 同时是相似变换。

相似矩阵的迹相同。代入 $a=4$ 后，

$$
\operatorname{tr}(A)=4+1+4=9,
\qquad \operatorname{tr}(B)=k+6.
$$

故

$$
\boxed{k=3}.
$$

**求特征值并选取相应特征向量。**

对于 $a=4$，直接计算特征多项式：

$$
\det(\lambda I-A)
=\lambda^3-9\lambda^2+18\lambda
=\lambda(\lambda-3)(\lambda-6).
$$

三个特征值为 $3,6,0$。为使最后的对角矩阵顺序为 $\operatorname{diag}(3,6,0)$，下面按这个顺序排列特征向量。

对 $\lambda=3$，方程 $(A-3I)\boldsymbol x=0$ 化简得到 $x_1=x_2=x_3$，可取

$$
v_1=(1,1,1)^{\mathrm T}.
$$

对 $\lambda=6$，方程 $(A-6I)\boldsymbol x=0$ 化简得到 $x_2=0,x_3=-x_1$，可取

$$
v_2=(1,0,-1)^{\mathrm T}.
$$

对 $\lambda=0$，方程 $A\boldsymbol x=0$ 化简得到 $x_3=x_1,x_2=-2x_1$，可取

$$
v_3=(1,-2,1)^{\mathrm T}.
$$

**单位化并组成正交矩阵。**

三个向量两两正交，长度分别为 $\sqrt3,\sqrt2,\sqrt6$。令

$$
q_1=\frac{v_1}{\sqrt3},\qquad
q_2=\frac{v_2}{\sqrt2},\qquad
q_3=\frac{v_3}{\sqrt6}.
$$

取

$$
\boxed{Q=(q_1,q_2,q_3)
=\begin{pmatrix}
1/\sqrt3&1/\sqrt2&1/\sqrt6\\
1/\sqrt3&0&-2/\sqrt6\\
1/\sqrt3&-1/\sqrt2&1/\sqrt6
\end{pmatrix}}.
$$

由列向量标准正交，$Q^{\mathrm T}Q=I$；又 $Aq_1=3q_1$、$Aq_2=6q_2$、$Aq_3=0$，所以

$$
\boxed{Q^{\mathrm T}AQ=\operatorname{diag}(3,6,0)=B}.
$$

任意一列整体乘以 $-1$，仍是对应特征值的单位特征向量，因此正交矩阵 $Q$ 不唯一。
