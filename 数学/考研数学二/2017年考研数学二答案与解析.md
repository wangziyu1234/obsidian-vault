# 2017年考研数学二答案与解析

> 我的解答，未与官方核对。

## 答案速查

> 选择题：1.A　2.B　3.D　4.C　5.D　6.C　7.B　8.B。

> 填空题：9.$y=x+2$；10.$-\dfrac18$；11.$1$；12.$xy\mathrm e^y$；13.$-\ln(\cos1)$；14.$-1$。

15. 极限为 $\dfrac23$。

16. $y'(0)=f_u(1,1)$，$y''(0)=f_{uu}(1,1)+f_u(1,1)-f_v(1,1)$。

17. 极限为 $\dfrac14$。

18. 在 $x=-1$ 处取得极小值 $0$，在 $x=1$ 处取得极大值 $1$。

19. 两个存在性结论的证明见正文。

20. 积分为 $\dfrac{5\pi}{4}$。

21. $\ln(x^2+y^2)+2\arctan(y/x)=0$，取经过 $(1,0)$ 的分支 $y>-x$。

22. $r(A)=2$；$\boldsymbol x=(2,3,0)^{\mathrm T}+t(-1,-2,1)^{\mathrm T}$。

23. $a=2$；正交变换后可得 $6y_1^2-3y_2^2$，正交矩阵见正文。

## 一、选择题（1～8）

**1.（2017.1）答案：A，$ab=\dfrac12$。**

由 $1-\cos u\sim u^2/2$，取 $u=\sqrt x$，可得

$$
\lim_{x\to0^+}\frac{1-\cos\sqrt x}{ax}
=\frac1{2a}.
$$

函数在零点的值及左极限都为 $b$，所以连续条件是 $b=1/(2a)$，即 $ab=1/2$。

这个条件本身也保证 $a\ne0$，原函数的右侧表达式有意义。

**从连续定义出发计算。**

连续要求左极限、右极限以及函数值三者相等。左侧是常函数，所以 $f(0)=\lim_{x\to0^-}f(x)=b$，唯一需要计算的是右极限。

利用半角公式也可直接得到

$$
\frac{1-\cos\sqrt x}{ax}
=\frac{1}{2a}\left(\frac{\sin(\sqrt x/2)}{\sqrt x/2}\right)^2
\longrightarrow\frac1{2a}.
$$

因此 $b=1/(2a)$ 是必要且充分的连续条件。这里不能直接把 $x=0$ 代进右侧分式，而应先处理它的 $0/0$ 型极限。

**2.（2017.2）答案：B。**

$f''>0$ 说明图像严格位于两点之间的弦的下方。

在 $[-1,0]$ 上，连接 $(-1,1)$ 与 $(0,-1)$ 的弦为 $y=-2x-1$，所以内部有

$$
f(x)<-2x-1.
$$

在 $[0,1]$ 上，连接 $(0,-1)$ 与 $(1,1)$ 的弦为 $y=2x-1$，所以内部有

$$
f(x)<2x-1.
$$

分别积分并相加：

$$
\int_{-1}^1 f(x)\,\mathrm dx
<\int_{-1}^0(-2x-1)\,\mathrm dx+\int_0^1(2x-1)\,\mathrm dx=0.
$$

故 B 成立。两个半区间的积分之间没有固定的严格大小关系，例如 $f(x)=2x^2-1$ 满足全部条件，却为偶函数，两边积分相等。

**弦不等式的证明与积分展开。**

对任意 $a<x<b$，在 $[a,x]$ 与 $[x,b]$ 上分别应用中值定理。因为 $f'$ 严格增加，有

$$
\frac{f(x)-f(a)}{x-a}<\frac{f(b)-f(x)}{b-x}.
$$

整理得到

$$
f(x)<\frac{b-x}{b-a}f(a)+\frac{x-a}{b-a}f(b),
$$

右边正是连接两个端点的弦。分别代入 $a=-1,b=0$ 与 $a=0,b=1$，便得到正文中的两条直线。

它们的积分为

$$
\left[-x^2-x\right]_{-1}^{0}=0,
\qquad
\left[x^2-x\right]_{0}^{1}=0.
$$

在每个开区间内，曲线都严格低于相应弦，所以两个半区间的积分其实都小于零；但仅凭这些条件，无法断定哪一个更小。

**3.（2017.3）答案：D。**

设数列极限为 $L$，利用连续性，D 中的条件给出

$$
L+\sin L=0.
$$

函数 $h(x)=x+\sin x$ 严格增加，因为 $h'(x)=1+\cos x\ge0$，且它不可能在任一区间内恒为零。因此 $h(x)=0$ 只有根 $x=0$，从而 $L=0$。

其他选项可用常数数列排除：

A 取 $x_n=\pi$，有 $\sin x_n=0$，但 $x_n$ 的极限为 $\pi$。

B、C 都取 $x_n=-1$，分别有 $-1+\sqrt{|-1|}=0$、$-1+(-1)^2=0$，但极限不是零。

**为什么 D 中只能得到零极限。**

已知数列收敛，才能先设其极限 $L$，再使用复合函数的连续性，把题设极限转化为关于 $L$ 的方程。

虽然 $h'(x)=1+\cos x$ 在某些孤立点等于零，但对任意 $a<b$，连续函数 $1+\cos x$ 在 $[a,b]$ 上非负，并在某个小区间上为正，故

$$
h(b)-h(a)=\int_a^b(1+\cos t)\,\mathrm dt>0.
$$

所以 $h$ 严格增加，结合 $h(0)=0$，零点只能是 $0$。

其余选项对应的极限方程分别具有非零根：$\sin L=0$ 有 $L=\pi$，$L+\sqrt{|L|}=0$ 有 $L=-1$，$L+L^2=L(1+L)=0$ 也有 $L=-1$。常数数列正好把这些根转化成反例。

**4.（2017.4）答案：C。**

对应齐次方程的特征根为

$$
r^2-4r+8=0,
\qquad r=2\pm2\mathrm i.
$$

因此齐次通解中包含 $\mathrm e^{2x}\cos2x$、$\mathrm e^{2x}\sin2x$。

右端第一部分 $\mathrm e^{2x}$ 对应指数 $2$，它不是特征根，可以设特解为 $A\mathrm e^{2x}$。

右端第二部分 $\mathrm e^{2x}\cos2x$ 与齐次解重复，且对应的是单根，所以需要乘一次 $x$。合起来可设

$$
y^*=A\mathrm e^{2x}+x\mathrm e^{2x}(B\cos2x+C\sin2x).
$$

因此选 C。

**代入验证特解的形式。**

令 $y=\mathrm e^{2x}z$，则

$$
y'=\mathrm e^{2x}(z'+2z),\qquad
y''=\mathrm e^{2x}(z''+4z'+4z).
$$

原方程因此化为

$$
z''+4z=1+\cos2x.
$$

按照所选形式令 $z=A+x(B\cos2x+C\sin2x)$。计算可得

$$
z''+4z=4A-4B\sin2x+4C\cos2x.
$$

比较系数，得到 $A=1/4,B=0,C=1/4$，所以具体可取

$$
y^*=\frac14\mathrm e^{2x}+\frac14x\mathrm e^{2x}\sin2x.
$$

这说明该待定形式确实能产生所需的两部分非齐次项。乘一次 $x$ 的作用，是使原本代入齐次算子后为零的三角函数部分产生非零结果。

**5.（2017.5）答案：D。**

$f_x>0$ 说明保持 $y$ 不变时，函数随 $x$ 严格增加；$f_y<0$ 说明保持 $x$ 不变时，函数随 $y$ 严格减少。

先把 $x$ 从0增到1，再把 $y$ 从1降到0，有

$$
f(0,1)<f(1,1)<f(1,0).
$$

所以 $f(0,1)<f(1,0)$，D 正确。

**把偏导数条件变成一元函数比较。**

固定 $y=1$，函数 $g(x)=f(x,1)$ 满足 $g'(x)=f_x(x,1)>0$，所以 $g(0)<g(1)$。固定 $x=1$，函数 $h(y)=f(1,y)$ 满足 $h'(y)=f_y(1,y)<0$，所以 $h(1)<h(0)$。两次比较连接起来，就得到 D。

A、B 涉及 $f(0,0)$ 与 $f(1,1)$，此时两个自变量同时增加：$x$ 的变化使函数增大，$y$ 的变化却使它减小，两种作用谁强并未给出。

例如 $f(x,y)=2x-y$ 时，$f(1,1)>f(0,0)$；改为 $f(x,y)=x-2y$ 时，不等号反向。两函数均满足题设偏导数条件，所以 A、B 都不是必然结论。

**6.（2017.6）答案：C，$t_0=25$。**

记两人位置之差为“甲的位置减乙的位置”，则

$$
\Delta s(t)=10+\int_0^t[v_1(\tau)-v_2(\tau)]\,\mathrm d\tau.
$$

开始时甲领先10米。在 $0\le t\le10$ 这一段，实线在虚线上方，甲还要多跑10米，因此

$$
\Delta s(10)=10+10=20.
$$

从10秒到25秒，虚线在实线上方，对应面积为20。乙在这一段逐渐追回20米，所以

$$
\Delta s(25)=20-20=0.
$$

在25秒之前，位置差仍为正；25秒时首次变为零，因此乙追上甲的时刻为25秒。

**速度图中的面积表示什么。**

速度积分表示位移，所以两条速度曲线之间的有向面积表示两人的相对位移。应先带上初始位置差 $10$，再累计速度差，不能把两条速度曲线相交的时刻当成追上的时刻。

在 $t=10$ 时，两人速度相等，但甲的领先距离已从 $10$ 增至 $20$。在 $10<t<25$ 内，乙更快，位置差持续减少；直到两曲线间累计面积达到 $20$，才抵消这段领先距离。

图中 $25$ 秒之后的阴影面积不影响首次追赶时刻。判断“追上”对应的是位置差为零，判断“速度相等”对应的才是两条速度曲线相交。

**7.（2017.7）答案：B。**

由

$$
P^{-1}AP=\operatorname{diag}(0,1,2)
$$

两边左乘 $P$，得到 $AP=P\operatorname{diag}(0,1,2)$，逐列读出

$$
A\alpha_1=0,\qquad A\alpha_2=\alpha_2,\qquad A\alpha_3=2\alpha_3.
$$

利用矩阵乘法的线性性，

$$
A(\alpha_1+\alpha_2+\alpha_3)=\alpha_2+2\alpha_3.
$$

**用基下坐标理解矩阵乘法。**

因为 $P$ 可逆，$\alpha_1,\alpha_2,\alpha_3$ 构成一组基。在这组基下，线性变换 $A$ 的矩阵是 $D=\operatorname{diag}(0,1,2)$。

向量 $\alpha_1+\alpha_2+\alpha_3$ 在该基下的坐标为 $(1,1,1)^{\mathrm T}$，作用一次 $A$ 后，坐标变为

$$
D\begin{pmatrix}1\\1\\1\end{pmatrix}
=\begin{pmatrix}0\\1\\2\end{pmatrix}.
$$

再乘以 $P$，把基下坐标转回原坐标，就得到 $\alpha_2+2\alpha_3$。其中 $\alpha_1$ 的系数变为零，是因为它属于零特征值对应的特征子空间。

**8.（2017.8）答案：B。**

三个矩阵的特征值都是 $1,2,2$。要与对角矩阵 $C$ 相似，需有三个线性无关的特征向量，尤其二重特征值2的特征子空间必须为二维。

对 $A$，

$$
A-2I=\begin{pmatrix}0&0&0\\0&0&1\\0&0&-1\end{pmatrix}
$$

的秩为1，所以零空间维数为2。再取特征值1的一个特征向量，即可得到完整特征向量基，故 $A$ 与 $C$ 相似。

对 $B$，

$$
B-2I=\begin{pmatrix}0&1&0\\0&0&0\\0&0&-1\end{pmatrix}
$$

的秩为2，所以属于特征值2的线性无关特征向量只有一个，不能对角化。因此 $B$ 与 $C$ 不相似。

**显式构造 A 的对角化矩阵。**

当特征值为 $1$ 时，解 $(A-I)p=0$ 得到 $p_1=(0,-1,1)^{\mathrm T}$。当特征值为 $2$ 时，只要求第3个分量为零，可以取 $p_2=(1,0,0)^{\mathrm T}$、$p_3=(0,1,0)^{\mathrm T}$。

按 $C$ 的对角元顺序排列，令

$$
P=(p_1,p_2,p_3)=\begin{pmatrix}0&1&0\\-1&0&1\\1&0&0\end{pmatrix}.
$$

$\det P=1$，且 $AP=PC$，所以 $P^{-1}AP=C$。

对 $B$，方程 $(B-2I)p=0$ 同时要求 $p_2=0,p_3=0$，只有 $p_1$ 一个自由分量，特征空间维数为 $1$。它与 $C$ 在特征值 $2$ 处的二维特征空间不一致，因此不能相似。

## 二、填空题（9～14）

**9.（2017.9）答案：$y=x+2$。**

当 $x\to\pm\infty$ 时，

$$
\frac yx=1+\arcsin\frac2x\longrightarrow1.
$$

截距由差值极限给出：

$$
y-x=x\arcsin\frac2x
=2\frac{\arcsin(2/x)}{2/x}\longrightarrow2.
$$

因此正负无穷两端的斜渐近线都是 $y=x+2$。

**渐近线两个系数的求法。**

若 $y=kx+b$ 是斜渐近线，应满足原函数与直线的纵向差趋于零。通常先求

$$
k=\lim_{x\to\pm\infty}\frac{y(x)}x,
\qquad
b=\lim_{x\to\pm\infty}[y(x)-kx].
$$

本题定义域为 $|x|\ge2$，所以正负无穷两端都可以考虑。令 $u=2/x$，则 $u\to0$，而 $\arcsin u/u\to1$，得到相同的截距 $2$。

最后直接检验

$$
y-(x+2)=2\left(\frac{\arcsin(2/x)}{2/x}-1\right)\longrightarrow0.
$$

因此所得直线确实满足渐近线定义。

**10.（2017.10）答案：$-\dfrac18$。**

参数导数为

$$
x'=1+\mathrm e^t,\quad x''=\mathrm e^t,
\qquad y'=\cos t,\quad y''=-\sin t.
$$

使用参数曲线二阶导数公式，

$$
\frac{\mathrm d^2y}{\mathrm dx^2}
=\frac{y''x'-y'x''}{(x')^3}.
$$

在 $t=0$ 处，分子为 $0\cdot2-1\cdot1=-1$，分母为 $2^3=8$，所以结果为 $-1/8$。

**二阶参数导数公式的来源。**

先有

$$
\frac{\mathrm dy}{\mathrm dx}=\frac{\mathrm dy/\mathrm dt}{\mathrm dx/\mathrm dt}
=\frac{\cos t}{1+\mathrm e^t}.
$$

再对 $x$ 求一次导数时，要先对参数 $t$ 求导，再除以 $\mathrm dx/\mathrm dt$：

$$
\frac{\mathrm d^2y}{\mathrm dx^2}
=\frac1{1+\mathrm e^t}\frac{\mathrm d}{\mathrm dt}
\left(\frac{\cos t}{1+\mathrm e^t}\right)
=\frac{-\sin t(1+\mathrm e^t)-\mathrm e^t\cos t}{(1+\mathrm e^t)^3}.
$$

这里 $1+\mathrm e^t>0$，所以局部把 $y$ 看成 $x$ 的函数没有问题。不能直接把 $\mathrm d^2y/\mathrm dt^2$ 除以 $\mathrm d^2x/\mathrm dt^2$，那不符合链式法则。

**11.（2017.11）答案：$1$。**

令 $u=1+x$，原积分成为

$$
I=\int_1^{+\infty}\frac{\ln u}{u^2}\,\mathrm du.
$$

分部积分可得原函数

$$
-\frac{\ln u+1}{u}.
$$

由于 $\ln u/u\to0$，所以

$$
I=\left[-\frac{\ln u+1}{u}\right]_1^{+\infty}=1.
$$

**分部积分的边界项。**

先将上限截为有限数 $R>1$，取 $U=\ln u$、$\mathrm dV=u^{-2}\mathrm du$，则 $\mathrm dU=\mathrm du/u$、$V=-1/u$。于是

$$
\begin{aligned}
\int_1^R\frac{\ln u}{u^2}\,\mathrm du
&=\left[-\frac{\ln u}{u}\right]_1^R+\int_1^R\frac{\mathrm du}{u^2}\\
&=-\frac{\ln R}{R}+1-\frac1R.
\end{aligned}
$$

令 $R\to+\infty$，两项含 $R$ 的分式都趋于零。特别地，$\ln R/R\to0$ 可以用洛必达法则验证：其导数之比为 $1/R$，极限为零。故反常积分收敛且等于 $1$。

**12.（2017.12）答案：$f(x,y)=xy\mathrm e^y$。**

由 $f_x=y\mathrm e^y$，对 $x$ 积分，得

$$
f(x,y)=xy\mathrm e^y+h(y).
$$

再对 $y$ 求偏导，并与给定全微分比较：

$$
f_y=x(1+y)\mathrm e^y+h'(y)=x(1+y)\mathrm e^y.
$$

所以 $h'(y)=0$，即 $h$ 是常数。由 $f(0,0)=0$，知该常数为零，结论得证。

**积分常数为什么先写成函数。**

对 $x$ 积分时，$y$ 只是暂时固定的参数；任何只依赖 $y$ 的函数，对 $x$ 的偏导数都为零，所以最初必须保留 $h(y)$，不能直接写成与两个变量都无关的常数。

再由 $y$ 方向的偏导数限制得到 $h'(y)=0$，这时才能把它化成常数。初值 $f(0,0)=0$ 最后确定这个常数为零。

直接求全微分检验：

$$
\mathrm d(xy\mathrm e^y)
=y\mathrm e^y\,\mathrm dx
+x(\mathrm e^y+y\mathrm e^y)\,\mathrm dy,
$$

与给定微分逐项一致，且函数在原点的值为零。

**13.（2017.13）答案：$-\ln(\cos1)$。**

积分区域为 $0\le y\le x\le1$，换序后

$$
\begin{aligned}
I&=\int_0^1\frac{\tan x}{x}\left(\int_0^x\mathrm dy\right)\mathrm dx\\
&=\int_0^1\tan x\,\mathrm dx\\
&=\left[-\ln\cos x\right]_0^1=-\ln(\cos1).
\end{aligned}
$$

在 $x=0$ 处 $\tan x/x$ 的极限为1，可以连续补定义。

**换序时积分限的确定。**

原来的积分顺序先 $x$ 后 $y$：$0\le y\le1$，固定 $y$ 后有 $y\le x\le1$。因此区域是由 $y=0$、$x=1$、$y=x$ 围成的三角形。

改成先 $y$ 后 $x$ 后，$x$ 的总范围为 $[0,1]$，对每个固定的 $x$，竖直线段从 $y=0$ 延伸到 $y=x$。内层积分于是为

$$
\int_0^x\frac{\tan x}{x}\,\mathrm dy
=\frac{\tan x}{x}\,x=\tan x.
$$

换序的好处是利用区间长度 $x$ 消去分母，而不必求 $\tan x/x$ 的初等原函数。在 $[0,1]$ 上 $\cos x>0$，所以最后的对数可以写成 $-\ln\cos x$。

**14.（2017.14）答案：$a=-1$。**

设对应特征值为 $\lambda$。矩阵乘以给定向量，得

$$
A\begin{pmatrix}1\\1\\2\end{pmatrix}
=\begin{pmatrix}1\\3+2a\\2\end{pmatrix}
=\lambda\begin{pmatrix}1\\1\\2\end{pmatrix}.
$$

第一分量立即给出 $\lambda=1$，第二分量要求 $3+2a=1$，故 $a=-1$。第三分量也满足 $2=2\lambda$，与此一致。

**逐行相乘即可确定参数。**

给定特征向量非零，因此必须存在同一个标量 $\lambda$，使三个分量同时满足特征向量关系。矩阵乘法逐行为

$$
\begin{aligned}
4+1-2\cdot2&=1,\\
1+2+2a&=3+2a,\\
3+1-1\cdot2&=2.
\end{aligned}
$$

由于向量的第1个分量为 $1$，第一行直接确定 $\lambda=1$，再利用第二行求得 $a=-1$。最后核对第三行，是为了确认参数并未只满足其中部分方程。此题无需展开整个三阶特征多项式。

## 三、解答题（15～23）

**15.（2017.15）答案：$\dfrac23$。**

在 $0\le t\le x$、$x>0$ 时，$1\le\mathrm e^t\le\mathrm e^x$，且 $\sqrt{x-t}\ge0$。

因此

$$
\int_0^x\sqrt{x-t}\,\mathrm dt
\le\int_0^x\sqrt{x-t}\,\mathrm e^t\,\mathrm dt
\le\mathrm e^x\int_0^x\sqrt{x-t}\,\mathrm dt.
$$

计算两边的简单积分：

$$
\int_0^x\sqrt{x-t}\,\mathrm dt=\frac23x^{3/2}.
$$

除以 $\sqrt{x^3}=x^{3/2}>0$，得到

$$
\frac23\le
\frac{\int_0^x\sqrt{x-t}\,\mathrm e^t\,\mathrm dt}{\sqrt{x^3}}
\le\frac23\mathrm e^x.
$$

当 $x\to0^+$ 时，左右两端都趋于 $2/3$，由夹逼定理即得结论。

**把变区间固定下来的另一种计算。**

令 $t=xs$，则 $\mathrm dt=x\,\mathrm ds$，且 $0\le s\le1$。由于 $x>0$，有 $\sqrt{x-t}=\sqrt x\sqrt{1-s}$，所以

$$
\frac{\int_0^x\sqrt{x-t}\,\mathrm e^t\,\mathrm dt}{x^{3/2}}
=\int_0^1\sqrt{1-s}\,\mathrm e^{xs}\,\mathrm ds.
$$

当 $x\to0^+$ 时，$\mathrm e^{xs}$ 在 $s\in[0,1]$ 上一致趋于 $1$。更具体地，

$$
\left|\int_0^1\sqrt{1-s}(\mathrm e^{xs}-1)\,\mathrm ds\right|
\le(\mathrm e^x-1)\int_0^1\sqrt{1-s}\,\mathrm ds\longrightarrow0.
$$

于是极限为

$$
\int_0^1\sqrt{1-s}\,\mathrm ds
=\left[-\frac23(1-s)^{3/2}\right]_0^1=\frac23.
$$

该换元清楚说明了分母为什么恰好是 $x^{3/2}$：根式贡献 $x^{1/2}$，微分贡献 $x$。

**16.（2017.16）复合函数的一、二阶导数。**

令 $u=\mathrm e^x$、$v=\cos x$。一阶链式法则为

$$
y'=f_u(u,v)u'+f_v(u,v)v'
=f_u\mathrm e^x-f_v\sin x.
$$

当 $x=0$ 时，$u=v=1$、$u'=1$、$v'=0$，因此

$$
y'(0)=f_u(1,1).
$$

再次求导，并利用混合偏导相等，得

$$
y''=f_{uu}(u')^2+2f_{uv}u'v'+f_{vv}(v')^2+f_u u''+f_v v''.
$$

代入 $u' = u''=\mathrm e^x$、$v'=-\sin x$、$v''=-\cos x$，得到

$$
y''=f_{uu}\mathrm e^{2x}-2f_{uv}\mathrm e^x\sin x
+f_{vv}\sin^2x+f_u\mathrm e^x-f_v\cos x.
$$

所以

$$
y''(0)=f_{uu}(1,1)+f_u(1,1)-f_v(1,1).
$$

所有偏导数都要在复合后的点 $(1,1)$ 处取值。

**二阶链式法则逐项展开。**

把一阶导数分成两项，分别使用乘积法则：

$$
\frac{\mathrm d}{\mathrm dx}(f_u u')
=(f_{uu}u'+f_{uv}v')u'+f_u u'',
$$

$$
\frac{\mathrm d}{\mathrm dx}(f_v v')
=(f_{vu}u'+f_{vv}v')v'+f_v v''.
$$

题设有二阶连续偏导数，故 $f_{uv}=f_{vu}$，两个混合项合为 $2f_{uv}u'v'$。除此之外，还必须保留 $f_u u''$ 与 $f_v v''$，它们来自内层函数的二阶变化。

在 $x=0$ 处，$v'=0$，所以混合项与 $f_{vv}(v')^2$ 都消失；但 $v''=-1$ 并未消失，因此最终仍保留 $-f_v(1,1)$。

也可用二阶展开核对：$u-1=x+x^2/2+o(x^2)$，$v-1=-x^2/2+o(x^2)$。代入 $f$ 的二元泰勒式，$x$ 的系数为 $f_u$，$x^2$ 的系数为 $(f_{uu}+f_u-f_v)/2$，与所得两阶导数相符。

**17.（2017.17）答案：$\dfrac14$。**

**第一步：识别黎曼和。**

$$
\sum_{k=1}^n\frac{k}{n^2}\ln\left(1+\frac kn\right)
=\sum_{k=1}^n\frac1n\left[\frac kn\ln\left(1+\frac kn\right)\right].
$$

被积函数 $x\ln(1+x)$ 在 $[0,1]$ 连续，因此极限等于

$$
I=\int_0^1x\ln(1+x)\,\mathrm dx.
$$

**第二步：分部积分。**

$$
I=\left[\frac{x^2}{2}\ln(1+x)\right]_0^1
-\frac12\int_0^1\frac{x^2}{1+x}\,\mathrm dx.
$$

利用多项式除法

$$
\frac{x^2}{1+x}=x-1+\frac1{1+x},
$$

可得

$$
\begin{aligned}
I&=\frac12\ln2-\frac12\left(\frac12-1+\ln2\right)\\
&=\frac14.
\end{aligned}
$$

**求和到积分的对应关系。**

把 $[0,1]$ 等分成 $n$ 段，宽度为 $\Delta x=1/n$，每段取右端点 $x_k=k/n$。原式第 $k$ 项恰为

$$
x_k\ln(1+x_k)\,\Delta x.
$$

因此它就是函数 $g(x)=x\ln(1+x)$ 的右端点黎曼和。$g$ 在闭区间上连续，故黎曼和极限存在并等于定积分。

分部积分中选 $u=\ln(1+x)$、$\mathrm dv=x\,\mathrm dx$，目的在于把对数变成有理式。多项式除法可由恒等式

$$
x^2=(x+1)(x-1)+1
$$

直接核对。于是

$$
\int_0^1\frac{x^2}{1+x}\,\mathrm dx
=\left[\frac{x^2}{2}-x+\ln(1+x)\right]_0^1
=\ln2-\frac12.
$$

代回后两个 $\tfrac12\ln2$ 相消，留下 $1/4$。

**18.（2017.18）答案：极小值为 $0$，极大值为 $1$。**

**第一步：求隐函数导数。**

对原方程求导，得到

$$
3x^2+3y^2y'-3+3y'=0,
$$

所以

$$
y'=\frac{1-x^2}{1+y^2}.
$$

分母始终为正，导数符号只由 $1-x^2$ 决定。

**第二步：确定单调性。**

当 $x<-1$ 时，$y'<0$；当 $-1<x<1$ 时，$y'>0$；当 $x>1$ 时，$y'<0$。

因此 $x=-1$ 为极小值点，$x=1$ 为极大值点。

**第三步：求对应函数值。**

令 $x=-1$，原方程化为

$$
y^3+3y=0.
$$

唯一实根为 $y=0$。

令 $x=1$，得到 $y^3+3y=4$，唯一实根为 $y=1$。

唯一性来自 $y^3+3y$ 的导数 $3y^2+3>0$。故所求极小值、极大值分别为0、1，对应点为 $(-1,0)$、$(1,1)$。

**隐函数的存在性与极值类型。**

将原方程整理为

$$
y^3+3y=-x^3+3x+2.
$$

左侧作为 $y$ 的函数，导数恒为正，且值域为整个实数轴，所以对每个实数 $x$ 恰有一个实数 $y$ 满足方程。又因为原方程对 $y$ 的偏导数为 $3y^2+3\ne0$，这个隐函数处处可微。

导数的分母不可能为零，因此所有驻点都由 $1-x^2=0$ 给出，没有遗漏其他临界点。在 $x=-1$ 处，函数先减后增，所以是极小值；在 $x=1$ 处，函数先增后减，所以是极大值。

也可以计算驻点处的二阶导数。由

$$
y''=\frac{-2x(1+y^2)-2y(1-x^2)y'}{(1+y^2)^2},
$$

在 $(-1,0)$ 处有 $y''=2>0$，在 $(1,1)$ 处有 $y''=-1<0$。这里求的是局部极值，不能把 $0$、$1$ 直接称为函数在全实数轴上的最小值、最大值。

**19.（2017.19）两个存在性结论的证明。**

**（Ⅰ）证明至少存在一个内部零点。**

设题设有限极限为 $L<0$。由连续性及 $f(x)=x\,[f(x)/x]$，得到 $f(0)=0$。

因为 $f(x)/x\to L<0$，存在 $\delta>0$，使得 $0<x<\delta$ 时 $f(x)<0$。

在该区间取一点 $x_0$，由 $f(x_0)<0$、$f(1)>0$ 以及连续性，零点定理保证存在 $c\in(x_0,1)$，使 $f(c)=0$。

**（Ⅱ）构造平方函数，连续使用罗尔定理。**

令

$$
F(x)=f(x)^2.
$$

由于 $f(0)=f(c)=0$，有 $F(0)=F(c)=0$。罗尔定理给出某个 $u\in(0,c)$，使 $F'(u)=0$。

另一方面，

$$
F'(x)=2f(x)f'(x),
$$

所以 $F'(0)=F'(c)=0$。这样，$F'$ 在 $0,u,c$ 三个不同点都等于零。

分别在 $[0,u]$、$[u,c]$ 上对 $F'$ 使用罗尔定理，可得

$$
\eta_1\in(0,u),\qquad \eta_2\in(u,c),
$$

使得 $F''(\eta_1)=F''(\eta_2)=0$。

而

$$
F''(x)=2\bigl([f'(x)]^2+f(x)f''(x)\bigr).
$$

因此 $\eta_1,\eta_2$ 是所求方程的两个不同实根，且都在 $(0,1)$ 内。

**第一问中负值点的严格选取。**

记有限极限为 $L<0$，取 $\varepsilon=-L/2>0$。由极限定义，当 $0<x<\delta$ 时，

$$
\frac{f(x)}x<L+\varepsilon=\frac L2<0.
$$

由于 $x>0$，可得 $f(x)<0$。再选 $x_0\in(0,\min\{\delta,1\})$，就能与 $f(1)>0$ 配合使用零点定理。因为两个端点函数值异号，得到的零点 $c$ 必在开区间 $(x_0,1)$ 内。

**第二问中三次罗尔定理的作用。**

目标表达式是乘积的导数：

$$
f f''+(f')^2=(f f')'=\frac12(f^2)''.
$$

因此选择 $F=f^2$，就是为了把目标转化为二阶导数的零点问题。$F(0)=F(c)=0$ 先提供一个内部驻点 $u$；与此同时，$F'=2ff'$ 在两个端点也为零，于是 $F'$ 有三个有序零点 $0<u<c$。

分别在相邻的两段使用罗尔定理，得到 $0<\eta_1<u<\eta_2<c<1$，从而不仅证明根存在，还证明它们不同。题设二阶可导保证 $F'$ 在所用闭区间上连续、开区间上可导，因此这些应用条件均得到满足。

**20.（2017.20）答案：$\dfrac{5\pi}{4}$。**

区域为圆盘

$$
x^2+(y-1)^2\le1.
$$

展开被积函数：

$$
(x+1)^2=x^2+2x+1.
$$

区域关于 $y$ 轴对称，所以 $\iint_D2x\,\mathrm dx\mathrm dy=0$；常数项的积分为圆盘面积 $\pi$。

对剩余部分，以圆心为极坐标原点，令 $x=r\cos\theta$、$y=1+r\sin\theta$，得到

$$
\begin{aligned}
\iint_Dx^2\,\mathrm dx\mathrm dy
&=\int_0^{2\pi}\int_0^1r^3\cos^2\theta\,\mathrm dr\,\mathrm d\theta\\
&=\frac14\cdot\pi=\frac\pi4.
\end{aligned}
$$

所以总积分为 $\pi+\pi/4=5\pi/4$。

**平移圆心与雅可比的处理。**

将区域不等式配方，

$$
x^2+y^2\le2y
\iff x^2+(y-1)^2\le1.
$$

令 $u=x,v=y-1$，平移的雅可比为 $1$，区域变为单位圆盘。再令 $u=r\cos\theta,v=r\sin\theta$，面积元变为 $r\,\mathrm dr\mathrm d\theta$。

所以 $x^2$ 的积分中出现的是 $r^2\cos^2\theta\cdot r=r^3\cos^2\theta$，不能漏掉面积元里的一个 $r$。两个一元积分分别为

$$
\int_0^1r^3\,\mathrm dr=\frac14,
\qquad
\int_0^{2\pi}\cos^2\theta\,\mathrm d\theta=\pi.
$$

也可利用单位圆盘的旋转对称性：$\iint u^2=\iint v^2$，而二者之和是 $\iint r^2=\pi/2$，故每个都是 $\pi/4$。

**21.（2017.21）答案：$\ln(x^2+y^2)+2\arctan(y/x)=0$，取 $y>-x$ 的分支。**

**第一步：从几何截距得到微分方程。**

设点 $P=(x,y)$ 处的切线斜率为 $m=y'$。切线方程为

$$
Y-y=m(X-x),
$$

所以纵截距为 $Y_P=y-xm$。

法线的一个方向向量是 $(-m,1)$，可用参数 $s$ 表示为

$$
(X,Y)=(x,y)+s(-m,1).
$$

令 $Y=0$，得到 $s=-y$，从而横截距为

$$
X_P=x+my.
$$

这种写法在 $m=0$、法线竖直时也成立。

由 $X_P=Y_P$，得到

$$
x+yy'=y-xy',
\qquad (x+y)y'=y-x.
$$

**第二步：使用齐次方程换元。**

由于 $x>0$，令 $v=y/x$，则 $y=vx$、$y'=v+xv'$。代回，得

$$
(1+v)(v+xv')=v-1,
$$

即

$$
x(1+v)v'=-(1+v^2).
$$

整理为

$$
\frac{1+v}{1+v^2}\,\mathrm dv=-\frac{\mathrm dx}{x}.
$$

两边积分：

$$
\frac12\ln(1+v^2)+\arctan v=-\ln x+C.
$$

代入 $v=y/x$，并利用 $x>0$ 合并对数，得到

$$
\frac12\ln(x^2+y^2)+\arctan\frac yx=C.
$$

**第三步：代入经过的点，选择对应分支。**

由 $y(1)=0$，知 $C=0$，所以

$$
\ln(x^2+y^2)+2\arctan\frac yx=0.
$$

原微分方程还表明 $x+y$ 不能为零：若 $y=-x$，左边为零，右边为 $-2x\ne0$，矛盾。

曲线连续且在 $(1,0)$ 处 $x+y>0$，因此在题设的整个连通区间内都应取 $x+y>0$，即 $y>-x$ 的分支。

**截距条件与变量分离的细节。**

切线的方向向量是 $(1,m)$，法线方向向量取 $(-m,1)$，两者内积为零，因此法线参数式在任意有限斜率下都适用。法线与横轴交点的横坐标应为 $x+my$，其符号可以在初始点处核对：最终得到的斜率是 $-1$，这时两个截距均为 $1$。

代入 $y=vx$ 后，

$$
v(1+v)+x(1+v)v'=v-1,
$$

移项得到 $x(1+v)v'=-(1+v^2)$。变量分离时只除以恒正的 $1+v^2$ 和题设为正的 $x$，没有丢失任何实常值解。

左侧积分拆为

$$
\int\frac{v}{1+v^2}\,\mathrm dv+\int\frac1{1+v^2}\,\mathrm dv
=\frac12\ln(1+v^2)+\arctan v.
$$

合并对数时使用 $x>0$：

$$
\frac12\ln\left(1+\frac{y^2}{x^2}\right)+\ln x
=\frac12\ln(x^2+y^2).
$$

**把隐式答案代回验证。**

令 $H(x,y)=\ln(x^2+y^2)+2\arctan(y/x)$，则

$$
H_x=\frac{2(x-y)}{x^2+y^2},\qquad
H_y=\frac{2(x+y)}{x^2+y^2}.
$$

在所选分支 $x+y>0$ 上，$H_y\ne0$，由隐函数求导得 $y'=-H_x/H_y=(y-x)/(x+y)$，与几何条件导出的方程完全相同；同时 $H(1,0)=0$，初值也满足。

**22.（2017.22）答案：$r(A)=2$，通解如下。**

**（Ⅰ）先由列关系得到上界。**

因为 $\alpha_3=\alpha_1+2\alpha_2$，三列相关，所以 $\det A=0$，且 $r(A)\le2$。

$A$ 有三个不同特征值，因此零特征值只能出现一次，另两个特征值均非零。三个互异特征值使 $A$ 可对角化，相似对角矩阵恰有两个非零对角元，故

$$
r(A)=2.
$$

这也说明 $\alpha_1,\alpha_2$ 线性无关，否则全部列最多只张成一维空间。

**（Ⅱ）比较两个独立向量的系数。**

右端向量为

$$
\beta=\alpha_1+\alpha_2+\alpha_3=2\alpha_1+3\alpha_2.
$$

左端为

$$
A\boldsymbol x=(x_1+x_3)\alpha_1+(x_2+2x_3)\alpha_2.
$$

由独立性，得到

$$
x_1+x_3=2,\qquad x_2+2x_3=3.
$$

令 $x_3=t$，通解为

$$
\boldsymbol x=\begin{pmatrix}2\\3\\0\end{pmatrix}
+t\begin{pmatrix}-1\\-2\\1\end{pmatrix},\qquad t\in\mathbb R.
$$

**秩等于二的两个方向。**

列关系首先给出一个明确的非零零空间向量：

$$
A\begin{pmatrix}-1\\-2\\1\end{pmatrix}
=-\alpha_1-2\alpha_2+\alpha_3=0.
$$

因此零是特征值，秩不超过 $2$。另一方面，另外两个互异特征值都非零。若相应特征向量为 $v_1,v_2$，则

$$
v_j=\frac1{\lambda_j}Av_j\in\operatorname{Col}(A).
$$

不同特征值的特征向量线性无关，所以列空间至少含两个线性无关向量，秩不低于 $2$。这也直接证明了 $r(A)=2$。

通解还可以核对成“特解加齐次通解”：$A(2,3,0)^{\mathrm T}=2\alpha_1+3\alpha_2=\beta$，而 $(-1,-2,1)^{\mathrm T}$ 是上述零空间向量。由秩为 $2$，齐次解空间维数恰为 $3-2=1$，所以一个自由参数已经给出了全部解。

**23.（2017.23）答案：$a=2$，一组正交矩阵如下。**

**第一步：写出矩阵并确定参数。**

二次型矩阵为

$$
A=\begin{pmatrix}2&1&-4\\1&-1&1\\-4&1&a\end{pmatrix}.
$$

正交变换后的表达式没有第三个平方项，所以矩阵有零特征值，必须 $\det A=0$。

计算

$$
\det A=6-3a,
$$

故 $a=2$。原矩阵左上二阶子式为 $-3\ne0$，因此此时秩恰为2。

**第二步：求特征值及特征向量。**

代入 $a=2$ 后，特征多项式为

$$
\det(\lambda I-A)=\lambda(\lambda-6)(\lambda+3).
$$

对应 $6,-3,0$ 的特征向量可分别取

$$
p_1=(1,0,-1)^{\mathrm T},\quad
p_2=(1,-1,1)^{\mathrm T},\quad
p_3=(1,2,1)^{\mathrm T}.
$$

直接相乘可以验证 $Ap_1=6p_1$、$Ap_2=-3p_2$、$Ap_3=0$。这三个向量两两正交。

**第三步：单位化，构造正交矩阵。**

三向量的长度分别为 $\sqrt2,\sqrt3,\sqrt6$。因此可取

$$
Q=\begin{pmatrix}
1/\sqrt2&1/\sqrt3&1/\sqrt6\\
0&-1/\sqrt3&2/\sqrt6\\
-1/\sqrt2&1/\sqrt3&1/\sqrt6
\end{pmatrix}.
$$

有 $Q^{\mathrm T}Q=I$，并且

$$
Q^{\mathrm T}AQ=\operatorname{diag}(6,-3,0).
$$

于是 $\boldsymbol x=Q\boldsymbol y$ 将二次型化为 $6y_1^2-3y_2^2$，满足要求。

**行列式与特征向量的求解展开。**

二次型交叉项 $2x_ix_j$ 的系数等于矩阵对称位置元素的两倍，所以 $-8x_1x_3$ 对应 $a_{13}=a_{31}=-4$。按第一行展开，

$$
\begin{aligned}
\det A
&=2(-a-1)-(a+4)+(-4)(1-4)\\
&=6-3a.
\end{aligned}
$$

代入 $a=2$ 后，迹为 $3$，三个二阶主子式之和为 $-3-12-3=-18$，行列式为零，故

$$
\det(\lambda I-A)=\lambda^3-3\lambda^2-18\lambda
=\lambda(\lambda-6)(\lambda+3).
$$

当 $\lambda=6$ 时，方程组 $(A-6I)p=0$ 化为

$$
-4p_1+p_2-4p_3=0,\qquad p_1-7p_2+p_3=0.
$$

联立得 $p_2=0,p_3=-p_1$，取 $p_1=1$ 得到第一向量。

当 $\lambda=-3$ 时，联立 $(A+3I)p=0$，得到 $p_3=p_1,p_2=-p_1$。当 $\lambda=0$ 时，第二行给出 $p_2=p_1+p_3$，代入第一行得 $p_3=p_1$，所以 $p_2=2p_1$。

**正交变换的最终检验。**

三个向量的两两内积分别为 $1-1=0$、$1-1=0$、$1-2+1=0$。单位化后，$Q$ 的列是标准正交基，所以 $Q^{-1}=Q^{\mathrm T}$。

最后按变换 $\boldsymbol x=Q\boldsymbol y$ 代入，

$$
f=\boldsymbol x^{\mathrm T}A\boldsymbol x
=\boldsymbol y^{\mathrm T}(Q^{\mathrm T}AQ)\boldsymbol y
=6y_1^2-3y_2^2+0y_3^2.
$$

正交变换保留特征值；这里不能把系数任意缩放成 $1$ 与 $-1$，因为那样一般会破坏变换矩阵的正交性。
