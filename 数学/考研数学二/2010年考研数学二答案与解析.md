# 2010年考研数学二答案与解析

> 我的解答，未与官方核对。

## 答案速查

> 选择题：1.B　2.A　3.C　4.D　5.B　6.D　7.A　8.D。

> 填空题：9.$C_1\mathrm e^{2x}+C_2\cos x+C_3\sin x$；10.$y=2x$；11.$-2^n(n-1)!$；12.$\sqrt2(\mathrm e^\pi-1)$；13.$3\ \mathrm{cm/s}$；14.$3$。

15. 在 $x=\pm1$ 处取极小值0，在 $x=0$ 处取极大值 $(1-\mathrm e^{-1})/2$；单调区间见正文。

16. 第一积分较小，$\lim u_n=0$。

17. $\psi(t)=t^3+\dfrac32t^2$。

18. 油的质量为 $\rho abl(2\pi/3+\sqrt3/4)$。

19. $(a,b)=(-2,-2/5)$ 或 $(-2/5,-2)$。

20. 积分为 $1/3-\pi/16$。

21. 中值结论的证明见正文。

22. $\lambda=-1,a=-2$；通解为 $(3/2,-1/2,0)^{\mathrm T}+t(1,0,1)^{\mathrm T}$。

23. $a=-1$；满足指定第一列的正交矩阵见正文。

## 一、选择题（1～8）

**1.（2010.1）答案：B，1个无穷间断点。**

在原定义域内约分，并正确处理平方根中的绝对值，得到

$$
f(x)=\frac{x}{x+1}\frac{\sqrt{1+x^2}}{|x|}
=\frac{\operatorname{sgn}(x)\sqrt{1+x^2}}{x+1}.
$$

候选间断点为 $-1,0,1$。

在 $x=-1$ 处，分母趋于零、分子趋于非零值，所以是无穷间断点。

在 $x=0$ 处，左右极限分别为 $-1,1$，均有限，是跳跃间断点。

在 $x=1$ 处，约分后极限为 $\sqrt2/2$，是可去间断点。

故只有1个无穷间断点。

**2.（2010.2）答案：A，$\lambda=\mu=1/2$。**

记线性算子 $L[y]=y'+p(x)y$。题设两个特解满足 $L[y_1]=L[y_2]=q(x)$，且非齐次意味着 $q$ 不恒为零。

由 $\lambda y_1+\mu y_2$ 仍为非齐次解，得 $\lambda+\mu=1$。

由 $\lambda y_1-\mu y_2$ 为齐次解，得 $\lambda-\mu=0$。

联立得到 $\lambda=\mu=1/2$。

**3.（2010.3）答案：C，$a=2\mathrm e$。**

设切点横坐标为 $x_0>0$。两曲线在切点函数值相同、导数也相同，所以

$$
x_0^2=a\ln x_0,\qquad 2x_0=\frac a{x_0}.
$$

第二式给出 $a=2x_0^2$，代入第一式并约去 $x_0^2>0$，得 $\ln x_0=1/2$。

因此 $x_0=\sqrt{\mathrm e}$，$a=2\mathrm e$。

**4.（2010.4）答案：D，对所有正整数 $m,n$ 均收敛。**

**在零点附近。** 因 $\ln(1-x)\sim-x$，被积函数等价于

$$
x^{2/m-1/n}.
$$

$m,n$ 都是正整数，所以 $1/n\le1$、$2/m>0$，从而 $2/m-1/n>-1$，零点处总是可积。

**在 $x=1$ 附近。** 分母趋于1，主要行为为 $|\ln(1-x)|^{2/m}$。

令 $u=-\ln(1-x)$，该端点积分化为无穷远处的 $u^{2/m}\mathrm e^{-u}$ 型积分，它总收敛。

因此收敛性不依赖于这两个正整数的具体取值，选 D。

**5.（2010.5）答案：B，$z$。**

记 $u=y/x$、$v=z/x$，并用 $F_1,F_2$ 表示外函数的偏导数。

分别对 $x,y$ 求偏导，清除分母后得到

$$
-yF_1+(xz_x-z)F_2=0,
$$

$$
F_1+F_2z_y=0.
$$

将第二式乘以 $y$ 后与第一式相加，得到

$$
F_2(xz_x+yz_y-z)=0.
$$

因为 $F_2\ne0$，所以 $xz_x+yz_y=z$。

**6.（2010.6）答案：D。**

将求和项中的 $n$ 提出来：

$$
\frac{n}{(n+i)(n^2+j^2)}
=\frac1{n^2}\frac1{(1+i/n)[1+(j/n)^2]}.
$$

两个下标都独立地从1到 $n$，因此这是单位正方形上的二维黎曼和，极限为

$$
\int_0^1\mathrm dx\int_0^1\frac{\mathrm dy}{(1+x)(1+y^2)}.
$$

这正是 D。作为检验，这个积分也等于 $(\ln2)(\pi/4)$。

**7.（2010.7）答案：A。**

向量组Ⅰ可由向量组Ⅱ线性表示，说明前者张成的空间包含在后者张成的空间中，所以

$$
r(\mathrm I)\le r(\mathrm {II})\le s.
$$

若Ⅰ线性无关，则其秩等于向量个数 $r$，于是 $r\le s$，A 成立。

**8.（2010.8）答案：D。**

若 $\lambda$ 是 $A$ 的特征值，则 $A^2+A=0$ 推出

$$
\lambda(\lambda+1)=0.
$$

因此特征值只能为0或 $-1$。

$A$ 实对称，可正交对角化；它的秩为3，所以四个特征值中恰有三个非零，必为 $-1,-1,-1,0$。

因此与 $\operatorname{diag}(-1,-1,-1,0)$ 相似，选 D。

## 二、填空题（9～14）

**9.（2010.9）答案：$C_1\mathrm e^{2x}+C_2\cos x+C_3\sin x$。**

特征方程可分解为

$$
r^3-2r^2+r-2=(r-2)(r^2+1)=0.
$$

根为 $2,\mathrm i,-\mathrm i$，所以通解为所列形式。

**10.（2010.10）答案：$y=2x$。**

多项式除法给出

$$
y=2x-\frac{2x}{1+x^2}.
$$

当 $x\to\pm\infty$ 时，第二项趋于零，因此共同的斜渐近线为 $y=2x$。分母没有实零点，所以没有竖直渐近线。

**11.（2010.11）答案：$-2^n(n-1)!$。**

由展开式

$$
\ln(1-2x)=-\sum_{n=1}^{\infty}\frac{2^n}{n}x^n,
$$

可知 $x^n$ 的系数为 $-2^n/n$。乘以 $n!$，得到

$$
y^{(n)}(0)=-2^n(n-1)!.
$$

**12.（2010.12）答案：$\sqrt2(\mathrm e^\pi-1)$。**

极坐标曲线的弧长公式为

$$
L=\int_0^\pi\sqrt{r^2+(r')^2}\,\mathrm d\theta.
$$

这里 $r=r'=\mathrm e^\theta$，所以

$$
L=\sqrt2\int_0^\pi\mathrm e^\theta\,\mathrm d\theta
=\sqrt2(\mathrm e^\pi-1).
$$

**13.（2010.13）答案：$3\ \mathrm{cm/s}$。**

记对角线长为 $d=\sqrt{l^2+w^2}$。对时间求导，得

$$
\frac{\mathrm dd}{\mathrm dt}
=\frac{l l'+w w'}{\sqrt{l^2+w^2}}.
$$

代入 $l=12,w=5,l'=2,w'=3$，得到

$$
d'=\frac{24+15}{13}=3\ \mathrm{cm/s}.
$$

**14.（2010.14）答案：$3$。**

矩阵恒等式

$$
A(A^{-1}+B)B^{-1}=A+B^{-1}
$$

给出

$$
|A+B^{-1}|=|A|\,|A^{-1}+B|\,|B|^{-1}
=3\cdot2\cdot\frac12=3.
$$

这里按原顺序分解，不需要矩阵 $A,B$ 交换。

## 三、解答题（15～23）

**15.（2010.15）单调区间与极值。**

**第一步：对含参积分求导。**

积分上限项含因子 $x^2-t$，在 $t=x^2$ 时等于零，所以

$$
f'(x)=2x\int_1^{x^2}\mathrm e^{-t^2}\,\mathrm dt.
$$

被积函数始终为正，因此这个积分在 $|x|>1$ 时为正，在 $|x|<1$ 时为负，在 $x=\pm1$ 时为零。

**第二步：逐区间判断符号。**

在 $(-\infty,-1)$ 上，$x<0$、积分为正，所以 $f'<0$；在 $(-1,0)$ 上，两者均负，所以 $f'>0$。

在 $(0,1)$ 上，$x>0$、积分为负，所以 $f'<0$；在 $(1,+\infty)$ 上，两者均正，所以 $f'>0$。

故递减区间为 $(-\infty,-1)$、$(0,1)$，递增区间为 $(-1,0)$、$(1,+\infty)$。

**第三步：计算极值。**

在 $x=\pm1$ 处，导数由负变正，且 $f(\pm1)=0$，所以极小值为0。

在 $x=0$ 处，导数由正变负，是极大值点，其值为

$$
f(0)=\int_1^0(-t)\mathrm e^{-t^2}\,\mathrm dt
=\int_0^1t\mathrm e^{-t^2}\,\mathrm dt
=\frac{1-\mathrm e^{-1}}2.
$$

**16.（2010.16）积分比较与数列极限。**

**（Ⅰ）比较被积函数。**

对 $0<t<1$，有 $0<\ln(1+t)<t$。提高到正整数 $n$ 次方，再乘以 $|\ln t|>0$，得到

$$
|\ln t|[\ln(1+t)]^n<t^n|\ln t|.
$$

因此第一个积分严格小于第二个积分。

**（Ⅱ）给出趋于零的上界。**

分部积分计算得

$$
\begin{aligned}
\int_0^1t^n|\ln t|\,\mathrm dt
&=-\int_0^1t^n\ln t\,\mathrm dt\\
&=\frac1{n+1}\int_0^1t^n\,\mathrm dt
=\frac1{(n+1)^2}.
\end{aligned}
$$

边界项为零，因为 $t^{n+1}\ln t\to0$。

所以 $0<u_n<1/(n+1)^2$，夹逼定理给出 $\lim u_n=0$。

**17.（2010.17）答案：$\psi(t)=t^3+\dfrac32t^2$。**

**第一步：把二阶导数转为关于参数的一阶关系。**

令 $w(t)=\mathrm dy/\mathrm dx$。由 $x'(t)=2(1+t)$，有

$$
w'(t)=\frac{\mathrm d^2y}{\mathrm dx^2}\frac{\mathrm dx}{\mathrm dt}
=\frac3{4(1+t)}\cdot2(1+t)=\frac32.
$$

因此 $w(t)=3t/2+C$。

在 $t=1$ 时，$w(1)=\psi'(1)/x'(1)=6/4=3/2$，故 $C=0$。

**第二步：恢复 $\psi$。**

$$
\psi'(t)=w(t)x'(t)=\frac32t\cdot2(1+t)=3t+3t^2.
$$

积分得到 $\psi(t)=3t^2/2+t^3+C_1$。利用 $\psi(1)=5/2$，得 $C_1=0$，从而得到所求函数。

**18.（2010.18）答案：$\rho abl(2\pi/3+\sqrt3/4)$。**

**第一步：确定椭圆截面的油面高度。**

以椭圆中心为坐标原点，端面方程为

$$
\frac{x^2}{a^2}+\frac{y^2}{b^2}=1.
$$

罐底在 $y=-b$，油面距罐底 $3b/2$，所以油面为 $y=b/2$。

**第二步：计算充油截面积。**

高度 $y$ 处的横向宽度为 $2a\sqrt{1-y^2/b^2}$，所以

$$
S=2a\int_{-b}^{b/2}\sqrt{1-y^2/b^2}\,\mathrm dy
=2ab\int_{-1}^{1/2}\sqrt{1-t^2}\,\mathrm dt.
$$

利用圆弧积分的原函数，

$$
\begin{aligned}
S&=ab\left[t\sqrt{1-t^2}+\arcsin t\right]_{-1}^{1/2}\\
&=ab\left(\frac{\sqrt3}{4}+\frac\pi6+\frac\pi2\right)\\
&=ab\left(\frac{2\pi}{3}+\frac{\sqrt3}{4}\right).
\end{aligned}
$$

**第三步：从面积求质量。**

油罐沿轴向长度为 $l$，所以油的体积为 $lS$，质量为

$$
m=\rho lS=\rho abl\left(\frac{2\pi}{3}+\frac{\sqrt3}{4}\right),
$$

单位为千克。

**19.（2010.19）答案：$(a,b)=(-2,-2/5)$ 或互换。**

**第一步：写出微分算子的变化。**

由 $\xi=x+ay$、$\eta=x+by$，得到

$$
\partial_x=\partial_\xi+\partial_\eta,
\qquad \partial_y=a\partial_\xi+b\partial_\eta.
$$

因此

$$
u_{xx}=u_{\xi\xi}+2u_{\xi\eta}+u_{\eta\eta},
$$

$$
u_{xy}=a u_{\xi\xi}+(a+b)u_{\xi\eta}+b u_{\eta\eta},
$$

$$
u_{yy}=a^2u_{\xi\xi}+2ab u_{\xi\eta}+b^2u_{\eta\eta}.
$$

**第二步：令两个纯二阶导数的系数为零。**

代回原式，$u_{\xi\xi}$、$u_{\eta\eta}$ 的系数分别为

$$
5a^2+12a+4,\qquad5b^2+12b+4.
$$

方程 $5t^2+12t+4=0$ 的两个根为 $-2,-2/5$。

还需变换可逆，即雅可比 $b-a\ne0$，所以 $a,b$ 必须分别取这两个不同的根。

**第三步：检查混合项系数。**

这时 $a+b=-12/5$、$ab=4/5$，混合项系数为

$$
8+12(a+b)+10ab=-\frac{64}{5}\ne0.
$$

因此方程化为 $(-64/5)u_{\xi\eta}=0$，等价于 $u_{\xi\eta}=0$。

**20.（2010.20）答案：$1/3-\pi/16$。**

**第一步：把积分改写到直角坐标。**

令 $x=r\cos\theta$、$y=r\sin\theta$，则 $\mathrm dx\mathrm dy=r\,\mathrm dr\,\mathrm d\theta$。

原式中的 $r^2\sin\theta\,\mathrm dr\,\mathrm d\theta$ 正好化为 $y\,\mathrm dx\mathrm dy$，而

$$
1-r^2\cos2\theta=1-x^2+y^2.
$$

区域条件化为 $0\le y\le x\le1$。因此

$$
I=\int_0^1\mathrm dx\int_0^x y\sqrt{1-x^2+y^2}\,\mathrm dy.
$$

**第二步：先算内层积分。**

$$
\int_0^x y\sqrt{1-x^2+y^2}\,\mathrm dy
=\frac13\left[1-(1-x^2)^{3/2}\right].
$$

所以

$$
I=\frac13-\frac13\int_0^1(1-x^2)^{3/2}\,\mathrm dx.
$$

**第三步：三角代换。**

令 $x=\sin t$，得到

$$
\int_0^1(1-x^2)^{3/2}\,\mathrm dx
=\int_0^{\pi/2}\cos^4t\,\mathrm dt=\frac{3\pi}{16}.
$$

因此 $I=1/3-\pi/16$。

**21.（2010.21）中值结论的证明。**

为使导数中出现 $x^2$，令

$$
g(x)=f(x)-\frac{x^3}{3}.
$$

题设给出 $g(0)=g(1)=0$。

在 $[0,1/2]$ 上应用拉格朗日中值定理，存在 $\xi\in(0,1/2)$，使

$$
g'(\xi)=2[g(1/2)-g(0)]=2g(1/2).
$$

在 $[1/2,1]$ 上同样存在 $\eta\in(1/2,1)$，使

$$
g'(\eta)=2[g(1)-g(1/2)]=-2g(1/2).
$$

两式相加，得到 $g'(\xi)+g'(\eta)=0$，即

$$
f'(\xi)+f'(\eta)=\xi^2+\eta^2.
$$

两个点分别位于指定的两个开区间，故也自动互不相同。

**22.（2010.22）答案：$\lambda=-1,a=-2$，通解如下。**

**（Ⅰ）由多个解确定奇异参数。**

存在两个不同的解，说明方程有解且系数矩阵不可逆。计算

$$
\det A=(\lambda-1)^2(\lambda+1).
$$

所以 $\lambda=1$ 或 $-1$。

当 $\lambda=1$ 时，第2行方程为 $0=1$，无解，应排除。

当 $\lambda=-1$ 时，三行系数之和为零，因此有解要求右端之和也为零，即 $a+1+1=0$。所以 $a=-2$。

**（Ⅱ）求通解。**

代入参数，第二行给出 $x_2=-1/2$。第一行再给出 $x_1=x_3+3/2$，第三行自动满足。

令 $x_3=t$，得到

$$
\boldsymbol x=\begin{pmatrix}3/2\\-1/2\\0\end{pmatrix}
+t\begin{pmatrix}1\\0\\1\end{pmatrix},\qquad t\in\mathbb R.
$$

**23.（2010.23）答案：$a=-1$，正交矩阵如下。**

**第一步：利用指定的第一列。**

正交对角化矩阵的列向量是单位特征向量，所以 $(1,2,1)^{\mathrm T}$ 是 $A$ 的特征向量。

直接相乘，得到

$$
A\begin{pmatrix}1\\2\\1\end{pmatrix}
=\begin{pmatrix}2\\5+a\\4+2a\end{pmatrix}
=\lambda\begin{pmatrix}1\\2\\1\end{pmatrix}.
$$

第一分量给出 $\lambda=2$，第二分量给出 $a=-1$，第三分量也满足。

**第二步：补出其余正交特征向量。**

代入参数后，另外两个特征值为 $-4,5$，可分别取特征向量

$$
p_2=(1,0,-1)^{\mathrm T},\qquad p_3=(1,-1,1)^{\mathrm T}.
$$

它们相互正交，也都与指定向量 $(1,2,1)^{\mathrm T}$ 正交。

单位化以后，可取

$$
Q=\begin{pmatrix}
1/\sqrt6&1/\sqrt2&1/\sqrt3\\
2/\sqrt6&0&-1/\sqrt3\\
1/\sqrt6&-1/\sqrt2&1/\sqrt3
\end{pmatrix}.
$$

其第一列恰为题设给定列，且

$$
Q^{\mathrm T}Q=I,\qquad Q^{\mathrm T}AQ=\operatorname{diag}(2,-4,5).
$$

因此满足全部要求。
