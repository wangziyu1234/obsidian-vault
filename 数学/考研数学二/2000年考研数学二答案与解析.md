# 2000年考研数学二答案与解析

> 我的解答，未与官方核对。

## 答案速查

> 填空题（原卷一）：1.$-1/6$；2.$(\ln2-1)\mathrm dx$；3.$\pi/3$；4.$y=2x+1$；5.$(I+A)/2$。

> 选择题（原卷二）：1.D　2.C　3.A　4.C　5.B。

三（2000.11）：原函数为 $x-(1+\mathrm e^{-x})\ln(1+\mathrm e^x)+C$。

四（2000.12）：面积函数的积分见分段表达式。

五（2000.13）：$f^{(n)}(0)=(-1)^{n-1}n!/(n-2)$，$n\ge3$。

六（2000.14）：所求极限为 $2/\pi$。

七（2000.15）：至多 $6\ln3\approx6.59$ 年；按完整年度计为7年。

八（2000.16）：两个不同内部零点的存在性证明见正文。

九（2000.17）：切线为 $y=2(x-6)$。

十（2000.18）：$a=4$ 时体积最大，为 $32\pi/(375\sqrt5)$。

十一（2000.19）：$f'(x)=-\mathrm e^{-x}/(x+1)$，并有 $\mathrm e^{-x}\le f(x)\le1$。

十二（2000.20）：$\boldsymbol x=(0,0,-1/2)^{\mathrm T}+t(1,2,1)^{\mathrm T}$。

十三（2000.21）：$a=15,b=5$。

## 一、填空题（原卷一，习题册1～5）

**1.（2000.1；原卷一·1）答案：$-1/6$。**

分子有一次项相消，必须展开到三次项：

$$
\arctan x=x-\frac{x^3}{3}+O(x^5),
\qquad \arctan x-x=-\frac{x^3}{3}+O(x^5).
$$

分母则有

$$
\ln(1+2x^3)=2x^3+O(x^6).
$$

所以

$$
\lim_{x\to0}\frac{\arctan x-x}{\ln(1+2x^3)}
=\lim_{x\to0}\frac{-1/3+O(x^2)}{2+O(x^3)}=-\frac16.
$$

如果只用 $\arctan x\sim x$，相减后会丢掉真正决定极限的三次项。

**2.（2000.2；原卷一·2）答案：$(\ln2-1)\mathrm dx$。**

先确定零点对应的 $y$ 值。将 $x=0$ 代入 $2^{xy}=x+y$，得 $1=y$。

再对原等式求导：

$$
2^{xy}\ln2\,(y+xy')=1+y'.
$$

代入 $(x,y)=(0,1)$，得到 $\ln2=1+y'(0)$，故 $y'(0)=\ln2-1$。

题目要求的是微分，因此应写

$$
\left.\mathrm dy\right|_{x=0}=(\ln2-1)\mathrm dx.
$$

**3.（2000.3；原卷一·3）答案：$\pi/3$。**

**第一步：根式换元。**

令 $t=\sqrt{x-2}$，则 $x=t^2+2$、$\mathrm dx=2t\,\mathrm dt$、$x+7=t^2+9$。

当 $x$ 从2变到正无穷时，$t$ 从0变到正无穷。因此

$$
I=2\int_0^{+\infty}\frac{\mathrm dt}{t^2+9}.
$$

**第二步：使用反正切原函数。**

$$
I=\frac23\left[\arctan\frac t3\right]_0^{+\infty}
=\frac23\cdot\frac\pi2=\frac\pi3.
$$

这个换元同时消除了下端点的根式奇性，变换后在0附近连续、无穷远按 $1/t^2$ 衰减，因此积分收敛。

**4.（2000.4；原卷一·4）答案：$y=2x+1$。**

当 $x\to\pm\infty$ 时，先求斜率：

$$
k=\lim\frac yx=\lim\left(2-\frac1x\right)\mathrm e^{1/x}=2.
$$

再计算截距，不能仅凭 $y\sim2x$ 就把截距当成零。使用

$$
\mathrm e^{1/x}=1+\frac1x+\frac1{2x^2}+O(x^{-3}),
$$

得到

$$
y=(2x-1)\mathrm e^{1/x}=2x+1+O(x^{-2}).
$$

因此 $y-2x\to1$，两端共有斜渐近线 $y=2x+1$。

**5.（2000.5；原卷一·5）答案：$(I+A)/2$。**

由 $B=(I+A)^{-1}(I-A)$，将单位矩阵也写成同一左因子的形式，得到

$$
\begin{aligned}
I+B
&=(I+A)^{-1}(I+A)+(I+A)^{-1}(I-A)\\
&=2(I+A)^{-1}.
\end{aligned}
$$

所以

$$
(I+B)^{-1}=\frac12(I+A)
=\begin{pmatrix}
1&0&0&0\\
-1&2&0&0\\
0&-2&3&0\\
0&0&-3&4
\end{pmatrix}.
$$

$I+A$ 是对角元为2、4、6、8的三角矩阵，确实可逆。以上计算避免了分别展开两个四阶逆矩阵。

## 二、选择题（原卷二，习题册6～10）

**6.（2000.6；原卷二·1）答案：D，$a\ge0,b<0$。**

**第一步：由负无穷极限判断 $b$。**

若 $b=0$，函数为 $x/(a+1)$，只要有定义，就不可能在负无穷趋于零。

若 $b>0$，当 $x\to-\infty$ 时 $\mathrm e^{bx}\to0$。如果 $a\ne0$，函数按 $x/a$ 发散；如果 $a=0$，函数为 $x\mathrm e^{-bx}$，也不趋于零。

因此必须 $b<0$。此时 $\mathrm e^{bx}\to+\infty$，且 $x/\mathrm e^{bx}\to0$，满足所需极限。

**第二步：由全实轴连续判断 $a$。**

当 $b\ne0$ 时，$\mathrm e^{bx}$ 的值域是 $(0,+\infty)$。若 $a<0$，总有某个实数使 $\mathrm e^{bx}=-a$，分母为零，不可能在全实轴连续。

若 $a\ge0$，分母始终为正，所以没有这一问题。合并得到 $a\ge0,b<0$，选 D。

**7.（2000.7；原卷二·2）答案：C，$(0,f(0))$ 是拐点。**

**第一步：逐级求出零点处的导数信息。**

由原关系与 $f'(0)=0$，有 $f''(0)=0$。又因为

$$
f''(x)=x-[f'(x)]^2,
$$

右侧可导，所以 $f'''$ 存在，且

$$
f'''(x)=1-2f'(x)f''(x),\qquad f'''(0)=1.
$$

因此

$$
f'(x)=\frac{x^2}{2}+o(x^2),
\qquad f''(x)=x+o(x).
$$

**第二步：分别判断极值与拐点。**

在零点两侧充分近的非零点，$f'(x)>0$，所以函数一直增加，零点不是极值点。

但 $f''(x)$ 在左侧为负、右侧为正，凹凸性改变；函数又在该点连续，所以 $(0,f(0))$ 是拐点，选 C。

这里 $f'(0)=0$ 只说明驻点，不能直接据此判为极值。

**8.（2000.8；原卷二·3）答案：A。**

令 $h(x)=f(x)/g(x)$。因为两函数均为正，分母不为零，商法则给出

$$
h'(x)=\frac{f'(x)g(x)-f(x)g'(x)}{g(x)^2}<0.
$$

所以 $h$ 严格减少。对 $a<x<b$，有

$$
\frac{f(a)}{g(a)}>\frac{f(x)}{g(x)}>\frac{f(b)}{g(b)}.
$$

由右侧不等式同乘正数 $g(x)g(b)$，得到 $f(x)g(b)>f(b)g(x)$，即 A。左侧不等式则说明 B 的方向相反。

C、D 涉及的是乘积 $fg$，题设只控制比值的变化，不能固定乘积的增减。取 $f=1,g=\mathrm e^x$ 可排除 C；取 $f=\mathrm e^{-2x},g=\mathrm e^{-x}$ 可排除 D。

**9.（2000.9；原卷二·4）答案：C，$36$。**

展开正弦时注意三次项的系数：

$$
\sin6x=6x-\frac{(6x)^3}{6}+O(x^5)
=6x-36x^3+O(x^5).
$$

代入已知极限中的分式，得到

$$
\frac{\sin6x+xf(x)}{x^3}
=\frac{6+f(x)}{x^2}-36+O(x^2).
$$

左边极限为零，最后一项也趋于零，因此中间的目标分式必须趋于36。全过程不需要预先假设 $f$ 可导。

**10.（2000.10；原卷二·5）答案：B。**

特解 $\mathrm e^{-x}$ 和 $2x\mathrm e^{-x}$ 表明特征根 $-1$ 至少为二重根；特解 $3\mathrm e^x$ 表明1也是特征根。

方程为三阶，因此特征多项式只能是首一形式

$$
(r+1)^2(r-1)=r^3+r^2-r-1.
$$

所以微分方程为 $y'''+y''-y'-y=0$，对应 B。特解前面的常数2、3不改变其特征根。

## 三、解答题（原卷三～十三，习题册11～21）

**11.（2000.11；原卷三）答案：$x-(1+\mathrm e^{-x})\ln(1+\mathrm e^x)+C$。**

**第一步：先恢复 $f$ 的表达式。**

已知等式给的是 $f(\ln x)$。令 $u=\ln x$，则 $x=\mathrm e^u$，所以

$$
f(u)=\mathrm e^{-u}\ln(1+\mathrm e^u).
$$

将自变量重新记为 $x$，目标积分就是

$$
I=\int\mathrm e^{-x}\ln(1+\mathrm e^x)\,\mathrm dx.
$$

**第二步：分部积分。**

取 $u=\ln(1+\mathrm e^x)$、$\mathrm dv=\mathrm e^{-x}\mathrm dx$，有

$$
\mathrm du=\frac{\mathrm e^x}{1+\mathrm e^x}\,\mathrm dx,
\qquad v=-\mathrm e^{-x}.
$$

因此

$$
I=-\mathrm e^{-x}\ln(1+\mathrm e^x)
+\int\frac{\mathrm dx}{1+\mathrm e^x}.
$$

**第三步：拆分剩余被积函数。**

$$
\frac1{1+\mathrm e^x}=1-\frac{\mathrm e^x}{1+\mathrm e^x}.
$$

所以其原函数为 $x-\ln(1+\mathrm e^x)$。代回并合并，得到

$$
I=x-(1+\mathrm e^{-x})\ln(1+\mathrm e^x)+C.
$$

**检验。** 对最后的表达式求导，非对数部分相互抵消，只剩 $\mathrm e^{-x}\ln(1+\mathrm e^x)=f(x)$。

**12.（2000.12；原卷四）面积函数及其积分。**

**第一步：根据直线位置求 $S(t)$。**

当 $0\le t\le1$ 时，直线 $x+y=t$ 在正方形左下角截出直角三角形，两直角边长均为 $t$，所以 $S(t)=t^2/2$。

当 $1\le t\le2$ 时，左下方区域等于整个正方形减去右上角三角形。后者两直角边长均为 $2-t$，所以 $S(t)=1-(2-t)^2/2$。

当 $t\ge2$ 时，整个正方形都位于直线左下方，所以 $S(t)=1$。

因此

$$
S(t)=\begin{cases}
t^2/2,&0\le t\le1,\\
1-(2-t)^2/2,&1\le t\le2,\\
1,&t\ge2.
\end{cases}
$$

**第二步：按上限 $x$ 的位置逐段积分。**

记 $F(x)=\int_0^xS(t)\,\mathrm dt$。当 $0\le x\le1$ 时，直接有 $F(x)=x^3/6$。

当 $1\le x\le2$ 时，必须保留前一段的累计面积：

$$
\begin{aligned}
F(x)&=F(1)+\int_1^x\left[1-\frac{(2-t)^2}{2}\right]\mathrm dt\\
&=\frac16+(x-1)+\frac{(2-x)^3-1}{6}\\
&=x-1+\frac{(2-x)^3}{6}.
\end{aligned}
$$

当 $x\ge2$ 时，$F(2)=1$，所以 $F(x)=1+\int_2^x1\,\mathrm dt=x-1$。

最终

$$
\boxed{F(x)=\begin{cases}
x^3/6,&0\le x\le1,\\[3pt]
x-1+(2-x)^3/6,&1\le x\le2,\\[3pt]
x-1,&x\ge2.
\end{cases}}
$$

三个表达式在1、2处分别衔接，且求导后都恢复相应的 $S(x)$。

**13.（2000.13；原卷五）答案：$(-1)^{n-1}n!/(n-2)$，$n\ge3$。**

在 $|x|<1$ 内，

$$
\ln(1+x)=\sum_{k=1}^{\infty}\frac{(-1)^{k+1}}k x^k.
$$

乘以 $x^2$，得到

$$
f(x)=\sum_{k=1}^{\infty}\frac{(-1)^{k+1}}k x^{k+2}.
$$

要读出 $x^n$ 的系数，令 $k=n-2$，从而

$$
[x^n]f(x)=\frac{(-1)^{n-1}}{n-2}.
$$

麦克劳林系数等于 $f^{(n)}(0)/n!$，所以

$$
f^{(n)}(0)=\frac{(-1)^{n-1}n!}{n-2}.
$$

例如 $n=3$ 时，$f(x)$ 的首项为 $x^3$，给出 $f'''(0)=6$，与公式一致。

**14.（2000.14；原卷六）积分界与极限。**

**（Ⅰ）第一步：利用绝对余弦的周期。**

$|\cos t|$ 的周期为 $\pi$，一个周期的积分为

$$
\int_0^\pi|\cos t|\,\mathrm dt=2.
$$

当 $n\pi\le x<(n+1)\pi$ 时，将积分拆成 $n$ 个完整周期和最后一段：

$$
S(x)=2n+\int_{n\pi}^{x}|\cos t|\,\mathrm dt.
$$

**第二步：估计最后的不足一个周期的部分。**

该部分非负；而它比一个完整周期短，剩余区间上 $|\cos t|$ 的积分严格为正，因此该部分严格小于2。故

$$
2n\le S(x)<2(n+1).
$$

**（Ⅱ）把积分界转为商的界。**

对 $x\ge\pi$，令 $n=\lfloor x/\pi\rfloor\ge1$。由 $n\pi\le x<(n+1)\pi$，有

$$
\frac{2n}{(n+1)\pi}<\frac{S(x)}x<\frac{2(n+1)}{n\pi}.
$$

当 $x\to+\infty$ 时，$n\to\infty$，两侧都趋于 $2/\pi$。由夹逼定理，

$$
\lim_{x\to+\infty}\frac{S(x)}x=\frac2\pi.
$$

这个值也就是周期函数 $|\cos t|$ 在一个周期内的平均值。

**15.（2000.15；原卷七）答案：至多 $6\ln3$ 年，按整年计7年。**

**第一步：以污染物总量建立收支关系。**

设从2000年初起经过 $t$ 年时，湖中的污染物总量为 $m(t)$，初值 $m(0)=5m_0$。

湖水流入、流出总量相同，因此水量始终为 $V$。水均匀混合，流出浓度为 $m(t)/V$，年流出水量为 $V/3$，所以污染物年流出率为 $m(t)/3$。

允许的污水浓度至多 $m_0/V$，污水年流入量为 $V/6$，所以污染物年流入率至多为 $m_0/6$。

因此

$$
m'(t)\le\frac{m_0}{6}-\frac{m(t)}3.
$$

**第二步：求最慢达标情形的污染量上界。**

持续按照最大允许浓度排入时，取等号得到

$$
M'+\frac13M=\frac{m_0}{6},\qquad M(0)=5m_0.
$$

积分因子为 $\mathrm e^{t/3}$，解得

$$
M(t)=\frac{m_0}{2}+\frac{9m_0}{2}\mathrm e^{-t/3}.
$$

对原不等式同乘积分因子并积分，也得到 $m(t)\le M(t)$，所以这个等号解确实给出最迟达标的上界。

**第三步：解达标时间不等式。**

要求 $M(t)\le m_0$，即

$$
\frac12+\frac92\mathrm e^{-t/3}\le1
\quad\Longleftrightarrow\quad
\mathrm e^{-t/3}\le\frac19.
$$

取对数并注意乘负数时方向改变，得到

$$
t\ge3\ln9=6\ln3\approx6.59.
$$

因此最迟约6.59年可降至 $m_0$；如果时间按完整年度计数，则7年可以保证达标。若实际排入浓度低于上限，达标时间只会提前。

**16.（2000.16；原卷八）证明存在两个不同的内部零点。**

**第一步：排除恒零情形，并说明至少有一个内部零点。**

若 $f\equiv0$，任取两个不同的内部点即可。以下设 $f$ 不恒为零。

因为 $\int_0^\pi f(x)\,\mathrm dx=0$，连续非零函数不能在整个区间保持同一个符号，所以 $f$ 在内部既有正值也有负值。零点定理保证至少有一个内部零点。

**第二步：反设只有一个内部零点 $c$。**

在 $(0,c)$ 与 $(c,\pi)$ 内，函数都不再为零，由连续性，它在每一段上只能保持固定符号。又积分为零，所以左右两段的符号必须相反。

余弦函数在 $[0,\pi]$ 上严格减少，因此

$$
\cos x-\cos c>0\quad(x<c),\qquad
\cos x-\cos c<0\quad(x>c).
$$

**第三步：构造一个积分既应为零、又不可能为零的函数。**

令 $g(x)=f(x)(\cos x-\cos c)$。由于两因子都在 $c$ 两侧改变符号，乘积在两侧的符号相同；除 $c$ 和可能的端点外，这个符号严格非零。

因此 $\int_0^\pi g(x)\,\mathrm dx$ 必严格为正或严格为负。

另一方面，利用两个已知积分，

$$
\int_0^\pi g(x)\,\mathrm dx
=\int_0^\pi f(x)\cos x\,\mathrm dx
-\cos c\int_0^\pi f(x)\,\mathrm dx=0,
$$

矛盾。所以内部零点不能只有一个，至少存在两个不同的点 $\xi_1,\xi_2$ 满足结论。

**17.（2000.17；原卷九）答案：$y=2(x-6)$。**

**第一步：用给定关系确定 $f(1)$。**

当 $x\to0$ 时，连续性给出左边极限为 $f(1)-3f(1)=-2f(1)$，右边因 $\alpha(x)=o(x)$ 而趋于零。所以 $f(1)=0$。

**第二步：提取一阶增量，求 $f'(1)$。**

由 $f$ 在1处可导，

$$
f(1+\sin x)=f(1)+f'(1)\sin x+o(x),
$$

$$
f(1-\sin x)=f(1)-f'(1)\sin x+o(x).
$$

代入给定关系，并使用 $f(1)=0$，得到

$$
4f'(1)\sin x+o(x)=8x+o(x).
$$

除以 $x$ 取极限，得到 $4f'(1)=8$，所以 $f'(1)=2$。

**第三步：利用周期把结果移到 $x=6$。**

周期为5，故 $f(6)=f(1)=0$。且对小增量 $h$，$f(6+h)=f(1+h)$，所以导数差商相同，$f'(6)=f'(1)=2$。

因此经过 $(6,0)$ 的切线为 $y=2(x-6)$。

**18.（2000.18；原卷十）答案：$a=4$，最大体积 $32\pi/(375\sqrt5)$。**

**第一步：求交点和连接直线。**

两曲线交于 $ax^2=1-x^2$，所以在非负半轴的交点横坐标为

$$
r=\frac1{\sqrt{a+1}}.
$$

交点为 $(r,ar^2)$，直线 $OA$ 的斜率为 $ar$，方程为 $y=arx$。

**第二步：用圆环截面求体积。**

在 $0\le x\le r$ 上，$arx\ge ax^2\ge0$，所以外半径为 $arx$、内半径为 $ax^2$。

$$
\begin{aligned}
V(a)&=\pi\int_0^r[(arx)^2-(ax^2)^2]\,\mathrm dx\\
&=\pi a^2\left(\frac{r^5}{3}-\frac{r^5}{5}\right)\\
&=\frac{2\pi a^2}{15(a+1)^{5/2}}.
\end{aligned}
$$

**第三步：对参数求导并判断全局最大值。**

$a>0$ 且 $V(a)>0$，取对数求导可简化计算：

$$
\frac{V'(a)}{V(a)}=\frac2a-\frac5{2(a+1)}
=\frac{4-a}{2a(a+1)}.
$$

分母恒正，所以 $V$ 在 $(0,4)$ 上增加，在 $(4,+\infty)$ 上减少，唯一最大值点为 $a=4$。

代回得到

$$
V_{\max}=\frac{2\pi\cdot16}{15\cdot5^{5/2}}
=\frac{32\pi}{375\sqrt5}.
$$

$a\to0^+$ 与 $a\to+\infty$ 时，体积都趋于零，也与此全局最大值结论一致。

**19.（2000.19；原卷十一）答案：$f'(x)=-\mathrm e^{-x}/(x+1)$，并有 $\mathrm e^{-x}\le f(x)\le1$。**

**（Ⅰ）第一步：说明可继续求导。**

记 $F(x)=\int_0^x f(t)\,\mathrm dt$。原式给出

$$
f'(x)=\frac{F(x)}{x+1}-f(x).
$$

由于 $f$ 可导、$F$ 可导，右侧可导，因此 $f''$ 存在，可以对下面整理后的等式求导：

$$
(x+1)[f'(x)+f(x)]=F(x).
$$

**第二步：消去积分，得到关于 $f'$ 的一阶方程。**

求导后

$$
f'+f+(x+1)(f''+f')=f,
$$

即

$$
(x+1)f''+(x+2)f'=0.
$$

令 $p=f'$，则 $p'+[1+1/(x+1)]p=0$。积分因子为 $(x+1)\mathrm e^x$，所以

$$
p(x)=C\frac{\mathrm e^{-x}}{x+1}.
$$

原式在0处给出 $f'(0)+f(0)=0$，即 $f'(0)=-1$，故 $C=-1$。

**（Ⅱ）把导数积分，建立上下界。**

$$
f(x)=f(0)+\int_0^x f'(t)\,\mathrm dt
=1-\int_0^x\frac{\mathrm e^{-t}}{t+1}\,\mathrm dt.
$$

当 $t\ge0$ 时，$0<\mathrm e^{-t}/(t+1)\le\mathrm e^{-t}$。因此

$$
0\le\int_0^x\frac{\mathrm e^{-t}}{t+1}\,\mathrm dt
\le\int_0^x\mathrm e^{-t}\,\mathrm dt=1-\mathrm e^{-x}.
$$

从1减去这组不等式，得到

$$
\mathrm e^{-x}\le f(x)\le1,
\qquad x\ge0.
$$

**20.（2000.20；原卷十二）答案：$\boldsymbol x=(0,0,-1/2)^{\mathrm T}+t(1,2,1)^{\mathrm T}$。**

**第一步：先区分外积矩阵与内积标量。**

$A=\alpha\beta^{\mathrm T}$ 是三阶矩阵，而 $B=\beta^{\mathrm T}\alpha$ 是一个标量：

$$
B=1\cdot1+\frac12\cdot2+0\cdot1=2.
$$

由矩阵乘法结合律，

$$
A^2=\alpha(\beta^{\mathrm T}\alpha)\beta^{\mathrm T}=2A,
\qquad A^4=8A.
$$

**第二步：把高次矩阵方程化为一次方程。**

原式变为

$$
16A\boldsymbol x=8A\boldsymbol x+16\boldsymbol x+\gamma,
$$

所以

$$
(A-2I)\boldsymbol x=\frac\gamma8=(0,0,1)^{\mathrm T}.
$$

**第三步：写出系数矩阵并消元。**

$$
A-2I=\begin{pmatrix}-1&1/2&0\\2&-1&0\\1&1/2&-2\end{pmatrix}.
$$

前两行给出同一条件 $x_2=2x_1$。代入第三行，得 $2x_1-2x_3=1$，所以 $x_3=x_1-1/2$。

令 $x_1=t$，即得完整通解。代回检查时，特解产生右端 $(0,0,1)^{\mathrm T}$，方向向量 $\alpha$ 满足 $(A-2I)\alpha=0$。

**21.（2000.21；原卷十三）答案：$a=15,b=5$。**

**第一步：求已知向量组的秩与张成平面。**

可直接验证

$$
\alpha_3=3\alpha_1+2\alpha_2.
$$

$\alpha_1,\alpha_2$ 不是比例向量，因此秩为2。一个同时垂直于它们的法向量是 $(1,-5,-3)^{\mathrm T}$，所以它们张成的平面为

$$
x_1-5x_2-3x_3=0.
$$

**第二步：利用 $\beta_3$ 可表示确定 $b$。**

$\beta_3=(b,1,0)^{\mathrm T}$ 属于上面的平面，所以 $b-5=0$，得到 $b=5$。

还可直接求出 $\beta_3=\frac12\alpha_1+\frac32\alpha_2$，验证其可表示性。

**第三步：利用相同秩确定 $a$。**

以三个 $\beta$ 为列组成矩阵，其行列式为

$$
\begin{vmatrix}0&a&5\\1&2&1\\-1&1&0\end{vmatrix}=15-a.
$$

它的秩必须等于2，因此行列式为零，得到 $a=15$。

此时前两行中由第1、3列构成的二阶子式为 $\begin{vmatrix}0&5\\1&1\end{vmatrix}=-5\ne0$，所以矩阵秩至少为2；结合行列式为零，秩恰为2。

因此两个条件均满足，参数为 $a=15,b=5$。
