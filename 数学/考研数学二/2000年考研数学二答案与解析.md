# 2000年考研数学二答案与解析

> 我的解答，未与官方核对。

## 答案速查

> 填空题（原卷一）：1.$-1/6$；2.$(\ln2-1)\mathrm dx$；3.$\pi/3$；4.$y=2x+1$；5.$(I+A)/2$。

> 选择题（原卷二）：1.D　2.C　3.A　4.C　5.B。

三（2000.11）：原函数为 $x-(1+\mathrm e^{-x})\ln(1+\mathrm e^x)+C$。

四（2000.12）：面积函数的积分见分段表达式。

五（2000.13）：$f^{(n)}(0)=(-1)^{n-1}n!/(n-2)$，$n\ge3$。

六（2000.14）：所求极限为 $2/\pi$。

七（2000.15）：至多 $6\ln3\approx6.59$ 年；按完整年度计为7年。

八（2000.16）：两个不同内部零点的存在性证明见正文。

九（2000.17）：切线为 $y=2(x-6)$。

十（2000.18）：$a=4$ 时体积最大，为 $32\pi/(375\sqrt5)$。

十一（2000.19）：$f'(x)=-\mathrm e^{-x}/(x+1)$，并有 $\mathrm e^{-x}\le f(x)\le1$。

十二（2000.20）：$\boldsymbol x=(0,0,-1/2)^{\mathrm T}+t(1,2,1)^{\mathrm T}$。

十三（2000.21）：$a=15,b=5$。

## 一、填空题（原卷一，习题册1～5）

**1.（2000.1；原卷一·1）答案：$-1/6$。**

**第一步：找出分子相消后剩余的最低次项。**

当 $x\to0$ 时，反正切的泰勒展开为

$$
\arctan x=x-\frac{x^3}{3}+O(x^5),
\qquad \arctan x-x=-\frac{x^3}{3}+O(x^5).
$$

分母以 $u=2x^3$ 代入 $\ln(1+u)=u+O(u^2)$，得到

$$
\ln(1+2x^3)=2x^3+O(x^6).
$$

**第二步：约去共同的三次无穷小。**

$$
\lim_{x\to0}\frac{\arctan x-x}{\ln(1+2x^3)}
=\lim_{x\to0}\frac{-1/3+O(x^2)}{2+O(x^3)}=-\frac16.
$$

也可以用一次洛必达法则核验。分子、分母均趋于零，且分母导数在零点的去心邻域内非零，故

$$
\lim_{x\to0}
\frac{1/(1+x^2)-1}{6x^2/(1+2x^3)}
=\lim_{x\to0}-\frac{1+2x^3}{6(1+x^2)}=-\frac16.
$$

相减式中只使用 $\arctan x\sim x$ 会丢掉三次项，必须保留到首次不相消的项。

**2.（2000.2；原卷一·2）答案：$(\ln2-1)\mathrm dx$。**

**第一步：确定对应点，并检查隐函数条件。**

将 $x=0$ 代入 $2^{xy}=x+y$，得 $y=1$。令

$$
F(x,y)=2^{xy}-x-y.
$$

$F$ 的偏导数连续，且

$$
F_y(x,y)=x\ln2\,2^{xy}-1,
\qquad F_y(0,1)=-1\ne0.
$$

因此方程在 $(0,1)$ 附近确实确定一个可导的隐函数 $y=y(x)$。

**第二步：对等式求导，最后写成微分。**

指数中的 $xy$ 要用乘积求导法则：

$$
2^{xy}\ln2\,(y+xy')=1+y'.
$$

代入 $(x,y)=(0,1)$，得到

$$
\ln2=1+y'(0),\qquad y'(0)=\ln2-1.
$$

题目要求的是 $\mathrm dy$，而不是导数的数值，所以

$$
\boxed{\left.\mathrm dy\right|_{x=0}=(\ln2-1)\,\mathrm dx.}
$$

**3.（2000.3；原卷一·3）答案：$\pi/3$。**

**第一步：根式换元。**

令 $t=\sqrt{x-2}$，则 $x=t^2+2$、$\mathrm dx=2t\,\mathrm dt$、$x+7=t^2+9$。

当 $x$ 从2变到正无穷时，$t$ 从0变到正无穷。因此

$$
I=2\int_0^{+\infty}\frac{\mathrm dt}{t^2+9}.
$$

**第二步：使用反正切原函数。**

$$
I=\frac23\left[\arctan\frac t3\right]_0^{+\infty}
=\frac23\cdot\frac\pi2=\frac\pi3.
$$

这个换元同时消除了下端点的根式奇性，变换后在0附近连续、无穷远按 $1/t^2$ 衰减，因此积分收敛。

也可直接检查原积分的两个反常端点：当 $x\to2^+$ 时，被积函数与 $1/(9\sqrt{x-2})$ 等价，幂指数为 $1/2<1$；当 $x\to+\infty$ 时，与 $x^{-3/2}$ 等价，幂指数为 $3/2>1$。两端分别收敛，换元与取极限都有依据。

**4.（2000.4；原卷一·4）答案：$y=2x+1$。**

**第一步：按斜渐近线的定义求斜率。**

若直线 $y=kx+b$ 是斜渐近线，则应有 $y-(kx+b)\to0$。先计算

$$
k=\lim_{x\to\pm\infty}\frac yx
=\lim_{x\to\pm\infty}\left(2-\frac1x\right)\mathrm e^{1/x}=2.
$$

**第二步：求截距，注意一次倒数项相消。**

利用

$$
\mathrm e^{1/x}=1+\frac1x+\frac1{2x^2}+\frac1{6x^3}+O(x^{-4}),
$$

逐项相乘可得

$$
\begin{aligned}
(2x-1)\mathrm e^{1/x}
&=2x+2+\frac1x+\frac1{3x^2}
 -1-\frac1x-\frac1{2x^2}+O(x^{-3})\\
&=2x+1-\frac1{6x^2}+O(x^{-3}).
\end{aligned}
$$

因此

$$
b=\lim_{x\to\pm\infty}(y-2x)=1,
\qquad \lim_{x\to\pm\infty}[y-(2x+1)]=0.
$$

两端共有的斜渐近线为 $\boxed{y=2x+1}$。仅有 $y\sim2x$ 只能确定斜率，不能省略截距的计算。

**5.（2000.5；原卷一·5）答案：$(I+A)/2$。**

由 $B=(I+A)^{-1}(I-A)$，将单位矩阵也写成同一左因子的形式，得到

$$
\begin{aligned}
I+B
&=(I+A)^{-1}(I+A)+(I+A)^{-1}(I-A)\\
&=2(I+A)^{-1}.
\end{aligned}
$$

所以

$$
(I+B)^{-1}=\frac12(I+A)
=\begin{pmatrix}
1&0&0&0\\
-1&2&0&0\\
0&-2&3&0\\
0&0&-3&4
\end{pmatrix}.
$$

$I+A$ 是对角元为2、4、6、8的三角矩阵，确实可逆。以上计算避免了分别展开两个四阶逆矩阵。

核验时，将上式与 $I+B$ 相乘：

$$
\bigl[2(I+A)^{-1}\bigr]\frac{I+A}{2}=I.
$$

按相反次序相乘也得 $I$，因此所得矩阵确为逆矩阵；这里始终保持乘法次序，无须把一般矩阵当成可交换的数。

## 二、选择题（原卷二，习题册6～10）

**6.（2000.6；原卷二·1）答案：D，$a\ge0,b<0$。**

**第一步：由负无穷极限判断 $b$。**

若 $b=0$，函数为 $x/(a+1)$，只要有定义，就不可能在负无穷趋于零。

若 $b>0$，当 $x\to-\infty$ 时 $\mathrm e^{bx}\to0$。如果 $a\ne0$，函数按 $x/a$ 发散；如果 $a=0$，函数为 $x\mathrm e^{-bx}$，也不趋于零。

因此必须 $b<0$。此时 $\mathrm e^{bx}\to+\infty$，且 $x/\mathrm e^{bx}\to0$，满足所需极限。

**第二步：由全实轴连续判断 $a$。**

当 $b\ne0$ 时，$\mathrm e^{bx}$ 的值域是 $(0,+\infty)$。若 $a<0$，总有某个实数使 $\mathrm e^{bx}=-a$，分母为零，不可能在全实轴连续。

若 $a\ge0$，分母始终为正，所以没有这一问题。合并得到 $a\ge0,b<0$，选 D。

还需验证这些条件足够。令 $c=-b>0$、$u=-x\to+\infty$，则

$$
f(-u)=-\frac{u\mathrm e^{-cu}}{1+a\mathrm e^{-cu}}\longrightarrow0.
$$

$a\ge0$ 又保证分母处处大于零，因此这组条件同时满足连续性和极限要求。

**7.（2000.7；原卷二·2）答案：C，$(0,f(0))$ 是拐点。**

**第一步：逐级求出零点处的导数信息。**

由原关系与 $f'(0)=0$，有 $f''(0)=0$。又因为

$$
f''(x)=x-[f'(x)]^2,
$$

右侧可导，所以 $f'''$ 存在，且

$$
f'''(x)=1-2f'(x)f''(x),\qquad f'''(0)=1.
$$

因此

$$
f'(x)=\frac{x^2}{2}+o(x^2),
\qquad f''(x)=x+o(x).
$$

**第二步：分别判断极值与拐点。**

在零点两侧充分近的非零点，$f'(x)>0$，所以函数一直增加，零点不是极值点。

但 $f''(x)$ 在左侧为负、右侧为正，凹凸性改变；函数又在该点连续，所以 $(0,f(0))$ 是拐点，选 C。

这里 $f'(0)=0$ 只说明驻点，不能直接据此判为极值。

上述符号判断可以直接从两个比值看出：

$$
\lim_{x\to0}\frac{f'(x)}{x^2}=\frac12>0,
\qquad \lim_{x\to0}\frac{f''(x)}x=1>0.
$$

所以 $f'$ 在两侧同为正，而 $f''$ 与 $x$ 同号；这分别排除了 A、B、D。

**8.（2000.8；原卷二·3）答案：A。**

**第一步：把题设识别为商函数的导数。**

令 $h(x)=f(x)/g(x)$。因为 $g(x)>0$，商函数处处有定义，且

$$
h'(x)=\frac{f'(x)g(x)-f(x)g'(x)}{g(x)^2}<0.
$$

由中值定理，$h$ 在相应区间内严格减少。因此 $a<x<b$ 时，

$$
\frac{f(a)}{g(a)}>\frac{f(x)}{g(x)}>\frac{f(b)}{g(b)}.
$$

**第二步：用分母为正确定不等号方向。**

右侧不等式同乘 $g(x)g(b)>0$，得到

$$
f(x)g(b)>f(b)g(x),
$$

即 A。左侧不等式则给出 $f(x)g(a)<f(a)g(x)$，所以 B 的方向相反。

**第三步：检查乘积型选项为何不成立。**

取 $f(x)=1$、$g(x)=\mathrm e^x$，两者为正，题设左端为 $-\mathrm e^x<0$，但 $fg=\mathrm e^x$ 递增，$x<b$ 时有 $f(x)g(x)<f(b)g(b)$，排除 C。

再取 $f(x)=\mathrm e^{-2x}$、$g(x)=\mathrm e^{-x}$，题设左端为 $-\mathrm e^{-3x}<0$，但 $fg=\mathrm e^{-3x}$ 递减，$x>a$ 时有 $f(x)g(x)<f(a)g(a)$，排除 D。

**9.（2000.9；原卷二·4）答案：C，$36$。**

展开正弦时注意三次项的系数：

$$
\sin6x=6x-\frac{(6x)^3}{6}+O(x^5)
=6x-36x^3+O(x^5).
$$

代入已知极限中的分式，得到

$$
\frac{\sin6x+xf(x)}{x^3}
=\frac{6+f(x)}{x^2}-36+O(x^2).
$$

左边极限为零，最后一项也趋于零，因此中间的目标分式必须趋于36。全过程不需要预先假设 $f$ 可导。

也可以先作恒等拆分，使待求极限直接出现：

$$
\frac{6+f(x)}{x^2}
=\frac{\sin6x+xf(x)}{x^3}
 +\frac{6x-\sin6x}{x^3}.
$$

第一项由题设趋于0；第二项令 $u=6x$，得 $6^3(u-\sin u)/u^3\to216/6=36$。

**10.（2000.10；原卷二·5）答案：B。**

**第一步：由指数解和指数乘多项式的解确定根与重数。**

常系数齐次方程的特征多项式记为 $P(r)$。$\mathrm e^{rx}$ 是解时必须 $P(r)=0$；$x\mathrm e^{rx}$ 也是解时，还必须 $P'(r)=0$。

因此 $\mathrm e^{-x}$ 与 $2x\mathrm e^{-x}$ 同时为解，说明 $-1$ 至少为二重根；$3\mathrm e^x$ 为解，说明 $1$ 是根。常数因子2、3均非零，不改变解所对应的特征根。

**第二步：利用阶数确定整个特征多项式。**

方程是三阶，已经找到的根按重数计算共有三个，故其首一特征多项式为

$$
\begin{aligned}
P(r)&=(r+1)^2(r-1)\\
&=(r^2+2r+1)(r-1)\\
&=r^3+r^2-r-1.
\end{aligned}
$$

将 $r^3,r^2,r,1$ 对应到 $y''',y'',y',y$，得到

$$
\boxed{y'''+y''-y'-y=0,}
$$

对应 B，其通解为 $(C_1+C_2x)\mathrm e^{-x}+C_3\mathrm e^x$，包含题目给出的全部特解。

## 三、解答题（原卷三～十三，习题册11～21）

**11.（2000.11；原卷三）答案：$x-(1+\mathrm e^{-x})\ln(1+\mathrm e^x)+C$。**

**第一步：由复合函数等式恢复 $f$。**

原等式中的 $x>0$。令 $u=\ln x$，则 $x=\mathrm e^u$，且 $u$ 可以取任意实数，于是

$$
f(u)=\mathrm e^{-u}\ln(1+\mathrm e^u).
$$

把自变量重新记作 $x$，目标积分为 $I=\int\mathrm e^{-x}\ln(1+\mathrm e^x)\,\mathrm dx$。

**第二步：用分部积分降低对数的复杂程度。**

取 $u=\ln(1+\mathrm e^x)$、$\mathrm dv=\mathrm e^{-x}\mathrm dx$，则

$$
\mathrm du=\frac{\mathrm e^x}{1+\mathrm e^x}\,\mathrm dx,
\qquad v=-\mathrm e^{-x}.
$$

因此

$$
I=-\mathrm e^{-x}\ln(1+\mathrm e^x)
 +\int\frac{\mathrm dx}{1+\mathrm e^x}.
$$

**第三步：拆分剩余积分并合并结果。**

$$
\frac1{1+\mathrm e^x}=1-\frac{\mathrm e^x}{1+\mathrm e^x}
\quad\Longrightarrow\quad
\int\frac{\mathrm dx}{1+\mathrm e^x}=x-\ln(1+\mathrm e^x)+C.
$$

故 $\boxed{I=x-(1+\mathrm e^{-x})\ln(1+\mathrm e^x)+C}$。求导检验为

$$
I'=1+\mathrm e^{-x}\ln(1+\mathrm e^x)
 -(1+\mathrm e^{-x})\frac{\mathrm e^x}{1+\mathrm e^x}
=\mathrm e^{-x}\ln(1+\mathrm e^x)=f(x).
$$

**12.（2000.12；原卷四）面积函数及其积分。**

**第一步：根据直线位置求 $S(t)$。**

当 $0\le t\le1$ 时，直线 $x+y=t$ 在正方形左下角截出直角三角形，两直角边长均为 $t$，所以 $S(t)=t^2/2$。

当 $1\le t\le2$ 时，左下方区域等于整个正方形减去右上角三角形。后者两直角边长均为 $2-t$，所以 $S(t)=1-(2-t)^2/2$。

当 $t\ge2$ 时，整个正方形都位于直线左下方，所以 $S(t)=1$。

因此

$$
S(t)=\begin{cases}
t^2/2,&0\le t\le1,\\
1-(2-t)^2/2,&1\le t\le2,\\
1,&t\ge2.
\end{cases}
$$

**第二步：按上限 $x$ 的位置逐段积分。**

记 $F(x)=\int_0^xS(t)\,\mathrm dt$。当 $0\le x\le1$ 时，直接有 $F(x)=x^3/6$。

当 $1\le x\le2$ 时，必须保留前一段的累计面积：

$$
\begin{aligned}
F(x)&=F(1)+\int_1^x\left[1-\frac{(2-t)^2}{2}\right]\mathrm dt\\
&=\frac16+(x-1)+\frac{(2-x)^3-1}{6}\\
&=x-1+\frac{(2-x)^3}{6}.
\end{aligned}
$$

当 $x\ge2$ 时，$F(2)=1$，所以 $F(x)=1+\int_2^x1\,\mathrm dt=x-1$。

各段在1、2处衔接，且求导恢复 $S(x)$，综上

$$
\boxed{F(x)=\begin{cases}
x^3/6,&0\le x\le1,\\[3pt]
x-1+(2-x)^3/6,&1\le x\le2,\\[3pt]
x-1,&x\ge2.
\end{cases}}
$$

**13.（2000.13；原卷五）答案：$(-1)^{n-1}n!/(n-2)$，$n\ge3$。**

**第一步：用莱布尼茨公式，利用 $x^2$ 的高阶导数为零。**

记 $g(x)=\ln(1+x)$。对 $f(x)=x^2g(x)$ 求 $n$ 阶导数时，$x^2$ 只能被求导0、1、2次，因此

$$
\begin{aligned}
f^{(n)}(x)
&=x^2g^{(n)}(x)+\binom n1(2x)g^{(n-1)}(x)
 +\binom n2\cdot2g^{(n-2)}(x)\\
&=x^2g^{(n)}(x)+2nxg^{(n-1)}(x)+n(n-1)g^{(n-2)}(x).
\end{aligned}
$$

在 $x=0$ 处，前两项都消失，所以

$$
f^{(n)}(0)=n(n-1)g^{(n-2)}(0).
$$

**第二步：写出对数函数的任意阶导数。**

从 $g'(x)=1/(1+x)$ 逐次求导，可归纳出

$$
g^{(k)}(x)=\frac{(-1)^{k-1}(k-1)!}{(1+x)^k},\qquad k\ge1.
$$

由于 $n\ge3$，可取 $k=n-2\ge1$，得到

$$
\begin{aligned}
f^{(n)}(0)
&=n(n-1)(-1)^{n-3}(n-3)!\\
&=\boxed{\frac{(-1)^{n-1}n!}{n-2}}.
\end{aligned}
$$

最后使用了 $(-1)^{n-3}=(-1)^{n-1}$ 及 $n!=n(n-1)(n-2)(n-3)!$。例如 $n=3$ 时得6，与 $f(x)=x^3+O(x^4)$ 的首项一致。

**14.（2000.14；原卷六）积分界与极限。**

**（Ⅰ）第一步：利用绝对余弦的周期。**

$|\cos t|$ 的周期为 $\pi$，一个周期的积分为

$$
\int_0^\pi|\cos t|\,\mathrm dt
=\int_0^{\pi/2}\cos t\,\mathrm dt
 -\int_{\pi/2}^{\pi}\cos t\,\mathrm dt=1+1=2.
$$

当 $n\pi\le x<(n+1)\pi$ 时，将积分拆成 $n$ 个完整周期和最后一段：

$$
S(x)=2n+\int_{n\pi}^{x}|\cos t|\,\mathrm dt.
$$

**第二步：估计最后的不足一个周期的部分。**

该部分非负；而它比一个完整周期短，剩余区间上 $|\cos t|$ 的积分严格为正，因此该部分严格小于2。故

$$
2n\le S(x)<2(n+1).
$$

**（Ⅱ）把积分界转为商的界。**

对 $x\ge\pi$，令 $n=\lfloor x/\pi\rfloor\ge1$。由 $n\pi\le x<(n+1)\pi$，有

$$
\frac{2n}{(n+1)\pi}<\frac{S(x)}x<\frac{2(n+1)}{n\pi}.
$$

当 $x\to+\infty$ 时，$n\to\infty$，两侧都趋于 $2/\pi$。由夹逼定理，

$$
\lim_{x\to+\infty}\frac{S(x)}x=\frac2\pi.
$$

这个值也就是周期函数 $|\cos t|$ 在一个周期内的平均值。

**15.（2000.15；原卷七）答案：至多 $6\ln3$ 年，按整年计7年。**

**第一步：由污染物的流入量和流出量建立不等式。**

从2000年初起计时，设 $t$ 年后的污染物总量为 $m(t)$，则 $m(0)=5m_0$。两路水的流入量之和为 $V/6+V/6=V/3$，与流出量相同，因此湖水量恒为 $V$。

设污水浓度为 $c(t)$。均匀混合时，流出水浓度是 $m(t)/V$；清水不带入污染物，故

$$
m'(t)=\frac V6c(t)-\frac V3\frac{m(t)}V
\le\frac{m_0}{6}-\frac{m(t)}3,
\qquad c(t)\le\frac{m_0}V.
$$

**第二步：用积分因子求所有允许排放情形共同的上界。**

移项后同乘正数 $\mathrm e^{t/3}$，不等号方向不变：

$$
\bigl[\mathrm e^{t/3}m(t)\bigr]'
=\mathrm e^{t/3}\left(m'+\frac13m\right)
\le\frac{m_0}{6}\mathrm e^{t/3}.
$$

在 $[0,t]$ 上积分，得到

$$
\begin{aligned}
\mathrm e^{t/3}m(t)-5m_0
&\le\frac{m_0}{6}\int_0^t\mathrm e^{s/3}\,\mathrm ds
=\frac{m_0}{2}(\mathrm e^{t/3}-1),\\
m(t)&\le\frac{m_0}{2}+\frac{9m_0}{2}\mathrm e^{-t/3}
=:M(t).
\end{aligned}
$$

**第三步：解出最迟达标时间，并说明这个上界能取到。**

$$
M(t)\le m_0
\iff \mathrm e^{-t/3}\le\frac19
\iff t\ge3\ln9=6\ln3\approx6.59.
$$

若污水始终按允许的最大浓度 $m_0/V$ 排入，上述各步都取等号，实际含量就是 $M(t)$。它严格递减，并恰在 $t=6\ln3$ 时降到 $m_0$，因此这确是最迟达标时间。按完整年度计，第7年内可以保证达标。

**16.（2000.16；原卷八）证明存在两个不同的内部零点。**

**第一步：排除恒零情形，并说明至少有一个内部零点。**

若 $f\equiv0$，任取两个不同的内部点即可。以下设 $f$ 不恒为零。

因为 $\int_0^\pi f(x)\,\mathrm dx=0$，连续非零函数不能在整个区间保持同一个符号，所以 $f$ 在内部既有正值也有负值。零点定理保证至少有一个内部零点。

**第二步：反设只有一个内部零点 $c$。**

在 $(0,c)$ 与 $(c,\pi)$ 内，函数都不再为零，由连续性，它在每一段上只能保持固定符号。又积分为零，所以左右两段的符号必须相反。

余弦函数在 $[0,\pi]$ 上严格减少，因此

$$
\cos x-\cos c>0\quad(x<c),\qquad
\cos x-\cos c<0\quad(x>c).
$$

**第三步：构造一个积分既应为零、又不可能为零的函数。**

令 $g(x)=f(x)(\cos x-\cos c)$。由于两因子都在 $c$ 两侧改变符号，乘积在两侧的符号相同；除 $c$ 和可能的端点外，这个符号严格非零。

因此 $\int_0^\pi g(x)\,\mathrm dx$ 必严格为正或严格为负。

另一方面，利用两个已知积分，

$$
\int_0^\pi g(x)\,\mathrm dx
=\int_0^\pi f(x)\cos x\,\mathrm dx
-\cos c\int_0^\pi f(x)\,\mathrm dx=0,
$$

矛盾。所以内部零点不能只有一个，至少存在两个不同的点 $\xi_1,\xi_2$ 满足结论。

**17.（2000.17；原卷九）答案：$y=2(x-6)$。**

**第一步：用给定关系确定 $f(1)$。**

当 $x\to0$ 时，连续性给出左边极限为 $f(1)-3f(1)=-2f(1)$，右边因 $\alpha(x)=o(x)$ 而趋于零。所以 $f(1)=0$。

**第二步：提取一阶增量，求 $f'(1)$。**

由 $f$ 在1处可导，

$$
f(1+\sin x)=f(1)+f'(1)\sin x+o(x),
$$

$$
f(1-\sin x)=f(1)-f'(1)\sin x+o(x).
$$

代入给定关系，并使用 $f(1)=0$，得到

$$
4f'(1)\sin x+o(x)=8x+o(x).
$$

除以 $x$ 取极限，得到 $4f'(1)=8$，所以 $f'(1)=2$。

**第三步：利用周期把结果移到 $x=6$。**

周期为5，故 $f(6)=f(1)=0$。且对小增量 $h$，$f(6+h)=f(1+h)$，所以导数差商相同，$f'(6)=f'(1)=2$。

因此经过 $(6,0)$ 的切线为 $y=2(x-6)$。

这里把 $o(\sin x)$ 写成 $o(x)$ 的依据是 $\sin x/x\to1$。周期性对任意足够小的 $h$ 都给出

$$
\frac{f(6+h)-f(6)}h=\frac{f(1+h)-f(1)}h,
$$

所以1处可导同时保证6处可导，切线斜率的平移有严格的差商依据。

**18.（2000.18；原卷十）答案：$a=4$，最大体积 $32\pi/(375\sqrt5)$。**

**第一步：求交点和连接直线。**

两曲线交于 $ax^2=1-x^2$，所以在非负半轴的交点横坐标为

$$
r=\frac1{\sqrt{a+1}}.
$$

交点为 $(r,ar^2)$，直线 $OA$ 的斜率为 $ar$，方程为 $y=arx$。

**第二步：用圆环截面求体积。**

在 $0\le x\le r$ 上，$arx\ge ax^2\ge0$，所以外半径为 $arx$、内半径为 $ax^2$。

$$
\begin{aligned}
V(a)&=\pi\int_0^r[(arx)^2-(ax^2)^2]\,\mathrm dx\\
&=\pi a^2\left(\frac{r^5}{3}-\frac{r^5}{5}\right)\\
&=\frac{2\pi a^2}{15(a+1)^{5/2}}.
\end{aligned}
$$

**第三步：对参数求导并判断全局最大值。**

$a>0$ 且 $V(a)>0$，取对数求导可简化计算：

$$
\frac{V'(a)}{V(a)}=\frac2a-\frac5{2(a+1)}
=\frac{4-a}{2a(a+1)}.
$$

分母恒正，所以 $V$ 在 $(0,4)$ 上增加，在 $(4,+\infty)$ 上减少，唯一最大值点为 $a=4$。

代回得到

$$
V_{\max}=\frac{2\pi\cdot16}{15\cdot5^{5/2}}
=\frac{32\pi}{375\sqrt5}.
$$

$a\to0^+$ 与 $a\to+\infty$ 时，体积都趋于零，也与此全局最大值结论一致。

**19.（2000.19；原卷十一）答案：$f'(x)=-\mathrm e^{-x}/(x+1)$，并有 $\mathrm e^{-x}\le f(x)\le1$。**

**（Ⅰ）消去积分，求导函数。**

记 $F(x)=\int_0^x f(t)\,\mathrm dt$。原式给出 $f'=F/(x+1)-f$；右侧可导，所以 $f''$ 存在。把原式乘以 $x+1$ 后求导，得

$$
\begin{aligned}
(x+1)(f'+f)&=F,\\
f'+f+(x+1)(f''+f')&=F'=f,\\
(x+1)f''+(x+2)f'&=0.
\end{aligned}
$$

令 $p=f'$，得到 $p'+[1+1/(x+1)]p=0$。其积分因子为 $\exp(x+\ln(x+1))=(x+1)\mathrm e^x$，因此

$$
\bigl[(x+1)\mathrm e^xp(x)\bigr]'=0
\quad\Longrightarrow\quad
p(x)=C\frac{\mathrm e^{-x}}{x+1}.
$$

原等式在0处给出 $f'(0)+f(0)=0$，即 $p(0)=-1$，从而 $C=-1$，故

$$
\boxed{f'(x)=-\frac{\mathrm e^{-x}}{x+1}.}
$$

**（Ⅱ）积分导函数，比较被积函数以建立上下界。**

由初值及牛顿—莱布尼茨公式，

$$
f(x)=1-\int_0^x\frac{\mathrm e^{-t}}{t+1}\,\mathrm dt.
$$

当 $t\ge0$ 时，$0<\mathrm e^{-t}/(t+1)\le\mathrm e^{-t}$，所以

$$
0\le\int_0^x\frac{\mathrm e^{-t}}{t+1}\,\mathrm dt
\le\int_0^x\mathrm e^{-t}\,\mathrm dt=1-\mathrm e^{-x}.
$$

从1减去两端及中间的各项，不等号方向相应反转，便得到 $\boxed{\mathrm e^{-x}\le f(x)\le1}$。$x=0$ 时等号成立；$x>0$ 时两侧均为严格不等式。

**20.（2000.20；原卷十二）答案：$\boldsymbol x=(0,0,-1/2)^{\mathrm T}+t(1,2,1)^{\mathrm T}$，$t\in\mathbb R$。**

**第一步：区分外积矩阵与内积标量，降低矩阵幂次。**

$A=\alpha\beta^{\mathrm T}$ 是三阶矩阵，而 $B=\beta^{\mathrm T}\alpha$ 是标量。具体为

$$
A=\begin{pmatrix}1&1/2&0\\2&1&0\\1&1/2&0\end{pmatrix},
\qquad B=1\cdot1+\frac12\cdot2+0\cdot1=2.
$$

由乘法结合律，

$$
A^2=\alpha(\beta^{\mathrm T}\alpha)\beta^{\mathrm T}=2A,
\qquad A^4=(2A)^2=4A^2=8A.
$$

**第二步：代回原式，得到普通非齐次线性方程组。**

原式化为 $16A\boldsymbol x=8A\boldsymbol x+16\boldsymbol x+\gamma$，即

$$
(A-2I)\boldsymbol x=\frac\gamma8=(0,0,1)^{\mathrm T}.
$$

增广矩阵消元为

$$
\begin{pmatrix}-1&1/2&0&0\\2&-1&0&0\\1&1/2&-2&1\end{pmatrix}
\ \longrightarrow\
\begin{pmatrix}-1&1/2&0&0\\0&1&-2&1\\0&0&0&0\end{pmatrix}.
$$

**第三步：选取自由变量，写出全部解。**

第一行给出 $x_2=2x_1$，第二行给出 $x_2-2x_3=1$。令 $x_1=t$，则

$$
\boxed{\boldsymbol x=
\begin{pmatrix}0\\0\\-1/2\end{pmatrix}
+t\begin{pmatrix}1\\2\\1\end{pmatrix},\qquad t\in\mathbb R.}
$$

系数矩阵及增广矩阵的秩均为2，因此三个未知量恰有一个自由参数。特解代入产生右端 $(0,0,1)^{\mathrm T}$，方向向量 $\alpha$ 又满足 $(A-2I)\alpha=0$，所以通解既满足原式，也没有遗漏。

**21.（2000.21；原卷十三）答案：$a=15,b=5$。**

**第一步：确定 $\alpha$ 向量组的秩。**

直接计算可得

$$
\alpha_3=3\alpha_1+2\alpha_2.
$$

$\alpha_1,\alpha_2$ 的前两分量组成的二阶子式为

$$
\begin{vmatrix}1&3\\2&0\end{vmatrix}=-6\ne0,
$$

所以这两个向量线性无关，整个 $\alpha$ 组的秩恰为2。因此 $\beta$ 组的秩也必须为2。

**第二步：利用 $\beta_3$ 的可表示性求 $b$。**

由于 $\alpha_3$ 已可由前两个向量表示，$\beta_3$ 可由 $\alpha$ 组表示等价于存在 $c,d$ 使 $\beta_3=c\alpha_1+d\alpha_2$。按分量写成

$$
\begin{cases}
c+3d=b,\\
2c=1,\\
-3c+d=0.
\end{cases}
\qquad\Longrightarrow\qquad
c=\frac12,\quad d=\frac32,\quad b=5.
$$

**第三步：用秩为2求 $a$，并核验充分性。**

三个 $\beta$ 向量构成的三阶行列式必须为零：

$$
\begin{aligned}
\det(\beta_1,\beta_2,\beta_3)
&=\begin{vmatrix}0&a&5\\1&2&1\\-1&1&0\end{vmatrix}\\
&=-a\begin{vmatrix}1&1\\-1&0\end{vmatrix}
 +5\begin{vmatrix}1&2\\-1&1\end{vmatrix}
=-a+15=0.
\end{aligned}
$$

故 $a=15$。此时 $\beta_2=-\beta_1+3\beta_3$；而 $\beta_1,\beta_3$ 的前两分量所成子式为 $-5\ne0$，故 $\beta$ 组的秩确为2。再结合 $\beta_3=\tfrac12\alpha_1+\tfrac32\alpha_2$，两个题设条件都已满足。
