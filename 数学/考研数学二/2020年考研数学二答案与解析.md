# 2020年考研数学二答案与解析

> 我的解答，未与官方核对。

## 答案速查

> 选择题：1.D　2.C　3.A　4.A　5.B　6.B　7.C　8.D。

> 填空题：9.$-\sqrt2$；10.$\dfrac29(2\sqrt2-1)$；11.$(\pi-1)\mathrm dx-\mathrm dy$；12.$\dfrac13\rho ga^3$；13.$1$；14.$a^2(a^2-4)$。

15. 斜渐近线为 $y=\dfrac{x}{\mathrm e}+\dfrac1{2\mathrm e}$。

16. $g'(0)=\dfrac12$，其余点的导数见正文；$g'$ 在 $0$ 处连续。

17. 在 $(\dfrac16,\dfrac1{12})$ 处取得极小值 $-\dfrac1{216}$，无极大值。

18. $f(x)=\dfrac{x}{\sqrt{1+x^2}}$；旋转体体积 $\dfrac{\pi^2}{6}$。

19. 积分值为 $\dfrac34[\sqrt2+\ln(1+\sqrt2)]$。

20. 两个中值结论的证明见正文。

21. 曲线为 $y=Cx^3$，其中 $C>0$。

22. $a=-\dfrac12$；可逆变换矩阵见正文。

23. $P^{-1}AP=\begin{pmatrix}0&6\\1&-1\end{pmatrix}$；$A$ 可相似对角化。

## 一、选择题（1～8）

**1.（2020.1）答案：D。**

分别求四个积分的首项，再比较幂次。这里 $x\to0^+$，各积分上限均趋于零。

**A。** $\mathrm e^{t^2}-1\sim t^2$，所以

$$
\int_0^x(\mathrm e^{t^2}-1)\,\mathrm dt\sim\frac{x^3}{3}.
$$

**B。** $\ln(1+\sqrt{t^3})\sim t^{3/2}$，所以

$$
\int_0^x\ln(1+\sqrt{t^3})\,\mathrm dt\sim\frac25x^{5/2}.
$$

**C。** $\sin t^2\sim t^2$，再利用 $\sin x\sim x$，得到

$$
\int_0^{\sin x}\sin t^2\,\mathrm dt\sim\frac{\sin^3x}{3}\sim\frac{x^3}{3}.
$$

**D。** 在积分区间内 $t\ge0$，$\sqrt{\sin^3t}\sim t^{3/2}$，故

$$
\begin{aligned}
\int_0^{1-\cos x}\sqrt{\sin^3t}\,\mathrm dt
&\sim\frac25(1-\cos x)^{5/2}\\
&\sim\frac25\left(\frac{x^2}{2}\right)^{5/2}
=\frac{x^5}{10\sqrt2}.
\end{aligned}
$$

四者的阶数分别为 $3,5/2,3,5$，最高阶的是 D。

这些积分等价式可由同一个法则得到：若 $h(t)\sim c t^p$，$p>-1$，则

$$
\lim_{u\to0^+}\frac{\int_0^u h(t)\,\mathrm dt}{u^{p+1}/(p+1)}
=\lim_{u\to0^+}\frac{h(u)}{u^p}=c.
$$

这是变上限积分求导后使用洛必达法则的结果。再把各自的上限代入，尤其 D 的上限本身为二阶无穷小，才得到五阶，而不只是 $5/2$ 阶。

**2.（2020.2）答案：C，第二类间断点共有3个。**

先找表达式可能失去意义的位置：$x=-1,0,1,2$。其余位置由连续函数复合得到，均连续。

**在 $x=-1$ 处。** $\ln|1+x|\to-\infty$，其他因子趋于非零有限值，故函数无界，是第二类间断点。

**在 $x=0$ 处。** 由 $\ln(1+x)\sim x$、$\mathrm e^x-1\sim x$，

$$
\lim_{x\to0}f(x)
=\frac{\mathrm e^{-1}}{-2}
=-\frac1{2\mathrm e}.
$$

这是可去间断点，属于第一类。

**在 $x=1$ 处。** $\mathrm e^{1/(x-1)}$ 在左侧趋于 $0$，在右侧趋于 $+\infty$，而剩余因子趋于非零有限值。因此有一侧极限无穷，是第二类间断点。

**在 $x=2$ 处。** 分母中的 $x-2$ 趋于零，分子与分母其他因子均趋于非零值，所以是无穷间断点，属于第二类。

故第二类间断点为 $-1,1,2$，共3个。

还可将无穷极限的方向写明：在 $-1$ 附近，除对数外的总因子为正，故两侧均趋于 $-\infty$；在1处，右侧极限为 $-\infty$，左侧极限为0；在2处，左侧极限为 $-\infty$，右侧为 $+\infty$。第二类间断点不要求两侧极限都不存在，只要不能同时为有限值即可。

**3.（2020.3）答案：A，$\dfrac{\pi^2}{4}$。**

令 $u=\arcsin\sqrt x$，则 $x=\sin^2u$，$0\le u\le\pi/2$，并且

$$
\mathrm dx=2\sin u\cos u\,\mathrm du,
\qquad \sqrt{x(1-x)}=\sin u\cos u.
$$

代入后根式消去：

$$
I=2\int_0^{\pi/2}u\,\mathrm du
=\left[u^2\right]_0^{\pi/2}=\frac{\pi^2}{4}.
$$

端点的根式奇性经换元后消失，积分收敛。

也可识别为完全微分：对 $0<x<1$，

$$
\frac{\mathrm d}{\mathrm dx}(\arcsin\sqrt x)^2
=2\arcsin\sqrt x\cdot\frac1{2\sqrt{x(1-x)}}.
$$

因此把积分写成两端极限后，其值就是 $(\pi/2)^2-0^2$。这里根号里的两个因子在积分区间内均非负，换元时没有额外的绝对值符号变化。

**4.（2020.4）答案：A，$-\dfrac{n!}{n-2}$。**

**第一步：用莱布尼茨公式处理多项式与对数的乘积。**

令 $g(x)=\ln(1-x)$。由于 $x^2$ 的三阶及更高阶导数为零，$n\ge3$ 时

$$
\begin{aligned}
f^{(n)}(x)
&=x^2g^{(n)}(x)+\binom n1(2x)g^{(n-1)}(x)
 +\binom n2\cdot2g^{(n-2)}(x)\\
&=x^2g^{(n)}(x)+2nxg^{(n-1)}(x)+n(n-1)g^{(n-2)}(x).
\end{aligned}
$$

代入0，前两项为零，得到

$$
f^{(n)}(0)=n(n-1)g^{(n-2)}(0).
$$

**第二步：求对数函数的相应阶导数。**

从 $g'(x)=-1/(1-x)$ 逐次求导，可归纳出

$$
g^{(k)}(x)=-\frac{(k-1)!}{(1-x)^k},\qquad k\ge1.
$$

注意这里各阶导数的符号始终为负。取 $k=n-2$，得到

$$
f^{(n)}(0)=-n(n-1)(n-3)!
=\boxed{-\frac{n!}{n-2}}.
$$

例如 $f(x)=-x^3+O(x^4)$，给出 $f'''(0)=-6$，与 $n=3$ 的结果一致。

**5.（2020.5）答案：B，①③④正确。**

由定义，在原点 $f(0,0)=0$。

**①正确。** 沿 $x$ 轴有 $f(h,0)=h$，所以

$$
f_x(0,0)=\lim_{h\to0}\frac{f(h,0)-f(0,0)}h=1.
$$

**②错误。** 混合偏导存在，需要相应的一阶偏导在附近有定义。

例如固定 $x\ne0$，沿 $y$ 方向在 $(x,0)$ 求偏导时，差商为

$$
\frac{f(x,k)-f(x,0)}k
=\frac{xk-x}{k}=x-\frac xk,
$$

其有限极限不存在。因此不能继续对这个一阶偏导求 $x$ 偏导。反过来的求导次序也存在同样问题，②所写混合偏导并不存在。

**③正确。** 对任意 $(x,y)$，均有

$$
|f(x,y)|\le|x|+|y|+|xy|\longrightarrow0.
$$

由夹逼定理，二元极限为零。

**④正确。** 固定 $y\ne0$，有 $\lim_{x\to0}f(x,y)=\lim_{x\to0}xy=0$；当 $y=0$ 时，内层极限也为 $\lim_{x\to0}x=0$。所以内层极限是恒为零的函数，外层极限仍为零。

三个结论正确，选 B。

在非坐标轴的点，确实可以从 $xy$ 求得混合偏导1，但这一公式的适用范围不包含原点。③说明 $f$ 在原点连续，①说明一个一阶偏导存在，这些都不能代替②所要求的“先在轴上求出一阶偏导，再对它求导”的过程。

**6.（2020.6）答案：B。**

令 $F(x)=\mathrm e^{-x}f(x)$，则

$$
F'(x)=\mathrm e^{-x}[f'(x)-f(x)]>0.
$$

所以 $F$ 在 $[-2,2]$ 上严格增加。取 $-1<0$，得到

$$
f(0)=F(0)>F(-1)=\mathrm e f(-1).
$$

由于 $f(-1)>0$，可除以它，得 $f(0)/f(-1)>\mathrm e$，正是 B。

同样比较其他端点可得

$$
\frac{f(-2)}{f(-1)}<\mathrm e^{-1}<1,
\qquad
\frac{f(1)}{f(-1)}>\mathrm e^2,
\qquad
\frac{f(2)}{f(-1)}>\mathrm e^3.
$$

因此 A、C、D 的不等号方向均不符合题设。

四个选项也可统一用 $a<b$ 时的比较式处理：

$$
\mathrm e^{-b}f(b)>\mathrm e^{-a}f(a)
\quad\Longrightarrow\quad
\frac{f(b)}{f(a)}>\mathrm e^{b-a}.
$$

取 $(a,b)=(-1,0),(-1,1),(-1,2)$ 即得后三个比值的正确方向；取 $(-2,-1)$ 后求倒数，即得第一个比值小于 $\mathrm e^{-1}$。

**7.（2020.7）答案：C。**

**第一步：由非零三阶子式确定 $A$ 与伴随矩阵的秩。**

$A$ 为四阶且不可逆，故 $r(A)\le3$；$A_{12}\ne0$ 又说明存在非零三阶子式，故 $r(A)\ge3$。因此 $r(A)=3$，齐次方程 $A\boldsymbol x=0$ 的解空间维数为1。

因为 $AA^*=(\det A)I=0$，$A^*$ 的每一列都属于这个一维解空间，故 $r(A^*)\le1$。$A^*$ 含非零元素 $A_{12}$，所以

$$
r(A^*)=1,\qquad \dim\ker A^*=4-1=3.
$$

**第二步：从 $A$ 的列向量中选择三个独立的解。**

由另一个乘法次序的伴随恒等式，

$$
A^*A=0
\quad\Longrightarrow\quad A^*\alpha_j=0\quad(j=1,2,3,4).
$$

所以 $A$ 的四列都是待求方程的解。$A_{12}$ 对应删除第1行和第2列后的行列式，使用的是原矩阵第1、3、4列。

该三阶行列式非零，说明这三列限制到第2、3、4行后已线性无关，因而原四维列向量 $\alpha_1,\alpha_3,\alpha_4$ 也线性无关。

**第三步：利用解空间维数写出完整通解。**

三个独立解恰好达到 $\ker A^*$ 的维数，故构成基础解系，通解为

$$
\boxed{\boldsymbol X=k_1\alpha_1+k_2\alpha_3+k_3\alpha_4,\qquad k_1,k_2,k_3\in\mathbb R.}
$$

**8.（2020.8）答案：D。**

相似对角化矩阵的各列，必须按照目标对角元 $1,-1,1$ 的顺序，分别选取对应特征向量。

属于特征值 $1$ 的向量可以取 $\alpha_1+\alpha_2$ 与 $\alpha_2$；属于特征值 $-1$ 的向量可以取 $-\alpha_3$。

所以令

$$
P=(\alpha_1+\alpha_2,-\alpha_3,\alpha_2).
$$

它的三列线性无关：若

$$
c_1(\alpha_1+\alpha_2)-c_2\alpha_3+c_3\alpha_2=0,
$$

则由 $\alpha_1,\alpha_2,\alpha_3$ 线性无关，得 $c_1=0$、$c_1+c_3=0$、$c_2=0$，从而全部系数为零。

因此 $P$ 可逆，而且

$$
AP=P\operatorname{diag}(1,-1,1),
$$

满足要求，选 D。

其中三列的对应关系可直接核验：

$$
A(\alpha_1+\alpha_2)=\alpha_1+\alpha_2,
\qquad A(-\alpha_3)=\alpha_3=-(-\alpha_3),
\qquad A\alpha_2=\alpha_2.
$$

而 $\alpha_1+\alpha_3$ 混合了不同特征值的方向，通常不再是特征向量；本题由它们线性无关可知它不可能属于所需的特征值1，故 A、C 中的第一列不合要求。B 的列顺序对应 $1,1,-1$，也与目标不同。

## 二、填空题（9～14）

**9.（2020.9）答案：$-\sqrt2$。**

先求参数导数。因为 $t+\sqrt{1+t^2}>0$，对数在所有实数 $t$ 上有定义，且

$$
y'(t)=\frac{1+t/\sqrt{1+t^2}}{t+\sqrt{1+t^2}}
=\frac1{\sqrt{1+t^2}}.
$$

所以

$$
\frac{\mathrm dx}{\mathrm dt}=\frac t{\sqrt{1+t^2}},
\qquad
\frac{\mathrm dy}{\mathrm dt}=\frac1{\sqrt{1+t^2}}.
$$

所以在 $t\ne0$ 时，

$$
\frac{\mathrm dy}{\mathrm dx}=\frac1t.
$$

再次求 $x$ 导数：

$$
\frac{\mathrm d^2y}{\mathrm dx^2}
=\frac{-1/t^2}{t/\sqrt{1+t^2}}
=-\frac{\sqrt{1+t^2}}{t^3}.
$$

代入 $t=1$，即为 $-\sqrt2$。

二阶导数的计算使用的是 $\mathrm d/\mathrm dx=(1/x'(t))\mathrm d/\mathrm dt$。$t=1$ 时 $x'(1)=1/\sqrt2\ne0$，公式适用；仅把 $\mathrm d(1/t)/\mathrm dt=-1/t^2$ 代入，会漏除一次 $x'(t)$。

**10.（2020.10）答案：$\dfrac29(2\sqrt2-1)$。**

原区域满足 $0\le y\le1$、$\sqrt y\le x\le1$。等价地，

$$
0\le x\le1,\qquad0\le y\le x^2.
$$

换序后先对 $y$ 积分：

$$
\begin{aligned}
I&=\int_0^1\left(\int_0^{x^2}\sqrt{1+x^3}\,\mathrm dy\right)\mathrm dx\\
&=\int_0^1x^2\sqrt{1+x^3}\,\mathrm dx.
\end{aligned}
$$

令 $u=1+x^3$，得到

$$
I=\frac13\int_1^2u^{1/2}\,\mathrm du
=\frac29(2\sqrt2-1).
$$

区域换序的关键是 $x\ge\sqrt y\ge0$，在非负范围内平方后等价于 $y\le x^2$。换序后被积函数与 $y$ 无关，内层积分贡献的正是竖直截线长度 $x^2$，它又与换元 $u=1+x^3$ 的微分 $\mathrm du=3x^2\mathrm dx$ 匹配。

**11.（2020.11）答案：$(\pi-1)\mathrm dx-\mathrm dy$。**

记 $u=xy+\sin(x+y)$，则

$$
\mathrm dz=\frac{\mathrm du}{1+u^2},
$$

$$
\mathrm du=[y+\cos(x+y)]\mathrm dx+[x+\cos(x+y)]\mathrm dy.
$$

在 $(x,y)=(0,\pi)$ 处，$u=0$、$\cos(x+y)=-1$，所以

$$
\mathrm dz=(\pi-1)\mathrm dx-\mathrm dy.
$$

等价地，两个偏导数为

$$
z_x=\frac{y+\cos(x+y)}{1+[xy+\sin(x+y)]^2},
\qquad
z_y=\frac{x+\cos(x+y)}{1+[xy+\sin(x+y)]^2}.
$$

它们在该点分别为 $\pi-1$ 和 $-1$，代入全微分 $\mathrm dz=z_x\mathrm dx+z_y\mathrm dy$ 即得答案。这里 $\mathrm dx,\mathrm dy$ 是两个独立的自变量增量。

**12.（2020.12）答案：$\dfrac13\rho ga^3$。**

等腰直角三角形的斜边长为 $2a$，从直角顶点到斜边的高为 $a$。

以水面为深度零点，记向下的深度为 $h$，则 $0\le h\le a$。由三角形相似，深度 $h$ 处的水平截线长为

$$
w(h)=2a\left(1-\frac ha\right)=2(a-h).
$$

压强为 $\rho gh$。厚度为 $\mathrm dh$ 的细条所受压力为

$$
\mathrm dF=\rho gh\cdot2(a-h)\,\mathrm dh.
$$

因此

$$
F=2\rho g\int_0^a h(a-h)\,\mathrm dh
=2\rho g\left(\frac{a^3}{2}-\frac{a^3}{3}\right)
=\frac13\rho ga^3.
$$

几何尺寸可由面积核验：两条直角边均为 $\sqrt2a$，面积为 $a^2$，以斜边 $2a$ 为底时高为 $2a^2/(2a)=a$。三角形重心距水面 $a/3$，所以总压力也等于 $\rho g\times a^2\times(a/3)$，与积分结果一致。

**13.（2020.13）答案：$1$。**

特征方程 $(r+1)^2=0$ 给出通解

$$
y=(C_1+C_2x)\mathrm e^{-x}.
$$

由 $y(0)=0$ 得 $C_1=0$；再由 $y'(0)=1$ 得 $C_2=1$，故 $y=x\mathrm e^{-x}$。

于是

$$
\int_0^{+\infty}y(x)\,\mathrm dx
=\left[-(x+1)\mathrm e^{-x}\right]_0^{+\infty}=1.
$$

求初值常数时，完整的一阶导数为

$$
y'=(C_2-C_1-C_2x)\mathrm e^{-x}.
$$

所以 $y'(0)=C_2-C_1=1$。反常积分按 $\lim_{R\to+\infty}\int_0^R y(x)\,\mathrm dx$ 计算；因为 $(R+1)\mathrm e^{-R}\to0$，得到有限值1，同时证明收敛。

**14.（2020.14）答案：$a^2(a^2-4)$。**

**第一步：利用两个相同的对角块构造变换。**

把原矩阵写成

$$
M=\begin{pmatrix}aI_2&C\\C&aI_2\end{pmatrix},
\qquad C=\begin{pmatrix}-1&1\\1&-1\end{pmatrix}.
$$

取

$$
T=\begin{pmatrix}I_2&I_2\\I_2&-I_2\end{pmatrix},
\qquad T^{-1}=\frac12T,
$$

因为 $T^2=2I_4$，此变换对任意参数 $a$ 都可逆。直接相乘可得

$$
T^{-1}MT=
\begin{pmatrix}aI_2+C&0\\0&aI_2-C\end{pmatrix}.
$$

**第二步：计算两个二阶块的行列式。**

相似变换不改变行列式，故

$$
\begin{aligned}
\det M
&=\begin{vmatrix}a-1&1\\1&a-1\end{vmatrix}
  \begin{vmatrix}a+1&-1\\-1&a+1\end{vmatrix}\\
&=[(a-1)^2-1][(a+1)^2-1]\\
&=a(a-2)\,a(a+2)=\boxed{a^2(a^2-4)}.
\end{aligned}
$$

计算中没有除以 $a$ 或 $a\pm2$，因此 $a=0,\pm2$ 时也适用，此时行列式均为零。

## 三、解答题（15～23）

**15.（2020.15）答案：$y=\dfrac{x}{\mathrm e}+\dfrac1{2\mathrm e}$。**

**第一步：改写函数，求渐近线斜率。**

斜渐近线 $y=kx+b$ 要求 $y-(kx+b)\to0$。对题设函数，

$$
y=x\left(1+\frac1x\right)^{-x},
\qquad k=\lim_{x\to+\infty}\frac yx
=\lim_{x\to+\infty}\left(1+\frac1x\right)^{-x}=\mathrm e^{-1}.
$$

**第二步：保留下一阶项，求截距。**

由 $\ln(1+u)=u-u^2/2+O(u^3)$，令 $u=1/x$，再取指数，依次得到

$$
\begin{aligned}
-x\ln\left(1+\frac1x\right)
&=-1+\frac1{2x}+O(x^{-2}),\\
\left(1+\frac1x\right)^{-x}
&=\mathrm e^{-1}\left[1+\frac1{2x}+O(x^{-2})\right],\\
y&=\frac{x}{\mathrm e}+\frac1{2\mathrm e}+O(x^{-1}).
\end{aligned}
$$

第二行使用了 $\mathrm e^v=1+v+O(v^2)$，其中 $v=1/(2x)+O(x^{-2})$；第三行乘以 $x$ 后，余项由 $O(x^{-2})$ 变成 $O(x^{-1})$。因此

$$
b=\lim_{x\to+\infty}\left(y-\frac{x}{\mathrm e}\right)=\frac1{2\mathrm e},
\qquad \boxed{y=\frac{x}{\mathrm e}+\frac1{2\mathrm e}}.
$$

**第三步：检查函数与整条直线之差。**

由展开式直接有

$$
y-\left(\frac{x}{\mathrm e}+\frac1{2\mathrm e}\right)
=O(x^{-1})\longrightarrow0.
$$

所以该直线确为斜渐近线。斜率来自零阶指数极限，截距来自指数中的 $1/(2x)$ 项；只使用 $(1+1/x)^x\to\mathrm e$ 尚不能确定截距。

**16.（2020.16）导数及连续性如下。**

**第一步：换元后求非零点处的导数。**

由 $f$ 连续且 $f(x)/x\to1$，有 $f(0)=0$、$f(x)=x+o(x)$，并且 $g(0)=0$。当 $x\ne0$ 时令 $u=xt$，得

$$
g(x)=\frac1x\int_0^x f(u)\,\mathrm du,
\qquad
g'(x)=\frac{x f(x)-\int_0^x f(u)\,\mathrm du}{x^2}.
$$

对负的 $x$，换元后积分方向由上、下限自动处理，公式仍成立。求导只用了 $f$ 连续及变上限积分定理，无须在积分号下求 $f'$。

**第二步：严格估计积分余项，求 $g'(0)$。**

对任意 $\varepsilon>0$，可取 $\delta>0$，使 $0<|u|<\delta$ 时 $|f(u)-u|\le\varepsilon|u|$。因此 $0<|x|<\delta$ 时，无论 $x$ 正负都有

$$
\left|\int_0^x[f(u)-u]\,\mathrm du\right|
\le\varepsilon\int_{\min(0,x)}^{\max(0,x)}|u|\,\mathrm du
=\frac{\varepsilon x^2}{2}.
$$

故 $\int_0^x f(u)\,\mathrm du=x^2/2+o(x^2)$，于是

$$
g'(0)=\lim_{x\to0}\frac{g(x)-g(0)}x
=\lim_{x\to0}\frac1{x^2}\int_0^x f(u)\,\mathrm du=\frac12.
$$

**第三步：汇总导数，并验证它在0处连续。**

$$
\boxed{g'(x)=\begin{cases}
\displaystyle\frac{f(x)}x-\frac1{x^2}\int_0^x f(u)\,\mathrm du,&x\ne0,\\[6pt]
\dfrac12,&x=0.
\end{cases}}
$$

由题设及第二步的估计，$x\to0$ 时上式两项分别趋于1与 $1/2$，所以 $\lim_{x\to0}g'(x)=1-1/2=1/2=g'(0)$，连续性得证。

**17.（2020.17）答案：极小值为 $-\dfrac1{216}$，无极大值。**

**第一步：求全部驻点。**

$$
f_x=3x^2-y,\qquad f_y=24y^2-x.
$$

由 $f_x=f_y=0$，得 $y=3x^2$。代入另一式：

$$
x=24(3x^2)^2=216x^4,
\qquad x(216x^3-1)=0.
$$

因此驻点为

$$
(0,0),\qquad\left(\frac16,\frac1{12}\right).
$$

**第二步：用二阶偏导分类。**

$$
f_{xx}=6x,\qquad f_{xy}=-1,\qquad f_{yy}=48y.
$$

在 $(0,0)$ 处，Hessian 行列式为 $-1<0$，所以是鞍点。

在 $(1/6,1/12)$ 处，

$$
f_{xx}=1>0,\qquad f_{yy}=4,\qquad
f_{xx}f_{yy}-f_{xy}^2=3>0.
$$

故此点是极小值点，其值为

$$
\frac1{216}+8\cdot\frac1{1728}-\frac1{72}
=-\frac1{216}.
$$

所有驻点均已分类，故没有极大值。这是局部极小值；例如沿 $y=0$、$x\to-\infty$ 时函数趋于负无穷，因而它不是全局最小值。

本题为整个平面上的多项式函数，二阶偏导连续，局部极值点必须满足两个一阶偏导同时为零。原点的鞍点性质还可从两条路径看出：$f(x,x)=9x^3-x^2<0$，$f(x,-x)=x^2-7x^3>0$，对充分小的非零 $x$ 成立，所以原点附近同时有大于和小于 $f(0,0)=0$ 的值。

**18.（2020.18）答案：$f(x)=\dfrac{x}{\sqrt{1+x^2}}$，$V=\dfrac{\pi^2}{6}$。**

**第一步：把 $x$ 与 $1/x$ 的两个关系联立。**

原式为

$$
2f(x)+x^2f(1/x)=\frac{x^2+2x}{\sqrt{1+x^2}}.
$$

将自变量换成 $1/x$，由于 $x>0$，有 $\sqrt{1+1/x^2}=\sqrt{1+x^2}/x$，所以

$$
2f(1/x)+\frac1{x^2}f(x)=\frac{1+2x}{x\sqrt{1+x^2}}.
$$

两边乘以 $x^2$，得到

$$
f(x)+2x^2f(1/x)=\frac{x+2x^2}{\sqrt{1+x^2}}.
$$

用第一式的2倍减去第二式，消去 $f(1/x)$：

$$
3f(x)=\frac{3x}{\sqrt{1+x^2}}.
$$

所以 $f(x)=x/\sqrt{1+x^2}$。

**第二步：以 $y$ 为变量描述区域。**

解出反函数，得到

$$
x=\frac{y}{\sqrt{1-y^2}},\qquad0<y<1.
$$

因为 $f'(x)=(1+x^2)^{-3/2}>0$，曲线从0严格增至极限1，上述反函数在 $0<y<1$ 内唯一。题设区域的横向截线为

$$
\frac12\le y\le\frac{\sqrt3}{2},\qquad
0\le x\le\frac{y}{\sqrt{1-y^2}}.
$$

**第三步：使用圆柱壳法计算体积。**

高度为 $\mathrm dy$ 的水平细条绕 $x$ 轴旋转，壳半径为 $y$，壳长为 $x(y)$，所以

$$
V=2\pi\int_{1/2}^{\sqrt3/2}\frac{y^2}{\sqrt{1-y^2}}\,\mathrm dy.
$$

令 $y=\sin\theta$，相应 $\pi/6\le\theta\le\pi/3$，则

$$
\begin{aligned}
V&=2\pi\int_{\pi/6}^{\pi/3}\sin^2\theta\,\mathrm d\theta\\
&=\pi\left[\theta-\frac12\sin2\theta\right]_{\pi/6}^{\pi/3}.
\end{aligned}
$$

两个端点的 $\sin2\theta$ 都为 $\sqrt3/2$，恰好抵消，故

$$
V=\pi\left(\frac\pi3-\frac\pi6\right)=\frac{\pi^2}{6}.
$$

**用圆环截面法计算同一体积。**

曲线与两条水平线的交点横坐标分别为 $1/\sqrt3$、$\sqrt3$。当 $0\le x\le1/\sqrt3$ 时，内、外半径分别为 $1/2,\sqrt3/2$；当 $1/\sqrt3\le x\le\sqrt3$ 时，内半径变为 $f(x)$，外半径仍为 $\sqrt3/2$。故

$$
\begin{aligned}
V&=\pi\int_0^{1/\sqrt3}\left(\frac34-\frac14\right)\mathrm dx
 +\pi\int_{1/\sqrt3}^{\sqrt3}\left(\frac34-\frac{x^2}{1+x^2}\right)\mathrm dx\\
&=\frac{\pi}{2\sqrt3}
 +\pi\left[-\frac x4+\arctan x\right]_{1/\sqrt3}^{\sqrt3}\\
&=\frac{\pi}{2\sqrt3}-\frac{\pi}{2\sqrt3}
 +\pi\left(\frac\pi3-\frac\pi6\right)=\frac{\pi^2}{6}.
\end{aligned}
$$

竖直截面的下边界在 $x=1/\sqrt3$ 处改变，因此圆环法需要分段；水平圆柱壳法的范围固定，计算更直接。

**19.（2020.19）答案：$\dfrac34[\sqrt2+\ln(1+\sqrt2)]$。**

**第一步：用直角坐标写出区域，并消去内层积分上限中的 $x$。**

四条边围成的区域为 $1\le x\le2,\ 0\le y\le x$。对内层积分令 $u=y/x$，此时 $x$ 为固定正数，故 $\mathrm dy=x\,\mathrm du$，$\sqrt{x^2+y^2}/x=\sqrt{1+u^2}$，新上下限为0和1。因此

$$
\begin{aligned}
I&=\int_1^2\mathrm dx\int_0^x\frac{\sqrt{x^2+y^2}}x\,\mathrm dy\\
&=\int_1^2x\,\mathrm dx\int_0^1\sqrt{1+u^2}\,\mathrm du
=\frac32J,
\qquad J=\int_0^1\sqrt{1+u^2}\,\mathrm du.
\end{aligned}
$$

**第二步：用分部积分计算 $J$。**

将 $\sqrt{1+u^2}$ 作为被求导部分，得到

$$
\begin{aligned}
J&=\left[u\sqrt{1+u^2}\right]_0^1
 -\int_0^1\frac{u^2}{\sqrt{1+u^2}}\,\mathrm du\\
&=\sqrt2-\int_0^1\left[\sqrt{1+u^2}-\frac1{\sqrt{1+u^2}}\right]\mathrm du\\
&=\sqrt2-J+\int_0^1\frac{\mathrm du}{\sqrt{1+u^2}}.
\end{aligned}
$$

因为 $[\ln(u+\sqrt{1+u^2})]'=1/\sqrt{1+u^2}$，最后一个积分为 $\ln(1+\sqrt2)$。因此

$$
J=\frac12[\sqrt2+\ln(1+\sqrt2)],
\qquad
\boxed{I=\frac34[\sqrt2+\ln(1+\sqrt2)]}.
$$

**20.（2020.20）两个中值结论的证明。**

由积分定义，$f(1)=0$，且 $f'(x)=\mathrm e^{x^2}$。

**（Ⅰ）构造两端同值的函数，使用罗尔定理。**

目标等式可写成 $-f(\xi)+(2-\xi)f'(\xi)=0$，左端恰是乘积 $(2-x)f(x)$ 的导数。因此令

$$
F(x)=(2-x)f(x),\qquad 1\le x\le2.
$$

$F$ 在闭区间连续、开区间可导，并且 $F(1)=f(1)=0$、$F(2)=0$，满足罗尔定理的全部条件。因此存在 $\xi\in(1,2)$，使 $F'(\xi)=0$。由乘积求导，

$$
F'(x)=-f(x)+(2-x)\mathrm e^{x^2}.
$$

代入 $x=\xi$，得到

$$
\boxed{f(\xi)=(2-\xi)\mathrm e^{\xi^2}}.
$$

**（Ⅱ）对 $f(x)$ 与 $\ln x$ 使用柯西中值定理。**

目标中既有 $\ln2$，又有 $\eta f'(\eta)$，提示比较 $f$ 与导数为 $1/x$ 的函数 $\ln x$。二者在 $[1,2]$ 上连续，在 $(1,2)$ 内可导，且 $(\ln x)'=1/x\ne0$。

因此存在 $\eta\in(1,2)$，使

$$
\frac{f(2)-f(1)}{\ln2-\ln1}
=\frac{f'(\eta)}{1/\eta}
=\eta\mathrm e^{\eta^2}.
$$

利用 $f(1)=0$、$\ln1=0$，整理即得第二问所需的等式：

$$
\boxed{f(2)=\ln2\cdot\eta\mathrm e^{\eta^2}}.
$$

**21.（2020.21）答案：$y=Cx^3$，$C>0$。**

**第一步：把两块面积写成 $f$ 与 $f'$ 的表达式。**

设 $M=(x,f(x))$，$x>0$，记 $I(x)=\int_0^x f(t)\,\mathrm dt$。曲线经过原点且在正半轴严格增加，所以 $f(x)>0$、$I(x)>0$。

在 $M$ 处的切线及它与 $x$ 轴交点的横坐标分别为

$$
Y-f(x)=f'(x)(X-x),
\qquad X_T=x-\frac{f(x)}{f'(x)}.
$$

由于 $f,f'$ 均正，$T$ 在 $P$ 左侧，$TP=f(x)/f'(x)$、$MP=f(x)$。三角形面积及题设比例给出

$$
S_{\triangle}=\frac{f(x)^2}{2f'(x)},
\qquad I:S_{\triangle}=3:2
\quad\Longrightarrow\quad 4I(x)f'(x)=3f(x)^2.
$$

**第二步：用积分函数 $I$ 降低求解难度。**

由变上限积分定理 $I'=f$。把上式除以 $4If>0$，得

$$
\frac{f'}f=\frac34\frac{I'}I.
$$

这是 $\ln f$ 与 $\tfrac34\ln I$ 的导数相等，故它们之差为常数：

$$
\ln f=\frac34\ln I+C_0,
\qquad f=K I^{3/4},\quad K=\mathrm e^{C_0}>0.
$$

这一改写直接使用 $f'$ 与 $I'$，没有对题设微分关系再次求导，也没有预先要求 $f''$ 存在。

**第三步：利用过原点的条件确定积分常数。**

将 $I'=f$ 代入第二步结果，在 $x>0$ 上分离变量：

$$
I'=K I^{3/4}
\quad\Longrightarrow\quad
\left(I^{1/4}\right)'=\frac K4
\quad\Longrightarrow\quad I^{1/4}=\frac K4x+C_1.
$$

因为 $f$ 在0处连续，所以 $I(x)\to I(0)=0$。令 $x\to0^+$，得 $C_1=0$，从而
$$
I(x)=\frac{K^4x^4}{256},
\qquad f(x)=I'(x)=\frac{K^4}{64}x^3.
$$

把正数 $K^4/64$ 记为 $C$，得到 $\boxed{y=Cx^3,\ C>0}$。

**第四步：代回切线和面积比检验。**

对 $f=Cx^3$，在 $x>0$ 上 $f'=3Cx^2>0$，切线交点为 $T=(2x/3,0)$，故 $TP=x/3$。于是

$$
I(x)=\frac{Cx^4}{4},
\qquad S_{\triangle}=\frac12\cdot\frac x3\cdot Cx^3=\frac{Cx^4}{6},
\qquad \frac{I(x)}{S_{\triangle}}=\frac32.
$$

曲线满足面积比，且 $f(0)=0$。面积比与正导数条件用于 $x>0$ 的点，原点确定曲线的端点。

**22.（2020.22）答案：$a=-\dfrac12$，一组 $P$ 如下。**

**（Ⅰ）利用惯性指数确定参数。**

原二次型矩阵为

$$
A(a)=\begin{pmatrix}1&a&a\\a&1&a\\a&a&1\end{pmatrix}
=(1-a)I+aJ,
$$

其中 $J$ 为全1矩阵。令 $\boldsymbol e=(1,1,1)^{\mathrm T}$，则 $J\boldsymbol e=3\boldsymbol e$；若 $\boldsymbol v\perp\boldsymbol e$，则三个分量之和为零，故 $J\boldsymbol v=0$。因此 $A$ 的特征值为

$$
1+2a\quad\text{（一次）},\qquad 1-a\quad\text{（两次）}.
$$

目标二次型为

$$
g=(y_1+y_2)^2+4y_3^2,
$$

其秩为2，正惯性指数为2，负惯性指数为0。

由可逆线性变换保持惯性指数，$A(a)$ 必须恰有一个零特征值和两个正特征值。

当 $a=1$ 时，两个 $1-a$ 都为零，秩只有1，不合条件。因此只能

$$
1+2a=0,\qquad a=-\frac12.
$$

此时另两个特征值均为 $3/2>0$，条件满足。

**（Ⅱ）先取正交特征向量，再作尺度变换。**

取

$$
u_1=\frac1{\sqrt2}(1,-1,0)^{\mathrm T},\quad
u_2=\frac1{\sqrt6}(1,1,-2)^{\mathrm T},\quad
u_0=\frac1{\sqrt3}(1,1,1)^{\mathrm T}.
$$

令 $Q=(u_1,u_2,u_0)$，则
$$
Q^{\mathrm T}AQ=\operatorname{diag}\left(\frac32,\frac32,0\right).
$$

在 $\boldsymbol x=Q\boldsymbol z$ 后，再令

$$
z_1=\sqrt{\frac23}(y_1+y_2),\qquad
z_2=\sqrt{\frac83}\,y_3,\qquad z_3=y_2.
$$

第二步的变换矩阵为

$$
R=\begin{pmatrix}\sqrt{2/3}&\sqrt{2/3}&0\\0&0&\sqrt{8/3}\\0&1&0\end{pmatrix},
\qquad \det R=-\frac43\ne0.
$$

所以 $\boldsymbol z=R\boldsymbol y$ 可逆，并且

$$
\frac32z_1^2+\frac32z_2^2=(y_1+y_2)^2+4y_3^2=g.
$$

$u_1,u_2,u_0$ 两两正交且均为单位向量，故 $Q$ 可逆；它们所对应的特征值依次为 $3/2,3/2,0$。两步合并为 $\boldsymbol x=QR\boldsymbol y$，可取 $P=QR$，即

$$
P=\begin{pmatrix}
1/\sqrt3&2/\sqrt3&2/3\\
-1/\sqrt3&0&2/3\\
0&1/\sqrt3&-4/3
\end{pmatrix}.
$$

有 $\det P=-4/3\ne0$，并且直接相乘得到

$$
P^{\mathrm T}AP=\begin{pmatrix}1&1&0\\1&1&0\\0&0&4\end{pmatrix},
$$

正是目标二次型矩阵，构造完成。

**23.（2020.23）答案：$P^{-1}AP=\begin{pmatrix}0&6\\1&-1\end{pmatrix}$，$A$ 可对角化。**

**（Ⅰ）证明 $P$ 可逆。**

$P$ 的两列为 $\alpha,A\alpha$。若它们线性相关，由 $\alpha\ne0$ 可得

$$
A\alpha=\lambda\alpha
$$

对某个数 $\lambda$ 成立，这会说明 $\alpha$ 是 $A$ 的特征向量，与题设矛盾。

所以两列线性无关，二阶矩阵 $P$ 可逆。

**（Ⅱ）求新基下的矩阵。**

由已知关系，

$$
A^2\alpha=6\alpha-A\alpha.
$$

因此

$$
\begin{aligned}
AP&=(A\alpha,A^2\alpha)\\
&=(\alpha,A\alpha)\begin{pmatrix}0&6\\1&-1\end{pmatrix}
=P\begin{pmatrix}0&6\\1&-1\end{pmatrix}.
\end{aligned}
$$

左乘 $P^{-1}$，即得所求矩阵。

**判断能否对角化。** 该矩阵的特征方程为

$$
\lambda^2+\lambda-6=(\lambda-2)(\lambda+3)=0.
$$

两个特征值 $2,-3$ 互不相同，所以它可对角化，进而与它相似的 $A$ 也可对角化。

**构造原矩阵的对角化变换并检验。**

令 $v_1=3\alpha+A\alpha$、$v_2=-2\alpha+A\alpha$，利用 $A^2\alpha=6\alpha-A\alpha$，分别计算：

$$
\begin{aligned}
Av_1&=3A\alpha+A^2\alpha
=6\alpha+2A\alpha=2v_1,\\
Av_2&=-2A\alpha+A^2\alpha
=6\alpha-3A\alpha=-3v_2.
\end{aligned}
$$

两个向量确实分别对应特征值 $2,-3$。再令

$$
R=\begin{pmatrix}3&-2\\1&1\end{pmatrix},
\qquad S=PR=(v_1,v_2).
$$

因为 $\det R=5\ne0$，且第一问已证 $P$ 可逆，所以 $S$ 可逆。由逐列关系 $AS=S\operatorname{diag}(2,-3)$，有

$$
\boxed{S^{-1}AS=\operatorname{diag}(2,-3)}.
$$

这样同时验证了所取特征向量非零、线性无关以及对角元的顺序。
