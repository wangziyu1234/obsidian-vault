# 2005年考研数学二答案与解析

> 我的解答，未与官方核对。

## 答案速查

> 填空题：1.$-\pi\,\mathrm dx$；2.$y=x+3/2$；3.$\pi/4$；4.$y=x\ln x/3-x/9$；5.$3/4$；6.$2$。

> 选择题：7.C　8.A　9.A　10.D　11.B　12.D　13.B　14.C。

15. 极限为 $1/2$。

16. 图示分支为 $x=\varphi(y)=\ln y-1/2+1/(2y)$。

17. 积分为20。

18. $y=2x+\sqrt{1-x^2}$，$-1<x<1$。

19. 两个中值结论的证明见正文。

20. 最大值3，最小值 $-2$。

21. 积分为 $\pi/4-1/3$。

22. $a=1$。

23. 按 $k\ne9$、$k=9$ 及 $r(A)$ 分类，全部通解见正文。

## 一、填空题（1～6）

**1.（2005.1）答案：$-\pi\,\mathrm dx$。**

在 $x=\pi$ 的邻域内，底数为正，可以取对数：

$$
\ln y=x\ln(1+\sin x).
$$

两边求导：

$$
\frac{y'}y=\ln(1+\sin x)+\frac{x\cos x}{1+\sin x}.
$$

在 $x=\pi$ 时，$y=1$、$\sin\pi=0$、$\cos\pi=-1$，因此 $y'(\pi)=-\pi$。题目要求微分，故答案为 $-\pi\,\mathrm dx$。

**2.（2005.2）答案：$y=x+3/2$。**

曲线定义域为 $x>0$。当 $x\to+\infty$ 时，

$$
y=x\left(1+\frac1x\right)^{3/2}
=x\left(1+\frac{3}{2x}+O(x^{-2})\right)
=x+\frac32+O(x^{-1}).
$$

因此斜渐近线的斜率为1、截距为 $3/2$。

**3.（2005.3）答案：$\pi/4$。**

令 $u=\sqrt{1-x^2}$，则 $\mathrm du=-x\mathrm dx/\sqrt{1-x^2}$，且 $2-x^2=1+u^2$。

上下限由 $x=0,1$ 变为 $u=1,0$，所以

$$
I=\int_0^1\frac{\mathrm du}{1+u^2}
=[\arctan u]_0^1=\frac\pi4.
$$

**4.（2005.4）答案：$y=x\ln x/3-x/9$。**

同乘 $x$，原方程化为

$$
(x^2y)'=x^2\ln x.
$$

分部积分得

$$
x^2y=\frac{x^3}{3}\ln x-\frac{x^3}{9}+C.
$$

由 $y(1)=-1/9$，得到 $C=0$，所以所求解为 $y=x\ln x/3-x/9$，定义域为 $x>0$。

**5.（2005.5）答案：$k=3/4$。**

由 $x\arcsin x=x^2+O(x^4)$，有

$$
\sqrt{1+x\arcsin x}=1+\frac{x^2}{2}+O(x^4).
$$

又因为 $\cos x=1-x^2/2+O(x^4)$，所以

$$
\sqrt{\cos x}=1-\frac{x^2}{4}+O(x^4).
$$

相减得到 $\beta(x)=3x^2/4+O(x^4)$，要与 $kx^2$ 等价，必须 $k=3/4$。

**6.（2005.6）答案：$2$。**

将三个新列向量的系数按列排列，得到

$$
B=A\begin{pmatrix}1&1&1\\1&2&3\\1&4&9\end{pmatrix}.
$$

右侧系数矩阵为范德蒙德矩阵，行列式等于

$$
(2-1)(3-1)(3-2)=2.
$$

所以 $|B|=|A|\cdot2=2$。

## 二、选择题（7～14）

**7.（2005.7）答案：C，恰有两个不可导点。**

当 $|x|\le1$ 时，$1\le1+|x|^{3n}\le2$，取 $n$ 次方根再令 $n\to\infty$，极限为1。

当 $|x|>1$ 时，提出 $|x|^{3n}$，极限为 $|x|^3$。因此

$$
f(x)=\begin{cases}1,&|x|\le1,\\|x|^3,&|x|>1.\end{cases}
$$

在 $x=\pm1$ 处，外侧导数分别为 $-3,3$，内侧导数为0，所以这两点不可导。其余位置均可导，故选 C。

**8.（2005.8）答案：A。**

如果原函数 $F$ 为偶函数，则对 $F(-x)=F(x)$ 求导，得到 $-f(-x)=f(x)$，所以 $f$ 为奇函数。

反过来，若 $f$ 为奇函数，则

$$
F(x)-F(0)=\int_0^x f(t)\,\mathrm dt
$$

为偶函数，加常数不改变偶性，所以 $F$ 必为偶函数。

B 不成立，因为给原函数加一个非零常数就可能破坏奇性；C 不成立，例如 $f\equiv1$ 是周期函数而其原函数 $x$ 不是周期函数；D 不成立，例如 $F=x^3$ 单调，而 $f=3x^2$ 在实轴不单调。

**9.（2005.9）答案：A，$3+\ln2/8$。**

由 $x=t^2+2t=3$，并结合 $\ln(1+t)$ 的定义域 $t>-1$，取 $t=1$。对应点为 $(3,\ln2)$。

切线斜率为

$$
\frac{\mathrm dy}{\mathrm dx}=\frac{1/(1+t)}{2t+2},
$$

在 $t=1$ 处为 $1/8$，因此法线斜率为 $-8$。

法线方程为 $y-\ln2=-8(x-3)$。令 $y=0$，得到横坐标 $x=3+\ln2/8$。

**10.（2005.10）答案：D，$(a+b)\pi/2$。**

区域关于 $y=x$ 对称。将 $x,y$ 交换后，积分值保持不变，因此将原积分与交换后的积分相加，可得

$$
2I=\iint_D
\frac{(a+b)[\sqrt{f(x)}+\sqrt{f(y)}]}{\sqrt{f(x)}+\sqrt{f(y)}}\,\mathrm d\sigma
=(a+b)\operatorname{Area}(D).
$$

$D$ 是半径2的四分之一圆盘，面积为 $\pi$，所以 $I=(a+b)\pi/2$。

**11.（2005.11）答案：B，$u_{xx}=u_{yy}$。**

令 $\Psi$ 是 $\psi$ 的一个原函数，原式可写成

$$
u(x,y)=[\varphi+\Psi](x+y)+[\varphi-\Psi](x-y).
$$

对于任意二阶可导函数 $H$，$H(x+y)$ 的两个纯二阶偏导相同；$H(x-y)$ 对 $y$ 求两次偏导时，两次负号相消，结果也与对 $x$ 求两次相同。

因此两部分都满足该关系，相加后仍有 $u_{xx}=u_{yy}$。

**12.（2005.12）答案：D。**

在 $x=0$ 附近，$x/(x-1)\sim-x$，所以分母 $\mathrm e^{x/(x-1)}-1\sim-x$，函数无界，零点为第二类间断点。

在 $x=1$ 左侧，指数趋于负无穷，函数趋于 $-1$；右侧指数趋于正无穷，函数趋于0。两单侧极限有限而不同，因此1是第一类跳跃间断点。

**13.（2005.13）答案：B，$\lambda_2\ne0$。**

有

$$
A(\alpha_1+\alpha_2)=\lambda_1\alpha_1+\lambda_2\alpha_2.
$$

由于两个特征值不同，$\alpha_1,\alpha_2$ 线性无关。新向量与 $\alpha_1$ 线性无关，恰好要求它沿 $\alpha_2$ 的系数不为零，即 $\lambda_2\ne0$。

**14.（2005.14）答案：C。**

令 $P$ 为交换第1、2行的初等矩阵，则 $B=PA$，$P^{-1}=P$，且 $|B|=-|A|$。

所以

$$
B^*=|B|B^{-1}=-|A|A^{-1}P=-A^*P.
$$

右乘 $P$ 对应交换第1、2列，因此交换 $A^*$ 的这两列得到的是 $-B^*$，选 C。

## 三、解答题（15～23）

**15.（2005.15）答案：$1/2$。**

**第一步：用同一换元消去积分区间的变化。**

分母中先令 $u=x-t$，得到 $\int_0^x f(x-t)\,\mathrm dt=\int_0^x f(u)\,\mathrm du$。

再分别令 $t=xs$、$u=xs$，原式变为

$$
\frac{x^2\int_0^1(1-s)f(xs)\,\mathrm ds}{x^2\int_0^1f(xs)\,\mathrm ds}
=\frac{\int_0^1(1-s)f(xs)\,\mathrm ds}{\int_0^1f(xs)\,\mathrm ds}.
$$

这一等式对正、负的非零 $x$ 都成立。

**第二步：使用零点处连续性。**

当 $x\to0$ 时，$f(xs)$ 在 $s\in[0,1]$ 上一致趋于 $f(0)$，所以两个积分分别趋于 $f(0)/2$ 和 $f(0)$。

因为 $f(0)\ne0$，分母极限不为零，故所求极限为 $1/2$。

**16.（2005.16）答案：$x=\ln y-1/2+1/(2y)$。**

**第一步：根据图示写出两块面积。**

图示分支取 $x\ge0$、$y=\mathrm e^x\ge1$。两条已知曲线间的面积为

$$
S_1(x)=\int_0^x\left[\mathrm e^t-\frac{1+\mathrm e^t}{2}\right]\mathrm dt
=\frac{\mathrm e^x-1-x}{2}.
$$

按水平截线表示第二块区域，其右边界为 $x=\ln y$，左边界为 $x=\varphi(y)$，所以

$$
S_2(y)=\int_1^y[\ln u-\varphi(u)]\,\mathrm du.
$$

**第二步：把面积相等写成同一个变量的等式。**

由 $x=\ln y$，有

$$
\int_1^y[\ln u-\varphi(u)]\,\mathrm du
=\frac{y-1-\ln y}{2}.
$$

对 $y$ 求导，得到

$$
\ln y-\varphi(y)=\frac12\left(1-\frac1y\right).
$$

因此

$$
\varphi(y)=\ln y-\frac12+\frac1{2y}.
$$

在 $y\ge1$ 上，$\varphi(1)=0$、$\varphi'(y)=(2y-1)/(2y^2)>0$，并有 $\varphi(y)\le\ln y$，与图中曲线的位置及增加性一致。

**17.（2005.17）答案：$20$。**

**第一步：从切线和拐点得到边界数据。**

图中给出 $f(0)=0$、$f(3)=2$。两条切线都经过 $(2,4)$，所以

$$
f'(0)=\frac{4-0}{2-0}=2,
\qquad f'(3)=\frac{4-2}{2-3}=-2.
$$

$f$ 有连续三阶导数，且 $(3,2)$ 是拐点，因此 $f''(3)=0$。

**第二步：连续分部积分。**

$$
\begin{aligned}
I&=[(x^2+x)f''(x)]_0^3-\int_0^3(2x+1)f''(x)\,\mathrm dx\\
&=-[(2x+1)f'(x)]_0^3+2\int_0^3f'(x)\,\mathrm dx\\
&=-[7(-2)-1\cdot2]+2[f(3)-f(0)]\\
&=16+4=20.
\end{aligned}
$$

第一行的边界项为零：上端使用 $f''(3)=0$，下端的多项式因子本身为零。

**18.（2005.18）答案：$y=2x+\sqrt{1-x^2}$。**

**第一步：将未知函数改写为参数的函数。**

令 $Y(t)=y(\cos t)$，其中 $0<t<\pi$，故 $\sin t>0$。由链式法则，

$$
Y'=-\sin t\,y',
\qquad Y''=-\cos t\,y'+\sin^2t\,y''.
$$

所以原方程恰好化为

$$
Y''+Y=0.
$$

**第二步：解方程并代回。**

$$
Y=C_1\cos t+C_2\sin t,
$$

利用 $x=\cos t$、$\sin t=\sqrt{1-x^2}$，得到

$$
y=C_1x+C_2\sqrt{1-x^2}.
$$

初值 $y(0)=1$ 给出 $C_2=1$；由 $y'(0)=2$，得 $C_1=2$。

故所求特解为 $y=2x+\sqrt{1-x^2}$，适用于包含0的区间 $(-1,1)$。

**19.（2005.19）两个中值结论的证明。**

**（Ⅰ）构造连续函数的异号端点。**

令 $F(x)=f(x)+x-1$。它在 $[0,1]$ 连续，且 $F(0)=-1$、$F(1)=1$。

由零点定理，存在 $\xi\in(0,1)$，使 $F(\xi)=0$，即 $f(\xi)=1-\xi$。

**（Ⅱ）分别在两个子区间使用中值定理。**

在 $[0,\xi]$ 上，存在 $\eta\in(0,\xi)$，使

$$
f'(\eta)=\frac{f(\xi)-f(0)}\xi=\frac{1-\xi}{\xi}.
$$

在 $[\xi,1]$ 上，存在 $\zeta\in(\xi,1)$，使

$$
f'(\zeta)=\frac{f(1)-f(\xi)}{1-\xi}=\frac\xi{1-\xi}.
$$

两式相乘得 $f'(\eta)f'(\zeta)=1$。两个点位于不同的开子区间，故互不相同。

**20.（2005.20）答案：最大值3，最小值 $-2$。**

由全微分，恢复函数为 $f=x^2-y^2+C$。利用 $f(1,1)=2$，得 $C=2$。

在椭圆域内，$0\le x^2\le1$、$0\le y^2\le4$。因此

$$
-2\le x^2-y^2+2\le3.
$$

当 $(x,y)=(\pm1,0)$ 时达到上界3；当 $(x,y)=(0,\pm2)$ 时达到下界 $-2$。这些点都在椭圆域边界上，所以两界就是全局最大、最小值。

**21.（2005.21）答案：$\pi/4-1/3$。**

**第一步：把绝对值的负部分单独补回。**

在单位正方形内，令 $D_0$ 为第一象限的单位四分之一圆盘。则

$$
|x^2+y^2-1|=(x^2+y^2-1)+2(1-x^2-y^2)\boldsymbol 1_{D_0}.
$$

因此原积分等于正方形上多项式积分，加上圆盘区域内的补偿积分。

**第二步：分别计算。**

正方形上的积分为 $1/3+1/3-1=-1/3$。

补偿项用极坐标得到

$$
2\int_0^{\pi/2}\int_0^1(1-r^2)r\,\mathrm dr\,\mathrm d\theta
=\pi\left(\frac12-\frac14\right)=\frac\pi4.
$$

两部分相加，得到 $\pi/4-1/3$。

**22.（2005.22）答案：$a=1$。**

**第一步：用空间包含关系限制参数。**

记 $U=(\alpha_1,\alpha_2,\alpha_3)$、$V=(\beta_1,\beta_2,\beta_3)$。题意是 $U$ 的列空间严格包含于 $V$ 的列空间，所以 $r(U)<r(V)\le3$，特别是 $U$ 不可逆。

计算得

$$
\det U=-(a-1)^2(a+2).
$$

因此只需检查 $a=1$ 与 $a=-2$。

**第二步：检查 $a=1$。**

这时三个 $\alpha$ 均为 $(1,1,1)^{\mathrm T}$，所以 $r(U)=1$。而

$$
\det V=(a-4)(a+2)=-9\ne0,
$$

故 $r(V)=3$。因此 $U$ 的列空间严格包含于 $V$ 的列空间，全部条件满足。

**第三步：排除 $a=-2$。**

此时 $V$ 的每列都满足第一、第二分量相等，其列空间包含在平面 $x_1=x_2$ 内。

但 $\alpha_2=(1,-2,1)^{\mathrm T}$ 不在这个平面内，不能由 $V$ 的列线性表示，所以条件不满足。

故唯一答案为 $a=1$。

**23.（2005.23）按参数与矩阵秩分类的通解。**

记

$$
p=(1,2,3)^{\mathrm T},\qquad q=(3,6,k)^{\mathrm T},
\qquad B=(p,2p,q).
$$

由 $AB=0$，有 $Ap=Aq=0$。又因 $A$ 第一行非零，$r(A)\ge1$。

**情形一：$k\ne9$。**

此时 $q-3p=(0,0,k-9)^{\mathrm T}\ne0$，所以 $p,q$ 线性无关，$r(B)=2$。

$A$ 的零空间包含两个独立向量，因此零空间维数至少为2，即 $r(A)\le1$。结合 $r(A)\ge1$，得到 $r(A)=1$，零空间维数恰为2。

所以 $p,q$ 构成一个基础解系，通解为

$$
\boldsymbol x=s\begin{pmatrix}1\\2\\3\end{pmatrix}
+t\begin{pmatrix}3\\6\\k\end{pmatrix},\qquad s,t\in\mathbb R.
$$

**情形二：$k=9$，且 $r(A)=2$。**

这时 $q=3p$，而 $Ap=0$。零空间维数为 $3-2=1$，因此通解为

$$
\boldsymbol x=t\begin{pmatrix}1\\2\\3\end{pmatrix},\qquad t\in\mathbb R.
$$

**情形三：$k=9$，且 $r(A)=1$。**

所有行均为第一行 $(a,b,c)$ 的倍数，所以方程组等价于单个方程

$$
ax_1+bx_2+cx_3=0.
$$

已知 $Ap=0$，所以 $a+2b+3c=0$。再取

$$
w=(3b-2c,\ c-3a,\ 2a-b)^{\mathrm T}.
$$

直接计算可验 $(a,b,c)w=0$ 且 $p^{\mathrm T}w=0$。$w$ 不会为零：若为零，则 $(a,b,c)$ 与 $p$ 平行，又与 $p$ 正交，只能是零行，与题设矛盾。

因此 $p,w$ 是两个正交的非零解向量，构成二维零空间的一组基，通解为

$$
\boldsymbol x=s\begin{pmatrix}1\\2\\3\end{pmatrix}
+t\begin{pmatrix}3b-2c\\c-3a\\2a-b\end{pmatrix},\qquad s,t\in\mathbb R.
$$

因为 $p\ne0$ 是齐次解，$A$ 不可逆；结合第一行非零，$r(A)$ 只能为1或2。上述三种情形覆盖了全部可能。
