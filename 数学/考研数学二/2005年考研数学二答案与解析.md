# 2005年考研数学二答案与解析

> 我的解答，未与官方核对。

## 答案速查

> 填空题：1.$-\pi\,\mathrm dx$；2.$y=x+3/2$；3.$\pi/4$；4.$y=x\ln x/3-x/9$；5.$3/4$；6.$2$。

> 选择题：7.C　8.A　9.A　10.D　11.B　12.D　13.B　14.C。

15. 极限为 $1/2$。

16. 图示分支为 $x=\varphi(y)=\ln y-1/2+1/(2y)$。

17. 积分为20。

18. $y=2x+\sqrt{1-x^2}$，$-1<x<1$。

19. 两个中值结论的证明见正文。

20. 最大值3，最小值 $-2$。

21. 积分为 $\pi/4-1/3$。

22. $a=1$。

23. 按 $k\ne9$、$k=9$ 及 $r(A)$ 分类，全部通解见正文。

## 一、填空题（1～6）

**1.（2005.1）答案：$-\pi\,\mathrm dx$。**

在 $x=\pi$ 的邻域内，底数为正，可以取对数：

$$
\ln y=x\ln(1+\sin x).
$$

两边求导：

$$
\frac{y'}y=\ln(1+\sin x)+\frac{x\cos x}{1+\sin x}.
$$

在 $x=\pi$ 时，$y=1$、$\sin\pi=0$、$\cos\pi=-1$，因此 $y'(\pi)=-\pi$。题目要求微分，故答案为 $-\pi\,\mathrm dx$。

对数求导适用于“底数和指数都含 $x$”的形式。求导后还须乘回 $y$，完整表达式是

$$
y'=(1+\sin x)^x\left[\ln(1+\sin x)+\frac{x\cos x}{1+\sin x}\right].
$$

在指定点，括号内第一项为0、第二项为 $-\pi$。最后使用 $\mathrm dy=y'\mathrm dx$，保留微分符号 $\mathrm dx$。

**2.（2005.2）答案：$y=x+3/2$。**

曲线定义域为 $x>0$。当 $x\to+\infty$ 时，

$$
y=x\left(1+\frac1x\right)^{3/2}
=x\left(1+\frac{3}{2x}+O(x^{-2})\right)
=x+\frac32+O(x^{-1}).
$$

因此斜渐近线的斜率为1、截距为 $3/2$。

也可以直接按渐近线的定义求斜率和截距。若 $y=kx+b$ 为斜渐近线，则

$$
k=\lim_{x\to+\infty}\frac{y}{x}
=\lim_{x\to+\infty}\left(1+\frac1x\right)^{3/2}=1,
$$

$$
b=\lim_{x\to+\infty}(y-x)
=\lim_{h\to0^+}\frac{(1+h)^{3/2}-1}{h}=\frac32.
$$

最后一个极限就是函数 $(1+h)^{3/2}$ 在0处的导数。因为实数定义域只有 $x>0$，无须考察负无穷方向。

**3.（2005.3）答案：$\pi/4$。**

令 $u=\sqrt{1-x^2}$，则 $\mathrm du=-x\mathrm dx/\sqrt{1-x^2}$，且 $2-x^2=1+u^2$。

上下限由 $x=0,1$ 变为 $u=1,0$，所以

$$
I=\int_0^1\frac{\mathrm du}{1+u^2}
=[\arctan u]_0^1=\frac\pi4.
$$

原积分在 $x=1$ 处含根式分母，应先在 $[0,b]$（$b<1$）上换元，再令 $b\to1^-$。此时严格的换元式为

$$
\int_0^b\frac{x\,\mathrm dx}{(2-x^2)\sqrt{1-x^2}}
=-\int_1^{\sqrt{1-b^2}}\frac{\mathrm du}{1+u^2}.
$$

右端上限趋于0，且新被积函数连续，所以得到上述有限结果，反常积分收敛。

**4.（2005.4）答案：$y=x\ln x/3-x/9$。**

同乘 $x$，原方程化为

$$
(x^2y)'=x^2\ln x.
$$

分部积分得

$$
x^2y=\frac{x^3}{3}\ln x-\frac{x^3}{9}+C.
$$

由 $y(1)=-1/9$，得到 $C=0$，所以所求解为 $y=x\ln x/3-x/9$，定义域为 $x>0$。

识别乘积导数的过程也可从标准线性方程出发：

$$
y'+\frac2x y=\ln x,\qquad x>0.
$$

积分因子是 $\mathrm e^{\int 2/x\,\mathrm dx}=x^2$。因此乘后左边为 $(x^2y)'$。右边分部积分的细节为

$$
\int x^2\ln x\,\mathrm dx
=\frac{x^3}3\ln x-\int\frac{x^3}3\frac{\mathrm dx}{x}
=\frac{x^3}3\ln x-\frac{x^3}9.
$$

除以 $x^2$，先得到通解 $y=x\ln x/3-x/9+C/x^2$，再用 $y(1)=-1/9$ 得 $C=0$，这样两个步骤各自的作用就很清楚。

**5.（2005.5）答案：$k=3/4$。**

由 $x\arcsin x=x^2+O(x^4)$，有

$$
\sqrt{1+x\arcsin x}=1+\frac{x^2}{2}+O(x^4).
$$

又因为 $\cos x=1-x^2/2+O(x^4)$，所以

$$
\sqrt{\cos x}=1-\frac{x^2}{4}+O(x^4).
$$

相减得到 $\beta(x)=3x^2/4+O(x^4)$，要与 $kx^2$ 等价，必须 $k=3/4$。

也可先有理化，避免直接展开两个平方根相减：

$$
\frac{\beta(x)}{x^2}
=\frac{(1-\cos x)/x^2+\arcsin x/x}
{\sqrt{1+x\arcsin x}+\sqrt{\cos x}}
\longrightarrow\frac{1/2+1}{2}=\frac34.
$$

若 $kx^2$ 与 $\beta(x)$ 等价，则二者之比必须趋于1，故 $k/(3/4)=1$，得到 $k=3/4$。

**6.（2005.6）答案：$2$。**

将三个新列向量的系数按列排列，得到

$$
B=A\begin{pmatrix}1&1&1\\1&2&3\\1&4&9\end{pmatrix}.
$$

右侧系数矩阵为范德蒙德矩阵，行列式等于

$$
(2-1)(3-1)(3-2)=2.
$$

所以 $|B|=|A|\cdot2=2$。

不使用范德蒙德公式，也可用列变换计算系数行列式。将第2、3列分别减第1列，得

$$
\begin{vmatrix}1&1&1\\1&2&3\\1&4&9\end{vmatrix}
=\begin{vmatrix}1&0&0\\1&1&2\\1&3&8\end{vmatrix}
=\begin{vmatrix}1&2\\3&8\end{vmatrix}=8-6=2.
$$

系数应按“每个新向量占一列”排列，因为矩阵乘法 $AC$ 的第 $j$ 列是 $A$ 的各列按 $C$ 第 $j$ 列系数组成的线性组合。

## 二、选择题（7～14）

**7.（2005.7）答案：C，恰有两个不可导点。**

当 $|x|\le1$ 时，$1\le1+|x|^{3n}\le2$，取 $n$ 次方根再令 $n\to\infty$，极限为1。

当 $|x|>1$ 时，提出 $|x|^{3n}$，极限为 $|x|^3$。因此

$$
f(x)=\begin{cases}1,&|x|\le1,\\|x|^3,&|x|>1.\end{cases}
$$

在 $x=\pm1$ 处，外侧导数分别为 $-3,3$，内侧导数为0，所以这两点不可导。其余位置均可导，故选 C。

在 $|x|>1$ 的情形，具体拆分为

$$
(1+|x|^{3n})^{1/n}
=|x|^3(1+|x|^{-3n})^{1/n}\longrightarrow|x|^3.
$$

分段函数在 $x=\pm1$ 处的两侧函数值都趋于1，所以这两点是连续而不可导。零点附近函数恒等于1，并不会因原表达式含绝对值就多出一个不可导点。

**8.（2005.8）答案：A。**

如果原函数 $F$ 为偶函数，则对 $F(-x)=F(x)$ 求导，得到 $-f(-x)=f(x)$，所以 $f$ 为奇函数。

反过来，若 $f$ 为奇函数，则

$$
F(x)-F(0)=\int_0^x f(t)\,\mathrm dt
$$

为偶函数，加常数不改变偶性，所以 $F$ 必为偶函数。

B 不成立，因为给原函数加一个非零常数就可能破坏奇性；C 不成立，例如 $f\equiv1$ 是周期函数而其原函数 $x$ 不是周期函数；D 不成立，例如 $F=x^3$ 单调，而 $f=3x^2$ 在实轴不单调。

反方向中的偶性可以直接写成

$$
\int_0^{-x}f(t)\,\mathrm dt
=-\int_0^xf(-u)\,\mathrm du
=\int_0^xf(u)\,\mathrm du.
$$

这里换元既改变 $\mathrm dt$ 的符号，也把奇函数 $f(-u)$ 变为 $-f(u)$，两个负号相消。B可用最简单的反例 $f\equiv1,F=x+1$：导函数为偶函数，但这个原函数不为奇函数。原函数的任意积分常数是区分A、B的关键。

**9.（2005.9）答案：A，$3+\ln2/8$。**

由 $x=t^2+2t=3$，并结合 $\ln(1+t)$ 的定义域 $t>-1$，取 $t=1$。对应点为 $(3,\ln2)$。

切线斜率为

$$
\frac{\mathrm dy}{\mathrm dx}=\frac{1/(1+t)}{2t+2},
$$

在 $t=1$ 处为 $1/8$，因此法线斜率为 $-8$。

法线方程为 $y-\ln2=-8(x-3)$。令 $y=0$，得到横坐标 $x=3+\ln2/8$。

参数方程 $t^2+2t=3$ 分解为 $(t-1)(t+3)=0$，另一个代数解 $t=-3$ 因 $1+t<0$ 而不在对数定义域内。

所求是法线与横轴的交点，因此必须在法线方程中令纵坐标为0：

$$
-\ln2=-8(x-3),\qquad x-3=\frac{\ln2}{8}.
$$

切线斜率与法线斜率互为负倒数，交点横坐标增加的是 $\ln2/8$。

**10.（2005.10）答案：D，$(a+b)\pi/2$。**

区域关于 $y=x$ 对称。将 $x,y$ 交换后，积分值保持不变，因此将原积分与交换后的积分相加，可得

$$
2I=\iint_D
\frac{(a+b)[\sqrt{f(x)}+\sqrt{f(y)}]}{\sqrt{f(x)}+\sqrt{f(y)}}\,\mathrm d\sigma
=(a+b)\operatorname{Area}(D).
$$

$D$ 是半径2的四分之一圆盘，面积为 $\pi$，所以 $I=(a+b)\pi/2$。

为看清交换变量的过程，设 $p=\sqrt{f(x)}$、$q=\sqrt{f(y)}$。同一区域上的两个被积表达式相加为

$$
\frac{ap+bq}{p+q}+\frac{aq+bp}{p+q}=a+b.
$$

题设 $f>0$ 保证分母 $p+q>0$。换元 $(x,y)\mapsto(y,x)$ 的雅可比绝对值为1，且将四分之一圆盘映回自身，因此两个积分确实相等。面积是 $\pi\cdot2^2/4=\pi$，代入便得到D。

**11.（2005.11）答案：B，$u_{xx}=u_{yy}$。**

令 $\Psi$ 是 $\psi$ 的一个原函数，原式可写成

$$
u(x,y)=[\varphi+\Psi](x+y)+[\varphi-\Psi](x-y).
$$

对于任意二阶可导函数 $H$，$H(x+y)$ 的两个纯二阶偏导相同；$H(x-y)$ 对 $y$ 求两次偏导时，两次负号相消，结果也与对 $x$ 求两次相同。

因此两部分都满足该关系，相加后仍有 $u_{xx}=u_{yy}$。

若直接求导，也能核实变限积分的上下限符号。一次偏导分别为

$$
u_x=\varphi'(x+y)+\varphi'(x-y)+\psi(x+y)-\psi(x-y),
$$

$$
u_y=\varphi'(x+y)-\varphi'(x-y)+\psi(x+y)+\psi(x-y).
$$

对 $x$、$y$ 各自再求一次导数，两式均得到

$$
\varphi''(x+y)+\varphi''(x-y)+\psi'(x+y)-\psi'(x-y).
$$

特别注意下限 $x-y$ 对 $y$ 求导为 $-1$，与变下限积分自身的负号相消，因此第一次 $y$ 偏导中的两个 $\psi$ 项均为正。

**12.（2005.12）答案：D。**

在 $x=0$ 附近，$x/(x-1)\sim-x$，所以分母 $\mathrm e^{x/(x-1)}-1\sim-x$，函数无界，零点为第二类间断点。

在 $x=1$ 左侧，指数趋于负无穷，函数趋于 $-1$；右侧指数趋于正无穷，函数趋于0。两单侧极限有限而不同，因此1是第一类跳跃间断点。

在1附近，先作代数分解更容易判断指数方向：

$$
\frac{x}{x-1}=1+\frac1{x-1}.
$$

当 $x\to1^-$ 时，该式趋于 $-\infty$，所以 $f(x)\to1/(0-1)=-1$；当 $x\to1^+$ 时趋于 $+\infty$，所以 $f(x)\to0$。第一类只要求两侧极限有限，并不要求左右相等。

在0附近则 $f(x)\sim-1/x$，左右分别趋于 $+\infty$ 与 $-\infty$，因此属于第二类。

**13.（2005.13）答案：B，$\lambda_2\ne0$。**

有

$$
A(\alpha_1+\alpha_2)=\lambda_1\alpha_1+\lambda_2\alpha_2.
$$

由于两个特征值不同，$\alpha_1,\alpha_2$ 线性无关。新向量与 $\alpha_1$ 线性无关，恰好要求它沿 $\alpha_2$ 的系数不为零，即 $\lambda_2\ne0$。

将两个方向的论证分别写出：若 $\lambda_2=0$，第二个向量退化为 $\lambda_1\alpha_1$，显然相关。若 $\lambda_2\ne0$，设

$$
s\alpha_1+tA(\alpha_1+\alpha_2)=0.
$$

整理得 $(s+t\lambda_1)\alpha_1+t\lambda_2\alpha_2=0$。由 $\alpha_1,\alpha_2$ 线性无关，有 $t\lambda_2=0$，故 $t=0$，再得 $s=0$。因此充分性与必要性都成立。

**14.（2005.14）答案：C。**

令 $P$ 为交换第1、2行的初等矩阵，则 $B=PA$，$P^{-1}=P$，且 $|B|=-|A|$。

所以

$$
B^*=|B|B^{-1}=-|A|A^{-1}P=-A^*P.
$$

右乘 $P$ 对应交换第1、2列，因此交换 $A^*$ 的这两列得到的是 $-B^*$，选 C。

伴随矩阵公式 $A^*=\det(A)A^{-1}$ 在这里可用，是因为题设明确给出 $A$ 可逆。逆矩阵的乘法顺序必须倒转：

$$
(PA)^{-1}=A^{-1}P^{-1}=A^{-1}P.
$$

于是

$$
-B^*=A^*P.
$$

右乘置换矩阵交换的是列，而交换原矩阵两行又会使行列式变号，这分别解释了答案中的“交换列”和负号。

## 三、解答题（15～23）

**15.（2005.15）答案：$\dfrac12$。**

**第一步：将分母中的积分改写成普通变上限积分。**

在分母中令 $u=x-t$，则 $\mathrm dt=-\mathrm du$；当 $t=0,x$ 时，$u=x,0$。于是

$$
\int_0^x f(x-t)\,\mathrm dt
=-\int_x^0f(u)\,\mathrm du=\int_0^x f(u)\,\mathrm du.
$$

**第二步：把两个积分都换到固定区间。**

分子令 $t=xs$，分母积分令 $u=xs$。对任意非零 $x$ 都有

$$
\int_0^x(x-t)f(t)\,\mathrm dt
=x^2\int_0^1(1-s)f(xs)\,\mathrm ds,
$$

$$
x\int_0^x f(u)\,\mathrm du
=x^2\int_0^1f(xs)\,\mathrm ds.
$$

即使 $x<0$，方向的变化也已经由 $\mathrm dt=x\mathrm ds$ 的负号包含在内，不需要另加负号。约去 $x^2$，原商成为

$$
\frac{\int_0^1(1-s)f(xs)\,\mathrm ds}
{\int_0^1f(xs)\,\mathrm ds}.
$$

**第三步：用连续性控制两个积分的极限。**

因为 $f$ 在零点连续，当 $x\to0$ 时，所有 $xs$（$0\le s\le1$）都满足 $|xs|\le|x|$。记

$$
\varepsilon(x)=\sup_{|t|\le|x|}|f(t)-f(0)|,
$$

则 $\varepsilon(x)\to0$。由积分估计，

$$
\left|\int_0^1 f(xs)\,\mathrm ds-f(0)\right|
\le\varepsilon(x),
$$

$$
\left|\int_0^1(1-s)f(xs)\,\mathrm ds-\frac{f(0)}2\right|
\le\frac{\varepsilon(x)}2.
$$

所以分子、分母分别趋于 $f(0)/2$、$f(0)$。题设 $f(0)\ne0$ 保证分母在充分小的非零 $x$ 附近不为零，商的极限合法。因此

$$
\boxed{\lim_{x\to0}
\frac{\int_0^x(x-t)f(t)\,\mathrm dt}
{x\int_0^xf(x-t)\,\mathrm dt}=\frac12.}
$$

全程只使用了连续性，没有要求 $f$ 本身可导。

**16.（2005.16）答案：$x=\varphi(y)=\ln y-\dfrac12+\dfrac1{2y}$，图示部分 $y\ge1$。**

**第一步：用竖直截线表示第一块面积。**

图中点 $M$ 位于 $C_2:y=\mathrm e^x$ 上，图示分支满足 $x\ge0,y\ge1$。对 $0\le t\le x$，$C_2$ 位于 $C_1$ 上方，竖直截线的长度为

$$
\mathrm e^t-\frac{1+\mathrm e^t}{2}
=\frac{\mathrm e^t-1}{2}.
$$

两条曲线在 $(0,1)$ 相交，因此

$$
\begin{aligned}
S_1(x)
&=\frac12\int_0^x(\mathrm e^t-1)\,\mathrm dt\\
&=\frac12[\mathrm e^t-t]_0^x
=\frac{\mathrm e^x-1-x}{2}.
\end{aligned}
$$

**第二步：用水平截线表示第二块面积。**

$C_2$ 写成反函数形式为 $x=\ln y$，$C_3$ 写成 $x=\varphi(y)$。按图中位置，$C_3$ 位于 $C_2$ 左侧，因此高度为 $u$ 的水平截线长度是

$$
\ln u-\varphi(u).
$$

两条曲线同过 $(0,1)$，从高度1积到高度 $y$，得到

$$
S_2(y)=\int_1^y[\ln u-\varphi(u)]\,\mathrm du.
$$

这里积分变量是纵坐标，所以用“右边界减左边界”。

**第三步：将面积相等写成只含 $y$ 的等式。**

因为 $M$ 在 $y=\mathrm e^x$ 上，所以 $x=\ln y$。代入 $S_1$，题设变为

$$
\int_1^y[\ln u-\varphi(u)]\,\mathrm du
=\frac{y-1-\ln y}{2}.
$$

两边对 $y$ 求导：

$$
\ln y-\varphi(y)=\frac12\left(1-\frac1y\right).
$$

移项即得

$$
\boxed{\varphi(y)=\ln y-\frac12+\frac1{2y}.}
$$

**第四步：检验过点条件、单调性和面积。**

所得函数满足 $\varphi(1)=0$；在 $y\ge1$ 上，

$$
\varphi'(y)=\frac1y-\frac1{2y^2}
=\frac{2y-1}{2y^2}>0,
$$

故其反函数也单调增加。又有 $\ln y-\varphi(y)=(y-1)/(2y)\ge0$，与图中的左右关系一致。

将该差从1积到 $y$，得到 $(y-1-\ln y)/2$，与 $S_1(\ln y)$ 完全相同。因此没有因求导而遗漏面积等式中的常数条件。

**17.（2005.17）答案：$20$。**

**第一步：把图中信息转换成函数值与导数值。**

曲线经过 $(0,0)$、$(3,2)$，两条切线交于 $(2,4)$，因此

$$
f(0)=0,\qquad f(3)=2,
$$

$$
f'(0)=\frac{4-0}{2-0}=2,
\qquad f'(3)=\frac{4-2}{2-3}=-2.
$$

第二条切线向右下方倾斜，斜率为负。又由 $(3,2)$ 是拐点，且 $f''$ 连续，得 $f''(3)=0$；否则连续性会使二阶导数在该点两侧保持同号，无法改变凹凸性。

**第二步：第一次分部积分，消去三阶导数。**

将 $x^2+x$ 作为需要求导的因子，得到

$$
I=[(x^2+x)f''(x)]_0^3-\int_0^3(2x+1)f''(x)\,\mathrm dx.
$$

上端点项为 $12f''(3)=0$，下端点项因 $x^2+x=0$ 而为0。因此不需要知道 $f''(0)$，即可得到

$$
I=-\int_0^3(2x+1)f''(x)\,\mathrm dx.
$$

**第三步：再次分部积分，再代入边界数据。**

$$
\begin{aligned}
I&=-[(2x+1)f'(x)]_0^3+2\int_0^3 f'(x)\,\mathrm dx\\
 &=-[7f'(3)-f'(0)]+2[f(3)-f(0)]\\
 &=-[7(-2)-2]+2(2-0)=\boxed{20}.
\end{aligned}
$$

两次分部积分把未知函数的积分转化成了题图给出的边界数据。

**18.（2005.18）答案：$y=2x+\sqrt{1-x^2}$，$-1<x<1$。**

**第一步：明确换元后各个导数所对应的变量。**

令 $x=\cos t$、$Y(t)=y(\cos t)$。因为 $0<t<\pi$，有 $\sin t>0$，$x$ 与 $t$ 一一对应，且 $-1<x<1$。

以下 $Y'$、$Y''$ 表示对 $t$ 求导，$y'$、$y''$ 表示对 $x$ 求导。由链式法则，

$$
Y'(t)=y'(x)\frac{\mathrm dx}{\mathrm dt}
=-\sin t\,y'(x).
$$

再对 $t$ 求导：

$$
\begin{aligned}
Y''(t)
&=-\cos t\,y'(x)-\sin t\,y''(x)\frac{\mathrm dx}{\mathrm dt}\\
&=-\cos t\,y'(x)+\sin^2t\,y''(x).
\end{aligned}
$$

**第二步：代入并化为常系数方程。**

由于 $1-x^2=\sin^2t$、$x=\cos t$，原方程左边正好是

$$
(1-x^2)y''-xy'+y
=Y''+Y.
$$

因此换元后为

$$
\boxed{Y''+Y=0.}
$$

其特征方程 $r^2+1=0$ 的根为 $\pm i$，实通解为

$$
Y=C_1\cos t+C_2\sin t.
$$

**第三步：使用初始条件确定常数。**

$x=0$ 对应 $t=\pi/2$，故

$$
Y(\pi/2)=y(0)=1,
\qquad Y'(\pi/2)=-y'(0)=-2.
$$

从通解可得 $Y(\pi/2)=C_2$、$Y'(\pi/2)=-C_1$，因此 $C_2=1,C_1=2$。

最后由 $\sin t>0$ 选取正平方根 $\sin t=\sqrt{1-x^2}$，得到

$$
\boxed{y=2x+\sqrt{1-x^2},\qquad-1<x<1.}
$$

**第四步：代回核验。**

$$
y'=2-\frac{x}{\sqrt{1-x^2}},
\qquad y''=-\frac1{(1-x^2)^{3/2}}.
$$

代入原方程，根式项合并为

$$
-\frac1{\sqrt{1-x^2}}+\frac{x^2}{\sqrt{1-x^2}}
+\sqrt{1-x^2}=0,
$$

而 $-2x+2x$ 也相消，两个初值同时满足。

**19.（2005.19）两个中值结论的证明。**

**（Ⅰ）把交点条件转化为连续函数的零点。**

希望找到 $f(\xi)=1-\xi$，因此构造

$$
F(x)=f(x)+x-1.
$$

它在 $[0,1]$ 上连续，而且

$$
F(0)=f(0)-1=-1<0,
\qquad F(1)=f(1)=1>0.
$$

由连续函数的零点定理，存在 $\xi\in(0,1)$ 使 $F(\xi)=0$，即

$$
\boxed{f(\xi)=1-\xi.}
$$

**（Ⅱ）以这个内点分割区间，构造互为倒数的两条割线斜率。**

在 $[0,\xi]$ 上，$f$ 连续且在内部可导，由拉格朗日中值定理，存在 $\eta\in(0,\xi)$，使

$$
f'(\eta)=\frac{f(\xi)-f(0)}{\xi-0}
=\frac{1-\xi}{\xi}.
$$

同理，在 $[\xi,1]$ 上存在 $\zeta\in(\xi,1)$，使

$$
f'(\zeta)=\frac{f(1)-f(\xi)}{1-\xi}
=\frac{1-(1-\xi)}{1-\xi}
=\frac\xi{1-\xi}.
$$

因为 $0<\xi<1$，两个分母均非零。两式相乘，得到

$$
\boxed{f'(\eta)f'(\zeta)=1.}
$$

并且 $0<\eta<\xi<\zeta<1$，从而 $\eta,\zeta$ 确实是两个不同的内点。只在整个区间上使用一次中值定理，只能得到一个导数等于1的点，不能直接保证题目要求的两个不同点。

**20.（2005.20）答案：最大值3，最小值 $-2$。**

**第一步：由全微分恢复二元函数。**

将全微分与 $\mathrm dz=f_x\mathrm dx+f_y\mathrm dy$ 对比，得到

$$
f_x=2x,\qquad f_y=-2y.
$$

先对 $x$ 积分，得 $f=x^2+g(y)$，这里积分“常数”可以是 $y$ 的函数。再对 $y$ 求偏导，有 $g'(y)=-2y$，故

$$
f(x,y)=x^2-y^2+C.
$$

利用 $f(1,1)=2$，得 $C=2$，所以

$$
\boxed{f(x,y)=x^2-y^2+2.}
$$

**第二步：从椭圆约束直接给出全局上下界。**

若 $x^2+y^2/4\le1$，则 $0\le x^2\le1$、$0\le y^2\le4$。因此

$$
f=x^2-y^2+2\le x^2+2\le3,
$$

$$
f=x^2-y^2+2\ge2-y^2\ge-2.
$$

**第三步：检查上下界能否在区域内取到。**

取 $(x,y)=(1,0)$ 或 $(-1,0)$，椭圆约束取等号，且 $f=3$，所以上界能达到。

取 $(x,y)=(0,2)$ 或 $(0,-2)$，约束同样成立，且 $f=-2$，所以下界也能达到。于是

$$
\boxed{f_{\max}=3\quad\text{在 }(\pm1,0)\text{ 处取得},}
$$

$$
\boxed{f_{\min}=-2\quad\text{在 }(0,\pm2)\text{ 处取得}.}
$$

上述估计对椭圆域中的每一点都成立，因此已经同时覆盖内部与边界，无须只凭驻点判断全局最值。

**21.（2005.21）答案：$\dfrac\pi4-\dfrac13$。**

**第一步：按绝对值内部表达式的符号分区。**

记 $g=x^2+y^2-1$，令 $D_0=\{(x,y):x\ge0,y\ge0,x^2+y^2\le1\}$，即正方形 $D$ 内的单位四分之一圆盘。在 $D_0$ 内 $g\le0$，其外 $g\ge0$，所以

$$
\begin{aligned}
I&=\iint_{D\setminus D_0}g\,\mathrm d\sigma-\iint_{D_0}g\,\mathrm d\sigma\\
 &=\iint_Dg\,\mathrm d\sigma+2\iint_{D_0}(1-x^2-y^2)\,\mathrm d\sigma.
\end{aligned}
$$

系数2来自把圆盘中原来的负贡献改成相等大小的正贡献。

**第二步：计算整个正方形上的多项式积分。**

$$
\begin{aligned}
\iint_Dg\,\mathrm d\sigma
 &=\int_0^1\mathrm dx\int_0^1(x^2+y^2-1)\,\mathrm dy\\
 &=\int_0^1\left(x^2+\frac13-1\right)\mathrm dx
 =\frac13+\frac13-1=-\frac13.
\end{aligned}
$$

**第三步：用极坐标计算圆盘中的补偿项。**

在 $D_0$ 上，$0\le r\le1$、$0\le\theta\le\pi/2$，面积元为 $r\,\mathrm dr\,\mathrm d\theta$，故

$$
\begin{aligned}
2\iint_{D_0}(1-x^2-y^2)\,\mathrm d\sigma
 &=2\int_0^{\pi/2}\mathrm d\theta\int_0^1(1-r^2)r\,\mathrm dr\\
 &=\pi\left[\frac{r^2}{2}-\frac{r^4}{4}\right]_0^1=\frac\pi4.
\end{aligned}
$$

合并得 $\boxed{I=\pi/4-1/3}$，结果为正，与原被积函数非负一致。

**22.（2005.22）答案：$a=1$。**

**第一步：把单向线性表示转化为空间的严格包含。**

记 $U=(\alpha_1,\alpha_2,\alpha_3)$、$V=(\beta_1,\beta_2,\beta_3)$。题意表示

$$
\operatorname{Col}(U)\subsetneq\operatorname{Col}(V).
$$

若两者维数相同，包含关系就会变成相等，进而可以相互表示，与后半句矛盾。因此

$$
r(U)<r(V)\le3,
$$

特别有 $r(U)<3$，故 $\det U=0$。

**第二步：计算行列式，找出候选参数。**

将 $U$ 的第2、3列分别减去第1列，并各提出因子 $a-1$，得

$$
\begin{aligned}
\det U
&=\begin{vmatrix}1&1&a\\1&a&1\\a&1&1\end{vmatrix}\\
&=(a-1)^2\begin{vmatrix}1&0&1\\1&1&0\\a&-1&-1\end{vmatrix}\\
&=-(a-1)^2(a+2).
\end{aligned}
$$

因此只可能 $a=1$ 或 $a=-2$。但行列式为零只是必要条件，还需逐个验证原题的双重要求。

**第三步：验证 $a=1$。**

此时三个 $\alpha$ 都等于非零向量 $(1,1,1)^{\mathrm T}$，所以 $r(U)=1$。另一方面，

$$
\begin{aligned}
\det V
&=\begin{vmatrix}1&-2&-2\\1&a&a\\a&4&a\end{vmatrix}\\
&=(a-4)\begin{vmatrix}1&-2\\1&a\end{vmatrix}
=(a-4)(a+2).
\end{aligned}
$$

这里用了第3列减第2列。代入 $a=1$，得到 $\det V=-9\ne0$，故 $V$ 的列张成整个三维空间。自然可以表示所有 $\alpha$，但一维的 $\alpha$ 张成空间不可能表示三个独立的 $\beta$，所以全部条件成立。

**第四步：排除 $a=-2$。**

此时 $\beta_1=(1,1,-2)^{\mathrm T}$、$\beta_2=(-2,-2,4)^{\mathrm T}$、$\beta_3=(-2,-2,-2)^{\mathrm T}$ 的前两个分量均相等。因此它们的任意线性组合都满足 $x_1=x_2$。

但 $\alpha_2=(1,-2,1)^{\mathrm T}$ 不满足这个关系，不能由 $\beta$ 向量组表示，直接违反题意。故唯一答案为

$$
\boxed{a=1.}
$$

**23.（2005.23）按参数与矩阵秩分类的通解。**

**第一步：从矩阵乘积中读出已知齐次解。**

记

$$
p=(1,2,3)^{\mathrm T},\qquad q=(3,6,k)^{\mathrm T},
\qquad B=(p,2p,q).
$$

矩阵乘积 $AB=0$ 等价于逐列满足 $Ap=0$、$Aq=0$，所以 $p,q$ 都是齐次解。又因 $A$ 的第一行非零，有 $r(A)\ge1$；由于 $p\ne0$ 是齐次解，有 $r(A)\le2$。

先比较两个已知解是否独立：

$$
q-3p=(0,0,k-9)^{\mathrm T}.
$$

当 $k\ne9$ 时，两向量独立；当 $k=9$ 时，它们只能给出同一个方向。因此必须分情况处理。

**情形一：$k\ne9$。**

零空间包含独立向量 $p,q$，故维数至少为2，由 $3-r(A)\ge2$ 得 $r(A)\le1$。结合 $r(A)\ge1$，可知

$$
r(A)=1,\qquad\dim\ker A=2.
$$

因此 $p,q$ 已构成基础解系，通解为

$$
\boxed{\boldsymbol x=s\begin{pmatrix}1\\2\\3\end{pmatrix}
+t\begin{pmatrix}3\\6\\k\end{pmatrix},\qquad s,t\in\mathbb R.}
$$

还可以用第一行直接核验结构。由 $AB=0$ 得

$$
a+2b+3c=0,\qquad3a+6b+kc=0.
$$

第二式减第一式的3倍，得 $(k-9)c=0$，所以此时 $c=0,a=-2b$。第一行非零迫使 $b\ne0$，整个方程组等价于 $x_2=2x_1$，$x_3$ 自由，与上述二维通解一致。

**情形二：$k=9$，且 $r(A)=2$。**

此时 $q=3p$，已知解只剩一个独立方向。因为零空间维数为 $3-2=1$，而 $p$ 是非零解，故

$$
\boxed{\boldsymbol x=t\begin{pmatrix}1\\2\\3\end{pmatrix},\qquad t\in\mathbb R.}
$$

**情形三：$k=9$，且 $r(A)=1$。**

所有行都是非零第一行 $(a,b,c)$ 的倍数，所以方程组等价于

$$
ax_1+bx_2+cx_3=0,
\qquad a+2b+3c=0.
$$

此时零空间二维，除了 $p$ 还需一个独立解。取

$$
w=(3b-2c,\ c-3a,\ 2a-b)^{\mathrm T}.
$$

直接展开可得

$$
\begin{aligned}
(a,b,c)w
&=a(3b-2c)+b(c-3a)+c(2a-b)=0,\\
p^{\mathrm T}w
&=(3b-2c)+2(c-3a)+3(2a-b)=0.
\end{aligned}
$$

因此 $w$ 是齐次解，并与 $p$ 正交。还需确认 $w\ne0$：若 $w=0$，则行向量 $(a,b,c)$ 与 $p$ 平行；但 $a+2b+3c=0$ 又说明它与 $p$ 正交，只能是零向量，与题设矛盾。

所以 $p,w$ 为两个独立的非零解，构成基础解系，通解为

$$
\boxed{\boldsymbol x=s\begin{pmatrix}1\\2\\3\end{pmatrix}
+t\begin{pmatrix}3b-2c\\c-3a\\2a-b\end{pmatrix},\qquad s,t\in\mathbb R.}
$$

以上三种情形覆盖 $k$ 与 $r(A)$ 的全部可能。题设在 $k=9$ 时没有再固定矩阵的秩，因此需要同时保留一维与二维两类零空间的结果。
