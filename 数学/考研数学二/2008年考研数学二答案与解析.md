# 2008年考研数学二答案与解析

> 我的解答，未与官方核对。

## 答案速查

> 选择题：1.D　2.C　3.D　4.A　5.B　6.A　7.C　8.D。

> 填空题：9.$2$；10.$y=Cx-x\mathrm e^{-x}$；11.$y=x+1$；12.$(-1,-6)$；13.$(\ln2-1)/\sqrt2$；14.$-1$。

15. 极限为 $1/6$。

16. $y''=(x+1)\mathrm e^x$。

17. 积分为 $\pi^2/16+1/4$。

18. 积分为 $19/4+\ln2$。

19. $f(x)=(\mathrm e^x+\mathrm e^{-x})/2$。

20. 中值定理及二阶导数结论的证明见正文。

21. 最小值为6，最大值为72。

22. 行列式为 $(n+1)a^n$；唯一解时 $x_1=n/[(n+1)a]$；奇异情形的通解见正文。

23. $P^{-1}AP=\begin{pmatrix}-1&0&0\\0&1&1\\0&0&1\end{pmatrix}$。

## 一、选择题（1～8）

**1.（2008.1）答案：D，3个零点。**

展开并求导：

$$
f(x)=x^4-3x^3+2x^2,
\qquad f'(x)=x(4x^2-9x+4).
$$

二次因子的判别式为 $81-64=17>0$，所以它有两个不同的实根，且常数项不为零，这两个根均不为0。

加上前面的因子 $x$，导函数共有3个不同零点。

**2.（2008.2）答案：C，曲边三角形 $ACD$ 的面积。**

分部积分给出

$$
\int_0^a x f'(x)\,\mathrm dx
=af(a)-\int_0^a f(x)\,\mathrm dx
=\int_0^a[f(a)-f(x)]\,\mathrm dx.
$$

这表示横线 $y=f(a)$ 与曲线之间的面积，左侧由 $y$ 轴上的线段 $CD$ 封闭，右端在 $A$ 点相接。

因此对应图中的曲边三角形 $ACD$，选 C。

**3.（2008.3）答案：D。**

给出的通解对应特征根为 $1,2\mathrm i,-2\mathrm i$。所以特征多项式为

$$
(r-1)(r^2+4)=r^3-r^2+4r-4.
$$

对应微分方程为

$$
y'''-y''+4y'-4y=0,
$$

即 D。

**4.（2008.4）答案：A。**

需要检查 $x=0$ 和 $x=1$。

在零点附近，$\sin x\ln|x|\sim x\ln|x|\to0$，而 $|x-1|\to1$，所以零点为可去间断点。

在 $x=1$ 附近，$\ln x\sim x-1$，因此

$$
\lim_{x\to1^-}f(x)=-\sin1,
\qquad\lim_{x\to1^+}f(x)=\sin1.
$$

两个单侧极限有限但不相等，是跳跃间断点。因此有1个可去间断点、1个跳跃间断点。

**5.（2008.5）答案：B。**

如果数列 $x_n$ 单调，单调函数作用于它后，$f(x_n)$ 仍为单调数列。又因为 $f$ 在整个实轴有界，$f(x_n)$ 也有界，所以必收敛。

A 不成立，因为单调函数可能有跳跃。比如 $f(x)=\arctan x$（$x\le0$）、$f(x)=1+\arctan x$（$x>0$），它严格增加且有界，但 $x_n=(-1)^n/n\to0$ 时，函数值有两个不同子列极限。

C、D 也不成立：取 $f(x)=\arctan x$、$x_n=n$，函数值单调且收敛，而原数列发散。

**6.（2008.6）答案：A，$vf(u^2)$。**

图示区域是内半径为1、外半径为 $u$、角度范围为 $0\le\theta\le v$ 的圆环扇形。

换为极坐标，面积元中的 $r$ 与分母相消：

$$
F(u,v)=\int_0^v\mathrm d\theta\int_1^u f(r^2)\,\mathrm dr
=v\int_1^u f(r^2)\,\mathrm dr.
$$

对 $u$ 求偏导即得 $F_u=vf(u^2)$。

**7.（2008.7）答案：C，两者都可逆。**

由 $A^3=0$，直接计算

$$
(I-A)(I+A+A^2)=I,
$$

$$
(I+A)(I-A+A^2)=I.
$$

所以

$$
(I-A)^{-1}=I+A+A^2,
\qquad (I+A)^{-1}=I-A+A^2.
$$

二者均可逆，选 C。

**8.（2008.8）答案：D。**

取可逆矩阵 $P=\operatorname{diag}(1,-1)$，有

$$
P^{\mathrm T}\begin{pmatrix}1&2\\2&1\end{pmatrix}P
=\begin{pmatrix}1&-2\\-2&1\end{pmatrix}.
$$

这正是 D 中的矩阵，因此二者在实数域上合同。

## 二、填空题（9～14）

**9.（2008.9）答案：$f(0)=2$。**

由 $f$ 连续可知它在零点附近有界，所以 $xf(x)\to0$。

将极限中的比值写成

$$
\frac{1-\cos[xf(x)]}{x^2f(x)^2}
\cdot\frac{x^2}{\mathrm e^{x^2}-1}\cdot f(x).
$$

三个因子的极限依次为 $1/2,1,f(0)$。因此题设极限等于 $f(0)/2=1$，得到 $f(0)=2$。

**10.（2008.10）答案：$y=Cx-x\mathrm e^{-x}$。**

在 $x\ne0$ 的区间上，原方程化为

$$
y'-\frac yx=x\mathrm e^{-x}.
$$

同除以 $x$，得到

$$
\left(\frac yx\right)'=\mathrm e^{-x}.
$$

积分后 $y/x=C-\mathrm e^{-x}$，故通解为 $y=Cx-x\mathrm e^{-x}$。

**11.（2008.11）答案：$y=x+1$。**

对隐式方程求导：

$$
\cos(xy)(y+xy')+\frac{y'-1}{y-x}=1.
$$

代入 $(x,y)=(0,1)$，得到 $1+y'-1=1$，所以切线斜率为1。

经过点 $(0,1)$，切线为 $y=x+1$。

**12.（2008.12）答案：$(-1,-6)$。**

对 $x\ne0$，按实数立方根意义求导，可得

$$
y'=\frac53(x-2)x^{-1/3},
\qquad y''=\frac{10(x+1)}{9x^{4/3}}.
$$

分母在 $x\ne0$ 时为正。因此二阶导数在 $x=-1$ 左侧为负、右侧为正，凹凸性改变。

对应函数值为 $y(-1)=(-1-5)\cdot1=-6$，故拐点是 $(-1,-6)$。

$x=0$ 处虽然不可导，但两侧二阶导数均为正，没有凹凸性的改变，因此不再产生拐点。

**13.（2008.13）答案：$(\ln2-1)/\sqrt2$。**

先取对数：

$$
\ln z=\frac xy\ln\frac yx.
$$

对 $x$ 求偏导，得到

$$
\frac{z_x}{z}=\frac1y\left(\ln\frac yx-1\right).
$$

在 $(1,2)$ 处，$z=\sqrt2$，所以

$$
z_x=\frac{\sqrt2}{2}(\ln2-1)=\frac{\ln2-1}{\sqrt2}.
$$

**14.（2008.14）答案：$\lambda=-1$。**

$A$ 的行列式为特征值之积，即 $|A|=6\lambda$。

三阶矩阵整体乘2，行列式会乘 $2^3$，因此

$$
|2A|=8|A|=48\lambda=-48.
$$

故 $\lambda=-1$。

## 三、解答题（15～23）

**15.（2008.15）答案：$1/6$。**

令 $u=\sin x$，则 $u\to0$ 且 $u/x\to1$。原式可以写成

$$
\frac{(u-\sin u)u}{x^4}
=\frac{u-\sin u}{u^3}\left(\frac ux\right)^4.
$$

由 $u-\sin u\sim u^3/6$，两因子的极限分别为 $1/6$ 和1，因此原极限为 $1/6$。

**16.（2008.16）答案：$y''=(x+1)\mathrm e^x$。**

**第一步：求 $x(t)$。**

微分方程同乘 $\mathrm e^x$ 后成为

$$
(\mathrm e^x)'=2t.
$$

积分并用 $x(0)=0$，得到 $\mathrm e^x=1+t^2$，即 $x=\ln(1+t^2)$。

**第二步：消去参数。**

计算另一个积分：

$$
y=(1+t^2)\ln(1+t^2)-(1+t^2)+1.
$$

代入 $1+t^2=\mathrm e^x$，得到

$$
y=(x-1)\mathrm e^x+1.
$$

所以

$$
y'=x\mathrm e^x,\qquad y''=(x+1)\mathrm e^x.
$$

消去参数后，$t=0$ 处参数导数为零的问题也自然得到处理。

**17.（2008.17）答案：$\pi^2/16+1/4$。**

令 $t=\arcsin x$，则 $x=\sin t$，$0\le t\le\pi/2$，根式与 $\mathrm dx$ 中的余弦因子相消，得到

$$
I=\int_0^{\pi/2}t\sin^2t\,\mathrm dt
=\frac12\int_0^{\pi/2}t\,\mathrm dt-\frac12\int_0^{\pi/2}t\cos2t\,\mathrm dt.
$$

第一项为 $\pi^2/16$。第二个积分分部积分，得

$$
\int_0^{\pi/2}t\cos2t\,\mathrm dt
=\left[\frac t2\sin2t+\frac14\cos2t\right]_0^{\pi/2}
=-\frac12.
$$

所以 $I=\pi^2/16+1/4$。

**18.（2008.18）答案：$19/4+\ln2$。**

**第一步：以常数1的积分为基础。**

单位值1在整个正方形上的积分为4。只需在 $xy>1$ 的区域补上超出的部分 $xy-1$，因此

$$
I=4+\iint_{xy>1,\ (x,y)\in D}(xy-1)\,\mathrm dx\mathrm dy.
$$

**第二步：确定补充区域。**

双曲线 $y=1/x$ 与上边界 $y=2$ 交于 $x=1/2$，所以该区域为

$$
\frac12\le x\le2,\qquad \frac1x\le y\le2.
$$

于是

$$
\begin{aligned}
I&=4+\int_{1/2}^2\left[\frac{x y^2}{2}-y\right]_{1/x}^{2}\mathrm dx\\
&=4+\int_{1/2}^2\left(2x-2+\frac1{2x}\right)\mathrm dx\\
&=4+\left[x^2-2x+\frac12\ln x\right]_{1/2}^{2}\\
&=\frac{19}{4}+\ln2.
\end{aligned}
$$

**19.（2008.19）答案：$f(x)=(\mathrm e^x+\mathrm e^{-x})/2$。**

**第一步：把面积与体积关系写成积分。**

曲面面积和体积分别为

$$
S(t)=2\pi\int_0^t f(x)\sqrt{1+[f'(x)]^2}\,\mathrm dx,
\qquad V(t)=\pi\int_0^t f(x)^2\,\mathrm dx.
$$

已知 $S(t)=2V(t)$ 对任意 $t$ 成立。两边求导，并利用 $f(x)>0$，得到

$$
\sqrt{1+[f'(x)]^2}=f(x).
$$

**第二步：利用增加性选择导数符号。**

因为 $f$ 增加，$f'\ge0$，所以

$$
f'=\sqrt{f^2-1}.
$$

又由 $f(0)=1$ 及增加性，正半轴上取 $f>1$ 的分支。分离变量，得

$$
\int\frac{\mathrm df}{\sqrt{f^2-1}}=x+C,
\qquad \ln(f+\sqrt{f^2-1})=x+C.
$$

令 $x\to0^+$，由初值得 $C=0$。

**第三步：解出显式表达式。**

有 $f+\sqrt{f^2-1}=\mathrm e^x$，同时利用两共轭式乘积为1，得到 $f-\sqrt{f^2-1}=\mathrm e^{-x}$。

两式相加，即得

$$
f(x)=\frac{\mathrm e^x+\mathrm e^{-x}}2.
$$

该函数满足 $f(0)=1$、正半轴增加，且 $1+(f')^2=f^2$，所以也满足原面积体积关系。

**20.（2008.20）积分中值定理与二阶导数结论。**

**（Ⅰ）证明积分中值定理。**

连续函数在闭区间上取得最小值 $m$ 和最大值 $M$。由 $m\le f(x)\le M$，积分得到

$$
m\le\frac1{b-a}\int_a^b f(x)\,\mathrm dx\le M.
$$

这个平均值位于函数最小值和最大值之间。由连续函数的介值定理，存在 $\eta\in[a,b]$，使

$$
f(\eta)=\frac1{b-a}\int_a^b f(x)\,\mathrm dx.
$$

整理即得所需结论。若函数为常数，则任一点均可。

**（Ⅱ）先找一处上升、一处下降。**

由 $\varphi(2)>\varphi(1)$ 和拉格朗日中值定理，存在 $u\in(1,2)$，使 $\varphi'(u)>0$。

在 $[2,3]$ 上使用第一问，存在 $c\in[2,3]$，使

$$
\varphi(c)=\int_2^3\varphi(x)\,\mathrm dx<\varphi(2).
$$

所以 $c>2$。再在 $[2,c]$ 上使用中值定理，存在 $v\in(2,c)$，使 $\varphi'(v)<0$。

**最后对导函数使用中值定理。**

$u<v$，且 $\varphi$ 二阶可导，所以存在 $\xi\in(u,v)\subset(1,3)$，使

$$
\varphi''(\xi)=\frac{\varphi'(v)-\varphi'(u)}{v-u}<0.
$$

**21.（2008.21）答案：最小值6，最大值72。**

**第一步：把约束归结为 $z$ 的范围。**

由 $z=x^2+y^2$，有 $z\ge0$，并且 $x+y=4-z$。

由 $(x+y)^2\le2(x^2+y^2)$，得到

$$
(4-z)^2\le2z,
\qquad (z-2)(z-8)\le0.
$$

因此 $2\le z\le8$。

**第二步：比较目标函数。**

目标函数成为

$$
u=x^2+y^2+z^2=z+z^2.
$$

它在 $[2,8]$ 上严格增加，所以最小可能值为 $2+4=6$，最大可能值为 $8+64=72$。

**第三步：验证端点可达到。**

$z=2$ 时，取 $(x,y)=(1,1)$，两个约束均满足，得到点 $(1,1,2)$，目标值6。

$z=8$ 时，取 $(x,y)=(-2,-2)$，也满足两个约束，得到点 $(-2,-2,8)$，目标值72。

因此两界确实是全局最小值和最大值。

**22.（2008.22）三对角方程组。**

**（Ⅰ）用行列式递推证明公式。**

记 $D_n=\det A_n$，并取 $D_0=1$、$D_1=2a$。按第一行展开可得

$$
D_n=2aD_{n-1}-a^2D_{n-2}.
$$

若 $D_{n-1}=na^{n-1}$、$D_{n-2}=(n-1)a^{n-2}$，则

$$
D_n=[2n-(n-1)]a^n=(n+1)a^n.
$$

初始值也满足该式，所以归纳得到 $D_n=(n+1)a^n$。

**（Ⅱ）唯一解条件及 $x_1$。**

行列式非零当且仅当 $a\ne0$，因此这正是唯一解条件。

用右端向量替换第1列后，按该列展开，分子行列式等于 $D_{n-1}=na^{n-1}$。由克拉默法则，

$$
x_1=\frac{D_{n-1}}{D_n}=\frac{n}{(n+1)a}.
$$

**（Ⅲ）奇异情形。**

当 $a=0$ 且 $n\ge2$ 时，矩阵仅上副对角线为1，方程组化为

$$
x_2=1,\quad x_3=0,\quad\ldots,\quad x_n=0,
$$

最后一行是 $0=0$，$x_1$ 自由。因此有无穷多解，通解为

$$
\boldsymbol x=(t,1,0,\ldots,0)^{\mathrm T},\qquad t\in\mathbb R.
$$

若取退化的一元情形 $n=1$，则 $a=0$ 时方程为 $0=1$，无解。

**23.（2008.23）线性无关与相似变换矩阵。**

**（Ⅰ）证明三个向量线性无关。**

假设

$$
c_1\alpha_1+c_2\alpha_2+c_3\alpha_3=0.
$$

对等式作用算子 $A-I$。利用给定关系，得到

$$
-2c_1\alpha_1+c_3\alpha_2=0.
$$

$\alpha_1,\alpha_2$ 属于不同特征值，所以线性无关，故 $c_1=c_3=0$。再代回原式，得 $c_2=0$。

因此三向量线性无关，$P$ 可逆。

**（Ⅱ）按列写出矩阵的作用。**

$$
AP=(-\alpha_1,\alpha_2,\alpha_2+\alpha_3)
=P\begin{pmatrix}-1&0&0\\0&1&1\\0&0&1\end{pmatrix}.
$$

两边左乘 $P^{-1}$，即得

$$
P^{-1}AP=\begin{pmatrix}-1&0&0\\0&1&1\\0&0&1\end{pmatrix}.
$$
