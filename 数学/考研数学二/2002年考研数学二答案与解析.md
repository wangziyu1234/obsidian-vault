# 2002年考研数学二答案与解析

> 我的解答，未与官方核对。

## 答案速查

> 填空题（原卷一）：1.$-2$；2.$1$；3.$y=\sqrt{1+x}$；4.$2\sqrt2/\pi$；5.$4$。

> 选择题（原卷二）：1.D　2.D　3.C　4.B　5.A。

三（2002.11）：切线为 $y-x=(5-3\sqrt3)/4$；法线为 $x+y=(\sqrt3-1)/4$。

四（2002.12）：变上限积分的分段表达式见正文。

五（2002.13）：$f(x)=\mathrm e^{-1/x}$。

六（2002.14）：$y=x-75x^2/124$，最小体积为 $97\pi/1488$。

七（2002.15）：闸门矩形部分高度 $h=2\ \mathrm m$。

八（2002.16）：数列收敛于 $3/2$。

九（2002.17）：两个严格不等式的证明见正文。

十（2002.18）：唯一系数组为 $(\lambda_1,\lambda_2,\lambda_3)=(3,-3,1)$。

十一（2002.19）：$A-2I$ 可逆；$A=\begin{pmatrix}0&2&0\\-1&-1&0\\0&0&-2\end{pmatrix}$。

十二（2002.20）：$\boldsymbol x=(0,3,0,1)^{\mathrm T}+t(1,-2,1,0)^{\mathrm T}$，$t\in\mathbb R$。

## 一、填空题（原卷一，习题册1～5）

**1.（2002.1；原卷一·1）答案：$a=-2$。**

**第一步：写清连续性要比较的三个量。**

当 $x\le0$ 时，$f(x)=a\mathrm e^{2x}$，因此 $f(0)=a$，左极限也是 $a$。只需求右极限，再令它等于 $a$。

**第二步：把右侧的复合无穷小拆开。**

$$
\frac{1-\mathrm e^{\tan x}}{\arcsin(x/2)}
=-\frac{\mathrm e^{\tan x}-1}{\tan x}
\cdot\frac{\tan x}{x}
\cdot\frac{x/2}{\arcsin(x/2)}\cdot2.
$$

当 $x\to0^+$ 时，前三个分式的极限依次为1、1、1，因此右极限为 $-2$。

令右极限等于函数值，得 $a=-2$。代入后，左右极限与 $f(0)$ 全相等，故条件也充分。

**2.（2002.2；原卷一·2）答案：$1$。**

**第一步：把无界区域面积写成反常积分。**

在 $x\ge0$ 上，$x\mathrm e^{-x}\ge0$，所以面积为

$$
S=\lim_{R\to+\infty}\int_0^R x\mathrm e^{-x}\,\mathrm dx.
$$

**第二步：分部积分。**

取 $u=x$、$\mathrm dv=\mathrm e^{-x}\mathrm dx$，则 $v=-\mathrm e^{-x}$，得到

$$
\int_0^R x\mathrm e^{-x}\,\mathrm dx
=[-x\mathrm e^{-x}]_0^R+\int_0^R\mathrm e^{-x}\,\mathrm dx
=1-(R+1)\mathrm e^{-R}.
$$

因为指数衰减快于一次多项式增长，$(R+1)\mathrm e^{-R}\to0$，所以反常积分收敛，面积为1。

**3.（2002.3；原卷一·3）答案：$y=\sqrt{1+x}$，$x>-1$。**

**第一步：识别乘积的导数。**

原方程左边为

$$
yy''+(y')^2=(yy')'.
$$

因此积分一次得到 $yy'=C_1$。使用初值 $y(0)=1$、$y'(0)=1/2$，得 $C_1=1/2$。

**第二步：再积分并确定常数。**

由 $yy'=1/2$，有 $(y^2)'=1$，所以 $y^2=x+C_2$。代入 $y(0)=1$，得 $C_2=1$。

初值规定函数在0附近取正值，故应选择正平方根：

$$
y(x)=\sqrt{1+x}.
$$

**第三步：代回检验。**

$$
y'=\frac1{2\sqrt{1+x}},\qquad
 y''=-\frac1{4(1+x)^{3/2}}.
$$

所以 $yy''+(y')^2=-1/[4(1+x)]+1/[4(1+x)]=0$，两个初值也均满足。包含初值点的最大光滑区间为 $(-1,+\infty)$。

**4.（2002.4；原卷一·4）答案：$2\sqrt2/\pi$。**

**第一步：识别积分和中的分点与区间长度。**

所给和式为

$$
\frac1n\sum_{k=1}^n\sqrt{1+\cos(\pi k/n)}.
$$

函数 $g(t)=\sqrt{1+\cos\pi t}$ 在 $[0,1]$ 连续，因此它是等分区间、取右端点的黎曼和，极限为

$$
\int_0^1\sqrt{1+\cos\pi t}\,\mathrm dt.
$$

**第二步：用半角公式去根号。**

在该区间内，$0\le\pi t/2\le\pi/2$，余弦非负，所以

$$
\sqrt{1+\cos\pi t}=\sqrt2\cos\frac{\pi t}{2}.
$$

这里已经检查符号，不能省略开平方后的绝对值判断。于是

$$
\int_0^1\sqrt2\cos\frac{\pi t}{2}\,\mathrm dt
=\frac{2\sqrt2}{\pi}\left[\sin\frac{\pi t}{2}\right]_0^1
=\frac{2\sqrt2}{\pi}.
$$

**5.（2002.5；原卷一·5）答案：$4$。**

记给定矩阵为 $A$，计算特征多项式：

$$
\det(\lambda I-A)
=\begin{vmatrix}\lambda&2&2\\-2&\lambda-2&2\\2&2&\lambda-2\end{vmatrix}.
$$

沿第一行展开，得到

$$
\begin{aligned}
\det(\lambda I-A)
&=\lambda[(\lambda-2)^2-4]
-2[-2(\lambda-2)-4]+2[-4-2(\lambda-2)]\\
&=\lambda(\lambda^2-4\lambda)+4\lambda-4\lambda\\
&=\lambda^2(\lambda-4).
\end{aligned}
$$

所以全部特征值为 $0,0,4$，其中唯一非零特征值是4。其和为4，也与矩阵的迹 $0+2+2=4$ 相符。

## 二、选择题（原卷二，习题册6～10）

**6.（2002.6；原卷二·1）答案：D，$f'(1)=1/2$。**

“函数增量的线性主部”就是微分 $\mathrm dy=y'(x_0)\Delta x$，而不是完整的函数增量。

对 $y=f(x^2)$ 使用链式法则：

$$
y'(x)=2x f'(x^2).
$$

在 $x_0=-1$ 处，$y'(-1)=-2f'(1)$，因此

$$
\mathrm dy=[-2f'(1)]\cdot(-0.1)=0.2f'(1).
$$

题设给出这个线性主部等于0.1，所以 $f'(1)=0.1/0.2=0.5$，对应 D。

也可从完整增量看出线性主部的来源。令 $\delta=\Delta x$，则 $(-1+\delta)^2-1=-2\delta+\delta^2$，由 $f$ 在1处可导，

$$
\Delta y=f(1-2\delta+\delta^2)-f(1)
=-2f'(1)\delta+o(\delta).
$$

因此线性主部只取 $-2f'(1)\delta$。不能把内层自变量的完整增量 $0.21$ 直接当成线性部分；它包含了 $\delta^2=0.01$。

**7.（2002.7；原卷二·2）答案：D。**

**第一步：先判断 D 的被积函数。**

$f(t)+f(-t)$ 是偶函数，再乘以奇函数 $t$，得到奇函数

$$
h(t)=t[f(t)+f(-t)].
$$

记 $H(x)=\int_0^x h(t)\,\mathrm dt$。令 $t=-u$，有

$$
H(-x)=-\int_0^x h(-u)\,\mathrm du=\int_0^x h(u)\,\mathrm du=H(x).
$$

所以 D 必为偶函数。

**第二步：逐一排除其余选项。**

A 的被积函数 $f(t^2)$ 总是偶的，其变上限积分一般为奇函数；取 $f\equiv1$，结果为 $x$，不是偶函数。

B 也取 $f\equiv1$，积分仍为 $x$，所以不一定为偶函数。

C 取 $f(t)=t$，被积函数为 $2t^2$，积分为 $2x^3/3$，仍不是偶函数。因此唯一必然成立的是 D。

**8.（2002.8；原卷二·3）答案：C，极限为2。**

**第一步：从微分方程直接求二阶导数的初值。**

在 $x=0$ 处代入方程，结合 $y(0)=y'(0)=0$，得到

$$
y''(0)=1.
$$

因此函数在零点的二阶展开为

$$
y(x)=y(0)+y'(0)x+\frac12y''(0)x^2+o(x^2)
=\frac{x^2}{2}+o(x^2).
$$

**第二步：比较分子、分母的主项。**

分子满足 $\ln(1+x^2)=x^2+o(x^2)$，所以

$$
\lim_{x\to0}\frac{\ln(1+x^2)}{y(x)}
=\lim_{x\to0}\frac{1+o(1)}{1/2+o(1)}=2.
$$

无需求出整条微分方程的通解，也不需要知道 $p,q$ 的具体值。

这里二阶展开的依据也可从方程看出：$y''=\mathrm e^{3x}-py'-qy$ 的右端连续，所以 $y''$ 连续，能够应用二阶泰勒公式。又 $y(x)/x^2\to1/2>0$，故在充分小的去心邻域内分母不为零，原商有定义。

**9.（2002.9；原卷二·4）答案：B。**

**B 的证明。** 设 $\lim_{x\to+\infty}f'(x)=L$。若 $L>0$，则从某个 $R$ 起有 $f'(x)>L/2$。对 $x>R$ 使用中值定理：

$$
f(x)-f(R)>\frac L2(x-R)\longrightarrow+\infty,
$$

与函数有界矛盾。若 $L<0$，同理可推出 $f(x)\to-\infty$，也矛盾。因此有限极限只能为零。若把“存在”理解为允许无穷极限，同样用最终 $f'>1$ 或 $f'<-1$ 排除。

**A 的反例。** 取 $f(x)=\sin(x^2)/x$，$x>0$。它有界且在无穷远趋于零，但

$$
f'(x)=2\cos(x^2)-\frac{\sin(x^2)}{x^2}
$$

在无穷远不趋于零。具体地，取 $x_n=\sqrt{2n\pi}$，有 $f'(x_n)=2$；取 $z_n=\sqrt{(2n+1)\pi}$，有 $f'(z_n)=-2$，两列导数值极限不同。此反例在整个正半轴也确实有界：$0<x\le1$ 时 $|f(x)|\le x$，$x\ge1$ 时 $|f(x)|\le1/x$。

**C、D 的反例。** 取 $f(x)=1-\mathrm e^{-x}$。它在正半轴有界且可导，$f(x)\to0$（$x\to0^+$），但 $f'(x)\to1$，所以这两项都不成立。

**10.（2002.10；原卷二·5）答案：A。**

令 $V=\operatorname{span}(\alpha_1,\alpha_2,\alpha_3)$。题设说明 $\beta_1\in V$、$\beta_2\notin V$。

对于任意 $k$，若 $k\beta_1+\beta_2\in V$，则

$$
\beta_2=(k\beta_1+\beta_2)-k\beta_1\in V,
$$

矛盾。因此新向量始终在原三维空间之外，加入原来独立的三个向量后，四向量仍独立，A 成立，B 不成立。

C 在 $k=0$ 时，第四个向量就是 $\beta_1\in V$，必相关，所以不成立。

D 在 $k=1$ 时，$\beta_1+\beta_2\notin V$，四向量独立，因此也不成立。

直接按线性无关的定义验证 A：设

$$
c_1\alpha_1+c_2\alpha_2+c_3\alpha_3+c_4(k\beta_1+\beta_2)=0.
$$

若 $c_4\ne0$，移项并除以 $c_4$ 就会把 $\beta_2$ 表示成 $V$ 中的向量，与题设矛盾。因此 $c_4=0$，再由原三向量线性无关，得到 $c_1=c_2=c_3=0$。这也说明结论对每一个实数 $k$ 都成立。

## 三、解答题（原卷三～十二，习题册11～20）

**11.（2002.11；原卷三）切线：$y-x=(5-3\sqrt3)/4$；法线：$x+y=(\sqrt3-1)/4$。**

**第一步：写出参数方程，确定曲线点。**

由 $r=1-\cos\theta$，有 $x=r\cos\theta$、$y=r\sin\theta$。取 $\theta_0=\pi/6$，利用 $\cos\theta_0=\sqrt3/2$、$\sin\theta_0=1/2$，得

$$
x_0=\left(1-\frac{\sqrt3}{2}\right)\frac{\sqrt3}{2}
=\frac{2\sqrt3-3}{4},
\qquad
y_0=\left(1-\frac{\sqrt3}{2}\right)\frac12
=\frac{2-\sqrt3}{4}.
$$

**第二步：对参数求导，求切线斜率。**

$r'=\sin\theta$。按乘积法则，$x'=r'\cos\theta-r\sin\theta$、$y'=r'\sin\theta+r\cos\theta$，所以

$$
\begin{aligned}
x'(\theta_0)&=\frac12\frac{\sqrt3}{2}-\left(1-\frac{\sqrt3}{2}\right)\frac12
=\frac{\sqrt3-1}{2},\\
y'(\theta_0)&=\frac14+\left(1-\frac{\sqrt3}{2}\right)\frac{\sqrt3}{2}
=\frac{\sqrt3-1}{2}.
\end{aligned}
$$

两个导数均不为零，曲线点正则，且切线不竖直。因此切线斜率为 $k_T=y'/x'=1$，法线斜率为其负倒数 $k_N=-1$。

**第三步：代入点斜式，整理直角坐标方程。**

切线满足 $y-y_0=x-x_0$，法线满足 $y-y_0=-(x-x_0)$，故

$$
\begin{aligned}
\text{切线：}\quad y-x&=y_0-x_0=\boxed{\frac{5-3\sqrt3}{4}},\\
\text{法线：}\quad x+y&=x_0+y_0=\boxed{\frac{\sqrt3-1}{4}}.
\end{aligned}
$$

两条直线都经过 $(x_0,y_0)$，斜率乘积为 $-1$，因此相互垂直。极坐标中不能直接把 $\mathrm dr/\mathrm d\theta$ 当成直角坐标系中的切线斜率。

**12.（2002.12；原卷四）变上限积分的分段表达式。**

**第一段：$-1\le x\le0$，直接积分。**

积分区间完全位于 $f$ 的第一段定义域，所以

$$
F(x)=\int_{-1}^x\left(2t+\frac32t^2\right)\mathrm dt
=\left[t^2+\frac12t^3\right]_{-1}^x
=x^2+\frac12x^3-\frac12.
$$

特别地，$F(0)=-1/2$，这是计算下一段时必须保留的累计值。

**第二段：$0\le x\le1$，先在0处分拆积分。**

$$
F(x)=\int_{-1}^{0}f(t)\,\mathrm dt+\int_0^x f(t)\,\mathrm dt
=-\frac12+\int_0^x\frac{t\mathrm e^t}{(1+\mathrm e^t)^2}\,\mathrm dt.
$$

观察到 $[\mathrm e^t/(1+\mathrm e^t)]'=\mathrm e^t/(1+\mathrm e^t)^2$。分部积分时取 $u=t$、$v=\mathrm e^t/(1+\mathrm e^t)$，得

$$
\begin{aligned}
\int_0^x\frac{t\mathrm e^t}{(1+\mathrm e^t)^2}\,\mathrm dt
&=\left[\frac{t\mathrm e^t}{1+\mathrm e^t}\right]_0^x
-\int_0^x\frac{\mathrm e^t}{1+\mathrm e^t}\,\mathrm dt\\
&=\frac{x\mathrm e^x}{1+\mathrm e^x}-\ln(1+\mathrm e^x)+\ln2.
\end{aligned}
$$

其中对数积分来自换元 $u=1+\mathrm e^t$，下限 $t=0$ 对应 $u=2$。因此完整答案为

$$
\boxed{F(x)=\begin{cases}
x^2+\dfrac12x^3-\dfrac12,&-1\le x\le0,\\[6pt]
\dfrac{x\mathrm e^x}{1+\mathrm e^x}-\ln(1+\mathrm e^x)+\ln2-\dfrac12,&0\le x\le1.
\end{cases}}
$$

检验：两段在0处均为 $-1/2$，所以拼接连续；各段求导分别恢复题中的 $f(x)$，且 $F(-1)=0$。第二段若遗漏第一段贡献的 $-1/2$，即使导数正确，也不是题目指定的变上限积分。

**13.（2002.13；原卷五）答案：$f(x)=\mathrm e^{-1/x}$，$x>0$。**

**第一步：取对数，把幂式极限化成导数。**

固定 $x>0$。当 $h$ 充分接近0时，$x+hx=x(1+h)>0$；又 $f$ 恒正，所以题中的比值及其对数都有定义。由于原极限为正数 $\mathrm e^{1/x}$，由对数的连续性得

$$
\lim_{h\to0}\frac{\ln f(x+hx)-\ln f(x)}h=\frac1x.
$$

令内层自变量的增量 $u=hx$。固定的 $x$ 不随 $h$ 改变，因此

$$
\lim_{h\to0}\frac{\ln f(x+hx)-\ln f(x)}h
=x\lim_{u\to0}\frac{\ln f(x+u)-\ln f(x)}u
=x\frac{f'(x)}{f(x)}.
$$

这一步中的因子 $x$ 来自 $u/h=x$。于是得到微分方程 $f'(x)/f(x)=1/x^2$。

**第二步：积分并使用无穷远条件。**

因为 $f>0$，有 $(\ln f)'=f'/f$，积分得到

$$
\ln f(x)=-\frac1x+C,
\qquad f(x)=\mathrm e^C\mathrm e^{-1/x}.
$$

当 $x\to+\infty$ 时，题设 $f(x)\to1$，而 $\mathrm e^{-1/x}\to1$，所以 $\mathrm e^C=1$、$C=0$。因此 $\boxed{f(x)=\mathrm e^{-1/x}}$。

**第三步：代回原极限，验证所求函数。**

$$
\frac1h\ln\frac{f(x+hx)}{f(x)}
=\frac1h\left(-\frac1{x(1+h)}+\frac1x\right)
=\frac1{x(1+h)}\longrightarrow\frac1x.
$$

取指数后恢复 $\mathrm e^{1/x}$。所求函数在正半轴可导、恒正，且在无穷远趋于1，全部条件均满足。

**14.（2002.14；原卷六）答案：$y=x-\dfrac{75}{124}x^2$，最小体积为 $\dfrac{97\pi}{1488}$。**

**第一步：求微分方程的解族。**

在 $[1,2]$ 上可除以 $x$，将原方程写为 $y'-(2/x)y=-1$。积分因子为 $\mathrm e^{\int-2/x\,\mathrm dx}=x^{-2}$，所以

$$
\left(\frac y{x^2}\right)'=-\frac1{x^2}
\quad\Longrightarrow\quad
\frac y{x^2}=\frac1x+C
\quad\Longrightarrow\quad y=x+Cx^2.
$$

**第二步：用参数 $C$ 表示旋转体积。**

曲线可以在 $x$ 轴上方，也可以在下方。固定 $x$ 后，曲线与 $x$ 轴之间的线段绕轴旋转，圆盘半径为 $|y(x)|$，截面积为 $\pi y(x)^2$。因此即使曲线穿过 $x$ 轴，仍有

$$
\begin{aligned}
V(C)&=\pi\int_1^2(x+Cx^2)^2\,\mathrm dx\\
&=\pi\left[\frac{x^3}{3}+\frac{Cx^4}{2}+\frac{C^2x^5}{5}\right]_1^2\\
&=\pi\left(\frac73+\frac{15}{2}C+\frac{31}{5}C^2\right).
\end{aligned}
$$

三个系数分别来自 $(2^3-1)/3$、$(2^4-1)/2$、$(2^5-1)/5$。

**第三步：最小化这个二次函数。**

$$
V'(C)=\pi\left(\frac{15}{2}+\frac{62}{5}C\right)=0
\quad\Longrightarrow\quad C=-\frac{75}{124}.
$$

又 $V''(C)=62\pi/5>0$，所以驻点为唯一全局最小值点。配方能同时得到最小值：

$$
V(C)=\pi\left[\frac{31}{5}\left(C+\frac{75}{124}\right)^2+\frac{97}{1488}\right].
$$

故所求曲线为 $\boxed{y=x-75x^2/124}$，$\boxed{V_{\min}=97\pi/1488}$。该曲线在 $x=124/75\in(1,2)$ 处穿过横轴，但体积积分已经使用半径的平方，无须因变号另改公式。

**15.（2002.15；原卷七）答案：$h=2\ \mathrm m$。**

**第一步：根据尺寸图写出水深与条带宽度。**

以抛物线顶点 $O$ 为原点，对称轴为纵轴，向上为正，长度以米计。图中 $A=(-1,1)$、$B=(1,1)$，所以抛物线为 $y=x^2$；矩形宽2、高 $h$，水面位于 $y=1+h$。

在高度 $y$ 处，水深为 $1+h-y$，压强为 $p(y)=\rho g(1+h-y)$。矩形内的水平条带宽为2；下部 $0\le y\le1$ 内，左右边界为 $x=\pm\sqrt y$，故条带宽为 $2\sqrt y$。

**第二步：分别积分计算两部分承受的水压力。**

矩形部分的压力为

$$
F_1=2\rho g\int_1^{1+h}(1+h-y)\,\mathrm dy
=\rho g h^2.
$$

它也等于矩形面积 $2h$ 乘以中心深度 $h/2$ 处的压强。下部抛物线弓形的压力为

$$
\begin{aligned}
F_2&=2\rho g\int_0^1(1+h-y)\sqrt y\,\mathrm dy\\
&=2\rho g\left[(1+h)\frac23y^{3/2}-\frac25y^{5/2}\right]_0^1\\
&=\rho g\left[\frac43(1+h)-\frac45\right]
=\rho g\left(\frac{4h}{3}+\frac8{15}\right).
\end{aligned}
$$

**第三步：利用压力比求高度，并排除负值。**

由 $F_1:F_2=5:4$，有 $4F_1=5F_2$。约去相同的正因子 $\rho g$，得到

$$
4h^2=5\left(\frac{4h}{3}+\frac8{15}\right)
\quad\Longleftrightarrow\quad
3h^2-5h-2=(3h+1)(h-2)=0.
$$

两根为 $h=2$、$h=-1/3$。高度必须为正，所以 $\boxed{h=2\ \mathrm m}$。此时 $F_1=4\rho g$、$F_2=(16/5)\rho g$，比值确为 $5:4$。

水压力取决于每条带所在的水深，不能直接用两部分面积之比代替压力之比。

**16.（2002.16；原卷八）答案：$\lim x_n=3/2$。**

**第一步：先证明递推一直有意义，并找到第二项后的统一范围。**

若 $0<x<3$，则 $x(3-x)>0$，且

$$
x(3-x)=\frac94-\left(x-\frac32\right)^2\le\frac94.
$$

所以 $0<\sqrt{x(3-x)}\le3/2$。由初值归纳可知所有项都为正，且从第二项起均落在 $(0,3/2]$。

**第二步：比较相邻两项。**

对 $n\ge2$，有

$$
x_{n+1}^2-x_n^2=x_n(3-2x_n)\ge0.
$$

两项都为正，所以 $x_{n+1}\ge x_n$。因此从第二项开始，数列单调不减，并以上界 $3/2$ 约束，必收敛。

第一项即使大于 $3/2$，也不影响尾部单调有界的结论。

**第三步：求极限并排除零根。**

设极限为 $L$。因为 $x_n\ge x_2>0$ 对所有 $n\ge2$ 成立，所以 $L\ge x_2>0$。

在递推式中取极限，得到

$$
L^2=L(3-L),\qquad L(2L-3)=0.
$$

零根已被严格正的下界排除，因此 $L=3/2$。

**17.（2002.17；原卷九）两个严格不等式的证明。**

**左侧不等式：用积分平均值给出更强的下界。**

在 $a<x<b$ 上，$1/x>1/b$，因此

$$
\frac{\ln b-\ln a}{b-a}
=\frac1{b-a}\int_a^b\frac{\mathrm dx}{x}>\frac1b.
$$

又因为

$$
\frac1b-\frac{2a}{a^2+b^2}
=\frac{(b-a)^2}{b(a^2+b^2)}>0,
$$

所以得左侧严格不等式。

**右侧不等式：用比例变量消去量纲。**

令 $t=\sqrt{b/a}>1$，则 $b=at^2$。待证右侧不等式等价于

$$
\frac{2\ln t}{a(t^2-1)}<\frac1{at},
$$

也就是 $2\ln t<t-1/t$。

构造 $H(t)=t-1/t-2\ln t$，有 $H(1)=0$，并且

$$
H'(t)=1+\frac1{t^2}-\frac2t=\frac{(t-1)^2}{t^2}>0,
\qquad t>1.
$$

所以 $H(t)>0$，右侧严格不等式成立。两边结合得到完整结论。

上述等价变形中，$a>0$、$t>1$，所以 $a(t^2-1)$、$at$ 均为正，乘除时不改变不等号方向。由 $H'(t)>0$ 及 $H(1)=0$，还可用中值定理写成 $H(t)=H'(\xi)(t-1)>0$，其中 $1<\xi<t$，从而保证得到的是严格不等式。

**18.（2002.18；原卷十）答案：唯一系数组为 $(3,-3,1)$。**

**第一步：将三个函数值统一展开到二阶。**

所求“比 $h^2$ 高阶”即 $R(h)/h^2\to0$。二阶连续可导足以使用带 $o(h^2)$ 余项的泰勒公式，无须三阶导数；对于固定 $j=1,2,3$，

$$
f(jh)=f(0)+jhf'(0)+\frac{j^2h^2}{2}f''(0)+o(h^2).
$$

因此题中线性组合减去 $f(0)$，等于

$$
\begin{aligned}
R(h)&=(\lambda_1+\lambda_2+\lambda_3-1)f(0)\\
&\quad+h(\lambda_1+2\lambda_2+3\lambda_3)f'(0)\\
&\quad+\frac{h^2}{2}(\lambda_1+4\lambda_2+9\lambda_3)f''(0)+o(h^2).
\end{aligned}
$$

**第二步：说明消去三个系数是必要条件。**

若 $R(h)=o(h^2)$，先令 $h\to0$，常数项必须为零；常数项消去后除以 $h$ 并取极限，一次项系数必须为零；再除以 $h^2$ 取极限，二次项系数也必须为零。

因为题设中 $f(0),f'(0),f''(0)$ 都不为零，所以恰好得到

$$
\begin{cases}
\lambda_1+\lambda_2+\lambda_3=1,\\
\lambda_1+2\lambda_2+3\lambda_3=0,\\
\lambda_1+4\lambda_2+9\lambda_3=0.
\end{cases}
$$

**第三步：解线性方程组，并验证充分性。**

第二式减第一式，得 $\lambda_2+2\lambda_3=-1$；第三式减第二式再除以2，得 $\lambda_2+3\lambda_3=0$。

相减得到 $\lambda_3=1$，继而 $\lambda_2=-3,\lambda_1=3$。

系数矩阵的范德蒙德行列式为 $(2-1)(3-1)(3-2)=2\ne0$，因此解唯一。

代入这组系数后，常数、一次、二次项全部消失，只剩固定三个 $o(h^2)$ 项的线性组合，仍为 $o(h^2)$。所以它不仅必要，而且确实满足题设。

**19.（2002.19；原卷十一）$A-2I$ 可逆，$A=\begin{pmatrix}0&2&0\\-1&-1&0\\0&0&-2\end{pmatrix}$。**

**（Ⅰ）从原矩阵等式证明可逆性。**

题设含 $A^{-1}$，所以 $A$ 可逆。在 $2A^{-1}B=B-4I$ 两边左乘 $A$，得到

$$
2B=AB-4A,
\qquad (A-2I)B=4A.
$$

对两边取行列式。$A$ 为三阶矩阵，常数4应产生 $4^3$ 倍，因此

$$
|A-2I|\,|B|=|4A|=4^3|A|\ne0.
$$

乘积非零说明每个因子都非零，特别是 $|A-2I|\ne0$，从而 $A-2I$ 可逆，同时也得到 $B$ 可逆。

**（Ⅱ）按矩阵乘法顺序解出 $A$。**

由 $2B=AB-4A$，提取左因子 $A$ 得 $A(B-4I)=2B$。给定矩阵满足

$$
B-4I=\begin{pmatrix}-3&-2&0\\1&-2&0\\0&0&-2\end{pmatrix},
\qquad
(B-4I)^{-1}=\begin{pmatrix}-1/4&1/4&0\\-1/8&-3/8&0\\0&0&-1/2\end{pmatrix}.
$$

求逆时，左上二阶块的行列式为 $(-3)(-2)-(-2)\cdot1=8$；交换主对角元、改变副对角元符号，再除以8。右下独立元素 $-2$ 的逆为 $-1/2$。

在 $A(B-4I)=2B$ 两边右乘 $(B-4I)^{-1}$，得

$$
A=2B(B-4I)^{-1}
=2\begin{pmatrix}0&1&0\\-1/2&-1/2&0\\0&0&-1\end{pmatrix}
=\boxed{\begin{pmatrix}0&2&0\\-1&-1&0\\0&0&-2\end{pmatrix}}.
$$

例如乘积的第 $(1,1)$ 元为 $-1/4+(-2)(-1/8)=0$，第 $(1,2)$ 元为 $1/4+(-2)(-3/8)=1$。结果满足 $\det A=-4\ne0$、$A(B-4I)=2B$，故可左乘 $A^{-1}$ 恢复原等式。

**20.（2002.20；原卷十二）答案：$\boldsymbol x=(0,3,0,1)^{\mathrm T}+t(1,-2,1,0)^{\mathrm T}$。**

**第一步：用同一组独立向量表达左右两边。**

已知 $\alpha_1=2\alpha_2-\alpha_3$，所以

$$
\beta=\alpha_1+\alpha_2+\alpha_3+\alpha_4
=3\alpha_2+\alpha_4.
$$

而

$$
A\boldsymbol x
=(2x_1+x_2)\alpha_2+(-x_1+x_3)\alpha_3+x_4\alpha_4.
$$

**第二步：比较独立向量的系数。**

因为 $\alpha_2,\alpha_3,\alpha_4$ 线性无关，左右相等当且仅当

$$
2x_1+x_2=3,\qquad -x_1+x_3=0,\qquad x_4=1.
$$

令 $x_1=t$，则 $x_2=3-2t,x_3=t,x_4=1$，于是得到所列通解。

**第三步：检验参数个数与两个向量。**

四个列向量的秩为3，因此齐次零空间维数为1，通解只含一个自由参数，数量正确。

特解 $(0,3,0,1)^{\mathrm T}$ 的像为 $3\alpha_2+\alpha_4=\beta$；齐次方向 $(1,-2,1,0)^{\mathrm T}$ 的像为 $\alpha_1-2\alpha_2+\alpha_3=0$。这也直接验证全部参数都给出解。

此外，由 $\beta=\alpha_1+\alpha_2+\alpha_3+\alpha_4$，也能直接选取特解 $(1,1,1,1)^{\mathrm T}$，写成

$$
\boldsymbol x=\begin{pmatrix}1\\1\\1\\1\end{pmatrix}
+s\begin{pmatrix}1\\-2\\1\\0\end{pmatrix},\qquad s\in\mathbb R.
$$

它与上式完全等价，只需令 $t=s+1$。特解可以有不同选法，但齐次方向及全部解的集合相同。
