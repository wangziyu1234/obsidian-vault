---
create: 2026-08-30
modify: 2026-09-09
tags: [知识点, 线性代数, 公式速查]
---

> 返回目录：[[线性代数公式速查]]

# 附录 二次型与正定判定速查

> [!abstract] 附录定位
> 二次型标准形、惯性定理、正定/负定/半定判定的**纯速查**。所有推导与例题见 [[06 线代第6讲 二次型]]。

## 二次型与矩阵表示

$$
f(x_1,\dots,x_n)=x^{T}Ax\qquad（A=A^{T}，\text{对称矩阵唯一确定}）
$$

> [!note] 二次型的核心：$A$ 必须是对称矩阵。非对称部分 $(A-A^{T})/2$ 在二次型中自动消去。

## 化标准形的方法

| 方法 | 保不保长度 | 变换矩阵 | 标准形系数 |
| :-- | :--: | :-- | :-- |
| **正交变换法** | 保 | 正交矩阵 $Q$（$Q^{-1}=Q^{T}$） | **特征值** |
| **配方法** | 不保 | 可逆矩阵 $C$（未必正交） | 不一定是特征值（但保持惯性指数） |
| **初等变换法** | 不保 | 对 $\binom{A}{E}$ 每做一次列变换就同步做相同的行变换，下方 $E$ 即 $C$ | 不一定是特征值（保持惯性指数） |

三法分工：要特征值走正交变换；只要标准形走配方；要变换阵或抽象题走初等变换法。完整例题见 [[06 线代第6讲 二次型]]。

**惯性定理**

> 不管用什么可逆线性变换化标准形，**正惯性指数 $p$ 和负惯性指数 $q$ 不变**。

- 标准形 $f=d_{1}z_{1}^{2}+\cdots+d_{n}z_{n}^{2}$ 中：$d_{i}>0$ 的个数 $=p$，$d_{i}<0$ 的个数 $=q$
- 规范形 $f=z_{1}^{2}+\cdots+z_{p}^{2}-z_{p+1}^{2}-\cdots-z_{p+q}^{2}$（系数化为 $\pm1$ 和 $0$）

## 正定判定（五大充要条件）

$f=x^{T}Ax$ 正定（$x\ne0 \Rightarrow f>0$）**⟺** 任取其一：

| # | 条件 | 说明 |
| :-- | :-- | :-- |
| 1 | 特征值全 $>0$ | 计算 $\lvert\lambda E-A\rvert$ |
| 2 | 顺序主子式全 $>0$ | $D_k>0$（$k=1,2,\dots,n$） |
| 3 | 正惯性指数 $p=n$ | 配方法 / 正交变换法求标准形 |
| 4 | 合同于 $E$：$C^{T}AC=E$（$C$ 可逆） | 即 $A=D^{T}D$（$D$ 可逆） |
| 5 | 配方法：$f=\sum(\text{一次式})^{2}$，各平方式独立 | 系数矩阵行列式 $\ne0$ |

**正定必要条件（快速排除）**

- $a_{ii}>0$（主对角元全正）
- $\lvert A\rvert>0$

**正定常用结论**

| 结论 | 说明 |
| :-- | :-- |
| $A^{-1}$ 正定 ⟺ $A$ 正定 | 特征值取倒数 |
| $A^{T}$ 正定 ⟺ $A$ 正定 | 对称阵 $A^T=A$ |
| $aE+A$ 正定 ⟺ $a>-\lambda_{\min}$ | 特征值 $a+\lambda_i$ 全正 |
| $A,B$ 正定 ⟹ $A+B$ 正定 | 但 $AB$ 一般不正定（除非可交换） |
| $kA$ 正定（$k>0$） | 特征值乘 $k$ |

**正定判定步骤**

**① 先证 $A=A^{T}$**（正定的前提是对称矩阵）→ **② 再用五法中任一判正定**

> [!warning] 高频易错
> 负惯性指数 $q=0$ 不充分：例如 $A=\operatorname{diag}(1,0)$，$p=1<2$，非零向量 $(0,1)^T$ 使二次型为0，故不正定。
> - $A=C^{T}C$ 需 $C$ **可逆**才正定（$C$ 不可逆只能半正定）
> - $A,B$ 正定且 $AB=BA$ ⟹ $AB$ 正定（需可交换条件）

## 负定与半定

| 类型 | 充要条件 |
| :-- | :-- |
| **负定**（$x\ne0 \Rightarrow f<0$） | 奇数阶顺序主子式 $<0$，偶数阶 $>0$（即 $-A$ 正定） |
| **半正定**（此表指非负但不正定） | 特征值全非负且至少一个0；所有主子式非负且 $\det A=0$；可写 $A=C^TC$，其中C为奇异实方阵 |
| **半负定** | $-A$ 半正定 |

## 合同判定

| 条件 | 说明 |
| :-- | :-- |
| 实对称：$A\cong B$ ⟺ 正负惯性指数相同 | 充要 |
| 实对称：$A\cong B$ ⟺ 特征值正负号分布相同 | 充要（不要求特征值相同） |

> **合同 ⟹ 等价**（$C^{T}AC=B$，$C$ 可逆 ⟹ $r(A)=r(B)$）；合同只在**对称矩阵**间讨论。