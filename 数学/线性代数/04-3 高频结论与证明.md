---
create: 2026-09-08
modify: 2026-09-08
tags: [知识点, 线性代数, 线性方程组]
---

> 返回：[[04 线代第4讲 线性方程组]] | 返回目录：[[线性代数公式速查]]

# 第4讲（三） 高频结论与证明

> [!abstract] 本讲定位
> 本页是第二遍复习用的结论页，不是入门起点。每条结论先说适用条件，再说明它怎样从“解与秩”得到。
> 想分清同解、矩阵等价和行组等价，直达 [[#同解与等价的关系|同解与等价的关系]]。

教材依据：[[27张宇基础30讲线代.pdf#page=124|书内 p.117 的类消去律]]、[[27张宇基础30讲线代.pdf#page=128|例4.3–4.4]]、[[27张宇基础30讲线代.pdf#page=131|例4.6–4.7]]、[[27张宇基础30讲线代.pdf#page=137|同解判据与例4.14–4.15]]。以下均在实数范围内讨论。

## ⚖️ 4.14 行满秩保证有解，列满秩保证至多一个解

设 $A$ 为 $m\times n$ 矩阵。

- **行满秩** $r(A)=m$：对每个 $b\in\mathbb R^m$，$Ax=b$ 都有解。因为增广矩阵也只有 $m$ 行，其秩不可能再增大。
- **列满秩** $r(A)=n$：$Ax=0$ 只有零解，所以 $Ax=b$ **若有解就唯一**；不能省掉“若有解”。

> [!derivation] 列满秩为什么能“消去左边的 $A$”？
> 若 $AB=AC$，则
>
> $$
> A(B-C)=O.
> $$
>
> $B-C$ 的每一列都满足 $Ax=0$；列满秩时只能是零列，故 $B=C$。
> 这不要求 $A$ 是方阵或可逆，但必须有 $r(A)=n$。

## 🧩 4.15 从矩阵乘积为零读出齐次解

设 $A$ 有 $n$ 列，且 $AB=O$，则 $B$ 的每一列都是 $Ax=0$ 的解。

> [!derivation] 由解空间大小得到秩不等式
> $Ax=0$ 最多能容纳 $n-r(A)$ 个线性无关的解向量，因此
>
> $$
> r(B)\le n-r(A).
> $$
>
> 即
>
> $$
> \boxed{r(A)+r(B)\le n}.
> $$

> [!note] 伴随矩阵的列怎样使用？
> $A$ 为 $n\ge2$ 阶方阵且 $|A|=0$ 时，由 $AA^*=O$ 可知，$A^*$ 的列都是 $Ax=0$ 的解。
> 若 $r(A)=n-1$，则 $r(A^*)=1$，而齐次基础解系只需一个向量，故取 $A^*$ 的**任意一列非零列**即可。
> **不是把 $A^*$ 的所有列一起当基础解系**：它们线性相关，且可能包含零列。
> 教材例4.4还反向利用 $A^*A=O$，把 $A$ 的列看成 $A^*x=0$ 的解。

## 🔄 4.16 转置乘积与必有解的方程组

> [!derivation] 实矩阵 $A$：$Ax=0$ 与 $A^TAx=0$ 同解
> 从 $Ax=0$ 左乘 $A^T$，立即得 $A^TAx=0$。
> 反过来，若 $A^TAx=0$，则
>
> $$
> x^TA^TAx=(Ax)^T(Ax)=\|Ax\|^2=0.
> $$
>
> 因而 $Ax=0$。两组未知量个数相同，又同解，所以
>
> $$
> \boxed{r(A^TA)=r(A)}.
> $$
>
> 对 $A^T$ 作同样处理，还得到 $r(AA^T)=r(A)$。

> [!example] ✏️ 教材例4.7：为什么 $A^TAx=A^Tb$ 总有解？
> 设 $A\in\mathbb R^{m\times n}$，$b\in\mathbb R^m$ 任意。增广矩阵可写成
>
> $$
> (A^TA\mid A^Tb)=A^T(A\mid b).
> $$

> [!derivation] 比较系数矩阵与增广矩阵的秩
> 一方面，添列不能让秩变小；另一方面，矩阵乘积的秩不超过任一因子的秩。因此
>
> $$
> \begin{aligned}
> r(A)&=r(A^TA)\\
> &\le r(A^TA\mid A^Tb)\\
> &\le r(A^T)=r(A).
> \end{aligned}
> $$
>
> 夹在两端的秩只能相等，故方程组必有解。
> $r(A)=n$ 时唯一；$r(A)<n$ 时无穷多解。

> [!note] 对称形式也成立，但不要混淆向量维数
> 把上式中的 $A$ 换成 $A^T$：对任意 $c\in\mathbb R^n$，$AA^Ty=Ac$ 必有解，其中 $y\in\mathbb R^m$。
> 这是一条推论；教材例4.7本身写的是 $A^TAx=A^Tb$。

## 🔗 4.17 解的包含与非齐次同解

### 同解与等价的关系

教材依据：[[27张宇基础30讲线代.pdf#page=77|书内 p.70：矩阵等价]]、[[27张宇基础30讲线代.pdf#page=105|p.98：向量组等价]]、[[27张宇基础30讲线代.pdf#page=137|p.130：齐次同解判据]]。下面把三处结论连起来，反例为按定义编写。

| 说的是什么 | 含义 | 与同解的关系 |
| :-- | :-- | :-- |
| **方程组同解** | 同一组未知量下，解集完全相同 | 解向量相同，不只是解的个数相同 |
| **矩阵等价** | 同型矩阵可经初等行、列变换互变，即 $B=PAQ$，$P,Q$ 可逆 | 只需 $r(A)=r(B)$，**不保证同解** |
| **行等价** | 同型矩阵只经初等行变换互变，即 $B=PA$，$P$ 可逆 | 齐次保同解；非齐次要连右端一起变 |
| **向量组等价** | 两组向量可互相线性表示 | 必须说明比较的是行组、列组，还是基础解系 |

“等价方程组”若指解集相同，就是同解；**不能据此把“系数矩阵等价”当作同解**。

### 齐次：同解等价于系数矩阵的行向量组等价

设 $A,B$ 都有 $n$ 列，行数可以不同。以下三个条件互相等价：

1. $Ax=0$ 与 $Bx=0$ 同解。
2. $A,B$ 的**行向量组等价**，即两组方程能互相由线性组合得到。
3. 叠加两组方程没有增加独立约束，即

$$
r(A)=r(B)=r\!\begin{pmatrix}A\\B\end{pmatrix}
$$

> [!derivation] 为什么必须比较“行组”？
> 一行系数对应一个方程。行组能互相线性表示，就表示两组方程的每一式都能由另一组组合出来，因此解集相同。
> 反过来，若同解，联立后的解集仍相同；未知量个数同为 $n$，由基础解系个数 $n-r$ 得上面的三秩相等，故行组互相表示。也可比较去掉全零行后的行最简形。

若 $A,B$ **还同型**，行组等价就相当于行等价，因而有

$$
Ax=0\text{ 与 }Bx=0\text{ 同解}\iff B=PA\quad(P\text{ 可逆})
$$

这进一步推出 $A,B$ 矩阵等价，但**反向不成立**。若行数不同，只说行组等价，不直接写两矩阵等价。

> [!note] 若“等价”指基础解系
> 两个齐次方程组都有非零解时，它们同解，当且仅当两组基础解系能互相线性表示，即**基础解系作为向量组等价**。它们表示的是同一批解方向，不要求基向量逐个相同；若两组都只有零解，也显然同解。

#### ✏️ 反例：矩阵等价，甚至列组等价，仍不一定同解

取

$$
A=\begin{pmatrix}1&0\\0&0\end{pmatrix}
$$

交换两列得到

$$
B=\begin{pmatrix}0&1\\0&0\end{pmatrix}
$$

两矩阵同型且秩均为 1，所以矩阵等价；列组也等价，都是一个非零列 $(1,0)^T$ 加一个零列。但原方程组分别要求 $x_1=0$、$x_2=0$：

$$
Ax=0\text{ 的通解为 }x=t\begin{pmatrix}0\\1\end{pmatrix}
$$

$$
Bx=0\text{ 的通解为 }x=t\begin{pmatrix}1\\0\end{pmatrix}
$$

它们不是同一个解集。问题在于**行组不等价**：叠加矩阵的秩为 2，而不是 1。列变换后怎样还原变量，见 [[04-4 行列变换、线性相关性与解的关系#🧮 4.22 解的关系：行变换同解，列变换要还原变量|同解与一一对应的区别]]。

### 非齐次：比较增广矩阵的行向量组

设 $Ax=b$ 与 $Bx=c$ 未知量个数相同，且**两者均有解**。同解当且仅当 $(A\mid b)$、$(B\mid c)$ 的**行向量组等价**，即

$$
\begin{aligned}
r(A\mid b)&=r(B\mid c)\\
&=r\!\begin{pmatrix}A&b\\B&c\end{pmatrix}
\end{aligned}
$$

操作上，比较两增广矩阵去掉全零行后的行最简形即可。若方程条数也相同，则等价于存在同一个可逆矩阵 $P$，同时满足

$$
(B\mid c)=P(A\mid b)
$$

也就是**系数和右端一起做行变换**，不能只比较 $A,B$。从解的结构看，同解还等价于“导出齐次方程组同解，且存在一个公共特解”。

> [!derivation] 为什么要保留右端？
> 取公共特解 $x^*$，其余解都由它加齐次解得到。若原两组同解，解之差组成的齐次解也相同，故系数行组等价。
> 此时一行 $a$ 对应的右端必为 $ax^*$；把系数行按同样系数组合，右端也随之组合。因此增广行组等价。反过来，增广行组互相表示，就能把完整方程互相组合，当然同解。

#### ✏️ 反例：系数相同、增广矩阵等价，仍不一定同解

方程 $x=1$ 与 $x=2$ 的系数矩阵相同，增广矩阵分别为

$$
(1\mid1)
$$

$$
(1\mid2)
$$

它们同型且秩均为 1，作为**矩阵**等价；但两行并非互为倍数，作为**行向量组**不等价，所以解不同。仅有齐次解相同还不够，特解也必须对应。

> [!warning] “两者均有解”不能省略
> 两组都无解时，解集虽同为空集，增广行组却未必等价。例如两组为 $0x=1,\ 0x=0$ 和 $x=0,\ 0x=1$，均无解，增广秩却分别为 1、2。
> 因此“同解推出矩阵等价”不能不加条件地套到非齐次方程组。

### 解的包含：只要求一边能表示另一边

$Bx=0$ 的每个解都满足 $Ax=0$，等价于叠加 $A$ 没有增加新约束：

$$
r\!\begin{pmatrix}A\\B\end{pmatrix}=r(B)
$$

也等价于 $A$ 的每一行都能由 $B$ 的行线性表示，即存在矩阵 $C$ 使 $A=CB$。**互相包含才是同解**，仅一个方向不够。

## 🧮 4.18 克拉默法则

只适用于 $A$ 为方阵且 $D=|A|\ne0$ 的情形，此时唯一解为

$$
x_i=\frac{D_i}{D}.
$$

$D_i$ 是把系数行列式的第 $i$ 列换成右端向量 $b$ 后的行列式。详见 [[01-3 克拉默法则与重要应用]]。

$D=0$ 时不能再用此公式，也不能直接判无解，应转而比较两个秩。

## 4.24 解题套路

1. **具体方程**：行化简 → 判相容 → 找自由变量 → 写特解与齐次方向 → 代回。
2. **含参方阵**：行列式找降秩候选值 → 每个候选值检查增广矩阵，不能跳过无解分支。
3. **抽象通解**：用已知关系构造解 → 验无关 → 验个数 $n-r(A)$ → 加一个特解。
4. **公共解**：联立；有现成通解时，先考虑代入以减少未知参数。
5. **同解**：证明互相包含；若一组只是多了几条方程，就检查旧通解是否自动满足新增方程。
6. **行列变换**：先分清“秩不变”“关系系数不变”和“同解”；若列变换后求得新变量的解，按记录的可逆矩阵还原。

> [!tip] 知识回链
> 行列变换、相关性和解的对应关系见 [[04-4 行列变换、线性相关性与解的关系]]；回总览可按 [[04 线代第4讲 线性方程组#🔎 知识点直达|知识点直达]] 查找。
> 秩与矩阵乘积见 [[02 线代第2讲 矩阵（矩阵与秩）]]；线性无关与等价向量组见 [[03 线代第3讲 向量组]]；纯公式速查见 [[附录 线性方程组解的结构速查]]。
