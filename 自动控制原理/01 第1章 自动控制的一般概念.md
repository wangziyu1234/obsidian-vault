---
create: 2026-07-25
modify: 2026-07-29
tags: [知识点, 自动控制原理, 公式速查]
---

> 返回目录：[[自动控制原理]]

# 第 1 章 自动控制的一般概念

> [!abstract] 本讲定位
> 自动控制的一般概念：反馈控制原理、开/闭环控制、基本要求（稳/准/快）与系统分类。

## 1.1 基本概念

- **反馈控制原理**：取被控量与给定量比较产生偏差，按偏差进行控制（按偏差调节）。
- **开环控制**：结构简单、无抗扰能力；**闭环控制**：抗扰、精度高，但存在稳定性问题。
- **复合控制**：反馈 + 前馈（按扰动或按输入补偿）。
- **对系统的基本要求**：**稳**（稳定性）、**准**（稳态精度）、**快**（动态性能）。
- **系统组成**：测量元件、比较元件、放大元件、执行元件、校正元件、给定元件。

![[自控-闭环负反馈框图.png|620]]
*闭环负反馈控制系统的方框图：按偏差 $e=r-b$ 调节，输出经 $H(s)$ 反馈*

---

## 1.2 系统分类

| 分类依据 | 类型 |
|:--|:--|
| 按给定量变化规律 | **恒值控制系统**（给定值恒定）、**随动系统**（跟踪任意变化输入）、**程序控制系统**（按预定规律变化） |
| 按系统特性 | **线性连续控制**（$n$ 阶常系数微分方程 $a_nc^{(n)}+\cdots+a_0c = b_mr^{(m)}+\cdots+b_0r$）、**线性离散控制**（差分方程描述）、**非线性控制**（含非线性元件的微分方程） |

其他分类：定常/时变、单变量/多变量、连续/离散采样。

---

## 1.3 典型外作用（常考点：四种输入函数的拉氏变换对）

在零初始条件下，系统响应均由以下四种典型输入定义：

| 名称 | 时域 $r(t)$ | 拉氏变换 $R(s)$ | 说明 |
|:--|:--|:--|:--|
| **单位阶跃** | $1(t) = \begin{cases}0, & t<0 \\ 1, & t\ge0\end{cases}$ | $\dfrac{1}{s}$ | 最常用，反映恒定给定或突变扰动 |
| **单位斜坡** | $t \cdot 1(t)$ | $\dfrac{1}{s^2}$ | 反映匀速跟踪性能 |
| **单位加速度** | $\dfrac{t^2}{2} \cdot 1(t)$ | $\dfrac{1}{s^3}$ | 反映匀加速跟踪性能 |
| **单位脉冲** | $\delta(t)$ | $1$ | 瞬间冲击，面积 = 1，理想模型 |

- 实际脉冲函数用宽度 $b$ 的矩形脉冲近似，$b$ 远小于系统时间常数时可近似为理想脉冲。
- 正弦函数 $\sin\omega t$ 的拉氏变换 $\dfrac{\omega}{s^2+\omega^2}$ 也是常用输入（用于频域分析）。
- 输入之间的微分/积分关系对应输出的同样关系（线性定常系统通性）。

---

## 1.4 常考概念辨析

| 概念 | 要点 |
|:--|:--|
| 稳/准/快 | 稳定性（稳）→ 极点在左半平面；稳态精度（准）→ 型别与增益；动态性能（快）→ 阻尼比与自然频率 |
| 反馈 vs 前馈 | 反馈按**偏差**调节（闭环），前馈按**扰动/输入**补偿（开环）；前馈不改变稳定性 |
| 线性 vs 非线性 | 线性系统满足叠加原理、可定义传递函数；非线性系统不满足叠加、不能用统一传递函数 |
| 恒值 vs 随动 | 恒值：给定恒定，扰动是主要矛盾；随动：输入变化，跟踪性能是主要矛盾 |
| 采样 vs 数字控制 | 采样系统 = 连续对象 + 采样开关；数字系统 = A/D + 数字计算机 + D/A |
