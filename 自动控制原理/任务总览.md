---
create: 2026-08-21
modify: 2026-08-21
tags: [任务, 汇总, 自动控制原理]
---

> 返回总目录：[[自动控制原理]]

# ✅ 任务总览

> [!abstract] 📋 本页用途
> 用 **Obsidian Tasks** 自动汇总全库所有任务（勾选清单），按文件分组、一键跳转到原处。未完成任务在上，已完成任务在下；`templates\` 里的模板占位任务已排除。
>
> 在任意任务行后可加元数据增强排序，如：
> - 截止日期：`📅 2026-08-25`
> - 优先级：`⏫`（高）`🔼`（中）`🔽`（低）
> - 计划开始：`🛫 2026-08-21`
> - 预计用时：`⏳ 2h`
>
> 当前任务主要来自 [[12 资料索引（OneDrive）]] 的复习清单。

---

## 🔴 未完成任务

````tasks
not done
not (path includes templates)
group by filename
sort by due
````

## ✅ 已完成任务

````tasks
done
not (path includes templates)
group by filename
sort by done reverse
limit 50
````

---

## 📌 说明

- 本页为**任务总索引**，勾选状态仍以原笔记为准（在任意处勾选都实时同步到这里）。
- 若只想看某科目（数学 / 英语），把上面 `not (path includes templates)` 改为 `path includes 数学` 或 `path includes 英语` 等。
- 本页本身不新增任务，只做聚合展示。
